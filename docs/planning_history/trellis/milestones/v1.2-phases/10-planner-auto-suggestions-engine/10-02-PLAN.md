---
phase: 10-planner-auto-suggestions-engine
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - app/src/screens/PlannerScreen.tsx
  - app/src/screens/SettingsScreen.tsx
autonomous: true
requirements: [PLANNER-01, PLANNER-02, PLANNER-03, PLANNER-05]
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "PODCAST_GENERATION_COMPLETED event triggers suggestion refresh via useDailyRefresh hook"
    - "Planner refresh toggle and time settings persist across app restarts via localStorage"
  artifacts:
    - path: app/src/screens/PlannerScreen.tsx
      provides: "Mounts useDailyRefresh hook so event-driven refresh is active"
      contains: "useDailyRefresh"
    - path: app/src/screens/SettingsScreen.tsx
      provides: "localStorage persistence for plannerRefreshEnabled and plannerRefreshTime"
      contains: "echolearn_planner_refresh"
  key_links:
    - from: "app/src/screens/PlannerScreen.tsx"
      to: "app/src/state/useDailyRefresh.ts"
      via: "import and call useDailyRefresh()"
      pattern: "useDailyRefresh\\(\\)"
    - from: "app/src/screens/SettingsScreen.tsx"
      to: "localStorage"
      via: "setItem/getItem for planner refresh settings"
      pattern: "localStorage\\.(setItem|getItem).*echolearn_planner_refresh"
---

<objective>
Close two verified gaps from Phase 10 verification:

1. Wire the orphaned `useDailyRefresh` hook into PlannerScreen so the PODCAST_GENERATION_COMPLETED event chain is live.
2. Persist `plannerRefreshEnabled` and `plannerRefreshTime` to localStorage so user preferences survive app restarts.

Purpose: Complete PLANNER-03 (daily auto-refresh logic) and fix a UX regression where planner settings reset on every app restart.
Output: Two modified screen files with gap fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-planner-auto-suggestions-engine/10-01-SUMMARY.md
@.planning/phases/10-planner-auto-suggestions-engine/10-VERIFICATION.md

@app/src/state/useDailyRefresh.ts
@app/src/screens/PlannerScreen.tsx
@app/src/screens/SettingsScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useDailyRefresh into PlannerScreen</name>
  <files>app/src/screens/PlannerScreen.tsx</files>
  <read_first>
    - app/src/screens/PlannerScreen.tsx (current imports and hook calls)
    - app/src/state/useDailyRefresh.ts (hook signature and return type)
  </read_first>
  <action>
Import and call the `useDailyRefresh` hook in PlannerScreen.tsx so the PODCAST_GENERATION_COMPLETED event subscription becomes active when the Planner screen is mounted.

1. Add import at the top of PlannerScreen.tsx:
   ```typescript
   import { useDailyRefresh } from '../state/useDailyRefresh';
   ```

2. Inside the PlannerScreen component function body (near the existing `usePlannerAutoGen()` call), add:
   ```typescript
   const { isRefreshing: isDailyRefreshing } = useDailyRefresh();
   ```

3. The hook's internal `useEffect` subscriptions handle the rest:
   - On mount: checks if 24h has elapsed since last refresh, triggers if needed
   - On PODCAST_GENERATION_COMPLETED event: triggers refresh with 5-min debounce

No other changes needed. The hook is self-contained; it calls `plannerAutoGenService.generateAndStoreSuggestions()` internally, which updates localStorage. The existing `usePlannerAutoGen` hook in PlannerScreen already reads from the same localStorage key and will pick up the refreshed moves on next render.
  </action>
  <verify>
    <automated>grep -n "useDailyRefresh" app/src/screens/PlannerScreen.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*useDailyRefresh.*from.*state/useDailyRefresh" app/src/screens/PlannerScreen.tsx returns exactly 1 match
    - grep "useDailyRefresh()" app/src/screens/PlannerScreen.tsx returns at least 1 match (the call site)
    - cd app && npx tsc --noEmit exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>useDailyRefresh hook is imported and called in PlannerScreen. PODCAST_GENERATION_COMPLETED event subscription is active when Planner screen is mounted. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Persist planner refresh settings to localStorage</name>
  <files>app/src/screens/SettingsScreen.tsx</files>
  <read_first>
    - app/src/screens/SettingsScreen.tsx (full file — find lines 134-135 for plannerRefreshEnabled/plannerRefreshTime state, and lines 897-903 for the UI controls)
  </read_first>
  <action>
Persist `plannerRefreshEnabled` and `plannerRefreshTime` to localStorage so they survive app restarts.

**localStorage keys:**
- `echolearn_planner_refresh_enabled` — stores `"true"` or `"false"`
- `echolearn_planner_refresh_time` — stores time string like `"08:00"`

**Changes to SettingsScreen.tsx:**

1. Update the `useState` initializers (lines 134-135) to read from localStorage with fallback defaults:
   ```typescript
   const [plannerRefreshEnabled, setPlannerRefreshEnabled] = useState(() => {
     const stored = localStorage.getItem('echolearn_planner_refresh_enabled');
     return stored !== null ? stored === 'true' : true;
   });
   const [plannerRefreshTime, setPlannerRefreshTime] = useState(() => {
     return localStorage.getItem('echolearn_planner_refresh_time') ?? '08:00';
   });
   ```

2. Create two wrapper functions that persist on change (place near the other `save*` helpers in the component):
   ```typescript
   const savePlannerRefreshEnabled = (value: boolean) => {
     setPlannerRefreshEnabled(value);
     localStorage.setItem('echolearn_planner_refresh_enabled', String(value));
   };
   const savePlannerRefreshTime = (value: string) => {
     setPlannerRefreshTime(value);
     localStorage.setItem('echolearn_planner_refresh_time', value);
   };
   ```

3. Update the UI controls (around line 897-903) to use the wrapper functions instead of the raw setters:
   - Change `onChange` for the toggle from `setPlannerRefreshEnabled` to `savePlannerRefreshEnabled`
   - Change `onChange` for the time input from `setPlannerRefreshTime` to `savePlannerRefreshTime`

Do NOT change any other settings or touch any other state variables.
  </action>
  <verify>
    <automated>grep -n "echolearn_planner_refresh" app/src/screens/SettingsScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "localStorage.getItem('echolearn_planner_refresh_enabled')" app/src/screens/SettingsScreen.tsx returns at least 1 match
    - grep "localStorage.getItem('echolearn_planner_refresh_time')" app/src/screens/SettingsScreen.tsx returns at least 1 match
    - grep "localStorage.setItem('echolearn_planner_refresh_enabled'" app/src/screens/SettingsScreen.tsx returns at least 1 match
    - grep "localStorage.setItem('echolearn_planner_refresh_time'" app/src/screens/SettingsScreen.tsx returns at least 1 match
    - grep "savePlannerRefreshEnabled" app/src/screens/SettingsScreen.tsx returns at least 2 matches (definition + usage)
    - grep "savePlannerRefreshTime" app/src/screens/SettingsScreen.tsx returns at least 2 matches (definition + usage)
    - cd app && npx tsc --noEmit exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>plannerRefreshEnabled and plannerRefreshTime are initialized from localStorage with fallback defaults. Changes are persisted immediately via wrapper functions. User preferences survive app restarts.</done>
</task>

</tasks>

<verification>
1. `grep -rn "useDailyRefresh" app/src/ --include="*.tsx"` returns at least 1 match in PlannerScreen.tsx (was 0 before)
2. `grep -n "echolearn_planner_refresh" app/src/screens/SettingsScreen.tsx` returns 4+ matches (2 getItem + 2 setItem)
3. `cd app && npx tsc --noEmit` passes with no errors
4. All existing tests still pass: `cd app && node --test tests/services/plannerAutoGen.test.mjs tests/services/suggestionScorer.test.mjs tests/services/trajectoryAnalyzer.test.mjs`
</verification>

<success_criteria>
- useDailyRefresh hook is mounted in PlannerScreen, making PODCAST_GENERATION_COMPLETED -> refresh wiring live
- Planner refresh toggle and time settings persist to and load from localStorage
- No regressions: TypeScript compiles, existing tests pass
- Both gaps from 10-VERIFICATION.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/10-planner-auto-suggestions-engine/10-02-SUMMARY.md`
</output>
