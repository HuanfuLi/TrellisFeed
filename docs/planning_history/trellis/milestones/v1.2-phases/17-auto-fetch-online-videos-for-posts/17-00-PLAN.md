---
phase: 17-auto-fetch-online-videos-for-posts
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/services/youtube.test.mjs
  - tests/services/concept-feed.test.mjs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-07, D-09, D-10]

must_haves:
  truths:
    - "Test scaffold exists before any implementation begins"
    - "Tests define expected behavior for youtube.service.ts methods"
    - "Tests define expected behavior for feed interleaving logic"
    - "Tests can run and fail (RED phase) before implementation"
  artifacts:
    - path: "tests/services/youtube.test.mjs"
      provides: "Unit test scaffold for youtubeService"
      contains: "searchVideos"
    - path: "tests/services/concept-feed.test.mjs"
      provides: "Unit test scaffold for concept-feed interleaving"
      contains: "interleaveVideoPosts"
  key_links:
    - from: "tests/services/youtube.test.mjs"
      to: "app/src/services/youtube.service.ts"
      via: "import youtubeService (will resolve after Plan 01)"
      pattern: "youtubeService"
    - from: "tests/services/concept-feed.test.mjs"
      to: "app/src/services/concept-feed.service.ts"
      via: "tests interleave logic (will resolve after Plan 02)"
      pattern: "interleaveVideoPosts"
---

<objective>
Create the test scaffolds for youtube.service.ts and concept-feed interleaving before any implementation begins.

Purpose: Nyquist compliance -- tests must exist at Wave 0 so Plans 01 and 02 can reference them in automated verify steps.
Output: tests/services/youtube.test.mjs with failing tests covering D-01, D-02, D-03, D-07, D-09, D-10. tests/services/concept-feed.test.mjs with failing tests covering D-04 interleaving.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-auto-fetch-online-videos-for-posts/17-CONTEXT.md
@.planning/phases/17-auto-fetch-online-videos-for-posts/17-RESEARCH.md
@.planning/phases/17-auto-fetch-online-videos-for-posts/17-VALIDATION.md

@app/src/types/index.ts
@app/src/services/mock/review.mock.ts
@app/src/providers/llm/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/services/youtube.test.mjs test scaffold</name>
  <files>tests/services/youtube.test.mjs</files>
  <action>
Create the directory `tests/services/` and the test file `tests/services/youtube.test.mjs` using Node.js built-in test runner (`node:test` and `node:assert`).

Since youtube.service.ts is a browser/Capacitor module with dependencies on localStorage, CapacitorHttp, fetch, and mockSettingsService, tests must mock these. Use the following approach:

**1. Create the test file** with mock-first design. The tests define expected behavior -- they will fail (RED) until Plan 01 implements the service. Structure:

```javascript
import { describe, it, mock, beforeEach } from 'node:test';
import assert from 'node:assert/strict';
```

**2. Since this is a browser module that cannot be directly imported in Node.js** (it uses CapacitorHttp, localStorage, etc.), the test file should:
- Define the expected interface shape and behavior as contract tests
- Mock `fetch` globally to capture YouTube API calls
- Test the interleave helper function directly (extract and export it or copy the logic)
- Test query grouping logic if extractable

**3. Write these test groups:**

**Group: "YouTube search query formation" (D-01, D-02)**
- Test: "groups due concepts into search queries" -- given a list of concept titles, verify they are grouped into batched queries (2-3 concepts per query string)
- Test: "constructs correct YouTube Data API v3 URL" -- verify URL includes `part=snippet`, `type=video`, `videoEmbeddable=true`, `key=`, and the encoded query

**Group: "Video post generation" (D-03, D-07)**
- Test: "generates 3 video posts on initial call" -- mock fetch to return YouTube search results, verify 3 DailyPost objects returned
- Test: "generates 4 video posts on pull-for-more" -- verify generateMoreVideoPosts returns 4 posts
- Test: "video posts have sourceType 'video'" -- verify each returned post has sourceType === 'video'
- Test: "video posts have videoMeta populated" -- verify videoMeta.videoId, videoMeta.channelTitle, videoMeta.thumbnailUrl exist

**Group: "Transcript summarization" (D-09, D-10)**
- Test: "calls chatCompletion with serviceName 'video-summary'" -- mock chatCompletion, verify serviceName in options
- Test: "truncates transcript to 4000 chars" -- provide a 10000 char transcript, verify the user message content is truncated
- Test: "falls back to title-based summary when no transcript" -- verify chatCompletion is called even when transcript is null

**Group: "Video post caching"**
- Test: "getCachedVideoPosts returns cached posts for today" -- mock localStorage with today's date, verify posts returned
- Test: "getCachedVideoPosts returns empty array for stale cache" -- mock localStorage with yesterday's date, verify empty array

**4. Since direct import may not work** in Node.js (ESM + browser APIs), use this pragmatic approach:
- For logic that can be tested in isolation (URL construction, query grouping, interleaving, caching), write pure function tests
- For integration behavior (full generateVideoPosts flow), write contract-style tests that document expected behavior and will be validated via `npx tsc --noEmit` + manual verification in later plans
- Mark integration tests with `{ todo: true }` if they cannot run without browser APIs, so they show as pending rather than erroring

The key requirement is that `node --test tests/services/youtube.test.mjs` runs without crashing and reports test results (passing, failing, or todo).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && mkdir -p tests/services && node --test tests/services/youtube.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>tests/services/youtube.test.mjs exists, runs via `node --test`, defines test cases for D-01, D-02, D-03, D-07, D-09, D-10. Tests are in RED state (failing or todo) awaiting implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/services/concept-feed.test.mjs test scaffold for interleaving (D-04)</name>
  <files>tests/services/concept-feed.test.mjs</files>
  <action>
Create `tests/services/concept-feed.test.mjs` using Node.js built-in test runner (`node:test` and `node:assert`). This test file covers D-04 (feed interleaving) behavior.

Since the interleave function will be internal to concept-feed.service.ts (a browser module), test the interleaving logic as a standalone pure function. Copy the expected interleave algorithm into the test file and validate its behavior directly.

**1. Define a local `interleaveVideoPosts` function** matching the expected implementation from Plan 02:
```javascript
function interleaveVideoPosts(aiPosts, videoPosts) {
  if (videoPosts.length === 0) return aiPosts;
  const result = [];
  let vIdx = 0;
  for (let i = 0; i < aiPosts.length; i++) {
    result.push(aiPosts[i]);
    if ((i + 1) % 2 === 0 && vIdx < videoPosts.length) {
      result.push(videoPosts[vIdx++]);
    }
  }
  while (vIdx < videoPosts.length) result.push(videoPosts[vIdx++]);
  return result;
}
```

**2. Write these test groups:**

**Group: "interleaveVideoPosts" (D-04)**
- Test: "returns only AI posts when no video posts" -- pass 4 AI posts and empty array, verify result equals AI posts
- Test: "inserts video post after every 2nd AI post" -- pass 6 AI posts and 3 video posts, verify video posts appear at indices 2, 5, 8
- Test: "appends remaining video posts at end" -- pass 2 AI posts and 5 video posts, verify 1 interleaved + 4 appended at end
- Test: "handles empty AI posts array" -- pass empty AI array and 3 video posts, verify result is the 3 video posts
- Test: "handles single AI post" -- pass 1 AI post and 2 video posts, verify AI post first then 2 video posts appended
- Test: "preserves post order within each group" -- verify AI posts maintain relative order, video posts maintain relative order

**3. Use simple mock post objects** for test data:
```javascript
function makePost(id, sourceType = 'recent') {
  return { id, sourceType, title: `Post ${id}` };
}
function makeVideoPost(id) {
  return { id: `video-${id}`, sourceType: 'video', title: `Video ${id}`, videoMeta: { videoId: id } };
}
```

**4. All tests should PASS immediately** since the function is defined locally. This validates the interleave algorithm contract. Plan 02 must implement the same logic in concept-feed.service.ts.

Mark a final todo test: "concept-feed.service.ts uses interleaveVideoPosts for getDailyPosts" with `{ todo: true }` -- this will be validated when Plan 02 runs.

The key requirement is that `node --test tests/services/concept-feed.test.mjs` runs without crashing and reports passing test results for the interleave logic.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && node --test tests/services/concept-feed.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>tests/services/concept-feed.test.mjs exists, runs via `node --test`, defines and validates interleaveVideoPosts algorithm for D-04. Pure function tests pass. One todo test awaits Plan 02 integration.</done>
</task>

</tasks>

<verification>
- `node --test tests/services/youtube.test.mjs` runs without crashing
- `node --test tests/services/concept-feed.test.mjs` runs without crashing
- Test file youtube.test.mjs covers all requirement IDs from VALIDATION.md: D-01, D-02, D-03, D-07, D-09, D-10
- Test file concept-feed.test.mjs covers D-04 interleaving behavior
- Tests define expected behavior contracts for youtubeService methods and interleave logic
</verification>

<success_criteria>
1. tests/services/youtube.test.mjs exists and runs via `node --test`
2. tests/services/concept-feed.test.mjs exists and runs via `node --test`
3. YouTube test groups cover: search query formation, video post generation, transcript summarization, caching
4. Concept-feed test groups cover: interleave algorithm edge cases (empty, single, overflow)
5. YouTube tests are in RED state (failing/todo) -- implementation comes in Plan 01
6. Concept-feed interleave tests PASS (pure function validated locally)
</success_criteria>

<output>
After completion, create `.planning/phases/17-auto-fetch-online-videos-for-posts/17-00-SUMMARY.md`
</output>
