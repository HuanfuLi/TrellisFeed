---
phase: 12-portal-navigation-rich-moves
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/src/lib/moveNavigator.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PLANNER-06
  - NAV-01
  - NAV-02
gap_closure: true
must_haves:
  truths:
    - "deepdive moves navigate to PostDetailScreen at /posts/:id"
    - "NAV-01 and NAV-02 are defined in REQUIREMENTS.md and can be traced from 12-PLAN.md"
  artifacts:
    - path: "app/src/lib/moveNavigator.ts"
      provides: "Fixed routing: deepdive→posts, buildMoveNavigationPath returns /posts/:id"
      exports: ["navigateToMove()", "buildMoveNavigationPath()", "getMoveDestination()"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical NAV-01 and NAV-02 requirement definitions"
      contains: "NAV-01, NAV-02"
  key_links:
    - from: "MOVE_TYPE_ROUTES.deepdive"
      to: "screen: 'posts'"
      via: "MOVE_TYPE_ROUTES constant"
      pattern: "deepdive.*posts"
    - from: "buildMoveNavigationPath"
      to: "'/posts/${linkedResource.id}'"
      via: "case 'posts' in switch statement"
      pattern: "case 'posts'"
---

<objective>
Close two verified gaps from Phase 12 verification:

1. deepdive moves currently route to /ask/:id (QuestionDetailScreen) instead of
   /posts/:id (PostDetailScreen). The MOVE_TYPE_ROUTES constant maps deepdive to
   screen 'ask', and buildMoveNavigationPath has no 'posts' case — so it falls
   through to the default null branch. This makes PostDetailScreen's
   parseMoveNavigationState integration permanently dead code.

2. NAV-01 and NAV-02 are referenced in 12-PLAN.md's requirements frontmatter but
   do not exist in .planning/REQUIREMENTS.md. They are orphaned IDs with no
   canonical definition.

Purpose: PLANNER-06 requires that suggestions link directly to Posts, Questions,
or Review sessions. The post→PostDetailScreen link is currently broken. Both
routing fix and requirement registration are needed for full phase closure.

Output: Fixed moveNavigator.ts with deepdive→posts routing, and REQUIREMENTS.md
extended with NAV-01/NAV-02 definitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-portal-navigation-rich-moves/12-01-SUMMARY.md
@app/src/lib/moveNavigator.ts
@app/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deepdive routing in moveNavigator.ts</name>
  <files>app/src/lib/moveNavigator.ts</files>
  <action>
Make two targeted edits to app/src/lib/moveNavigator.ts:

EDIT 1 — Change the deepdive entry in MOVE_TYPE_ROUTES (lines 19-22).

Current:
```typescript
  deepdive: {
    screen: 'ask', // Navigate to AskScreen for Q&A deep dive
    paramKey: 'questionId',
  },
```

Replace with:
```typescript
  deepdive: {
    screen: 'posts', // Navigate to PostDetailScreen for learning posts
    paramKey: 'postId',
  },
```

EDIT 2 — Add a 'posts' case to the switch statement in buildMoveNavigationPath,
inserting it between the 'ask' case and the 'podcast' case (around line 135).

Current switch body:
```typescript
    case 'ask':
      // QuestionDetailScreen uses :id URL param
      return `/ask/${linkedResource.id}`;

    case 'podcast':
```

Replace with:
```typescript
    case 'ask':
      // QuestionDetailScreen uses :id URL param
      return `/ask/${linkedResource.id}`;

    case 'posts':
      // PostDetailScreen uses :id URL param
      return `/posts/${linkedResource.id}`;

    case 'podcast':
```

No other changes. The getMoveDestination function already has screenLabels['posts']
= 'Post Detail' defined (line 202) — it becomes reachable as a side effect of
this fix and requires no edit.

The TypeScript `as const` on MOVE_TYPE_ROUTES means changing 'ask' to 'posts' in
the deepdive entry also changes the inferred type of route.screen. The switch
statement's case 'posts' branch must be added to avoid the default null path.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - MOVE_TYPE_ROUTES.deepdive.screen is 'posts' (not 'ask')
    - buildMoveNavigationPath switch has a 'posts' case returning '/posts/${linkedResource.id}'
    - npx tsc --noEmit exits 0 with no errors related to moveNavigator.ts
    - getMoveDestination('deepdive move') now returns 'Post Detail' (was 'Unknown' before this fix because 'posts' was unreachable)
  </done>
</task>

<task type="auto">
  <name>Task 2: Register NAV-01 and NAV-02 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add a new section to .planning/REQUIREMENTS.md registering NAV-01 and NAV-02.
Insert the new section after the "### Planner Intelligence (PLANNER)" block and
before the "### Milestone Card Variety (CARDS)" block — keeping the file
organized by feature area.

Add these lines in the "## Active Requirements" section, as a new subsection:

```markdown
### Navigation (NAV)
- [x] **NAV-01**: Move taps navigate to the screen matching the linkedResource type (review→ReviewScreen, post→PostDetailScreen, question→QuestionDetailScreen)
- [x] **NAV-02**: Navigation preserves history stack so back button returns to the originating screen (Planner or Explore)
```

Also add a corresponding summary line in the "## Requirements by Category" section
after the Planner Intelligence entry:

```markdown
### Navigation (2 requirements)
- NAV-01 through NAV-02: Move-type routing to correct target screens, history stack preservation
```

Use the exact checkbox format `- [x]` since Phase 12 implemented both. Do not
modify any existing requirement entries.
  </action>
  <verify>
    <automated>grep -n "NAV-01\|NAV-02" /Users/Code/EchoLearn/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - .planning/REQUIREMENTS.md contains NAV-01 definition with [x] checked state
    - .planning/REQUIREMENTS.md contains NAV-02 definition with [x] checked state
    - Both entries follow the existing `- [x] **ID**: description` format
    - grep returns at least 2 matches (one per ID) in REQUIREMENTS.md
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm build passes</name>
  <files></files>
  <action>
Run the full TypeScript check and build to confirm no regressions were introduced
by the moveNavigator.ts edit.

Step 1 — TypeScript check (must pass before proceeding to build):
```
cd /Users/Code/EchoLearn/app && npx tsc --noEmit
```

Step 2 — If tsc exits 0, run the Vite build:
```
cd /Users/Code/EchoLearn/app && npm run build
```

If tsc reports errors: diagnose before running build. The most likely error is a
type narrowing issue in buildMoveNavigationPath if the MOVE_TYPE_ROUTES as const
type now includes 'posts' as a possible route.screen value and the switch is
exhausted — but adding the 'posts' case in Task 1 addresses this. If a TS error
does appear, fix it in moveNavigator.ts before completing this task.

Do not suppress errors with @ts-ignore or any-casting. Fix the root cause.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - npx tsc --noEmit exits 0
    - npm run build completes without errors
    - No TypeScript diagnostics related to moveNavigator.ts or move routing
  </done>
</task>

</tasks>

<verification>
After all tasks complete, confirm:

1. Routing fix is in place:
   grep -n "screen: 'posts'" /Users/Code/EchoLearn/app/src/lib/moveNavigator.ts
   → Should match the deepdive entry and the 'posts' case in buildMoveNavigationPath

2. NAV requirements are registered:
   grep -c "NAV-0" /Users/Code/EchoLearn/.planning/REQUIREMENTS.md
   → Should return 2 (NAV-01 and NAV-02)

3. No type errors introduced:
   cd /Users/Code/EchoLearn/app && npx tsc --noEmit
   → Exit 0

The gap in truth #6 ("All move types route to correct screen") is closed when
deepdive moves use screen 'posts' and buildMoveNavigationPath returns
/posts/${linkedResource.id}. PostDetailScreen's parseMoveNavigationState
integration — previously dead code — becomes reachable.
</verification>

<success_criteria>
- MOVE_TYPE_ROUTES.deepdive.screen === 'posts' in moveNavigator.ts
- buildMoveNavigationPath switch includes case 'posts' returning '/posts/${id}'
- NAV-01 and NAV-02 exist in .planning/REQUIREMENTS.md with [x] checked
- TypeScript compiles with 0 errors
- Vite build succeeds
- VERIFICATION.md gap #1 (truth #6 failed) is addressed by the routing fix
- VERIFICATION.md gap #2 (orphaned NAV-01/NAV-02) is addressed by requirement registration
</success_criteria>

<output>
After completion, create `.planning/phases/12-portal-navigation-rich-moves/12-02-SUMMARY.md`

Summary must document:
- Which lines in moveNavigator.ts were changed (line numbers for MOVE_TYPE_ROUTES deepdive entry and the new 'posts' case)
- That NAV-01 and NAV-02 are now defined in REQUIREMENTS.md
- Build status (tsc + npm run build exit codes)
- Confirmation that PostDetailScreen is now reachable from deepdive moves
</output>
