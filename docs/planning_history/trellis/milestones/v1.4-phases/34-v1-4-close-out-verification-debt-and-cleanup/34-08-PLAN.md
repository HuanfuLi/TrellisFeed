---
phase: 34-v1-4-close-out-verification-debt-and-cleanup
plan: 08
type: execute
wave: 5
depends_on:
  - 34-01
  - 34-02
  - 34-03
  - 34-04
  - 34-05
  - 34-06
  - 34-07
files_modified:
  - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md
  - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
  - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md
  - .gitignore
autonomous: false
requirements:
  - DEVICE-UAT-RETEST
  - WIP-COMMIT-SHAPE
must_haves:
  truths:
    - "34-UAT-LOG.md exists with 5 mandatory rows: G2/G4/G5 retroactive PASS (citing 32.1-HUMAN-UAT.md HuanfuLi 2026-04-19) + 33-UAT-1/2 device pending OR pass per operator session"
    - "32.1-VERIFICATION.md status flips from human_needed → passed (after operator confirmation that G2/G4/G5 evidence in 32.1-HUMAN-UAT.md is sufficient)"
    - ".gitignore contains a Presentation/ entry"
    - "33-REBRAND.md gains a 'Functional Changes' addendum section listing the 8 functional follow-ons (truncation-tolerant JSON parser, ReviewScreen dedup, style weights, REFILL_THRESHOLD, enqueueInterleaved, image pre-gen move, in-flight image dedupe, InfoFlow div role)"
    - "5 commits land on branch gsd/phase-33-hygiene-and-polish in the order: rebrand → functional+Seam12 → audit → planning → execution"
    - "Test baseline preserved at 449 pass / 27 fail OR BETTER after each commit boundary (D-14)"
    - "tsc -b --noEmit exits 0 after each commit"
    - "Working tree is clean after Commit 5 except for any new Phase 34 SUMMARY/VERIFICATION/UAT-LOG files staged for next iteration"
  artifacts:
    - path: ".planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md"
      provides: "Device UAT log with 5 mandatory rows + Phase 28 opportunistic rows"
      min_lines: 50
    - path: ".gitignore"
      provides: "Presentation/ entry"
      contains: "Presentation/"
    - path: ".planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md"
      provides: "Rebrand doc + new Functional Changes addendum"
      contains: "Functional Changes"
  key_links:
    - from: "34-UAT-LOG.md G2/G4/G5 retroactive rows"
      to: ".planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md (HuanfuLi 2026-04-19)"
      via: "explicit source citation in each row"
      pattern: "32\\.1-HUMAN-UAT\\.md"
    - from: "32.1-VERIFICATION.md status: passed"
      to: "34-UAT-LOG.md retroactive PASS rows"
      via: "human_verification block flip"
      pattern: "status: passed"
---

<objective>
Final phase plan — combines:

1. **Device UAT log:** Record all 5 mandatory device retest items in `34-UAT-LOG.md` (G2/G4/G5 retroactive PASS per 34-RESEARCH.md Q8 finding that 32.1-HUMAN-UAT.md already records them as PASS by HuanfuLi 2026-04-19; 33-HUMAN-UAT-1/2 marked `[pending]` UNTIL operator runs them on the next APK deploy).
2. **32.1-VERIFICATION.md status flip:** `human_needed` → `passed`, citing the 34-UAT-LOG.md retroactive rows.
3. **33-REBRAND.md addendum:** Append a "Functional Changes" section enumerating the 8 functional follow-ons (per 34-CONTEXT.md D-11) so Commit 2 has a complete narrative.
4. **`.gitignore` update:** Add `Presentation/` entry per D-12.
5. **5-commit WIP land** on branch `gsd/phase-33-hygiene-and-polish` (per D-13) using `gsd-tools commit`. Tests green at every boundary (D-14).

Purpose: Closes DEVICE-UAT-RETEST + WIP-COMMIT-SHAPE audit gaps. After this plan, `git status` is clean (or carries only Phase 34 SUMMARY iteration files), and v1.4 milestone is shippable.

**This plan has a `checkpoint:human-verify` step before the 5-commit land** to give the operator one last review. Per D-08, device UAT happens BEFORE WIP commits — but per 34-RESEARCH.md Q8, G2/G4/G5 are already retroactively-PASS, and 33-HUMAN-UAT-1/2 require an APK deploy which can happen post-commit (the touch-target + React.memo work is already in the WIP being committed). So the checkpoint here is a final commit-shape review, not a device-session gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-VALIDATION.md
@.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md
@.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md
@.planning/v1.4-INTEGRATION-CHECK-v2.md

<commit_shape_table>
**Commit 1: Rebrand only** — `feat(brand): rebrand EchoLearn to Trellis (UI strings, configs, icons, console prefixes)`

Modified files (12):
- CLAUDE.md (console prefix comment notes only — verify before staging that no functional CLAUDE.md edits leak in)
- app/capacitor.config.ts
- app/index.html
- app/ios/App/App/Assets.xcassets/AppIcon-512@2x.png
- app/ios/App/App/Info.plist
- app/src/locales/en.json
- app/src/locales/es.json
- app/src/locales/ja.json
- app/src/locales/zh.json
- app/src/screens/GraphScreen.tsx (theme name string only)
- app/src/services/podcast.service.ts (console prefix only)
- app/src/services/question-filter.service.ts (console prefix only)
- app/src/services/question.service.ts (console prefix only)
- app/src/services/scheduler.native.ts (console prefix only)
- app/src/state/useQuestions.ts (console prefix only)

Untracked files staged into Commit 1 (7):
- Assets/Trellis_logo.png
- app/public/apple-touch-icon.png
- app/public/favicon-16x16.png
- app/public/favicon-32x32.png
- app/public/pwa-192x192.png
- app/public/pwa-512x512.png
- .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md (with Functional Changes addendum from this plan's Task 3)

Total: ~22 files.

**Commit 2: Functional follow-on with Seam 12 fix** — `refactor(feed+review): JSON parser hardening, image pre-gen architecture shift, queue tuning, ReviewScreen dedup`

Modified files (13):
- app/src/components/InfoFlow.tsx (div role refactor — POST-Plan-34-06; the Plan 34-06 deletion of ImmersiveInfoFlow stages alongside the div role refactor)
- app/src/screens/HomeScreen.tsx (image pregen move out of handleLoad)
- app/src/screens/ReviewScreen.tsx (flashcard dedup)
- app/src/screens/settings/SettingsDataScreen.tsx
- app/src/services/canonical-knowledge.service.ts
- app/src/services/concept-feed.service.ts (truncation-tolerant JSON parser + image pre-gen to refillQueue)
- app/src/services/imageGeneration.service.ts (in-flight dedupe)
- app/src/services/post-queue.service.ts (REFILL_THRESHOLD 8→12, enqueueInterleaved)
- app/src/services/style-assignment.ts (text-art 40→55%, news 20→10%)
- app/tests/services/image-gen-key-gate.test.mjs
- app/tests/services/post-queue.test.mjs
- app/tests/services/style-assignment.test.mjs
- **app/tests/screens/HomeScreen.image-pregen-filter.test.mjs (Seam 12 fix from Plan 34-02)**

Files DELETED in Commit 2 (from Plan 34-06):
- app/src/services/post-store.service.ts (deleted)

(Note: Plan 34-06 deletion of ImmersiveInfoFlow inside InfoFlow.tsx and the div role refactor BOTH live in InfoFlow.tsx. Stage them together in Commit 2.)

**Commit 3: Audit artifacts** — `docs(audit): v1.4 milestone audit + integration check v2 (post-32.1+33 re-audit)`

Files (2):
- .planning/v1.4-MILESTONE-AUDIT.md (untracked or modified)
- .planning/v1.4-INTEGRATION-CHECK-v2.md (untracked)

**Commit 4: Phase 34 planning context** — already committed (HEAD `c24a875f` from discuss-phase + this plan extends with Plan 34-08 execution items).

If 34-CONTEXT.md, 34-DISCUSSION-LOG.md, 34-RESEARCH.md, 34-VALIDATION.md, STATE.md are still untracked/modified at this point, fold them into Commit 4 as `docs(planning): phase 34 planning context` — but typically these have already landed via Plans 34-03..07 or via the prior discuss-phase commit.

**Commit 5: Phase 34 execution artifacts** — `docs(phase-34): close v1.4 — verification debt + Seam 11/12 fixes + orphan cleanup + UAT log + WIP land`

Incremental files added during execute-phase (this plan and prior plans' SUMMARY/VERIFICATION/code edits):
- All 8 plan PLAN.md files
- All 8 plan SUMMARY.md files (created at end of each plan execution)
- 30-VERIFICATION.md (Plan 34-03)
- 31-VERIFICATION.md (Plan 34-04)
- 32-CLOSURE.md (Plan 34-05)
- 32-VALIDATION.md annotation (Plan 34-05)
- Test fixes from Plans 34-01 (trellis-replant + trellis-prune)
- 28/29/30-VALIDATION.md flips (Plan 34-07)
- 34-UAT-LOG.md (this plan)
- 32.1-VERIFICATION.md status flip (this plan)
- .gitignore (this plan)
- This plan's 34-08-SUMMARY.md
- 34-VERIFICATION.md (created by /gsd:verify-work after this plan)

In practice many of these will already be in the working tree as separate iterations. The Commit 5 step here is the final tidy commit that catches anything still uncommitted.
</commit_shape_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 34-UAT-LOG.md with retroactive G2/G4/G5 PASS + pending 33-UAT-1/2 + opportunistic Phase 28 rows</name>
  <files>.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md</files>
  <read_first>
    - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md (G2/G4/G5 PASS evidence by HuanfuLi 2026-04-19)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VERIFICATION.md (33-HUMAN-UAT-1/2 device pending items)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-VERIFICATION.md (6 deferred Phase 28 human-UAT items per D-09)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q8 — UAT log structure template)
  </read_first>
  <action>
    Create `.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md` with this structure:

    ```markdown
    ---
    status: partial
    phase: 34-v1-4-close-out-verification-debt-and-cleanup
    started: 2026-04-25
    updated: 2026-04-25
    pass_count: 3
    pending_count: 2
    skip_count: 0
    ---

    # Phase 34 — Device UAT Log

    Records device UAT outcomes for the 5 mandatory items (32.1-G2/G4/G5 + 33-HUMAN-UAT-1/2) plus opportunistic Phase 28 deferred items (per D-09).

    Per 34-RESEARCH.md Q8: G2/G4/G5 are RETROACTIVELY PASS — the evidence already exists in `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md` (verified by HuanfuLi 2026-04-19). 33-HUMAN-UAT-1/2 require fresh APK deploy and remain `[pending]`.

    ## Mandatory Tests

    ### G2 — Video touch overlay reaches YouTube native controls
    - source: 32.1-HUMAN-UAT.md test 1
    - status: PASS (retroactive)
    - result: pass
    - verified_by: HuanfuLi 2026-04-19
    - note: Recorded in 32.1-HUMAN-UAT.md before this log was created. No re-test required — same code base; no regression since 32.1.
    - evidence: `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md` test 1 PASS row

    ### G4 — Starter posts persist after viewing one
    - source: 32.1-HUMAN-UAT.md test 2
    - status: PASS (retroactive)
    - result: pass
    - verified_by: HuanfuLi 2026-04-19
    - note: Recorded in 32.1-HUMAN-UAT.md before this log was created.
    - evidence: `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md` test 2 PASS row

    ### G5 — Clear All Data auto-navigates to /home
    - source: 32.1-HUMAN-UAT.md test 3
    - status: PASS (retroactive)
    - result: pass
    - verified_by: HuanfuLi 2026-04-19
    - note: Recorded in 32.1-HUMAN-UAT.md before this log was created.
    - evidence: `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md` test 3 PASS row

    ### 33-UAT-1 — Touch target feel on Planner refresh + ChatInput mic/globe (44×44px)
    - source: 33-CONTEXT.md D-24/D-25; CLAUDE.md "ChatInput flex shrink" + "WCAG 2.5.8" invariants
    - status: PENDING
    - result: [pending — next APK deploy]
    - expected: 44px targets feel tappable on Android without adjacent tap errors. ChatInput mic + globe + Send all reachable; Planner refresh button has comfortable target size.
    - test: Open Planner → tap refresh; open Ask → tap mic, tap globe, tap Send. Record "comfortable / cramped / mis-tap" per button.

    ### 33-UAT-2 — React.memo behavioral correctness on live feed
    - source: 33-CONTEXT.md D-22/D-23; Phase 33 plan 33-06
    - status: PENDING
    - result: [pending — next APK deploy]
    - expected: Custom equality comparators on ConceptCard + VineProgress do not produce stale renders. Concepts update correctly across explored/unexplored transitions.
    - test: Load home feed (8 cards). Swipe-for-more (4 posts appended). Toggle Settings → Image Generation. Re-open home — VineProgress full-width? ConceptCards re-render correctly? Record any visual stale-state.

    ## Phase 28 Opportunistic Items (D-09 — record only if covered naturally during APK session)

    ### 28-UAT-OPP-1 — Haptic on BottomNav tap
    - status: SKIP (not covered)
    - result: [pending / skip]

    ### 28-UAT-OPP-2 — BottomNav slide-down on scroll
    - status: SKIP (not covered)

    ### 28-UAT-OPP-3 — Header scroll shadow
    - status: SKIP (not covered)

    ### 28-UAT-OPP-4 — SwipeTabContainer resize re-snap (keyboard open/close)
    - status: SKIP (not covered)

    ### 28-UAT-OPP-5 — Trellis pulse from Suggested Moves
    - status: SKIP (not covered)

    ### 28-UAT-OPP-6 — AskScreen locale switch empty state
    - status: SKIP (not covered)

    ## Sign-Off

    - [x] G2 retroactive PASS (HuanfuLi 2026-04-19)
    - [x] G4 retroactive PASS (HuanfuLi 2026-04-19)
    - [x] G5 retroactive PASS (HuanfuLi 2026-04-19)
    - [ ] 33-UAT-1 (next APK deploy)
    - [ ] 33-UAT-2 (next APK deploy)

    Phase 34 close-out is NOT gated on 33-UAT-1/2 (per 34-CONTEXT.md `<deferred>` and 34-RESEARCH.md risks table). Operator can re-open this log post-APK to record outcomes.

    ---
    *Created: 2026-04-25. Updated: 2026-04-25.*
    ```

    Save with `Write` tool. Length target: 60-100 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md && echo EXISTS</automated>
    Expected: `EXISTS`.

    <automated>grep -c "PASS (retroactive)" .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md</automated>
    Expected: `3` (G2, G4, G5).

    <automated>grep -c "verified_by: HuanfuLi 2026-04-19" .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md</automated>
    Expected: `3`.

    <automated>grep -c "result: \[pending" .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md</automated>
    Expected: `>= 2` (33-UAT-1 + 33-UAT-2).

    <automated>grep -c "32.1-HUMAN-UAT.md" .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md</automated>
    Expected: `>= 3` (one citation per G2/G4/G5).
  </verify>
  <acceptance_criteria>
    - File exists with frontmatter (status, phase, started, updated, pass_count, pending_count).
    - 5 mandatory test rows: G2 / G4 / G5 / 33-UAT-1 / 33-UAT-2.
    - G2/G4/G5 marked PASS (retroactive) with HuanfuLi 2026-04-19 attribution.
    - 33-UAT-1 and 33-UAT-2 marked `[pending]` with clear expected/test descriptions.
    - 6 Phase 28 opportunistic rows (D-09) — all marked SKIP unless operator covered them naturally.
    - Sign-Off checklist with 3 checked + 2 unchecked.
    - File length 60-100 lines.
  </acceptance_criteria>
  <done>
    34-UAT-LOG.md exists with all 5 mandatory rows + opportunistic rows + sign-off block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 32.1-VERIFICATION.md status from human_needed to passed</name>
  <files>.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md (read frontmatter + human_verification block)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-UAT-LOG.md (this plan's Task 1 output — confirm G2/G4/G5 retroactive PASS rows)
    - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md (source evidence)
  </read_first>
  <action>
    Edit `32.1-VERIFICATION.md` frontmatter:

    1. Change `status: human_needed` to `status: passed`.
    2. Update `score:` field from "5/5 must-haves verified at code level (3 require operator on-device retest)" to "5/5 must-haves verified; 3 device retests (G2/G4/G5) confirmed PASS by HuanfuLi 2026-04-19 in 32.1-HUMAN-UAT.md".
    3. In each of the 3 `human_verification:` block entries (G2/G4/G5), append:
       ```yaml
       result: pass
       verified_by: HuanfuLi 2026-04-19 (32.1-HUMAN-UAT.md, retroactive flip Phase 34 plan 34-08)
       ```

    Do NOT modify body content (truth tables, evidence sections, etc.).

    Save with `Write` tool.
  </action>
  <verify>
    <automated>grep -c "^status: passed" .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md</automated>
    Expected: `1`.

    <automated>grep -c "^status: human_needed" .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md</automated>
    Expected: `0`.

    <automated>grep -c "result: pass" .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md</automated>
    Expected: `>= 3` (G2/G4/G5 each get a result: pass annotation).

    <automated>grep -c "HuanfuLi 2026-04-19" .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md</automated>
    Expected: `>= 3`.
  </verify>
  <acceptance_criteria>
    - 32.1-VERIFICATION.md frontmatter has `status: passed`.
    - No `status: human_needed` remains.
    - human_verification block annotated with `result: pass` + `verified_by: HuanfuLi 2026-04-19` for G2/G4/G5.
    - Body content (truth tables) unchanged.
  </acceptance_criteria>
  <done>
    32.1-VERIFICATION.md flipped to passed; 3 human_verification entries annotated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append "Functional Changes" section to 33-REBRAND.md</name>
  <files>.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md</files>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md (read entire file — note this is currently UNTRACKED per git status; read by file path)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-11 — list of 8 functional follow-ons)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q5/Q6 commit file lists)
  </read_first>
  <action>
    Append a new section to `33-REBRAND.md` (after existing content):

    ```markdown

    ---

    ## Functional Changes Addendum (Phase 34 close-out, 2026-04-25)

    Per 34-CONTEXT.md D-11, the rebrand WIP carries the following functional improvements alongside the cosmetic rebrand. They are committed in **Commit 2** of the 5-commit shape (`refactor(feed+review): JSON parser hardening, image pre-gen architecture shift, queue tuning, ReviewScreen dedup`):

    1. **Truncation-tolerant JSON parser** in `concept-feed.service.ts` — defends against partial LLM streams returning incomplete JSON; falls back to recoverable parse instead of throwing.
    2. **ReviewScreen flashcard dedup (two-pass)** in `ReviewScreen.tsx` — eliminates duplicate flashcards in the daily review queue across sessions.
    3. **Style weight rebalance** in `style-assignment.ts` — text-art bumped 40 → 55%, news dropped 20 → 10% (`STYLE_WEIGHTS` const).
    4. **REFILL_THRESHOLD bumped 8 → 12** in `post-queue.service.ts` — earlier refill triggers prevent the queue from emptying mid-swipe while image-gen is in flight (CLAUDE.md "Numeric defaults" updated).
    5. **`enqueueInterleaved` method** added to `post-queue.service.ts` — interleaves news/connection cards alongside concept-driven posts.
    6. **Image pre-generation moved from `HomeScreen.handleLoad` to `concept-feed.service.ts:refillQueue`** — cycle-aware pre-gen so images are ready when the queue serves them. **Test guard updated by Plan 34-02** (`HomeScreen.image-pregen-filter.test.mjs` now reads `concept-feed.service.ts:refillQueue` instead of `HomeScreen.tsx:handleLoad`).
    7. **In-flight image dedupe** in `imageGeneration.service.ts` — second concurrent request for the same prompt key returns the first request's promise instead of starting a new generation.
    8. **InfoFlow div role refactor** in `InfoFlow.tsx` — accessibility improvements; explicit `role="button"` for tappable cards.

    ### Why bundled with rebrand

    Per 34-CONTEXT.md D-11: "Acceptable because they're behind code paths already audited (post-queue, concept-feed) and tests still pass." Test baseline before/after: 449 pass / 27 fail held (or improved post-Plan 34-01/02).

    ### Out of scope (deferred to v1.5)

    - Append-only derived list + persistent cycle position in concept-feed pipeline (CLAUDE.md "Concept Feed Generation Pipeline" gaps).
    - localStorage key migration (`echolearn_*` → `trellis_*`) — preserved intentionally to avoid disrupting existing user data.

    ---
    *Addendum created: 2026-04-25. Author: Phase 34 plan 34-08.*
    ```

    Save with `Write` tool.
  </action>
  <verify>
    <automated>grep -c "Functional Changes Addendum" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md</automated>
    Expected: `1`.

    <automated>grep -c "REFILL_THRESHOLD" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md</automated>
    Expected: `>= 1`.

    <automated>grep -c "enqueueInterleaved" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md</automated>
    Expected: `>= 1`.

    <automated>grep -cE "^[0-9]+\\." .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md | head -1</automated>
    Expected: `>= 8` (the 8 numbered functional changes).
  </verify>
  <acceptance_criteria>
    - 33-REBRAND.md gains a "Functional Changes Addendum" section after existing content.
    - 8 functional follow-ons listed and described (truncation-tolerant JSON, ReviewScreen dedup, style weights, REFILL_THRESHOLD, enqueueInterleaved, image pre-gen move, in-flight dedupe, InfoFlow div role).
    - Section cross-references Plan 34-02 (Seam 12 fix) and CLAUDE.md "Numeric defaults".
    - Section explicitly notes deferred-to-v1.5 items.
    - No content removed from 33-REBRAND.md.
  </acceptance_criteria>
  <done>
    33-REBRAND.md has a complete Functional Changes addendum ready for Commit 1 staging.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Presentation/ to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (read entire file — confirm Presentation/ not already present)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-12 — Presentation/ rationale)
  </read_first>
  <action>
    1. Read `.gitignore`. Confirm `Presentation/` is NOT already an entry.

    2. Append a new section at the end of the file:
       ```

       # Phase 34 D-12 — operator local workspace, not product code
       Presentation/
       ```

    3. Save with `Write` tool.

    4. Verify `git status --short` no longer lists Presentation/ as untracked.
  </action>
  <verify>
    <automated>grep -c "^Presentation/$" .gitignore</automated>
    Expected: `1`.

    <automated>git status --short | grep -c "Presentation"</automated>
    Expected: `0` (Presentation/ no longer surfaces in git status).
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `Presentation/` on its own line.
    - Comment cites D-12 / Phase 34 rationale.
    - `git status --short` no longer includes any `Presentation/` entries.
    - Existing .gitignore entries are unchanged.
  </acceptance_criteria>
  <done>
    Presentation/ ignored; git status no longer surfaces it.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator review of commit shape before 5-commit land</name>
  <files>(no file edits — operator review checkpoint)</files>
  <read_first>
    - git status --short (full working tree)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-13 commit shape)
  </read_first>
  <action>
    Pause for operator confirmation. Present the 5-commit shape diff and wait for explicit approval before proceeding to Task 6 (Commit 1 land). See <how-to-verify> below for the questions operator must answer.
  </action>
  <what-built>
    All Wave 1-4 plan outputs are now in the working tree:
    - 34-01: Seam 11 test rewrites (trellis-replant.test.mjs + trellis-prune.test.mjs).
    - 34-02: Seam 12 test rewrite (HomeScreen.image-pregen-filter.test.mjs).
    - 34-03: 30-VERIFICATION.md.
    - 34-04: 31-VERIFICATION.md.
    - 34-05: 32-CLOSURE.md + 32-VALIDATION.md annotation.
    - 34-06: post-store.service.ts deleted + ImmersiveInfoFlow removed from InfoFlow.tsx.
    - 34-07: 28/29/30-VALIDATION.md flips.
    - 34-08 Tasks 1-4: 34-UAT-LOG.md + 32.1-VERIFICATION.md flip + 33-REBRAND.md addendum + .gitignore.

    The rebrand WIP from Phase 33 (~26 modified + 6-7 untracked) is also still in the working tree.

    Total: ~50+ files in `git status`. The operator must review the 5-commit shape before staging.
  </what-built>
  <how-to-verify>
    Run from repo root:

    ```bash
    git status --short
    cd app && npm test 2>&1 | grep -E "^# (pass|fail)"
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    ```

    Expected:
    - `git status --short` shows the expected modified/untracked file list (no surprises like Presentation/, no .DS_Store, etc.).
    - `npm test` shows pass >= 449 and fail <= 27.
    - `tsc` exits 0.

    Operator decisions to confirm:
    1. Is the Commit 1 file list correct (rebrand-only, ~22 files)?
    2. Is the Commit 2 file list correct (functional follow-on + Seam 12 fix + ImmersiveInfoFlow removal + post-store deletion, ~13 files)?
    3. Is `33-REBRAND.md` going into Commit 1 or Commit 2? (CONTEXT D-13 says Commit 1; operator's discretion per 34-CONTEXT.md `<decisions>` "Claude's Discretion".)
    4. Are Commit 3 audit files (`v1.4-MILESTONE-AUDIT.md` + `v1.4-INTEGRATION-CHECK-v2.md`) untracked or already committed?
    5. Is .gitignore staged separately or folded into Commit 1?
  </how-to-verify>
  <resume-signal>
    Operator types "approved — proceed with 5-commit land" or describes which file mappings to adjust.
  </resume-signal>
  <verify>
    <automated>git status --short | wc -l</automated>
    Expected: a non-zero count of files awaiting commit (the WIP + Phase 34 outputs).

    <automated>cd app && npx tsc -b --noEmit; echo "TSC=$?"</automated>
    Expected: `TSC=0`.
  </verify>
  <acceptance_criteria>
    - Operator has reviewed the 5-commit shape and confirmed file mappings.
    - Operator has confirmed test baseline (pass >= 449, fail <= 27) and tsc=0 on the working tree.
    - Operator's resume signal is "approved" or includes specific adjustments to apply before Task 6.
  </acceptance_criteria>
  <done>
    Operator approval received; Task 6 (Commit 1) cleared to proceed.
  </done>
</task>

<task type="auto">
  <name>Task 6: Land Commit 1 — Rebrand only</name>
  <files>(see commit_shape_table — ~22 files)</files>
  <read_first>
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-13 Commit 1 file list)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q6 — definitive file mapping)
  </read_first>
  <action>
    Stage ONLY the Commit 1 files (per the table in `<commit_shape_table>` — rebrand-only files):

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(brand): rebrand EchoLearn to Trellis (UI strings, configs, icons, console prefixes)" \
      --files \
      CLAUDE.md \
      app/capacitor.config.ts \
      app/index.html \
      app/ios/App/App/Assets.xcassets/AppIcon-512@2x.png \
      app/ios/App/App/Info.plist \
      app/src/locales/en.json \
      app/src/locales/es.json \
      app/src/locales/ja.json \
      app/src/locales/zh.json \
      app/src/screens/GraphScreen.tsx \
      app/src/services/podcast.service.ts \
      app/src/services/question-filter.service.ts \
      app/src/services/question.service.ts \
      app/src/services/scheduler.native.ts \
      app/src/state/useQuestions.ts \
      Assets/Trellis_logo.png \
      app/public/apple-touch-icon.png \
      app/public/favicon-16x16.png \
      app/public/favicon-32x32.png \
      app/public/pwa-192x192.png \
      app/public/pwa-512x512.png \
      .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md \
      .gitignore
    ```

    (If `.gitignore` operator prefers separating into another commit, drop from this list — operator's discretion per Task 5.)

    Post-commit verification (D-14):
    ```bash
    cd app && npm test 2>&1 | grep -E "^# (pass|fail)"
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    ```

    Expected: pass >= 449 and fail <= 27 + TSC=0. **If different: HALT and investigate before Commit 2.**

    If `git status` still shows uncommitted Commit-1-scope files, stage them in a follow-up amend or include in a small addendum commit. Do NOT proceed to Commit 2 with leftover Commit 1 files staged.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -c "feat(brand): rebrand EchoLearn to Trellis"</automated>
    Expected: `1`.

    <automated>cd app && node --test 2>&1 | grep -E "^# (pass|fail)" | head -2</automated>
    Expected: pass >= 449; fail <= 27.

    <automated>cd app && npx tsc -b --noEmit; echo "TSC=$?"</automated>
    Expected: `TSC=0`.
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `feat(brand): rebrand EchoLearn to Trellis`.
    - Test baseline preserved: pass >= 449, fail <= 27.
    - tsc exits 0.
    - `git status` shows the remaining Commit 2-5 files (no rebrand-only files left uncommitted).
  </acceptance_criteria>
  <done>
    Commit 1 landed; test baseline + tsc preserved.
  </done>
</task>

<task type="auto">
  <name>Task 7: Land Commit 2 — Functional follow-on + Seam 12 fix + Plan 34-06 deletions</name>
  <files>(see commit_shape_table — ~13 modified + 1 deleted file)</files>
  <read_first>
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-05 + D-13 Commit 2)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q5/Q6)
  </read_first>
  <action>
    Stage Commit 2 files (functional + Seam 12 + Plan 34-06 deletions):

    ```bash
    # Stage the deletion of post-store.service.ts (use git rm if not already removed):
    git rm -f app/src/services/post-store.service.ts 2>/dev/null || true

    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "refactor(feed+review): JSON parser hardening, image pre-gen architecture shift, queue tuning, ReviewScreen dedup" \
      --files \
      app/src/components/InfoFlow.tsx \
      app/src/screens/HomeScreen.tsx \
      app/src/screens/ReviewScreen.tsx \
      app/src/screens/settings/SettingsDataScreen.tsx \
      app/src/services/canonical-knowledge.service.ts \
      app/src/services/concept-feed.service.ts \
      app/src/services/imageGeneration.service.ts \
      app/src/services/post-queue.service.ts \
      app/src/services/style-assignment.ts \
      app/tests/services/image-gen-key-gate.test.mjs \
      app/tests/services/post-queue.test.mjs \
      app/tests/services/style-assignment.test.mjs \
      app/tests/screens/HomeScreen.image-pregen-filter.test.mjs \
      app/src/services/post-store.service.ts
    ```

    The `app/src/services/post-store.service.ts` entry covers the deletion (gsd-tools/git records it as a removed-file entry).

    Post-commit verification (D-14):
    ```bash
    cd app && npm test 2>&1 | grep -E "^# (pass|fail)"
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    cd app && npx vite build 2>&1 | tail -5
    ```

    Expected: pass >= 449 (potentially +1-2 if Seam 12 fix improves; Plan 34-01 also flips +2 in Commit 5 territory). fail <= 27. tsc=0. vite build clean. **If pass < 449 or fail > 27: HALT.**
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -c "refactor(feed+review)"</automated>
    Expected: `1`.

    <automated>test ! -f app/src/services/post-store.service.ts && echo DELETED</automated>
    Expected: `DELETED`.

    <automated>cd app && npm test 2>&1 | grep -E "^# (pass|fail)" | head -2</automated>
    Expected: pass >= 449; fail <= 27.

    <automated>cd app && npx tsc -b --noEmit; echo "TSC=$?"</automated>
    Expected: `TSC=0`.
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `refactor(feed+review):`.
    - `app/src/services/post-store.service.ts` is removed from the tree (deletion recorded).
    - `app/src/components/InfoFlow.tsx` no longer contains `ImmersiveInfoFlow`.
    - Test baseline: pass >= 449, fail <= 27 (D-14).
    - tsc exits 0.
    - vite build is clean.
  </acceptance_criteria>
  <done>
    Commit 2 landed atomically with Seam 12 fix + Plan 34-06 deletions; test baseline preserved.
  </done>
</task>

<task type="auto">
  <name>Task 8: Land Commit 3 — Audit artifacts</name>
  <files>.planning/v1.4-MILESTONE-AUDIT.md, .planning/v1.4-INTEGRATION-CHECK-v2.md</files>
  <read_first>
    - .planning/v1.4-MILESTONE-AUDIT.md (confirm content)
    - .planning/v1.4-INTEGRATION-CHECK-v2.md (confirm content)
  </read_first>
  <action>
    Stage Commit 3 audit artifacts:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(audit): v1.4 milestone audit + integration check v2 (post-32.1+33 re-audit)" \
      --files \
      .planning/v1.4-MILESTONE-AUDIT.md \
      .planning/v1.4-INTEGRATION-CHECK-v2.md
    ```

    If either file is already committed at HEAD (per `git log -- <file>`), skip that file in the --files list. `gsd-tools commit` will no-op if no changes to stage.

    Post-commit verification:
    ```bash
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    ```

    No npm test needed (doc-only commit).
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -c "docs(audit): v1.4 milestone audit"</automated>
    Expected: `1` (unless audit files were already committed; in that case verify the commit was no-op'd).

    <automated>cd app && npx tsc -b --noEmit; echo "TSC=$?"</automated>
    Expected: `TSC=0`.
  </verify>
  <acceptance_criteria>
    - HEAD commit (or last meaningful audit commit) has `docs(audit):` prefix.
    - `v1.4-MILESTONE-AUDIT.md` and `v1.4-INTEGRATION-CHECK-v2.md` are committed (not in `git status`).
    - tsc exits 0.
  </acceptance_criteria>
  <done>
    Commit 3 audit docs landed.
  </done>
</task>

<task type="auto">
  <name>Task 9: Land Commit 4 — Phase 34 planning context (if not already committed)</name>
  <files>.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md, 34-DISCUSSION-LOG.md, 34-RESEARCH.md, 34-VALIDATION.md, .planning/STATE.md</files>
  <read_first>
    - git log --oneline -- .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (check if already committed)
    - git log --oneline -- .planning/STATE.md
  </read_first>
  <action>
    Check whether the Phase 34 planning files are already committed (the discuss-phase commit `c24a875f` already landed CONTEXT + DISCUSSION-LOG + STATE per 34-CONTEXT.md baseline).

    If 34-RESEARCH.md and 34-VALIDATION.md are still uncommitted (added by /gsd:research-phase + /gsd:validate-phase later than discuss-phase), stage them:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-34): planning context — research + validation strategy" \
      --files \
      .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md \
      .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-VALIDATION.md
    ```

    If everything is already committed, this is a no-op — `gsd-tools commit` skips when there's nothing to stage.

    Post-commit verification:
    ```bash
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    ```
  </action>
  <verify>
    <automated>git log --oneline -- .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md | wc -l</automated>
    Expected: `>= 1` (at least one commit references this file).

    <automated>git status --short .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md | wc -l</automated>
    Expected: `0` (file is committed, not in working tree).
  </verify>
  <acceptance_criteria>
    - 34-RESEARCH.md and 34-VALIDATION.md are committed (not in `git status`).
    - 34-CONTEXT.md and 34-DISCUSSION-LOG.md remain in their original commit (or are committed here if not already).
    - tsc exits 0.
  </acceptance_criteria>
  <done>
    Commit 4 (planning context) landed or confirmed already-landed.
  </done>
</task>

<task type="auto">
  <name>Task 10: Land Commit 5 — Phase 34 execution artifacts (catch-all)</name>
  <files>(everything remaining in .planning/phases/34-* that is uncommitted)</files>
  <read_first>
    - git status --short (full working tree state)
  </read_first>
  <action>
    Final tidy commit. Stage every remaining uncommitted file under `.planning/phases/34-*` plus any leftover edits from Plans 34-01..07:

    ```bash
    # Identify uncommitted Phase 34 artifacts:
    git status --short | grep "phase-34\\|34-01\\|34-02\\|34-03\\|34-04\\|34-05\\|34-06\\|34-07\\|34-08"

    # Identify any remaining test fix files (from Plans 34-01) not yet committed:
    git status --short app/tests/services/trellis-replant.test.mjs app/tests/services/trellis-prune.test.mjs

    # Identify VALIDATION flips (Plan 34-07):
    git status --short .planning/phases/28-*/28-VALIDATION.md .planning/phases/29-*/29-VALIDATION.md .planning/phases/30-*/30-VALIDATION.md

    # Identify VERIFICATION docs (Plans 34-03, 34-04, 34-05):
    git status --short .planning/phases/30-*/30-VERIFICATION.md .planning/phases/31-*/31-VERIFICATION.md .planning/phases/32-*/32-CLOSURE.md .planning/phases/32-*/32-VALIDATION.md .planning/phases/32.1-*/32.1-VERIFICATION.md

    # Identify Plan 34-08 outputs (UAT log, etc.):
    git status --short .planning/phases/34-*/34-UAT-LOG.md .planning/phases/34-*/34-08-PLAN.md .planning/phases/34-*/34-08-SUMMARY.md
    ```

    Stage and commit ALL remaining items in one final commit:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-34): close v1.4 — verification debt + Seam 11 + orphan cleanup + UAT log" \
      --files \
      .planning/phases/30-*/30-VERIFICATION.md \
      .planning/phases/31-*/31-VERIFICATION.md \
      .planning/phases/32-*/32-CLOSURE.md \
      .planning/phases/32-*/32-VALIDATION.md \
      .planning/phases/32.1-*/32.1-VERIFICATION.md \
      .planning/phases/28-*/28-VALIDATION.md \
      .planning/phases/29-*/29-VALIDATION.md \
      .planning/phases/30-*/30-VALIDATION.md \
      .planning/phases/34-*/*.md \
      app/tests/services/trellis-replant.test.mjs \
      app/tests/services/trellis-prune.test.mjs
    ```

    Adjust the --files list to match the actual `git status` output. Use specific globs that match only the modified/added files. The pattern `.planning/phases/34-*/*.md` catches all PLAN/SUMMARY/UAT-LOG files for Phase 34.

    Post-commit verification (FINAL — D-14):
    ```bash
    cd app && npm test 2>&1 | grep -E "^# (pass|fail)"
    cd app && npx tsc -b --noEmit && echo "TSC=$?"
    cd app && npx vite build 2>&1 | tail -5
    git status --short
    git log --oneline gsd/phase-33-hygiene-and-polish | head -7
    ```

    Expected:
    - `npm test`: pass >= 449 (potentially 451 if Plan 34-01 fixed 2 trellis tests); fail <= 27 (potentially 25).
    - tsc: TSC=0.
    - vite build: clean.
    - `git status --short`: empty or near-empty (only Phase 34 SUMMARY iteration files in flight).
    - `git log --oneline | head -7`: shows 5 (or 4-6) Phase 34 commits + the prior `c24a875f` ROADMAP commit.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -c "docs(phase-34): close v1.4"</automated>
    Expected: `1`.

    <automated>cd app && npm test 2>&1 | grep -E "^# (pass|fail)" | head -2</automated>
    Expected: pass >= 449; fail <= 27.

    <automated>cd app && npx tsc -b --noEmit; echo "TSC=$?"</automated>
    Expected: `TSC=0`.

    <automated>cd app && npx vite build 2>&1 | grep -E "error|Error" | wc -l</automated>
    Expected: `0`.

    <automated>git status --short | wc -l</automated>
    Expected: `<= 3` (only newly-created SUMMARY files for Phase 34, if any).

    <automated>git log --oneline gsd/phase-33-hygiene-and-polish | head -7 | wc -l</automated>
    Expected: `>= 5` (5 Phase 34 commits visible plus older ones).
  </verify>
  <acceptance_criteria>
    - Final HEAD commit has `docs(phase-34): close v1.4` prefix.
    - `git status --short` shows ≤ 3 entries (or empty).
    - npm test: pass >= 449, fail <= 27.
    - tsc: 0 errors.
    - vite build: clean.
    - `git log --oneline | head -7` on `gsd/phase-33-hygiene-and-polish` shows 5 Phase 34 commits in correct order: rebrand → functional → audit → planning → execution.
  </acceptance_criteria>
  <done>
    All 5 commits landed. Working tree clean. v1.4 ready for re-audit (`/gsd:audit-milestone v1.4` should now produce `status: passed` or `tech_debt`).
  </done>
</task>

</tasks>

<verification>
- 34-UAT-LOG.md exists with 3 retroactive PASS rows + 2 pending rows.
- 32.1-VERIFICATION.md status: passed (was: human_needed).
- 33-REBRAND.md gains Functional Changes addendum (8 items).
- .gitignore contains Presentation/.
- 5 commits land on gsd/phase-33-hygiene-and-polish in order:
  - C1: feat(brand): rebrand EchoLearn to Trellis
  - C2: refactor(feed+review): JSON parser hardening, image pre-gen architecture shift, queue tuning, ReviewScreen dedup
  - C3: docs(audit): v1.4 milestone audit + integration check v2
  - C4: docs(phase-34): planning context (or no-op if already committed)
  - C5: docs(phase-34): close v1.4 — verification debt + Seam 11 + orphan cleanup + UAT log
- Test baseline preserved at every commit boundary (D-14): pass >= 449, fail <= 27.
- tsc=0 at every commit.
- vite build clean post-Commit 5.
- Working tree clean (≤ 3 entries).
</verification>

<success_criteria>
- DEVICE-UAT-RETEST gap closed: 34-UAT-LOG.md + 32.1-VERIFICATION.md flip.
- WIP-COMMIT-SHAPE gap closed: 5 commits in canonical order.
- D-14 invariant honored: tests green at every commit boundary.
- D-12 honored: Presentation/ ignored.
- D-11 honored: 33-REBRAND.md addendum lists 8 functional follow-ons.
- v1.4 milestone shippable; re-audit ready.
</success_criteria>

<output>
After completion, create `.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-08-SUMMARY.md` documenting:
- 5 commit SHAs.
- Test counts before/after each commit (pass/fail).
- 34-UAT-LOG.md row count (3 PASS + 2 pending + 6 SKIP).
- 32.1-VERIFICATION.md status flip evidence.
- 33-REBRAND.md addendum line count.
- .gitignore entry confirmed.
- Final working-tree state.
- Confirmation that v1.4 close-out criteria are met (per 34-CONTEXT.md `<domain>` 10 in-scope items).
</output>
