---
phase: 32.1-v1-4-uat-retest-gap-closure
plan: 05
subsystem: ui
tags: [capacitor-android, settings, clear-data, navigation, location-assign, gap-closure]

# Dependency graph
requires:
  - phase: 32.1-v1-4-uat-retest-gap-closure
    provides: "32.1-01 retest evidence — operator HuanfuLi (2026-04-19) verbatim report 'When cleared all, not refreshed: user still in Settings page, not in Home page'. Sourced finding R1 in 32.1-01-SUMMARY.md routed to this plan."
provides:
  - "Single-line fix at SettingsDataScreen.tsx:68 — Clear All Data now uses window.location.assign('/home') instead of window.location.reload()"
  - "Closes G5 / CLEAR-RELOAD: app auto-navigates to /home after Clear All Data on Capacitor Android (no more silent no-op reload)"
affects: [32.1-VERIFICATION (future), 31-VERIFICATION.md (future), v1.4 milestone close-out]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Capacitor WebView navigation pattern: window.location.assign('/path') for destructive-then-route operations (does not add history entry, triggers full page load + route change in one shot)"
    - "Single-call-site UI behavior fix exempted from regression test per CONTEXT D-20 (manual operator retest after APK deploy is sufficient)"

key-files:
  created:
    - ".planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-05-SUMMARY.md"
  modified:
    - "app/src/screens/settings/SettingsDataScreen.tsx (line 68: window.location.reload() → window.location.assign('/home'))"
    - ".planning/STATE.md"
    - ".planning/ROADMAP.md"

key-decisions:
  - "D-19 honored: replaced window.location.reload() with window.location.assign('/home'). Chose assign() over href= because assign() does not add a history entry — cleaner semantics for a destructive operation."
  - "D-20 honored: no regression test created. Single call-site UI behavior change; on-device manual retest after APK deploy is the acceptance gate (bundles into the next operator deploy cycle naturally)."
  - "D-14 honored: atomic plan, atomic commit. Did NOT include the two adjacent unrelated i18n changes already present in the working tree (toast key + button label at lines 219, 225) — those are out of scope for this plan and remain unstaged for the owner of that work to commit separately."
  - "D-17 honored at the SUMMARY level: G5 has no Phase 31 UAT counterpart (it's a NEW gap surfaced by 32.1-01 retest). Per D-17 the outcome is recorded in this 32.1-05-SUMMARY.md, not in 31-UAT.md."

patterns-established:
  - "Atomic-hunk staging via `git apply --cached <isolated.patch>` when working tree contains pre-existing unrelated changes — preserves single-file-per-commit acceptance criterion without losing the unrelated work"
  - "Operator-evidence cross-reference: 32.1-01-SUMMARY.md Routed Findings table R1 explicitly cites this plan as the closure venue, creating a bidirectional trace from observation → fix"

requirements-completed: [CLEAR-RELOAD]

# Metrics
duration: 4min
completed: 2026-04-19
---

# Phase 32.1 Plan 05: G5 — Clear All Data uses window.location.assign('/home') instead of reload (CLEAR-RELOAD) Summary

**Replaced silent-no-op `window.location.reload()` at SettingsDataScreen.tsx:68 with `window.location.assign('/home')` so Clear All Data on Capacitor Android both reloads AND auto-navigates to /home in one shot — closes G5 routed from 32.1-01 operator evidence**

## Performance

- **Duration:** ~4 min (parallel execution wave alongside 32.1-02 and 32.1-03)
- **Started:** 2026-04-19
- **Completed:** 2026-04-19
- **Tasks:** 1/1
- **Files modified:** 1 code file (atomic commit) + 2 doc files (this SUMMARY + STATE/ROADMAP)

## Accomplishments

- Single-line behavior fix: `setTimeout(() => window.location.reload(), 800)` → `setTimeout(() => window.location.assign('/home'), 800)` at `app/src/screens/settings/SettingsDataScreen.tsx:68`
- Closed G5 / CLEAR-RELOAD gap discovered by operator (HuanfuLi, 2026-04-19) during 32.1-01 retest — verbatim: "When cleared all, not refreshed: user still in Settings page, not in Home page"
- Atomic commit `3f4f35c3` containing only the G5 hunk; preserved the two pre-existing unrelated i18n hunks (toast key + button label at lines 219, 225) unstaged for separate ownership

## Task Commits

1. **Task 05-01: Replace reload() with assign('/home') in Clear All Data handler** — `3f4f35c3` (fix)

## Files Created/Modified

- `app/src/screens/settings/SettingsDataScreen.tsx` — Line 68 reload → assign('/home') (1 insertion, 1 deletion)
- `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-05-SUMMARY.md` — This file (created)
- `.planning/STATE.md` — Marked plan 32.1-05 complete; appended Latest Decisions entries
- `.planning/ROADMAP.md` — Checked off `32.1-05-PLAN.md` in Phase 32.1 plan list (line 841)

## Decisions Made

- **D-19 honored** (replaced reload() with assign('/home')): Chose `window.location.assign('/home')` over `window.location.href = '/home'` per CONTEXT rationale — `assign()` does not push a history entry, which is semantically correct for a destructive operation (no "back button" return to the now-cleared state).
- **D-20 honored** (no regression test): Single call-site UI behavior change. On-device manual retest after the next APK deploy cycle is the acceptance gate. The G3 plan (32.1-01) already gates on `apk-deployed` so the retest cycle naturally validates G5 too.
- **D-14 honored** (atomic plan, atomic commit): Used `git apply --cached <isolated.patch>` to stage ONLY the line-68 hunk for this commit. The two pre-existing unrelated i18n hunks (line 219 toast key + line 225 button label) remain in the working tree unstaged, awaiting their separate ownership commit (likely a Phase 27 follow-up cleanup, not this plan's domain).
- **D-17 honored at SUMMARY level**: G5 has no Phase 31 UAT counterpart (it's a NEW gap surfaced by the 32.1-01 retest, not a Phase 31 first-pass failure). Per D-17 the outcome is recorded HERE in `32.1-05-SUMMARY.md` rather than in `31-UAT.md`. The bidirectional trace is: 32.1-01-SUMMARY.md `## Routed Findings` row R1 → this plan; this SUMMARY `## Outcome` → operator evidence in 32.1-01-SUMMARY.md.

## Verification

All `<acceptance_criteria>` from `32.1-05-PLAN.md` Task 05-01 verified post-commit:

```bash
$ grep -c "window.location.assign('/home')" app/src/screens/settings/SettingsDataScreen.tsx
1   # ✓ assign call present at line 68

$ grep -n "window.location" app/src/screens/settings/SettingsDataScreen.tsx
68:      setTimeout(() => window.location.assign('/home'), 800);
91:      setTimeout(() => window.location.reload(), 500);
220:              setTimeout(() => window.location.reload(), 600);

$ cd app && npx tsc -b --noEmit 2>&1 | grep -c "error TS"
0   # ✓ no new tsc errors (baseline preserved)

$ git show --stat 3f4f35c3 | grep -c "SettingsDataScreen.tsx"
1   # ✓ exactly one file modified in commit

$ git show 3f4f35c3 -- app/src/screens/settings/SettingsDataScreen.tsx | grep -c "^[-+].*window.location"
2   # ✓ exactly one removed reload + one added assign (no other location-related changes)
```

**Note on the literal `grep -c "window.location.reload()"` acceptance criterion (PLAN line 132):** As written, that criterion expected `0` matches in the file. The file contains 2 OTHER `reload()` calls — at line 91 (`handleReset` settings reset, NOT G5's domain) and line 220 (`Reset Today` button, NOT G5's domain). Per the plan's `<read_first>` scope (lines 51-70 only) and the more specific per-commit criterion at PLAN line 135 (`^[-+].*window.location` returns exactly 2), the operative interpretation is "the G5 reload call is fully removed" — which IS satisfied (line 68 no longer contains `.reload()`). The two unrelated reload calls at lines 91 and 220 were never in this plan's scope and would be inappropriate to touch in an atomic G5 commit. This is a planner-spec ambiguity flagged transparently for the verifier; no rule deviation applied.

## Outcome

**Code change landed atomically.** The `handleClearAllData` flow on Capacitor Android will now, after the user confirms the destructive action and the toast appears, navigate to `/home` and trigger a fresh page load 800ms later — wiping in-memory React state AND landing on the home route in one go.

**Operator-evidence root cause** (HuanfuLi, 2026-04-19, surfaced during 32.1-01 retest, verbatim per `32.1-01-SUMMARY.md` Outcome section):

> When cleared all, not refreshed: user still in Settings page, not in Home page. When user navigate to Home manually, user firstly see no posts at all, only empty state. A few seconds later, user see 3 introduction posts.

The first sentence — "user still in Settings page, not in Home page" — is what this plan closes. The second sentence (loading flicker before starter posts) is logged as G6 in CONTEXT.md `## Out of scope`, expected to resolve naturally once 32.1-04 (G4 starter persistence) lands.

**On-device acceptance** is bundled with the next operator APK deploy cycle (no separate retest plan required per D-20). Acceptance check: after Settings → Data → Clear All Data → confirm → ~800ms later, app should auto-navigate to /home (no manual nav required). If the operator confirms this in a follow-up retest, no additional code action is needed; if it fails, escalate via a new gap-closure plan.

## Deviations from Plan

None — plan executed exactly as written. Single-line edit at SettingsDataScreen.tsx:68 + atomic commit + SUMMARY.

**Working-tree note (NOT a deviation):** The file had two pre-existing uncommitted i18n changes at lines ~219, ~225 (`toast(t('settings.toast.todayReset'), 'success')` replacing the hardcoded English string, and `<RotateCcw size={16} /> {t('settings.buttons.resetToday')}` replacing the hardcoded "Reset Today"). These belong to a separate Phase 27 i18n cleanup concern — they are not in this plan's scope and were intentionally NOT staged for this commit. They remain in the working tree as unstaged modifications for whoever owns that work to commit separately. Used `git apply --cached <isolated.patch>` to stage only the G5 hunk.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **G5 / CLEAR-RELOAD closed at the code level.** The fix is in commit `3f4f35c3` on `main`. Will be exercised on-device when the next APK is built and deployed (per D-20, no separate retest plan).
- **Routed-finding closure trace:** `32.1-01-SUMMARY.md ## Routed Findings R1` → this plan. The bidirectional trace is now complete; future verifier (`/gsd:verify 32.1`) can follow the trail from operator evidence → routed finding → atomic fix → commit.
- **Phase 32.1 Wave 2 progress:** 32.1-05 complete; 32.1-02 (G1) and 32.1-03 (G2) and 32.1-04 (G4) are sibling parallel agents executing concurrently per CONTEXT D-15. Once all four Wave 2 plans land, Phase 32.1 is ready for `/gsd:verify 32.1` and v1.4 milestone close-out.
- **No blockers** for sibling Wave 2 plans — this plan touched only `SettingsDataScreen.tsx`, which the other plans do not modify.

## Self-Check: PASSED

**Files verified:**

- `app/src/screens/settings/SettingsDataScreen.tsx` — FOUND, line 68 contains `window.location.assign('/home')`
- `.planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-05-SUMMARY.md` — FOUND (this file)

**Commits verified:**

- `3f4f35c3` — FOUND on `main` (`fix(32.1-05): Clear All Data uses location.assign('/home') instead of reload (CLEAR-RELOAD)`)

**Acceptance criteria:**

- All 5 `<acceptance_criteria>` from 32.1-05-PLAN.md Task 05-01 verified passing (with the noted planner-spec ambiguity around literal `reload()` count, transparently documented above for verifier)
- All 5 `<success_criteria>` from continuation prompt verified

---
*Phase: 32.1-v1-4-uat-retest-gap-closure*
*Completed: 2026-04-19*
