---
plan: 29-04
phase: 29-final-polishment
type: execute
wave: 1
depends_on: []
autonomous: false
requirements:
  - UAT-20
  - UAT-21
  - UAT-22
  - UAT-26
files_modified:
  - .planning/phases/29-final-polishment/29-UAT-LOG.md
  - .planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md
  - .planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md
  - .planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md
  - .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md

must_haves:
  truths:
    - "29-UAT-LOG.md exists with 22 active rows (PASS or FAIL→fixed→PASS) + 3 SKIP rows for D-23 items"
    - "All 22 active items resolved — no row remains in 'pending' or 'FAIL-unfixed' state"
    - "Phases 20, 21, 22, 26 VERIFICATION.md frontmatter status flipped from human_needed → passed"
    - "Each of the 4 VERIFICATION.md files has a re_verification block pointing to 29-UAT-LOG.md"
    - "Every walkthrough-surfaced FAIL has a separate git commit with message fix(29-04): ... referencing the UAT item ID"
  artifacts:
    - path: ".planning/phases/29-final-polishment/29-UAT-LOG.md"
      provides: "Single authoritative flat file recording all UAT walkthrough results"
    - path: ".planning/milestones/v1.3-phases/{20,21,22,26}-*/*-VERIFICATION.md"
      provides: "Updated frontmatter with status: passed + re_verification block"
  key_links:
    - from: ".planning/milestones/v1.3-phases/20-*/20-VERIFICATION.md"
      to: ".planning/phases/29-final-polishment/29-UAT-LOG.md"
      via: "re_verification.log field"
      pattern: "log:\\s*\\.planning/phases/29-final-polishment/29-UAT-LOG\\.md"
    - from: ".planning/milestones/v1.3-phases/21-*/21-VERIFICATION.md"
      to: ".planning/phases/29-final-polishment/29-UAT-LOG.md"
      via: "re_verification.log field"
    - from: ".planning/milestones/v1.3-phases/22-*/22-VERIFICATION.md"
      to: ".planning/phases/29-final-polishment/29-UAT-LOG.md"
      via: "re_verification.log field"
    - from: ".planning/milestones/v1.3-phases/26-*/26-VERIFICATION.md"
      to: ".planning/phases/29-final-polishment/29-UAT-LOG.md"
      via: "re_verification.log field"
---

<objective>
Close all `human_needed` UAT items across archived phases 20/21/22/26. Operator executes each walkthrough item on-device (or in browser dev mode as appropriate); Claude records results in `29-UAT-LOG.md` in real time; each FAIL spawns an inline fix commit; each `VERIFICATION.md` flips from `status: human_needed` to `status: passed` with a `re_verification` block pointing to the log.

Purpose: This is the final human-in-the-loop validation gate for the v1.3 milestone. Every Claude-automated must-have has already been verified in its original phase; these 22 items are the behaviors that require a human to eyeball the animation, tap the button, or listen for the TTS. Per D-11, Phase 29 is NOT complete while any `human_needed` item is open.

Output: 1 new `29-UAT-LOG.md` + 4 updated archived VERIFICATION.md files + N inline fix commits (unbounded per D-10/D-11).

**Plan shape — checkpoint-heavy single wave (per CONTEXT.md Claude's Discretion):**
- Task 0: Scaffold log (autonomous, no checkpoint)
- Tasks 1-4: Per-source-phase operator walkthroughs (`checkpoint:human-verify`)
- Task 5: Fix sub-loop (autonomous; only runs if FAILs surfaced in tasks 1-4)
- Task 6: Frontmatter flip (autonomous, gated on all tasks 1-4 + 5 resolving)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-final-polishment/29-CONTEXT.md
@.planning/phases/29-final-polishment/29-RESEARCH.md
@.planning/phases/29-final-polishment/29-VALIDATION.md
@.planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md
@.planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md
@.planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md
@.planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md

<interfaces>
<!-- No code interfaces — this plan operates on markdown + git commits. -->

UAT-LOG schema (from 29-RESEARCH.md lines 682-707 — D-09):
```markdown
| Item ID | Source Phase | Test | Expected | Actual | Pass/Fail | Fix Commit | Date | Notes |
|---------|-------------|------|----------|--------|-----------|-----------|------|-------|
| 20-UAT-1 | 20 | Portal card layout | ... | ... | PASS/FAIL/SKIP | — or <git-sha> | 2026-04-XX | ... |
```

re_verification frontmatter schema (from 29-RESEARCH.md lines 712-744 — D-21, template from 26-VERIFICATION.md):
```yaml
re_verification:
  previous_status: human_needed
  previous_score: X/X
  gaps_closed:
    - "..."
  gaps_remaining: []
  regressions: []
  log: .planning/phases/29-final-polishment/29-UAT-LOG.md
```

Skip list (D-23):
- Phase 21 item 4 (daily goal progress bar — already marked N/A in VERIFICATION frontmatter; REVIEW-03 descoped)
- Phase 22 items 4 and 5 (animated tab-tap + non-adjacent slide — REVERTED 2026-04-15 per addendum in 22-VERIFICATION.md)

Active item count: 4 (phase 20) + 4 (phase 21, item 4 SKIP) + 9 (phase 22, items 4+5 SKIP) + 7 (phase 26) = 24 active + 3 SKIP. Research settled on 22 after reconciling Phase 21 numbering — confirm by reading each VERIFICATION.md before scaffolding the log.
</interfaces>
</context>

<tasks>

<task id="29-04-00" type="auto">
  <name>Task 0 (Wave 0): Scaffold 29-UAT-LOG.md with all active rows + 3 SKIP rows</name>
  <files>.planning/phases/29-final-polishment/29-UAT-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md (extract 4 human_verification items with test + expected)
    - .planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md (extract 4-5 human_verification items; note item 4 is daily goal bar — already marked N/A)
    - .planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md (extract 9-11 human_verification items; apply 2026-04-15 addendum — skip items 4+5)
    - .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md (extract 7 human_verification items)
    - .planning/phases/29-final-polishment/29-RESEARCH.md (item breakdown lines 608-677 — canonical expected item count reconciliation)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-09, D-23)
  </read_first>
  <action>
    Create `.planning/phases/29-final-polishment/29-UAT-LOG.md` with this exact structure:

    ```markdown
    ---
    phase: 29-final-polishment
    log_type: uat-walkthrough
    started: [YYYY-MM-DD]
    completed: [leave blank — filled on 29-04-06]
    operator: [operator name]
    active_items: 22
    skip_items: 3
    total_items: 25
    ---

    # 29 UAT Log

    All `human_needed` items across archived v1.3 phases 20, 21, 22, 26.
    Skip items (per D-23) are recorded for audit completeness but require NO walkthrough.

    ## Summary

    | Column | Meaning |
    |--------|---------|
    | Item ID | `{source-phase}-UAT-{n}` (n = index from VERIFICATION.md human_verification list) |
    | Source Phase | 20 / 21 / 22 / 26 |
    | Test | Brief from VERIFICATION.md `test` field |
    | Expected | From VERIFICATION.md `expected` field |
    | Actual | What operator observed on device |
    | Pass/Fail | PASS / FAIL / SKIP |
    | Fix Commit | git SHA (if FAIL→fixed); `—` otherwise |
    | Date | YYYY-MM-DD item was tested |
    | Notes | Deviation, workaround, context |

    ## Items

    ### Source Phase 20 — Orchestration strategy + diagnostic dialogue (4 items)

    | Item ID | Source Phase | Test | Expected | Actual | Pass/Fail | Fix Commit | Date | Notes |
    |---------|-------------|------|----------|--------|-----------|-----------|------|-------|
    | 20-UAT-1 | 20 | [copy from 20-VERIFICATION.md item 1 test field] | [copy expected] | | pending | — | | |
    | 20-UAT-2 | 20 | [copy item 2] | | | pending | — | | |
    | 20-UAT-3 | 20 | [copy item 3] | | | pending | — | | |
    | 20-UAT-4 | 20 | [copy item 4] | | | pending | — | | |

    ### Source Phase 21 — Review cap fix + generate-on-enter posts (4 active + 1 SKIP)

    | Item ID | Source Phase | Test | Expected | Actual | Pass/Fail | Fix Commit | Date | Notes |
    |---------|-------------|------|----------|--------|-----------|-----------|------|-------|
    | 21-UAT-1 | 21 | [copy item 1] | | | pending | — | | |
    | 21-UAT-2 | 21 | [copy item 2] | | | pending | — | | |
    | 21-UAT-3 | 21 | [copy item 3] | | | pending | — | | |
    | 21-UAT-4 | 21 | Daily goal progress bar | (N/A) | — | SKIP | — | — | D-23: REVIEW-03 descoped per feedback_daily_goal.md; already N/A in 21-VERIFICATION.md |
    | 21-UAT-5 | 21 | [copy item 5] | | | pending | — | | |

    ### Source Phase 22 — Swipe navigation (9 active + 2 SKIP)

    | Item ID | Source Phase | Test | Expected | Actual | Pass/Fail | Fix Commit | Date | Notes |
    |---------|-------------|------|----------|--------|-----------|-----------|------|-------|
    | 22-UAT-1 | 22 | [copy item 1] | | | pending | — | | |
    | 22-UAT-2 | 22 | [copy item 2] | | | pending | — | | |
    | 22-UAT-3 | 22 | [copy item 3] | | | pending | — | | |
    | 22-UAT-4 | 22 | Tab tap slide animation | (REVERTED) | — | SKIP | — | — | D-23: SWIPE-09 reverted 2026-04-15 per 22-VERIFICATION.md addendum |
    | 22-UAT-5 | 22 | Non-adjacent tab tap direct slide | (REVERTED) | — | SKIP | — | — | D-23: reverted 2026-04-15 |
    | 22-UAT-6 | 22 | [copy item 6] | | | pending | — | | |
    | 22-UAT-7 | 22 | [copy item 7] | | | pending | — | | |
    | 22-UAT-8 | 22 | [copy item 8] | | | pending | — | | |
    | 22-UAT-9 | 22 | [copy item 9] | | | pending | — | | |
    | 22-UAT-10 | 22 | [copy item 10] | | | pending | — | | |
    | 22-UAT-11 | 22 | [copy item 11] | | | pending | — | | |

    ### Source Phase 26 — Trellis harvest panel (7 items)

    | Item ID | Source Phase | Test | Expected | Actual | Pass/Fail | Fix Commit | Date | Notes |
    |---------|-------------|------|----------|--------|-----------|-----------|------|-------|
    | 26-UAT-1 | 26 | [copy item 1] | | | pending | — | | |
    | 26-UAT-2 | 26 | [copy item 2] | | | pending | — | | |
    | 26-UAT-3 | 26 | [copy item 3] | | | pending | — | | |
    | 26-UAT-4 | 26 | [copy item 4] | | | pending | — | | |
    | 26-UAT-5 | 26 | [copy item 5] | | | pending | — | | |
    | 26-UAT-6 | 26 | [copy item 6] | | | pending | — | | |
    | 26-UAT-7 | 26 | [copy item 7] | | | pending | — | | |

    ## Walkthrough-surfaced issues (inline fixes)

    (Empty at scaffold; populated by task 29-04-05 as operator surfaces bugs.)

    | Issue | Surfaced During | Fix Commit | Re-test Result | Date |
    |-------|-----------------|-----------|----------------|------|

    ## Sign-off

    - [ ] All 22 active items PASS (or FAIL → inline fix → re-test → PASS)
    - [ ] All 3 SKIP items documented with D-23 reference
    - [ ] All walkthrough-surfaced issues fixed and re-tested
    - [ ] Phase 20/21/22/26 VERIFICATION.md frontmatter flipped to `status: passed`
    - [ ] Operator sign-off: [name + date]
    ```

    Before writing, READ each `human_verification` block in the 4 source VERIFICATION.md files and COPY the `test` and `expected` field values verbatim into the cells. Do NOT paraphrase or truncate — the log must be auditable against the source VERIFICATION.md.

    If the actual item counts from the source files differ from the template (e.g., Phase 21 has 6 items, or Phase 22 has 10), adjust the row count accordingly and update the frontmatter `active_items`, `skip_items`, `total_items` fields to match. The research estimate was 22 active; confirm by counting non-SKIP rows after population.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-final-polishment/29-UAT-LOG.md && grep -c "pending" .planning/phases/29-final-polishment/29-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/29-final-polishment/29-UAT-LOG.md` exits 0
    - `grep -c "^| 20-UAT-" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 4
    - `grep -c "^| 21-UAT-" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns >= 4 (plus SKIP rows)
    - `grep -c "^| 22-UAT-" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns >= 9 (plus 2 SKIP rows)
    - `grep -c "^| 26-UAT-" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 7
    - `grep -c "SKIP" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns >= 3
    - `grep -c "pending" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns >= 22 (all active rows start pending)
    - `grep -c "D-23" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns >= 3 (SKIP reason documented on each)
    - Frontmatter fields present: `phase:`, `log_type: uat-walkthrough`, `active_items:`, `skip_items:`, `total_items:`
  </acceptance_criteria>
  <done>
    29-UAT-LOG.md exists with all rows scaffolded. Every active row reads `pending`. Every SKIP row cites D-23 reason. Frontmatter counts match row counts.
  </done>
</task>

<task id="29-04-01" type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (Checkpoint): Operator walks through Phase 20 UAT items (4 items)</name>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (confirm Phase 20 rows exist and are pending)
    - .planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md (original test + expected for each of 4 items)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-22 — operator executes, Claude records)
  </read_first>
  <files>.planning/phases/29-final-polishment/29-UAT-LOG.md</files>
  <action>
    <what-built>
    Phase 20 shipped in the v1.3 milestone. The 4 human_needed items cover:
    - Portal card visual layout (colored border + 3 tappable content-type indicators with counts)
    - Diagnostic chat multi-turn flow (submit → follow-up → reply → Done with 3-turn cap)
    - Portal card indicator navigation (flashcard icon → /review, post icon → /posts/:id, Q icon → /ask/:id)
    - Portal card primary CTA navigateToMove routing
  </what-built>
  <how-to-verify>
    **Operator steps (execute each in sequence):**

    **20-UAT-1 — Portal card layout:**
    1. Launch app on device (or browser dev mode).
    2. Navigate to `/planner` (Planner tab in bottom nav).
    3. Scroll to the Suggested Moves section.
    4. Confirm each portal card displays: (a) topic/title, (b) colored border matching the move type, (c) 3 small tappable indicators showing counts for flashcards/posts/questions.
    5. Tap each indicator — confirm it is tappable (highlights / shows ripple).
    6. Report PASS if all 3 indicator types render with correct counts; FAIL with description otherwise.

    **20-UAT-2 — Diagnostic chat multi-turn:**
    1. From `/home`, trigger the diagnostic check-in modal (the entry point varies — consult 20-VERIFICATION.md for exact UI path).
    2. Submit the first prompt.
    3. Confirm the LLM returns a follow-up question.
    4. Reply to the follow-up.
    5. Confirm a second follow-up arrives (turn 2/3).
    6. Reply again.
    7. Confirm a 3rd-turn Done signal — the UI closes or shows summary after turn 3.
    8. Confirm the chat does NOT allow a 4th turn.
    9. Report PASS with turn count observed; FAIL with description otherwise.

    **20-UAT-3 — Portal card indicator navigation:**
    1. From `/planner` Suggested Moves, tap each of the 3 indicators (flashcard, post, Q) on a card.
    2. Confirm: flashcard icon → navigates to `/review`; post icon → `/posts/:id`; Q icon → `/ask/:id`.
    3. Report PASS if all 3 routings correct; FAIL with the broken route.

    **20-UAT-4 — Portal card primary CTA:**
    1. Tap the primary CTA button on a Suggested Move card.
    2. Confirm `navigateToMove` fires and routes correctly (varies by move type — heal→/review, replant→/posts/anchor-post-{id}, etc.).
    3. Report PASS with observed route; FAIL otherwise.

    **Recording (Claude does this):**
    For each of the 4 items, Claude edits `29-UAT-LOG.md` updating the row:
    - `Actual` column: operator's description
    - `Pass/Fail` column: PASS or FAIL
    - `Date` column: today's date
    - `Notes` column: any deviation, observation, or screenshot reference

    If any item FAILs, Claude marks the row with `FAIL` and defers the fix to task 29-04-05.
  </how-to-verify>
  </action>
  <verify>
    <automated>grep -cE "^\| 20-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - After operator session, all 4 rows `20-UAT-1` through `20-UAT-4` in `29-UAT-LOG.md` have non-empty `Actual`, `Pass/Fail`, and `Date` cells
    - `grep -cE "^\| 20-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0
    - Any FAIL rows are recorded with enough detail that task 29-04-05 can reproduce and fix
  </acceptance_criteria>
  <resume-signal>Operator types "phase-20 done" or describes results item-by-item; Claude records and proceeds to 29-04-02.</resume-signal>
  <done>
    All 4 Phase 20 UAT rows populated in `29-UAT-LOG.md` with operator observations. FAIL items flagged for task 5.
  </done>
</task>

<task id="29-04-02" type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (Checkpoint): Operator walks through Phase 21 UAT items (4 active, 1 SKIP)</name>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (confirm Phase 21 rows exist and are pending; 21-UAT-4 pre-marked SKIP)
    - .planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md (original test + expected)
    - .planning/phases/29-final-polishment/29-RESEARCH.md (Phase 21 item notes lines 617-644)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-22, D-23)
  </read_first>
  <files>.planning/phases/29-final-polishment/29-UAT-LOG.md</files>
  <action>
    <what-built>
    Phase 21 shipped generate-on-enter post essays (streaming bodyMarkdown when user opens a post card) and a review cap fix. 4 active items cover:
    - Feed load time (<3s, card-face-only posts)
    - On-enter streaming UX (UI shell immediate, essay streams with no layout shift)
    - Cache-hit on re-visit (cached essay loads instantly)
    - Video/news posts stream summaries on-enter

    21-UAT-4 (daily goal progress bar) is pre-marked SKIP per D-23.
  </what-built>
  <how-to-verify>
    **Operator steps:**

    **21-UAT-1 — Feed load time:**
    1. Launch app. Navigate to `/home`.
    2. Start a stopwatch the moment the Home tab is active.
    3. Observe when the feed (post cards) finishes initial render.
    4. Report PASS if < 3s; FAIL with measured time otherwise.

    **21-UAT-2 — On-enter streaming UX:**
    1. From `/home`, tap any post card that does NOT have a cached essay.
    2. Confirm: UI shell (title, card chrome) renders immediately (< 500ms).
    3. Confirm: the essay body streams token-by-token WITHOUT causing layout shift (text area expands downward, chrome stays stable).
    4. Report PASS / FAIL with specific visual bug description.

    **21-UAT-3 — Cache-hit on re-visit:**
    1. Open a post (wait for full stream completion).
    2. Navigate away (back button).
    3. Re-open the same post.
    4. Confirm: cached essay appears INSTANTLY (no streaming animation; no network round-trip).
    5. Report PASS / FAIL.

    **21-UAT-4 — SKIP** (daily goal progress bar — REVIEW-03 descoped; row already marked SKIP).

    **21-UAT-5 — Video/news posts stream on-enter:**
    1. From `/home`, identify a video post (YouTube embed) or news post (news source).
    2. Tap to open.
    3. Confirm: summary/essay streams on-enter for these post types (not just standard posts).
    4. Report PASS / FAIL with post type tested.

    Claude records results in `29-UAT-LOG.md`.
  </how-to-verify>
  </action>
  <verify>
    <automated>grep -cE "^\| 21-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - After operator session, all 4 active rows (21-UAT-1, 21-UAT-2, 21-UAT-3, 21-UAT-5) have non-empty `Actual`, `Pass/Fail`, `Date` cells
    - `21-UAT-4` row remains unchanged (SKIP, no walkthrough performed)
    - `grep -cE "^\| 21-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0
  </acceptance_criteria>
  <resume-signal>Operator types "phase-21 done" or describes results item-by-item.</resume-signal>
  <done>
    All 4 active Phase 21 UAT rows populated. 21-UAT-4 remains SKIP. FAIL items flagged for task 5.
  </done>
</task>

<task id="29-04-03" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): Operator walks through Phase 22 UAT items (9 active, 2 SKIP)</name>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (confirm Phase 22 rows)
    - .planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md (including the 2026-04-15 addendum that reverts items 4+5)
    - .planning/phases/29-final-polishment/29-RESEARCH.md (Phase 22 item breakdown lines 646-662)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-22, D-23)
  </read_first>
  <files>.planning/phases/29-final-polishment/29-UAT-LOG.md</files>
  <action>
    <what-built>
    Phase 22 shipped finger-swipe navigation between the 5 first-level screens (Home/Planner/Ask/Graph/Settings) with rubber-band edges, axis-lock threshold, and nested-gesture guards. The 2026-04-15 addendum REVERTED animated tab-tap back to instant transport. 9 items cover bottom nav, rubber-band, snap-back, carousel/mind-elixir/keyboard conflict suppression, GraphScreen first-swipe, sub-screen disable, scroll position preservation.

    22-UAT-4, 22-UAT-5 are pre-marked SKIP per D-23 (reverted).
  </what-built>
  <how-to-verify>
    **Operator steps (9 items):**

    **22-UAT-1 — Bottom nav real-time tracking:** Swipe between screens. Confirm the bottom nav active-tab indicator moves IN SYNC with the finger drag (not just snapping at the end).

    **22-UAT-2 — Rubber-band edge resistance:** On `/home` (leftmost), try to swipe right-to-left past the edge. Confirm rubber-band resistance (can drag a little, snaps back).

    **22-UAT-3 — Snap-back on short swipe:** Start a swipe but release before 20% threshold. Confirm the strip snaps back to current screen (no screen change).

    **22-UAT-4 — SKIP** (animated tab-tap — reverted).
    **22-UAT-5 — SKIP** (non-adjacent tab-tap slide — reverted).

    **22-UAT-6 — PostCarousel conflict suppression:** On a screen showing PostCarousel (if applicable), swipe horizontally within the carousel. Confirm the outer first-level swipe does NOT trigger.

    **22-UAT-7 — MindElixir pan conflict suppression:** Open `/graph`. Pan the graph canvas horizontally. Confirm the outer swipe does NOT trigger (graph pan wins).

    **22-UAT-8 — Keyboard-open swipe suppression:** Open AskScreen, focus the chat input (keyboard opens). Try to swipe. Confirm swipe is suppressed (no first-level navigation while keyboard is up).

    **22-UAT-9 — GraphScreen visible on first swipe:** Start the app fresh. Swipe to `/graph` for the first time. Confirm the screen renders correctly on first swipe (not blank / white / broken).

    **22-UAT-10 — Sub-screen swipe disabled:** Navigate to a sub-screen (e.g., `/posts/:id` or `/review`). Try to swipe horizontally. Confirm no first-level navigation fires.

    **22-UAT-11 — Scroll position preservation:** Scroll `/home` feed halfway down. Swipe to `/planner`. Swipe back to `/home`. Confirm scroll position is preserved (not reset to top).

    Claude records results in `29-UAT-LOG.md`.
  </how-to-verify>
  </action>
  <verify>
    <automated>grep -cE "^\| 22-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - After operator session, all 9 active rows (22-UAT-1,2,3,6,7,8,9,10,11) have non-empty `Actual`, `Pass/Fail`, `Date` cells
    - 22-UAT-4 and 22-UAT-5 rows unchanged (SKIP)
    - `grep -cE "^\| 22-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0
  </acceptance_criteria>
  <resume-signal>Operator types "phase-22 done" or item-by-item pass/fail reports.</resume-signal>
  <done>
    All 9 active Phase 22 UAT rows populated. 22-UAT-4, 22-UAT-5 remain SKIP.
  </done>
</task>

<task id="29-04-04" type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (Checkpoint): Operator walks through Phase 26 UAT items (7 items)</name>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (confirm Phase 26 rows)
    - .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md (original tests + the simplifications from UAT per project memory)
    - .planning/phases/29-final-polishment/29-RESEARCH.md (Phase 26 item list lines 665-674)
    - ~/.claude/projects/-Users-Code-EchoLearn/memory/MEMORY.md (Trellis section — simplified behavior: harvest button direct-tap, no bottom sheets, re-plant does NOT reset flashcards)
  </read_first>
  <files>.planning/phases/29-final-polishment/29-UAT-LOG.md</files>
  <action>
    <what-built>
    Phase 26 shipped the flat Trellis status panel (Dying | Fruit | Dead) with direct-tap harvest + fly-to-counter + confetti, and action rows in Suggested Moves (heal / re-plant / prune). 7 items cover harvest animation, fruit glow, heal flow, replant flow, prune button, priority ordering, autoGen dedup.
  </what-built>
  <how-to-verify>
    **Operator steps (7 items):**

    **26-UAT-1 — Harvest animation:**
    1. Navigate to `/planner`, find a fruit node in the TrellisStatusPanel (center column).
    2. Tap the fruit button.
    3. Confirm: node flies from panel to the fruit counter (top of screen or settings icon), and confetti particles trigger.
    4. Confirm the counter increments by the harvested count.

    **26-UAT-2 — Fruit column glow:**
    1. If fruit count > 0, confirm the Fruit column has a visible glow/highlight state.
    2. If fruit count = 0, confirm no glow.

    **26-UAT-3 — Heal flow:**
    1. In Suggested Moves, find a dying anchor row (yellow/falling state).
    2. Tap the ROW (not a child button).
    3. Confirm: the anchor is added to the podcast queue AND the app navigates to `/review`.

    **26-UAT-4 — Re-plant flow:**
    1. In Suggested Moves, find a dead anchor row (fallen state).
    2. Tap the ROW.
    3. Confirm: schedules for the anchor + its Q&A are reset to "dying" (yesterday review date, reviewCount ≥ 1) — does NOT reset flashcards per simplified behavior.
    4. Confirm navigation to `/posts/anchor-post-{id}` (Learn-as-post flow).
    5. Confirm a toast appears.

    **26-UAT-5 — Prune button (scissors):**
    1. On a dying or dead row, tap the scissors button.
    2. Confirm: the anchor is archived (flagged `prunedFromTrellis: true`), the PrunedSection at the bottom of PlannerScreen shows the pruned item, and the tap did NOT trigger the row's heal/replant behavior (stopPropagation).

    **26-UAT-6 — Suggested Moves priority ordering:**
    1. View Suggested Moves.
    2. Confirm ordering: dead items FIRST, dying items SECOND, autoGen items THIRD.

    **26-UAT-7 — AutoGen dedup:**
    1. If an anchor appears in both trellis rows AND would have appeared in autoGen moves, confirm it appears ONLY in trellis rows (autoGen dedup filter from PlannerScreen.tsx:54).

    Claude records results in `29-UAT-LOG.md`.
  </how-to-verify>
  </action>
  <verify>
    <automated>grep -cE "^\| 26-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - After operator session, all 7 rows (26-UAT-1 through 26-UAT-7) have non-empty `Actual`, `Pass/Fail`, `Date` cells
    - `grep -cE "^\| 26-UAT-.+\| pending \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0
  </acceptance_criteria>
  <resume-signal>Operator types "phase-26 done" or item-by-item pass/fail reports.</resume-signal>
  <done>
    All 7 Phase 26 UAT rows populated. FAIL items flagged for task 5.
  </done>
</task>

<task id="29-04-05" type="auto">
  <name>Task 5: Fix sub-loop — for each FAIL row + each walkthrough-surfaced issue, produce an inline fix commit</name>
  <files>(variable — whatever files the fix requires)</files>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (every row with Pass/Fail = FAIL + the "Walkthrough-surfaced issues" table)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-03 inline-fix mandate, D-10 one-commit-per-fix, D-11 unbounded, D-22 operator describes bug)
    - The relevant source file(s) for each failing behavior (identify from the UAT item test description)
  </read_first>
  <action>
    **Unbounded sub-loop per D-10, D-11:**

    For EACH row in `29-UAT-LOG.md` currently marked `FAIL` AND for EACH entry in the "Walkthrough-surfaced issues" table:

    1. Operator describes the bug in concrete steps-to-reproduce (this may happen interactively during tasks 1-4 or in a dedicated follow-up session — Claude asks clarifying questions as needed).
    2. Claude identifies the source file(s) responsible, reads current state, and implements a fix.
    3. Run relevant tests (if any exist): `cd app && node --test tests/...` for the affected subsystem.
    4. Commit the fix individually:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "fix(29-04): {one-line description} [refs {item-id}]" --files {affected files}
       ```
       Commit message format (per D-10, from 29-CONTEXT.md <specifics>): `fix(29-04): {description}` referencing the item ID (e.g., `fix(29-04): restore scroll preservation on Planner→Home swipe [refs 22-UAT-11]`).
    5. Update the UAT-LOG.md row:
       - `Pass/Fail` cell: change from `FAIL` to `PASS` (post-fix re-test)
       - `Fix Commit` cell: insert the git SHA
       - `Notes` cell: append fix summary
    6. Operator re-tests the fix on-device to confirm. If re-test still FAILs, iterate (steps 2-5 again with a different fix). No size cap.
    7. Repeat for every FAIL row.

    **Newly-surfaced issues (not on the 22-item list):**

    If the operator spots bugs during walkthrough that are NOT in the original VERIFICATION.md items (per D-03 "inline-fix whatever the walkthrough turns up"), add them to the "Walkthrough-surfaced issues" table at the bottom of `29-UAT-LOG.md`:

    | Issue | Surfaced During | Fix Commit | Re-test Result | Date |
    |-------|-----------------|-----------|----------------|------|
    | {description} | {task 1..4 item ID or general observation} | {sha} | PASS/FAIL | {date} |

    Then fix + commit + re-test same as above.

    **Scope rule:** Walkthrough-surfaced fixes are still bounded to v1.3 behaviors. If operator discovers a v1.4 or Phase-27-i18n issue, do NOT fix here — defer to a v1.4 issue. Claude decides the boundary; when unclear, ask operator.

    **Test integrity:** If a fix touches a file that has automated tests, run those tests before committing to confirm no regression. Also run `npm test` before the final task 29-04-06 to confirm the whole suite is green.

    **Zero FAILs case:** If all 22 active items PASS on first walkthrough AND the operator reports no incidental bugs, this task trivially completes. Mark the task done by noting "no fixes required" in the UAT-LOG.md summary.
  </action>
  <verify>
    <automated>grep -c "FAIL" .planning/phases/29-final-polishment/29-UAT-LOG.md | awk '{ if ($1 == 0) print "ALL-PASS"; else print "FAIL-STILL-OPEN-" $1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Every `FAIL` in `29-UAT-LOG.md` has a corresponding `Fix Commit` SHA recorded
    - Every walkthrough-surfaced issue in the "Walkthrough-surfaced issues" table has `Fix Commit` SHA and `Re-test Result: PASS`
    - `grep -cE "^\|.*\| FAIL \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0 after all fixes committed
    - `git log --grep='fix(29-04):' --oneline` shows one commit per fix (no bundled commits per D-10)
    - `cd app && npm test` still green (no regressions from fixes)
  </acceptance_criteria>
  <done>
    Zero FAIL rows remain. Every walkthrough-surfaced issue has been fixed and re-tested. Full test suite green.
  </done>
</task>

<task id="29-04-06" type="edit">
  <name>Task 6: Flip archived VERIFICATION.md frontmatter status: human_needed → passed + add re_verification block</name>
  <files>.planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md, .planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md, .planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md, .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (must show zero FAIL rows — precondition for this task)
    - Each of the 4 archived VERIFICATION.md files (to preserve existing frontmatter except the fields being updated)
    - .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md (CANONICAL schema for re_verification block — lines 6-18 — per D-21)
    - .planning/phases/29-final-polishment/29-CONTEXT.md (D-21)
    - .planning/phases/29-final-polishment/29-RESEARCH.md (re_verification template lines 712-744)
  </read_first>
  <action>
    For EACH of the 4 VERIFICATION.md files, modify the frontmatter:

    1. Flip `status: human_needed` → `status: passed`
    2. Add a `re_verification:` block immediately after `status:` (schema from 26-VERIFICATION.md:6-18):

    **Phase 20 template:**
    ```yaml
    ---
    phase: 20-orchestration-strategy-diagnostic-dialogue
    verified: {original date}
    status: passed              # <-- flip from human_needed
    score: {original score}
    re_verification:
      previous_status: human_needed
      previous_score: {original score}
      re_verified_on: {date task completed, YYYY-MM-DD}
      gaps_closed:
        - "Portal card layout + 3 content-type indicators confirmed (20-UAT-1)"
        - "Diagnostic chat multi-turn 3-turn limit confirmed (20-UAT-2)"
        - "Portal card indicator navigation routing confirmed (20-UAT-3)"
        - "Portal card primary CTA navigateToMove routing confirmed (20-UAT-4)"
      gaps_remaining: []
      regressions: []
      log: .planning/phases/29-final-polishment/29-UAT-LOG.md
    ---
    ```

    **Phase 21 template (note 21-UAT-4 in gaps_remaining with descope note):**
    ```yaml
    ---
    phase: 21-review-cap-fix-generate-on-enter-posts
    verified: {original}
    status: passed
    score: {original}
    re_verification:
      previous_status: human_needed
      previous_score: {original}
      re_verified_on: {date}
      gaps_closed:
        - "Feed load time <3s confirmed (21-UAT-1)"
        - "On-enter streaming UX no layout shift confirmed (21-UAT-2)"
        - "Cache-hit on re-visit instant-load confirmed (21-UAT-3)"
        - "Video/news post streaming on-enter confirmed (21-UAT-5)"
      gaps_remaining:
        - "21-UAT-4 daily goal progress bar — SKIP per D-23 (REVIEW-03 descoped per feedback_daily_goal.md); already marked N/A in original verification"
      regressions: []
      log: .planning/phases/29-final-polishment/29-UAT-LOG.md
    ---
    ```

    **Phase 22 template (note 22-UAT-4, 22-UAT-5 in gaps_remaining as reverted):**
    ```yaml
    ---
    phase: 22-swipe-navigation-between-first-level-screens
    verified: {original}
    status: passed
    score: {original}
    re_verification:
      previous_status: human_needed
      previous_score: {original}
      re_verified_on: {date}
      gaps_closed:
        - "Bottom nav real-time tracking (22-UAT-1)"
        - "Rubber-band edge resistance (22-UAT-2)"
        - "Snap-back on short swipe (22-UAT-3)"
        - "PostCarousel swipe conflict suppression (22-UAT-6)"
        - "MindElixir pan conflict suppression (22-UAT-7)"
        - "Keyboard-open swipe suppression (22-UAT-8)"
        - "GraphScreen visible on first swipe (22-UAT-9)"
        - "Sub-screen swipe disabled (22-UAT-10)"
        - "Scroll position preservation (22-UAT-11)"
      gaps_remaining:
        - "22-UAT-4 animated tab-tap — SKIP per D-23; reverted to instant transport 2026-04-15 (see 22-VERIFICATION.md addendum)"
        - "22-UAT-5 non-adjacent tab-tap direct slide — SKIP per D-23; reverted same date"
      regressions: []
      log: .planning/phases/29-final-polishment/29-UAT-LOG.md
    ---
    ```

    **Phase 26 template:**
    ```yaml
    ---
    phase: 26-trellis-harvest-panel-unified-heal-replant-prune-actions
    verified: {original}
    status: passed
    score: {original}
    re_verification:
      previous_status: human_needed
      previous_score: {original}
      re_verified_on: {date}
      gaps_closed:
        - "Harvest animation: fly-to-counter + confetti (26-UAT-1)"
        - "Fruit column glow when count > 0 (26-UAT-2)"
        - "Heal flow: dying tap → podcast + /review (26-UAT-3)"
        - "Re-plant flow: dead tap → schedule reset + post generation + toast + /review (26-UAT-4)"
        - "Prune scissors: archive + PrunedSection + stopPropagation (26-UAT-5)"
        - "Suggested Moves priority: dead → dying → autoGen (26-UAT-6)"
        - "AutoGen dedup against trellis rows (26-UAT-7)"
      gaps_remaining: []
      regressions: []
      log: .planning/phases/29-final-polishment/29-UAT-LOG.md
    ---
    ```

    Preserve all other original frontmatter fields (phase, verified, score, author, etc.). Only insert the `re_verification` block + flip `status`. Do NOT modify the body of any VERIFICATION.md — only the frontmatter.

    If any UAT item ended as FAIL→fix→re-test→PASS, mention the fix commit SHA in the corresponding `gaps_closed` bullet for that item (e.g., `"Scroll position preservation (22-UAT-11; fixed in {sha})"`).

    **Also update `29-UAT-LOG.md` frontmatter:** set `completed: {date}` and mark the Sign-off checklist items as complete.
  </action>
  <verify>
    <automated>grep -l "status: passed" .planning/milestones/v1.3-phases/20-orchestration-strategy-diagnostic-dialogue/20-VERIFICATION.md .planning/milestones/v1.3-phases/21-review-cap-fix-generate-on-enter-posts/21-VERIFICATION.md .planning/milestones/v1.3-phases/22-swipe-navigation-between-first-level-screens/22-VERIFICATION.md .planning/milestones/v1.3-phases/26-trellis-harvest-panel-unified-heal-replant-prune-actions/26-VERIFICATION.md | wc -l | grep -qx 4 && echo "ALL-4-FLIPPED"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 VERIFICATION.md files contain `status: passed` (grep -l returns 4 paths)
    - All 4 files contain a `re_verification:` block with `previous_status: human_needed`, `re_verified_on:`, `gaps_closed`, `gaps_remaining`, `regressions`, `log` fields
    - `grep -l "re_verification:" .planning/milestones/v1.3-phases/{20,21,22,26}-*/*-VERIFICATION.md | wc -l` returns 4
    - Phase 21 VERIFICATION.md gaps_remaining includes a note for 21-UAT-4 SKIP (D-23 reason cited)
    - Phase 22 VERIFICATION.md gaps_remaining includes notes for 22-UAT-4 AND 22-UAT-5 SKIPs (D-23 reason cited)
    - `log: .planning/phases/29-final-polishment/29-UAT-LOG.md` appears in all 4 files (grep count >= 4 across the files)
    - `grep -c "status: human_needed" .planning/milestones/v1.3-phases/{20,21,22,26}-*/*-VERIFICATION.md` returns 0 (no residual human_needed status)
    - `29-UAT-LOG.md` frontmatter `completed:` field is filled with today's date
  </acceptance_criteria>
  <done>
    All 4 archived VERIFICATION.md files show status: passed with full re_verification blocks. UAT-LOG.md marked complete. Phase 29 is now closed from the archived-phases perspective.
  </done>
</task>

</tasks>

<verification>
- `.planning/phases/29-final-polishment/29-UAT-LOG.md` exists; all 22 active rows show PASS (or FAIL→fixed→PASS); all 3 SKIP rows documented with D-23 reason
- `grep -l "status: passed" .planning/milestones/v1.3-phases/{20,21,22,26}-*/*-VERIFICATION.md | wc -l` returns 4
- `grep -l "re_verification:" .planning/milestones/v1.3-phases/{20,21,22,26}-*/*-VERIFICATION.md | wc -l` returns 4
- `grep -cE "^\|.*\| FAIL \|" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns 0
- `git log --grep='fix(29-04):' --oneline` shows individual commits per fix (one-per-fix per D-10)
- `cd app && npm test` still exits 0
- `cd app && npm run build` still exits 0
</verification>

<success_criteria>
Plan 29-04 complete when:
- [ ] 29-UAT-LOG.md exists with all 22 active rows + 3 SKIP rows
- [ ] Every active row has Pass/Fail populated (PASS after any needed fix)
- [ ] Every FAIL has a fix commit SHA recorded
- [ ] Walkthrough-surfaced issues (if any) are all fixed and re-tested
- [ ] All 4 archived VERIFICATION.md files flipped status: human_needed → passed with re_verification blocks
- [ ] UAT-LOG.md sign-off checklist all ticked
- [ ] Full test suite green; vite build green
</success_criteria>

<output>
After completion, create `.planning/phases/29-final-polishment/29-04-SUMMARY.md` per the standard summary template. Include:
- Total items tested: 22
- Total items SKIP: 3
- Total walkthrough-surfaced issues: {N}
- Total inline fix commits: {N}
- Archived phases closed: 20, 21, 22, 26
</output>
