---
phase: 28-ui-ux-polish-from-audit-findings
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - app/src/components/trellis/TrellisLeaf.tsx
  - app/src/components/trellis/TrellisCanvas.tsx
  - app/src/components/trellis/TrellisHero.tsx
  - app/src/screens/PlannerScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/components/TrellisLeaf.shake.test.mjs
  - app/tests/components/TrellisCanvas.focus.test.mjs
  - app/tests/services/trellis-perf-mask.test.mjs
  - app/tests/locales/bundle-parity.test.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Tapping any trellis leaf (any state: bud/green/yellow/falling/fallen/blossom/fruit) triggers ~300ms shake + hapticImpactLight"
    - "Wave 0 unit test (TrellisLeaf.shake.test.mjs) mocks `@capacitor/haptics` via injected haptic fn and asserts `hapticImpactLight()` is invoked exactly once per non-perf-guarded leaf tap (D-11 Nyquist)"
    - "Tapping a Suggested Moves row (dead/dying action) on PlannerScreen emits focusedAnchorId on pointerDown BEFORE navigation, causing the corresponding leaf to pulse (scale 1→1.15→1, glow drop-shadow 0 0 8px var(--primary-40), ~2s fade)"
    - "When layout.nodes.length > 30, shake/pulse animations run only on viewport-visible leaves (count-gated at minimum, IntersectionObserver optionally layered)"
    - "Graph screen header reads 'Knowledge Graph' / '知识图谱' / 'Grafo de conocimiento' / 'ナレッジグラフ' depending on locale (value swap on existing graph.headerTitle key)"
    - "Bundle parity test extended: asserts graph.headerTitle value equals expected literal per locale, not just key-set parity"
    - "Existing ambient sway on trellis leaves is preserved (not reset or broken by nested motion.g wrappers)"
    - "Repeated taps on the same Suggested Moves row re-trigger the pulse (key={`pulse-${anchorId}-${focusCounter}`} ensures re-mount)"
  artifacts:
    - path: "app/src/components/trellis/TrellisLeaf.tsx"
      provides: "Shake-on-tap + pulse-on-focus + hapticImpactLight integration; exports pure onLeafTap factory for D-11 test"
      contains: "useAnimationControls"
    - path: "app/src/components/trellis/TrellisCanvas.tsx"
      provides: "pointerEvents: 'auto' on individual leaf <g>; focusedAnchorId thread; perf guard at 30-leaf threshold"
      contains: "TAP_ANIMATION_THRESHOLD"
    - path: "app/src/components/trellis/TrellisHero.tsx"
      provides: "focusedAnchorId prop forwarding"
      contains: "focusedAnchorId"
    - path: "app/src/screens/PlannerScreen.tsx"
      provides: "focusedAnchorId state + onPointerDown emission on row + setTimeout 2000ms auto-clear"
      contains: "focusedAnchorId"
    - path: "app/src/locales/en.json"
      provides: "graph.headerTitle value = 'Knowledge Graph'"
      contains: "Knowledge Graph"
    - path: "app/src/locales/zh.json"
      provides: "graph.headerTitle value = '知识图谱'"
      contains: "知识图谱"
    - path: "app/src/locales/es.json"
      provides: "graph.headerTitle value = 'Grafo de conocimiento'"
      contains: "Grafo de conocimiento"
    - path: "app/src/locales/ja.json"
      provides: "graph.headerTitle value = 'ナレッジグラフ'"
      contains: "ナレッジグラフ"
    - path: "app/tests/components/TrellisLeaf.shake.test.mjs"
      provides: "Pure-logic test for shake variant rotate array [0, 4, -4, 2, 0] + D-11 haptic invocation assertion via mocked haptic injection"
      contains: "hapticImpactLight"
    - path: "app/tests/components/TrellisCanvas.focus.test.mjs"
      provides: "Pure-logic test: focused prop propagation given focusedAnchorId"
      contains: "focused"
    - path: "app/tests/services/trellis-perf-mask.test.mjs"
      provides: "leafAnimationMask(count, index, inView) predicate test"
      contains: "leafAnimationMask"
  key_links:
    - from: "app/src/screens/PlannerScreen.tsx"
      to: "app/src/components/trellis/TrellisHero.tsx"
      via: "focusedAnchorId prop"
      pattern: "focusedAnchorId=\\{"
    - from: "app/src/components/trellis/TrellisHero.tsx"
      to: "app/src/components/trellis/TrellisCanvas.tsx"
      via: "focusedAnchorId prop"
      pattern: "focusedAnchorId=\\{"
    - from: "app/src/components/trellis/TrellisCanvas.tsx"
      to: "app/src/components/trellis/TrellisLeaf.tsx"
      via: "focused prop (derived from anchorId match)"
      pattern: "focused=\\{"
    - from: "app/src/components/trellis/TrellisLeaf.tsx"
      to: "app/src/lib/haptics.ts"
      via: "hapticImpactLight() call on tap"
      pattern: "hapticImpactLight"
    - from: "app/src/screens/GraphScreen.tsx"
      to: "app/src/locales/{en,zh,es,ja}.json"
      via: "t('graph.headerTitle')"
      pattern: "graph.headerTitle"
---

<objective>
Land Wave C (trellis micro-interactions + Knowledge Graph rename) for Phase 28.

Purpose: Give the trellis tactile personality via tap-to-shake + haptic (D-10/D-11), link Suggested Moves to their anchor leaves via pulse-on-focus (D-12), guard perf when there are many leaves (D-13), and rename "Mind Map" to "Knowledge Graph" across all 4 locale bundles via i18n value swap (D-14). Foundation (D-26 tokens, D-04 header polish) landed in Plan 28-01.

Output: Modified TrellisLeaf + TrellisCanvas + TrellisHero + PlannerScreen + 4 locale bundles. 3 new Wave 0 test files, 1 existing bundle-parity extended. All tests green. Ambient sway preserved. Tsc/vite clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ui-ux-polish-from-audit-findings/28-CONTEXT.md
@.planning/phases/28-ui-ux-polish-from-audit-findings/28-RESEARCH.md
@.planning/phases/28-ui-ux-polish-from-audit-findings/28-UI-SPEC.md
@.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md
@.planning/phases/28-01-SUMMARY.md
@.planning/phases/25-anime-knowledge-tree-for-planner-page-motivational-review-visualization/25-CONTEXT.md
@.planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key contracts extracted from live source at 2026-04-16. Executor should use these directly — no codebase exploration needed. -->

From app/src/components/trellis/TrellisLeaf.tsx:
```typescript
// ~line 516: existing outer motion.g with ambient sway animation — DO NOT REMOVE
// Current: animate={{ scale, opacity, rotate: [rotation, +3, -3, rotation] }}
// transition.rotate: { duration: 3, repeat: Infinity }
// Task adds nested inner motion.g for shake + pulse — preserves ambient sway on outer wrapper
// Props currently do NOT include `focused` — add it
```

From app/src/components/trellis/TrellisCanvas.tsx:
```typescript
// ~line 32: svgProps = { style: { pointerEvents: 'none' } } — the SVG root
// ~line 68: leaves rendered in a <g> iterator
// Existing perf: AMBIENT_SWAY_THRESHOLD = 20 (Phase 25 D-55 convention)
// Add: TAP_ANIMATION_THRESHOLD = 30 (NOT 20 — RESEARCH Open Question 3 locks 30 for tap animations)
```

From app/src/components/trellis/TrellisHero.tsx:
```typescript
// Currently takes no props — self-sources layout via useTrellisData()
// Needs new prop: focusedAnchorId?: string (forward to TrellisCanvas)
```

From app/src/screens/PlannerScreen.tsx:
```typescript
// Row onClick at ~line 176 (dead → handleReplant) and ~line 224 (dying → handleHeal) already navigate
// Add state: const [focusedAnchorId, setFocusedAnchorId] = useState<string | null>(null);
// Add onPointerDown on row: sets focusedAnchorId = node.id, then setTimeout clear after 2000ms
// Pass focusedAnchorId into TrellisHero
```

From app/src/lib/haptics.ts:
```typescript
export const hapticImpactLight: () => Promise<void>; // Web no-op, Capacitor native fires
```

From app/src/locales/en.json (lines 194-195 verified in RESEARCH):
```json
"graph": {
  "title": "Knowledge Graph",     // already exists (vestigial — not rendered)
  "headerTitle": "Mind Map",     // THIS is the rendered key by GraphScreen.tsx:522 — SWAP this value
  ... other graph keys
}
```

Locale values to set (per UI-SPEC Copywriting Contract):
- en: "Knowledge Graph"
- zh: "知识图谱"
- es: "Grafo de conocimiento"
- ja: "ナレッジグラフ"

From app/tests/locales/bundle-parity.test.mjs:
```javascript
// Existing: asserts identical key sets across 4 bundles
// Extend: add a value-level assertion for graph.headerTitle matching the 4 expected strings above
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests (shake variant + focus propagation + perf mask) + bundle-parity value extension</name>
  <files>
    app/tests/components/TrellisLeaf.shake.test.mjs,
    app/tests/components/TrellisCanvas.focus.test.mjs,
    app/tests/services/trellis-perf-mask.test.mjs,
    app/tests/locales/bundle-parity.test.mjs,
    app/src/services/trellis-perf-mask.ts
  </files>
  <read_first>
    - app/tests/components/_trellis-tsx-loader.mjs (if present — the esbuild tsx loader pattern used by trellis tests)
    - app/tests/canonical-knowledge.test.mjs (reference node --test pattern)
    - app/tests/locales/bundle-parity.test.mjs (existing test — append value assertion, do NOT rewrite)
    - app/src/components/trellis/TrellisLeaf.tsx (need to know final export surface for the SHAKE_KEYFRAMES constant)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md (lines 68-78 for the 5 Wave 0 requirements)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-RESEARCH.md (Validation Architecture lines 656-697)
  </read_first>
  <behavior>
    - Test 1 (TrellisLeaf.shake.test.mjs): `SHAKE_KEYFRAMES` exported constant equals `[0, 4, -4, 2, 0]` exactly. `SHAKE_DURATION_MS` equals `300`.
    - Test 2 (TrellisCanvas.focus.test.mjs): Pure helper `isLeafFocused(focusedAnchorId, leafAnchorId)` returns true when strings equal, false when mismatched, false when focusedAnchorId is null/undefined.
    - Test 3 (trellis-perf-mask.test.mjs): `leafAnimationMask({ totalCount: N, inView: bool })` returns `true` when (totalCount ≤ 30) OR (totalCount > 30 AND inView === true); returns `false` when totalCount > 30 AND inView === false.
    - Test 4 (bundle-parity extension): For each locale in ['en','zh','es','ja'], `bundle.graph.headerTitle` equals the expected string per the locked mapping.
    - Test 5 (TrellisLeaf.shake.test.mjs, D-11 haptic): given a mocked `@capacitor/haptics` module, when a pure-logic `onLeafTap` handler is invoked (not perf-guarded), `hapticImpactLight()` is called exactly once. Asserted via a counter spy installed around the imported `hapticImpactLight` from `app/src/lib/haptics.ts` — this validates D-11 Nyquist requirement without requiring DOM render.
  </behavior>
  <action>
    **Step 1 — Create `app/src/services/trellis-perf-mask.ts`:**

    ```typescript
    export interface LeafAnimationMaskInput {
      totalCount: number;
      inView: boolean;
    }

    export const TAP_ANIMATION_THRESHOLD = 30;

    export const leafAnimationMask = ({ totalCount, inView }: LeafAnimationMaskInput): boolean => {
      if (totalCount <= TAP_ANIMATION_THRESHOLD) return true;
      return inView === true;
    };
    ```

    **Step 2 — Export constants from `app/src/components/trellis/TrellisLeaf.tsx`:**

    At the top of the file (after imports), add and export (these may already exist inside the component — hoist them to module scope):

    ```typescript
    export const SHAKE_KEYFRAMES: [number, number, number, number, number] = [0, 4, -4, 2, 0];
    export const SHAKE_DURATION_MS = 300;
    ```

    Only hoist/export — the actual consumption in the component happens in Task 2. If these constants already live inside the component body, move them to module scope.

    **Step 3 — Export helper from `app/src/components/trellis/TrellisCanvas.tsx`:**

    At module scope (above the component), add:

    ```typescript
    export const isLeafFocused = (focusedAnchorId: string | null | undefined, leafAnchorId: string | null | undefined): boolean => {
      if (!focusedAnchorId || !leafAnchorId) return false;
      return focusedAnchorId === leafAnchorId;
    };
    ```

    **Step 4 — Create 3 new test files:**

    (a) `app/tests/components/TrellisLeaf.shake.test.mjs`:

    ```javascript
    import { test, mock } from 'node:test';
    import assert from 'node:assert/strict';
    // Use same esbuild tsx loader pattern as other trellis tests
    // If _trellis-tsx-loader.mjs exists, use it via node --import or --loader flag in package.json scripts
    const { SHAKE_KEYFRAMES, SHAKE_DURATION_MS, onLeafTap } = await import('../../src/components/trellis/TrellisLeaf.tsx');

    test('SHAKE_KEYFRAMES is exactly [0, 4, -4, 2, 0]', () => {
      assert.deepStrictEqual(SHAKE_KEYFRAMES, [0, 4, -4, 2, 0]);
    });

    test('SHAKE_DURATION_MS is 300', () => {
      assert.equal(SHAKE_DURATION_MS, 300);
    });

    // D-11 Nyquist test: pure-logic onLeafTap helper invokes haptic
    test('onLeafTap invokes hapticImpactLight exactly once when not perf-guarded', async () => {
      // Install a counting spy by swapping the haptics module's export via dynamic mock
      // Option 1 (preferred): Use test.mock.module if available, OR
      // Option 2 (fallback): Export onLeafTap as a factory that accepts injected haptic fn:
      //   export const onLeafTap = ({ perfGuardActive, shakeControls, haptic }) => { ... }
      // Then test: const spy = mock.fn(); onLeafTap({ perfGuardActive: false, shakeControls: { start: () => {} }, haptic: spy }); assert.equal(spy.mock.callCount(), 1);
      const spy = mock.fn();
      onLeafTap({ perfGuardActive: false, shakeControls: { start: () => {} }, haptic: spy });
      assert.equal(spy.mock.callCount(), 1);
    });

    test('onLeafTap does NOT invoke haptic when perfGuardActive is true (D-13 cross-check)', () => {
      const spy = mock.fn();
      onLeafTap({ perfGuardActive: true, shakeControls: { start: () => {} }, haptic: spy });
      assert.equal(spy.mock.callCount(), 0);
    });
    ```

    To make Tests 3–4 above testable as pure logic, ALSO export a factory helper `onLeafTap` from `TrellisLeaf.tsx` at module scope (alongside SHAKE_KEYFRAMES / SHAKE_DURATION_MS). Task 2 consumes this helper from within the component:

    ```typescript
    export interface OnLeafTapDeps {
      perfGuardActive: boolean;
      shakeControls: { start: (animate: unknown) => unknown };
      haptic: () => void | Promise<void>;
    }

    export const onLeafTap = ({ perfGuardActive, shakeControls, haptic }: OnLeafTapDeps): void => {
      if (perfGuardActive) return;
      void haptic();
      void shakeControls.start({
        rotate: SHAKE_KEYFRAMES,
        transition: { duration: SHAKE_DURATION_MS / 1000, ease: 'easeInOut' },
      });
    };
    ```

    Task 2's `handleTap` (inside the component) becomes a thin wrapper: `useCallback(() => onLeafTap({ perfGuardActive, shakeControls, haptic: hapticImpactLight }), [...])`. This gives D-11 a clean pure-logic test surface while preserving runtime behavior.

    (b) `app/tests/components/TrellisCanvas.focus.test.mjs`:

    ```javascript
    import { test } from 'node:test';
    import assert from 'node:assert/strict';
    const { isLeafFocused } = await import('../../src/components/trellis/TrellisCanvas.tsx');

    test('isLeafFocused returns true on match', () => {
      assert.equal(isLeafFocused('a1', 'a1'), true);
    });
    test('isLeafFocused returns false on mismatch', () => {
      assert.equal(isLeafFocused('a1', 'a2'), false);
    });
    test('isLeafFocused returns false when focusedAnchorId is null', () => {
      assert.equal(isLeafFocused(null, 'a1'), false);
    });
    test('isLeafFocused returns false when focusedAnchorId is undefined', () => {
      assert.equal(isLeafFocused(undefined, 'a1'), false);
    });
    test('isLeafFocused returns false when leafAnchorId is null', () => {
      assert.equal(isLeafFocused('a1', null), false);
    });
    ```

    (c) `app/tests/services/trellis-perf-mask.test.mjs`:

    ```javascript
    import { test } from 'node:test';
    import assert from 'node:assert/strict';
    const { leafAnimationMask, TAP_ANIMATION_THRESHOLD } = await import('../../src/services/trellis-perf-mask.ts');

    test('TAP_ANIMATION_THRESHOLD is 30', () => {
      assert.equal(TAP_ANIMATION_THRESHOLD, 30);
    });
    test('returns true when count <= 30 regardless of inView', () => {
      assert.equal(leafAnimationMask({ totalCount: 10, inView: false }), true);
      assert.equal(leafAnimationMask({ totalCount: 30, inView: false }), true);
      assert.equal(leafAnimationMask({ totalCount: 30, inView: true }), true);
    });
    test('returns inView when count > 30', () => {
      assert.equal(leafAnimationMask({ totalCount: 31, inView: true }), true);
      assert.equal(leafAnimationMask({ totalCount: 50, inView: true }), true);
      assert.equal(leafAnimationMask({ totalCount: 50, inView: false }), false);
    });
    ```

    **Step 5 — Extend `app/tests/locales/bundle-parity.test.mjs`:**

    Read the existing file. At the bottom (after the existing key-set parity test), append a new test block:

    ```javascript
    test('graph.headerTitle values match expected per locale (D-14)', async () => {
      const expected = {
        en: 'Knowledge Graph',
        zh: '知识图谱',
        es: 'Grafo de conocimiento',
        ja: 'ナレッジグラフ',
      };
      for (const [locale, expectedValue] of Object.entries(expected)) {
        const bundle = await import(`../../src/locales/${locale}.json`, { with: { type: 'json' } });
        const actual = bundle.default?.graph?.headerTitle ?? bundle.graph?.headerTitle;
        assert.equal(actual, expectedValue, `${locale}.graph.headerTitle should be "${expectedValue}" but was "${actual}"`);
      }
    });
    ```

    NOTE: If the existing bundle-parity.test.mjs uses a different JSON-import style (e.g., readFileSync + JSON.parse per Phase 27 Plan 01 decision due to Node 25 JSON-import-attribute failures), mirror that pattern instead. Check the first few lines of the existing file during read_first.

    **Step 6 — Verify:**

    Initially at least Test 4 will FAIL (locale values still say "Mind Map"). The tests are the RED side of TDD for Task 3. Tests 1-3 should pass once constants are exported in Steps 2-3 of THIS task. Commit with Test 4 still RED — Task 3 makes it green.

    ```bash
    cd app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs
    # Expect: 3 files green (tests 1-3)

    cd app && node --test tests/locales/bundle-parity.test.mjs
    # Expect: existing parity test green; new value test RED (Mind Map ≠ Knowledge Graph) — expected, Task 3 fixes
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File /Users/Code/EchoLearn/app/src/services/trellis-perf-mask.ts exists
    - File /Users/Code/EchoLearn/app/tests/components/TrellisLeaf.shake.test.mjs exists
    - File /Users/Code/EchoLearn/app/tests/components/TrellisCanvas.focus.test.mjs exists
    - File /Users/Code/EchoLearn/app/tests/services/trellis-perf-mask.test.mjs exists
    - grep -q "SHAKE_KEYFRAMES" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "export const SHAKE_DURATION_MS = 300" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "export const onLeafTap" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "hapticImpactLight exactly once" /Users/Code/EchoLearn/app/tests/components/TrellisLeaf.shake.test.mjs
    - grep -q "isLeafFocused" /Users/Code/EchoLearn/app/src/components/trellis/TrellisCanvas.tsx
    - grep -q "TAP_ANIMATION_THRESHOLD = 30" /Users/Code/EchoLearn/app/src/services/trellis-perf-mask.ts
    - grep -q "graph.headerTitle values match expected" /Users/Code/EchoLearn/app/tests/locales/bundle-parity.test.mjs
    - `cd app && node --test tests/components/TrellisLeaf.shake.test.mjs` exits 0
    - `cd app && node --test tests/components/TrellisCanvas.focus.test.mjs` exits 0
    - `cd app && node --test tests/services/trellis-perf-mask.test.mjs` exits 0
  </acceptance_criteria>
  <done>4 Wave 0 test files land (3 new + 1 extended). Pure-logic helpers (SHAKE_KEYFRAMES, isLeafFocused, leafAnimationMask, onLeafTap) exported from source. D-11 haptic Nyquist test covered via mocked haptic injection in TrellisLeaf.shake.test.mjs. Bundle-parity value test is RED pending Task 3 locale edits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trellis interactions — shake on tap (D-10) + haptic (D-11) + pulse on focus (D-12) + perf guard (D-13)</name>
  <files>
    app/src/components/trellis/TrellisLeaf.tsx,
    app/src/components/trellis/TrellisCanvas.tsx,
    app/src/components/trellis/TrellisHero.tsx,
    app/src/screens/PlannerScreen.tsx
  </files>
  <read_first>
    - app/src/components/trellis/TrellisLeaf.tsx (FULL file — understand existing motion.g at line ~516, ambient sway animate prop, rotation variable, shape rendering structure)
    - app/src/components/trellis/TrellisCanvas.tsx (FULL file — pointerEvents: 'none' at ~line 32, leaves map at ~line 68, existing AMBIENT_SWAY_THRESHOLD = 20 at lines 13-24)
    - app/src/components/trellis/TrellisHero.tsx (FULL file — understand current prop signature, useTrellisData hook consumption)
    - app/src/screens/PlannerScreen.tsx (lines 160-240 — dead/dying row onClick handlers, handleReplant/handleHeal navigation timing)
    - app/src/lib/haptics.ts (verify hapticImpactLight export signature)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-RESEARCH.md (Pattern 4 lines 253-284; Pattern 5 lines 286-306; Pattern 6 lines 308-329; Pitfall 4 lines 440-449)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-UI-SPEC.md (D-10 spec lines 188-198; D-11 spec lines 200-205; D-12 spec lines 207-220; D-13 spec lines 222-229)
    - .planning/phases/25-anime-knowledge-tree-for-planner-page-motivational-review-visualization/25-CONTEXT.md (D-55 perf convention)
  </read_first>
  <action>
    **Step 1 — TrellisLeaf.tsx: shake + pulse + haptic + focused prop:**

    (a) Import dependencies at top (preserve existing imports):
    ```typescript
    import { useAnimationControls } from 'framer-motion';
    import { hapticImpactLight } from '../../lib/haptics';
    ```

    (b) Extend Props interface:
    ```typescript
    interface TrellisLeafProps {
      // ...existing props (anchor, position, leafState, etc.)
      focused?: boolean;
      perfGuardActive?: boolean;  // from TrellisCanvas when layout.nodes.length > 30 AND NOT inView
    }
    ```

    (c) Inside the component body, add:
    ```typescript
    const shakeControls = useAnimationControls();
    const [focusCounter, setFocusCounter] = useState(0);

    const handleTap = useCallback(() => {
      if (perfGuardActive) return;  // D-13: no-op when off-screen on large canvases
      void hapticImpactLight();
      void shakeControls.start({
        rotate: SHAKE_KEYFRAMES,
        transition: { duration: SHAKE_DURATION_MS / 1000, ease: 'easeInOut' },
      });
    }, [perfGuardActive, shakeControls]);

    useEffect(() => {
      if (focused) setFocusCounter(c => c + 1);
    }, [focused]);
    ```

    (d) Find the existing outermost `<motion.g>` (around line 516) — this carries ambient sway. DO NOT MODIFY its existing animate prop. Nest TWO new inner `<motion.g>` wrappers inside it, wrapping the existing `{shape}` render:

    ```typescript
    {/* outer motion.g (EXISTING, unchanged): ambient sway */}
    <motion.g animate={/* existing ambient sway */}>

      {/* NEW shake wrapper (inner 1) */}
      <motion.g
        animate={shakeControls}
        onClick={handleTap}
        style={{ cursor: 'pointer', pointerEvents: 'auto' }}
      >

        {/* NEW pulse wrapper (inner 2) */}
        <motion.g
          key={`pulse-${anchor.id}-${focusCounter}`}
          animate={focused && !perfGuardActive ? {
            scale: [1, 1.15, 1],
            filter: [
              'drop-shadow(0 0 0px transparent)',
              'drop-shadow(0 0 8px var(--primary-40))',
              'drop-shadow(0 0 0px transparent)',
            ],
          } : { scale: 1, filter: 'drop-shadow(0 0 0px transparent)' }}
          transition={focused ? {
            scale: { duration: 0.6, ease: 'easeInOut' },
            filter: { duration: 2.0, ease: 'easeOut' },
          } : { duration: 0 }}
        >
          {shape}  {/* or {children} — whatever the existing render is */}
        </motion.g>

      </motion.g>
    </motion.g>
    ```

    The `key={`pulse-${anchor.id}-${focusCounter}`}` forces React to re-mount the pulse wrapper on each focus re-trigger so animations fire anew (UI-SPEC D-12 explicit).

    **Step 2 — TrellisCanvas.tsx: pointerEvents + focusedAnchorId thread + perf guard:**

    (a) Add `import { leafAnimationMask, TAP_ANIMATION_THRESHOLD } from '../../services/trellis-perf-mask';` at top.

    (b) Extend Props interface:
    ```typescript
    interface TrellisCanvasProps {
      // ...existing props (layout, etc.)
      focusedAnchorId?: string | null;
    }
    ```

    (c) Inside the component, when mapping over `layout.nodes` to render leaves:
    ```typescript
    const totalCount = layout.nodes.length;
    const perfGuardThresholdExceeded = totalCount > TAP_ANIMATION_THRESHOLD;

    // Side-by-side comment documenting both thresholds:
    // AMBIENT_SWAY_THRESHOLD = 20 (Phase 25 D-55 — continuous animation ceiling)
    // TAP_ANIMATION_THRESHOLD = 30 (Phase 28 D-13 — event-driven animations can tolerate more leaves)

    {layout.nodes.map(node => {
      // For simplicity at this pass, use count-only gate. IntersectionObserver optional per RESEARCH Pattern 6.
      // When perfGuardThresholdExceeded is true, all leaves get perfGuardActive=true (simplest) OR conditionally
      // pass false for leaves computed as in-view. At 30 leaves with no IO, perfGuardActive=false is acceptable per UI-SPEC.
      const inView = true; // TODO: layer IntersectionObserver here if needed on physical device
      const perfGuardActive = perfGuardThresholdExceeded && !leafAnimationMask({ totalCount, inView });

      return (
        <TrellisLeaf
          key={node.anchor.id}
          /* ...existing props */
          focused={isLeafFocused(focusedAnchorId, node.anchor.id)}
          perfGuardActive={perfGuardActive}
        />
      );
    })}
    ```

    (d) Leave the SVG root's `pointerEvents: 'none'` (line ~32) UNCHANGED — the individual leaf `<g>` sets `pointerEvents: 'auto'` in TrellisLeaf.tsx (Step 1), so taps pass through the transparent canvas but hit the leaves.

    **Step 3 — TrellisHero.tsx: forward focusedAnchorId:**

    ```typescript
    interface TrellisHeroProps {
      focusedAnchorId?: string | null;
    }

    export default function TrellisHero({ focusedAnchorId }: TrellisHeroProps) {
      // ...existing layout sourcing via useTrellisData()
      return (
        <div style={{ /* existing styles */ }}>
          <TrellisCanvas layout={layout} focusedAnchorId={focusedAnchorId} />
        </div>
      );
    }
    ```

    **Step 4 — PlannerScreen.tsx: focusedAnchorId state + onPointerDown emission:**

    Near the top of the component (after other hooks):
    ```typescript
    const [focusedAnchorId, setFocusedAnchorId] = useState<string | null>(null);
    const focusClearTimerRef = useRef<number | null>(null);

    const focusAnchor = useCallback((anchorId: string) => {
      setFocusedAnchorId(anchorId);
      if (focusClearTimerRef.current) window.clearTimeout(focusClearTimerRef.current);
      focusClearTimerRef.current = window.setTimeout(() => {
        setFocusedAnchorId(null);
        focusClearTimerRef.current = null;
      }, 2000);
    }, []);

    useEffect(() => () => {
      if (focusClearTimerRef.current) window.clearTimeout(focusClearTimerRef.current);
    }, []);
    ```

    Then on the dead row (~line 176) AND dying row (~line 224) element, ADD (do not replace existing onClick):
    ```typescript
    onPointerDown={() => focusAnchor(node.id)}
    // existing onClick remains — fires after pointer release
    ```

    Pass `focusedAnchorId` into TrellisHero:
    ```typescript
    <TrellisHero focusedAnchorId={focusedAnchorId} />
    ```

    **Step 5 — Verify:**

    ```bash
    cd app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs
    cd app && npx vite build
    ```

    The ambient sway should continue running on the outer motion.g; tap-to-shake should queue on the inner motion.g via shakeControls; pulse should fire when focused prop is true and key change forces re-mount.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useAnimationControls" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "hapticImpactLight" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "SHAKE_KEYFRAMES" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "drop-shadow(0 0 8px var(--primary-40))" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "scale: \[1, 1.15, 1\]" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "pointerEvents: 'auto'" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "focused?" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "perfGuardActive" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx
    - grep -q "focusedAnchorId" /Users/Code/EchoLearn/app/src/components/trellis/TrellisCanvas.tsx
    - grep -q "TAP_ANIMATION_THRESHOLD" /Users/Code/EchoLearn/app/src/components/trellis/TrellisCanvas.tsx
    - grep -q "isLeafFocused" /Users/Code/EchoLearn/app/src/components/trellis/TrellisCanvas.tsx
    - grep -q "focusedAnchorId" /Users/Code/EchoLearn/app/src/components/trellis/TrellisHero.tsx
    - grep -q "focusedAnchorId" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx
    - grep -q "onPointerDown" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx
    - grep -q "setTimeout" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx
    - `cd app && npx vite build` exits 0
    - `cd app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs` exits 0
  </acceptance_criteria>
  <done>Any trellis leaf tap fires shake animation (300ms) + haptic on native. Suggested Moves row onPointerDown emits focusedAnchorId, pulse runs on matched leaf for ~2s (auto-clears). Perf guard threshold 30 documented alongside Phase 25's 20. Ambient sway preserved (outer motion.g untouched).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: i18n value swap — "Mind Map" → "Knowledge Graph" across 4 locale bundles (D-14)</name>
  <files>
    app/src/locales/en.json,
    app/src/locales/zh.json,
    app/src/locales/es.json,
    app/src/locales/ja.json,
    app/src/screens/AskScreen.tsx,
    CLAUDE.md
  </files>
  <read_first>
    - app/src/locales/en.json (locate "graph" block; current: "headerTitle": "Mind Map" at line ~195)
    - app/src/locales/zh.json (locate graph.headerTitle — currently "脑图")
    - app/src/locales/es.json (locate graph.headerTitle — currently "Mapa mental")
    - app/src/locales/ja.json (locate graph.headerTitle — currently "マインドマップ")
    - app/src/screens/GraphScreen.tsx (line ~522 — verify t('graph.headerTitle') is the rendered key; no code change needed here)
    - app/src/screens/AskScreen.tsx (line ~234 — update stale code comment mentioning "Mind Map" for grep hygiene)
    - CLAUDE.md (no section requires an update for value-swap; verify)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-UI-SPEC.md (Copywriting Contract lines 107-118 — exact locked values)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-RESEARCH.md (Pattern 7 lines 331-353 — verification that Path 1 (swap values) is chosen)
    - .planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md (D-08 workflow — Sonnet subagent)
    - app/scripts/translate-locales.md (Sonnet subagent prompt template)
  </read_first>
  <action>
    **Step 1 — Value swaps in 4 locale bundles:**

    Since we have EXACT locked translations per UI-SPEC, we can apply them directly without spawning a Sonnet subagent (the executor has the values). This matches Phase 27 Plan 07's executor-inline translation pattern — deterministic mapping, no LLM needed.

    Apply exactly these edits:

    (a) `app/src/locales/en.json`:
    - Find `"headerTitle": "Mind Map"` under `"graph":` block
    - Replace with `"headerTitle": "Knowledge Graph"`

    (b) `app/src/locales/zh.json`:
    - Find `"headerTitle": "脑图"` under `"graph":` block
    - Replace with `"headerTitle": "知识图谱"`

    (c) `app/src/locales/es.json`:
    - Find `"headerTitle": "Mapa mental"` under `"graph":` block
    - Replace with `"headerTitle": "Grafo de conocimiento"`

    (d) `app/src/locales/ja.json`:
    - Find `"headerTitle": "マインドマップ"` under `"graph":` block
    - Replace with `"headerTitle": "ナレッジグラフ"`

    Preserve all other keys in the `graph` block (title, anchor.*, cluster.*, reorganizeModal, selected, toast, etc.). Preserve JSON formatting (indentation, trailing commas policy matches existing file).

    **Step 2 — Update AskScreen comment (grep hygiene):**

    In `app/src/screens/AskScreen.tsx` at line ~234, find the comment mentioning "Mind Map". Replace the words "Mind Map" with "Knowledge Graph" inside the comment. Example:
    - Before: `// feed into Mind Map, Review, and Podcast surfaces.`
    - After: `// feed into Knowledge Graph, Review, and Podcast surfaces.`

    No user-visible change — comment-only edit.

    **Step 3 — Verify bundle parity + values:**

    ```bash
    cd app && node --test tests/locales/bundle-parity.test.mjs
    ```

    Both the existing key-set parity test AND the new value-level test (added in Task 1) should now be GREEN.

    ```bash
    cd app && grep -l "\"headerTitle\": \"Mind Map\"" src/locales/*.json && echo "REGRESSION" || echo "OK"
    cd app && grep -l "脑图\|Mapa mental\|マインドマップ" src/locales/*.json && echo "STALE VALUE" || echo "OK"
    cd app && grep "Knowledge Graph" src/locales/en.json
    cd app && grep "知识图谱" src/locales/zh.json
    cd app && grep "Grafo de conocimiento" src/locales/es.json
    cd app && grep "ナレッジグラフ" src/locales/ja.json
    ```

    All 4 grep commands for new values should exit 0.

    ```bash
    cd app && npx vite build
    ```

    Build green.

    **Step 4 — CLAUDE.md check:**

    Read CLAUDE.md and search for "Mind Map" — if any user-facing copy reference exists in the i18n section allowlist or examples, update to "Knowledge Graph". CLAUDE.md's "What NOT to translate" proper-nouns list does NOT include "Mind Map" (it was translated before — that's the entire point of D-14), so no allowlist edit needed. If CLAUDE.md has zero mentions of "Mind Map", no edit required.

    Do NOT update CLAUDE.md's existing structure/headings — only replace stale "Mind Map" strings if present.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "\"headerTitle\": \"Knowledge Graph\"" /Users/Code/EchoLearn/app/src/locales/en.json
    - grep -q "\"headerTitle\": \"知识图谱\"" /Users/Code/EchoLearn/app/src/locales/zh.json
    - grep -q "\"headerTitle\": \"Grafo de conocimiento\"" /Users/Code/EchoLearn/app/src/locales/es.json
    - grep -q "\"headerTitle\": \"ナレッジグラフ\"" /Users/Code/EchoLearn/app/src/locales/ja.json
    - ! grep -q "\"Mind Map\"" /Users/Code/EchoLearn/app/src/locales/en.json
    - ! grep -q "\"脑图\"" /Users/Code/EchoLearn/app/src/locales/zh.json
    - ! grep -q "\"Mapa mental\"" /Users/Code/EchoLearn/app/src/locales/es.json
    - ! grep -q "\"マインドマップ\"" /Users/Code/EchoLearn/app/src/locales/ja.json
    - ! grep -q "Mind Map" /Users/Code/EchoLearn/app/src/screens/AskScreen.tsx
    - `cd app && node --test tests/locales/bundle-parity.test.mjs` exits 0 (INCLUDING the new value-level test from Task 1)
    - `cd app && npx vite build` exits 0
  </acceptance_criteria>
  <done>4 locale bundles carry the renamed value "Knowledge Graph" / 知识图谱 / Grafo de conocimiento / ナレッジグラフ on graph.headerTitle. AskScreen comment updated. Bundle-parity value-level test (added in Task 1) flips to GREEN. GraphScreen renders correctly in all 4 locales without code change (already renders t('graph.headerTitle')).</done>
</task>

</tasks>

<verification>
Phase-wide gates for this plan:

1. `cd app && node --test tests/components/TrellisLeaf.shake.test.mjs tests/components/TrellisCanvas.focus.test.mjs tests/services/trellis-perf-mask.test.mjs tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs` exits 0
2. `cd app && npx vite build` exits 0
3. `cd app && npx tsc -b --noEmit` — no NEW errors in touched trellis files (pre-existing 8 errors acceptable)
4. Bundle sanity: all 4 locale files parse as valid JSON (`node -e "JSON.parse(require('fs').readFileSync('app/src/locales/en.json'))"` per file — no trailing-comma regressions)

Manual UAT (deferred to phase gate):
- Tap any leaf on /planner → verify 300ms shake + (on native device) light haptic. Tap rapidly 5 times on same leaf — no stacking jank.
- Tap a Suggested Moves row → verify corresponding leaf pulses (scale + glow) just before nav commits; if user taps fast, the pulse animation re-triggers (key change forces re-mount).
- Switch locale in Settings (Language / 语言 / Idioma / 言語) → open Graph tab → header reads "Knowledge Graph" / "知识图谱" / "Grafo de conocimiento" / "ナレッジグラフ" respectively.
- Seed 35+ anchors (via localStorage helper or dev mode) → tap a leaf → with TAP_ANIMATION_THRESHOLD=30, off-screen leaves skip shake no-op (no jank).
</verification>

<success_criteria>
- TrellisLeaf supports tap-to-shake via useAnimationControls, haptic via hapticImpactLight, pulse via focused prop, perf-gated by perfGuardActive.
- TrellisCanvas threads focusedAnchorId, sets TAP_ANIMATION_THRESHOLD=30, documents both thresholds side-by-side.
- TrellisHero accepts and forwards focusedAnchorId.
- PlannerScreen tracks focusedAnchorId with 2000ms auto-clear, emits on onPointerDown before navigation.
- All 4 locale bundles: graph.headerTitle carries the renamed value.
- AskScreen comment at line ~234 updated for grep hygiene.
- Bundle-parity value-level test passes (was RED end of Task 1).
- All Wave 0 Plan 28-02 tests green; vite build green.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-ux-polish-from-audit-findings/28-02-SUMMARY.md` documenting:
- Exact locations where SHAKE_KEYFRAMES, SHAKE_DURATION_MS, isLeafFocused, leafAnimationMask were hoisted from (if they existed before) or created fresh
- Any deviations (e.g., whether IntersectionObserver was layered for D-13 or count-gate only)
- Locale bundle diff: 4 files × 1 value each = 4 changes
- Perf note: what ambient-sway threshold (20) vs tap-animation threshold (30) documentation was added in TrellisCanvas.tsx
- Test counts (Wave 0 Plan 28-02 adds: 3 new test files; 1 existing extended)
- Decisions D-10, D-11, D-12, D-13, D-14 marked as landed (5 decisions total)
</output>
