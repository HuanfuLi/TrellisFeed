---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/src/screens/PlannerScreen.tsx
  - app/src/screens/SettingsScreen.tsx
  - app/src/screens/settings/SettingsDataScreen.tsx
  - app/src/screens/settings/SettingsFeaturesScreen.tsx
  - app/src/services/daily-read.service.ts
  - app/src/services/post-history.service.ts
  - app/tests/concept-quota.test.mjs
autonomous: true
requirements: [WIP-FLUSH]
must_haves:
  truths:
    - "Working tree is clean — no uncommitted WIP when downstream waves begin"
    - "3 i18n keys added consistently across all 4 locale bundles (scrollToTop, resetToday, todayReset)"
    - "WIP commit message reflects Phase 31 + Phase 32.1 UAT-feedback polish, not a mixed-intent commit"
  artifacts:
    - path: "app/src/services/daily-read.service.ts"
      provides: "Quota refactor: derive from questionsById anchors; EXCLUDED_SOURCE_TYPES renamed NON_CONCEPT_SOURCE_TYPES"
      must_contain: "NON_CONCEPT_SOURCE_TYPES"
      must_not_contain: "EXCLUDED_SOURCE_TYPES"
    - path: "app/src/locales/en.json"
      provides: "3 new keys: home.feed.scrollToTop, common.buttons.resetToday, common.toast.todayReset"
      must_contain: "scrollToTop"
  key_links:
    - from: "git status --porcelain"
      to: "empty string"
      via: "single chore commit"
      pattern: "^$"
---

<objective>
Flush all uncommitted WIP from the working tree before Wave 1 starts, so downstream rename/delete tasks don't collide with experimental edits. Per researcher (33-RESEARCH.md Finding #7) AND re-baseline at revision time (2026-04-19): the working tree currently carries 11 modified files (Phase 31 + Phase 32.1 UAT-feedback polish — i18n fixes, screen refinements, quota refactor).

**Revision baseline notes (2026-04-19):**
- The 3 test files the original 33-CONTEXT.md listed as "untracked" (`concept-batch-filter.test.mjs`, `daily-generation-cap.test.mjs`, `starter-posts.test.mjs`) ALREADY landed in commit `9486799a test(32.1-02): commit existing concept-batch + daily-generation-cap + starter-posts tests`. They are NOT in the current working tree.
- `app/src/services/starter-posts-decay.ts` (which was briefly untracked earlier in the phase) was committed in `87850b60 feat(32.1-04): persist starter posts to cache + decay at 3+ organic posts`. It is NOT untracked anymore.
- `app/tests/services/starter-posts-persist.test.mjs` is tracked as of the same 32.1-04 feat commit.
- There are ZERO untracked files in the working tree at revision time. The WIP to flush is SOLELY the 11 modified files listed in `files_modified`.

Purpose: D-19/D-20/D-21 — a clean working tree is the prerequisite for TD-06's atomic rename commit per D-15.
Output: One `chore(v1.4)` commit; `git status --porcelain --untracked-files=all` returns empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Re-baseline working tree, review each WIP diff, and confirm clean disposition</name>
  <read_first>
    - app/src/services/daily-read.service.ts (behavioral change — review carefully)
    - app/tests/concept-quota.test.mjs (paired with daily-read change — must match)
    - app/src/screens/settings/SettingsDataScreen.tsx (i18n conversion)
    - app/src/locales/en.json (paired i18n additions)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #7 (expected disposition per file — adjusted per revision baseline notes in objective)
  </read_first>
  <action>
    Step 1 — Re-baseline: `cd /Users/Code/EchoLearn && git status --porcelain --untracked-files=all` (filter out any planning-artifact rows that may accrue during execution, e.g., `.planning/phases/33-.../*-PLAN.md` edits). Expected production-code WIP output (11 lines, one per file):
    ```
     M app/src/locales/en.json
     M app/src/locales/es.json
     M app/src/locales/ja.json
     M app/src/locales/zh.json
     M app/src/screens/PlannerScreen.tsx
     M app/src/screens/SettingsScreen.tsx
     M app/src/screens/settings/SettingsDataScreen.tsx
     M app/src/screens/settings/SettingsFeaturesScreen.tsx
     M app/src/services/daily-read.service.ts
     M app/src/services/post-history.service.ts
     M app/tests/concept-quota.test.mjs
    ```

    If any untracked file surfaces (e.g., a new `app/src/services/starter-posts-decay.ts` or a `?? app/tests/services/{concept-batch-filter,daily-generation-cap,starter-posts}.test.mjs`), that's unexpected per the re-baseline note — HALT and triage before proceeding. Most likely they've reappeared via a branch rebase; either land them in a pre-33-00 commit of their own or incorporate them into Task 0.2's stage list after auditing.

    If `.planning/ROADMAP.md` shows as modified (it was flagged as modified at checker-time but NOT at revision-time), add it to the Task 0.2 stage list to keep the working tree fully clean.

    Step 2 — For each of the 11 modified files, run `git diff -- <path>` and confirm the diff matches the described shape:

    - **4 locale bundles** (`en/zh/es/ja.json`): 3-key additions only. Run `git diff app/src/locales/en.json | grep "^+[^+]" | grep -v "^+++"` and verify you see exactly these 3 additions (plus whatever preceding-line trailing-comma punctuation edits they incur):
      - `"scrollToTop": "Scroll to top"` (in `home.feed.*`)
      - `"resetToday": "Reset Today"` (in `common.buttons.*`)
      - `"todayReset": "Today's review/post status reset"` (in `common.toast.*`)

      Confirm zh/es/ja mirror the same 3-key set with translated values. Note: the `home.feed.scrollToTop` key is being ADDED (it does not yet exist at HEAD); ScrollToTopFAB.tsx references it but the key was not yet in the bundles.

    - **`PlannerScreen.tsx`**: 1-line removal of unused `useEffect` import.
    - **`SettingsScreen.tsx`**: 1-line removal of unused `Palette` import from `lucide-react`.
    - **`SettingsDataScreen.tsx`**: hardcoded-string to t() conversion for the "Reset Today" button + toast.
    - **`SettingsFeaturesScreen.tsx`**: `const [reviewLimit, setReviewLimit] = ...` → `const [reviewLimit] = ...`.
    - **`daily-read.service.ts`**: getConceptQuota refactor (derive from questionsById anchors; rename EXCLUDED_SOURCE_TYPES → NON_CONCEPT_SOURCE_TYPES as `['starter', 'connection', 'suggestion']`).
    - **`post-history.service.ts`**: 1-line removal of dead `const DEFAULT_RETENTION_DAYS = 7;`.
    - **`concept-quota.test.mjs`**: rewrite matching the daily-read refactor.

    If any diff contains unexpected content (console.log, debug prints, secrets like `sk-` / `api_key`, TODO comments, half-commented code), HALT and escalate.

    Step 3 — Run the paired `concept-quota.test.mjs` to confirm the test rewrite matches the daily-read.service.ts refactor:
    ```
    cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs
    ```
    Expected: `fail 0`.

    Step 4 — Run bundle-parity regression to confirm the 3-key locale additions are consistent:
    ```
    cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs
    ```
    Expected: `fail 0`. If keys diverge across locales, HALT and fix before committing (violating CLAUDE.md's bundle-parity rule).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain --untracked-files=all` lists exactly the 11 production-code files enumerated in Step 1 (modulo any planning-artifact rows introduced during execute phase).
    - Each of the 11 modified diffs matches the described shape; no surprise content.
    - Targeted run of `concept-quota.test.mjs` and `bundle-parity.test.mjs` reports `fail 0`.
    - No HALT condition triggered (no secrets, no debug logs, no half-commented code, no unexpected untracked files surfacing).
  </acceptance_criteria>
  <done>All 11 files reviewed and categorized safe-to-commit; paired tests green.</done>
</task>

<task type="auto">
  <name>Task 0.2: Stage all 11 WIP files as one atomic chore commit</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-19 commit message + D-20 review policy)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #7
  </read_first>
  <action>
    Step 1 — Stage only the 11 WIP files (do NOT use `git add -A` to avoid accidentally including phase-33 planning artifacts that will be committed later):

    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/locales/en.json \
      app/src/locales/zh.json \
      app/src/locales/es.json \
      app/src/locales/ja.json \
      app/src/screens/PlannerScreen.tsx \
      app/src/screens/SettingsScreen.tsx \
      app/src/screens/settings/SettingsDataScreen.tsx \
      app/src/screens/settings/SettingsFeaturesScreen.tsx \
      app/src/services/daily-read.service.ts \
      app/src/services/post-history.service.ts \
      app/tests/concept-quota.test.mjs
    ```

    **Runtime additions:** If Task 0.1 Step 1 flagged additional unexpected files (untracked helpers, `.planning/ROADMAP.md` modifications, etc.), append them to this `git add` list AFTER a per-file review. Do NOT silently skip them — the acceptance criterion is `git status --porcelain --untracked-files=all` returns empty AFTER this task. Any file left in the working tree would violate D-19.

    Step 2 — Verify `git status --porcelain` now shows only the staged lines as `M ` / `A ` (and no unexpected adds). Run `git diff --cached --stat` and visually confirm line counts are modest (no blob additions > 500 LOC per file except locale bundles, which are fine).

    Step 3 — Create the commit via HEREDOC (per D-19 exact message, adjusted to reflect the actual WIP composition at revision time):

    ```
    git commit -m "$(cat <<'EOF'
    chore(v1.4): flush WIP — quota refactor + i18n polish (3 new keys)

    Flushes uncommitted working-tree state from Phase 31 + Phase 32.1
    UAT-feedback polish:
    - daily-read.service.ts: getConceptQuota derives quota from questionsById
      anchors (Phase 31 D-12 alignment); rename
      EXCLUDED_SOURCE_TYPES -> NON_CONCEPT_SOURCE_TYPES
    - concept-quota.test.mjs: rewrite to match new shape (fail 0)
    - i18n polish: 3 new keys landed in all 4 locale bundles (bundle parity preserved)
        home.feed.scrollToTop       — ScrollToTopFAB accessibility label
        common.buttons.resetToday   — SettingsDataScreen button
        common.toast.todayReset     — SettingsDataScreen toast
    - SettingsDataScreen: hardcoded Reset Today strings -> t() calls
    - PlannerScreen / SettingsScreen: drop unused imports
    - SettingsFeaturesScreen: drop unused setReviewLimit setter
    - post-history.service.ts: drop dead DEFAULT_RETENTION_DAYS const

    Prerequisite flush for Phase 33 TD-06 rename (D-19/D-20/D-21).
    Revision-time baseline: 11 modified files, zero untracked. The 3 test
    files and starter-posts-decay.ts helper listed in the original Phase 33
    context all landed pre-33 (commits 9486799a + 87850b60).
    EOF
    )"
    ```

    Step 4 — Run `git status --porcelain --untracked-files=all` and assert the output contains ZERO production-code rows. Planning-artifact rows (e.g., the 5 `33-*-PLAN.md` files under revision) are EXCLUDED from this check because they are intentionally uncommitted until the planner hands off — the WIP flush must NOT sweep them into this chore commit. The check is specifically: no `app/**` or `src/**` files should remain in the working tree.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git status --porcelain --untracked-files=all | grep -v "^.. .planning/phases/33-" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Commit created with subject line exactly `chore(v1.4): flush WIP — quota refactor + i18n polish (3 new keys)`.
    - After commit, `git status --porcelain --untracked-files=all | grep -v "^.. .planning/phases/33-" | wc -l` returns `0` (zero non-planning rows; planning artifacts may still be present and are expected).
    - `git log -1 --format=%s` returns the subject line above.
    - `git log -1 --stat | grep -c "app/"` returns at least 9 (matching the 9+ app/ files staged — 4 locale bundles + 4 screens/services + 1 test).
  </acceptance_criteria>
  <done>Working tree clean of production-code WIP; single commit recorded; downstream waves unblocked.</done>
</task>

</tasks>

<verification>
Combined phase 33-00 checks:
- `cd /Users/Code/EchoLearn && git status --porcelain --untracked-files=all | grep -v "^.. .planning/phases/33-"` returns empty string.
- `cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/locales/bundle-parity.test.mjs` — fail 0.
- `git log -1 --format=%s` returns the Phase 33 WIP flush subject line.
</verification>

<success_criteria>
- All production-code WIP flushed — only planning artifacts may remain.
- Exactly one new commit landed on the branch with subject matching D-19's specified message.
- Paired tests green; no surprise regressions in the paired services.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md` recording the commit SHA, file list, and test counts.
</output>
