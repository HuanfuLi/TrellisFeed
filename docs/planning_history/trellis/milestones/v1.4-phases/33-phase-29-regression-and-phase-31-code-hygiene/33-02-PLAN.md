---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 02
type: execute
wave: 1
depends_on: [33-00]
files_modified:
  - app/tests/services/concept-feed-strategy.test.mjs
  - app/tests/services/orchestration-strategy.test.mjs
  - .planning/phases/29-final-polishment/29-VERIFICATION.md
  - .planning/phases/29-final-polishment/29-UAT-LOG.md
autonomous: true
requirements: [TD-04]
must_haves:
  truths:
    - "concept-feed-strategy.test.mjs no longer exists (all 11 tests were obsolete under D-01)"
    - "orchestration-strategy.test.mjs retains 9 tests — the plannerAutoGen-side TD-01 plumbing assertion at line 114 stays; only the concept-feed-side assertion (lines 126-137) is removed"
    - "29-VERIFICATION.md TD-01 row status changed from SATISFIED to SUPERSEDED-BY-PHASE-31 with evidence pointer to 31-CONTEXT.md D-14"
    - "29-UAT-LOG.md has a new Post-sign-off supersession section that preserves the original TD-01 SATISFIED row"
    - "npm test baseline drops by 6 v1.4 regressions (5 from deleted file + 1 from orphan assertion removal)"
  artifacts:
    - path: "app/tests/services/concept-feed-strategy.test.mjs"
      provides: "Deleted — applyStrategyBias doesn't exist in concept-feed.service.ts anymore"
      state: "absent"
    - path: ".planning/phases/29-final-polishment/29-VERIFICATION.md"
      provides: "TD-01 row marked SUPERSEDED-BY-PHASE-31 with D-14 pointer"
      must_contain: "SUPERSEDED-BY-PHASE-31"
  key_links:
    - from: "29-VERIFICATION.md TD-01 row"
      to: "31-CONTEXT.md D-14 (generation-time weak-concept prioritization)"
      via: "text pointer in Evidence cell"
      pattern: "31-CONTEXT.*D-14"
    - from: "29-UAT-LOG.md Post-sign-off supersession section"
      to: "Phase 33 TD-04 resolution (33-CONTEXT.md D-05)"
      via: "text pointer in Recorded by cell"
      pattern: "33-CONTEXT.*D-05"
---

<objective>
Close TD-04 by SUPERSEDING (not restoring) the Phase 29 TD-01 contract. Per D-01, Phase 31 D-14's generation-time weak-concept prioritization (`buildConceptBatch` — 2 posts per important concept) subsumes the Phase 29 runtime sort bias; restoring `applyStrategyBias` would double-layer the weighting.

This plan runs parallel with Plan 33-01 (both are Wave 1, different files, no overlap).

Purpose: D-02 + D-03 + D-04 + D-05 — delete 6 now-obsolete test assertions and update the two historical records (29-VERIFICATION.md + 29-UAT-LOG.md) to reflect the Phase 31 redesign.
Output: one atomic commit covering all four files (plus a short amendment commit for the SHA backfill); `npm test` drops 6 v1.4-specific failures; 29 docs no longer stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@.planning/phases/29-final-polishment/29-VERIFICATION.md
@.planning/phases/29-final-polishment/29-UAT-LOG.md
@app/tests/services/concept-feed-strategy.test.mjs
@app/tests/services/orchestration-strategy.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Delete concept-feed-strategy.test.mjs and remove orphan TD-01 plumbing assertion from orchestration-strategy.test.mjs</name>
  <read_first>
    - app/tests/services/concept-feed-strategy.test.mjs (whole file — all 11 tests obsolete under D-01)
    - app/tests/services/orchestration-strategy.test.mjs (locate concept-feed-side TD-01 plumbing assertion at lines 126-137 — delete ONLY this block; keep lines 114-124 plannerAutoGen assertion + 33-108 defaultStrategy.computeHints tests)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #6 + Code Example 2 (exact text of assertion to delete)
  </read_first>
  <action>
    Step 1 — Delete the strategy-bias test file:
    `rm /Users/Code/EchoLearn/app/tests/services/concept-feed-strategy.test.mjs`

    Step 2 — In `app/tests/services/orchestration-strategy.test.mjs`, locate this test block (approximately lines 126-137):

    ```
    test('TD-01 plumbing: concept-feed.service.ts applyStrategyBias passes checkInSignals to computeHints', () => {
      const src = readFileSync(path.join(repoRoot, 'src/services/concept-feed.service.ts'), 'utf8');
      assert.ok(
        src.includes('computeHints(signals, checkInSignals)'),
        'concept-feed.service.ts must pass checkInSignals to computeHints',
      );
      assert.ok(
        src.includes('const recentSignals = plannerService.getRecentSignals()'),
        'concept-feed.service.ts must retain pre-existing line-251 recentSignals call',
      );
    });
    ```

    Using the `Edit` tool, delete this entire test block (from `test('TD-01 plumbing: concept-feed.service.ts applyStrategyBias ...` through the closing `});`). Also delete any blank line immediately above it to prevent double-empty lines.

    Do NOT touch:
    - `test('TD-01 plumbing: plannerAutoGen.service.ts passes checkInSignals to computeHints', ...)` — plannerAutoGen-side test stays per D-03.
    - The 8 `describe('defaultStrategy.computeHints', ...)` tests — all keep.

    Step 3 — Grep-verify: concept-feed-side assertion gone, plannerAutoGen-side retained:
    ```
    cd /Users/Code/EchoLearn && grep -c "TD-01 plumbing: concept-feed.service.ts" app/tests/services/orchestration-strategy.test.mjs
    # expect: 0
    grep -c "TD-01 plumbing: plannerAutoGen.service.ts" app/tests/services/orchestration-strategy.test.mjs
    # expect: 1
    ```

    Step 4 — Run the file to confirm 9 remaining tests pass:
    `cd /Users/Code/EchoLearn/app && node --test tests/services/orchestration-strategy.test.mjs`
    Expected: tests 9, pass 9, fail 0.

    Do NOT commit yet — Task 2.2 commits all 4 files atomically per Architecture Pattern #3 (code + docs together).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/orchestration-strategy.test.mjs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f /Users/Code/EchoLearn/app/tests/services/concept-feed-strategy.test.mjs` exits 0.
    - `grep -c "TD-01 plumbing: concept-feed.service.ts" /Users/Code/EchoLearn/app/tests/services/orchestration-strategy.test.mjs` returns `0`.
    - `grep -c "TD-01 plumbing: plannerAutoGen.service.ts" /Users/Code/EchoLearn/app/tests/services/orchestration-strategy.test.mjs` returns `1`.
    - `node --test tests/services/orchestration-strategy.test.mjs` reports fail 0.
  </acceptance_criteria>
  <done>Code-side TD-04 deletions complete; awaiting docs update in Task 2.2 for the atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2.2: Update 29-VERIFICATION.md TD-01 row + append SUPERSEDED section to 29-UAT-LOG.md, then commit all 4 changes atomically</name>
  <read_first>
    - .planning/phases/29-final-polishment/29-VERIFICATION.md (locate the TD-01 row in Requirements Coverage ~line 93, AND the Observable Truths row 1 ~line 21 — both stale per RESEARCH.md Finding #9)
    - .planning/phases/29-final-polishment/29-UAT-LOG.md (read Sign-off block; append section must come AFTER it per D-05)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #9 (exact markdown for both row rewrites + UAT-LOG append)
    - .planning/phases/31-curiosity-feed-redesign-post-lifecycle-and-display/31-CONTEXT.md (locate D-14 text for evidence cell)
  </read_first>
  <action>
    Step 1 — In `.planning/phases/29-final-polishment/29-VERIFICATION.md`, find the TD-01 row in the Requirements Coverage table. Current text (~line 93):

    `| TD-01 | 29-01 | Curiosity-signal wiring at plannerAutoGen + concept-feed computeHints call sites | SATISFIED | Both call sites verified; 2 static-grep plumbing tests pass |`

    Replace with:

    `| TD-01 | 29-01 | Curiosity-signal wiring at plannerAutoGen + concept-feed computeHints call sites | SUPERSEDED-BY-PHASE-31 | Phase 31 D-14 (31-CONTEXT.md) implements weak-concept prioritization at generation time — 2 posts per important concept. This subsumes the Phase 29 runtime sort bias. concept-feed.service.ts still calls plannerService.getRecentSignals() for LLM prompt context, but applyStrategyBias is intentionally absent. See Phase 33 TD-04 (33-CONTEXT.md D-01). |`

    Step 2 — In the same file, find the Observable Truths row 1 (~line 21). Current text:

    `| 1 | TD-01: checkInSignals threaded to computeHints at both call sites | VERIFIED | \`computeHints(signals, checkInSignals)\` present in both service files; 2 TD-01 plumbing tests pass |`

    Replace with:

    `| 1 | TD-01: checkInSignals threaded to computeHints at the plannerAutoGen call site; concept-feed branch superseded by Phase 31 D-14 | VERIFIED / SUPERSEDED | \`computeHints(signals, checkInSignals)\` present in plannerAutoGen.service.ts; concept-feed applyStrategyBias removed per 33-CONTEXT.md D-01. 1 TD-01 plumbing test passes (orchestration-strategy.test.mjs plannerAutoGen assertion) |`

    Step 3 — In `.planning/phases/29-final-polishment/29-UAT-LOG.md`, APPEND a new section at the END of the file (after the existing Sign-off block). Do NOT modify the original TD-01 SATISFIED row anywhere in the file per D-05. Appended content:

    ```

    ## Post-sign-off supersession

    ### TD-01 SUPERSEDED by Phase 31 (recorded 2026-04-19)

    | Entry | Value |
    |-------|-------|
    | Original item | TD-01 — curiosity-signal wiring at plannerAutoGen + concept-feed call sites |
    | Superseded by | Phase 31 D-14 (31-CONTEXT.md) — generation-time weak-concept prioritization |
    | Rationale | Phase 31's `buildConceptBatch` generates 2 posts per important concept (ease < 1.5 or dying/falling/dead LeafState) AT generation time. The Phase 29 runtime sort bias would double-layer the weighting. |
    | Code evidence | applyStrategyBias removed from concept-feed.service.ts; plannerAutoGen.service.ts retains wiring |
    | Closure commit | PENDING — backfilled post-commit |
    | Recorded by | Phase 33 TD-04 resolution (33-CONTEXT.md D-05) |
    ```

    Step 4 — Grep-verify edits landed before committing:
    ```
    cd /Users/Code/EchoLearn
    grep -c "SUPERSEDED-BY-PHASE-31" .planning/phases/29-final-polishment/29-VERIFICATION.md   # >= 1
    grep -c "TD-01 SUPERSEDED" .planning/phases/29-final-polishment/29-UAT-LOG.md              # >= 1
    grep -c "SATISFIED" .planning/phases/29-final-polishment/29-UAT-LOG.md                    # >= 1 (original preserved)
    ```

    Step 5 — Stage and commit all 4 files atomically (test deletions from Task 2.1 + doc updates from this task, per Architecture Pattern #3). This is the CONTENT-BEARING closure commit:

    ```
    cd /Users/Code/EchoLearn && git add \
      app/tests/services/concept-feed-strategy.test.mjs \
      app/tests/services/orchestration-strategy.test.mjs \
      .planning/phases/29-final-polishment/29-VERIFICATION.md \
      .planning/phases/29-final-polishment/29-UAT-LOG.md

    git commit -m "$(cat <<'EOF'
    refactor(29): supersede TD-01 — Phase 31 D-14 generation-time prioritization subsumes runtime sort bias (TD-04)

    Phase 30/31 feed rewrite intentionally removed applyStrategyBias from
    concept-feed.service.ts. Phase 31 D-14 (buildConceptBatch) now prioritizes
    weak concepts at GENERATION time (2 posts per important concept: ease<1.5
    or dying/falling/dead LeafState) — this subsumes the Phase 29 runtime sort
    bias. Restoring applyStrategyBias would double-layer the weighting.

    Per Phase 33 D-01..D-05:

    - Delete app/tests/services/concept-feed-strategy.test.mjs (11 tests, 5 failing
      on applyStrategyBias structural presence).
    - Remove one orphan TD-01 plumbing assertion from orchestration-strategy.test.mjs
      (concept-feed-side, lines 126-137). Retain plannerAutoGen-side assertion.
    - Update 29-VERIFICATION.md TD-01 row and Observable Truths row 1 to
      SUPERSEDED-BY-PHASE-31 with pointer to 31-CONTEXT.md D-14.
    - Append Post-sign-off supersession section to 29-UAT-LOG.md (preserves
      original SATISFIED row per D-05).

    plannerAutoGen call site unaffected. npm test: 6 fewer v1.4 regressions.
    EOF
    )"
    ```

    Step 6 — IMMEDIATELY after the Step 5 main commit lands (and BEFORE making any amendment edit), capture the short SHA of that closure commit. HEAD currently points at the main closure commit — `git rev-parse --short HEAD` returns its SHA. Record the value:

    ```
    cd /Users/Code/EchoLearn && CLOSURE_SHA=$(git rev-parse --short HEAD) && echo "CLOSURE_SHA=$CLOSURE_SHA"
    ```

    Echo the literal string value to the session (e.g., `CLOSURE_SHA=a1b2c3d`). You will substitute this exact 7+ character hex into the UAT-LOG in Step 7.

    Step 7 — Use the `Edit` tool on `.planning/phases/29-final-polishment/29-UAT-LOG.md` to replace the placeholder line `| Closure commit | PENDING — backfilled post-commit |` with `| Closure commit | <CLOSURE_SHA> |`, literally substituting the value captured in Step 6. Do NOT leave the `<CLOSURE_SHA>` angle-brackets in place — paste the actual hex.

    Step 8 — Create the short amendment commit (docs-only; carries the SHA reference pointing backward to the Step 5 closure commit):

    ```
    cd /Users/Code/EchoLearn && git add .planning/phases/29-final-polishment/29-UAT-LOG.md && git commit -m "docs(29): record TD-04 closure commit SHA in UAT-LOG supersession entry"
    ```

    Why two commits: the closure-commit SHA doesn't exist until after Step 5 lands, and `git commit --amend` would rewrite the SHA we're trying to reference (yielding a forever-stale pointer). Capturing BEFORE the amendment edit and committing SEPARATELY afterward is the only path where the `Closure commit` cell references the content-bearing commit, not the docs-only amendment.

    Step 9 — Final verify:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain      # must be empty
    grep "Closure commit" .planning/phases/29-final-polishment/29-UAT-LOG.md   # shows real 7+ char hex, not PENDING
    # Confirm the SHA references a real commit:
    git cat-file -e $(grep "Closure commit" .planning/phases/29-final-polishment/29-UAT-LOG.md | awk -F'|' '{print $3}' | tr -d ' ') && echo "SHA resolves"
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "SUPERSEDED-BY-PHASE-31" .planning/phases/29-final-polishment/29-VERIFICATION.md && grep -c "TD-01 SUPERSEDED" .planning/phases/29-final-polishment/29-UAT-LOG.md && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUPERSEDED-BY-PHASE-31" .planning/phases/29-final-polishment/29-VERIFICATION.md` returns at least 1.
    - `grep -c "TD-01 SUPERSEDED" .planning/phases/29-final-polishment/29-UAT-LOG.md` returns at least 1.
    - `grep "SATISFIED" .planning/phases/29-final-polishment/29-UAT-LOG.md` still shows the original TD-01 SATISFIED row (preservation verified).
    - `grep -c "VERIFIED / SUPERSEDED" .planning/phases/29-final-polishment/29-VERIFICATION.md` returns at least 1 (Observable Truth row updated).
    - `grep "Closure commit" .planning/phases/29-final-polishment/29-UAT-LOG.md` shows a 7+ character hex SHA, not the string `PENDING`.
    - The SHA in the `Closure commit` cell resolves to the Step-5 content-bearing commit (the one whose subject is `refactor(29): supersede TD-01 ...`), NOT the Step-8 docs-only amendment. Verify: `git log --format=%s $(grep "Closure commit" 29-UAT-LOG.md | awk -F'|' '{print $3}' | tr -d ' ') -1` starts with `refactor(29): supersede TD-01`.
    - `git status --porcelain | wc -l` returns 0.
    - `git log -2 --format=%s` shows the two commit subjects in order (amend most recent, then main closure).
  </acceptance_criteria>
  <done>TD-04 supersession fully recorded in code + 29 docs; working tree clean; backfilled SHA references the content-bearing commit.</done>
</task>

</tasks>

<verification>
- Test suite delta: `cd /Users/Code/EchoLearn/app && npm test 2>&1 | tail -5` shows fail count drop by 6 compared to Plan 33-00 baseline.
- `node --test tests/services/orchestration-strategy.test.mjs` returns fail 0.
- 29-VERIFICATION.md + 29-UAT-LOG.md grep-verified (see acceptance criteria).
- `git status --porcelain` empty after both commits land.
</verification>

<success_criteria>
- concept-feed-strategy.test.mjs deleted; orchestration-strategy.test.mjs trimmed to 9 tests.
- 29-VERIFICATION.md TD-01 row + Observable Truth row 1 rewritten.
- 29-UAT-LOG.md append with original SATISFIED row preserved.
- Two commits on the branch (supersession closure commit + SHA backfill amendment, in that order).
- Backfilled SHA in UAT-LOG's `Closure commit` cell points at the content-bearing supersession commit, not the docs-only amendment.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-02-SUMMARY.md` with both commit SHAs, list of doc edits, test-delta measurement, and the captured backfilled SHA.
</output>
