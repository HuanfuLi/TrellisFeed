---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 05
type: execute
wave: 0
depends_on: []
status: satisfied-by-precedent
satisfied_by: 6066c709
satisfied_date: 2026-04-19
files_modified:
  - app/src/components/InfoFlow.tsx
  - app/src/components/SuggestionCard.tsx
  - app/src/components/VineProgress.tsx
  - app/src/services/canonical-knowledge.service.ts
  - app/src/services/concept-feed-dedup.ts
  - app/src/services/concept-feed.service.ts
  - app/src/services/youtube.service.ts
  - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md
  - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
autonomous: true
requirements: [WAVE-4-WIP]
gap_closure: false
must_haves:
  truths:
    - "All 7 Wave-4 follow-on source-file fixes commit cleanly in ONE fix(32.1) commit (per D-29 atomic-flush spirit, retargeted to actual current WIP)"
    - "The 2 Phase 32.1 UAT progress-note doc files commit in ONE follow-up docs(32.1) commit (kept separate from code so a future bisect can revert the bugfix without losing UAT notes)"
    - "Per-file diff review (D-30) confirms zero debug logs (only dev-mode-gated console.warn allowed), no commented-out code blocks, no leaked secrets, no half-implemented features, no unrelated experiments"
    - "Targeted test gate (D-31, broadened to current WIP coverage): canonical-knowledge.test, concept-feed-cross-cycle-dedup.test, post-essay.service.test, bundle-parity.test all pass BEFORE the flush commits land"
    - "After the commits, `git status --porcelain` is empty of source/doc WIP (only .planning/STATE.md may remain — that's orchestrator-managed) AND `npx tsc -b --noEmit` exits 0 — Wave 1 plans are unblocked"
    - "Source commit subject is exactly `fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)`"
    - "Docs commit subject is exactly `docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)`"
  artifacts:
    - path: "app/src/components/InfoFlow.tsx"
      provides: "Bug D — generalize wouldRenderVisual exhaustive check (replaces narrow isFailedImage); dev-mode console.warn surfaces fallback hits"
      state: "committed"
    - path: "app/src/components/SuggestionCard.tsx"
      provides: "Match suggestion-card styling to concept-post cards (operator-reported flat-card complaint, 2026-04-19) — gradient bg + 1.5px border + shadow-2"
      state: "committed"
    - path: "app/src/components/VineProgress.tsx"
      provides: "Bug 7 — vine spans full container width via measured ResizeObserver-driven svgWidth (replaces fixed 300 viewBox + N/(N+1) flower spacing)"
      state: "committed"
    - path: "app/src/services/canonical-knowledge.service.ts"
      provides: "Bug 8/9 — combined cluster+anchor naming prompt for new branches (replaces lazy `${branchName} fundamentals` + raw question.title placeholders); buildNewBranchClusterAnchorPrompt + parseClusterAnchorResponse exports"
      state: "committed"
    - path: "app/src/services/concept-feed-dedup.ts"
      provides: "Bug 14 — backfill seenVideoIds from echolearn_post_history on first dedup access per session (supersedes original D-02 in-memory-only); per-day cutoff preserved"
      state: "committed"
    - path: "app/src/services/concept-feed.service.ts"
      provides: "Bug A (cycle-stamp video/news/short post IDs to escape dedup loss), Bug 6 (rotate YouTube query modifier per cycle, widen pool to 15), Bug 10 (round-robin session-post provenance to session anchors)"
      state: "committed"
    - path: "app/src/services/youtube.service.ts"
      provides: "Bug C — defensive filter for non-video items (channelId/playlistId leakage despite &type=video URL param)"
      state: "committed"
    - path: ".planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md"
      provides: "Tests 10-18 added covering Wave 4 follow-on bugs; updated frontmatter timestamp"
      state: "committed"
    - path: ".planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md"
      provides: "Wave 4 progress notes appended; gap statuses tracked"
      state: "committed"
  key_links:
    - from: "git working tree (source files)"
      to: "single fix(32.1) commit on main branch"
      via: "explicit `git add <7-source-file-list>` then `git commit -m \"fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)\"`"
      pattern: "fix\\(32\\.1\\): Wave 4 follow-on bug fixes"
    - from: "git working tree (doc files)"
      to: "single docs(32.1) commit on main branch"
      via: "explicit `git add <2-doc-file-list>` then `git commit -m \"docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)\"`"
      pattern: "docs\\(32\\.1\\): UAT progress notes for tests 10-18"
    - from: "targeted test suite"
      to: "all specified test files exit 0"
      via: "node --test"
      pattern: "fail 0"
    - from: "tsc baseline"
      to: "exit 0 after both commits land"
      via: "npx tsc -b --noEmit"
      pattern: "exit code 0"
---

<objective>
Phase 33 v2 prerequisite: re-flush the Phase 32.1 Wave 4 follow-on WIP that has accumulated since Plan 33-05 was originally written. The originally-planned WIP set (ScrollToTopFAB, locale bundles, PlannerScreen, etc.) was already committed in `f8e82950` + the Wave 4 fix train (`b2061554`, `808c6e85`, `db918264`, `3263af4e`, `98692338`, `961347ec`). The CURRENT working tree carries a different set of 7 source-file bug fixes and 2 UAT progress notes — these must land BEFORE Plans 33-06 (perf memoization) or 33-07 (cosmetic polish) execute, otherwise their edits will collide with stale uncommitted work.

This plan splits the flush into TWO atomic commits (mirroring recent git-history style, e.g. `f2aef283` docs separate from `db918264` chore):
- Commit 1: `fix(32.1)` — 7 source files, real bug fixes for Bugs A/C/D + 6/7/8/9/10/14.
- Commit 2: `docs(32.1)` — 2 UAT progress-note files documenting the same bugs for operator testing.

Per CONTEXT.md decisions D-29/D-30/D-31:
- D-29: Atomic flush commits with the exact specified subject lines.
- D-30: Per-file diff review (no debug logs, no commented-out blocks, no half-implemented work, no unrelated experiments). Dev-mode-gated `console.warn` IS permitted (already present in InfoFlow.tsx fallback path — intentional regression detector per CLAUDE.md best practice #2).
- D-31: All targeted tests must pass at the flush boundary; do NOT skip a failing test — pause and fix root cause first.

Output: source/doc WIP cleared from working tree (only `.planning/STATE.md` may remain — orchestrator-managed); two commits with exact specified subjects; tsc still exit 0; targeted tests still green.

**Scope boundary:** This plan ONLY commits the existing Wave 4 follow-on working-tree changes. It does NOT modify behavior, refactor code, or add new features. If a per-file review surfaces any issue, HALT and escalate — do not "fix while flushing".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Per-file diff review (D-30 policy gate)</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-29, D-30, D-31)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md (commit-style reference)
    - CLAUDE.md "Best practices learned in Phase 32.1" — especially rules #1, #2, #5, #6 (these explain why dev-mode console.warn in InfoFlow.tsx is intentional, not a debug-log violation)
    - CLAUDE.md "News post pipeline" + "Concept Feed Generation Pipeline" + "Anchor name normalization" sections — load-bearing systems some of the WIP files touch
  </read_first>
  <action>
    Step 1 — Confirm working-tree state matches the current actual WIP (re-baselined 2026-04-19 after the original 33-05 file set was already flushed in `f8e82950`):

    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    ```

    Expected (modulo ordering; `.planning/STATE.md` may also appear — that's orchestrator-managed and is NOT part of this flush):
    ```
     M app/src/components/InfoFlow.tsx
     M app/src/components/SuggestionCard.tsx
     M app/src/components/VineProgress.tsx
     M app/src/services/canonical-knowledge.service.ts
     M app/src/services/concept-feed-dedup.ts
     M app/src/services/concept-feed.service.ts
     M app/src/services/youtube.service.ts
     M .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md
     M .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
    ```

    If the working tree contains ADDITIONAL source/doc files NOT in this list (i.e. anything besides `.planning/STATE.md` plus the 9 above), HALT and escalate — do not silently broaden the flush scope.

    Step 2 — For each file, run `git diff` and apply the D-30 review checklist:

    ```
    cd /Users/Code/EchoLearn && git diff app/src/components/InfoFlow.tsx
    cd /Users/Code/EchoLearn && git diff app/src/components/SuggestionCard.tsx
    cd /Users/Code/EchoLearn && git diff app/src/components/VineProgress.tsx
    cd /Users/Code/EchoLearn && git diff app/src/services/canonical-knowledge.service.ts
    cd /Users/Code/EchoLearn && git diff app/src/services/concept-feed-dedup.ts
    cd /Users/Code/EchoLearn && git diff app/src/services/concept-feed.service.ts
    cd /Users/Code/EchoLearn && git diff app/src/services/youtube.service.ts
    cd /Users/Code/EchoLearn && git diff .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md
    cd /Users/Code/EchoLearn && git diff .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
    ```

    Per-file D-30 checklist (HALT and escalate on ANY hit):
    1. **Debug logs:** Any `console.log` (other than dev-mode-gated `if (import.meta.env.DEV) console.log/warn/error`)? `console.error` with stack-trace dumping unrelated to error-handling? `debugger;` statements? **NOTE:** InfoFlow.tsx contains an intentional `if (import.meta.env.DEV) console.warn(...)` for the wouldRenderVisual fallback — this is the regression detector mandated by CLAUDE.md best practice #2 ("Tests must guard the LIVE code path") and is NOT a violation.
    2. **Commented-out blocks:** Multi-line `// ...` or `/* ... */` blocks containing actual code (not just doc comments)?
    3. **Secret leakage:** Hard-coded API keys (`sk-...`, `pk-...`, anything matching `[A-Za-z0-9]{30,}` in a string literal that looks like a token)? `localhost` URLs that should be relative?
    4. **Half-implemented features:** New `TODO` / `FIXME` / `XXX` comments? Unused imports added then not consumed? Functions added without callers? Type annotations like `any` introduced without justification?
    5. **Unrelated experiments:** Refactors that go beyond what each file's stated Wave 4 disposition would imply (per the artifacts table in this plan's frontmatter)?

    Cross-check load-bearing CLAUDE.md systems:
    - **InfoFlow.tsx** — must NOT touch the wouldRenderVisual exhaustive check semantics in a way that re-narrows it (Phase 32.1 Wave 4 D-W4-03 invariant). The diff should ONLY broaden coverage, not narrow it.
    - **concept-feed.service.ts** news branch — must STILL set `bodyMarkdown: ''` (no eager LLM, no `result.content` assignment). Re-read the diff specifically for the news block.
    - **concept-feed.service.ts** session-post path — round-robin provenance fix must NOT alter the daily-path's provenance (only fills gaps when `sourceQuestionIds.length === 0`).
    - **canonical-knowledge.service.ts** — anchor names from the new combined prompt must STILL flow through `normalizeAnchorName()` before persistence (verify by reading the call site, not just the new prompt builder).
    - **concept-feed-dedup.ts** — must NOT import `postHistoryService` (would pull settings → llm → i18n chain and break `node --test`). Verify by grepping the diff for `import.*postHistoryService`.

    If the diff for any file fails the checklist or the cross-check:
    - DO NOT modify the file in this plan.
    - DO NOT use `git stash` or `git checkout --` (destructive — preserves nothing).
    - HALT, return to the orchestrator with: filename, offending content excerpt, and recommendation.

    Step 3 — Capture the per-file disposition (will be referenced in 33-05-SUMMARY.md). For each file, record one of:
    - `clean — proceed with flush`
    - `HALT — <reason>`
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git status --porcelain | grep -vE '^\s*M\s+\.planning/STATE\.md$' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` lists exactly the 9 files above (plus optionally `.planning/STATE.md`).
    - For every file, `git diff` was executed and reviewed against the D-30 checklist + load-bearing cross-checks.
    - Zero files trigger any of the 5 D-30 violation classes; OR if any file does, the orchestrator received a HALT with details and Tasks 5.2/5.3/5.4 were NOT executed.
    - No new files appear in the working tree beyond the expected set (i.e., this task did not modify any source).
  </acceptance_criteria>
  <done>All 9 Wave 4 follow-on files reviewed; clean disposition recorded for each; ready for Task 5.2 test gate.</done>
</task>

<task type="auto">
  <name>Task 5.2: Run targeted test suite (D-31 gate, broadened to current WIP coverage)</name>
  <read_first>
    - app/tests/canonical-knowledge.test.mjs (covers buildStepPrompt + normalizeAnchorName surface — guards Bug 8/9 area)
    - app/tests/services/concept-feed-cross-cycle-dedup.test.mjs (covers the dedup path — guards Bug 14 area)
    - app/tests/services/post-essay.service.test.mjs (guards news path invariant — `bodyMarkdown: ''` MUST hold; would catch any regression in the concept-feed.service news branch)
    - app/tests/locales/bundle-parity.test.mjs (no locale changes in this WIP, but cheap to assert nothing broke)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-31 verbatim)
    - CLAUDE.md "Best practices learned in Phase 32.1" rule #2 ("Tests must guard the LIVE code path")
  </read_first>
  <action>
    Step 1 — Run the broadened D-31 targeted test suite as a single command:

    ```
    cd /Users/Code/EchoLearn/app && node --test tests/canonical-knowledge.test.mjs tests/services/concept-feed-cross-cycle-dedup.test.mjs tests/services/post-essay.service.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | tail -20
    ```

    Capture the output. Expected outcome:
    - All 4 suites report `fail 0`.
    - Aggregate pass count typically ~17-25 depending on internal sub-tests; the load-bearing assertion is `fail 0`.

    Step 2 — If `fail` count > 0:
    - Do NOT proceed to Tasks 5.3 / 5.4.
    - Do NOT delete or skip the failing test.
    - Investigate the root cause: read the failing test source, read the live code it asserts on, identify whether the assertion is stale (test needs update) or the live code regressed (code needs fix).
    - Fix the root cause via Edit (either the test OR the code, depending on diagnosis), re-run the suite, and only proceed once `fail 0` holds.
    - Record the fix in this plan's eventual SUMMARY ("Pre-flush fix" subsection).

    Step 3 — Tsc clean check (ensures the WIP files compile):

    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    ```

    Expected: `tsc-exit=0`. If non-zero, STOP and diagnose — the WIP introduced a type regression that must be fixed before flush.

    Step 4 — Run full test suite as a regression sweep (precaution per user's "be very cautious not to introduce regressions" directive):

    ```
    cd /Users/Code/EchoLearn/app && node --test --test-reporter=spec tests/**/*.test.mjs 2>&1 | tail -15
    ```

    Expected: `fail 0` aggregate. If any failure surfaces here that wasn't in the targeted suite, treat it the same way as Step 2 (root-cause + fix BEFORE Task 5.3).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/canonical-knowledge.test.mjs tests/services/concept-feed-cross-cycle-dedup.test.mjs tests/services/post-essay.service.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | grep -E "^# (tests|pass|fail)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - The aggregate `fail` count from the targeted suite is exactly `0`.
    - The aggregate `fail` count from the full-suite regression sweep is exactly `0`.
    - `npx tsc -b --noEmit` exits 0 (captured stdout shows `tsc-exit=0`).
    - If any failure was found, a root-cause fix landed BEFORE Task 5.3 began (recorded in the eventual SUMMARY).
  </acceptance_criteria>
  <done>D-31 targeted test gate + full regression sweep both pass; tsc clean; flush is safe to commit.</done>
</task>

<task type="auto">
  <name>Task 5.3: Stage source files explicitly and commit fix(32.1)</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-29 commit-subject discipline)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md (commit-message style reference)
    - CLAUDE.md `<git_safety>` (no `git add .` / no `git add -A`)
  </read_first>
  <action>
    Step 1 — Stage the 7 source files explicitly. Do NOT use `git add .` or `git add -A`.

    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/components/InfoFlow.tsx \
      app/src/components/SuggestionCard.tsx \
      app/src/components/VineProgress.tsx \
      app/src/services/canonical-knowledge.service.ts \
      app/src/services/concept-feed-dedup.ts \
      app/src/services/concept-feed.service.ts \
      app/src/services/youtube.service.ts
    ```

    Step 2 — Verify the staging set matches what's expected (no surprises — must NOT include the 2 doc files yet, must NOT include `.planning/STATE.md`):

    ```
    cd /Users/Code/EchoLearn && git diff --cached --name-only
    ```

    Output should list exactly the 7 source files. If anything else appears, unstage it:
    ```
    cd /Users/Code/EchoLearn && git reset HEAD <unexpected-file>
    ```

    Step 3 — Commit with the exact subject via heredoc to preserve formatting:

    ```
    cd /Users/Code/EchoLearn && git commit -m "$(cat <<'EOF'
    fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)

    Second tranche of Phase 32.1 Wave 4 fixes after the fix train through
    db918264. Per Phase 33 CONTEXT D-29/D-30/D-31:

    - Per-file diff review (D-30) confirmed clean: no debug logs (the dev-mode
      console.warn in InfoFlow.tsx is intentional regression-detector per
      CLAUDE.md best practice #2), no commented-out blocks, no secret leakage,
      no half-implemented features, no unrelated experiments.
    - Targeted test suite (D-31, broadened): canonical-knowledge,
      concept-feed-cross-cycle-dedup, post-essay.service, bundle-parity all pass.
    - Full-suite regression sweep also clean.
    - tsc -b --noEmit exits 0.

    Bug coverage:
    - Bug A — concept-feed.service.ts: cycle-stamp video/short/news post IDs so
      the same anchor across multiple refill cycles produces unique IDs (escapes
      seenPostIds dedup loss that caused "1 fresh post per swipe" symptom).
    - Bug C — youtube.service.ts: defensive filter rejects items without a
      truthy videoId (channelId/playlistId leakage despite &type=video).
    - Bug D — InfoFlow.tsx: replace narrow isFailedImage check with exhaustive
      wouldRenderVisual; dev-mode console.warn surfaces fallback hits in logs.
    - Bug 6 — concept-feed.service.ts: rotate YouTube query modifier per cycle
      (5 video modifiers, 4 short modifiers); widen pool from 3 to 15.
    - Bug 7 — VineProgress.tsx: ResizeObserver-driven dynamic svgWidth so vine
      spans full container; flowers spread evenly with edge padding.
    - Bug 8/9 — canonical-knowledge.service.ts: combined cluster+anchor naming
      prompt for new branches (replaces "${branchName} fundamentals" + raw
      question.title placeholders); buildNewBranchClusterAnchorPrompt and
      parseClusterAnchorResponse exports added.
    - Bug 10 — concept-feed.service.ts session path: round-robin
      sourceQuestionIds/Titles to session anchors when LLM omits them, so all
      session posts get concept badges matching the daily-path contract.
    - Bug 14 — concept-feed-dedup.ts: lazy backfill seenVideoIds from
      echolearn_post_history on first access per session (supersedes original
      D-02 in-memory-only choice); per-day cutoff preserved.
    - SuggestionCard.tsx: match suggestion-card visual to concept-post cards
      (gradient bg + 1.5px border + shadow-2) — operator-reported flat-card
      complaint, 2026-04-19.

    Files (7):
    - app/src/components/InfoFlow.tsx
    - app/src/components/SuggestionCard.tsx
    - app/src/components/VineProgress.tsx
    - app/src/services/canonical-knowledge.service.ts
    - app/src/services/concept-feed-dedup.ts
    - app/src/services/concept-feed.service.ts
    - app/src/services/youtube.service.ts

    UAT progress notes for these bugs land in a follow-up docs(32.1) commit.
    Unblocks Plans 33-06 (perf memoization) and 33-07 (cosmetic polish).
    EOF
    )"
    ```

    Step 4 — Verify the commit landed cleanly:

    ```
    cd /Users/Code/EchoLearn && git log -1 --format='%H %s'
    cd /Users/Code/EchoLearn && git status --porcelain
    ```

    Expected:
    - `git log -1` shows the new commit SHA + the EXACT subject line `fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)`.
    - `git status --porcelain` still shows the 2 doc files (and possibly `.planning/STATE.md`) — no source files left unstaged.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git log -1 --format='%s'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` returns exactly: `fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)`.
    - `git diff HEAD~1 --name-only` lists exactly the 7 source files (no docs, no STATE.md).
    - `git diff --cached --name-only` returns nothing (no staged changes left over).
    - `git status --porcelain` still shows the 2 doc files modified (will be flushed in Task 5.4).
    - `npx tsc -b --noEmit` exits 0 after the commit.
    - `git add .` and `git add -A` were NOT used (verifiable via Bash invocation history — only explicit file paths in the `git add` argument list).
  </acceptance_criteria>
  <done>Source-file fix(32.1) commit landed; working tree still has the 2 doc files staged for Task 5.4.</done>
</task>

<task type="auto">
  <name>Task 5.4: Stage doc files explicitly and commit docs(32.1)</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-29 commit-subject discipline)
    - CLAUDE.md `<git_safety>` (no `git add .` / no `git add -A`)
  </read_first>
  <action>
    Step 1 — Stage the 2 doc files explicitly:

    ```
    cd /Users/Code/EchoLearn && git add \
      .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md \
      .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
    ```

    Step 2 — Verify staging set:

    ```
    cd /Users/Code/EchoLearn && git diff --cached --name-only
    ```

    Output should list exactly those 2 files. If `.planning/STATE.md` snuck in, unstage:
    ```
    cd /Users/Code/EchoLearn && git reset HEAD .planning/STATE.md
    ```

    Step 3 — Commit with the exact docs subject:

    ```
    cd /Users/Code/EchoLearn && git commit -m "$(cat <<'EOF'
    docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)

    Documents the Wave 4 follow-on bug fixes landed in the prior fix(32.1) commit
    so the operator can run device UAT against the new code. Adds 9 new test
    entries to 32.1-HUMAN-UAT.md (Tests 10-18: Bugs A/B/C/D + 14/15 + 6 + 8/9
    + SuggestionCard + VineProgress) and tracking notes to 32.1-VERIFICATION.md.

    No code change. Pure UAT/verification doc updates.

    Files (2):
    - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-HUMAN-UAT.md
    - .planning/phases/32.1-v1-4-uat-retest-gap-closure/32.1-VERIFICATION.md
    EOF
    )"
    ```

    Step 4 — Verify the commit landed cleanly:

    ```
    cd /Users/Code/EchoLearn && git log -1 --format='%H %s'
    cd /Users/Code/EchoLearn && git status --porcelain
    ```

    Expected:
    - `git log -1` shows the docs commit SHA + EXACT subject `docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)`.
    - `git status --porcelain` is empty OR shows only `.planning/STATE.md` (orchestrator-managed).

    Step 5 — Final tsc + targeted test re-verify (sanity gate after both commits):

    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    cd /Users/Code/EchoLearn/app && node --test tests/canonical-knowledge.test.mjs tests/services/concept-feed-cross-cycle-dedup.test.mjs tests/services/post-essay.service.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | tail -8
    ```

    Both must still show clean state (tsc-exit=0, fail 0).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git log -1 --format='%s' && echo "---" && git status --porcelain | grep -vE '^\s*M\s+\.planning/STATE\.md$' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` returns exactly: `docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)`.
    - `git status --porcelain | grep -vE '^\s*M\s+\.planning/STATE\.md$' | wc -l` returns `0` (working tree clean except possibly the orchestrator-managed STATE.md).
    - `git diff HEAD~1 --name-only` lists exactly the 2 doc files.
    - `git diff --cached --name-only` returns nothing.
    - `npx tsc -b --noEmit` exits 0 after the commit.
    - The targeted test suite still reports `fail 0` after the commit.
    - `git add .` and `git add -A` were NOT used.
  </acceptance_criteria>
  <done>Both commits landed; source-WIP and doc-WIP cleared; downstream Plans 33-06/33-07 unblocked.</done>
</task>

</tasks>

<verification>
- Source-WIP and doc-WIP cleared: `cd /Users/Code/EchoLearn && git status --porcelain | grep -vE '^\s*M\s+\.planning/STATE\.md$' | wc -l | tr -d ' '` returns `0`.
- Source commit subject exact: `cd /Users/Code/EchoLearn && git log -2 --format='%s' | head -2 | tail -1` returns `fix(32.1): Wave 4 follow-on bug fixes (Bugs A/C/D + 6/7/8/9/10/14)`.
- Docs commit subject exact: `cd /Users/Code/EchoLearn && git log -1 --format='%s'` returns `docs(32.1): UAT progress notes for tests 10-18 (Wave 4 follow-on)`.
- Targeted tests green: `cd /Users/Code/EchoLearn/app && node --test tests/canonical-knowledge.test.mjs tests/services/concept-feed-cross-cycle-dedup.test.mjs tests/services/post-essay.service.test.mjs tests/locales/bundle-parity.test.mjs` reports `fail 0`.
- Full-suite sweep green: `cd /Users/Code/EchoLearn/app && node --test tests/**/*.test.mjs 2>&1 | tail -5` reports `fail 0`.
- tsc clean: `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
</verification>

<success_criteria>
- All 9 Wave 4 follow-on files committed across exactly 2 atomic commits (fix + docs) per D-29 (retargeted to actual current WIP).
- D-30 per-file diff review applied; no policy violations.
- D-31 targeted test gate (broadened to current WIP) passed.
- Full-suite regression sweep clean.
- Source-WIP and doc-WIP cleared post-commits; tsc clean; targeted tests still green.
- Plans 33-06 and 33-07 unblocked (their depends_on: [33-05] is satisfied).
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-05-SUMMARY.md` with:
- Both commit SHAs + exact subject lines
- Per-file disposition table (mirroring 33-00-SUMMARY.md format)
- Targeted test output (pass/fail counts) — both pre-commit gate and post-commit re-verify
- Full-suite regression-sweep result
- tsc exit code post-commits
- Any pre-flush fixes that landed (with file + reason)
- Confirmation that Wave 1 plans (33-06, 33-07) are unblocked
- Note explaining the file-set divergence: original 33-05 file set was already flushed in `f8e82950`; this re-aimed at the post-Wave 4 actual current WIP
</output>
