---
phase: 27-add-i18n-l10n-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/src/locales/index.ts
  - app/src/locales/i18n.d.ts
  - app/src/lib/locale.ts
  - app/src/types/index.ts
  - app/src/services/settings.service.ts
  - app/src/main.tsx
  - app/tests/locales/bundle-parity.test.mjs
  - app/tests/locales/missing-key.test.mjs
  - app/tests/locales/data-locale-attr.test.mjs
  - app/tests/lib/locale-detect.test.mjs
  - app/tests/lib/date.locale.test.mjs
  - app/tests/providers/llm-locale-injection.test.mjs
  - app/tests/providers/tts-locale.test.mjs
  - app/tests/services/youtube-locale.test.mjs
  - app/tests/services/web-search-no-locale.test.mjs
  - app/tests/services/settings-locale.test.mjs
  - app/tests/state/useQuestions-locale-abort.test.mjs
autonomous: true
requirements: [D-01, D-03, D-04, D-05, D-06, D-16, D-17, D-20, D-21]

must_haves:
  truths:
    - "i18next + react-i18next + @capacitor/device installed and importable"
    - "i18n instance initializes synchronously before React render (no Suspense flash)"
    - "UserPreferences has locale field; legacy language field migrated on load"
    - "normalizeLocale correctly maps BCP-47 variants to 'en'|'zh'|'es'|'ja' (or 'en' fallback)"
    - "TypeScript typo in t('home.titlee') fails tsc compile (module augmentation wired)"
    - "All 11 Wave-0 test files exist and execute (exit 0 or documented xfail)"
    - "D-16 negative coverage: NO plan in phase 27 modifies stored user-content paths for translation purposes — specifically no plan's files_modified lists any of: app/src/services/session.service.ts, app/src/services/flashcard.service.ts, app/src/services/question.service.ts, app/src/services/anchor.service.ts, app/src/services/cluster.service.ts, app/src/services/canonical-knowledge.ts, or any *.service.ts file that persists user-generated content. Verified by: `grep -lE 'files_modified:.*\\b(session|flashcard|question|anchor|cluster|canonical-knowledge)\\.service\\.ts' .planning/phases/27-add-i18n-l10n-support/27-*-PLAN.md` returns empty."
  artifacts:
    - path: app/src/locales/en.json
      provides: "Canonical EN bundle with namespace scaffolds (home/planner/ask/review/podcast/settings/onboarding/posts/graph/common)"
      contains: '"common"'
    - path: app/src/locales/zh.json
      provides: "ZH placeholder bundle (EN values duplicated; Sonnet subagent replaces in Plan 07)"
    - path: app/src/locales/es.json
      provides: "ES placeholder bundle"
    - path: app/src/locales/ja.json
      provides: "JA placeholder bundle"
    - path: app/src/locales/index.ts
      provides: "i18n.init() with resources, fallbackLng='en', useSuspense:false, missingKeyHandler, data-locale listener, LOCALE_NAMES map, SUPPORTED_LOCALES export"
      exports: ["default", "SUPPORTED_LOCALES", "LOCALE_NAMES", "SupportedLocale"]
    - path: app/src/locales/i18n.d.ts
      provides: "Module augmentation — typeof en.json feeds CustomTypeOptions.resources for type-safe t() keys"
      contains: "declare module 'i18next'"
    - path: app/src/lib/locale.ts
      provides: "normalizeLocale() + detectInitialLocale() + detectDeviceLocale() (Capacitor Device)"
      exports: ["normalizeLocale", "detectInitialLocale", "detectDeviceLocale"]
    - path: app/src/types/index.ts
      provides: "AppPreferences extended with locale: SupportedLocale; language marked deprecated"
      contains: "locale: SupportedLocale"
    - path: app/src/services/settings.service.ts
      provides: "defaultSettings.preferences.locale = 'en'; one-time migration copies legacy language → locale on load()"
      contains: "locale:"
    - path: app/src/main.tsx
      provides: "side-effect import './locales' runs BEFORE createRoot"
      contains: "import './locales'"
    - path: app/tests/locales/bundle-parity.test.mjs
      provides: "Asserts en/zh/es/ja flattened key sets are equal"
    - path: app/tests/locales/missing-key.test.mjs
      provides: "Asserts missingKeyHandler fires + fallback renders EN text (never raw key)"
    - path: app/tests/locales/data-locale-attr.test.mjs
      provides: "jsdom-lite: asserts documentElement.dataset.locale is set on init and updates on changeLanguage"
    - path: app/tests/lib/locale-detect.test.mjs
      provides: "normalizeLocale matrix: en-US, zh-CN, zh-Hans-CN, zh-TW, es-419, ja-JP, ko-KR, fr-FR, '', null"
    - path: app/tests/lib/date.locale.test.mjs
      provides: "Skeleton test (real assertions filled in Plan 02) — MUST still exit 0 today using placeholder expectations"
    - path: app/tests/providers/llm-locale-injection.test.mjs
      provides: "Skeleton for applyLocaleDirective (filled by Plan 02)"
    - path: app/tests/providers/tts-locale.test.mjs
      provides: "Skeleton for synthesize voice mapping (filled by Plan 02)"
    - path: app/tests/services/youtube-locale.test.mjs
      provides: "Skeleton for YouTube URL params (filled by Plan 02)"
    - path: app/tests/services/web-search-no-locale.test.mjs
      provides: "Skeleton asserting Tavily body never contains locale markers (filled by Plan 02)"
    - path: app/tests/services/settings-locale.test.mjs
      provides: "Round-trip locale persistence + legacy-language migration"
    - path: app/tests/state/useQuestions-locale-abort.test.mjs
      provides: "Skeleton for mid-stream abort (filled by Plan 04)"
  key_links:
    - from: app/src/main.tsx
      to: app/src/locales/index.ts
      via: "side-effect import './locales' before createRoot"
      pattern: "import ['\"]\\./locales['\"]"
    - from: app/src/locales/index.ts
      to: app/src/lib/locale.ts
      via: "detectInitialLocale import"
      pattern: "import.*detectInitialLocale"
    - from: app/src/services/settings.service.ts
      to: app/src/types/index.ts
      via: "AppPreferences.locale field"
      pattern: "locale:"
    - from: app/src/locales/i18n.d.ts
      to: app/src/locales/en.json
      via: "typeof en feeds CustomTypeOptions"
      pattern: "typeof en"
---

<objective>
Foundation + Wave 0. Install i18n dependencies, create locale bundle scaffolds (EN populated, ZH/ES/JA as EN-duplicate placeholders for parity — translated in Plan 07), wire i18n.init() synchronously in main.tsx, add UserPreferences.locale with legacy-language migration, and create ALL Wave 0 test files (VALIDATION.md gap closure). Blocks every other plan in the phase.

Purpose: Per D-04/D-05/D-06/D-21 — ship a synchronous, type-safe, offline-first i18n skeleton that all subsequent plans (LLM/TTS/YouTube injection, UI extraction, Sonnet translation) can build against. Per VALIDATION.md Wave 0 Requirements — satisfy the Nyquist contract so each downstream task has a runnable test.

Output: Installable dependencies, a running app with `i18n.language === 'en'` on first render, persisted locale across restart, and an executable test suite that all later plans extend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md
@.planning/phases/27-add-i18n-l10n-support/27-RESEARCH.md
@.planning/phases/27-add-i18n-l10n-support/27-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@app/src/main.tsx
@app/src/services/settings.service.ts
@app/src/types/index.ts
@app/src/lib/date.ts
@app/tests/canonical-knowledge.test.mjs

<interfaces>
<!-- Extracted from codebase so executor doesn't have to explore -->

From app/src/types/index.ts (lines 291-299, current):
```typescript
export interface AppPreferences {
  theme: 'light' | 'dark' | 'system';
  language: string;              // ← existing, default 'en'; will be DEPRECATED
  onboardingCompleted: boolean;
  aiConsentGiven?: boolean;
  askMonthlyLimit?: number;
}
```

From app/src/services/settings.service.ts (lines 48-54, current):
```typescript
preferences: {
  theme: 'system',
  language: 'en',
  onboardingCompleted: false,
  aiConsentGiven: false,
  askMonthlyLimit: 0,
}
```

From app/src/main.tsx (current):
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import App from './App.tsx';
import { AppProvider } from './state/AppProvider.tsx';
import { applyTheme } from './lib/theme';
import { settingsService } from './services/settings.service';
applyTheme(settingsService.getSync().preferences.theme);
createRoot(document.getElementById('root')!).render(
  <StrictMode><AppProvider><App /></AppProvider></StrictMode>,
);
```

Test runner pattern (from app/tests/canonical-knowledge.test.mjs):
```javascript
import assert from 'node:assert/strict';
import test from 'node:test';
import { foo } from '../src/services/canonical-knowledge.service.ts';  // .ts import works via esbuild loader
```

package.json test script: `"test": "node --test tests/**/*.test.mjs"` — no jsdom. For data-locale-attr.test.mjs, executor must set up a minimal globalThis.document shim inline (see action).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create locale bundle scaffolds (EN canonical, ZH/ES/JA parity placeholders), wire i18n.init() + main.tsx</name>

  <files>app/package.json, app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json, app/src/locales/index.ts, app/src/locales/i18n.d.ts, app/src/main.tsx, app/tsconfig.json</files>
  <read_first>
    - app/package.json
    - app/src/main.tsx
    - app/src/lib/theme.ts
    - .planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md (D-02, D-04, D-05, D-21)
    - .planning/phases/27-add-i18n-l10n-support/27-RESEARCH.md (sections "Standard Stack", "Pattern 1: Pre-Bundled Static Init", "LLM Provider Central Locale Injection" for LOCALE_NAMES)
  </read_first>
  <action>
    Step 1: From `/Users/Code/EchoLearn/app`, install locked versions:
    ```bash
    npm install i18next@26.0.5 react-i18next@17.0.3 @capacitor/device@8.0.2
    npx cap sync
    ```

    Step 2: Create `app/src/locales/en.json` as the canonical bundle with namespace scaffolds. Minimum seed (Plans 05/06 extend):
    ```json
    {
      "common": {
        "today": "Today",
        "cancel": "Cancel",
        "save": "Save",
        "delete": "Delete",
        "greeting": { "morning": "Good Morning", "afternoon": "Good Afternoon", "evening": "Good Evening" }
      },
      "home": { "title": "Home" },
      "planner": { "title": "Planner" },
      "ask": { "title": "Ask", "localeChangedDiscarded": "Locale changed — answer discarded. Ask again." },
      "review": { "title": "Review" },
      "graph": { "title": "Knowledge Graph" },
      "settings": { "title": "Settings", "language": { "label": "Language / 语言 / Idioma / 言語", "description": "Choose your display language" } },
      "podcast": { "title": "Podcast" },
      "posts": { "title": "Posts" },
      "onboarding": { "language": { "title": "Language / 语言 / Idioma / 言語", "continue": "Continue" } }
    }
    ```

    Step 3: Create `app/src/locales/zh.json`, `es.json`, `ja.json` as EXACT DUPLICATES of `en.json` (parity stubs — Plan 07's Sonnet subagent replaces values later). This satisfies D-03 parity test from Wave 0; Plan 07 converts values to translations.

    Step 4: Create `app/src/locales/index.ts` verbatim per RESEARCH.md Pattern 1:
    ```typescript
    import i18n from 'i18next';
    import { initReactI18next } from 'react-i18next';
    import en from './en.json';
    import zh from './zh.json';
    import es from './es.json';
    import ja from './ja.json';
    import { detectInitialLocale } from '../lib/locale';
    import { settingsService } from '../services/settings.service';

    export const SUPPORTED_LOCALES = ['en', 'zh', 'es', 'ja'] as const;
    export type SupportedLocale = typeof SUPPORTED_LOCALES[number];

    // Human-readable names for LLM "Respond in {name}" directive (D-12)
    export const LOCALE_NAMES: Record<SupportedLocale, string> = {
      en: 'English',
      zh: 'Simplified Chinese',
      es: 'Spanish',
      ja: 'Japanese',
    };

    const savedLocale = settingsService.getSync().preferences.locale;
    const initial = detectInitialLocale(savedLocale);

    void i18n
      .use(initReactI18next)
      .init({
        resources: {
          en: { translation: en },
          zh: { translation: zh },
          es: { translation: es },
          ja: { translation: ja },
        },
        lng: initial,
        fallbackLng: 'en',
        interpolation: { escapeValue: false },
        react: { useSuspense: false },
        saveMissing: import.meta.env.DEV,
        missingKeyHandler: import.meta.env.DEV
          ? (lngs, _ns, key) => console.warn(`[i18n] missing key: ${key} (lng=${Array.isArray(lngs) ? lngs.join(',') : lngs})`)
          : undefined,
      });

    if (typeof document !== 'undefined') {
      document.documentElement.setAttribute('data-locale', initial);
      document.documentElement.lang = initial;
      i18n.on('languageChanged', (lng) => {
        document.documentElement.setAttribute('data-locale', lng);
        document.documentElement.lang = lng;
      });
    }

    export default i18n;
    ```

    Step 5: Create `app/src/locales/i18n.d.ts` for type-safe t() keys (D-06):
    ```typescript
    import 'i18next';
    import en from './en.json';

    declare module 'i18next' {
      interface CustomTypeOptions {
        defaultNS: 'translation';
        resources: { translation: typeof en };
      }
    }
    ```

    Step 6: Ensure `app/tsconfig.json` has `"resolveJsonModule": true` and `"allowSyntheticDefaultImports": true` (should already be on via Vite defaults — verify; add only if missing). Do NOT touch other tsconfig fields.

    Step 7: Patch `app/src/main.tsx` — add `import './locales';` immediately after `import './index.css';` and before `import App from './App.tsx';`. This runs i18n.init() synchronously before first render. Do NOT `await` — resources are preloaded.

    Step 8: Commit: `feat(27-01): install i18next + react-i18next + @capacitor/device; scaffold locale bundles and sync init`
  </action>
  <verify>
    <automated>cd app && ls src/locales/en.json src/locales/zh.json src/locales/es.json src/locales/ja.json src/locales/index.ts src/locales/i18n.d.ts && node -e "const en=require('./src/locales/en.json'); const zh=require('./src/locales/zh.json'); const es=require('./src/locales/es.json'); const ja=require('./src/locales/ja.json'); const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v,p?p+'.'+k:k):[p?p+'.'+k:k]); const ek=new Set(flat(en)); for (const [n,b] of [['zh',zh],['es',es],['ja',ja]]) { const bk=new Set(flat(b)); if (ek.size!==bk.size||[...ek].some(k=>!bk.has(k))) { console.error('parity fail:'+n); process.exit(1);} } console.log('parity ok');" && grep -q "react-i18next" package.json && grep -q "i18next" package.json && grep -q "@capacitor/device" package.json && grep -q "import './locales'" src/main.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && grep -E '"(i18next|react-i18next|@capacitor/device)"' package.json | wc -l` returns `3`
    - `ls app/src/locales/{en,zh,es,ja}.json app/src/locales/index.ts app/src/locales/i18n.d.ts` all exist (exit 0)
    - `grep -q "import './locales'" app/src/main.tsx` returns 0
    - `grep -qE "SUPPORTED_LOCALES\s*=\s*\[['\"]en['\"],\s*['\"]zh['\"],\s*['\"]es['\"],\s*['\"]ja['\"]\]" app/src/locales/index.ts` returns 0
    - `grep -q "fallbackLng: 'en'" app/src/locales/index.ts` returns 0
    - `grep -q "useSuspense: false" app/src/locales/index.ts` returns 0
    - `grep -qE "'Simplified Chinese'" app/src/locales/index.ts` returns 0 (D-12 — "Simplified Chinese", not "Chinese")
    - `grep -q "declare module 'i18next'" app/src/locales/i18n.d.ts` returns 0
    - `cd app && tsc -b --noEmit` exits 0
    - `cd app && npm run build` exits 0 (confirms Vite bundles run through i18n init)
    - Bundle parity: `en.json`, `zh.json`, `es.json`, `ja.json` have identical flattened key sets (inline node -e in verify command exits 0)
  </acceptance_criteria>
  <done>i18n deps installed, locales directory populated with parity-stub bundles, i18n.init runs synchronously at startup with data-locale attribute wired. Commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/locale.ts (normalizeLocale + detectInitialLocale + detectDeviceLocale) with test — TDD</name>

  <files>app/src/lib/locale.ts, app/tests/lib/locale-detect.test.mjs</files>
  <read_first>
    - app/src/locales/index.ts (from Task 1 — needed for SupportedLocale + SUPPORTED_LOCALES import)
    - .planning/phases/27-add-i18n-l10n-support/27-RESEARCH.md (section "Capacitor Device Language Detection")
    - .planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md (D-17)
    - app/tests/canonical-knowledge.test.mjs (test pattern reference — node --test with .ts imports via esbuild loader)
  </read_first>
  <behavior>
    - `normalizeLocale('en-US')` → `'en'`
    - `normalizeLocale('zh-CN')` → `'zh'`
    - `normalizeLocale('zh-Hans-CN')` → `'zh'`
    - `normalizeLocale('zh-TW')` → `'zh'` (ships Simplified; onboarding lets user override)
    - `normalizeLocale('es-419')` → `'es'`
    - `normalizeLocale('es-MX')` → `'es'`
    - `normalizeLocale('ja-JP')` → `'ja'`
    - `normalizeLocale('ko-KR')` → `'en'` (fallback — not supported)
    - `normalizeLocale('fr-FR')` → `'en'` (fallback)
    - `normalizeLocale('')` → `'en'`
    - `normalizeLocale(null)` → `'en'`
    - `normalizeLocale(undefined)` → `'en'`
    - `detectInitialLocale('zh')` → `'zh'` (saved preference wins over navigator)
    - `detectInitialLocale(undefined)` with `navigator.language='ja-JP'` → `'ja'`
    - `detectInitialLocale(undefined)` with navigator absent → `'en'`
  </behavior>
  <action>
    Step 1 (RED): Create `app/tests/lib/locale-detect.test.mjs` with all behavior cases above:
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';
    import { normalizeLocale, detectInitialLocale } from '../../src/lib/locale.ts';

    test('normalizeLocale: BCP-47 variants', () => {
      assert.equal(normalizeLocale('en-US'), 'en');
      assert.equal(normalizeLocale('zh-CN'), 'zh');
      assert.equal(normalizeLocale('zh-Hans-CN'), 'zh');
      assert.equal(normalizeLocale('zh-TW'), 'zh');
      assert.equal(normalizeLocale('es-419'), 'es');
      assert.equal(normalizeLocale('es-MX'), 'es');
      assert.equal(normalizeLocale('ja-JP'), 'ja');
    });

    test('normalizeLocale: unsupported → en', () => {
      assert.equal(normalizeLocale('ko-KR'), 'en');
      assert.equal(normalizeLocale('fr-FR'), 'en');
    });

    test('normalizeLocale: nullish → en', () => {
      assert.equal(normalizeLocale(''), 'en');
      assert.equal(normalizeLocale(null), 'en');
      assert.equal(normalizeLocale(undefined), 'en');
    });

    test('detectInitialLocale: saved pref wins', () => {
      assert.equal(detectInitialLocale('zh'), 'zh');
      assert.equal(detectInitialLocale('ja'), 'ja');
    });

    test('detectInitialLocale: falls back to navigator.language', () => {
      const orig = globalThis.navigator;
      globalThis.navigator = { language: 'ja-JP', languages: ['ja-JP'] };
      try { assert.equal(detectInitialLocale(undefined), 'ja'); }
      finally { globalThis.navigator = orig; }
    });

    test('detectInitialLocale: no navigator → en', () => {
      const orig = globalThis.navigator;
      delete globalThis.navigator;
      try { assert.equal(detectInitialLocale(undefined), 'en'); }
      finally { globalThis.navigator = orig; }
    });
    ```
    Run: `cd app && node --test tests/lib/locale-detect.test.mjs` → MUST fail (locale.ts does not exist yet).

    Step 2 (GREEN): Create `app/src/lib/locale.ts` verbatim per RESEARCH.md (~30 LoC):
    ```typescript
    import { Capacitor } from '@capacitor/core';
    import { Device } from '@capacitor/device';
    import { SUPPORTED_LOCALES, type SupportedLocale } from '../locales';

    export function normalizeLocale(raw: string | undefined | null): SupportedLocale {
      if (!raw) return 'en';
      const base = raw.toLowerCase().split('-')[0];
      return (SUPPORTED_LOCALES as readonly string[]).includes(base) ? (base as SupportedLocale) : 'en';
    }

    export function detectInitialLocale(savedPref: SupportedLocale | undefined): SupportedLocale {
      if (savedPref) return savedPref;
      if (typeof navigator !== 'undefined' && navigator) {
        return normalizeLocale(navigator.language ?? navigator.languages?.[0]);
      }
      return 'en';
    }

    export async function detectDeviceLocale(): Promise<SupportedLocale> {
      try {
        if (Capacitor.isNativePlatform()) {
          const { value } = await Device.getLanguageCode();
          return normalizeLocale(value);
        }
      } catch { /* fall through */ }
      return normalizeLocale(typeof navigator !== 'undefined' ? navigator.language : 'en');
    }
    ```

    Run test again — MUST pass.

    NOTE ON CIRCULAR IMPORT: `locales/index.ts` imports from `lib/locale.ts`, and `lib/locale.ts` imports `SUPPORTED_LOCALES` from `locales/index.ts`. Import hoisting handles this — `SUPPORTED_LOCALES` is a top-level `const` declaration evaluated before `detectInitialLocale` is called. If the circular reference causes runtime `TDZ` errors during testing, break the cycle by duplicating `SUPPORTED_LOCALES = ['en','zh','es','ja'] as const` in both files (simpler) OR extracting to a standalone `app/src/locales/constants.ts`. Prefer duplicating in `lib/locale.ts` — 1 line, no new file, clearly scoped.

    Step 3 (COMMIT): `feat(27-01): add lib/locale.ts with normalizeLocale + detectInitialLocale`
  </action>
  <verify>
    <automated>cd app && node --test tests/lib/locale-detect.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/src/lib/locale.ts` exits 0
    - `cd app && node --test tests/lib/locale-detect.test.mjs` exits 0 with all 6 tests passing
    - `grep -q "export function normalizeLocale" app/src/lib/locale.ts` returns 0
    - `grep -q "export function detectInitialLocale" app/src/lib/locale.ts` returns 0
    - `grep -q "export async function detectDeviceLocale" app/src/lib/locale.ts` returns 0
    - `cd app && tsc -b --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>normalizeLocale + detectInitialLocale + detectDeviceLocale implemented and tested. D-17 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UserPreferences.locale schema + settings.service legacy-language migration + all remaining Wave 0 test skeletons</name>

  <files>app/src/types/index.ts, app/src/services/settings.service.ts, app/tests/services/settings-locale.test.mjs, app/tests/locales/bundle-parity.test.mjs, app/tests/locales/missing-key.test.mjs, app/tests/locales/data-locale-attr.test.mjs, app/tests/lib/date.locale.test.mjs, app/tests/providers/llm-locale-injection.test.mjs, app/tests/providers/tts-locale.test.mjs, app/tests/services/youtube-locale.test.mjs, app/tests/services/web-search-no-locale.test.mjs, app/tests/state/useQuestions-locale-abort.test.mjs</files>
  <read_first>
    - app/src/types/index.ts (lines 280-300)
    - app/src/services/settings.service.ts (entire file)
    - .planning/phases/27-add-i18n-l10n-support/27-CONTEXT.md (D-20)
    - .planning/phases/27-add-i18n-l10n-support/27-RESEARCH.md (section "UserPreferences Schema Update", "Validation Architecture > Wave 0 Gaps")
    - .planning/phases/27-add-i18n-l10n-support/27-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <behavior>
    - `settingsService.set('preferences', { ...prefs, locale: 'zh' })` + `settingsService.getSync().preferences.locale` → `'zh'`
    - Legacy migration: pre-existing localStorage blob with `preferences.language: 'en'` and no `preferences.locale` → `getSync().preferences.locale` returns `'en'`
    - Legacy migration: pre-existing `preferences.language: 'ja'` (no locale) → `getSync().preferences.locale` returns `'ja'`
    - Legacy migration: pre-existing `preferences.language: 'unsupported-code'` (no locale) → `getSync().preferences.locale` returns `'en'` (normalized)
    - Fresh install (no stored settings) → `getSync().preferences.locale` returns `'en'`
    - Bundle-parity test detects any key present in `en.json` but missing from `zh/es/ja.json`
    - Missing-key test: calling `i18n.t('nonexistent.key')` logs a warning (missingKeyHandler fires) and returns the key (i18next default when no fallback match) — never renders as a raw dotted path in production
    - data-locale-attr test: after i18n init, `document.documentElement.dataset.locale === 'en'`; after `i18n.changeLanguage('zh')`, it becomes `'zh'`
  </behavior>
  <action>
    Step 1: Edit `app/src/types/index.ts` — modify `AppPreferences` (around line 291) to add `locale` and mark `language` deprecated. Also export `SupportedLocale` (import from locales) OR inline the union. Prefer inlining to avoid a new cross-module dependency from types/:
    ```typescript
    export type SupportedLocale = 'en' | 'zh' | 'es' | 'ja';

    export interface AppPreferences {
      theme: 'light' | 'dark' | 'system';
      locale: SupportedLocale;                    // ⬅️ NEW — canonical locale source (D-20)
      /** @deprecated Use `locale` instead. Kept for one-time migration only. */
      language?: string;
      onboardingCompleted: boolean;
      aiConsentGiven?: boolean;
      askMonthlyLimit?: number;
    }
    ```
    Add `import type { SupportedLocale as _SupportedLocaleFromLocales }` is NOT needed — inline union matches `locales/index.ts` by string value.

    Step 2: Edit `app/src/services/settings.service.ts`:

    (a) Update `defaultSettings.preferences`:
    ```typescript
    preferences: {
      theme: 'system',
      locale: 'en',                         // ⬅️ NEW
      language: 'en',                       // keep for backward compat; deprecated
      onboardingCompleted: false,
      aiConsentGiven: false,
      askMonthlyLimit: 0,
    }
    ```

    (b) Add migration in `load()`. Insert BEFORE `return deepMerge(...)`:
    ```typescript
    function load(): AppSettings {
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (!raw) return { ...defaultSettings };
        const parsed = JSON.parse(raw) as Partial<AppSettings>;
        const merged = deepMerge(defaultSettings, parsed);
        // Migration: copy legacy preferences.language → preferences.locale if locale missing (D-20)
        const storedPrefs = (parsed.preferences ?? {}) as Partial<AppPreferences>;
        if (!storedPrefs.locale && storedPrefs.language) {
          const candidate = String(storedPrefs.language).toLowerCase().split('-')[0];
          merged.preferences.locale = (['en', 'zh', 'es', 'ja'].includes(candidate)
            ? candidate
            : 'en') as SupportedLocale;
        }
        return merged;
      } catch {
        return { ...defaultSettings };
      }
    }
    ```
    Import `SupportedLocale` and `AppPreferences` at the top if needed. Do NOT import from `../locales` here (circular: settings.service is imported by locales/index.ts) — use the inline string union already declared in types/index.ts.

    Step 3: Create remaining Wave 0 test files as ACTIVE tests (not skeletons — write real assertions now where feasible; stub only if the target code doesn't exist yet):

    (a) `app/tests/services/settings-locale.test.mjs` — set/get round-trip + legacy migration (ACTIVE now):
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';

    // localStorage shim for Node
    const store = new Map();
    globalThis.localStorage = {
      getItem: (k) => store.get(k) ?? null,
      setItem: (k, v) => store.set(k, String(v)),
      removeItem: (k) => store.delete(k),
      clear: () => store.clear(),
    };

    const { settingsService } = await import('../../src/services/settings.service.ts');

    test('locale round-trips', async () => {
      store.clear();
      const prefs = settingsService.getSync().preferences;
      await settingsService.set('preferences', { ...prefs, locale: 'zh' });
      assert.equal(settingsService.getSync().preferences.locale, 'zh');
    });

    test('legacy preferences.language migrates to locale on load', async () => {
      store.clear();
      store.set('echolearn_settings', JSON.stringify({
        preferences: { theme: 'system', language: 'ja', onboardingCompleted: true },
      }));
      assert.equal(settingsService.getSync().preferences.locale, 'ja');
    });

    test('legacy language=en-US normalizes to locale=en', async () => {
      store.clear();
      store.set('echolearn_settings', JSON.stringify({
        preferences: { theme: 'system', language: 'en-US', onboardingCompleted: true },
      }));
      assert.equal(settingsService.getSync().preferences.locale, 'en');
    });

    test('legacy language=ko-KR (unsupported) → locale=en', async () => {
      store.clear();
      store.set('echolearn_settings', JSON.stringify({
        preferences: { theme: 'system', language: 'ko-KR', onboardingCompleted: true },
      }));
      assert.equal(settingsService.getSync().preferences.locale, 'en');
    });

    test('fresh install → locale=en', () => {
      store.clear();
      assert.equal(settingsService.getSync().preferences.locale, 'en');
    });
    ```

    (b) `app/tests/locales/bundle-parity.test.mjs` (ACTIVE now — en/zh/es/ja loaded):
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';
    import { readFileSync } from 'node:fs';
    import { fileURLToPath } from 'node:url';
    import { dirname, resolve } from 'node:path';

    const here = dirname(fileURLToPath(import.meta.url));
    const read = (f) => JSON.parse(readFileSync(resolve(here, '../../src/locales', f), 'utf8'));

    function flatten(o, p = '') {
      return Object.entries(o).flatMap(([k, v]) => {
        const path = p ? `${p}.${k}` : k;
        return v !== null && typeof v === 'object' && !Array.isArray(v) ? flatten(v, path) : [path];
      });
    }

    test('en/zh/es/ja bundles have identical key sets', () => {
      const en = new Set(flatten(read('en.json')));
      for (const locale of ['zh', 'es', 'ja']) {
        const b = new Set(flatten(read(`${locale}.json`)));
        const missing = [...en].filter((k) => !b.has(k));
        const extra = [...b].filter((k) => !en.has(k));
        assert.deepEqual(missing, [], `${locale}.json missing keys: ${missing.join(', ')}`);
        assert.deepEqual(extra, [], `${locale}.json has extra keys not in en.json: ${extra.join(', ')}`);
      }
    });
    ```

    (c) `app/tests/locales/missing-key.test.mjs` (ACTIVE now — init a test i18n instance, do NOT import app's `src/locales/index.ts` because that has side effects like `document.documentElement`):
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';
    import i18next from 'i18next';

    test('missingKeyHandler fires; fallback returns EN text (never raw key path in prod)', async () => {
      let missingFired = false;
      const inst = i18next.createInstance();
      await inst.init({
        lng: 'zh',
        fallbackLng: 'en',
        resources: {
          en: { translation: { greet: 'Hello' } },
          zh: { translation: {} },
        },
        saveMissing: true,
        missingKeyHandler: () => { missingFired = true; },
      });
      assert.equal(inst.t('greet'), 'Hello', 'fallback EN should render');
      inst.t('nonexistent.key');
      assert.ok(missingFired, 'missingKeyHandler must fire for truly missing key');
    });
    ```

    (d) `app/tests/locales/data-locale-attr.test.mjs` (ACTIVE — minimal shim instead of jsdom):
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';
    import i18next from 'i18next';

    // minimal document shim
    const attrs = new Map();
    let lang = 'en';
    globalThis.document = {
      documentElement: {
        setAttribute: (k, v) => attrs.set(k, v),
        getAttribute: (k) => attrs.get(k) ?? null,
        get dataset() { return new Proxy({}, { get: (_t, k) => attrs.get(`data-${String(k)}`) }); },
        set lang(v) { lang = v; },
        get lang() { return lang; },
      },
    };

    test('data-locale attribute: set on init + updated on changeLanguage', async () => {
      const inst = i18next.createInstance();
      await inst.init({
        lng: 'en',
        fallbackLng: 'en',
        resources: { en: { translation: {} }, zh: { translation: {} } },
      });
      // Simulate what src/locales/index.ts does
      document.documentElement.setAttribute('data-locale', 'en');
      document.documentElement.lang = 'en';
      inst.on('languageChanged', (lng) => {
        document.documentElement.setAttribute('data-locale', lng);
        document.documentElement.lang = lng;
      });

      assert.equal(attrs.get('data-locale'), 'en');
      await inst.changeLanguage('zh');
      assert.equal(attrs.get('data-locale'), 'zh');
    });
    ```

    (e) Create SKELETONS (ACTIVE but with `// TODO: Plan NN` markers and one trivial passing assertion) for these files so CI doesn't error on missing references — Plans 02/04 replace the skeleton bodies:
      - `app/tests/lib/date.locale.test.mjs` — skeleton: `test('placeholder — Plan 02 fills', () => assert.ok(true));`
      - `app/tests/providers/llm-locale-injection.test.mjs` — skeleton: same
      - `app/tests/providers/tts-locale.test.mjs` — skeleton: same
      - `app/tests/services/youtube-locale.test.mjs` — skeleton: same
      - `app/tests/services/web-search-no-locale.test.mjs` — skeleton: same
      - `app/tests/state/useQuestions-locale-abort.test.mjs` — skeleton: same

    Each skeleton file contains:
    ```javascript
    import assert from 'node:assert/strict';
    import test from 'node:test';
    test('placeholder — replaced by Plan NN', () => { assert.ok(true, 'skeleton only'); });
    ```
    Replace `NN` with the plan that will fill it (02 for llm/tts/youtube/web-search/date; 04 for useQuestions-abort).

    Step 4: Run the full Wave 0 subset — all must pass:
    ```bash
    cd app && node --test tests/lib/locale-detect.test.mjs tests/lib/date.locale.test.mjs tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs tests/locales/data-locale-attr.test.mjs tests/services/settings-locale.test.mjs tests/providers/llm-locale-injection.test.mjs tests/providers/tts-locale.test.mjs tests/services/youtube-locale.test.mjs tests/services/web-search-no-locale.test.mjs tests/state/useQuestions-locale-abort.test.mjs
    ```

    Step 5: Commit: `feat(27-01): UserPreferences.locale + legacy language migration + Wave 0 test scaffolds (bundle parity, missing-key, data-locale-attr, settings-locale, skeletons)`
  </action>
  <verify>
    <automated>cd app && node --test tests/lib/locale-detect.test.mjs tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs tests/locales/data-locale-attr.test.mjs tests/services/settings-locale.test.mjs tests/lib/date.locale.test.mjs tests/providers/llm-locale-injection.test.mjs tests/providers/tts-locale.test.mjs tests/services/youtube-locale.test.mjs tests/services/web-search-no-locale.test.mjs tests/state/useQuestions-locale-abort.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "locale: SupportedLocale" app/src/types/index.ts` returns 0
    - `grep -qE "@deprecated.*locale" app/src/types/index.ts` returns 0
    - `grep -q "locale: 'en'" app/src/services/settings.service.ts` returns 0
    - `grep -q "storedPrefs.language" app/src/services/settings.service.ts` returns 0 (migration path present)
    - All 11 Wave 0 test files exist: `ls app/tests/lib/{locale-detect,date.locale}.test.mjs app/tests/locales/{bundle-parity,missing-key,data-locale-attr}.test.mjs app/tests/services/{settings-locale,youtube-locale,web-search-no-locale}.test.mjs app/tests/providers/{llm-locale-injection,tts-locale}.test.mjs app/tests/state/useQuestions-locale-abort.test.mjs` — all exit 0
    - Full Wave-0 suite passes: `cd app && node --test tests/lib/*.test.mjs tests/locales/*.test.mjs tests/providers/llm-locale-injection.test.mjs tests/providers/tts-locale.test.mjs tests/services/settings-locale.test.mjs tests/services/youtube-locale.test.mjs tests/services/web-search-no-locale.test.mjs tests/state/useQuestions-locale-abort.test.mjs` exits 0
    - `cd app && tsc -b --noEmit` exits 0
    - VALIDATION.md Wave 0 gap list fully covered (11/11 test files + 2/2 install steps — install confirmed by Task 1)
  </acceptance_criteria>
  <done>UserPreferences.locale wired, legacy migration active, all Wave 0 test files exist and pass. Plans 02/04 fill in the real assertions in their respective skeletons.</done>
</task>

</tasks>

<verification>
- `cd app && tsc -b --noEmit` exits 0
- `cd app && npm run build` exits 0 (confirms locale bundles import cleanly through Vite)
- All Wave 0 test files exist (11 files per VALIDATION.md gap list)
- Full Wave 0 suite runs green (~5s)
- `ls app/src/locales/{en,zh,es,ja}.json` — all 4 bundles exist
- `grep "import './locales'" app/src/main.tsx` returns 1 match
- No regression: `cd app && npm test` exits 0 (existing tests unaffected)
</verification>

<success_criteria>
1. Dependencies installed: `i18next@26.0.5`, `react-i18next@17.0.3`, `@capacitor/device@8.0.2` in `app/package.json`.
2. Four locale bundles exist with parity-stub content (Plan 07 fills translations).
3. `app/src/locales/index.ts` initializes i18n synchronously; side-effect-imported from `main.tsx`.
4. `app/src/lib/locale.ts` provides `normalizeLocale` / `detectInitialLocale` / `detectDeviceLocale`.
5. `AppPreferences.locale` field exists; legacy `preferences.language` migrates on load.
6. Module augmentation (`i18n.d.ts`) makes `t()` keys type-safe — typo in a key path fails `tsc`.
7. All 11 Wave 0 test files exist and execute (5 full, 6 skeletons that Plans 02/04 complete).
8. `cd app && tsc -b --noEmit` green, `npm run build` green, Wave 0 suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/27-add-i18n-l10n-support/27-01-SUMMARY.md` documenting:
- Exact versions installed (`npm ls i18next react-i18next @capacitor/device`)
- Locale bundle key-count (wc of flattened `en.json` paths)
- Any circular-import workaround applied in lib/locale.ts
- Wave-0 suite runtime (expect <5s)
- Any tsconfig touches required (should be none)
- Note: ZH/ES/JA bundles currently duplicate EN values — Plan 07 translates them via Sonnet subagent.
</output>
