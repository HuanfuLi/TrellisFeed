# Phase 27 Deferred Items

Out-of-scope issues discovered during Phase 27 execution. Do NOT fix as part of Phase 27; track for a future clean-up phase.

## Pre-existing tsc errors (unrelated to i18n)

Found while running `cd app && npx tsc -b --noEmit` during Plan 27-01 execution. Confirmed pre-existing by running against the commit prior to any Phase 27 changes.

- `src/screens/GraphScreen.tsx(6,10)`: `'ArrowLeft' is declared but its value is never read` (TS6133)
- `src/screens/GraphScreen.tsx(476,39)`: `"GRAPH_UPDATED"` not assignable to AppEvent union (TS2345)
- `src/services/canonical-knowledge.service.ts(710,19)`: same `"GRAPH_UPDATED"` issue (TS2322)
- `src/services/canonical-knowledge.service.ts(1355,41)`: `"COVERAGE_ERROR"` not assignable to `ErrorCode` (TS2322)
- `src/services/review.service.ts(73,29)`: Property `anchorId` does not exist on type `Question` (TS2339)
- `src/services/trellis-state.service.ts(1,15)`: `FlashCard` declared but never used (TS6196)
- `src/services/trellis-state.service.ts(94,7)`: `ALL_LEAF_STATES` declared but its value is never read (TS6133)
- `src/services/trellis-state.service.ts(140,15)`: Conversion cast error — mock Question missing `timestamp/date/answer/summary` fields (TS2352)

## Pre-existing test failures (Node 25 TS stripping)

`node --test` on Node 25.9 fails to resolve extension-less `.ts` imports from the `src/providers/llm/index.ts → src/services/token-usage.service` chain. Affects any test that transitively imports through LLM providers (canonical-knowledge, trellis-state, web-search, concept-feed).

Does NOT affect tests that import only pure-data modules (settings.service, ask-rate-limiter, FeedPostImage, PostCarousel logic, useInfiniteScroll).

**Suggested fix:** Add `.ts` extensions to all extension-less intra-src imports (or switch test runner back to explicit esbuild loader). Out of scope for Phase 27.

## Pre-existing git-tracked artifacts

- `Documents/UI_AUDIT_REPORT.md` (untracked) — predates Phase 27
- `.planning/phases/28-ui-ux-polish-from-audit-findings/` (untracked) — Phase 28 scaffold
- `src/assets/planner-trellis/trellis-bg-default.png` (untracked) — Phase 25/26 artifact
- `src/components/trellis/TrellisStatusPanel.tsx` (modified, unrelated to i18n)
- `android/app/capacitor.build.gradle`, `android/capacitor.settings.gradle` — regenerated by `npx cap sync` during Phase 27-01 Task 1 (i18n dep install → capacitor android update). Safe to commit with Phase 27 dep install.
