---
phase: 20-orchestration-strategy-diagnostic-dialogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/orchestration-strategy.service.ts
  - app/tests/services/orchestration-strategy.test.mjs
autonomous: true
requirements: [ORCH-01]

must_haves:
  truths:
    - "OrchestrationStrategy interface exists with computeHints method"
    - "defaultStrategy returns correct LearningMode based on TrajectorySignal thresholds"
    - "StrategyHints include mode, weakAreaBias, discoveryWeight, priorityConceptIds, curiosityTopics"
  artifacts:
    - path: "app/src/services/orchestration-strategy.service.ts"
      provides: "OrchestrationStrategy interface + defaultStrategy implementation"
      exports: ["OrchestrationStrategy", "StrategyHints", "LearningMode", "defaultStrategy"]
    - path: "app/tests/services/orchestration-strategy.test.mjs"
      provides: "Unit tests for all strategy mode transitions"
  key_links:
    - from: "app/src/services/orchestration-strategy.service.ts"
      to: "app/src/types/index.ts"
      via: "imports TrajectorySignal, CheckInSignals"
      pattern: "import type.*TrajectorySignal.*CheckInSignals"
---

<objective>
Define the OrchestrationStrategy interface and default implementation that translates TrajectorySignal into StrategyHints for downstream services.

Purpose: Per D-01 (light orchestration), create the strategy layer that services will consume independently. No central controller -- just a pure-function interface.
Output: orchestration-strategy.service.ts with interface + default implementation, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-orchestration-strategy-diagnostic-dialogue/20-CONTEXT.md
@.planning/phases/20-orchestration-strategy-diagnostic-dialogue/20-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From app/src/types/index.ts:
```typescript
export interface TrajectorySignal {
  reviewPerformance: number;      // 0-100
  questionFrequency: number;      // questions in last 7 days
  timeSinceLastReview: number;    // milliseconds
  feedEngagement: number;         // posts viewed in last 7 days
  conceptCoverage: number;        // 0-100 percentage
  weakAreas: string[];            // question IDs with low easeFactor
}

export interface CheckInSignals {
  confidence: string[];
  confusion: string[];
  connections: string[];
  curiosity: string[];
  revisitIntent: string[];
}
```

From app/src/services/trajectoryAnalyzer.service.ts:
```typescript
export const trajectoryAnalyzerService = {
  aggregateSignals(): TrajectorySignal;
  // ... (6-hour cached)
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OrchestrationStrategy interface, types, and test scaffold</name>
  <files>app/src/services/orchestration-strategy.service.ts, app/tests/services/orchestration-strategy.test.mjs</files>
  <read_first>app/src/types/index.ts, app/src/services/trajectoryAnalyzer.service.ts, app/tests/services/suggestionScorer.test.mjs</read_first>
  <behavior>
    - Test: computeHints with weakAreas.length > 3 returns mode 'retrieval', weakAreaBias 0.7, discoveryWeight 0.3
    - Test: computeHints with reviewPerformance < 40 returns mode 'retrieval'
    - Test: computeHints with conceptCoverage > 70 AND feedEngagement > 10 returns mode 'discovery', discoveryWeight 0.6
    - Test: computeHints with timeSinceLastReview > 3 days returns mode 'reinforcement'
    - Test: computeHints with balanced signals returns mode 'balanced', weights at 0.5
    - Test: priorityConceptIds populated from weakAreas
    - Test: curiosityTopics populated from checkInSignals.curiosity when provided
    - Test: computeHints without checkInSignals returns empty curiosityTopics
  </behavior>
  <action>
    1. Create `app/tests/services/orchestration-strategy.test.mjs` with RED tests first (follow existing test pattern from `suggestionScorer.test.mjs`). Import from the service file. Write all 8 test cases listed in behavior.

    2. Create `app/src/services/orchestration-strategy.service.ts` per D-01 (light orchestration):
       - Export `LearningMode` type: `'retrieval' | 'discovery' | 'reinforcement' | 'balanced'`
       - Export `StrategyHints` interface with fields: `mode: LearningMode`, `weakAreaBias: number`, `discoveryWeight: number`, `priorityConceptIds: string[]`, `curiosityTopics: string[]`
       - Export `OrchestrationStrategy` interface with single method: `computeHints(signals: TrajectorySignal, checkInSignals?: CheckInSignals): StrategyHints`
       - Export `defaultStrategy: OrchestrationStrategy` with logic:
         - `weakAreas.length > 3 || reviewPerformance < 40` -> retrieval mode, weakAreaBias 0.7, discoveryWeight 0.3
         - `conceptCoverage > 70 && feedEngagement > 10` -> discovery mode, weakAreaBias 0.3, discoveryWeight 0.6
         - `timeSinceLastReview > 3 * 24 * 60 * 60 * 1000` -> reinforcement mode, weakAreaBias 0.5, discoveryWeight 0.3
         - Otherwise -> balanced mode, weakAreaBias 0.5, discoveryWeight 0.5
       - `priorityConceptIds` = `[...signals.weakAreas]` (always from weakAreas)
       - `curiosityTopics` = `checkInSignals?.curiosity ?? []`
       - Import TrajectorySignal and CheckInSignals from `'../types'`

    3. Types are kept in the service file itself since the interface is small. Other services will import from the service file directly. Do NOT modify types/index.ts.

    4. Run tests GREEN.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/orchestration-strategy.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/services/orchestration-strategy.service.ts` exists
    - `grep -c "export interface OrchestrationStrategy" app/src/services/orchestration-strategy.service.ts` returns 1
    - `grep -c "export const defaultStrategy" app/src/services/orchestration-strategy.service.ts` returns 1
    - `grep -c "export type LearningMode" app/src/services/orchestration-strategy.service.ts` returns 1
    - `grep -c "export interface StrategyHints" app/src/services/orchestration-strategy.service.ts` returns 1
    - `grep "computeHints" app/src/services/orchestration-strategy.service.ts` shows method signature
    - All 8 tests pass in orchestration-strategy.test.mjs
  </acceptance_criteria>
  <done>OrchestrationStrategy interface defined, defaultStrategy implements all 4 mode transitions correctly, 8+ tests passing</done>
</task>

</tasks>

<verification>
cd /Users/Code/EchoLearn/app && node --test tests/services/orchestration-strategy.test.mjs
</verification>

<success_criteria>
- OrchestrationStrategy interface exported with computeHints method
- defaultStrategy correctly returns retrieval/discovery/reinforcement/balanced modes based on signal thresholds
- StrategyHints type includes mode, weakAreaBias, discoveryWeight, priorityConceptIds, curiosityTopics
- All unit tests pass
- No regression in existing tests: cd /Users/Code/EchoLearn/app && node --test tests/services/suggestionScorer.test.mjs
</success_criteria>

<output>
After completion, create `.planning/phases/20-orchestration-strategy-diagnostic-dialogue/20-01-SUMMARY.md`
</output>
