---
phase: 26-trellis-harvest-panel-dying-dead-node-actions-and-suggested-moves-refactor-to-reflect-trellis-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/trellis-credits.service.ts
  - app/src/types/index.ts
  - app/src/state/useTrellisData.ts
  - app/src/components/ui/BottomSheet.tsx
  - app/src/services/question.service.ts
autonomous: true
requirements: [D-01, D-04, D-09, D-16]

must_haves:
  truths:
    - "Fruit credits can be accumulated and persisted across app restarts"
    - "HARVEST_COMPLETED event triggers trellis recompute"
    - "BottomSheet component slides up from bottom with overlay"
    - "Pruned questions can be queried separately from normal questions"
  artifacts:
    - path: "app/src/services/trellis-credits.service.ts"
      provides: "Fruit credit CRUD (localStorage)"
      exports: ["trellisCreditsService"]
    - path: "app/src/components/ui/BottomSheet.tsx"
      provides: "Reusable slide-up bottom sheet"
      exports: ["BottomSheet"]
    - path: "app/src/types/index.ts"
      provides: "HARVEST_COMPLETED event type in AppEvent union"
      contains: "HARVEST_COMPLETED"
  key_links:
    - from: "app/src/state/useTrellisData.ts"
      to: "HARVEST_COMPLETED"
      via: "eventBus.subscribe"
      pattern: "subscribe.*HARVEST_COMPLETED"
    - from: "app/src/services/trellis-credits.service.ts"
      to: "localStorage"
      via: "trellis_fruit_credits key"
      pattern: "trellis_fruit_credits"
---

<objective>
Build the foundational service layer and reusable UI component for Phase 26: credits persistence service, HARVEST_COMPLETED event wiring, reusable BottomSheet component, and getPrunedQuestions query method.

Purpose: All subsequent plans (status panel, harvest flow, node actions, pruning) depend on these primitives existing.
Output: 4 new/modified files providing the building blocks for the trellis garden management layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-trellis-harvest-panel-dying-dead-node-actions-and-suggested-moves-refactor-to-reflect-trellis-status/26-CONTEXT.md
@.planning/phases/26-trellis-harvest-panel-dying-dead-node-actions-and-suggested-moves-refactor-to-reflect-trellis-status/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/src/types/index.ts (AppEvent union — add HARVEST_COMPLETED here):
```typescript
export type AppEvent =
  | { type: 'QUESTION_ASKED'; payload: Question }
  // ... existing events ...
  | { type: 'REVIEW_COMPLETED'; payload: { questionId: string; anchorId?: string } }
  | { type: 'CLASSIFICATION_COMPLETED'; payload: { anchorId: string; anchorName: string } }
  | { type: 'ANCHOR_DELETED'; payload: { anchorId: string } }
  | { type: 'NEWS_POSTS_READY'; payload: { posts: DailyPost[] } };
```

From app/src/state/useTrellisData.ts (subscribe to new event):
```typescript
export function useTrellisData(): UseTrellisDataResult {
  // Already subscribes to REVIEW_COMPLETED, CLASSIFICATION_COMPLETED, ANCHOR_DELETED
  // Must add HARVEST_COMPLETED subscription
}
```

From app/src/services/question.service.ts:
```typescript
// loadStore is private. getAll() filters out flagged.
// Need to expose getPrunedQuestions() that returns flagged-only questions.
getAll(opts?: { includeFlagged?: boolean }): Question[]
patchQuestion(questionId: string, patch: Partial<Question>): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credits service + HARVEST_COMPLETED event + useTrellisData subscription</name>
  <files>app/src/services/trellis-credits.service.ts, app/src/types/index.ts, app/src/state/useTrellisData.ts</files>
  <read_first>app/src/types/index.ts (lines 640-669 for AppEvent union), app/src/state/useTrellisData.ts, app/src/services/trellis-blossom-dates.service.ts (pattern reference for localStorage service)</read_first>
  <action>
    1. Create `app/src/services/trellis-credits.service.ts` (per D-01):
       - localStorage key: `trellis_fruit_credits`
       - Export `trellisCreditsService` object with:
         - `getTotal(): number` — parse int from localStorage, default 0
         - `add(count: number): number` — increment and persist, return new total
       - Follow the same try/catch pattern as trellis-blossom-dates.service.ts
       - No ServiceResult wrapper needed (pure read/write, no failure modes beyond localStorage)

    2. Add `HARVEST_COMPLETED` to AppEvent union in `app/src/types/index.ts`:
       - Add `| { type: 'HARVEST_COMPLETED'; payload: { count: number } }` before the closing semicolon
       - Place it after the ANCHOR_DELETED line for logical grouping with trellis events

    3. Subscribe useTrellisData to HARVEST_COMPLETED in `app/src/state/useTrellisData.ts`:
       - In the useEffect that sets up subscriptions, add:
         `const unsubHarvest = eventBus.subscribe('HARVEST_COMPLETED', () => recompute());`
       - Add `unsubHarvest()` to the cleanup return
       - This ensures fruit nodes reset to green after harvest (per D-06, Pitfall 1 from RESEARCH)
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/services/trellis-credits.service.ts` exists with exported `trellisCreditsService`
    - `trellisCreditsService.getTotal` and `trellisCreditsService.add` are exported functions
    - `app/src/types/index.ts` contains `HARVEST_COMPLETED` in AppEvent union with `payload: { count: number }`
    - `app/src/state/useTrellisData.ts` contains `subscribe('HARVEST_COMPLETED'` and corresponding unsubscribe in cleanup
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Credits service persists to localStorage, HARVEST_COMPLETED event triggers trellis recompute, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: BottomSheet component + getPrunedQuestions method</name>
  <files>app/src/components/ui/BottomSheet.tsx, app/src/services/question.service.ts</files>
  <read_first>app/src/components/ui/Card.tsx (pattern reference for UI component style), app/src/services/question.service.ts (lines 75-81 for loadStore, lines 485-487 for getAll)</read_first>
  <action>
    1. Create `app/src/components/ui/BottomSheet.tsx` (per D-09):
       - Export `BottomSheet` component with props: `{ open: boolean; onClose: () => void; title?: string; children: ReactNode }`
       - Overlay: fixed, inset 0, zIndex 500, backgroundColor rgba(0,0,0,0.45) when open / transparent when closed, pointerEvents auto/none
       - Panel: absolute bottom 0, left 0, right 0, backgroundColor var(--surface), borderRadius '20px 20px 0 0', padding '20px 16px 40px'
       - Transform: translateY(0) when open, translateY(100%) when closed
       - Transition: transform 0.3s cubic-bezier(0.32, 0.72, 0, 1), background-color 0.25s
       - maxHeight 75vh, overflowY auto
       - Overlay onClick calls onClose, panel onClick stops propagation
       - If title provided, render a drag handle bar (40px wide, 4px tall, centered, var(--border) color, marginBottom 16px) and h3 title below it
       - Use ONLY inline styles with CSS variables (project convention — no Tailwind)
       - zIndex 500 on overlay to clear Header at zIndex 190 (per Pitfall 5 from RESEARCH)

    2. Add `getPrunedQuestions()` method to questionService in `app/src/services/question.service.ts` (per D-16):
       - Add method: `getPrunedQuestions(): Question[]` that calls `loadStore({ includeFlagged: true }).filter(q => q.flagged === true && q.isAnchorNode === true)`
       - Filter requires BOTH flagged AND isAnchorNode because only anchor nodes are pruned from the trellis (regular Q&As flagged for off-topic reasons should not appear in pruned section)
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/components/ui/BottomSheet.tsx` exists with exported `BottomSheet` component
    - BottomSheet uses `zIndex: 500` on the overlay container
    - BottomSheet uses `transform: open ? 'translateY(0)' : 'translateY(100%)'` pattern
    - BottomSheet uses inline styles only (no className with Tailwind classes)
    - `app/src/services/question.service.ts` contains `getPrunedQuestions` method
    - getPrunedQuestions filters for `q.flagged === true && q.isAnchorNode === true`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Reusable BottomSheet slides up/down with overlay, getPrunedQuestions returns archived anchor nodes only, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `cd /Users/Code/EchoLearn/app && npx tsc --noEmit` passes with zero errors
- `app/src/services/trellis-credits.service.ts` exists and exports `trellisCreditsService`
- `app/src/components/ui/BottomSheet.tsx` exists and exports `BottomSheet`
- `app/src/types/index.ts` includes `HARVEST_COMPLETED` in AppEvent
- `app/src/state/useTrellisData.ts` subscribes to `HARVEST_COMPLETED`
- `app/src/services/question.service.ts` has `getPrunedQuestions` method
</verification>

<success_criteria>
All foundation primitives exist: credits service, HARVEST_COMPLETED event wired to trellis recompute, reusable BottomSheet component, and getPrunedQuestions query. TypeScript compiles clean. No runtime changes to PlannerScreen yet.
</success_criteria>

<output>
After completion, create `.planning/phases/26-trellis-harvest-panel-dying-dead-node-actions-and-suggested-moves-refactor-to-reflect-trellis-status/26-01-SUMMARY.md`
</output>
