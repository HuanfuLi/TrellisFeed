---
phase: 47-filter-redesign-off-topic-malicious-prompt-prevention
plan: 04
type: execute
wave: 2
depends_on:
  - 47-01
  - 47-02
files_modified:
  - app/src/state/useQuestions.ts
  - app/src/components/ChatMessage.tsx
  - app/src/types/index.ts
  - app/tests/state/useQuestions-pre-gate.test.mjs
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
tags:
  - filter
  - pipeline-inversion
  - useQuestions
  - chat-ui

must_haves:
  truths:
    - "useQuestions.askStreaming invokes filterQuestion(content, sessionContext, abortController.signal) BEFORE the first chatStream call (pre-gate inversion per D-18)"
    - "When filterResult.label === 'malicious', askStreaming appends a SessionMessage with kind:'malicious-block' carrying the i18n string AND returns null WITHOUT making any chatStream/chatCompletion call AND without calling buildAndSave (zero LLM tokens, no Question persisted per D-01)"
    - "When filterResult.label === 'off-topic', askStreaming proceeds through chatStream + buildAndSave + patchQuestion(flagged:true) and SKIPS classifyAndAnchorIncremental (D-01 — flagged questions never enter mind map)"
    - "When filterResult.label === 'on-topic', askStreaming follows the existing flow with classifyAndAnchorIncremental fired (no behavioral change for legitimate questions)"
    - "filterQuestion is called with the existing abortController.signal as the third argument so LOCALE_CHANGED cancellation propagates to embedding calls (D-19)"
    - "The Phase 35 byte-stable system prompt is unchanged; the new pre-gate runs BEFORE chatStream and does not mutate any role:'system' content"
    - "ChatMessage.tsx renders an inline malicious-block surface (NO override button per D-02) using i18n key chatMessage.maliciousBlocked.body when SessionMessage.kind === 'malicious-block'"
    - "QuestionFilterContext { priorQuestion, priorAnswer } continues to flow into filterQuestion unchanged (no plumbing churn per D-17)"
  artifacts:
    - path: app/src/state/useQuestions.ts
      provides: "Inverted askStreaming pipeline with three-branch handling (D-18)"
      contains: "filterResult.label"
    - path: app/src/components/ChatMessage.tsx
      provides: "Inline malicious-block render branch (D-02 — no override button)"
      contains: "maliciousBlocked"
    - path: app/src/types/index.ts
      provides: "SessionMessage.kind discriminator for malicious-block render"
      contains: "malicious-block"
    - path: app/tests/state/useQuestions-pre-gate.test.mjs
      provides: "Source-reading guard for D-18 ordering + D-19 abort + D-01 malicious-no-LLM"
      contains: "filterQuestion"
  key_links:
    - from: app/src/state/useQuestions.ts
      to: app/src/services/question-filter.service.ts (Plan 02)
      via: "filterQuestion(content, sessionContext, abortController.signal) call"
      pattern: "filterQuestion\\("
    - from: app/src/state/useQuestions.ts
      to: app/src/components/ChatMessage.tsx
      via: "SessionMessage.kind === 'malicious-block' rendering"
      pattern: "kind.*malicious-block"
    - from: app/src/components/ChatMessage.tsx
      to: app/src/locales/en.json (Plan 01)
      via: "t('chatMessage.maliciousBlocked.body') call"
      pattern: "maliciousBlocked\\.body"
---

<objective>
Wave 2 — Invert the pipeline in `useQuestions.askStreaming` from post-LLM-flag to pre-LLM-gate per D-18, branch on the three-label `FilterResult` from Plan 02 per D-01, and add the inline malicious-block render branch in `ChatMessage.tsx` per D-02. Plan 05 mirrors this in `question.service.ask` (parallel-safe — no file overlap).

Purpose: Stop spending LLM tokens on malicious prompts (the operator's "block it from the request entirely" requirement). Replace the existing post-flag pattern (`chatStream → filterQuestion → patchQuestion(flagged)`) with the pre-gate pattern (`filterQuestion → branch → optionally chatStream → buildAndSave with correct flag from the start`). Preserve the Phase 35 byte-stable system prompt invariant — the pre-gate is a NEW step inserted BEFORE `chatStream`, never mutating any `role: 'system'` content.

Output: useQuestions.ts pipeline inversion + ChatMessage.tsx malicious-block render branch + SessionMessage.kind discriminator + a source-reading test guarding the D-18/D-19/D-01 invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-CONTEXT.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-01-SUMMARY.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-02-SUMMARY.md
@CLAUDE.md
@app/src/state/useQuestions.ts
@app/src/components/ChatMessage.tsx
</context>

<interfaces>
Plan 02 contracts useQuestions consumes.

From app/src/services/question-filter.service.ts (rewritten by Plan 02):
- export type FilterLabel = 'on-topic' | 'off-topic' | 'malicious'
- export interface FilterResult { label: FilterLabel; bestMatch?: { label: string; exemplar: string; score: number } }
- export interface QuestionFilterContext { priorQuestion?: string; priorAnswer?: string }
- export async function evaluateQuestion(content: string, context?: QuestionFilterContext, signal?: AbortSignal): Promise FilterResult

Existing import alias in useQuestions.ts (line 9):
- import { evaluateQuestion as filterQuestion } from '../services/question-filter.service'

Keep this alias — call sites use the `filterQuestion` name.

From app/src/locales/en.json (added by Plan 01):
- chatMessage.maliciousBlocked.body — i18n key the new render branch reads via t()

Existing fire-and-forget classification site in useQuestions.ts (around line 316):
- void classifyAndAnchorIncremental(question, questionService.getAll(), llmConfig, abortController.signal).catch(...)

This call STAYS in the on-topic branch (unchanged); off-topic branch SKIPS it.

Existing abort-guard pattern in useQuestions.ts (around lines 208-212):
- if (abortController.signal.aborted) { toast(...); setIsAsking(false); return null }

The pre-gate uses the same abort-guard pattern.

Existing eventBus.emit({ type: 'QUESTION_ASKED', payload: question }) (around line 310) — KEEP for the off-topic branch so HomeScreen instances replace their pre-flag copy. Do NOT introduce new event types per CLAUDE.md "Event bus — unified GRAPH_UPDATED" + "One signal per semantic event".

Existing flagged-render block in ChatMessage.tsx (lines 296-381) — read the FULL block; the new malicious-block branch renders as a peer at the same indentation level, BUT with NO override button (D-02).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SessionMessage.kind discriminator + malicious-block render branch in ChatMessage.tsx</name>
  <read_first>
    - app/src/components/ChatMessage.tsx (FULL file — focus on lines 280-390 existing flagged-render block) — the analog UI shape; new malicious-block is a peer block, NOT a replacement
    - app/src/types/index.ts — `SessionMessage` interface; add the optional `kind` discriminator field
    - app/src/locales/en.json — verify `chatMessage.maliciousBlocked.body` exists from Plan 01
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Code Examples" §"Pattern: full-flow filter call" (lines 1059-1071) — describes the malicious branch render contract; sentinel-vs-discriminated-union choice (we pick discriminated union)
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-CONTEXT.md D-02 — "inline message in the chat thread with a clear, neutral reason. NO override button." — D-04 — "UI is already implemented — do not redesign" (the flagged-off-topic UI is preserved unchanged; new malicious-block is a SEPARATE peer surface)
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"app/src/components/ChatMessage.tsx" (lines 397-430) — analog mapping; same inline-style discipline (CSS variables, NOT Tailwind), ⚠️ emoji prefix
    - CLAUDE.md §"Style Conventions" — inline styles with CSS variables, NOT Tailwind classes
  </read_first>
  <files>app/src/components/ChatMessage.tsx, app/src/types/index.ts</files>
  <behavior>
    - When a SessionMessage carries `kind: 'malicious-block'`, ChatMessage renders an inline message styled distinctly from a normal AI reply
    - The malicious-block render shows the warning emoji prefix + the i18n string `t('chatMessage.maliciousBlocked.body')` — NO interactive buttons (no "Save anyway", no "Cancel", no "Override")
    - The existing flagged-off-topic render block (around lines 296-381) is UNCHANGED (D-04)
    - The malicious-block render uses inline-styles + CSS-variable palette: var(--surface), var(--border), var(--muted-foreground), borderRadius 12px, padding 12px 14px, fontSize 0.85rem
    - The malicious-block branch does NOT call any service (render-only) — no classifyAndAnchorIncremental, no patchQuestion, no event emission
    - When `kind === 'malicious-block'`, the normal markdown body render is short-circuited (the inline message replaces the body, NOT layered on top)
  </behavior>
  <action>
    Use the discriminated-union signaling shape (preferred over sentinel substring per RESEARCH §"Code Examples"). Update `app/src/types/index.ts` `SessionMessage` interface to add an optional `kind` discriminator: `kind?: 'normal' | 'malicious-block'`. Default semantics: absent OR `'normal'` follows existing render path. Only `'malicious-block'` triggers the new branch. Add a code comment next to the field referencing `Phase 47 D-01`.

    In `app/src/components/ChatMessage.tsx`, add a NEW peer render branch immediately ABOVE the existing flagged-off-topic block. Render condition: `type === 'ai' && message.kind === 'malicious-block'`. Use the styling palette listed in `<behavior>`. Use `role="status"` and `aria-live="polite"` for accessibility. Render the warning emoji as `<span aria-hidden="true">` followed by `<span>{t('chatMessage.maliciousBlocked.body')}</span>`.

    Replace the normal markdown body render with a guard: when `message.kind === 'malicious-block'`, do NOT render `<Markdown content={...}>`. Either short-circuit before the markdown render OR conditionally choose between the two render paths via a single ternary at the body site.

    Do NOT modify the existing flagged-off-topic block — D-04 explicitly preserves it. Do NOT add an override button to the new branch — D-02. Do NOT use Tailwind classes; use the same inline-style + CSS-variable discipline as the analog block.

    Use the existing `useTranslation()` hook (or whatever i18n hook this file uses) to read the i18n key. Do NOT inline the English copy.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit 2>&1 | tee /tmp/47-04-task1-tsc.log; if grep -q "error TS" /tmp/47-04-task1-tsc.log; then echo FAIL; exit 1; else echo OK; fi</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/types/index.ts` adds `kind?: 'normal' | 'malicious-block'` to the `SessionMessage` interface (verified by `grep -E "kind\\?:.*malicious-block" app/src/types/index.ts`)
    - `app/src/components/ChatMessage.tsx` contains a new conditional render branch on `message.kind === 'malicious-block'` (verified by `grep -F "malicious-block" app/src/components/ChatMessage.tsx`)
    - The new render branch references `chatMessage.maliciousBlocked.body` (verified by `grep -F "chatMessage.maliciousBlocked.body" app/src/components/ChatMessage.tsx`)
    - The new render branch contains NO `<button` element AND NO `onClick=` handler within the branch (verified by visually reading the new block)
    - The existing flagged-off-topic block at the analog of lines 296-381 is UNCHANGED (verify via git diff after the change — only an addition above it)
    - `npx tsc -b --noEmit` exits cleanly with no `error TS` lines
    - The new render block uses inline styles + CSS variables (NO `className=` with Tailwind utilities)
    - Existing tests in `app/tests/components/` for ChatMessage continue to pass (no regression)
  </acceptance_criteria>
  <done>
    SessionMessage.kind discriminator added; ChatMessage.tsx renders the malicious-block surface with i18n string + warning emoji + zero buttons; existing flagged-off-topic UI preserved untouched; typecheck green. Task 2 will set `kind: 'malicious-block'` on a new SessionMessage when the pre-gate label is malicious.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert useQuestions.askStreaming pipeline (pre-gate + three-branch handling)</name>
  <read_first>
    - app/src/state/useQuestions.ts (FULL file — focus on the askStreaming function body, roughly lines 90-330) — read the WHOLE function so the executor sees the existing structure: settings checks, abortController setup, chatStream Pass 1, optional Pass 2, buildAndSave, current post-flag filterQuestion call, current classifyAndAnchorIncremental fire-and-forget
    - app/src/services/question-filter.service.ts (rewritten by Plan 02) — the new three-label `FilterResult` contract evaluated here
    - app/src/services/question.service.ts (lines 184-285, lines 555-563) — `buildAndSave` + `patchQuestion` signatures the new branches call
    - app/src/lib/event-bus.ts — verify `QUESTION_ASKED` event payload type stays correct
    - app/src/lib/i18n.ts — `i18n.t` import to confirm the malicious-block message can be loaded
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Pipeline Inversion Sketch" §"useQuestions.askStreaming delta" (lines 729-792) — exact before/after shape
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Code Examples" §"Pattern: full-flow filter call" (lines 1059-1089) — concrete TypeScript snippet for the three-branch dispatch
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-CONTEXT.md D-01, D-18, D-19 binding constraints
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"app/src/state/useQuestions.ts:270-323" (lines 269-321) — pre-gate insertion site (right after abortController setup at line 134, BEFORE the try block at line 136); existing fire-and-forget pattern at line 316-319 stays for on-topic
    - CLAUDE.md §"Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)" — the system prompt construction MUST stay byte-stable; new pre-gate inserts BEFORE chatStream and does NOT touch the system prompt
    - CLAUDE.md §"Best practices learned in Phase 32.1" item 4 (async classification needs explicit re-read trigger) — the off-topic branch keeps the existing QUESTION_ASKED emit so HomeScreen + GraphScreen subscribers re-read flagged status
  </read_first>
  <files>app/src/state/useQuestions.ts</files>
  <behavior>
    - askStreaming calls `filterQuestion(content, sessionContext, abortController.signal)` immediately after the abortController setup (around line 134), BEFORE any chatStream invocation
    - When `filterResult.label === 'malicious'`: append a SessionMessage with `kind: 'malicious-block'` carrying `i18n.t('chatMessage.maliciousBlocked.body')` as content; call setIsAsking(false); return null; NEVER call chatStream or chatCompletion; NEVER call buildAndSave (no Question persisted — the malicious prompt does not enter the question store at all)
    - When `filterResult.label === 'off-topic'`: proceed through chatStream (existing pattern, ZERO changes to message-array construction or Pass-2 web-search dispatch); after buildAndSave, call patchQuestion(rawQuestion.id, {flagged: true}) and emit QUESTION_ASKED; SKIP the classifyAndAnchorIncremental call
    - When `filterResult.label === 'on-topic'`: proceed through the existing flow unchanged — chatStream, buildAndSave, fire-and-forget classifyAndAnchorIncremental
    - The DELETED post-LLM filterQuestion call (around lines 296-319) is GONE; the post-LLM patchQuestion-on-flag-change branch is GONE (replaced by the off-topic pre-flag patch)
    - filterQuestion accepts the existing abortController.signal as its third argument (D-19); when LOCALE_CHANGED fires mid-classification, the filterQuestion call is aborted cleanly via the AbortError path in Plan 02's evaluator
    - When filterQuestion throws a non-abort error (embedding outage), the pre-gate gracefully degrades to label='on-topic' (D-12) and the existing flow proceeds
    - sessionContext is constructed exactly as it is today (priorQuestion + priorAnswer extraction unchanged) and passed verbatim to filterQuestion
    - The system prompt construction (the byte-stable Phase 35 prompt) is UNCHANGED — the pre-gate is a separate function call BEFORE chatStream, never mutating the messages array passed to chatStream
  </behavior>
  <action>
    MODIFY `app/src/state/useQuestions.ts` askStreaming function in three editing steps.

    **Edit 1 — Insert pre-gate after abortController setup, before the try block (around line 134-136 per PATTERNS.md):**

    Add a block that calls `filterQuestion(content, sessionContext, abortController.signal)` inside try/catch:
      - On AbortError (signal.aborted): toast localeChangedDiscarded, setIsAsking(false), return null
      - On other error: console.warn the error message, set `filterResult = { label: 'on-topic' }` (D-12 graceful degradation)

    Then check `filterResult.label === 'malicious'`:
      - Construct a SessionMessage with the same id/role/timestamp shape used elsewhere when appending an AI message in this file (read the surrounding code to see the exact field set)
      - Set `kind: 'malicious-block'` and `content: i18n.t('chatMessage.maliciousBlocked.body')`
      - Append using whatever session-message-append API this file uses (read the file to find the pattern — may be `setSessions(...)`, `sessionService.appendMessage(...)`, etc.)
      - setIsAsking(false), return null
      - DO NOT call chatStream or buildAndSave for the malicious branch

    Add a type-only import at the top of the file: `import type { FilterResult } from '../services/question-filter.service';` The runtime `filterQuestion` alias import already exists.

    **Edit 2 — Update the post-LLM region (around lines 296-319) to branch on filterResult.label instead of running filterQuestion again:**

    Locate the existing block that:
      1. calls `questionService.buildAndSave(...)` storing into `rawQuestion`
      2. calls `incrementAskCount()`
      3. calls `await filterQuestion(rawQuestion, sessionContext)` storing into `question`
      4. patches the flag if changed and emits QUESTION_ASKED
      5. fires `void classifyAndAnchorIncremental(...)` when not flagged

    Replace steps 3-5 with the following branch logic (steps 1 and 2 unchanged):
      - If `filterResult.label === 'off-topic'`:
        - `questionService.patchQuestion(rawQuestion.id, { flagged: true })`
        - `rawQuestion.flagged = true` (mutate the local copy so downstream sees the flag)
        - `eventBus.emit({ type: 'QUESTION_ASKED', payload: rawQuestion })`
        - SKIP classifyAndAnchorIncremental entirely (D-01 — flagged questions do NOT enter mind map)
      - Else (on-topic — note malicious already returned earlier):
        - Fire `void classifyAndAnchorIncremental(rawQuestion, questionService.getAll(), llmConfig, abortController.signal).catch((err) => console.warn(...))` — preserves the existing line VERBATIM with `rawQuestion` substituted for the deleted `question` variable

    Update the function's return value (around line 322) to `return rawQuestion` instead of the deleted `return question`.

    **Edit 3 — Verify filterQuestion is referenced exactly twice in the file:**
      1. Once as the alias import at the top
      2. Once in Edit 1's pre-gate call

    If a third occurrence remains (the deleted post-LLM call), remove it. Run `grep -c "filterQuestion(" app/src/state/useQuestions.ts` — must return 1 (the call site; the alias import line uses different syntax `evaluateQuestion as filterQuestion` and is matched separately).

    **Critical non-modifications:**
      - DO NOT touch the system prompt construction `systemPrompt = '...'` near the function start — Phase 35 byte-stability invariant
      - DO NOT touch the `assistantContextMessage` / `USER_ACK_BEFORE_GRAPH_CONTEXT` plumbing — Phase 35 cache discipline
      - DO NOT touch the chatStream Pass-1 + optional Pass-2 web-search dispatch — out of scope
      - DO NOT add a new event type (e.g., do NOT introduce QUESTION_FLAGGED_PRE_GATE) — reuse existing QUESTION_ASKED per CLAUDE.md "One signal per semantic event"
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit 2>&1 | tee /tmp/47-04-task2-tsc.log; if grep -q "error TS" /tmp/47-04-task2-tsc.log; then echo TYPECHECK_FAIL; exit 1; fi; node --test tests/state/useQuestions-system-prompt-stability.test.mjs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filterQuestion(" app/src/state/useQuestions.ts` returns exactly 1 (the pre-gate call site; alias-import line uses different syntax)
    - The pre-gate `await filterQuestion(content, sessionContext, abortController.signal)` appears BEFORE the first `const stream = chatStream(` invocation in the askStreaming function (verified by source offset comparison via grep)
    - `grep -F "filterResult.label === 'malicious'" app/src/state/useQuestions.ts` returns ≥ 1 match (the early-return branch exists)
    - `grep -F "filterResult.label === 'off-topic'" app/src/state/useQuestions.ts` returns ≥ 1 match (the off-topic flag-set branch exists)
    - `grep -F "kind: 'malicious-block'" app/src/state/useQuestions.ts` returns ≥ 1 match (SessionMessage with discriminator is constructed in the malicious branch)
    - `grep -E "patchQuestion\\([^)]*flagged:\\s*(true\\|false)" app/src/state/useQuestions.ts` shows patchQuestion called with `flagged: true` (off-topic branch) — verified by reading the diff
    - The existing `eventBus.emit({ type: 'QUESTION_ASKED'` call is preserved in the off-topic branch
    - `npx tsc -b --noEmit` exits cleanly with no `error TS` lines (CRITICAL — type-narrowing on FilterResult.label must be sound)
    - `cd app && node --test tests/state/useQuestions-system-prompt-stability.test.mjs` PASSES (CRITICAL — Phase 35 byte-stability invariant preserved; failure means the system prompt or its ordering was inadvertently mutated)
    - No new event types introduced (verify by `grep -E "type:\\s*'(QUESTION_FLAGGED|FILTER_BLOCKED|MALICIOUS_BLOCKED)" app/src/state/useQuestions.ts` returns ZERO matches)
  </acceptance_criteria>
  <done>
    askStreaming inverts to pre-gate + three-branch dispatch; malicious branch never calls chatStream and never persists a Question; off-topic branch persists with `flagged: true` from the start (no patch round-trip); on-topic branch unchanged; Phase 35 byte-stability test still passes; typecheck green. Plan 05 mirrors this exact pattern in `question.service.ask`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Source-reading test enforcing D-18 ordering + D-19 abort + D-01 malicious-no-LLM</name>
  <read_first>
    - app/tests/state/useQuestions-system-prompt-stability.test.mjs (FULL file, lines 1-120) — exact analog: source-reading test, indexOf+offset comparison ordering pattern, slice-by-anchor-pair shape
    - app/src/state/useQuestions.ts (just modified by Task 2) — the source under test
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map" rows for FILTER-02 (lines 1205-1207) — the three invariants this test enforces
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"app/tests/state/useQuestions-pre-gate.test.mjs" (lines 679-717) — analog mapping; canonical "X happens BEFORE Y" source assertion
  </read_first>
  <files>app/tests/state/useQuestions-pre-gate.test.mjs</files>
  <action>
    Create `app/tests/state/useQuestions-pre-gate.test.mjs`. Mirror the `useQuestions-system-prompt-stability.test.mjs` scaffold:
      - Read source via `fs.readFileSync(new URL('../../src/state/useQuestions.ts', import.meta.url), 'utf-8')`
      - Use `describe`/`it` from `node:test`
      - Source-reading discipline: never instantiate the hook, never stub providers — operate purely on the source text

    Required assertions (each as a separate `it` case):

    1. **filterQuestion is called BEFORE the first chatStream invocation in askStreaming.**
       - Locate askStreaming via `source.indexOf('askStreaming')` (or whatever the function declaration anchor is — verify by reading the file)
       - Slice to the function's end (next top-level `const ` declaration OR the file's end)
       - Inside the slice: find offsets of `filterQuestion(content` AND `chatStream(`
       - Assert both are found (`!== -1`) and `filterQuestionOffset < chatStreamOffset`
       - Failure message: "Phase 47 D-18 — filterQuestion (pre-gate) MUST run BEFORE chatStream in askStreaming. The post-LLM-flag pattern was replaced. See 47-RESEARCH.md §'Pipeline Inversion Sketch'."

    2. **filterQuestion is called with abortController.signal as the third argument (D-19).**
       - Inside the askStreaming slice: assert `source.match(/filterQuestion\\s*\\(\\s*content\\s*,\\s*sessionContext\\s*,\\s*abortController\\.signal\\s*\\)/)` matches
       - Failure message: "Phase 47 D-19 — filterQuestion call MUST pass abortController.signal as third arg so LOCALE_CHANGED cancellation propagates to embedding."

    3. **The malicious branch does NOT invoke chatStream or chatCompletion.**
       - Slice the source between the `filterResult.label === 'malicious'` literal and the next `}` at the matching brace level (use the same anchor-pair slice pattern as the analog test)
       - Inside the slice, assert `source.match(/chatStream\\(|chatCompletion\\(/)` returns null AND `source.match(/buildAndSave\\(/)` returns null
       - Failure message: "Phase 47 D-01 — malicious branch MUST NOT call chatStream/chatCompletion or buildAndSave. Found a forbidden call inside the malicious branch."

    4. **The malicious branch sets SessionMessage.kind = 'malicious-block'.**
       - Inside the malicious-branch slice, assert `source.includes("kind: 'malicious-block'")` (single-quoted) OR `source.includes('kind: "malicious-block"')` (double-quoted)
       - Failure message: "Phase 47 D-01/D-02 — malicious branch MUST construct a SessionMessage with kind: 'malicious-block' so ChatMessage renders the inline rejection surface."

    5. **The off-topic branch calls patchQuestion with flagged: true and SKIPS classifyAndAnchorIncremental.**
       - Slice between `filterResult.label === 'off-topic'` and the next matching `}`
       - Inside the slice, assert `source.match(/patchQuestion\\([^)]*flagged:\\s*true/)` matches AND `source.match(/classifyAndAnchorIncremental\\(/)` returns null
       - Failure message: "Phase 47 D-01 — off-topic branch MUST patchQuestion(flagged:true) AND MUST NOT fire classifyAndAnchorIncremental (flagged questions do not enter mind map)."

    6. **No new event types introduced.**
       - Source-wide assertion: `source.match(/type:\\s*['\"](?:QUESTION_FLAGGED_PRE_GATE|FILTER_BLOCKED|MALICIOUS_BLOCKED)['\"]/)` returns null
       - Failure message: "Phase 47 — reuse existing QUESTION_ASKED event per CLAUDE.md 'One signal per semantic event'. New event types not allowed."

    7. **Phase 35 byte-stable system prompt construction is preserved.**
       - This is a regression-by-reference assertion: read both `useQuestions.ts` AND the EXISTING `tests/state/useQuestions-system-prompt-stability.test.mjs`. Just confirm the analog test file still exists. Actual byte-stability is enforced by running that test (which `<verify>` runs).

    Test scaffold notes:
      - Use `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';`
      - Read source ONCE at module top (as the analog does)
      - Each `it` is independent; no shared mutable state
      - Use the `slice + indexOf + match` discipline from the analog — do NOT instantiate the hook
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/state/useQuestions-pre-gate.test.mjs tests/state/useQuestions-system-prompt-stability.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `app/tests/state/useQuestions-pre-gate.test.mjs` exists with 6 `it` cases (cases 1-6 above; case 7 is implicit via the verify command running the analog test)
    - Each `it` case has a failure message that names the relevant decision ID (D-18, D-19, D-01, D-02) and points to RESEARCH.md or CLAUDE.md for context
    - The test reads source via `fs.readFileSync` — does NOT import or instantiate `useQuestions` directly (analog source-reading discipline)
    - `cd app && node --test tests/state/useQuestions-pre-gate.test.mjs` PASSES with all 6 cases green
    - `cd app && node --test tests/state/useQuestions-system-prompt-stability.test.mjs` PASSES (Phase 35 invariant unchanged — combined verify command above runs both)
    - The test does NOT use any provider stubbing, embedding mock, or settings shim — pure source-text inspection
  </acceptance_criteria>
  <done>
    Source-reading guard for D-18 ordering, D-19 abort threading, D-01 malicious-no-LLM-call, D-01 off-topic-skip-classification, D-02 malicious-block-render-signal, and one-event-per-semantic-event invariant lands; combined with the unchanged Phase 35 byte-stability test, this set is the FILTER-02 regression contract for the useQuestions side of the pipeline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→useQuestions.askStreaming | Untrusted user content enters the hook from ChatInput. |
| askStreaming→provider | After the pre-gate, only on-topic + off-topic content reaches chatStream (which then bracketing wraps via Plan 03). Malicious content NEVER reaches chatStream. |
| askStreaming→sessionService | Malicious content NEVER persists as a Question (zero rows added to the question store for malicious labels). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-15 | Tampering | Pipeline-inversion regression (post-LLM-flag returns) | mitigate | Source-reading test case 1 enforces filterQuestion BEFORE chatStream in askStreaming. |
| T-47-16 | DoS | Token spend on malicious prompts | mitigate | Source-reading test case 3 asserts the malicious branch does NOT invoke chatStream/chatCompletion/buildAndSave. |
| T-47-17 | Tampering | Phase 35 byte-stability regression breaks KV-cache | mitigate | The existing tests/state/useQuestions-system-prompt-stability.test.mjs continues to PASS after this plan lands; pre-gate inserted BEFORE chatStream and does NOT touch system prompt. Combined verify command runs both tests. |
| T-47-18 | Information Disclosure | Off-topic question silently entering mind map | mitigate | Source-reading test case 5 asserts the off-topic branch SKIPS classifyAndAnchorIncremental. |
| T-47-19 | Tampering | Cancel signal not propagating to embedding (LOCALE_CHANGED leaves stale call running) | mitigate | Source-reading test case 2 asserts abortController.signal is the 3rd arg to filterQuestion. |
| T-47-SC | Tampering | npm/pip/cargo install (none in this plan) | N/A | Zero new packages. |
</threat_model>

<verification>
- `cd app && npx tsc -b --noEmit` PASSES (no `error TS` lines)
- `cd app && node --test tests/state/useQuestions-pre-gate.test.mjs tests/state/useQuestions-system-prompt-stability.test.mjs` exits 0
- `grep -c "filterQuestion(" app/src/state/useQuestions.ts` returns exactly 1 (one call site)
- `grep -E "type:\\s*['\"](?:QUESTION_FLAGGED_PRE_GATE|FILTER_BLOCKED|MALICIOUS_BLOCKED)['\"]" app/src/state/useQuestions.ts` returns 0 (no new event types)
- `cd app && node --test tests/locales/bundle-parity.test.mjs` continues to PASS (Plan 01's i18n keys remain consistent)
- `cd app && node --test tests/components/` continues to PASS (existing ChatMessage tests do not regress; the new render branch is additive)
</verification>

<success_criteria>
1. SessionMessage.kind discriminator + ChatMessage malicious-block render branch landed (Task 1; D-01 + D-02)
2. askStreaming inverts to pre-gate + three-branch dispatch (Task 2; D-18)
3. Malicious branch makes ZERO LLM calls and persists ZERO Question rows (Task 3 case 3; D-01)
4. Off-topic branch persists with `flagged: true` and SKIPS classifyAndAnchorIncremental (Task 3 case 5; D-01)
5. abortController.signal threaded through filterQuestion (Task 3 case 2; D-19)
6. Phase 35 byte-stable system prompt invariant preserved — existing test still PASSES (Task 2 + Task 3 verify)
7. No new event types introduced (Task 3 case 6; CLAUDE.md "One signal per semantic event")
8. typecheck green; bundle-parity green; existing component tests green
</success_criteria>

<output>
Create `.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-04-SUMMARY.md` when done. Summary should record: the SessionMessage.kind diff in types/index.ts, the diff size in useQuestions.ts (lines added/removed), the three-branch dispatch shape, the 6 source-reading test cases, confirmation that the Phase 35 byte-stability test still passes, and the exact line where filterQuestion is now called.
</output>
