---
phase: 47-filter-redesign-off-topic-malicious-prompt-prevention
plan: 05
type: execute
wave: 2
depends_on:
  - 47-01
  - 47-02
files_modified:
  - app/src/services/question.service.ts
  - app/tests/services/question-service-pre-gate.test.mjs
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
tags:
  - filter
  - pipeline-inversion
  - question-service

must_haves:
  truths:
    - "question.service.ask invokes filterQuestion(content, sessionContext, signal) BEFORE the chatCompletion JSON-mode call AND BEFORE the embedding precompute (mirror of useQuestions inversion per D-18)"
    - "When filterResult.label === 'malicious', ask returns ServiceResult error {code:'BLOCKED_MALICIOUS', message: i18n string, retryable:false} WITHOUT making any chatCompletion call AND without computing the question embedding (zero LLM tokens, zero embedding tokens per D-01)"
    - "When filterResult.label === 'off-topic', ask proceeds through chatCompletion + buildAndSave + patchQuestion(flagged:true) and SKIPS classifyAndAnchorIncremental (D-01)"
    - "When filterResult.label === 'on-topic', ask follows the existing flow with classifyAndAnchorIncremental fired (no behavioral change for legitimate questions)"
    - "filterQuestion accepts the existing signal parameter as third arg (D-19)"
    - "QuestionFilterContext { priorQuestion, priorAnswer } continues to flow into filterQuestion unchanged (D-17)"
  artifacts:
    - path: app/src/services/question.service.ts
      provides: "Inverted ask method with three-branch handling (mirror of useQuestions per D-18)"
      contains: "filterResult.label"
    - path: app/tests/services/question-service-pre-gate.test.mjs
      provides: "Source-reading guard for D-18 ordering + D-19 abort + D-01 malicious-no-LLM in question.service.ask"
      contains: "filterQuestion"
  key_links:
    - from: app/src/services/question.service.ts
      to: app/src/services/question-filter.service.ts (Plan 02)
      via: "filterQuestion(content, sessionContext, signal) call"
      pattern: "filterQuestion\\("
---

<objective>
Wave 2 (parallel with Plan 04 — zero file overlap) — Mirror Plan 04's pipeline inversion in `question.service.ask` per D-18. The `ask` method has the same post-LLM-flag pattern as `useQuestions.askStreaming` and gets the same three-branch pre-gate dispatch.

Purpose: `question.service.ask` is a SECOND consumer of the filter (used by code paths that don't go through the React hook — e.g., direct service-layer calls per RESEARCH §"Pipeline Inversion Sketch" §"question.service.ask delta" line 794-800). Without this mirror, malicious prompts entering through this path would still hit the LLM. The inversion is structurally identical to Plan 04 but uses `ServiceResult<T>` for the malicious branch (since this is a service, not a hook).

Output: question.service.ts ask-method inversion + a source-reading test guarding the same D-18/D-19/D-01 invariants Plan 04 enforces for the hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-CONTEXT.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md
@.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-02-SUMMARY.md
@CLAUDE.md
@app/src/services/question.service.ts
</context>

<interfaces>
Plan 02 contracts question.service.ask consumes (same as Plan 04):

From app/src/services/question-filter.service.ts (rewritten by Plan 02):
- export type FilterLabel = 'on-topic' | 'off-topic' | 'malicious'
- export interface FilterResult { label: FilterLabel; bestMatch?: { ... } }
- export async function evaluateQuestion(content, context?, signal?): Promise FilterResult

Existing alias import in question.service.ts (line 15):
- import { evaluateQuestion as filterQuestion } from './question-filter.service'

Existing ServiceResult error shape used at line 191 (NOT_CONFIGURED precedent — read the file):
- { success: false, error: { code: 'NOT_CONFIGURED', message: '...', retryable: false } }

NEW error code for malicious branch:
- { success: false, error: { code: 'BLOCKED_MALICIOUS', message: i18n.t('chatMessage.maliciousBlocked.body'), retryable: false } }

Existing classifyAndAnchorIncremental fire-and-forget site (around lines 285-287):
- void classifyAndAnchorIncremental(flagged, ..., llmConfig, signal).catch(...)

The off-topic branch SKIPS this; the on-topic branch keeps it.

Existing pre-gate insertion site per PATTERNS.md (line 353):
- RIGHT AFTER the early-return for `!llmConfig.isConfigured` at line 197, BEFORE the embedding precompute at line 199 (so a malicious label avoids both the embedding precompute AND the chatCompletion JSON-mode call)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert question.service.ask pipeline (mirror Plan 04 with ServiceResult error shape)</name>
  <read_first>
    - app/src/services/question.service.ts (FULL file — focus on the `ask` method, roughly lines 184-298) — read the whole method body so the executor sees: settings/llmConfig check at line 191, embedding precompute at line 199, chatCompletion JSON-mode call, current post-LLM filterQuestion call at lines 269-285, fire-and-forget classifyAndAnchorIncremental at line 285-287
    - app/src/services/question-filter.service.ts (rewritten by Plan 02) — the new three-label `FilterResult` contract evaluated here
    - app/src/lib/i18n.ts — `i18n.t` import for the malicious-block error message
    - app/src/state/useQuestions.ts (just modified by Plan 04 Task 2) — the analog inversion; question.service.ask mirrors the SAME shape with ServiceResult error in the malicious branch
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Pipeline Inversion Sketch" §"question.service.ask delta" (lines 794-800) — same inversion pattern as useQuestions
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-CONTEXT.md D-01, D-18, D-19
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"app/src/services/question.service.ts:184-285" (lines 324-362) — pre-gate insertion site (right after the !llmConfig.isConfigured check at line 197, before embedding precompute at line 199); ServiceResult error shape with `code: 'BLOCKED_MALICIOUS'`; non-modification rule (do NOT add a flag-transition hook to patchQuestion)
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"Shared Patterns: ServiceResult<T> error shape" (lines 786-790) — pattern source
    - CLAUDE.md §"Best practices learned in Phase 32.1" item 1 (search for dead code BEFORE assuming) — verify `ask` is actually called (confirmed by reading PATTERNS.md which references it as a live consumer)
  </read_first>
  <files>app/src/services/question.service.ts</files>
  <behavior>
    - `ask(content, llmConfig, sessionContext, signal)` (or whatever the actual signature is — read the file) calls `filterQuestion(content, sessionContext, signal)` immediately AFTER the existing `if (!llmConfig.isConfigured) return ...` early-return at line ~197 AND BEFORE the embedding precompute at line ~199
    - When filterResult.label === 'malicious': return `{ success: false, error: { code: 'BLOCKED_MALICIOUS', message: i18n.t('chatMessage.maliciousBlocked.body'), retryable: false } }`. The method returns BEFORE invoking chatCompletion (no LLM call) AND BEFORE the embedding precompute (no embedding call) — verified by source-reading test
    - When filterResult.label === 'off-topic': proceed through embedding precompute + chatCompletion + buildAndSave; after buildAndSave, call patchQuestion({flagged: true}) and persist via the existing `freshStore[idx] = ...; saveStore(...); persistToSQLite(...)` discipline at lines 274-281; SKIP classifyAndAnchorIncremental
    - When filterResult.label === 'on-topic': existing flow unchanged (embedding precompute, chatCompletion, buildAndSave, fire-and-forget classifyAndAnchorIncremental)
    - The DELETED post-LLM filterQuestion call (lines 269) is GONE; the post-LLM patchQuestion-on-flag site is replaced by the off-topic pre-flag patch
    - filterQuestion accepts `signal` as the third argument (D-19) when the ask method already receives a signal parameter; if the method does not currently accept a signal, leave that addition as a planner-discretion implementation detail (RESEARCH §"Pipeline Inversion Sketch" defers this to Implementation)
    - When filterQuestion throws a non-abort error, the pre-gate gracefully degrades to label='on-topic' (D-12) and the existing flow proceeds
    - DO NOT add a flag-transition hook inside patchQuestion (PATTERNS.md "Important non-modification" — patchQuestion is used by 14+ call sites; spurious fires are unacceptable)
  </behavior>
  <action>
    MODIFY `app/src/services/question.service.ts` `ask` method in two editing steps.

    **Edit 1 — Insert pre-gate immediately after the !llmConfig.isConfigured early-return (around line 197), BEFORE the embedding precompute (around line 199):**

    Insert a block that calls `filterQuestion(content, sessionContext, signal)` inside try/catch:
      - On AbortError (signal?.aborted): return `{ success: false, error: { code: 'ABORTED', message: '...', retryable: false } }` if there is an existing aborted-error code in this file's pattern, OR mirror useQuestions.askStreaming's localeChangedDiscarded toast equivalent — read the existing patterns in the file to choose the closest match
      - On other error: log via `console.warn`, set `filterResult = { label: 'on-topic' }` (D-12 graceful degradation)

    Then check `filterResult.label === 'malicious'`:
      - Return `{ success: false, error: { code: 'BLOCKED_MALICIOUS', message: i18n.t('chatMessage.maliciousBlocked.body'), retryable: false } }`
      - DO NOT call embedText, chatCompletion, buildAndSave, patchQuestion, or classifyAndAnchorIncremental

    Add the type-only import at the top: `import type { FilterResult } from './question-filter.service';`

    **Edit 2 — Update the post-LLM region (around lines 266-287) to branch on filterResult.label instead of running filterQuestion again:**

    Locate the existing block that:
      1. calls `this.buildAndSave(content, answer, store, ...)` storing into `question`
      2. calls `await filterQuestion(question, sessionContext)` storing into `flagged`
      3. patches the flagged status back to store + SQLite if changed
      4. fires `void classifyAndAnchorIncremental(flagged, ..., llmConfig, signal)` when not flagged

    Replace with:
      - Step 1 unchanged (`this.buildAndSave(...)` storing into `question`)
      - If `filterResult.label === 'off-topic'`:
        - Patch flagged: read the existing `freshStore[idx] = ...; saveStore(freshStore); persistToSQLite(...)` pattern at lines 274-281; replicate it but set `freshStore[idx] = { ...question, flagged: true }` directly (no second filterQuestion call needed)
        - SKIP classifyAndAnchorIncremental
      - Else (on-topic — note malicious already returned earlier):
        - Fire `void classifyAndAnchorIncremental(question, loadStore({ includeFlagged: true }), llmConfig, signal).catch((err) => console.warn(...))` — preserves the existing line VERBATIM with `question` substituted for the deleted `flagged` variable

    Update the success return at the method's end accordingly — return the original/patched `question` per existing return shape.

    **Verify filterQuestion is referenced exactly twice in the file:**
      1. Once as the alias import at line ~15
      2. Once in Edit 1's pre-gate call

    Run `grep -c "filterQuestion(" app/src/services/question.service.ts` — must return 1 (the call site; the alias import line uses different syntax `evaluateQuestion as filterQuestion`).

    **Critical non-modifications:**
      - DO NOT add a flag-transition hook inside `patchQuestion` (PATTERNS.md line 521-525 + RESEARCH §"D-06 Gap Closure" line 521-525 — patchQuestion has 14+ call sites; spurious fires unacceptable). The override re-fire (D-06) is Plan 06's responsibility at the AskScreen call site, NOT here
      - DO NOT add a new event type — reuse existing event-emission pattern this file already has (if any)
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit 2>&1 | tee /tmp/47-05-tsc.log; if grep -q "error TS" /tmp/47-05-tsc.log; then echo TYPECHECK_FAIL; exit 1; else echo OK; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filterQuestion(" app/src/services/question.service.ts` returns exactly 1 (the pre-gate call site)
    - The pre-gate appears AFTER the `!llmConfig.isConfigured` early-return AND BEFORE the embedding precompute (verified by source offset comparison via grep — `filterQuestion` offset > `isConfigured` offset AND `filterQuestion` offset < first `embedText(` offset)
    - `grep -F "filterResult.label === 'malicious'" app/src/services/question.service.ts` returns ≥ 1 match
    - `grep -F "filterResult.label === 'off-topic'" app/src/services/question.service.ts` returns ≥ 1 match
    - `grep -F "BLOCKED_MALICIOUS" app/src/services/question.service.ts` returns ≥ 1 match (the new ServiceResult error code)
    - `grep -F "chatMessage.maliciousBlocked.body" app/src/services/question.service.ts` returns ≥ 1 match (i18n key referenced by the error message)
    - `npx tsc -b --noEmit` exits cleanly with no `error TS` lines
    - `grep -F "patchQuestion" app/src/services/question.service.ts` count does NOT decrease (the patchQuestion implementation is unchanged; only its callers may have shifted) — reference: count BEFORE the change vs AFTER
    - The `patchQuestion` method body (the helper that 14+ call sites depend on) has NO new flag-transition hook (read the method body — it remains pure persistence per PATTERNS.md)
    - No new event types introduced (verify by `grep -E "type:\\s*['\"](?:QUESTION_FLAGGED_PRE_GATE|FILTER_BLOCKED|MALICIOUS_BLOCKED)['\"]" app/src/services/question.service.ts` returns ZERO)
  </acceptance_criteria>
  <done>
    question.service.ask inverts to pre-gate + three-branch dispatch with ServiceResult error for malicious; embedding precompute + chatCompletion both skipped on malicious; off-topic persists with `flagged: true` from the start (no second filterQuestion call); on-topic unchanged; patchQuestion implementation unchanged (no spurious flag-transition hook); typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Source-reading test enforcing D-18 + D-19 + D-01 invariants for question.service.ask</name>
  <read_first>
    - app/tests/state/useQuestions-pre-gate.test.mjs (created by Plan 04 Task 3) — exact analog scaffold to copy/adapt; same source-reading + slice + offset-comparison discipline, just pointed at `question.service.ts`
    - app/src/services/question.service.ts (just modified by Task 1 above) — the source under test
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map" rows for FILTER-02 (line 1206 specifically: "Same inversion in question.service.ask")
    - .planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-PATTERNS.md §"app/tests/services/question-service-pre-gate.test.mjs" (lines 720-727) — analog confirmed
  </read_first>
  <files>app/tests/services/question-service-pre-gate.test.mjs</files>
  <action>
    Create `app/tests/services/question-service-pre-gate.test.mjs`. Mirror `app/tests/state/useQuestions-pre-gate.test.mjs` (created by Plan 04 Task 3) but pointed at `app/src/services/question.service.ts`.

    Required assertions (each as a separate `it` case):

    1. **filterQuestion is called BEFORE the chatCompletion call in `ask`.**
       - Locate the `ask` method via `source.indexOf('ask(') ` or whatever anchor matches the method signature in this file (read to confirm — it may be `async ask(` or similar)
       - Slice from the method declaration to the next top-level method/property OR file end
       - Inside the slice: find offsets of `filterQuestion(` AND `chatCompletion(`
       - Assert both found AND `filterQuestionOffset < chatCompletionOffset`
       - Failure message: "Phase 47 D-18 — filterQuestion (pre-gate) MUST run BEFORE chatCompletion in question.service.ask. See 47-RESEARCH.md §'Pipeline Inversion Sketch'."

    2. **filterQuestion is called BEFORE the embedding precompute (the FIRST embedText call) in `ask`.**
       - Inside the ask-method slice: find offsets of `filterQuestion(` AND `embedText(`
       - Assert `filterQuestionOffset < embedTextOffset`
       - Failure message: "Phase 47 D-01 — malicious prompts MUST avoid the embedding precompute too (zero embedding tokens). filterQuestion must precede embedText in question.service.ask."

    3. **filterQuestion is called with the signal parameter as the third argument (D-19) — IF the ask method accepts a signal parameter.**
       - Conditional assertion: if the ask method's signature includes a `signal` parameter (verify by `source.match(/ask\\([^)]*signal/)`), THEN assert `source.match(/filterQuestion\\([^)]*signal[^)]*\\)/)` matches inside the ask-method slice
       - Failure message: "Phase 47 D-19 — when ask accepts a signal param, filterQuestion MUST receive it for LOCALE_CHANGED cancellation."

    4. **The malicious branch returns ServiceResult error with code 'BLOCKED_MALICIOUS' and does NOT invoke chatCompletion or embedText.**
       - Slice between `filterResult.label === 'malicious'` and the next matching `}`
       - Inside the slice: assert presence of `code: 'BLOCKED_MALICIOUS'` (or `code: "BLOCKED_MALICIOUS"`)
       - Inside the slice: assert ABSENCE of `chatCompletion(` AND `embedText(` AND `buildAndSave(` AND `classifyAndAnchorIncremental(`
       - Failure message: "Phase 47 D-01 — malicious branch MUST return ServiceResult error AND MUST NOT invoke chatCompletion/embedText/buildAndSave/classifyAndAnchorIncremental."

    5. **The off-topic branch sets flagged: true and SKIPS classifyAndAnchorIncremental.**
       - Slice between `filterResult.label === 'off-topic'` and the next matching `}`
       - Inside the slice: assert presence of `flagged: true` (verifies the off-topic flag is set per D-01)
       - Inside the slice: assert ABSENCE of `classifyAndAnchorIncremental(`
       - Failure message: "Phase 47 D-01 — off-topic branch MUST set flagged:true AND MUST NOT fire classifyAndAnchorIncremental."

    6. **patchQuestion has no flag-transition hook (PATTERNS.md non-modification rule).**
       - Locate the `patchQuestion` method via `source.indexOf('patchQuestion(')` (the method declaration site, not call sites)
       - Slice the method body
       - Inside the slice: assert ABSENCE of `classifyAndAnchorIncremental(` AND ABSENCE of `eventBus.emit({\\s*type:\\s*['"]GRAPH_UPDATED` (regex)
       - Failure message: "Phase 47 / 47-PATTERNS.md — patchQuestion MUST remain pure persistence. The override re-fire belongs at the AskScreen.handleQuestionOverride call site (Plan 06), NOT inside patchQuestion. patchQuestion has 14+ call sites; a flag-transition hook here would fire spuriously."

    7. **No new event types introduced.**
       - Source-wide assertion: `source.match(/type:\\s*['\"](?:QUESTION_FLAGGED_PRE_GATE|FILTER_BLOCKED|MALICIOUS_BLOCKED)['\"]/)` returns null
       - Failure message: "Phase 47 — reuse existing events per CLAUDE.md 'One signal per semantic event'."

    Use the same scaffold as the analog test:
      - `import { describe, it } from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';`
      - Read source ONCE at module top
      - Each `it` is independent; pure source-text inspection
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/question-service-pre-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `app/tests/services/question-service-pre-gate.test.mjs` exists with 7 `it` cases (cases 1-7 above)
    - Each `it` case has a failure message that names the relevant decision ID (D-18, D-19, D-01) and points to RESEARCH.md or PATTERNS.md for context
    - The test reads source via `fs.readFileSync` — does NOT instantiate any service
    - `cd app && node --test tests/services/question-service-pre-gate.test.mjs` PASSES with all 7 cases green
    - Case 6 (patchQuestion non-modification) PASSES — confirms Task 1 did NOT regress the patchQuestion-side discipline
    - The test does NOT use any provider stubbing, embedding mock, or settings shim
  </acceptance_criteria>
  <done>
    Source-reading guard for D-18 ordering (vs both chatCompletion AND embedText), D-19 abort threading, D-01 malicious-no-LLM-no-embedding, D-01 off-topic-skip-classification, patchQuestion non-modification, and one-event-per-semantic-event invariant lands. Combined with Plan 04's pre-gate test, BOTH consumers of filterQuestion are now structurally guarded against post-LLM-flag regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→question.service.ask | Untrusted user content enters via direct service-layer calls (not through useQuestions). |
| ask→provider | After the pre-gate, only on-topic + off-topic content reaches chatCompletion. Malicious content NEVER reaches chatCompletion AND never reaches the embedding precompute. |
| ask→sessionService/store | Malicious content NEVER persists as a Question. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-20 | DoS | Token spend on malicious prompts via the service-layer path | mitigate | Source-reading test case 4 enforces malicious branch does NOT invoke chatCompletion/embedText/buildAndSave/classifyAndAnchorIncremental. |
| T-47-21 | Tampering | Pipeline-inversion regression in question.service.ask | mitigate | Source-reading test case 1 enforces filterQuestion BEFORE chatCompletion AND case 2 enforces filterQuestion BEFORE embedText. |
| T-47-22 | Information Disclosure | Off-topic question silently entering mind map via service-layer path | mitigate | Source-reading test case 5 enforces off-topic skips classifyAndAnchorIncremental. |
| T-47-23 | Tampering | Spurious flag-transition fire if patchQuestion gets a side-effect hook | mitigate | Source-reading test case 6 negative-asserts no classifyAndAnchorIncremental call inside patchQuestion body. |
| T-47-SC | Tampering | npm/pip/cargo install (none in this plan) | N/A | Zero new packages. |
</threat_model>

<verification>
- `cd app && npx tsc -b --noEmit` PASSES (no `error TS` lines)
- `cd app && node --test tests/services/question-service-pre-gate.test.mjs tests/state/useQuestions-pre-gate.test.mjs` exits 0 (combined Plan 04 + Plan 05 invariant tests)
- `grep -c "filterQuestion(" app/src/services/question.service.ts` returns exactly 1 (one call site)
- `grep -E "type:\\s*['\"](?:QUESTION_FLAGGED_PRE_GATE|FILTER_BLOCKED|MALICIOUS_BLOCKED)['\"]" app/src/services/question.service.ts` returns 0
- `cd app && node --test tests/services/classification-dedup.test.mjs` continues to PASS (no regression in the canonical-knowledge analog tests)
</verification>

<success_criteria>
1. ask method inverts to pre-gate + three-branch dispatch (Task 1; D-18)
2. Malicious branch returns ServiceResult error with code 'BLOCKED_MALICIOUS' and makes ZERO LLM/embedding calls (Task 2 case 4; D-01)
3. Off-topic branch persists with `flagged: true` and SKIPS classifyAndAnchorIncremental (Task 2 case 5; D-01)
4. signal threaded through filterQuestion when ask accepts it (Task 2 case 3; D-19)
5. patchQuestion implementation unchanged — no flag-transition hook (Task 2 case 6; PATTERNS.md non-modification rule)
6. No new event types introduced (Task 2 case 7)
7. typecheck green; existing service tests green
</success_criteria>

<output>
Create `.planning/phases/47-filter-redesign-off-topic-malicious-prompt-prevention/47-05-SUMMARY.md` when done. Summary should record: the diff size in question.service.ts (lines added/removed), the new ServiceResult error code BLOCKED_MALICIOUS, the 7 source-reading test cases, the exact line where filterQuestion is now called, and confirmation that patchQuestion remains pure persistence.
</output>
