---
phase: 52-podcast-quality-defaults-and-learner-controls
plan: 03
type: execute
wave: 2
depends_on:
  - 52-02
files_modified:
  - app/src/screens/PodcastScreen.tsx
  - app/src/screens/settings/SettingsFeaturesScreen.tsx
  - app/src/screens/settings/SettingsAIScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
autonomous: true
requirements:
  - PODCAST-02
  - PODCAST-03
  - PODCAST-05

must_haves:
  truths:
    - "PodcastScreen renders a Length chip group (Brief / Standard / Deep / Extended) and a Style chip group (Focused / Conversational / Review Drill) before the Generate button (PODCAST-02, D-01, D-02)."
    - "Chip selections initialize from settings.podcast.defaultLength ?? 'standard' and settings.podcast.defaultStyle ?? 'conversational' (D-11, D-14)."
    - "Tapping Generate passes the selected { length, style } object as the third argument to generatePodcast (PODCAST-02)."
    - "When a ready podcast exists, the player card shows an inline 'Cached: {length} · {style}' badge via podcast.player.optionsBadge (D-06)."
    - "When the chip selection's optionsHash differs from the cached podcast's optionsHash, an explicit 'Regenerate with new options' button appears below the player card (D-04). Tapping it discards script+audio (PODCAST-03 via the service-layer cache-mismatch path from Plan 52-02) and triggers regeneration."
    - "Player card has three playback-rate buttons (1x / 1.5x / 2x) that set audioRef.current.playbackRate without calling the provider or recomputing optionsHash (D-08)."
    - "SettingsFeaturesScreen Podcast section gains two SelectInput rows: Default Podcast Length and Default Podcast Style. Saving them persists into PodcastSettings (D-11)."
    - "SettingsAIScreen TTS section gains a TTS Model SelectInput (Standard tts-1 / HD tts-1-hd) shown only when tts.provider === 'openai' (D-07)."
    - "Every new UI string lands in all four locale bundles (en/zh/es/ja) in this same plan (CLAUDE.md i18n workflow + bundle-parity.test.mjs)."
    - "No new event types — PodcastScreen reacts to existing PODCAST_GENERATION_COMPLETED via the usePodcast hook (no-refresh rule)."
  artifacts:
    - path: "app/src/screens/PodcastScreen.tsx"
      provides: "Chip selectors + dirty badge + Regenerate button + playback-rate buttons"
      contains: "podcast.options.lengthLabel"
    - path: "app/src/screens/settings/SettingsFeaturesScreen.tsx"
      provides: "Default length/style SelectInputs in Podcast section"
    - path: "app/src/screens/settings/SettingsAIScreen.tsx"
      provides: "TTS Model SelectInput (OpenAI provider only)"
    - path: "app/src/locales/en.json"
      provides: "podcast.options.* + podcast.player.optionsBadge + settings.fields.podcast*/ttsModel*"
    - path: "app/src/locales/zh.json"
      provides: "Same key set as en.json, translated"
    - path: "app/src/locales/es.json"
      provides: "Same key set as en.json, translated"
    - path: "app/src/locales/ja.json"
      provides: "Same key set as en.json, translated"
  key_links:
    - from: "app/src/screens/PodcastScreen.tsx"
      to: "app/src/services/podcast-prompt.ts"
      via: "computeOptionsHash import for dirty-state derivation"
      pattern: "computeOptionsHash"
    - from: "app/src/screens/PodcastScreen.tsx"
      to: "app/src/state/usePodcast.ts"
      via: "generatePodcast call passes { length, style }"
      pattern: "generatePodcast\\([^)]*\\{\\s*length"
    - from: "app/src/screens/PodcastScreen.tsx"
      to: "HTML5 <audio>"
      via: "audioRef.current.playbackRate = rate"
      pattern: "audioRef\\.current\\.playbackRate\\s*="
    - from: "app/src/screens/settings/SettingsFeaturesScreen.tsx"
      to: "settingsService.set('podcast', ...)"
      via: "save handler extension"
      pattern: "defaultLength: podcastDefaultLength"
---

<objective>
Wire all Phase 52 UI surfaces and ship the i18n bundles. After this plan, the user can select Length × Style before generation, see a dirty badge + Regenerate button when chip selection diverges from cached options, change playback rate locally, set defaults in SettingsFeaturesScreen, and pick a TTS model in SettingsAIScreen — all in four locales.

Purpose: Service layer (Wave 1) already supports the option flow. This wave is presentation wiring + the i18n bundle parity that the operator requires in the same PR.

Output:
- `PodcastScreen.tsx` chip selectors for Length (4 chips) and Style (3 chips) shown above the Generate button when no podcast exists for today OR the cached podcast is pending/failed.
- Inline "Cached: {length} · {style}" badge under the player title when a ready podcast exists (D-06).
- Explicit "Regenerate with new options" button below the player card when chip selection ≠ cached options (D-04). Tapping calls `generatePodcast(date, conceptIds, { length, style })` — service-layer cache-mismatch logic from Plan 52-02 discards script + audio and re-runs.
- YouTube-style playback-rate cycle button (1x → 1.5x → 2x → 1x) wired to `audioRef.current.playbackRate` (D-08).
- `SettingsFeaturesScreen.tsx` gains two SelectInputs in the Podcast section: Default Podcast Length, Default Podcast Style. Save handler persists them.
- `SettingsAIScreen.tsx` TTS section gains a Model SelectInput (Standard / HD) gated on `tts.provider === 'openai'`. Default stays `tts-1` (D-10).
- Four locale bundles updated additively with new `podcast.options.*`, `podcast.player.optionsBadge`, `settings.fields.podcastDefaultLength/podcastDefaultStyle/ttsModel/ttsModelStandard/ttsModelHd`, plus matching `settings.descriptions` entries.

When this plan lands:
- `tests/screens/PodcastScreen.options.test.mjs` turns fully GREEN.
- `tests/locales/bundle-parity.test.mjs` continues to PASS (all four bundles gain the same key set).
- `tests/locales/missing-key.test.mjs` continues to PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-RESEARCH.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UI-SPEC.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-01-SUMMARY.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-02-SUMMARY.md
@CLAUDE.md
@app/src/screens/PodcastScreen.tsx
@app/src/screens/settings/SettingsFeaturesScreen.tsx
@app/src/screens/settings/SettingsAIScreen.tsx
@app/src/screens/settings/SettingsShared.tsx
@app/src/locales/en.json
@app/scripts/translate-locales.md

<interfaces>
<!-- Existing surfaces this plan consumes. -->

From app/src/state/usePodcast.ts (Plan 52-02):
- generatePodcast: (date: string, conceptIds?: string[], options?: PodcastOptions) => Promise<void>

From app/src/services/podcast-prompt.ts (Plan 52-01):
- computeOptionsHash(conceptIds: string[], locale: SupportedLocale, options: PodcastOptions): string

From app/src/screens/settings/SettingsShared.tsx (existing):
- export { SectionHeader, SettingRow, MaterialSwitch, TextInput, SelectInput, SUB_SCREEN_STYLE }
- SelectInput props: value, onChange(v), options: { value, label }[]

From app/src/lib/i18n-leaf.ts:
- export function getCurrentLocale(): string

Operator-locked copy from 52-UI-SPEC.md Copywriting Contract:
- Length: "Brief", "Standard", "Deep", "Extended"
- Style: "Focused", "Conversational", "Review Drill"
- Badge: "Cached: {length} · {style}"  (placeholders: {{length}}, {{style}})
- Primary CTA: "Regenerate with new options"
- Settings field labels: "Default Podcast Length", "Default Podcast Style", "TTS Model"

Audio element wiring (existing PodcastScreen.tsx):
- audioRef = useRef<HTMLAudioElement | null>(null) at line 40
- audioRef.current set at lines 172, 187, 195
- audio play/seek already implemented at lines 201-235
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add podcast.options + settings.fields/descriptions i18n keys to all four locale bundles (en canonical + zh/es/ja via translate-locales subagent)</name>
  <files>app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json</files>
  <read_first>
    - app/src/locales/en.json (lines 618-673 — existing podcast.* namespace shape, especially player object structure and settings.fields/descriptions blocks at lines 396-468)
    - app/src/locales/zh.json (same line range, to find insertion points)
    - app/src/locales/es.json (same)
    - app/src/locales/ja.json (same)
    - app/scripts/translate-locales.md (the operator-blessed prompt for the Sonnet translator subagent)
    - CLAUDE.md (§"i18n Workflow" — every UI string in all 4 bundles same PR; bundle-parity test enforced; proper nouns "OpenAI", "tts-1", "tts-1-hd" stay English)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UI-SPEC.md (Copywriting Contract — exact English strings)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/src/locales/{en,zh,es,ja}.json")
  </read_first>
  <behavior>
    - All four bundles gain identical key sets (`bundle-parity.test.mjs` enforces structural parity).
    - Canonical English strings come from the UI-SPEC Copywriting Contract.
    - Proper nouns NOT translated: "tts-1", "tts-1-hd", "OpenAI".
    - Each new key is a non-empty string.
    - `podcast.player.optionsBadge` value contains both `{{length}}` and `{{style}}` interpolation placeholders.
    - `tests/screens/PodcastScreen.options.test.mjs` locale-parity describe block runs green after this task lands (provided PodcastScreen.tsx source-reads still red at this point — that's Task 2/3).
    - `tests/locales/bundle-parity.test.mjs` continues to pass.
    - `tests/locales/missing-key.test.mjs` continues to pass.
  </behavior>
  <action>
    Step 1 — Add the new keys to `app/src/locales/en.json` (canonical, hand-author):

    Inside the existing `podcast` object (the one starting at line 618), add a new sub-object `options` after the existing `toast` object (around line 671 — insert before the closing `}` of `podcast`):

    ```
    "options": {
      "lengthLabel": "Length",
      "brief": "Brief",
      "standard": "Standard",
      "deep": "Deep",
      "extended": "Extended",
      "styleLabel": "Style",
      "focused": "Focused",
      "conversational": "Conversational",
      "review": "Review Drill",
      "regenerateWithNew": "Regenerate with new options"
    }
    ```

    Inside the existing `podcast.player` object (find the existing `player` block — look for `"podcastTitle"` near line 61 or similar), append two new keys:
    - `"optionsBadge": "Cached: {{length}} · {{style}}"`
    - `"playbackRateLabel": "Speed {{rate}}x"`

    Inside the existing `settings.fields` object (search for `"sleepTime"` near line 418 — adjacent group), append:
    - `"podcastDefaultLength": "Default Podcast Length"`
    - `"podcastDefaultStyle": "Default Podcast Style"`
    - `"ttsModel": "TTS Model"`
    - `"ttsModelStandard": "Standard (tts-1)"`
    - `"ttsModelHd": "HD (tts-1-hd)"`

    Inside the existing `settings.descriptions` object, append:
    - `"podcastDefaultLength": "Length used when auto-generating"`
    - `"podcastDefaultStyle": "Style used when auto-generating"`
    - `"ttsModel": "Higher quality costs more; standard works on all OpenAI endpoints"`

    Step 2 — Translate to zh/es/ja using the Sonnet translator subagent.

    Follow `app/scripts/translate-locales.md` literally. Run the subagent THREE times (once per non-EN locale), feeding it the canonical en.json additions. For each output:
    - DO NOT translate: "OpenAI", "tts-1", "tts-1-hd", `{{length}}`, `{{style}}`, `{{rate}}`.
    - DO translate: all other strings ("Length", "Brief", "Standard", "Deep", "Extended", "Style", "Focused", "Conversational", "Review Drill", "Regenerate with new options", "Cached: ...", "Speed ...", "Default Podcast Length", "Default Podcast Style", "TTS Model", "Standard (tts-1)" → keep "tts-1" but translate "Standard", "HD (tts-1-hd)" → same pattern, "Length used when auto-generating", "Style used when auto-generating", "Higher quality costs more; standard works on all OpenAI endpoints").
    - Use the existing tone of each bundle (e.g., zh.json uses Simplified Chinese with no honorifics; ja.json uses polite plain form).
    - Keep the badge placeholders `{{length}}` and `{{style}}` exactly intact.

    Human-review the translations briefly (Spanish runs ~20% longer; Japanese chip labels must fit narrow UI — if a label exceeds ~6 Japanese characters, abbreviate).

    Step 3 — Write the same key set into zh.json, es.json, ja.json at the same JSON paths (`podcast.options.*`, `podcast.player.optionsBadge/playbackRateLabel`, `settings.fields.*`, `settings.descriptions.*`).

    Do NOT introduce any new top-level namespace. Do NOT rename existing keys. Do NOT remove any existing key. Do NOT add a fifth locale.

    Do NOT bypass the translator subagent and write zh/es/ja by hand — operator memory `feedback_i18n_translation` requires Sonnet-translated bundles.

    Do NOT call any LLM at runtime to translate UI strings (CLAUDE.md: runtime LLM translation is PROHIBITED).
  </action>
  <verify>
    <automated>cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/locales/bundle-parity.test.mjs` exits 0.
    - `cd app && node --test tests/locales/missing-key.test.mjs` exits 0.
    - For each locale L in {en, zh, es, ja}: `node -e "const b = require('./app/src/locales/${L}.json'); console.log(JSON.stringify(b.podcast.options));"` prints an object containing keys `lengthLabel, brief, standard, deep, extended, styleLabel, focused, conversational, review, regenerateWithNew` with non-empty string values.
    - For each locale L: `node -e "const b = require('./app/src/locales/${L}.json'); console.log(b.podcast.player.optionsBadge);"` prints a string containing both `{{length}}` and `{{style}}`.
    - For each locale L: `node -e "const b = require('./app/src/locales/${L}.json'); ['podcastDefaultLength','podcastDefaultStyle','ttsModel','ttsModelStandard','ttsModelHd'].forEach(k => console.log(k, b.settings.fields[k]));"` shows all five keys with non-empty strings.
    - `grep -E "OpenAI" app/src/locales/zh.json app/src/locales/es.json app/src/locales/ja.json` returns at least one match per locale (proper noun preserved in description string).
    - `grep -E "tts-1-hd" app/src/locales/zh.json app/src/locales/es.json app/src/locales/ja.json` returns matches (proper noun preserved).
    - The locale-parity describe block in `cd app && node --test tests/screens/PodcastScreen.options.test.mjs 2>&1 | grep -A2 "Locale bundles carry"` shows passing assertions (PodcastScreen source-reads in this test will still fail until Task 2 — that's expected).
  </acceptance_criteria>
  <done>All four bundles parallel; bundle-parity test green; proper nouns preserved; locale-parity portion of screen test green ahead of UI wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PodcastScreen chip selectors + dirty badge + Regenerate button + playback-rate buttons</name>
  <files>app/src/screens/PodcastScreen.tsx</files>
  <read_first>
    - app/src/screens/PodcastScreen.tsx (entire file — 622 lines: header at 1-50, player card at 519-611, generate card at 580-600, no-podcast card at 620-640, audioRef at line 40, play/seek wiring at 201-235)
    - app/src/screens/settings/SettingsShared.tsx (SelectInput props pattern — for any inline import wiring decisions)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/src/screens/PodcastScreen.tsx")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-04, D-06, D-08, D-11, D-12, D-14)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UI-SPEC.md (Color tokens, Spacing, Typography)
    - CLAUDE.md (§Style Conventions — inline styles with CSS variables, NOT Tailwind; §Header positioning — do not add transforms; §Always-mounted screens — no extra useEffect resync needed since usePodcast hook already subscribes)
    - ~/.claude/projects/-Users-Code-EchoLearn/memory/feedback_tile_simplicity_preference.md (player card stays clean; dirty badge is a single muted-foreground line)
    - ~/.claude/projects/-Users-Code-EchoLearn/memory/feedback_no_refresh_assumption.md (event-bus reactivity; rely on usePodcast's existing subscription)
  </read_first>
  <behavior>
    - PodcastScreen imports `PodcastLength`, `PodcastStyle`, `PodcastOptions` types and `computeOptionsHash` from `'../services/podcast-prompt'`.
    - Two chip-group selectors render above the Generate button when no podcast exists for today OR cached status is pending/failed.
    - Chip selections initialize from settings defaults with fallback: `useState<PodcastLength>(() => settingsService.getSync().podcast.defaultLength ?? 'standard')` and `useState<PodcastStyle>(() => settingsService.getSync().podcast.defaultStyle ?? 'conversational')`.
    - Generate button (existing at line 632) and retry-generate button (existing at line 582) both pass `{ length: selectedLength, style: selectedStyle }` as the third arg.
    - When a `ready` podcast exists, the player card shows:
      - An inline badge using `t('podcast.player.optionsBadge', { length: t('podcast.options.' + selected.options.length), style: t('podcast.options.' + selected.options.style) })` — falls back gracefully when `selected.options` is undefined (pre-Phase-52 podcast).
      - The chip selectors are STILL rendered above (so users can change before regenerating), but in a smaller / "read-and-change" presentation (still tappable chips).
      - A "Regenerate with new options" button appears below the player card only when `isDirty === true`. Tap dispatches `generatePodcast(today(), todayConceptIds, { length: selectedLength, style: selectedStyle })`.
    - `isDirty` derives from `cachedHash !== currentHash` where `currentHash = computeOptionsHash(todayConceptIds, getCurrentLocale() as SupportedLocale, { length: selectedLength, style: selectedStyle })` (memoized via useMemo on `[todayConceptIds, selectedLength, selectedStyle]`).
    - Playback-rate button cycles 1x → 1.5x → 2x → 1x on tap. Side effect: `if (audioRef.current) audioRef.current.playbackRate = next;`. State is local React useState — NOT persisted, NOT part of optionsHash (D-08).
    - All new strings come from `t('podcast.options.*')`, `t('podcast.player.*')`.
    - No new event types added. No new useEffect resync — usePodcast already subscribes to PODCAST_GENERATION_COMPLETED at line 47, and `selected` is read from the hook's `podcasts` array so it stays reactive.
    - `tests/screens/PodcastScreen.options.test.mjs` PodcastScreen source-read invariants ALL pass.
  </behavior>
  <action>
    Edit `app/src/screens/PodcastScreen.tsx`:

    Step 1 — Imports near top. Add:
    - `import { useMemo, useState } from 'react';` (or add `useMemo` to the existing react import if useState already there)
    - `import type { PodcastLength, PodcastStyle, PodcastOptions, SupportedLocale } from '../types';`
    - `import { computeOptionsHash } from '../services/podcast-prompt';`
    - `import { getCurrentLocale } from '../lib/i18n-leaf';`
    - Ensure `settingsService` is imported (it likely already is — verify before re-adding).

    Step 2 — Inside the component body, near the existing useState declarations (look for the existing `useState`/`useRef` block around lines 30-50), add:
    - `const [selectedLength, setSelectedLength] = useState<PodcastLength>(() => settingsService.getSync().podcast.defaultLength ?? 'standard');`
    - `const [selectedStyle, setSelectedStyle] = useState<PodcastStyle>(() => settingsService.getSync().podcast.defaultStyle ?? 'conversational');`
    - `const [playbackRate, setPlaybackRate] = useState<1 | 1.5 | 2>(1);`

    Step 3 — Derive the dirty state. After the line where `selected` (today's podcast) is resolved from the hook, add:
    - `const cachedHash = selected?.optionsHash;`
    - `const todayConceptIds = todayConcepts.map((c) => c.id);` (reuse existing variable if already declared)
    - `const currentHash = useMemo(() => computeOptionsHash(todayConceptIds, getCurrentLocale() as SupportedLocale, { length: selectedLength, style: selectedStyle }), [todayConceptIds.join(','), selectedLength, selectedStyle]);`
    - `const isDirty = !!cachedHash && cachedHash !== currentHash;`

    Step 4 — Render the chip selectors. Insert a new JSX block ABOVE the existing Generate button (which is around line 580-600 for retry and around 620-640 for no-podcast). The chips render in two horizontal rows; one row per group:
    - First row: label `{t('podcast.options.lengthLabel')}` + four chip buttons mapping over `['brief','standard','deep','extended'] as const`. Each chip has inline style with `backgroundColor: selectedLength === l ? 'var(--primary-40)' : 'var(--surface-variant)'`, `color: selectedLength === l ? 'white' : 'var(--foreground)'`, `border: '1px solid var(--border)'`, `borderRadius: 'var(--radius-pill)'`, `padding: '6px 12px'`, `fontSize: '0.875rem'`, `fontWeight: 600`, `cursor: 'pointer'`. Label is `{t(\`podcast.options.${l}\`)}`. onClick sets `setSelectedLength(l)`.
    - Second row: label `{t('podcast.options.styleLabel')}` + three chip buttons for `['focused','conversational','review'] as const`. Same chip style. Label is `{t(\`podcast.options.${s}\`)}`.

    Step 5 — On the player card (around line 519-611), insert the dirty badge below the player title using inline style `color: 'var(--muted-foreground)'`, `fontSize: '0.85rem'`. Conditional: render only when `selected?.options` is defined. Use t() with interpolation:
    - `t('podcast.player.optionsBadge', { length: t(\`podcast.options.${selected.options.length}\`), style: t(\`podcast.options.${selected.options.style}\`) })`

    Step 6 — Insert the playback-rate button next to (or below) the existing play/seek button group (find the seek-back / play-pause / seek-forward buttons around line 537-548). The button has min-width 44px, height 44px, `borderRadius: 'var(--radius-pill)'`, `backgroundColor: 'transparent'`, `color: 'var(--foreground)'`, `border: '1px solid var(--border)'`, `fontSize: '0.78rem'`, `fontWeight: 600`. Label is `${playbackRate}x` (literal number + 'x'). onClick:
    - `const next: 1 | 1.5 | 2 = playbackRate === 1 ? 1.5 : playbackRate === 1.5 ? 2 : 1;`
    - `setPlaybackRate(next);`
    - `if (audioRef.current) audioRef.current.playbackRate = next;`

    Also, immediately AFTER `audioRef.current = audio;` lines (171-172, 187, 195 where the ref is set), add `audio.playbackRate = playbackRate;` so the rate persists when a new audio element is mounted (the user's current rate carries across podcasts).

    Step 7 — Show the "Regenerate with new options" button below the player card when `isDirty`. Place it after the playback-rate button area in the player card footer. Button uses the existing `Button` component (or matches the existing CTA style — see lines 632-635 for the no-podcast Generate button). Label: `{t('podcast.options.regenerateWithNew')}`. onClick: `generatePodcast(selected.date, todayConceptIds, { length: selectedLength, style: selectedStyle })`.

    Step 8 — Update the existing generate button call site at line 632 (no-podcast Generate) to pass options as the third arg:
    - Before: `generatePodcast(today(), todayConcepts.map((c) => c.id))`
    - After: `generatePodcast(today(), todayConcepts.map((c) => c.id), { length: selectedLength, style: selectedStyle })`

    And the retry-generate at line 582:
    - Before: `generatePodcast(selected.date, todayConcepts.map((c) => c.id))`
    - After: `generatePodcast(selected.date, todayConcepts.map((c) => c.id), { length: selectedLength, style: selectedStyle })`

    Do NOT add a new event subscription. usePodcast already handles PODCAST_GENERATION_COMPLETED; `selected.options` and `selected.optionsHash` flow through naturally.
    Do NOT add a "Save as default?" modal or prompt (D-12 — chip changes are ephemeral; persistence happens only via SettingsFeaturesScreen).
    Do NOT add a modal confirmation before regeneration (D-04 — the explicit button IS the confirmation).
    Do NOT add Tailwind classes — use inline styles with CSS variables (CLAUDE.md Style Conventions).
    Do NOT add any new event type.
    Do NOT include playbackRate in the optionsHash (D-08).
    Do NOT add transform/will-change/filter to any ancestor of the Header (CLAUDE.md Header positioning rule — the Header at the top of PodcastScreen is portal-aware).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -10 && cd app && node --test tests/screens/PodcastScreen.options.test.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/screens/PodcastScreen.options.test.mjs` exits 0 (all source-read + locale-parity assertions green).
    - `grep -E "podcast\.options\.lengthLabel" app/src/screens/PodcastScreen.tsx` returns at least one match.
    - `grep -E "podcast\.options\.styleLabel" app/src/screens/PodcastScreen.tsx` returns at least one match.
    - `grep -E "settings\.podcast\.defaultLength\s*\?\?\s*['\"]standard['\"]" app/src/screens/PodcastScreen.tsx` returns the chip initialization line.
    - `grep -E "settings\.podcast\.defaultStyle\s*\?\?\s*['\"]conversational['\"]" app/src/screens/PodcastScreen.tsx` returns the style fallback.
    - `grep -E "generatePodcast\([^)]*\{\s*length[^}]*style[^}]*\}\)" app/src/screens/PodcastScreen.tsx` returns ≥2 matches (no-podcast Generate + retry; Regenerate may match too).
    - `grep -E "podcast\.options\.regenerateWithNew" app/src/screens/PodcastScreen.tsx` returns at least one match.
    - `grep -E "podcast\.player\.optionsBadge" app/src/screens/PodcastScreen.tsx` returns at least one match.
    - `grep -E "audioRef\.current\.playbackRate\s*=" app/src/screens/PodcastScreen.tsx` returns ≥2 matches (on the cycle button AND on the mount-set side effect).
    - `grep -c "Save as default" app/src/screens/PodcastScreen.tsx` returns 0 (D-12 — no save-as-default modal).
    - `grep -cE "transform:|willChange|filter:" app/src/screens/PodcastScreen.tsx` does not increase from baseline (CLAUDE.md Header positioning rule).
    - `cd app && npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>PodcastScreen chips, dirty badge, Regenerate button, and playback-rate cycle all wired with inline-style CSS-variable styling; the screen test goes fully green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add default Length/Style SelectInputs to SettingsFeaturesScreen + TTS Model SelectInput to SettingsAIScreen</name>
  <files>app/src/screens/settings/SettingsFeaturesScreen.tsx, app/src/screens/settings/SettingsAIScreen.tsx</files>
  <read_first>
    - app/src/screens/settings/SettingsFeaturesScreen.tsx (entire file — Podcast section at lines 53-88, state declarations at 19-21, save handler at 71-82)
    - app/src/screens/settings/SettingsAIScreen.tsx (TTS section at lines 342-411, voice SelectInput pattern at 389-404, saveTts at 61-70)
    - app/src/screens/settings/SettingsShared.tsx (SettingRow, SelectInput, SectionHeader exports + props)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (sections "SettingsFeaturesScreen.tsx" + "SettingsAIScreen.tsx")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-07, D-09, D-11, D-12)
  </read_first>
  <behavior>
    - SettingsFeaturesScreen Podcast section gains two new `<SettingRow>` rows BEFORE the save Button, each containing a `<SelectInput>`:
      - Default Podcast Length: options brief/standard/deep/extended.
      - Default Podcast Style: options focused/conversational/review.
    - Local state initializes with `?? 'standard'` and `?? 'conversational'` fallbacks (D-14).
    - Save handler (existing `settingsService.set('podcast', {...})` call at lines 71-82) extends the payload object with `defaultLength: podcastDefaultLength` and `defaultStyle: podcastDefaultStyle`.
    - SettingsAIScreen TTS section gains one `<SettingRow>` containing a `<SelectInput>` for Model (Standard / HD). Render only when `tts.provider === 'openai'`. Default value when `tts.model` undefined is `'tts-1'` (D-07, D-10).
    - The Model SelectInput's onChange calls `saveTts(next)` immediately (matches the existing voice SelectInput pattern at SettingsAIScreen.tsx:389-404 which uses the same instant-save shape — operator UX preference per Phase 27 sub-screen convention).
    - All strings localized via `t('settings.fields.*')` and `t('settings.descriptions.*')` (already added in Task 1).
    - No new event types. No new types. No new service signatures.
    - Existing podcastSaved / podcastSaveFailed toast paths remain unchanged.
  </behavior>
  <action>
    Edit `app/src/screens/settings/SettingsFeaturesScreen.tsx`:

    Step 1 — Imports. Extend the existing `SettingsShared` import to include `SelectInput` if not already present:
    - `import { SectionHeader, SettingRow, MaterialSwitch, TextInput, SelectInput, SUB_SCREEN_STYLE } from './SettingsShared';`
    - Add type import: `import type { PodcastLength, PodcastStyle } from '../../types';`

    Step 2 — After the existing useState declarations (around line 21), add:
    - `const [podcastDefaultLength, setPodcastDefaultLength] = useState<PodcastLength>(() => settingsService.getSync().podcast.defaultLength ?? 'standard');`
    - `const [podcastDefaultStyle, setPodcastDefaultStyle] = useState<PodcastStyle>(() => settingsService.getSync().podcast.defaultStyle ?? 'conversational');`

    Step 3 — Inside the existing `<SectionHeader icon={<Radio />}>` block (lines 53-88), AFTER the `advanceMinutes` SettingRow (line 64) and BEFORE the save Button, add two new SettingRows:

    ```
    <SettingRow label={t('settings.fields.podcastDefaultLength')} description={t('settings.descriptions.podcastDefaultLength')}>
      <SelectInput
        value={podcastDefaultLength}
        onChange={(v) => setPodcastDefaultLength(v as PodcastLength)}
        options={[
          { value: 'brief', label: t('podcast.options.brief') },
          { value: 'standard', label: t('podcast.options.standard') },
          { value: 'deep', label: t('podcast.options.deep') },
          { value: 'extended', label: t('podcast.options.extended') },
        ]}
      />
    </SettingRow>
    <SettingRow label={t('settings.fields.podcastDefaultStyle')} description={t('settings.descriptions.podcastDefaultStyle')}>
      <SelectInput
        value={podcastDefaultStyle}
        onChange={(v) => setPodcastDefaultStyle(v as PodcastStyle)}
        options={[
          { value: 'focused', label: t('podcast.options.focused') },
          { value: 'conversational', label: t('podcast.options.conversational') },
          { value: 'review', label: t('podcast.options.review') },
        ]}
      />
    </SettingRow>
    ```

    Step 4 — Extend the existing save handler (lines 71-82) — the `settingsService.set('podcast', {...})` payload object — by adding two more fields:
    - `defaultLength: podcastDefaultLength,`
    - `defaultStyle: podcastDefaultStyle,`

    Keep all existing fields (autoGenerate, sleepTime, advanceMinutes) in their existing positions.

    Edit `app/src/screens/settings/SettingsAIScreen.tsx`:

    Step 5 — No new state needed; `tts` state already exists (line 30). The model field rides on the existing `TTSConfig` object.

    Step 6 — Insert a new SettingRow AFTER the voice SelectRow (which ends around line 404) and BEFORE the save Button area. Wrap in `{tts.provider === 'openai' && (...)}` conditional (matches the existing provider-conditional render pattern in the same file):

    ```
    {tts.provider === 'openai' && (
      <SettingRow label={t('settings.fields.ttsModel')} description={t('settings.descriptions.ttsModel')}>
        <SelectInput
          value={tts.model ?? 'tts-1'}
          onChange={(v) => {
            const next = { ...tts, model: v };
            setTts(next);
            saveTts(next);
          }}
          options={[
            { value: 'tts-1', label: t('settings.fields.ttsModelStandard') },
            { value: 'tts-1-hd', label: t('settings.fields.ttsModelHd') },
          ]}
        />
      </SettingRow>
    )}
    ```

    Do NOT change the default voice (D-09 — voice is configurable but unchanged in Phase 52).
    Do NOT change the default model (D-10 — defaults stay `tts-1` until device UAT).
    Do NOT introduce instant-save for the new length/style rows in SettingsFeaturesScreen — they save via the existing "Save" button at the bottom of the Podcast card (consistent with the existing autoGenerate/sleepTime/advanceMinutes UX in that file).
    Do NOT add SelectInput rows for gptsovits provider — D-07 limits the model picker to OpenAI.
    Do NOT remove the existing voice SelectInput or test-TTS button.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -10 && cd app && node --test tests/locales/bundle-parity.test.mjs tests/screens/PodcastScreen.options.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "podcastDefaultLength" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns ≥3 matches (useState + onChange + save payload).
    - `grep -E "podcastDefaultStyle" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns ≥3 matches.
    - `grep -E "settings\.fields\.podcastDefaultLength" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns 1.
    - `grep -E "settings\.fields\.podcastDefaultStyle" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns 1.
    - `grep -E "defaultLength: podcastDefaultLength" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns 1.
    - `grep -E "defaultStyle: podcastDefaultStyle" app/src/screens/settings/SettingsFeaturesScreen.tsx` returns 1.
    - `grep -E "tts\.provider === ['\"]openai['\"]" app/src/screens/settings/SettingsAIScreen.tsx` returns ≥2 matches (one for the existing provider-conditional pattern + the new TTS Model row guard).
    - `grep -E "settings\.fields\.ttsModel\b" app/src/screens/settings/SettingsAIScreen.tsx` returns 1.
    - `grep -E "value=\{tts\.model \?\? ['\"]tts-1['\"]\}" app/src/screens/settings/SettingsAIScreen.tsx` returns 1.
    - `grep -E "tts-1-hd" app/src/screens/settings/SettingsAIScreen.tsx` returns ≥1 match.
    - `cd app && npx tsc -b --noEmit` exits 0.
    - `cd app && node --test tests/locales/bundle-parity.test.mjs tests/screens/PodcastScreen.options.test.mjs tests/services/podcast-prompt.test.mjs tests/services/podcast-options.test.mjs` exits 0.
  </acceptance_criteria>
  <done>Settings sub-screens carry the default-options + TTS-model controls; all Phase 52 tests green; bundle-parity preserved; defaults unchanged (D-09, D-10).</done>
</task>

</tasks>

<verification>
After Plan 52-03 lands the full Phase 52 acceptance posture:
- `cd app && npx tsc -b --noEmit` exits 0.
- `cd app && node --test tests/services/podcast-prompt.test.mjs tests/services/podcast-options.test.mjs tests/screens/PodcastScreen.options.test.mjs tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs tests/providers/tts-bracketing-exempt.test.mjs` exits 0.
- `cd app && npm test` exits 0 (entire suite green).
- Manual verification (queued for verify-phase + operator device UAT per D-10 and 52-VALIDATION.md Manual-Only section):
  - tts-1-hd audio on iOS + Android (PODCAST-05).
  - playbackRate 1.5x / 2x on iOS + Android (PODCAST-05).
  - Educational content quality across 4 × 3 = 12 length×style combos (PODCAST-04).
</verification>

<success_criteria>
- Length chip group renders four chips matching D-01 enum (PODCAST-02).
- Style chip group renders three chips matching D-02 enum (PODCAST-02).
- Chip initialization respects D-11 + D-14 silent fallback.
- Tapping Generate uses the selected options.
- "Cached: {length} · {style}" inline badge appears only when a ready podcast has `options` (D-06).
- "Regenerate with new options" button appears only when `isDirty` (chip selection ≠ cached optionsHash) and triggers options-aware regeneration (D-04, PODCAST-03).
- Playback-rate cycle button (1x → 1.5x → 2x → 1x) modifies `audioRef.current.playbackRate` only; no provider call, no optionsHash impact (D-08).
- SettingsFeaturesScreen carries the two SelectInput rows persisted via the existing save handler (D-11).
- SettingsAIScreen carries the TTS Model SelectInput gated on OpenAI provider (D-07); default unchanged (D-10).
- All UI strings localized in en/zh/es/ja in this same plan.
- No new event types introduced.
- No `normalize()` helper added anywhere.
- No "Save as default?" prompt anywhere (D-12).
- No modal confirmation before regeneration (D-04).
</success_criteria>

<output>
Create `.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-03-SUMMARY.md` capturing:
- PodcastScreen UI surface: chips (4 × 3), badge, Regenerate button, playback-rate cycle
- Generate call site change (3 sites)
- audioRef.current.playbackRate wiring (cycle button + mount-time sync)
- SettingsFeaturesScreen new SelectInput rows + save payload extension
- SettingsAIScreen TTS Model row (OpenAI-only, default tts-1)
- i18n key set added in all 4 locales + which keys are placeholders (badge / playbackRateLabel)
- Final test status: full Phase 52 suite green
- Outstanding manual verifications queued for 52-VERIFICATION.md (tts-1-hd device UAT, playbackRate device UAT, educational content quality across 12 combos)
</output>