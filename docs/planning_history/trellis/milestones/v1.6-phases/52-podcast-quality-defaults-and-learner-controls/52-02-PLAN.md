---
phase: 52-podcast-quality-defaults-and-learner-controls
plan: 02
type: execute
wave: 1
depends_on:
  - 52-01
files_modified:
  - app/src/services/podcast.service.ts
  - app/src/state/usePodcast.ts
  - app/src/services/scheduler.service.ts
  - app/src/providers/tts/index.ts
autonomous: true
requirements:
  - PODCAST-01
  - PODCAST-02
  - PODCAST-03
  - PODCAST-04
  - PODCAST-05

must_haves:
  truths:
    - "generatePodcast(date, conceptIds?, options?) accepts an optional third PodcastOptions parameter (D-03 default {standard, conversational} when options undefined and settings defaults undefined)."
    - "podcast.service.ts imports buildPodcastPrompt + computeOptionsHash from './podcast-prompt' and replaces the inline 90-second prompt with the leaf-module call (PODCAST-01)."
    - "Cache-skip condition now requires `existing.status === 'ready' && audioBlobUrls.has(existing.id) && existing.optionsHash === computeOptionsHash(...)` — mismatch triggers regeneration (PODCAST-03, PODCAST-04, D-05)."
    - "Completed DailyPodcast persists `options` and `optionsHash`."
    - "retryGeneration passes `pod.options` through (D-12 ephemeral overrides; the cached options ARE the regeneration target)."
    - "scheduler.service.ts:87 passes `{ length: settings.podcast.defaultLength ?? 'standard', style: settings.podcast.defaultStyle ?? 'conversational' }` (D-13 silent fallback)."
    - "usePodcast.generatePodcast signature is a pass-through accepting (date, conceptIds?, options?)."
    - "providers/tts/index.ts reads `config.model ?? 'tts-1'` instead of the hardcoded literal (PODCAST-05, D-07). Default unchanged (D-10)."
    - "addConceptToPodcast continues to invalidate via status: 'pending' — no additional code needed (R14)."
  artifacts:
    - path: "app/src/services/podcast.service.ts"
      provides: "Options-aware generation + cache invalidation"
      contains: "computeOptionsHash"
    - path: "app/src/state/usePodcast.ts"
      provides: "Hook pass-through of options"
    - path: "app/src/services/scheduler.service.ts"
      provides: "Auto-generation uses settings defaults"
    - path: "app/src/providers/tts/index.ts"
      provides: "Configurable TTS model"
  key_links:
    - from: "app/src/services/podcast.service.ts"
      to: "app/src/services/podcast-prompt.ts"
      via: "import { buildPodcastPrompt, computeOptionsHash }"
      pattern: "import \\{[^}]*buildPodcastPrompt[^}]*computeOptionsHash[^}]*\\} from ['\"]\\./podcast-prompt['\"]"
    - from: "app/src/services/scheduler.service.ts"
      to: "app/src/services/podcast.service.ts"
      via: "generatePodcast(today(), undefined, defaultOptions)"
      pattern: "generatePodcast\\(today\\(\\),\\s*undefined,\\s*"
    - from: "app/src/providers/tts/index.ts"
      to: "TTSConfig.model"
      via: "config.model ?? 'tts-1'"
      pattern: "config\\.model\\s*\\?\\?\\s*['\"]tts-1['\"]"
---

<objective>
Wire the Phase 52 contracts into the production service layer. After this plan, every code path that generates a podcast honors length/style options, caches by `optionsHash`, and reads TTS model from config — but the UI for selecting options is still missing (Plan 52-03's job).

Purpose: Establish the load-bearing service-layer behavior so Plan 52-03's UI changes are pure presentation wiring with no backend surprises.

Output:
- `podcast.service.ts:generatePodcast` accepts an `options?: PodcastOptions` third arg, resolves to `{ length: 'standard', style: 'conversational' }` via fallback chain (arg → settings → default), assembles the system+user prompt via `buildPodcastPrompt`, computes `optionsHash`, and persists both on the completed podcast.
- Cache-skip at the existing line ~155 site extends with `existing.optionsHash === computeOptionsHash(...)` equality check.
- `retryGeneration` passes through the cached podcast's `options` so retries do not silently change settings.
- `scheduler.service.ts:87` reads `settings.podcast.defaultLength` / `.defaultStyle` (with silent fallback) and passes them as third arg.
- `usePodcast.generatePodcast` accepts options and forwards them.
- `tts/index.ts:synthesize` reads `config.model ?? 'tts-1'` so the model becomes configurable without changing the default (D-10 — defaults stay `tts-1` until device UAT in a future phase).
- The `addConceptToPodcast` flow stays untouched (status flip already invalidates cache; R14 belt-and-suspenders observation).
- The duration estimate replaces `script.length / 15` with the word-count-based estimate `Math.round(wordCount / 2.5)` per Claude's-discretion note.

When this plan lands, `tests/services/podcast-options.test.mjs` turns fully GREEN (the four cross-plan source-read invariants now match).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-RESEARCH.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-01-SUMMARY.md
@CLAUDE.md
@app/src/services/podcast.service.ts
@app/src/state/usePodcast.ts
@app/src/services/scheduler.service.ts
@app/src/providers/tts/index.ts

<interfaces>
<!-- New surface from Plan 52-01 (already landed in Wave 0). -->

From app/src/services/podcast-prompt.ts (Plan 52-01):
- export function buildPodcastPrompt(conceptLines: string, options: PodcastOptions): { system: string; user: string }
- export function computeOptionsHash(conceptIds: string[], locale: SupportedLocale, options: PodcastOptions): string

From app/src/types/index.ts (Plan 52-01):
- export type PodcastLength = 'brief' | 'standard' | 'deep' | 'extended'
- export type PodcastStyle = 'focused' | 'conversational' | 'review'
- export interface PodcastOptions { length: PodcastLength; style: PodcastStyle }
- DailyPodcast { ..., options?: PodcastOptions, optionsHash?: string }
- PodcastSettings { ..., defaultLength?: PodcastLength, defaultStyle?: PodcastStyle }
- TTSConfig { ..., model?: string }

From app/src/lib/i18n-leaf.ts:
- export function getCurrentLocale(): string

Existing signatures this plan amends:
- podcast.service.ts:151 `async generatePodcast(date: string, conceptIds?: string[]): Promise<ServiceResult<DailyPodcast>>`
- podcast.service.ts:155 cache-skip: `if (existing?.status === 'ready' && audioBlobUrls.has(existing.id)) { return { success: true, data: existing }; }`
- podcast.service.ts:204-211 inline chatCompletion with hardcoded "90-second" system prompt — TO BE REPLACED.
- podcast.service.ts:280 `retryGeneration(podcastId)` currently calls `this.generatePodcast(pod.date)` — TO BE EXTENDED to pass `pod.questionIds` and `pod.options`.
- scheduler.service.ts:87 `await podcastService.generatePodcast(today());`
- usePodcast.ts:13 type signature; usePodcast.ts:80 useCallback; usePodcast.ts:84 service call.
- providers/tts/index.ts:54 `model: 'tts-1'` hardcoded literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate PodcastOptions into podcast.service.ts (signature, prompt, cache key, retry, duration)</name>
  <files>app/src/services/podcast.service.ts</files>
  <read_first>
    - app/src/services/podcast.service.ts (entire file — must understand current 342 lines: cache-skip at 155, inline prompt at 204-211, completed-object assembly at 251-260, retryGeneration at 280, addConceptToPodcast at 320-340)
    - app/src/services/podcast-prompt.ts (Plan 52-01 output — buildPodcastPrompt + computeOptionsHash signatures)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/src/services/podcast.service.ts (MODIFIED — signature, hash, cache key)")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-03, D-04, D-05, D-13, D-14)
    - app/src/lib/i18n-leaf.ts (getCurrentLocale signature)
    - app/tests/services/podcast-options.test.mjs (Plan 52-01 — target invariants this task makes green)
  </read_first>
  <behavior>
    - `podcast.service.ts` imports `buildPodcastPrompt` and `computeOptionsHash` from `'./podcast-prompt'`.
    - `podcast.service.ts` imports `getCurrentLocale` from `'../lib/i18n-leaf'` and `type { PodcastOptions, SupportedLocale }` from `'../types'`.
    - `generatePodcast` accepts an optional third `options?: PodcastOptions` parameter.
    - Inside `generatePodcast`, options resolve via the three-step fallback: caller-arg → `settingsService.getSync().podcast.defaultLength/defaultStyle` → literal `'standard'` / `'conversational'`.
    - The resolved locale is `getCurrentLocale() as SupportedLocale`.
    - `optionsHash` is computed once from `conceptIdList` (sorted by `computeOptionsHash`), locale, and resolved options.
    - Cache-skip at ~line 155 now requires status === 'ready' AND audioBlobUrls.has(...) AND existing.optionsHash === current optionsHash.
    - The inline `chatCompletion(...)` call at lines 204-211 is replaced with a `buildPodcastPrompt(questionLines, resolvedOptions)` destructure + `chatCompletion([{ role: 'system', content: system }, { role: 'user', content: user }], settings.llm, { serviceName: 'podcast' })`.
    - The completed `DailyPodcast` object now includes `options: resolvedOptions, optionsHash`.
    - Duration estimate replaces `script.length / 15` with `Math.round(script.trim().split(/\s+/).length / 2.5)` (150 wpm).
    - `retryGeneration(podcastId)` re-calls `this.generatePodcast(pod.date, pod.questionIds, pod.options)` to preserve the cached generation's options (D-12).
    - `addConceptToPodcast` is NOT modified (status flip + script clear at line 326-335 already invalidate the cache; R14 documented).
    - `tests/services/podcast-options.test.mjs` runs green after this task.
  </behavior>
  <action>
    Edit `app/src/services/podcast.service.ts`:

    Imports near line 7-8 — add:
    - `import { buildPodcastPrompt, computeOptionsHash } from './podcast-prompt';`
    - `import { getCurrentLocale } from '../lib/i18n-leaf';`
    - Extend the existing type import block: `import type { ... existing ... , PodcastOptions, SupportedLocale } from '../types';`. Keep all existing type imports.

    Method signature at line 151 — change to:
    `async generatePodcast(date: string, conceptIds?: string[], options?: PodcastOptions): Promise<ServiceResult<DailyPodcast>>`

    Immediately after the method opens and before any other logic, add the option-resolution block (use exact literal strings `'standard'` and `'conversational'` so the Wave-0 source-reads pass; per D-03 default and D-14 silent fallback):
    - Read `const settings = settingsService.getSync();` (this is already done later — pull it up if necessary; otherwise inline the read).
    - `const resolvedOptions: PodcastOptions = { length: options?.length ?? settings.podcast.defaultLength ?? 'standard', style: options?.style ?? settings.podcast.defaultStyle ?? 'conversational' };`
    - `const locale = getCurrentLocale() as SupportedLocale;`

    After resolving `conceptIdList` (the list of question IDs that drive the prompt — find the existing variable used to populate `questionIds` in the completed podcast, likely `questions.map((q) => q.id)`), compute:
    - `const optionsHash = computeOptionsHash(conceptIdList, locale, resolvedOptions);`

    Update the cache-skip at line 155 (the `if (existing?.status === 'ready' && audioBlobUrls.has(existing.id))` block) — extend the condition to also require `existing.optionsHash === optionsHash`. The final condition must exactly match the regex `/existing\?\.status\s*===\s*['"]ready['"]/` AND contain `existing.optionsHash === computeOptionsHash(` OR `existing.optionsHash === optionsHash` (Plan 52-01 source-read test allows either; prefer storing `optionsHash` in a local first, then referencing it).

    Replace the inline `chatCompletion([{ role: 'system', content: 'Write a 90-second...' }, ...])` block at lines 203-211 with:
    - `const questionLines = questions.map((q) => \`- ${q.content}: ${q.summary}\`).join('\n');`
    - `const { system, user } = buildPodcastPrompt(questionLines, resolvedOptions);`
    - `script = await chatCompletion([{ role: 'system', content: system }, { role: 'user', content: user }], settings.llm, { serviceName: 'podcast' });`

    Fix the existing.script reuse path (current line 198: `if (existing?.script) { script = existing.script; }`). When the cache-skip falls through to script generation, an existing script must NOT be reused if it was generated under different options. Replace the existing.script condition with: `if (existing?.script && existing.optionsHash === optionsHash) { script = existing.script; }`. This preserves the resume-after-TTS-failure behavior (where script was produced but audio failed) for the same-options case, while ensuring that an options change forces a fresh script generation per D-05.

    Replace the duration estimate `const duration = Math.round(script.length / 15);` with `const wordCount = script.trim().split(/\s+/).length; const duration = Math.max(1, Math.round(wordCount / 2.5));` (the `Math.max(1, ...)` floor prevents 0-second podcasts for tiny scripts).

    Update the completed-object construction (around lines 251-260) to include both new fields: `options: resolvedOptions,` and `optionsHash,`. Keep all existing fields in their existing positions.

    Update `retryGeneration` (line 280): change the call to `return this.generatePodcast(pod.date, pod.questionIds, pod.options);`.

    Do NOT modify `addConceptToPodcast` (lines 320-340). Its existing `status: 'pending'` + cleared script already invalidates the cache (R14 belt-and-suspenders).

    Do NOT remove the `serviceName: 'podcast'` option from the chatCompletion call — token tracking depends on it.

    Do NOT change the IndexedDB / `audioBlobUrls` behavior.

    Do NOT introduce a normalize() helper or a migration function.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -10 && cd app && node --test tests/services/podcast-options.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/podcast-options.test.mjs` exits 0 (all tests green — previously red cross-plan invariants now match).
    - `cd app && node --test tests/services/podcast-prompt.test.mjs` still exits 0.
    - `grep -E "generatePodcast\(date: string, conceptIds\?: string\[\], options\?: PodcastOptions" app/src/services/podcast.service.ts` returns the new signature.
    - `grep -E "import \{[^}]*buildPodcastPrompt[^}]*computeOptionsHash[^}]*\} from ['\"]\\./podcast-prompt['\"]" app/src/services/podcast.service.ts` returns the import line.
    - `grep -E "getCurrentLocale" app/src/services/podcast.service.ts` returns the locale read.
    - `grep -cE "existing\.optionsHash\s*===\s*" app/src/services/podcast.service.ts` returns ≥2 (the cache-skip equality AND the existing-script-reuse guard).
    - `grep -cE "options: resolvedOptions" app/src/services/podcast.service.ts` returns ≥1.
    - `grep -E "Math\.round\(\s*wordCount\s*/\s*2\.5\s*\)" app/src/services/podcast.service.ts` returns the new duration estimate.
    - `grep -E "this\.generatePodcast\(pod\.date,\s*pod\.questionIds,\s*pod\.options\)" app/src/services/podcast.service.ts` returns the retry call.
    - `grep -c "Write a 90-second" app/src/services/podcast.service.ts` returns 0 (old prompt removed).
    - `grep -c "script\.length / 15" app/src/services/podcast.service.ts` returns 0 (old duration removed).
    - `cd app && npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>Service layer is options-aware; podcast-options test fully green; retry preserves options; addConceptToPodcast untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass options through usePodcast hook and scheduler.service.ts default options</name>
  <files>app/src/state/usePodcast.ts, app/src/services/scheduler.service.ts</files>
  <read_first>
    - app/src/state/usePodcast.ts (entire file — 119 lines; signature at line 13, useCallback at line 80, service call at 84)
    - app/src/services/scheduler.service.ts (lines 60-95 — settings read at 65, the podcast generation call at 87)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (sections "usePodcast.ts" + "scheduler.service.ts")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-13, D-14)
    - ~/.claude/projects/-Users-Code-EchoLearn/memory/project_serverless_no_background_tasks.md (auto-gen runs only with app open)
  </read_first>
  <behavior>
    - `usePodcast` hook exposes `generatePodcast: (date: string, conceptIds?: string[], options?: PodcastOptions) => Promise<void>` (matches service signature).
    - Inside the hook, `podcastService.generatePodcast(date, conceptIds, options)` is called with the forwarded options. No transformation, no extra logic.
    - `PodcastOptions` is imported from `'../types'` at the top of the file.
    - `scheduler.service.ts:87` constructs a default-options object reading from `settings.podcast.defaultLength ?? 'standard'` and `settings.podcast.defaultStyle ?? 'conversational'`, then passes it as the third argument to `podcastService.generatePodcast(today(), undefined, defaultOptions)`.
    - `PodcastOptions` is imported as `type` in `scheduler.service.ts`.
    - The existing scheduler `settings.podcast.autoGenerate` short-circuit at line 78 is preserved.
    - Existing `PODCAST_GENERATION_*` event subscriptions in `usePodcast` work unchanged because `DailyPodcast` payload now naturally carries `options` + `optionsHash` after Plan 52-01.
  </behavior>
  <action>
    Edit `app/src/state/usePodcast.ts`:

    Imports at top (line 2 area): extend the existing `import type { ... } from '../types';` block to include `PodcastOptions`. If the import was a default + named pattern, keep it; only ADD `PodcastOptions` to the named list.

    Type signature at line 13: change `generatePodcast: (date: string, conceptIds?: string[]) => Promise<void>;` to `generatePodcast: (date: string, conceptIds?: string[], options?: PodcastOptions) => Promise<void>;`.

    useCallback at line 80: change `const generatePodcast = useCallback(async (date: string, conceptIds?: string[]) => {` to `const generatePodcast = useCallback(async (date: string, conceptIds?: string[], options?: PodcastOptions) => {`.

    Service call at line 84: change `const result = await podcastService.generatePodcast(date, conceptIds);` to `const result = await podcastService.generatePodcast(date, conceptIds, options);`. Leave everything else (setIsGenerating, error mapping, dispatch logic) unchanged.

    Do NOT modify the eventBus.subscribe blocks (lines 38-73) — they continue to work because the DailyPodcast payload already carries the new options/optionsHash fields after Plan 52-01.

    Edit `app/src/services/scheduler.service.ts`:

    Add `import type { PodcastOptions } from '../types';` near the existing type imports (search for the existing `import type` block — likely near top).

    At line 87 (the `await podcastService.generatePodcast(today());` line), construct the default-options object before the call and pass it as the third arg:
    - `const defaultOptions: PodcastOptions = { length: settings.podcast.defaultLength ?? 'standard', style: settings.podcast.defaultStyle ?? 'conversational' };`
    - `await podcastService.generatePodcast(today(), undefined, defaultOptions);`

    `settings` is already in scope at this point (it's read earlier at line 65). Do NOT re-read settings.

    Do NOT modify the `settings.podcast.autoGenerate` check at line 78 (auto-gen still requires the user setting and the app being open — memory `project_serverless_no_background_tasks`).

    Do NOT add a settings-write side effect (D-12 ephemeral overrides — scheduler reads settings, never writes them).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "generatePodcast:\s*\(date: string, conceptIds\?: string\[\], options\?: PodcastOptions\)" app/src/state/usePodcast.ts` returns the new type signature.
    - `grep -E "podcastService\.generatePodcast\(date, conceptIds, options\)" app/src/state/usePodcast.ts` returns the pass-through call.
    - `grep -E "settings\.podcast\.defaultLength\s*\?\?\s*['\"]standard['\"]" app/src/services/scheduler.service.ts` returns the default-options read.
    - `grep -E "settings\.podcast\.defaultStyle\s*\?\?\s*['\"]conversational['\"]" app/src/services/scheduler.service.ts` returns the style fallback.
    - `grep -E "generatePodcast\(today\(\),\s*undefined,\s*defaultOptions\)" app/src/services/scheduler.service.ts` returns the scheduler call.
    - `cd app && npx tsc -b --noEmit` exits 0.
    - `cd app && node --test tests/services/podcast-options.test.mjs` still exits 0.
  </acceptance_criteria>
  <done>Hook + scheduler forward options correctly. Auto-generated podcasts inherit user defaults with silent fallback to standard/conversational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Un-hardcode TTS model in providers/tts/index.ts (PODCAST-05 — config-driven, default unchanged)</name>
  <files>app/src/providers/tts/index.ts</files>
  <read_first>
    - app/src/providers/tts/index.ts (entire file — synthesize() at line 44, hardcoded model literal at line 54, LOCALE_VOICE_FALLBACK at 21-26, bracketing-exempt comment block at lines 4-12)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/src/providers/tts/index.ts (MODIFIED — un-hardcode model)")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-07, D-10)
    - app/tests/providers/tts-bracketing-exempt.test.mjs (load-bearing — must not regress)
  </read_first>
  <behavior>
    - `synthesize(text, config)` reads `config.model ?? 'tts-1'` and uses that value in the request body's `model` field.
    - The default behavior when `config.model` is undefined is unchanged: requests still go out with `model: 'tts-1'`.
    - The bracketing-exempt comment block at lines 4-12 stays intact (load-bearing — `tests/providers/tts-bracketing-exempt.test.mjs` reads it).
    - No new imports needed (TTSConfig already gained `model?: string` in Plan 52-01).
    - All other request fields (`input`, `voice`, `speed`) stay identical.
  </behavior>
  <action>
    Edit `app/src/providers/tts/index.ts` line 54:

    Replace the literal:
    - Before: `model: 'tts-1',`
    - After: `model: config.model ?? 'tts-1',`

    Do NOT modify anything else in the file. In particular:
    - Do NOT remove the bracketing-exempt comment block at lines 4-12 (load-bearing per `tests/providers/tts-bracketing-exempt.test.mjs`).
    - Do NOT change the locale → voice fallback map at lines 21-26.
    - Do NOT touch the testTTSConnection function.
    - Do NOT change the request URL or HTTP method.
    - Do NOT remove or reorder body fields.

    No new imports needed.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -10 && cd app && node --test tests/providers/tts-bracketing-exempt.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "model:\s*config\.model\s*\?\?\s*['\"]tts-1['\"]" app/src/providers/tts/index.ts` returns exactly one line.
    - `grep -c "model: 'tts-1'" app/src/providers/tts/index.ts` returns 0 (old hardcoded literal removed).
    - `cd app && node --test tests/providers/tts-bracketing-exempt.test.mjs` exits 0 (bracketing-exempt invariant preserved).
    - `cd app && npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>TTS model is configurable; default `tts-1` preserved; bracketing-exempt invariant untouched (D-10 — defaults do not change until device UAT in a future phase).</done>
</task>

</tasks>

<verification>
After Plan 52-02 lands:
- `cd app && npx tsc -b --noEmit` exits 0.
- `cd app && node --test tests/services/podcast-prompt.test.mjs tests/services/podcast-options.test.mjs` exits 0 (all green — Wave 0's red invariants now match).
- `cd app && node --test tests/screens/PodcastScreen.options.test.mjs` STILL RED (Wave 2 target — UI changes have not yet landed; locale bundles still missing `podcast.options.*`).
- `cd app && node --test tests/providers/tts-bracketing-exempt.test.mjs` exits 0 (no regression).
- `cd app && npm test` shows the Phase 52 prompt + options tests green and the screen test red — that is the expected mid-phase state.
</verification>

<success_criteria>
- generatePodcast accepts options? as third arg with three-step fallback (arg → settings → default).
- Cache-skip honors optionsHash equality; mismatch triggers regeneration discarding script + audio (D-05).
- buildPodcastPrompt replaces the hardcoded 90-second conversational radio prompt (PODCAST-01).
- retryGeneration preserves the cached podcast's options (D-12).
- Duration estimate is word-count based.
- Scheduler reads settings defaults with silent fallback to standard/conversational (D-13, D-14).
- usePodcast forwards options through to the service.
- TTS model is config-driven; default `tts-1` unchanged (D-10).
- addConceptToPodcast remains untouched — its existing status-flip path is the cache invalidation route for that flow (R14).
</success_criteria>

<output>
Create `.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-02-SUMMARY.md` capturing:
- Final generatePodcast signature with options parameter and three-step fallback
- Cache-skip extension exact shape (file + line)
- Prompt assembly call site replacement
- retryGeneration options passthrough
- Duration estimate change (wordCount / 2.5)
- Scheduler default-options block
- usePodcast pass-through one-liner
- TTS model un-hardcode location
- Confirmation that podcast-options test went green; PodcastScreen.options test still red awaiting Wave 2
</output>