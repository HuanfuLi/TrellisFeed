---
phase: 50-retrieval-and-library-foundation
plan: 07
type: execute
wave: 3
depends_on: [50-06]
files_modified:
  - app/src/components/LongPressMenu.tsx
  - app/tests/components/LongPressMenu.test.mjs
autonomous: true
requirements: [RETRIEVE-02]
threat_refs: [T-50-SHEET-FLASH]
must_haves:
  truths:
    - "LongPressMenu accepts an optional onOpenCollectionPicker prop; when provided, the Save row calls it instead of toggling engagementService directly"
    - "Save row visual is byte-stable: same Bookmark icon, same isSaved-driven fill, same Save/Unsave label semantics (per UI-SPEC §Surface 9)"
    - "LongPressMenu accepts an optional collectionContext prop; when provided, a new Remove-from-collection row appears between Save and Not interested"
    - "Save row tap sequence sets picker state FIRST, then calls onClose() (RESEARCH Pitfall 4 — sheet-flash prevention)"
    - "When onOpenCollectionPicker is NOT provided (graceful degradation), Save row uses the existing direct-toggle path so no surface breaks"
  artifacts:
    - path: app/src/components/LongPressMenu.tsx
      provides: "Extended LongPressMenu API: onOpenCollectionPicker + collectionContext props + Remove-from-collection row"
      contains: "onOpenCollectionPicker"
  key_links:
    - from: app/src/components/LongPressMenu.tsx
      to: app/src/components/CollectionPickerSheet.tsx
      via: "onOpenCollectionPicker callback wired by host (HomeScreen / CollectionDrillInScreen) — opens picker with the post id"
      pattern: "onOpenCollectionPicker"
    - from: app/src/components/LongPressMenu.tsx
      to: app/src/services/collection.service.ts
      via: "Remove-from-collection row calls collectionService.removePost(collectionContext.collectionId, postId)"
      pattern: "collectionService\\.removePost"
---

<objective>
Extend the existing `LongPressMenu` API with two optional props: `onOpenCollectionPicker?: (postId: string) => void` (Save row behavior change per D-04 — opens picker instead of direct toggle) and `collectionContext?: { collectionId: string; collectionName: string }` (drill-in context — adds Remove-from-collection row). Preserve the byte-stable visual contract and the graceful-degradation path so any surface not yet wired to the picker keeps working unchanged.

Purpose: This plan is intentionally narrow — it only modifies the LongPressMenu component contract. Host wiring (HomeScreen + CollectionDrillInScreen state lifting) happens in plans 50-08 and 50-09 respectively. Isolating the component change here makes the contract change deterministic and the existing LongPressMenu test (`tests/components/LongPressMenu.test.mjs`) the single source of behavioral truth.

Output: Updated LongPressMenu.tsx + updated LongPressMenu.test.mjs covering the new props and the existing rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md
@.planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md
@.planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md
@.planning/phases/50-retrieval-and-library-foundation/50-06-SUMMARY.md
@app/src/components/LongPressMenu.tsx

<interfaces>
<!-- Current LongPressMenu props (app/src/components/LongPressMenu.tsx:7-12): -->

interface LongPressMenuProps {
  open: boolean;
  onClose: () => void;
  postId: string | null;
  anchorId: string | null;
}

<!-- Extended Phase 50 props (UI-SPEC §Surface 9 + PATTERNS §LongPressMenu modify): -->

interface LongPressMenuProps {
  open: boolean;
  onClose: () => void;
  postId: string | null;
  anchorId: string | null;
  onOpenCollectionPicker?: (postId: string) => void;                          // NEW — D-04
  collectionContext?: { collectionId: string; collectionName: string };        // NEW — drill-in context
}

<!-- Save row behavior change (RESEARCH Pitfall 4 ordering): -->

const handleSave = () => {
  if (onOpenCollectionPicker && postId) {
    onOpenCollectionPicker(postId);   // set picker state FIRST
    onClose();                         // then close LongPressMenu (React 19 batches both in one render cycle)
  } else {
    // Graceful degradation: existing direct-toggle path preserved for surfaces not yet wired to picker
    if (isSaved) { engagementService.removeSavedPost(postId); toast(t('engagement.toast.unsaved'), 'info'); }
    else         { engagementService.savePost(postId); toast(t('engagement.toast.saved'), 'success'); }
    onClose();
  }
};

<!-- New Remove-from-collection row (only when collectionContext provided) — inserted between Save and Not interested: -->

{collectionContext && (
  <button type="button" style={rowStyle} onClick={handleRemoveFromCollection}>
    <FolderMinus size={22} color="var(--foreground)" fill="none" />
    <span>{t('library.collections.removeFromCollection')}</span>
  </button>
)}

const handleRemoveFromCollection = () => {
  if (!postId || !collectionContext) return;
  collectionService.removePost(collectionContext.collectionId, postId);
  toast(
    t('library.collections.toast.removed', { collection: collectionContext.collectionName }),
    'info',
    { action: { label: t('common.undo'), onAction: () => {
      collectionService.addPost(collectionContext.collectionId, postId);
    } } }
  );
  onClose();
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LongPressMenu with onOpenCollectionPicker + collectionContext props (+ tests)</name>
  <files>app/src/components/LongPressMenu.tsx, app/tests/components/LongPressMenu.test.mjs</files>
  <read_first>
    - app/src/components/LongPressMenu.tsx (full file — interface at lines 7-12; rowStyle at 87-103; handleSave at 59-68; render JSX at 105-141)
    - app/tests/components/LongPressMenu.test.mjs (full file — existing source-reading test pattern + anti-wire assertions; new assertions must NOT break existing ones)
    - .planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md §"Surface 9 — LongPressMenu extension"
    - .planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md §"app/src/components/LongPressMenu.tsx — Save row behavior change"
    - .planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md §"Pitfall 4" (sheet flash; state setter ordering)
    - .planning/phases/50-retrieval-and-library-foundation/50-06-SUMMARY.md (CollectionPickerSheet prop signature)
    - app/src/services/collection.service.ts (from 50-03 — addPost + removePost signatures)
    - app/src/lib/toast.ts (3rd-arg action signature)
    - app/src/locales/en.json (verify keys `library.collections.removeFromCollection`, `library.collections.toast.removed`, `common.undo` exist — added in 50-02)
  </read_first>
  <behavior>
    - Existing 3 rows (Like / Save / Not interested) still render unchanged when only postId + anchorId are provided (no onOpenCollectionPicker, no collectionContext)
    - Save row visual UNCHANGED: Bookmark icon with fill driven by isSaved; label toggles between Save/Unsave based on isSaved
    - When onOpenCollectionPicker is provided and Save row is tapped: calls onOpenCollectionPicker(postId) FIRST, then onClose() — no direct engagementService.savePost call in that branch
    - When onOpenCollectionPicker is NOT provided: Save row still toggles engagement directly + fires toast (existing path preserved)
    - When collectionContext is provided, a 4th row "Remove from collection" appears between Save and Not interested — uses FolderMinus icon, color var(--foreground), label t('library.collections.removeFromCollection')
    - When collectionContext is NOT provided, no Remove row renders (existing 3-row layout)
    - Tapping Remove row: collectionService.removePost(collectionContext.collectionId, postId) + toast with Undo action that calls collectionService.addPost on tap
    - Existing anti-wire invariants still hold: NO direct event-bus emit; NO direct dailyRead service call; NO explored-anchor event reference
  </behavior>
  <action>
1. Modify `app/src/components/LongPressMenu.tsx`:
   a. Extend props interface (lines 7-12) with two NEW optional fields: `onOpenCollectionPicker?: (postId: string) => void;` and `collectionContext?: { collectionId: string; collectionName: string };`. Place ordering: open, onClose, postId, anchorId, onOpenCollectionPicker, collectionContext.
   b. Add imports: `FolderMinus` to the existing lucide-react import line; `collectionService` from `../services/collection.service`.
   c. Update component destructure: `function LongPressMenu({ open, onClose, postId, anchorId, onOpenCollectionPicker, collectionContext })`.
   d. Replace handleSave (lines 59-68) with the branched logic per `<interfaces>` block above. Key ordering: when picker path is taken, call `onOpenCollectionPicker(postId)` BEFORE `onClose()` (Pitfall 4).
   e. Add `handleRemoveFromCollection` function before the JSX return.
   f. Insert the conditional `{collectionContext && <button ...>}` row in the JSX between the Save row (current lines 119-126) and the Not-interested row (lines 130-137). Use the same `rowStyle` constant.
   g. Update the component file-header JSDoc to note the two new optional props + the D-04 / drill-in context use cases.

2. Update `app/tests/components/LongPressMenu.test.mjs` with NEW assertions ADDED to the existing test file (do NOT delete existing assertions):
   - Existing rows still render (Heart / Bookmark / EyeOff present).
   - Save row handler references `onOpenCollectionPicker` followed by `onClose` (assert via source-reading that `onOpenCollectionPicker(postId)` appears BEFORE `onClose()` in handleSave body).
   - Save row STILL preserves the direct-toggle fallback path: file contains both `engagementService.savePost` and `engagementService.removeSavedPost` (graceful degradation).
   - Conditional Remove row: source contains `{collectionContext && ` (JSX conditional render).
   - Remove row handler references `collectionService.removePost` AND `toast(` with an `{ action:` options argument (Undo affordance per UI-SPEC §Surface 9).
   - Anti-wire invariants preserved: source does NOT contain `eventBus.emit` calls and does NOT reference any explored-anchor event by name (carry over the existing assertions from LongPressMenu.test.mjs).
  </action>
  <verify>
    <automated>cd app && node --test tests/components/LongPressMenu.test.mjs 2>&1 | tail -15 && cd app && grep -c "onOpenCollectionPicker" src/components/LongPressMenu.tsx && grep -c "collectionContext" src/components/LongPressMenu.tsx</automated>
  </verify>
  <done>
    LongPressMenu.test.mjs passes (both existing + new assertions). Greps for `onOpenCollectionPicker` and `collectionContext` each return ≥2 (prop declaration + use site). `cd app && npx tsc -b --noEmit` clean modulo pre-existing SavedScreen errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Save row tap → sheet transition | Two sheets (LongPressMenu + CollectionPickerSheet) coordinate at the host-screen level via state updates. Wrong ordering causes a visual flash. |
| Remove-from-collection row → service write + Undo | Single-tap action with built-in Undo toast — no separate confirmation step (per UI-SPEC: non-destructive, post stays in Saved/History). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-SHEET-FLASH | Availability / UX | LongPressMenu → CollectionPickerSheet transition | mitigate | RESEARCH Pitfall 4: handleSave MUST call onOpenCollectionPicker(postId) BEFORE onClose() so React 19 batches both state updates in one render cycle — picker mounts as menu unmounts, no blank frame. Test asserts source ordering. |
| T-50-REMOVE-DESTRUCTIVE | Information Disclosure / Data Loss | Remove-from-collection row | accept | Per UI-SPEC §Color rules, Remove is NON-destructive (post stays in Saved/History, only collection membership clears). Confirmation NOT required. Undo toast affordance covers accidental taps. |
</threat_model>

<verification>
- `cd app && node --test tests/components/LongPressMenu.test.mjs` passes.
- `cd app && grep -c "onOpenCollectionPicker(postId)" src/components/LongPressMenu.tsx` returns ≥1 and that line appears BEFORE the next `onClose()` line in handleSave.
- `cd app && grep -c "FolderMinus" src/components/LongPressMenu.tsx` returns ≥1.
- `cd app && grep -c "collectionService.removePost" src/components/LongPressMenu.tsx` returns ≥1.
- `cd app && npx tsc -b --noEmit` clean modulo pre-existing SavedScreen errors.
- HomeScreen current consumer (`app/src/screens/HomeScreen.tsx:1053`) still compiles — it doesn't pass the new optional props yet, but optional means no break (verified by tsc).
</verification>

<success_criteria>
- LongPressMenu API extended with onOpenCollectionPicker + collectionContext.
- Save row visual unchanged; behavior branches on onOpenCollectionPicker presence.
- Remove-from-collection row renders only when collectionContext is provided.
- Existing 3-row surface continues to work for HomeScreen (which does not pass new props yet — wiring lands in 50-09).
- Anti-wire invariants preserved (no direct event-bus emit, no explored-anchor reference).
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-07-SUMMARY.md` when done. List the new prop signatures so plans 50-08 (CollectionDrillInScreen) and 50-09 (SavedScreen + HomeScreen wiring) can wire them without re-reading source.
</output>
