---
phase: 50-retrieval-and-library-foundation
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/library-search.service.ts
  - app/tests/services/library-search.service.test.mjs
autonomous: true
gap_closure: true
gaps_addressed: [G4]
requirements: [RETRIEVE-01]

must_haves:
  truths:
    - "Searching '3D printing' on Liked (or any) tab does NOT return an unrelated Kanji video whose body has scattered single-char overlap"
    - "Single-character queries (e.g. 'e') do NOT produce single-char highlights across every title/body containing that letter"
    - "Substring 'system' on a body containing 'system prompt' highlights the contiguous 'system' run only — not stray 's','y','s','t','e','m' runs"
    - "Body match at character position 250 STILL returns (Pitfall 1 / ignoreLocation: true preserved — no regression)"
  artifacts:
    - path: "app/src/services/library-search.service.ts"
      provides: "Tuned FUSE_OPTIONS — threshold and minMatchCharLength bands that suppress single-char fuzzy noise while preserving long-string matches"
      contains: "FUSE_OPTIONS"
  key_links:
    - from: "app/src/services/library-search.service.ts"
      to: "Fuse.js Bitap algorithm"
      via: "threshold + minMatchCharLength tuning"
      pattern: "threshold:\\s*0\\.[0-3]"
---

<objective>
Close gap G4 from 50-UAT.md Test 4.

Current `FUSE_OPTIONS` use `threshold: 0.4` and `minMatchCharLength: 2`. Operator reported: searching `3D printing` on Liked tab returns an unrelated Kanji video with scattered single-char/2-char highlights (`i R`, `di`, `T`, `nd`, `ti`, `n`, `R`, `R`, `eco`, `gniti`). The Bitap fuzzy threshold is permissive enough that sub-runs of "printing" (e.g. `eco`, `gniti`, `ti`, `nd`) and single character overlaps satisfy the score, even on bodies whose semantic content is unrelated.

Tune FUSE_OPTIONS to suppress single-char noise while preserving long-string matching (and keeping `ignoreLocation: true` — Pitfall 1, do NOT regress).

Purpose: Make the Library search feel like substring search — high-precision, low-noise.
Output: Updated FUSE_OPTIONS with tighter `threshold` and higher `minMatchCharLength`; updated test file with new regression tests proving the false-positive Kanji case fails AND the legitimate body-position-250 case still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-retrieval-and-library-foundation/50-UAT.md
@.planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md

@app/src/services/library-search.service.ts
@app/tests/services/library-search.service.test.mjs

<interfaces>
From app/src/services/library-search.service.ts:
```typescript
export const FUSE_OPTIONS: IFuseOptions<DailyPost> = {
  keys: [
    { name: 'title', weight: 0.5 },
    { name: 'bodyMarkdown', weight: 0.3 },
    { name: 'sourceQuestionTitles', weight: 0.15 },
    { name: 'contextLabel', weight: 0.05 },
  ],
  threshold: 0.4,         // CURRENT — too lenient
  ignoreLocation: true,   // LOAD-BEARING (Pitfall 1) — DO NOT REMOVE
  includeMatches: true,
  includeScore: true,
  minMatchCharLength: 2,  // CURRENT — too short for substring UX
  shouldSort: true,
};

export const MAX_QUERY_LENGTH = 200;
export function buildIndex(posts: DailyPost[]): Fuse<DailyPost>;
export function search(index: Fuse<DailyPost>, query: string): FuseResult<DailyPost>[];
export function capQuery(query: string): string;
```
</interfaces>

<tuning_band>
Per RESEARCH.md Pattern 3 + 50-UAT.md analysis:
- `threshold`: target band 0.30–0.40; lower is stricter. UAT analysis says "~0.3-0.4". 0.4 is the current value (failing). Pick a value at the lower end of the band — recommend `0.3`.
- `minMatchCharLength`: target ≥ 3. Single-char matches and 2-char fragments are the source of the Kanji false positive. Recommend `3`.
- These are empirical bands. The phase RESEARCH also notes: lower to 0.78 if missing real matches, raise to 0.85 if blocking legitimate (those are auth-classifier values, but the same principle applies here — be ready to nudge based on dogfooding).
- DO NOT lower below 0.3 (over-strict — exact substring only, defeats Fuse's typo tolerance).
- DO NOT raise above 0.4 (current value is failing).
- DO NOT remove or change `ignoreLocation: true` (CLAUDE.md / RESEARCH Pitfall 1 — body matches past pos 60 silently miss without this).
- DO NOT lower `minMatchCharLength` below 3 (point of the fix).
- DO NOT change `keys` weights (D-14 relevance ordering depends on the title > body > source ratio that 50-04 already locked).
</tuning_band>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tune FUSE_OPTIONS — tighten threshold to 0.3 and raise minMatchCharLength to 3</name>
  <files>app/src/services/library-search.service.ts</files>
  <read_first>
    - app/src/services/library-search.service.ts (current FUSE_OPTIONS — header comment block documents what is load-bearing)
    - app/tests/services/library-search.service.test.mjs (existing tests — verify the body-position-250 test exists and continues to pass with new options)
    - .planning/phases/50-retrieval-and-library-foundation/50-UAT.md (Test 4 analysis — specifies tuning band)
    - .planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md (Pattern 3 — Fuse options rationale; Pitfall 1 — ignoreLocation guardrail)
    - CLAUDE.md (no specific section but follow general guardrail — do not introduce regression of a known-load-bearing knob)
  </read_first>
  <behavior>
    - FUSE_OPTIONS.threshold becomes 0.3 (down from 0.4).
    - FUSE_OPTIONS.minMatchCharLength becomes 3 (up from 2).
    - FUSE_OPTIONS.ignoreLocation remains true.
    - FUSE_OPTIONS.keys weights and shapes unchanged.
    - File header comment block updated: the "Load-bearing rules" section adds a new entry: "FUSE_OPTIONS.threshold = 0.3 and minMatchCharLength = 3 — DO NOT raise threshold above 0.4 or drop minMatchCharLength below 3. These values were tuned in plan 50-11 (gap G4) to suppress single-char Bitap noise that surfaced an unrelated Kanji video on a '3D printing' query. Lowering threshold further (< 0.3) defeats typo tolerance; raising it reopens the false-positive."
    - The current inline comment on the threshold line ("matches Fuse's default and is intentionally tunable later. Lower = stricter; higher = more lenient.") must be updated to reflect the new value and reference the gap-closure decision.
  </behavior>
  <action>
    Edit `app/src/services/library-search.service.ts`:

    1. Change `threshold: 0.4` to `threshold: 0.3` in the FUSE_OPTIONS object literal.
    2. Change `minMatchCharLength: 2` to `minMatchCharLength: 3` in the same object.
    3. Update the JSDoc block above FUSE_OPTIONS: replace the threshold/length sentence with: "`threshold: 0.3` tightened from Fuse's 0.4 default — tuned in 50-11 to suppress Bitap fuzzy noise that surfaced unrelated posts on short queries (50-UAT.md G4). `minMatchCharLength: 3` raised from 2 for the same reason — single-char and 2-char matches drove the scattered-highlight false positive."
    4. Add a new bullet in the file header "Load-bearing rules" list (currently rules 1–3, all at the top of the file). Add rule 4: `4. FUSE_OPTIONS.threshold (0.3) + minMatchCharLength (3) are tuned values. Do NOT raise threshold above 0.4 or drop minMatchCharLength below 3 — both regressions reopen the 50-UAT G4 false-positive surface where a '3D printing' query on Liked tab matched an unrelated Kanji video via scattered single-char Bitap hits.`

    Do NOT touch: keys array, ignoreLocation, includeMatches, includeScore, shouldSort, MAX_QUERY_LENGTH, capQuery, buildIndex, search, extractSnippet, dateFilter, rebaseIndices, or the leaf-module-discipline comments. Keep the existing Pitfall 1 reference intact.

    Comment header reference in code: `// Phase 50 gap closure 50-11 (G4): threshold tightened 0.4→0.3, minMatchCharLength 2→3 to suppress single-char fuzzy noise. Pitfall 1 (ignoreLocation: true) preserved.`
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/library-search.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "threshold:\\s*0\\.3" app/src/services/library-search.service.ts` returns at least one match.
    - `grep -nE "threshold:\\s*0\\.4" app/src/services/library-search.service.ts` returns zero matches.
    - `grep -nE "minMatchCharLength:\\s*3" app/src/services/library-search.service.ts` returns at least one match.
    - `grep -nE "minMatchCharLength:\\s*[12]\\b" app/src/services/library-search.service.ts` returns zero matches (no 1 or 2 left in the value position).
    - `grep -nE "ignoreLocation:\\s*true" app/src/services/library-search.service.ts` STILL returns at least one match (Pitfall 1 regression guard).
    - Source contains the literal token "50-11" in a code comment so the gap-closure provenance is searchable.
    - All existing tests in `app/tests/services/library-search.service.test.mjs` pass.
  </acceptance_criteria>
  <done>
    FUSE_OPTIONS.threshold is 0.3 and minMatchCharLength is 3. ignoreLocation remains true. File header documents the tuning band and the gap that motivated it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend library-search test suite with G4 false-positive regression tests</name>
  <files>app/tests/services/library-search.service.test.mjs</files>
  <read_first>
    - app/tests/services/library-search.service.test.mjs (existing structure — describe blocks, makePost helper, LATE_BODY constant used to enforce Pitfall 1)
    - app/src/services/library-search.service.ts (updated FUSE_OPTIONS — confirm test corpus exercises both the new tighter threshold and the preserved ignoreLocation knob)
    - .planning/phases/50-retrieval-and-library-foundation/50-UAT.md (Test 4 reported case — '3D printing' on Liked tab matching Kanji video with stray highlights)
  </read_first>
  <behavior>
    - G4-01: Searching `3D printing` against a corpus containing one post about 3D printing AND one Kanji-themed post (whose body has characters that overlap '3D printing' only as sub-runs like 'i','R','eco','gniti') returns the 3D-printing post first AND does NOT return the Kanji post at all (or, at minimum, the Kanji post has zero matches in its body OR has a relevance score worse than 0.4 so it would be filtered by a downstream UI threshold — assert it is absent from the result array).
    - G4-02: Searching `system` against a corpus containing 'understanding the system prompt' returns the post AND the single-key match indices on body contain only one [start, end] pair whose length is at least 6 (`system` = 6 chars), proving no single-char stray runs were returned.
    - G4-03: Searching a single-character query `e` against a multi-post corpus returns ZERO results (minMatchCharLength=3 rejects sub-3-char queries; Fuse internally treats them as empty).
    - G4-04: Searching a 2-character query `at` against a multi-post corpus also returns ZERO results (still below the new min).
    - G4-05 (regression guard for Pitfall 1): The existing late-body test asserting a match at character 250 STILL passes — `ignoreLocation: true` preserved. Confirm by referencing the test by name (if one exists) or add a fresh assertion using LATE_BODY that searches for `spaced repetition` and confirms the post returns with a non-empty matches array.
  </behavior>
  <action>
    Edit `app/tests/services/library-search.service.test.mjs`. Inside the existing top-level `describe('libraryService search — Phase 50 plan 50-04 (GREEN)', ...)` block (OR add a new sibling `describe('libraryService search — Phase 50 plan 50-11 gap G4 regression', ...)` if cleaner), add five new `it(...)` blocks named per `<behavior>` (G4-01 through G4-05).

    For G4-01 build a corpus with two posts. Post A: title `3D Printing at Scale`, bodyMarkdown `An overview of fused deposition modeling and resin printing`. Post B (the Kanji false-positive): title `Recognizing Kanji Radicals`, bodyMarkdown `Kanji recognition relies on identifying repeated structural radicals (R) and stroke compositions; this differs from alphabetic recognition.`. Build the index via `buildIndex([postA, postB])`, run `search(index, '3D printing')`, then assert `results[0].item.id === postA.id` AND `results.every(r => r.item.id !== postB.id)`. Use the actual ids assigned via `makePost({ id: 'p-3d', ... })`.

    For G4-02 build a corpus with one post: title `Understanding the System Prompt`, bodyMarkdown `The system prompt anchors the assistant's behavior across turns.` Run `search(index, 'system')`. Assert the result is non-empty AND for the first result's matches array, the `key: 'bodyMarkdown'` entry's indices array contains exactly one pair whose run length (`end - start + 1`) is at least 6.

    For G4-03 build a corpus with two posts containing various words. Search `e`. Assert `results.length === 0` (minMatchCharLength=3 rejects).

    For G4-04 same corpus, search `at`. Assert `results.length === 0`.

    For G4-05 reuse the existing LATE_BODY constant or rebuild a post with bodyMarkdown set to LATE_BODY (`spaced repetition strengthens memory through expanding intervals` starting at char position 250+). Search `spaced repetition`. Assert results non-empty AND the matched indices on bodyMarkdown include a pair where start ≥ 200 — this is the Pitfall 1 regression guard that fails loudly if a future edit drops `ignoreLocation: true`.

    File header comment update: insert a sentence in the existing header block: "// Plan 50-11 (gap G4) added regression tests G4-01..G4-05 for the threshold tightening to 0.3 + minMatchCharLength raise to 3."
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/services/library-search.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File contains five new test names matching `G4-0[1-5]`. Verify: `grep -cE "G4-0[1-5]" app/tests/services/library-search.service.test.mjs` returns at least 5.
    - File contains the test corpus for the Kanji false-positive (string literal containing `Kanji` and `R` runs). Verify: `grep -c "Kanji" app/tests/services/library-search.service.test.mjs` returns at least 1.
    - **W2 strengthened — negative assertions exist in the test bodies (not just in test names).** Each new G4-0X block must contain the literal assertion pattern its name implies. Verify by scoped grep against the test file:
      (a) `grep -cE "r\\.item\\.id\\s*!==?\\s*postB\\.id|\\.every\\([^)]*\\.id\\s*!==?\\s*postB" app/tests/services/library-search.service.test.mjs` returns ≥ 1 — proves G4-01 asserts the Kanji post is absent, not merely "present but ranked lower".
      (b) `grep -cE "results\\.length\\s*===?\\s*0|assert(\\.strictEqual)?\\(\\s*results\\.length\\s*,\\s*0" app/tests/services/library-search.service.test.mjs` returns ≥ 2 — proves G4-03 (single-char `e`) and G4-04 (`at`) both assert ZERO results, not "results.length < someBound".
      (c) `grep -cE "end\\s*-\\s*start\\s*\\+\\s*1|indices\\[0\\]\\[1\\]\\s*-\\s*indices\\[0\\]\\[0\\]" app/tests/services/library-search.service.test.mjs` returns ≥ 1 — proves G4-02 measures the actual highlight run length, not just "matches array non-empty".
      (d) `grep -cE "start\\s*>=\\s*200|start\\s*>=\\s*250|indices\\[0\\]\\[0\\]\\s*>=\\s*200" app/tests/services/library-search.service.test.mjs` returns ≥ 1 — proves G4-05 measures the late-body position, not just "results non-empty".
    - All tests in the file pass under `node --test`. Verify: command exits 0 with all G4-0X tests reporting `ok`.
    - The single-char and 2-char rejection tests are independent (no shared mutable state between them).
    - The Pitfall 1 regression test G4-05 references the existing LATE_BODY constant OR rebuilds a body string with ≥ 200 chars of prefix before the searchable phrase.
  </acceptance_criteria>
  <done>
    Five new regression tests prove (a) the Kanji false-positive case from UAT-4 is closed, (b) short queries below minMatchCharLength are rejected, and (c) the ignoreLocation Pitfall 1 guard still holds.
  </done>
</task>

</tasks>

<verification>
- `cd app && node --test tests/services/library-search.service.test.mjs` exits 0 with all tests passing
- `cd app && npm test` exits 0 (no other tests regressed by the threshold change)
- Threshold + minMatchCharLength values match the tuning band (0.3 / 3)
- ignoreLocation: true preserved (Pitfall 1)
</verification>

<success_criteria>
- G4 closed: A user searching `3D printing` on any /saved tab no longer surfaces an unrelated Kanji video. Single-char and 2-char queries return empty results.
- New regression tests G4-01..G4-05 codify the fix at the unit-test level.
- Pitfall 1 (ignoreLocation: true) is preserved with a dedicated G4-05 regression test.
- File header documents the tuning band so future edits know not to widen the threshold or lower the min match length.
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-11-SUMMARY.md` when done.
</output>
</content>
</invoke>