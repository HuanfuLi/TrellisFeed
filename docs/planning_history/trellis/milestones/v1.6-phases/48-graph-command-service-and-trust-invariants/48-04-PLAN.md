---
phase: 48-graph-command-service-and-trust-invariants
plan: 04
type: execute
wave: 3
# Wave 3: sequenced after Plan 03 commits (Warning #1 fix — Plans 03 + 04 both touch graph-command.service.ts; Plan 04 starts only after Plan 03 has landed its merge/detach/prune implementations).
depends_on: ["48-01", "48-02", "48-03"]
files_modified:
  - app/src/services/graph-command.service.ts
  - app/src/services/question.service.ts
  - app/tests/services/_actions-mock-question.mjs
  - app/tests/services/graph-command-service.undo.test.mjs
  - app/tests/services/graph-command-service.integration.test.mjs
  - app/tests/services/graph-command-service.reload-survival.test.mjs
  - app/tests/services/graph-command-service.concurrency.test.mjs
  - app/package.json
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-03
  - GRAPH-04
must_haves:
  truths:
    - "undo() pops the newest journal entry and applies the inverse mutation directly. It then writes a NEW journal entry with the SAME `cmd` as the popped entry and SWAPPED `before`/`after` snapshots (Blocker #3 fix — inverse-verb-with-swapped-snapshots strategy from RESEARCH Summary point 6). The journal cmd union never contains a synthetic 'undo' literal."
    - "After a rename → undo → undo round-trip, the journal contains exactly TWO 'rename' entries: the original rename + the inverse rename written by the first undo. The second undo pops that inverse entry and re-applies the original direction, producing a third 'rename' entry. Repeated undo is naturally supported without a special 'undo of undo' branch."
    - "undo trusts the journal: every entry corresponds to a committed mutation; failed mutations are not journaled (Plans 02 + 03 abort on questionService.delete failure BEFORE journal append). undo therefore never encounters an entry whose mutation didn't actually happen."
    - "After each successful command (rename / move / delete / merge / detach / prune / undo), questionService.getAll() returns the mutated state WITHOUT any in-flight UI re-read needed — this is the SERVICE-LEVEL invariant Phase 49's GraphScreen will rely on (Blocker #5 fix — service guarantee; UI subscription is deferred to Phase 49)."
    - "User-visible graph state survives a localStorage 'reload' (drop in-memory question store, re-read from localStorage; restored state matches post-command state)."
    - "Cross-command sequence rename → move → merge → undo composes correctly: undo of merge resurrects loser + reparents children back; undo of move restores old parent; undo of rename restores old title."
    - "Concurrent commands serialized by the per-process mutex: two parallel rename calls against the same id observe each other's writes (journal entries chain consistently)."
    - "Pre-image validation via isValidPreImage rejects tampered journal entries (e.g., before === null) during undo; returns VALIDATION_ERROR rather than crashing or writing garbage to the store."
  artifacts:
    - path: "app/src/services/graph-command.service.ts"
      provides: "undo() method fully implemented — inverse-verb-with-swapped-snapshots dispatch with isValidPreImage guard."
    - path: "app/src/services/question.service.ts"
      provides: "restoreDeleted(question: Question): void thin wrapper — the single new exception to the no-direct-write rule; used ONLY by undo() to resurrect from delete/merge journal entries."
    - path: "app/tests/services/_actions-mock-question.mjs"
      provides: "Extended with opt-in _enableLocalStorageMirror() helper (default OFF) so the reload-survival test can exercise the localStorage round-trip without polluting Plan 02/03 tests (Warning #6 fix)."
    - path: "app/tests/services/graph-command-service.undo.test.mjs"
      provides: "Per-cmd undo coverage + empty-journal NOT_FOUND + N=10 retention edge case + tampered pre-image rejection + rename→undo→undo round-trip (Blocker #3 fix)."
    - path: "app/tests/services/graph-command-service.integration.test.mjs"
      provides: "End-to-end command compositions (rename→move→merge→undo×3) verifying store + journal state at each step."
    - path: "app/tests/services/graph-command-service.reload-survival.test.mjs"
      provides: "Drop _store + re-read from localStorage; verify post-command state survives (success criterion 3) + SERVICE-LEVEL post-command store-truthful invariant for every cmd (Blocker #5 fix)."
    - path: "app/tests/services/graph-command-service.concurrency.test.mjs"
      provides: "Mutex serialization tests — concurrent rename + concurrent move + concurrent merge."
  key_links:
    - from: "app/src/services/graph-command.service.ts undo()"
      to: "app/src/services/graph-edit-journal.service.ts popNewest + isValidPreImage"
      via: "popNewest() returns entry; isValidPreImage(entry.before) gates the inverse application"
      pattern: "popNewest|isValidPreImage"
    - from: "app/src/services/graph-command.service.ts undo()"
      to: "app/src/services/question.service.ts patchQuestion + restoreDeleted"
      via: "For each cmd: patch back to before state; for delete/merge: re-insert resurrected record via the new restoreDeleted (single permitted exception — see <action>)"
      pattern: "questionService\\.patchQuestion|restoreDeleted"
---

<deferred>
**GraphScreen.tsx subscription to GRAPH_UPDATED for selected-node re-render is Phase 49 (GRAPHUI-03 — "user sees the corrected graph after navigation away, navigation back, or app reload"). Phase 48 proves the SERVICE-LEVEL invariant only — that after every command, `questionService.getAll()` returns the mutated state. The reload-survival test asserts that invariant. Cross-reference: `48-VALIDATION.md` "Manual-Only Verifications" table documents this deferral (Blocker #5 fix).**
</deferred>

<objective>
Complete Phase 48 by:
1. Implementing `undo()` using the inverse-verb-with-swapped-snapshots strategy (Blocker #3 fix — RESEARCH Summary point 6). undo() pops the newest entry, applies the inverse mutation directly, then appends a NEW journal entry with the SAME `cmd` and SWAPPED before/after.
2. Wiring the threat-T-48-01 mitigation: undo MUST call `isValidPreImage(entry.before)` from Plan 01 before passing any snapshot to `patchQuestion` — rejects tampered journal entries.
3. End-to-end integration tests: rename → move → merge → undo×3 sequence; verifies store + journal state at each step (closes GRAPH-01 boundary verification).
4. Reload-survival tests: simulate full app reload by clearing the mock's in-memory _store and reloading from localStorage; assert post-command state survives. ALSO add a SERVICE-LEVEL test (Blocker #5 fix) that for each cmd, after the command resolves, `questionService.getAll()` reflects the mutated state without any reload — this is the invariant Phase 49's GraphScreen subscription will depend on.
5. Concurrency tests: assert the per-process mutex serializes parallel commands consistently.

Purpose: Phase 48's "service ships Phase-49-ready" goalpost requires undo PLUS end-to-end proof of the four success criteria. This plan closes them.

Output:
- `app/src/services/graph-command.service.ts` — `undo()` fills in the last stub using inverse-verb-with-swapped-snapshots.
- `app/src/services/question.service.ts` — adds `restoreDeleted(question: Question): void` (single permitted exception used ONLY by undo's delete/merge resurrect branches).
- `app/tests/services/_actions-mock-question.mjs` — extended with opt-in `_enableLocalStorageMirror()` (default OFF; Warning #6 fix).
- 4 new test files (undo, integration, reload-survival, concurrency).
- `app/package.json` — `test:actions` script extended with the 4 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Code/EchoLearn/CLAUDE.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-VALIDATION.md
@/Users/Code/EchoLearn/app/src/services/graph-command.service.ts
@/Users/Code/EchoLearn/app/src/services/graph-edit-journal.service.ts
@/Users/Code/EchoLearn/app/src/services/graph-edit-journal-phrasing.ts
@/Users/Code/EchoLearn/app/src/services/question.service.ts
@/Users/Code/EchoLearn/app/src/services/trellis-actions.service.ts
@/Users/Code/EchoLearn/app/src/types/index.ts
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-question.mjs

<interfaces>
<!-- Contract being completed -->

In app/src/services/graph-command.service.ts:

  undo(): Promise<ServiceResult<{
    undoneCmd: 'rename' | 'move' | 'merge' | 'detach' | 'prune' | 'delete';
    targetIds: string[];
    summary: string;  // human-readable, e.g., "Undid: rename 'Spaced Rep' → 'Spaced Repetition'"
  }>>;

  // Returns NOT_FOUND when journal is empty (per R9).
  // Returns VALIDATION_ERROR when popped entry has invalid pre-image (T-48-01).
  // Returns NOT_FOUND when popped entry's targetIds reference deleted nodes
  //   (R10 risk 4 — "Undo unavailable, original record no longer exists").
  // NOTE: there is NO "undo of undo → VALIDATION_ERROR" branch (Blocker #3 fix).
  // The inverse-verb-with-swapped-snapshots strategy naturally supports repeated
  // undo: a popped "rename A→B" entry produces an inverse "rename B→A" entry on
  // the journal tail; the next undo pops that inverse entry and re-applies the
  // original direction, producing yet another "rename A→B" entry. Round-trip is
  // user-controlled, journal stays consistent, no special-case code.

In app/src/services/question.service.ts (NEW method added by this plan):

  /**
   * Used ONLY by graphCommandService.undo() to resurrect a hard-deleted record
   * from the journal's full pre-image. Writes through the standard saveStore
   * cycle (single localStorage path); fires the SQLite write-through.
   *
   * Do NOT call from any other path. The journal owns the pre-image; this is the
   * single permitted exception to the "no direct writes outside questionService"
   * rule.
   */
  restoreDeleted(question: Question): void;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement undo() with inverse-verb-with-swapped-snapshots dispatch + pre-image validation</name>
  <files>app/src/services/graph-command.service.ts, app/src/services/question.service.ts, app/tests/services/graph-command-service.undo.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (Plans 02 + 03)
    - app/src/services/graph-edit-journal.service.ts (Plan 01 — popNewest, isValidPreImage)
    - app/src/services/graph-edit-journal-phrasing.ts (Plan 01 — for summary string)
    - app/src/services/question.service.ts lines 360-380 (buildAndSave — pattern for restoreDeleted), lines 565-571 (delete signature for reference)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-06, D-10, D-12, D-17, D-18)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §Summary point 6 (inverse-verb-with-swapped-snapshots — THE strategy for this plan) + §R10 risk 4 (undo-after-reorg)
  </read_first>
  <behavior>
    - Empty journal: `undo()` returns `{ success: false, error: { code: 'NOT_FOUND', message: 'Nothing to undo.', retryable: false } }`.
    - Tampered pre-image: if `popNewest()` returns an entry where `isValidPreImage(entry.before) === false`, return `{ success: false, error: { code: 'VALIDATION_ERROR', message: 'Journal entry corrupted.', retryable: false } }`. The popped entry is NOT re-pushed (it's already gone — corruption is fatal-to-that-entry; the NEXT undo pops the next-newest entry).
    - **Blocker #3 fix — Per cmd inverse dispatch using SWAPPED SNAPSHOTS, SAME CMD:**
      - `rename`: patch target id with `before.title`, `before.content`, `before.summary`, `before.embeddingVector` (restore). If target id no longer exists in store → return NOT_FOUND ("Original record no longer exists.") per R10 risk 4. **Append a NEW journal entry: `graphEditJournal.append({ cmd: 'rename', targetIds: entry.targetIds, before: entry.after, after: entry.before })`** (same cmd, swapped snapshots). Emit GRAPH_UPDATED with `payload.kind === 'undo'`. Summary string: `phraseJournalEntry({ cmd: 'rename', ts: Date.now(), id: 'undo', targetIds: entry.targetIds, before: entry.after, after: entry.before })` prepended with `"Undid: "`.
      - `move`: patch target id with `before.parentId`, `before.clusterNodeId`, `before.branchLabel`, `before.clusterLabel`, `before.placementReason` (restore). Also walk children to recompute old/new parent qaCount + nodeSummary (mirror Plan 02 move's side-effect recompute, but in reverse). **Append a NEW `cmd: 'move'` journal entry with swapped before/after.** Emit GRAPH_UPDATED.
      - `merge`: resurrection — re-insert `before.loser` into the store (with its ORIGINAL ID) via `questionService.restoreDeleted(before.loser as Question)` AND reparent each child in `before.reparentedChildren` back to its OLD parentId. Survivor's `qaCount` + `embeddingVector` + `nodeSummary` restored from `before.survivor`. **Append a NEW `cmd: 'merge'` journal entry with swapped before/after** — the inverse merge entry's `before` is the post-merge state (which was `entry.after`) and `after` is the pre-merge state (which was `entry.before`). Repeated undo naturally re-applies the original merge direction. Emit GRAPH_UPDATED.
      - `detach`: patch target id back to `before` placement fields. DO NOT re-fire classifyAndAnchorIncremental (D-13 inline note — "Undo: restore these fields AND skip re-classification"). Update old parent's qaCount + nodeSummary (re-add the line). **Append a NEW `cmd: 'detach'` journal entry with swapped before/after.** Emit GRAPH_UPDATED.
      - `prune`: call `trellisActionsService.unpruneQuestion(anchorId)` — preserves the existing emit chain (R6 migration path). **Append a NEW `cmd: 'prune'` journal entry with swapped before/after.** Emit GRAPH_UPDATED with `payload.kind === 'undo'`.
      - `delete`: resurrection — re-insert `before.deletedRecord` via `questionService.restoreDeleted(before.deletedRecord as Question)`. Walk `before.reparentedChildren` and revert each child's placement fields to old values. **Append a NEW `cmd: 'delete'` journal entry with swapped before/after.** Emit GRAPH_UPDATED.
    - **NO 'undo' branch — Blocker #3:** there is no `case 'undo'` in the switch and no "undo of undo → VALIDATION_ERROR" branch. The journal's `cmd` union (six verbs) makes that branch unreachable. Repeated undo round-trips work via the inverse-verb mechanism:
      - rename A→B (journal: [rename A→B])
      - undo (applies B→A, journal: [rename A→B, rename B→A])
      - undo (pops the inverse, applies A→B again, journal: [rename A→B, rename B→A, rename A→B])
      - …repeats indefinitely; operator-controlled.
    - **N=10 retention edge:** append-only invariant means each undo adds an entry, potentially evicting the oldest. After 10 commands + 1 undo, journal has 10 entries (the oldest command was evicted; the popped command and the inverse entry are now in the tail).
    - **GRAPH_UPDATED payload kind:** undo emits `kind: 'undo'` on the event bus (NOT `kind: <popped cmd>`) so Phase 49 subscribers can distinguish "undo just happened" toasts from forward-command toasts. The journal entry still uses the SAME cmd as the popped entry (swapped snapshots) — these are separate concerns: the journal records what literally happened to the data; the event bus carries the user's semantic intent.
    - Returns `{ success: true, data: { undoneCmd: <popped cmd>, targetIds: <popped targetIds>, summary: <human string> } }` on success.
  </behavior>
  <action>
    Replace `undo` stub in `graph-command.service.ts`. Inside mutex:
    1. `const entry = graphEditJournal.popNewest();` If undefined → NOT_FOUND.
    2. Guard: `if (!isValidPreImage(entry.before)) return VALIDATION_ERROR;`. Do NOT re-push (corruption-removed entry stays removed — next undo() pops the next-newest entry).
    3. Switch on `entry.cmd`. For each branch, implement the inverse per the `<behavior>` spec. The switch has EXACTLY 6 cases: 'rename', 'move', 'merge', 'detach', 'prune', 'delete'. No 'undo' case (Blocker #3 fix — the journal's cmd union does not include 'undo' so this case is unreachable; TypeScript exhaustive-check ensures the switch covers the union).

       For `delete` and `merge` (resurrection): use the new `questionService.restoreDeleted(question: Question): void` method added in this task. Inline-comment in graph-command.service.ts at the call site: `// undo() is the SECOND permitted exception to the "no direct writes" rule; the journal owns the full pre-image, so restoring is a single round-trip that question.service.restoreDeleted brokers via the standard saveStore path.`

    4. For each inverse, AFTER patches: **append an inverse entry using the SAME cmd as the popped entry, with SWAPPED before/after** (Blocker #3 fix — RESEARCH Summary point 6):

       ```
       graphEditJournal.append({
         cmd: entry.cmd,           // SAME as the popped entry — never 'undo'
         targetIds: entry.targetIds,
         before: entry.after,      // SWAPPED
         after: entry.before,      // SWAPPED
       });
       ```

       Inline comment at this append: `// Inverse-verb-with-swapped-snapshots per RESEARCH Summary point 6. The journal cmd union is the six real verbs; undo writes the inverse direction as another entry of the same cmd. Repeated undo naturally re-applies via the same mechanism — no synthetic 'undo' cmd, no special "undo of undo" branch.`

    5. Emit one `{ type: 'GRAPH_UPDATED', payload: { kind: 'undo', anchorId: entry.targetIds[0], affectedIds: entry.targetIds } }`. (kind: 'undo' is event-bus-only — see Plan 01 GRAPH_UPDATED payload union. The journal's cmd field stays one of the six real verbs.)

    6. Construct summary string for the return value. Use `phraseJournalEntry(entry)` from Plan 01 and prepend `"Undid: "`.

    7. Return `{ success: true, data: { undoneCmd: entry.cmd, targetIds: entry.targetIds, summary } }`.

    Add `restoreDeleted(question: Question): void` to `app/src/services/question.service.ts`. Implementation: read store via `loadStore({ includeFlagged: true })`, push the question, call `saveStore(store)`, fire-and-forget `persistToSQLite(question)`. (Same pattern as `buildAndSave` but accepting a pre-formed record.) Do NOT add a separate emit — undo's command-boundary emit covers the GRAPH_UPDATED. Add a JSDoc note: `Used ONLY by graphCommandService.undo() to resurrect a hard-deleted record. Do not call from other paths.`

    Test file `app/tests/services/graph-command-service.undo.test.mjs`:
    - Empty journal: undo → NOT_FOUND.
    - Tampered pre-image: directly inject a journal entry with `before: null` via localStorage; undo → VALIDATION_ERROR; subsequent undo → NOT_FOUND (corrupted entry removed; next-newest entry is whatever was before — if none, NOT_FOUND). Asserts T-48-01 mitigation.
    - Undo of rename: rename then undo; assert title/content/summary restored; assert journal contains TWO `rename` entries (the original + the inverse with swapped before/after) — Blocker #3 assertion.
    - **Rename → undo → undo round-trip (Blocker #3 explicit test):** rename A→B; undo (applies B→A; journal now [rename A→B, rename B→A]); undo (applies A→B again; journal now [rename A→B, rename B→A, rename A→B]). Assert store title is back to B; assert journal length is 3; assert all three entries have `cmd === 'rename'` and NO entry has `cmd === 'undo'`.
    - Undo of move: similar — after undo, journal has TWO `move` entries.
    - Undo of merge: merge then undo; assert loser resurrected with original ID (via restoreDeleted); children reparented back; survivor qaCount + embeddingVector + nodeSummary restored. Journal contains TWO `merge` entries (original + inverse with swapped before/after).
    - Undo of detach: detach then undo; assert placement fields restored; assert classifyAndAnchorIncremental was NOT called the second time (mock spy on classify count — increments only on the original detach).
    - Undo of prune: prune then undo; assert flagged + prunedFromTrellis both false; assert trellisActionsService.unpruneQuestion was called.
    - Undo of delete: delete an anchor (with cascade); undo; assert anchor resurrected via restoreDeleted; assert all children's placements restored.
    - **NO "undo of undo VALIDATION_ERROR" test** — Blocker #3 fix: that branch doesn't exist anymore. Instead, the rename→undo→undo test above proves repeated undo works naturally.
    - N=10 retention edge: append 10 entries, undo, assert journal length === 10 (the oldest was evicted on the undo append; the undone command + inverse entry now occupy the tail).
    - Undo-after-reorg edge (R10 risk 4): rename anchor → simulate reorg by mutating store to delete the anchor → undo → assert NOT_FOUND with appropriate message; assert the popped entry is NOT re-pushed (acceptance — once popped, gone).
    - Source-reading negative invariant: assert that `app/src/services/graph-command.service.ts` does NOT contain any of: `cmd: 'undo'`, `"cmd": "undo"`, or `case 'undo':` (Blocker #3 enforcement).

    Add `graph-command-service.undo.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.undo.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+undo\s*\(" app/src/services/graph-command.service.ts` finds implementation.
    - `grep -c "code: 'NOT_IMPLEMENTED'" app/src/services/graph-command.service.ts` returns 0 (all seven verbs implemented).
    - Source calls `isValidPreImage` from graph-edit-journal.service: `grep -F "isValidPreImage" app/src/services/graph-command.service.ts` returns >= 1.
    - Source dispatches on every cmd except 'undo': `grep -E "case '(rename|move|merge|detach|prune|delete)'" app/src/services/graph-command.service.ts` returns >= 6.
    - **Blocker #3 negative invariant:** `grep -F "cmd: 'undo'" app/src/services/graph-command.service.ts` returns 0 (the journal append uses `cmd: entry.cmd` — never the literal 'undo').
    - **Blocker #3 negative invariant:** `grep -E "case '(undo)'" app/src/services/graph-command.service.ts` returns 0 (no undo-of-undo case).
    - Source appends an inverse entry with swapped snapshots: `grep -F "before: entry.after" app/src/services/graph-command.service.ts` returns >= 1 AND `grep -F "after: entry.before" app/src/services/graph-command.service.ts` returns >= 1.
    - Source emits kind: 'undo' on event bus: `grep -F "kind: 'undo'" app/src/services/graph-command.service.ts` returns >= 1 (event-bus signal is allowed; journal cmd is not).
    - `app/src/services/question.service.ts` contains `restoreDeleted` method: `grep -E "restoreDeleted" app/src/services/question.service.ts` returns >= 1.
    - `cd app && npm run test:actions -- tests/services/graph-command-service.undo.test.mjs` exits 0.
    - Test file covers: empty-journal NOT_FOUND, tampered VALIDATION_ERROR, undo of each of the 6 cmd verbs, the rename→undo→undo round-trip (Blocker #3), N=10 retention edge, undo-after-reorg NOT_FOUND, AND the source-reading negative invariant asserting no `cmd: 'undo'` and no `case 'undo':` in the production source.
    - `package.json` `test:actions` script includes `graph-command-service.undo.test.mjs`.
  </acceptance_criteria>
  <done>
    undo() implemented with inverse-verb-with-swapped-snapshots dispatch (Blocker #3 fix). Append-only invariant (D-06) enforced. T-48-01 tampered-pre-image rejection wired. delete/merge undo paths resurrect via `questionService.restoreDeleted`. All seven command verbs now fully implemented. NO synthetic 'undo' literal anywhere in the journal cmd union or switch statement.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration + reload-survival (with service-level invariant) + concurrency tests + opt-in mock localStorage mirror</name>
  <files>app/tests/services/_actions-mock-question.mjs, app/tests/services/graph-command-service.integration.test.mjs, app/tests/services/graph-command-service.reload-survival.test.mjs, app/tests/services/graph-command-service.concurrency.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (complete service after Task 1)
    - app/src/services/graph-edit-journal.service.ts
    - app/src/services/question.service.ts (look at `loadStore` + `saveStore` to understand how reload-survival should simulate cold boot)
    - app/tests/services/_actions-mock-question.mjs (extend with an opt-in `_enableLocalStorageMirror()` helper — Warning #6 fix)
    - .planning/ROADMAP.md lines 53-62 (Phase 48 success criteria — 1, 2, 3, 4)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-VALIDATION.md "Manual-Only Verifications" table (cross-screen UX re-read deferred to Phase 49)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R10 (risks, esp. risk 9 concurrency)
    - CLAUDE.md §"Best practices learned in Phase 32.1" §"Anchor name normalization" §"Event bus — unified GRAPH_UPDATED" §"Always-mounted screens must explicitly re-read service state on navigation" (relevant for Phase 49 — informs Blocker #5 deferral rationale)
  </read_first>
  <behavior>
    **MOCK EXTENSION (Warning #6 fix — opt-in, default OFF):**
    - `_actions-mock-question.mjs` exports a new module-scope flag `_localStorageMirrorEnabled` (default `false`) and a public setter `_enableLocalStorageMirror(enabled = true)`.
    - When the flag is `true`: every `patchQuestion` and `delete` call ALSO writes JSON.stringify(_store) to `localStorage.setItem('trellis_questions', ...)`. When `false` (default): no localStorage write — existing Plan 02 + Plan 03 tests are UNCHANGED in behavior.
    - A new helper `_reloadFromStorage()` reads `JSON.parse(localStorage.getItem('trellis_questions') ?? '[]')` and `_resetStore(...)` with it. This helper is callable regardless of the mirror flag (the test using it asserts the flag is on first).
    - Reload-survival test enables the mirror in its `before()` hook; resets in `after()`.

    INTEGRATION TEST:
    - Seed store with 1 cluster, 2 anchors under it, 4 QAs (2 per anchor).
    - Sequence: `rename(anchor-A, 'X')` → `move(qa-1, anchor-B)` → `merge(anchor-A, anchor-B)` → `undo()` × 3 (reverses merge, then move, then rename).
    - After each step, assert store state + journal state match expectations:
      - After rename: anchor-A title === 'X'; journal length === 1 (one rename entry).
      - After move: qa-1.parentId === anchor-B; anchor-A.qaCount decremented; anchor-B.qaCount incremented; journal length === 2 (rename + move).
      - After merge: anchor-A hard-deleted; anchor-B has all 4 QAs (well, 3 if one was already moved + the 2 originals); journal length === 3 (rename + move + merge).
      - After undo (merge inverse): anchor-A resurrected with original ID via restoreDeleted; children reparented per the merge's pre-image; journal length === 4 (rename + move + merge + inverse-merge, all with cmd in the six verbs).
      - After undo (move inverse): qa-1.parentId === anchor-A again; journal length === 5.
      - After undo (rename inverse): anchor-A title === <original>; journal length === 6.
    - Verifies GRAPH-01 (one service boundary), GRAPH-02 (preservation), GRAPH-03 (undo without losing source Q&A content).
    - Source-reading negative invariant inside the test file: assert that every journal entry's `cmd` is in `['rename', 'move', 'merge', 'detach', 'prune', 'delete']` — no entry has `cmd === 'undo'`.

    RELOAD-SURVIVAL TEST (success criterion 3) + SERVICE-LEVEL INVARIANT (Blocker #5 fix):
    - `before()`: call `_enableLocalStorageMirror(true)` so patches mirror to localStorage.
    - `after()`: call `_enableLocalStorageMirror(false)` so subsequent test files start with the mock in its default behavior.

    **Reload-after-each-command sub-test (Blocker #5 service-level invariant):**
    - For each of `rename`, `move`, `delete`, `merge`, `detach`, `prune`, then `undo`:
      a. Seed store fresh; enable mirror.
      b. Call the command. Assert success.
      c. **Service-level assertion:** `questionService.getAll()` returns the mutated state WITHOUT any reload — this is the invariant Phase 49's GraphScreen subscription will depend on. Inline comment: `// SERVICE-LEVEL INVARIANT (Blocker #5 fix): after each command, a fresh questionService.getAll() returns the post-command store state — no in-flight UI re-read required. Phase 49 GraphScreen.tsx subscribes to GRAPH_UPDATED and calls getAll(); this test proves the data is there for that subscription to read.`
      d. Then `_reloadFromStorage()` — simulates cold boot.
      e. Assert `questionService.getAll()` STILL returns the mutated state (success criterion 3 — survives reload).

    - Comment block at top of the test file:
      ```
      // success criterion 3: corrected graph survives reload
      // Blocker #5 deferral: GraphScreen.tsx subscription to GRAPH_UPDATED for selected-node
      // re-render is Phase 49 (GRAPHUI-03). Phase 48 proves the SERVICE-LEVEL invariant only:
      // that after every command, questionService.getAll() returns the mutated state.
      // The reload-after-each-command sub-test below proves that the service invariant holds
      // for every cmd, which is what Phase 49's UI subscription will rely on.
      ```

    - Additional standalone post-rename reload assertion (preserved from previous plan version): rename → confirm via getAll() → reload → confirm via getAll().

    CONCURRENCY TEST (R10 risk 9):
    - Seed store with one Question.
    - Call `Promise.all([rename(id, 'A'), rename(id, 'B'), rename(id, 'C')])`.
    - Assert all three returned `{ success: true }` (or, if one or two were no-ops due to ordering, count successes + skips).
    - Assert final store title is one of {'A', 'B', 'C'} (depends on mutex serialization order).
    - Assert journal contains entries for the actual changes: chained `before` titles match the prior `after` titles (each entry's `before.title` equals the previous entry's `after.title`). Concretely: if order is A→B→C, journal entries' titles are [<orig>→A, A→B, B→C].
    - Similar test for `Promise.all([move(qa, parent-X), move(qa, parent-Y)])` — second move's `before.parentId` equals first move's target.
  </behavior>
  <action>
    **MOCK EXTENSION (Warning #6 fix):** Edit `app/tests/services/_actions-mock-question.mjs`. Add at module scope:
    ```js
    let _localStorageMirrorEnabled = false;
    export function _enableLocalStorageMirror(enabled = true) {
      _localStorageMirrorEnabled = enabled;
    }
    function _mirrorToStorageIfEnabled() {
      if (!_localStorageMirrorEnabled) return;
      try { localStorage.setItem('trellis_questions', JSON.stringify(_store)); } catch {}
    }
    export function _reloadFromStorage() {
      try {
        const raw = localStorage.getItem('trellis_questions');
        _store = raw ? JSON.parse(raw) : [];
      } catch { _store = []; }
    }
    ```
    Inside the existing `patchQuestion`, `delete`, `restoreDeleted` mock implementations, ADD a call to `_mirrorToStorageIfEnabled()` AFTER the store mutation. Existing Plan 02 + Plan 03 tests pass `_localStorageMirrorEnabled === false` by default → no localStorage write occurs → their behavior is unchanged. Add inline comment: `// Warning #6 fix (revision 1): localStorage mirror is opt-in via _enableLocalStorageMirror(true). Default OFF preserves Plan 02 + Plan 03 test behavior exactly.`

    INTEGRATION: Create `app/tests/services/graph-command-service.integration.test.mjs`. Use the actions-mock loader pattern. Seed a realistic 1-cluster + 2-anchors + 4-QAs store. Execute the rename → move → merge → undo×3 sequence. At each step, read `_getStore()` from the mock and `graphEditJournal.list()` to assert state + journal coherency. Include the source-reading negative invariant asserting no journal entry has `cmd === 'undo'`.

    RELOAD-SURVIVAL: Create `app/tests/services/graph-command-service.reload-survival.test.mjs`. `before()` enables the mirror; `after()` disables it. Implement the per-command reload-survival sub-test described in `<behavior>` for ALL of rename, move, delete, merge, detach, prune, and undo. Each sub-test asserts both the service-level invariant (Blocker #5) AND the reload survival (success criterion 3).

    CONCURRENCY: Create `app/tests/services/graph-command-service.concurrency.test.mjs`. Run `Promise.all([...])` with 3 parallel renames against the same id; assert mutex serialization via chained journal entries. Repeat for move + a self-merge edge case.

    Add all 3 test files to `app/package.json` `test:actions` script.

    Verify the FINAL `test:actions` script contains all Phase 48 test files plus the 3 existing trellis-actions tests (R8 inventory): `trellis-heal`, `trellis-prune`, `trellis-replant`, `graph-command-service.{rename,move,delete,merge,detach,prune,undo,integration,reload-survival,concurrency}.test.mjs`. The journal + injection tests stay in `test:main` (leaf modules).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions &amp;&amp; npm run test:main &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 3 new test files exist: `graph-command-service.{integration,reload-survival,concurrency}.test.mjs`.
    - `_actions-mock-question.mjs` exports `_enableLocalStorageMirror` and `_reloadFromStorage`: `grep -E "(_enableLocalStorageMirror|_reloadFromStorage)" app/tests/services/_actions-mock-question.mjs` returns >= 2.
    - `_actions-mock-question.mjs` mock defaults are unchanged for Plan 02/03 tests (mirror OFF by default): `grep -F "_localStorageMirrorEnabled = false" app/tests/services/_actions-mock-question.mjs` returns >= 1.
    - **Warning #6 acceptance criterion:** `cd app && npm run test:actions -- tests/services/graph-command-service.rename.test.mjs tests/services/graph-command-service.move.test.mjs tests/services/graph-command-service.delete.test.mjs tests/services/graph-command-service.merge.test.mjs tests/services/graph-command-service.detach.test.mjs tests/services/graph-command-service.prune.test.mjs` exits 0 — Plan 02 + Plan 03 tests pass with the mock extension committed but localStorage mirror disabled (default state).
    - `cd app && npm run test:actions` exits 0 with all Phase 48 + existing trellis tests passing.
    - `cd app && npm test` (full suite) exits 0.
    - Integration test contains the full rename→move→merge→undo×3 sequence with assertions at each step AND asserts no journal entry has `cmd === 'undo'` (Blocker #3 enforcement at integration level).
    - **Blocker #5 service-level invariant explicitly tested:** reload-survival test contains AT LEAST one assertion per command (rename, move, delete, merge, detach, prune, undo) of the form `after the command, questionService.getAll() returns the post-command state without any reload`. Inline comment at the assertion: `// SERVICE-LEVEL INVARIANT (Blocker #5 fix): proves the post-command store is truthful — Phase 49 GraphScreen subscription depends on this.`
    - Reload-survival test contains assertions for AT LEAST 4 commands: post-rename reload, post-move reload, post-merge reload, post-delete-then-undo reload.
    - Concurrency test contains AT LEAST 2 parallel-call scenarios with chained-before-title assertions.
    - `package.json` `test:actions` script includes all graph-command-service test files. Verify via `grep -c "graph-command-service" app/package.json` returns >= 8.
    - Reload-survival assertion explicitly demonstrates success criterion 3 — there must be an assertion comment like `// success criterion 3: corrected graph survives reload`.
  </acceptance_criteria>
  <done>
    End-to-end integration suite proves cross-command composition correctness (and the no-'undo'-cmd invariant). Reload-survival suite proves localStorage durability AND the service-level invariant Phase 49's GraphScreen will rely on (Blocker #5 fix — UI subscription deferred to Phase 49 per `48-VALIDATION.md` Manual-Only Verifications table). Concurrency suite proves mutex serialization. Mock extension is opt-in (Warning #6 fix — Plan 02 + 03 tests pass unchanged). All Phase 48 test files in `test:actions`; full suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review — Phase 48 service surface end-to-end</name>
  <what-built>
    Phase 48 ships:
    - `graph-edit-journal.service.ts` (append-only N=10 journal with pre-image validator; cmd union = six real verbs, no synthetic 'undo')
    - `graph-edit-journal-phrasing.ts` (canonical per-cmd phrasing — six cases, no 'undo' case)
    - `graph-command.service.ts` with 7 verbs (rename / move / merge / detach / prune / delete / undo). The first six write journal entries with their literal cmd; undo applies inverses + writes a new journal entry with the popped entry's cmd + swapped before/after (RESEARCH Summary point 6 — Blocker #3 fix).
    - Reorg LLM system prompt injection ("Manual corrections to preserve:" block)
    - `GraphEditLogEntry` type (cmd = six verbs) + extended `GRAPH_UPDATED` payload (kind union includes 'undo' on event-bus side only — separate from journal cmd)
    - `questionService.restoreDeleted` thin wrapper (single new exception to no-direct-write rule, used ONLY by undo)
    - Opt-in mock localStorage mirror in `_actions-mock-question.mjs` (default OFF — Warning #6 fix; Plan 02 + 03 tests unchanged)
    - 10 test files: `graph-edit-journal`, `reorg-prompt-journal-injection`, 8 `graph-command-service.*` files
    - All 7 commands return `ServiceResult<...>`; LAST GRAPH_UPDATED observed has `payload.kind === cmd` from the command boundary. For delete + merge, an additional untyped GRAPH_UPDATED also fires from `questionService.delete` (subscribers are idempotent per CLAUDE.md §Event bus — accepted double-emit, documented inline). Detach has a second emit from the downstream classify (documented as intentional per R7).
    - Failed `questionService.delete` aborts merge/delete commands BEFORE journal append — no journal entry, no command-boundary emit (Blocker #2 fix).
    - Rename + merge embedding strategy: never overwrites vector with undefined; graceful degradation when embedding unconfigured OR provider fails (Blocker #4 fix).
    - No UI yet — Phase 49 owns selected-node controls. GraphScreen subscription to GRAPH_UPDATED is GRAPHUI-03 (Phase 49) per Blocker #5 deferral; this phase proves the SERVICE-level invariant only (`questionService.getAll()` truthful after every command).
  </what-built>
  <how-to-verify>
    1. Run full test suite: `cd /Users/Code/EchoLearn/app && npm test`. All tests pass.
    2. Spot-check the journal: open the app, trigger a rename via dev console (`(await import('/src/services/graph-command.service.ts')).graphCommandService.rename('<any-question-id>', 'Test Rename')`). Open DevTools → Application → Local Storage → `trellis_graph_edit_log`. Confirm one entry with `cmd: 'rename'`, the operator's typed title, original title in `before.title`.
    3. Spot-check undo + the inverse-verb mechanism (Blocker #3 fix): in dev console, `await graphCommandService.undo()`. Confirm the rename reverted. Confirm `trellis_graph_edit_log` now has TWO entries — BOTH with `cmd: 'rename'` (the original + the inverse with swapped before/after). NEITHER entry has `cmd: 'undo'`. Run `undo()` again — journal grows to THREE rename entries; store title flips back. This is repeated-undo working via the inverse-verb mechanism.
    4. Spot-check reorg injection: if you trigger a reorg with at least one journal entry, dev-console-log the system prompt passed to chatCompletion (you may need to add a temporary console.log in `_doReorganize`). Confirm it contains the "Manual corrections to preserve:" block with your rename listed.
    5. Confirm no UI regressions on Home, Planner, Ask, Graph screens — they should look identical to pre-Phase-48 since no UI changed.
    6. Confirm CLAUDE.md "Event bus — unified GRAPH_UPDATED" still holds — `grep "CLASSIFICATION_COMPLETED\|GRAPH_RENAMED\|GRAPH_MERGED" app/src` returns 0 hits.

    If any spot-check fails: describe the actual behavior and which file looks wrong.

    Issues to surface BEFORE Phase 49:
    - Is the journal entry shape clear enough for the Phase 49 confirmation toasts? (Note: undo writes the popped entry's cmd with swapped snapshots — Phase 49 toasts should look at the EVENT BUS `payload.kind === 'undo'` to render "Undid X" rather than re-deriving from the journal entry's cmd.)
    - Is the `payload.kind` discriminator on `GRAPH_UPDATED` sufficient for Phase 49 to render verb-specific toasts? (The LAST event after a command has `payload.kind === cmd` from the command boundary. Subscribers should dedup on `kind` rather than re-rendering for every event in a burst.)
    - Does `undo()` returning `summary: string` give Phase 49 enough info to display a single-line toast?
    - Did `restoreDeleted` land cleanly, or does it need a separate review with the operator?
    - GraphScreen subscription deferred per Blocker #5 — confirm Phase 49 has GRAPHUI-03 explicitly scoped to wire `GRAPH_UPDATED` → re-render of the selected-node card.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 48 ready for Phase 49, or describe issues to address before sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graphCommandService.undo ↔ tampered journal | If a journal entry's `before` was tampered with via XSS or malicious extension, undo could inject arbitrary content into the Question store via `patchQuestion` (or `restoreDeleted` for delete/merge undo). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 (closure) | Tampering | undo() applies popped journal `before` | mitigate | `isValidPreImage(entry.before)` called BEFORE every inverse-apply branch. Returns VALIDATION_ERROR for null/non-object/array values. The popped entry is removed (not re-pushed) — corruption is treated as fatal-to-that-entry, not permanently breaking undo for subsequent entries. Test asserts the rejection AND that subsequent undo returns NOT_FOUND or pops the next-newest entry. |
| T-48-12 | Tampering | `restoreDeleted` accepts any Question shape | accept | Caller is undo(), which has already passed `isValidPreImage(entry.before)`. The `before.deletedRecord` shape is a Question that was originally written by `questionService.patchQuestion` (a single legitimate writer), so the shape is trusted. Adding another shape gate inside `restoreDeleted` is defensive overkill for the single-user local app. |
| T-48-13 | Repudiation | Repeated undo (operator confusion) | mitigate | Inverse-verb-with-swapped-snapshots means repeated undo cycles the store between two states; journal grows by one entry per undo. No special "undo of undo" branch (Blocker #3 fix removed it). Operator surface in Phase 49 should disable the undo button when the next undo would re-apply a state the operator JUST undid (UX concern; out of scope for Phase 48 service). |
</threat_model>

<verification>
- `cd /Users/Code/EchoLearn/app && npm test` (full suite — `test:main` + `test:actions`) exits 0.
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit` exits 0.
- All 10 Phase 48 test files exist in `app/tests/services/`.
- `grep -c "code: 'NOT_IMPLEMENTED'" app/src/services/graph-command.service.ts` returns 0.
- `grep -F "cmd: 'undo'" app/src/services/graph-command.service.ts` returns 0 (Blocker #3 negative invariant — journal append uses `cmd: entry.cmd`, never literal 'undo').
- `grep -F "isValidPreImage" app/src/services/graph-command.service.ts` returns >= 1 (T-48-01 mitigation wired).
- `grep -F "restoreDeleted" app/src/services/question.service.ts` returns >= 1 (the new resurrect method).
- `grep -F "_enableLocalStorageMirror" app/tests/services/_actions-mock-question.mjs` returns >= 1 (Warning #6 fix).
</verification>

<success_criteria>
- All 4 ROADMAP success criteria met:
  1. ✓ Rename, move, merge, detach, prune/delete, and undo all route through `graphCommandService` boundary (Plans 02/03/04).
  2. ✓ Move + merge + delete preserve parent IDs, labels, cluster IDs, counts, summaries, review links, source Q&A content (Plans 02/03 implementation; integration test verifies preservation across compositions).
  3. ✓ Graph state survives app reload — reload-survival test asserts this. Service-level invariant (post-command `getAll()` truthful) also asserted per Blocker #5 fix; UI subscription deferred to Phase 49 GRAPHUI-03 per `48-VALIDATION.md` Manual-Only Verifications.
  4. ✓ In-flight reorganization cannot overwrite manual corrections — Plan 01 reorg prompt injection delivers this via D-01.
- All 4 GRAPH-XX requirement IDs covered.
- 10 test files green.
- All revision-round-1 blockers + warnings closed:
  - Blocker #2 (questionService.delete success check) — Plans 02 + 03 abort-before-journal on failure; tests assert.
  - Blocker #3 (no synthetic 'undo' cmd) — inverse-verb-with-swapped-snapshots restored from RESEARCH Summary point 6; tests assert via source-reading negative invariants AND a rename→undo→undo round-trip.
  - Blocker #4 (graceful embedding degradation) — rename + merge never overwrite vector with undefined; tests assert all three scenarios (configured+success, unconfigured, fail).
  - Blocker #5 (always-mounted re-read pattern) — service-level invariant test per command proves `getAll()` is truthful; UI subscription deferred to Phase 49 with deferral documented in `<deferred>` block + `48-VALIDATION.md`.
  - Warning #1 (Plan 03 promoted to Wave 3; depends on Plan 02; Plan 04 depends on Plan 03) — sequenced; no parallel mutation of graph-command.service.ts.
  - Warning #2 (detach AbortSignal cancellation test) — explicit test added per fix.
  - Warning #3 (shortSummary in interfaces) — Plan 02 + 03 interface blocks now list `shortSummary?: string`; verified at types/index.ts:36.
  - Warning #4 (kind discriminator on emit) — Plans 02 + 03 emit typed GRAPH_UPDATED from command boundary for delete + merge; tests assert LAST event has `payload.kind === cmd`.
  - Warning #5 (undo-of-undo branch) — MOOT; deleted per Blocker #3 fix.
  - Warning #6 (mock extension opt-in) — `_enableLocalStorageMirror()` defaults OFF; Plan 02 + 03 tests pass unchanged.
- Operator approval received in checkpoint task 3.
- Phase 49-ready: service surface, return types, GRAPH_UPDATED payload, summary strings all match R9 contract.
</success_criteria>

<output>
Create `.planning/phases/48-graph-command-service-and-trust-invariants/48-04-SUMMARY.md` when done.
</output>
</output>
