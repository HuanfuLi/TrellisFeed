---
phase: 55.1-device-test-bug-fixes
plan: 05
subsystem: ask-keyboard-ux
tags: [keyboard, chatinput, animation, android-webview, gap-closure]
requires:
  - "useKeyboard() hysteresis signal (Phase 55.1 BUGFIX-03)"
  - "keyboard-hysteresis.ts pure-helper module pattern"
provides:
  - "Eased ChatInput reposition on keyboard open/close (no teleport)"
  - "resolveChatInputOffset / resolveChatInputSettleDistance pure helpers"
affects:
  - "app/src/components/ChatInput.tsx (Ask-screen input bar)"
tech-stack:
  added: []
  patterns:
    - "React-free pure helper module (mirrors keyboard-hysteresis.ts) unit-tested under node --test"
    - "Transient-transform-decays-to-0 eased reposition driven by a CSS transition on the owned wrapper"
key-files:
  created:
    - app/src/state/chatinput-offset.ts
    - app/tests/state/chatinput-offset.test.mjs
  modified:
    - app/src/components/ChatInput.tsx
decisions:
  - "Resting offset is always 0 — adjustResize is the sole layout authority that lifts the bar; the helper must never add a second offset (would fight the native reflow)."
  - "Eased motion comes from a transient settle distance the component animates THROUGH on the keyboard-state edge, not a persistent offset."
  - "transform/willChange live on the ChatInput-owned <form> wrapper (a Header sibling, not an ancestor) so the Header-positioning invariant is preserved."
  - "Reused the existing useKeyboard() boolean signal instead of adding a second visualViewport listener."
metrics:
  duration: ~25m
  completed: 2026-05-21
  tasks_completed: 2
  files_touched: 3
---

# Phase 55.1 Plan 05: Smooth ChatInput Reposition (GAP-A) Summary

The Ask-screen input bar now eases into its keyboard-open position via a short CSS transition driven off a React-free `resolveChatInputSettleDistance` helper, instead of teleporting when Android `adjustResize` reflows the 100dvh flex column — and the BUGFIX-03 nav instant-hide plus all CLAUDE.md keyboard/overflow/ChatInput invariants are preserved.

## What Was Built

### Task 1 — `resolveChatInputOffset` pure helper + executing test (commit 188ba699)
- New `app/src/state/chatinput-offset.ts`: React-free module mirroring `keyboard-hysteresis.ts`. Exports:
  - `resolveChatInputOffset({ keyboardHeight, isKeyboardOpen })` → always `0` (resting contract: adjustResize already lifts the bar; the helper must not double-move it).
  - `resolveChatInputSettleDistance({ keyboardHeight, isKeyboardOpen })` → the bounded eased delta (px) the component animates through on the open edge: `0` when closed, `min(24, round(keyboardHeight/12))` when open.
  - `MAX_CHATINPUT_SETTLE_DISTANCE = 24`.
- New `app/tests/state/chatinput-offset.test.mjs`: 9 executing assertions (closed→0, open→0, idempotency, non-finite tolerance, settle distance bounds + cap clamp, no-react source guard).

### Task 2 — Eased ChatInput reposition (commit 6b266274)
- `app/src/components/ChatInput.tsx`: reuses `useKeyboard()` (the existing hysteresis signal — no second viewport listener). On every `keyboardOpen` flip an effect reads `window.visualViewport` once at the edge, computes the settle distance, applies a transient `translateY(settleY)`, then `requestAnimationFrame`s `settleY` back to `0`. The `<form>` wrapper carries `transition: transform 0.18s ease-out` so the bar eases into place.
- `transform`/`willChange` are on the ChatInput-owned `<form>` wrapper only — a leaf sibling of the AskScreen Header, never an ancestor (verified: `<ChatInput` is mounted once in AskScreen as a flex sibling; ChatInput renders no Header and accepts no children).

## Verification

All automated source-guard + pure-fn tests green (41 tests, 0 fail):
- `tests/state/chatinput-offset.test.mjs` — 9 pass
- `tests/components/ChatInput.flex-shrink.test.mjs`, `ChatInput.send-on-pointerdown.test.mjs`
- `tests/layout/root-horizontal-clip.test.mjs`
- `tests/components/SwipeTabContainer.resize-guard.test.mjs`
- `tests/components/BottomNavigation.slide.test.mjs`

`tsc -b --noEmit` clean (run against the worktree source with the main repo's TypeScript; the worktree has no installed `node_modules`, so a temporary symlink was used for the typecheck and removed afterward — `node_modules` is gitignored).

Acceptance source-guards:
- `grep -c "minWidth: 0" ChatInput.tsx` = 2 (input invariant intact)
- `grep -c "transition" ChatInput.tsx` = 8 (incl. the new wrapper transition)
- `grep -c "@capacitor/keyboard" useKeyboard.ts` = 0
- `grep "overflow: hidden" index.css` = 1 (root clip intact)
- No `transform`/`will-change`/`filter`/`contain`/`perspective` added to AskScreen's root flex column or any Header ancestor.

## Load-Bearing Invariants Preserved
- ChatInput `<input>` keeps `minWidth: 0` alongside `flex: 1`.
- `html, body { overflow: hidden }` (both axes), App-root `overflowX:'hidden'`, SwipeTabContainer `resync()`/`onFocusOut` untouched.
- `@capacitor/keyboard` not installed.
- `useKeyboard` / `nextKeyboardState` focus-driven instant-hide (commit 6fb7b325 / BUGFIX-03) untouched — the new animation is purely on the ChatInput wrapper and does not alter the nav state machine.
- Header positioning: the new `transform`/`willChange` is on a Header sibling, not an ancestor.

## Deviations from Plan
None — plan executed as written.

## Known Stubs
None.

## Pending Human Verification

### Checkpoint (Task 3): On-device UAT — smooth ChatInput reposition (GAP-A) [gate=blocking-human]
On-device UAT cannot run inside a worktree; deferred to the orchestrator's post-execution device round. Exact device test steps:

On a physical Android device (or the configured emulator) running the Capacitor build:
1. Navigate to the Ask screen and tap the text input to open the keyboard. EXPECT: the input bar rises smoothly (eased) to sit above the keyboard — it does NOT instantly teleport. The bottom-nav still hides in a single motion with no up/down oscillation.
2. Close the keyboard (back button / tap away). EXPECT: the bar eases back down; nav re-appears.
3. Repeat open/close 3–5 times rapidly. EXPECT: no flicker, no leftover offset, bar always lands flush above the keyboard or at rest.
4. Check Planner and Settings screens' keyboard interactions are unaffected (useKeyboard is global).

If residual teleport remains, the transition duration (`0.18s`) / settle mapping (`keyboardHeight/12`, cap `24px`) in `chatinput-offset.ts` + `ChatInput.tsx` may need device tuning — note the observed behavior in the resume signal.

Resume signal: Type "approved" or describe the residual motion issue.

## Self-Check: PASSED
- app/src/state/chatinput-offset.ts — FOUND
- app/tests/state/chatinput-offset.test.mjs — FOUND
- app/src/components/ChatInput.tsx — FOUND (modified)
- Commit 188ba699 — FOUND
- Commit 6b266274 — FOUND
