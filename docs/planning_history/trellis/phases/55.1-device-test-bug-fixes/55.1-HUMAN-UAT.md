---
status: complete
phase: 55.1-device-test-bug-fixes
source: [55.1-VERIFICATION.md]
started: 2026-05-21T19:16:38Z
updated: 2026-07-09T00:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. BUGFIX-01 — Cross-session response binding (rapid-switch race)
expected: On device, rapidly ask a question → New Chat → ask again → New Chat (repeat 3×). No prior session's streaming answer ever renders under a different session's question. Switching session aborts the in-flight stream.
result: passed (device UAT 2026-05-21)

### 2. BUGFIX-02 — Text-art survives provider/locale switch (live Gemini)
expected: With a Gemini thinking model configured and text-art posts already generated, switch provider to Gemini and/or change locale (incl. zh/ja). Existing text-art posts keep full sentences — no collapse to a few words / single token, no API error from `thinkingConfig.thinkingBudget:0`. Short CJK headlines are NOT discarded.
result: passed (device UAT 2026-05-21, round 2 — confirmed).

### 3. BUGFIX-03 — No bottom-nav flicker on keyboard open
expected: On the Ask screen (Android), tap the input. The input island raises smoothly and the bottom-navigation bar does NOT oscillate up/down during the keyboard transition (single settle, no flicker).
result: passed — nav-bar behavior passed after the focus-driven instant-hide re-fix (commit 6fb7b325). The separate input-bar teleport follow-on is tracked and accepted as skipped/won't-fix in Test 5.

### 4. BUGFIX-04 — Send fires on first tap with keyboard open
expected: On the Ask screen with the keyboard open, tap Send once. The message sends on the FIRST tap (not consumed by keyboard dismissal), fires exactly once, and the keyboard stays open.
result: passed (device UAT 2026-05-21)

### 5. GAP-A (BUGFIX-05) — ChatInput animates smoothly to keyboard-open position (no teleport)
expected: On the Ask screen (Android), tap the text input. The input bar should SLIDE/settle into its keyboard-open position over a short ease, not instantly jump/teleport.
result: **FAILED again (round 4, 2026-05-22).** Same teleport behavior. Two approaches now failed (55.1-05 eased-decay, 55.1-11 visualViewport-residual-follow). **Root cause identified (code-level, round 4):** under Android `adjustResize` the WebView LAYOUT viewport shrinks instantly in a SINGLE paint when the IME shows — there are no intermediate geometry frames for `visualViewport` to report. So 55.1-11's `residual = liveVisualInset − layoutReflow` collapses to ~0 (both jump together) → no transform → teleport. 55.1-05 failed because its invert+restore happened in the SAME paint (browser coalesces to final state). Neither did a proper TWO-FRAME FLIP. **RESOLVED — SKIPPED / won't-fix (round 5, 2026-05-22).** Research (operator-requested: "how does WeChat do it") confirmed a smooth follow needs the IME animation's per-frame progress, exposed ONLY natively (Android `WindowInsetsAnimation.onProgress`, API 30+ — WeChat's chat input is a native view). The web platform can't reach it from a WebView: `visualViewport` resize fires ONCE at the END of the animation (no per-frame stream), and the VirtualKeyboard API / `env(keyboard-inset-*)` is unsupported in Android WebView. Robust fix = a native Capacitor plugin bridging `WindowInsetsAnimation.onProgress` → out of scope ("not minor risk"). Per operator: skip. Both attempts (55.1-05, 55.1-11) REVERTED to clean baseline (commit 9e33efad); the teleport is accepted. BUGFIX-03/04 unaffected. See memory `feedback_keyboard_follow_webview_impossible`.

### 6. GAP-B (BUGFIX-05) — Planner trellis stays smooth at scale
expected: Smooth swipe + feed scroll with a large trellis.
result: **PASSED / ACCEPTED (round 6, 2026-07-08).** Round 4 identified the always-mounted off-screen Framer/filter cost; round 5 (`c08c30f2`) moved the off-screen tree to plain SVG. The 2026-07-08 device re-test still felt slightly janky because returning to Planner replaced the plain-SVG subtree with `motion.*` elements and replayed every entrance animation. Chrome identity markers proved both route transitions replaced the tree. Round 6 (`b367e773`) keeps dev/large layouts plain SVG while active and lets small normal layouts animate only on their first visit. Post-fix markers survive Home→Planner→Home and group count remains 118. Operator result: much better; frame rate is still somewhat low but smooth, with no significant drops. Current performance baseline accepted.

### 7. GAP-C (BUGFIX-06) — First LLM response after cold start is fast
expected: First roundtrip after relaunch not ~1 min.
result: **IMPROVED, accepted (round 3, 2026-05-21).** "A little wait, but far better than before." The 55.1-07 boot pre-warm landed. Residual latency is attributed by the operator to model-provider reasoning/thinking before streaming begins. NEW (separate) request: this is fine for Ask Q&A + categorization, but hurts the lazy-loaded POST BODY (user taps into a post and waits seconds for the model to "think" before the body streams). Asks for a **separate, configurable model entry for post-body generation only**, with thinking/reasoning disabled for low streaming latency. Tracked as a new feature (NOT cold-start) — see below.

### 8. GAP-D (BUGFIX-07) — "Nothing new today" no longer shows above a populated feed
expected: Card hidden when feed populated.
result: **PASSED (round 3, 2026-05-21).** Empty-state card no longer contradicts the feed.

### 9. GAP-E (BUGFIX-08) — configurable low-latency post-body model
expected: Configure a non-reasoning/fast model in Settings → AI; tap into a post; body streams immediately (no multi-second think wait).
result: **PASSED (round 4, 2026-05-22).** 55.1-10 fast-model routing (separate full provider entry, thinking disabled per provider) confirmed on device.

## Summary

total: 9
passed: 8
issues: 0
partial: 0
pending: 0
skipped: 1
blocked: 0

_Round 1+2 (BUGFIX-01..04): all 4 pass. Round 3: GAP-D pass; GAP-C improved+accepted and spawned GAP-E. Round 4: GAP-E pass; GAP-A/B still failed. Round 5: GAP-A skipped/won't-fix; GAP-B moved off-screen rendering to plain SVG. Round 6: route-return remount/replay eliminated by b367e773; operator accepted the smoother Android result._

## Gaps

None outstanding. GAP-A is explicitly skipped/won't-fix as a WebView platform limitation; all other items passed or were operator-accepted.
