---
phase: 55.1-device-test-bug-fixes
plan: 08
subsystem: home-feed-ui
tags: [i18n, home-screen, empty-state, gap-closure, bugfix]
requires:
  - HomeScreen empty-state card (D-17)
provides:
  - Empty-state card gated on a genuinely empty feed (no contradiction above populated MasonryFeed)
affects:
  - app/src/screens/HomeScreen.tsx
tech-stack:
  added: []
  patterns:
    - Pure JSX-conditional logic change; no new copy, no new locale keys
key-files:
  created: []
  modified:
    - app/src/screens/HomeScreen.tsx
decisions:
  - "GAP-D resolved as HIDE (operator decision, pre-dispatch): gate empty-state card on dailyPosts.length === 0 instead of > 0."
metrics:
  duration: ~10m
  completed: 2026-05-21
  tasks_completed: 1
  files_modified: 1
---

# Phase 55.1 Plan 08: GAP-D Empty-State Card vs Populated Feed Summary

Hid the "Nothing new today" empty-state card whenever the curiosity feed has content, so it can no longer render above a populated MasonryFeed and contradict it. Pure logic change — no copy, no locale-bundle churn.

## Decision Checkpoint Resolution

The plan's first task was `checkpoint:decision` (HIDE vs REWORD). **Resolved as HIDE by the operator before dispatch** (per the execution context). No pause was taken; the HIDE branch was applied directly.

- **HIDE** requires no new copy and no locale-bundle changes — existing `home.feed.emptyTitle` / `emptyBody` remain unchanged for the genuinely-empty case.
- Bundle parity is therefore preserved by construction (zero key churn across en/zh/es/ja).

## What Was Built

**Task 1 — Apply HIDE fix at HomeScreen.tsx:959** (commit `ab65b0d7`)

The empty-state gate changed from:

```jsx
{conceptQuota === 0 && dailyPosts.length > 0 && questions.length > 0 && (
```

to:

```jsx
{conceptQuota === 0 && dailyPosts.length === 0 && questions.length > 0 && (
```

The current source matched the plan's stated line 959 exactly, so the documented edit applied cleanly. The card now renders ONLY when there are no posts to show (`dailyPosts.length === 0`) — i.e. on a genuinely empty feed — and is hidden whenever the feed has content. Comment updated to record the GAP-D HIDE intent inline.

A truly empty feed (`dailyPosts.length === 0`, `questions.length > 0`, `conceptQuota === 0`) still shows the `home.feed.emptyTitle` / `emptyBody` message, satisfying the must-have that an empty feed retains an appropriate message.

## Verification

Run against the main repo's installed dependencies (the worktree has no `node_modules`; a temporary read-only symlink to `/Users/Code/EchoLearn/app/node_modules` was used for the verification run and then removed — it is gitignored and was never staged):

- `node --test tests/locales/bundle-parity.test.mjs` — PASS (identical key sets across en/zh/es/ja; trivially preserved since no keys changed).
- `node --test tests/locales/missing-key.test.mjs` — PASS (EN fallback intact).
- `tsc -b --noEmit` (via `node_modules/.bin/tsc`) — clean, exit 0 (no broken `t()` keys; the change introduces no new symbols).

## Load-Bearing Invariants Preserved

- **Runtime LLM translation prohibition (i18n):** moot — HIDE adds no copy; the LLM was never invoked.
- **HomeScreen always-mounted resync effects:** untouched. The diff is exactly 2 lines (the conditional + its comment); the `dailyPosts` / `exploredAnchors` resync `useEffect`s are unchanged.
- **MILESTONE_POOL and other static content:** untouched.
- **bundle-parity:** preserved by construction (no key churn).

## Deviations from Plan

### Path-targeting correction (no functional deviation)

- **Found during:** Task 1 first edit attempt.
- **Issue:** The initial Edit used the absolute path `/Users/Code/EchoLearn/app/src/screens/HomeScreen.tsx`, which resolves to the MAIN repo, not the worktree (#3099 absolute-path hazard). The edit and the first test/tsc run landed in the main repo.
- **Fix:** Reverted the accidental main-repo edit (`git checkout -- app/src/screens/HomeScreen.tsx` in the main repo), then re-applied the identical change to the worktree file at `/Users/Code/EchoLearn/.claude/worktrees/agent-a661aa4d66f730389/app/src/screens/HomeScreen.tsx`. The committed change lives in the worktree only; the main repo working tree is back to its pre-edit state.
- **Files modified:** worktree `app/src/screens/HomeScreen.tsx` (the only committed change).
- **Commit:** `ab65b0d7`.

The HIDE logic itself executed exactly as written in the plan.

## Pending Human Verification

The plan's final task is `checkpoint:human-verify` (screenshot UAT). This CANNOT run inside a worktree and was NOT blocked on. It must be performed on a device/emulator (or browser build) after merge:

1. Reproduce the GAP-D state: a day with `conceptQuota === 0` (no new concepts due) but a populated curiosity feed (`dailyPosts.length > 0`).
2. Open the Home screen. EXPECT: **no** "Nothing new today" card above the populated feed (HIDE branch — the contradiction is gone).
3. Switch locale to zh / es / ja: EXPECT no regression — the empty-state copy is unchanged from before, so no new translation work and no missing-key fallback artifacts.
4. Force a genuinely empty feed (no posts): EXPECT the existing `home.feed.emptyTitle` / `emptyBody` message still renders.

Resume signal for the human: type "approved" or describe the remaining copy/layout issue.

## Self-Check: PASSED
