---
phase: 56-ui-polish-documentation
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - app/src/index.css
  - app/src/components/InfoFlow.tsx
  - app/src/components/trellis/TrellisStatusPanel.tsx
  - app/src/screens/PlannerScreen.tsx
  - app/src/components/Flashcard.tsx
  - app/src/components/CollectionPickerSheet.tsx
autonomous: true
requirements: [POLISH-01, POLISH-02]

must_haves:
  truths:
    - "Jank keyframes approved for simplification animate transform/opacity only (no box-shadow/filter on a per-frame path); NO new motion or micro-interactions are added (D-04 fix-janky-only)"
    - "index.css has a @media (prefers-reduced-motion: reduce) block disabling approved ambient loops"
    - "Approved hardcoded-hex color drift is replaced with the matching CSS-variable token; dark-mode parity holds"
    - "No Header ancestor gains transform/will-change/filter/contain/perspective"
    - "All load-bearing guard tests stay green"
  artifacts:
    - path: "app/src/index.css"
      provides: "Compositor-safe approved keyframes + prefers-reduced-motion block"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "app/src/index.css"
      to: "Android WebView compositor thread"
      via: "transform/opacity-only keyframes"
      pattern: "transform"
---

<objective>
Execute the operator-APPROVED POLISH-01 (visual) and POLISH-02 (animation) fixes from 56-TRIAGE.md. Fix-janky-only per D-04: simplify approved jank keyframes to compositor-safe transform/opacity, add the missing prefers-reduced-motion block, and replace approved hardcoded-hex with design-system tokens. Add NO new motion. Touch ONLY items marked APPROVED/ADDED in 56-TRIAGE.md.

Purpose: screens feel finished within the Android WebView budget without regressing the load-bearing layout/Header invariants.

Output: updated index.css, InfoFlow.tsx, TrellisStatusPanel.tsx, PlannerScreen.tsx, Flashcard.tsx, CollectionPickerSheet.tsx (only the approved subset of each).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-ui-polish-documentation/56-TRIAGE.md
@.planning/phases/56-ui-polish-documentation/56-UI-SPEC.md
@.planning/phases/56-ui-polish-documentation/56-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animation jank fixes + prefers-reduced-motion (POLISH-02)</name>
  <files>app/src/index.css, app/src/components/InfoFlow.tsx, app/src/components/trellis/TrellisStatusPanel.tsx</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md (which jank items are APPROVED and whether each is remove or simplify)
    - .planning/phases/56-ui-polish-documentation/56-PATTERNS.md (Jank Candidate fix patterns for glow-pulse, aha-pulse, glow-ring, status-glow; prefers-reduced-motion block pattern)
    - app/src/index.css (glow-pulse, aha-pulse, glow-ring keyframes; the sub-screen-in opacity-only comment warning)
    - app/src/components/trellis/TrellisStatusPanel.tsx (inline status-glow keyframe + its conditional usage)
    - app/src/components/InfoFlow.tsx (call sites that apply glow-pulse / aha-pulse / glow-ring to feed tiles)
    - CLAUDE.md § "Header positioning (Phase 32.1)" and § "Best practices learned in Phase 32.1" (box-shadow/overflow Android WebView bug class)
    - app/tests/layout/root-horizontal-clip.test.mjs
  </read_first>
  <action>
    For each jank keyframe the operator marked APPROVED in 56-TRIAGE.md, apply the chosen resolution:
    - simplify: rewrite the keyframe to animate transform and opacity ONLY, deleting box-shadow / filter / drop-shadow from its frames. glow-pulse → scale + opacity (drop the box-shadow ring). aha-pulse → scale + opacity ring simulation (drop box-shadow). glow-ring → opacity on a glow layer (drop filter: drop-shadow). status-glow (inline in TrellisStatusPanel.tsx) → transform: scale + opacity on the element (drop the box-shadow infinite loop).
    - remove: delete the animation reference at the call site and the keyframe if unused elsewhere.
    Add a single @media (prefers-reduced-motion: reduce) block at the end of the animation section of index.css that sets animation: none for the approved ambient loops (e.g. mic-pulse, glow-pulse, skeleton-pulse, status-glow, blink, vineLoadingPulse) per the PATTERNS prefers-reduced-motion pattern — match the framer-motion MasonryFeed reducedMotion="user" intent. Do NOT touch keyframes the operator did not approve. Do NOT add any new keyframe or motion. CRITICAL: do not add transform/will-change/filter/contain/perspective to any Header ancestor to "smooth" jank — fix at the animating element only (the sub-screen-in opacity-only comment is the warning).
  </action>
  <verify>
    <automated>cd app && grep -q "prefers-reduced-motion" src/index.css && node --test tests/layout/root-horizontal-clip.test.mjs tests/components/InfoFlow.video-tap-emit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - index.css contains a `@media (prefers-reduced-motion: reduce)` block covering the approved ambient loops.
    - Each APPROVED-simplify keyframe no longer contains box-shadow or filter in its frame bodies (source assertion per keyframe touched).
    - status-glow in TrellisStatusPanel.tsx (if approved) no longer animates box-shadow.
    - No Header ancestor gained transform/will-change/filter/contain/perspective (grep the touched files; the sub-screen-in opacity-only comment is preserved).
    - `cd app && node --test tests/layout/root-horizontal-clip.test.mjs tests/components/InfoFlow.video-tap-emit.test.mjs` passes.
    - Only operator-APPROVED keyframes/call-sites were changed.
  </acceptance_criteria>
  <done>Approved jank animations are compositor-safe or removed, prefers-reduced-motion exists, Header invariant intact, guard tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Visual color-token + spacing/hierarchy fixes (POLISH-01)</name>
  <files>app/src/screens/PlannerScreen.tsx, app/src/components/Flashcard.tsx, app/src/components/CollectionPickerSheet.tsx, app/src/components/InfoFlow.tsx</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md (which color/spacing/hierarchy findings are APPROVED; whether each hex is replace-with-token or accept-as-functional)
    - .planning/phases/56-ui-polish-documentation/56-UI-SPEC.md (Color tokens + reserved-for list, Spacing Scale allowed set, dark-mode parity check)
    - .planning/phases/56-ui-polish-documentation/56-PATTERNS.md (token-only rule; identified hex→token mappings)
    - app/src/index.css (:root token definitions and [data-theme="dark"] overrides — confirm the dark-mode value before swapping a hex for a token)
    - app/src/screens/PlannerScreen.tsx (lines ~214 '#4CAF50', ~264 '#66BB6A'), app/src/components/Flashcard.tsx (lines ~25-28 rating colors), app/src/components/CollectionPickerSheet.tsx (lines ~414/452 '#fff'), app/src/components/InfoFlow.tsx (lines ~392/608 '#f59e0b', ~734/752 '#ffffff')
    - CLAUDE.md § "Style Conventions" and the feed-tile simplicity preference note
  </read_first>
  <action>
    Apply ONLY the APPROVED visual findings from 56-TRIAGE.md. For each approved hex→token replacement, swap the hardcoded hex for the exact CSS-variable token recorded in triage (e.g. PlannerScreen '#4CAF50'/'#66BB6A' → var(--primary-40); InfoFlow '#f59e0b' amber dot → var(--secondary-40)); first confirm in index.css that the token's dark-mode override preserves intent before swapping. Leave hex the operator marked accept-as-functional (e.g. white-on-colored-background, Flashcard rating semantics) unchanged. Apply approved spacing fixes by replacing off-scale ad-hoc pixel values with the nearest --space-* token from the allowed set (4/8/12/16/20/24/32). Apply approved visual-hierarchy fixes per their triage notes. For any approved InfoFlow feed-tile finding, bias to SIMPLIFICATION (fewer signals) — never add chips/metadata. Do NOT introduce streaks/goals/nudges. Do NOT change ChatInput button sizes (≥44px hit area is load-bearing).
  </action>
  <verify>
    <automated>cd app && tsc -b --noEmit && node --test tests/components/ChatInput.flex-shrink.test.mjs tests/components/InfoFlow.video-tap-emit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Each APPROVED hex→token finding: the named hex is replaced by the recorded var(--…) token in the cited file (source assertion).
    - Accept-as-functional hex values are unchanged.
    - `tsc -b --noEmit` is clean.
    - `cd app && node --test tests/components/ChatInput.flex-shrink.test.mjs tests/components/InfoFlow.video-tap-emit.test.mjs` passes.
    - No new feed-tile chips/metadata added; no pushy mechanics introduced.
    - Only operator-APPROVED findings changed.
  </acceptance_criteria>
  <done>Approved color/spacing/hierarchy findings are fixed against the token system with dark-mode parity preserved; tsc and guard tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

CSS/visual fixes only. No auth, no network, no new data flow, no new attack surface introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05 | (none — no new attack surface) | index.css / feed-tile / planner visuals | accept | Pure presentational change. No trust boundary crossed. Invariant to preserve: Header portal-vs-in-tree (no transform/filter on Header ancestors) and the InfoFlow inline-play negative invariants (tests/components/InfoFlow.video-tap-emit.test.mjs stays green) — do not re-add an iframe/videoPlaying state while editing InfoFlow.tsx. |
</threat_model>

<verification>
- Per task: `cd app && node --test tests/layout/root-horizontal-clip.test.mjs tests/components/ChatInput.flex-shrink.test.mjs tests/components/SwipeTabContainer.resize-guard.test.mjs`.
- Plus `tests/components/InfoFlow.video-tap-emit.test.mjs` (touched file's negative invariants).
- Per-wave merge: `cd app && npm test && tsc -b --noEmit`.
</verification>

<success_criteria>
- All operator-APPROVED POLISH-01/02 fixes applied; nothing un-approved touched.
- index.css has prefers-reduced-motion; approved jank keyframes are compositor-safe; Header invariant intact.
- Full suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/56-ui-polish-documentation/56-03-SUMMARY.md` when done.
</output>
