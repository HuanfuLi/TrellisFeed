---
phase: 56-ui-polish-documentation
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, DOCS-01, DOCS-02]

must_haves:
  truths:
    - "The operator has reviewed every finding in 56-FINDINGS.md and marked it approve / cut / add (D-01 hybrid discovery — operator triages before any fix lands)"
    - "The operator has decided remove-vs-simplify for each jank animation finding (D-05 remove-vs-simplify is a per-animation operator decision, not a blanket rule)"
    - "The operator has approved a resolution direction for each DRIFTED CLAUDE.md claim"
    - "An approved-fix worklist exists that the fix waves execute from — and ONLY from"
  artifacts:
    - path: ".planning/phases/56-ui-polish-documentation/56-TRIAGE.md"
      provides: "Operator-approved worklist (approved finding IDs + per-jank decision + per-drift approved direction)"
      contains: "APPROVED"
  key_links:
    - from: "56-TRIAGE.md"
      to: "fix plans 03/04/05"
      via: "fix waves execute only IDs marked APPROVED here"
      pattern: "APPROVED"
---

<objective>
The operator triage gate. The agent presents the Wave-1 findings and drift report and records the operator's per-item decisions into `56-TRIAGE.md`. This is the hard gate D-01 mandates: fixes execute ONLY on items the operator approves here. It also captures the D-05 per-jank remove-vs-simplify choices and the D-09 per-drift approved resolution directions.

Purpose: agents fix only approved items; CLAUDE.md is never silently auto-corrected. This plan is `autonomous: false` — execution PAUSES for the operator.

Output: `56-TRIAGE.md` — the single source of truth for what the fix waves are allowed to touch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-ui-polish-documentation/56-FINDINGS.md
@.planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md
@.planning/phases/56-ui-polish-documentation/56-CONTEXT.md
@.planning/phases/56-ui-polish-documentation/56-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator triage of polish/animation/navigation/doc findings</name>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-FINDINGS.md
  </read_first>
  <decision>For each finding ID in 56-FINDINGS.md (F-V*, F-A*, F-N*, F-D*): approve, cut, or add a new item. For each jank animation finding (glow-pulse, aha-pulse, glow-ring, status-glow), choose remove vs simplify (D-05). For each doc finding, confirm archive-to-Legacy vs update vs keep-live (D-08).</decision>
  <context>D-01 hybrid discovery: the agent audits, the operator triages, agents fix only approved items. D-05 leaves remove-vs-simplify to the operator per animation — there is no blanket rule. D-08 archival is move-to-Legacy-never-delete. Present findings grouped by track; ask the operator to mark each. One question at a time per the AskUserQuestion constraint — batch as a single reviewable list, not multiple simultaneous question blocks.</context>
  <options>
    <option id="approve-all-recommended">
      <name>Approve all auditor-recommended findings as-proposed</name>
      <pros>Fastest; uses the auditor's compositor-safe defaults (simplify jank to transform/opacity, replace hex with tokens, archive the dated docs)</pros>
      <cons>Operator may want to cut low-value visual nits or keep an ambient glow as-is</cons>
    </option>
    <option id="selective">
      <name>Selective triage — operator marks each finding individually</name>
      <pros>Full control; honors D-01/D-05 intent precisely</pros>
      <cons>More operator time</cons>
    </option>
  </options>
  <resume-signal>Operator provides the approve/cut/add decisions and per-jank remove-vs-simplify choices; agent records them.</resume-signal>
  <acceptance_criteria>
    - Every finding ID from 56-FINDINGS.md has an explicit operator verdict recorded.
    - Every jank animation finding has a remove-or-simplify choice recorded.
    - Every doc finding has an archive / update / keep verdict recorded.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator approval of CLAUDE.md drift corrections (D-09)</name>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md
  </read_first>
  <decision>For each DRIFTED claim in 56-CLAUDE-DRIFT-REPORT.md (DR-01, DR-02, any C-* confirmed drift): approve the resolution direction (update stale doc vs treat as code regression) and the exact corrected wording, OR reject. No CLAUDE.md edit happens without per-item approval.</decision>
  <context>D-09: CLAUDE.md is high-stakes/load-bearing — confirm-first, no silent auto-correction. DR-01 (echolearn/SQLite brand-history note now stale post-IndexedDB migration) and DR-02 (yesterday-snapshot storage medium/key) are the two confirmed drifts; both are recommended as stale-doc-updates but the operator decides whether each is a doc fix or signals a code regression to investigate instead.</context>
  <options>
    <option id="approve-stale-doc">
      <name>Approve both as stale-doc updates (recommended)</name>
      <pros>The IndexedDB migration (Phase 55) is intentional; updating the doc to reference IndexedDB/'trellis' matches reality</pros>
      <cons>If the operator intended backwards-compat naming, this hides it</cons>
    </option>
    <option id="investigate-regression">
      <name>Flag one or more as possible code regression to investigate before any doc edit</name>
      <pros>Catches a real regression if the doc was actually correct</pros>
      <cons>Defers the doc correction; spawns a code-investigation task</cons>
    </option>
  </options>
  <resume-signal>Operator approves resolution direction + corrected wording for each drift, or rejects.</resume-signal>
  <acceptance_criteria>
    - Each DRIFTED claim has an operator-approved direction and (if update) approved corrected wording recorded in 56-TRIAGE.md.
    - No CLAUDE.md edit has occurred in this plan.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write 56-TRIAGE.md approved worklist</name>
  <files>.planning/phases/56-ui-polish-documentation/56-TRIAGE.md</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-FINDINGS.md
    - .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md
  </read_first>
  <action>
    Record the operator's decisions from Tasks 1 and 2 into 56-TRIAGE.md as the single approved worklist the fix waves execute from. Structure: an APPROVED section listing each approved finding ID with its track (POLISH-01/02/03 or DOCS-01) and, for jank items, the chosen remove/simplify resolution; a CUT section listing cut IDs (for traceability); an ADDED section for any operator-added items with file:line and intent; and a CLAUDE.md CORRECTIONS section listing each approved drift with its approved corrected wording. Mark every line APPROVED / CUT / ADDED so the fix waves can mechanically filter.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-ui-polish-documentation/56-TRIAGE.md && grep -q "APPROVED" .planning/phases/56-ui-polish-documentation/56-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - 56-TRIAGE.md exists with APPROVED / CUT / ADDED / CLAUDE.md CORRECTIONS sections.
    - Each approved jank finding records remove vs simplify.
    - Each approved CLAUDE.md drift records the operator-approved corrected wording.
    - No app/src or CLAUDE.md edits in this plan.
  </acceptance_criteria>
  <done>56-TRIAGE.md is the operator-approved worklist; fix waves 03/04/05 execute only its APPROVED/ADDED items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Operator-interaction + report-writing plan. No application code, network, or data-flow change. No new trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03 | Repudiation | Fix-scope authorization | mitigate | 56-TRIAGE.md is the auditable record of exactly what the operator approved; fix waves must filter to APPROVED/ADDED IDs only, so an un-approved change is traceable as out-of-scope. |
| T-56-04 | Tampering | CLAUDE.md load-bearing claims | mitigate | D-09 confirm-first gate: no CLAUDE.md edit without a per-item operator approval recorded here; prevents silent erosion of a load-bearing/security-adjacent claim (e.g. Detector D origin allowlist). |
</threat_model>

<verification>
- Load-bearing guard tests remain green (no source changed): `cd app && node --test tests/layout/root-horizontal-clip.test.mjs tests/components/ChatInput.flex-shrink.test.mjs tests/components/SwipeTabContainer.resize-guard.test.mjs`.
- 56-TRIAGE.md exists and carries the APPROVED marker.
</verification>

<success_criteria>
- Operator has triaged every finding and approved each CLAUDE.md drift direction.
- 56-TRIAGE.md is the gating worklist for all fix waves.
</success_criteria>

<output>
Create `.planning/phases/56-ui-polish-documentation/56-02-SUMMARY.md` when done.
</output>
