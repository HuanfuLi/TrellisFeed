---
phase: 54-code-quality-bugs-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md
autonomous: true
requirements: [TECHDEBT-13]
must_haves:
  truths:
    - "A scored severity x reach tech-debt inventory enumerates all v1.4-v1.6 candidate debt"
    - "Each item has a FIX / RE-ACCEPT / NOTE-ONLY disposition with a documented rationale"
    - "The top tier (score >= 12) is clearly identified for resolution in the follow-on plan"
  artifacts:
    - path: ".planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md"
      provides: "Scored severity x reach matrix of accumulated tech debt (D-01/D-02 deliverable)"
      contains: "Severity"
  key_links:
    - from: "54-TECH-DEBT-INVENTORY.md"
      to: "Plan 54-04 resolution"
      via: "top-tier FIX items are the resolution worklist"
      pattern: "FIX"
---

<objective>
Produce the TECHDEBT-13 deliverable (D-01/D-02): a scored severity × reach inventory of accumulated v1.4–v1.6 tech debt. Score every candidate, assign each a FIX / RE-ACCEPT / NOTE-ONLY disposition with a documented rationale, and identify the top tier that Plan 04 will resolve.

This plan is the inventory itself — a planning artifact, not code. Resolution of the top tier happens in Plan 54-04 (which depends on this inventory existing).

Purpose: A prioritized, scored debt ledger so the top tier is resolved deliberately and the rest is formally re-accepted rather than silently carried.
Output: `.planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-CONTEXT.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-PATTERNS.md

# Raw material for scoring
@.planning/codebase/CONCERNS.md
# Format analog — the prior dead-code sweep table conventions
@.planning/milestones/v1.5-phases/45-code-quality-sweep/45-DEAD-CODE-SWEEP.md

<interfaces>
<!-- Scoring rubric (Claude's discretion per D-01; recommended in PATTERNS.md) -->
- Severity: 1=cosmetic, 2=dev experience, 3=data-correctness risk, 4=user-facing bug risk, 5=data loss/security.
- Reach: 1=one file, 2=one subsystem, 3=multiple screens, 4=all users all flows, 5=persistent/irreversible.
- Score = Severity × Reach. Tiers: FIX >= 12; RE-ACCEPT 6-11; NOTE-ONLY <= 5.
- Table columns: # | Item | File(s) | Severity (1-5) | Reach (1-5) | Score | Decision (with rationale).

<!-- Verified call-site counts (live app/src as of 2026-05-20) -->
- usePlanner (state/usePlanner.ts:19): 0 live call sites — DISTINCT from the LIVE usePlannerAutoGen. @deprecated Phase 26 D-22. FIX (delete).
- ConnectionPostScreen (screens/ConnectionPostScreen.tsx:27): 0 call sites, not in App.tsx routes. FIX (delete).
- recordFeedView (trajectoryAnalyzer.service.ts:63): 0 call sites BUT trajectoryAnalyzer.service.ts itself is imported by plannerAutoGen.service.ts (service is live; only this export is dead). Disposition pending operator decision (RESEARCH.md OQ#1) — flag as DECISION, not auto-FIX.
- scheduler.service.ts console.log: 7 instances; scheduler.native.ts console.log: 2 instances — violate eslint no-console (allow only warn/error). FIX (convert to console.warn).
- Stale eslint-disable PodcastScreen.tsx:102: FIX (trivial removal).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the scored severity x reach tech-debt inventory (TECHDEBT-13, D-01/D-02)</name>
  <read_first>
    - .planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md (§Tech Debt Inventory — the full raw candidate list with severity/reach estimates)
    - .planning/phases/54-code-quality-bugs-tech-debt/54-PATTERNS.md (scoring rubric + raw items to score + format analog)
    - .planning/codebase/CONCERNS.md (D-03 candidates: hybrid SQLite/localStorage, heavy mocking, CapacitorHttp streaming, theme-transition, SQLite serialization, localStorage quota)
    - .planning/milestones/v1.5-phases/45-code-quality-sweep/45-DEAD-CODE-SWEEP.md (table-format analog + Phase 45 deferred symbols)
  </read_first>
  <action>
    Create `.planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md` as a scored table using the rubric in the interfaces block. Enumerate AT MINIMUM every candidate RESEARCH.md lists, grouped as in RESEARCH.md §Tech Debt Inventory:
    - CONCERNS.md / D-03 candidates: hybrid SQLite/localStorage dual-backend, heavy service mocking, CapacitorHttp streaming fragility on Android, theme-transition coordination, SQLite encoding/serialization, localStorage quota, storage-key naming drift in debug docs.
    - Phase 45 deferred dead-code symbols (11): recordFeedView, replaceBlossomDates, recordStructuralSignalPatch, hasReorgBackup, revertReorganization, getMoveDestination, usePlanner, useTodayQuestions, ConnectionPostScreen, nanoBananaProvider, cancelNativeNotifications, InlineInfoFlow.
    - v1.6-introduced debt: scheduler.service.ts/native.ts console.log (lint), stale eslint-disable at PodcastScreen.tsx:102, large 4.5MB trellis-bg-default.png, 1.29MB index.js chunk, missing recordFeedView call sites.
    - Architecture/design debt (lower priority): purgeExpired only at HomeScreen mount, stateless dailyReadService reads, post-queue module-singleton, generateMorePosts empty-swipe edge, API keys in plaintext localStorage.

    Score each on severity (1-5) × reach (1-5). Assign a Decision per the tiers: FIX (>=12), RE-ACCEPT with documented rationale (6-11), NOTE-ONLY (<=5). Per D-02, every below-top-tier item gets an explicit re-acceptance rationale, not just a number. Confirm each dead-code call-site count by grep before assigning FIX-delete (the interfaces block has verified counts as of 2026-05-20).

    For items whose disposition depends on operator input (notably recordFeedView / trajectoryAnalyzer fate — RESEARCH.md OQ#1, and the scheduler-log preference — OQ#2), mark the Decision column as DECISION-PENDING and note that Plan 54-04 carries a `checkpoint:decision` to resolve them. Do NOT pre-decide those.

    Add a short "Top Tier (FIX) worklist" section at the end listing exactly which items Plan 04 must resolve, so the follow-on plan has an unambiguous worklist.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md && grep -qi "Severity" .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md && grep -qi "usePlanner" .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md && echo INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md` exists.
    - It is a scored table with Severity, Reach, Score, and Decision columns.
    - Every RESEARCH.md candidate appears (grep finds at least: usePlanner, ConnectionPostScreen, recordFeedView, "console.log" or scheduler, "SQLite", "localStorage quota", and the Phase 45 symbols).
    - Each item has a FIX / RE-ACCEPT / NOTE-ONLY / DECISION-PENDING decision; every RE-ACCEPT has a written rationale.
    - A "Top Tier (FIX) worklist" section lists the items Plan 04 will resolve.
    - recordFeedView and the scheduler-log preference are marked DECISION-PENDING (not pre-decided).
  </acceptance_criteria>
  <done>The scored inventory enumerates all candidate debt with severity×reach scores and per-item dispositions, and names the top-tier FIX worklist for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundary. This plan writes a single planning markdown document and runs read-only grep verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | N/A | (inventory document) | accept | No high/medium threats. The deliverable is a planning artifact; it adds no attack surface, no I/O boundary, and no untrusted input path. |
</threat_model>

<verification>
- `54-TECH-DEBT-INVENTORY.md` exists, is a scored severity × reach table, enumerates all RESEARCH.md candidates, and names a top-tier FIX worklist.
</verification>

<success_criteria>
- TECHDEBT-13 (inventory half): accumulated v1.4–v1.6 tech debt is inventoried and prioritized via a scored matrix, with each item's disposition documented. Resolution of the top tier is delegated to Plan 54-04.
</success_criteria>

<output>
Create `.planning/phases/54-code-quality-bugs-tech-debt/54-03-SUMMARY.md` when done.
</output>
