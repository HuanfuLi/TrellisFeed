---
phase: 54-code-quality-bugs-tech-debt
plan: 04
type: execute
wave: 2
depends_on: ["54-03"]
files_modified:
  - app/src/state/usePlanner.ts
  - app/src/screens/ConnectionPostScreen.tsx
  - app/src/services/scheduler.service.ts
  - app/src/services/scheduler.native.ts
  - app/src/screens/PodcastScreen.tsx
  - app/src/services/trajectoryAnalyzer.service.ts
  - .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md
autonomous: false
requirements: [TECHDEBT-13]
must_haves:
  truths:
    - "Confirmed-dead usePlanner and ConnectionPostScreen are deleted with 0 remaining references"
    - "scheduler console.log lint warnings are resolved (converted to an allowed console method)"
    - "The stale PodcastScreen eslint-disable is removed"
    - "The recordFeedView / trajectoryAnalyzer fate and scheduler-log preference are resolved by an operator decision and applied"
    - "Full suite + tsc remain green after the deletions"
  artifacts:
    - path: ".planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md"
      provides: "Updated inventory with the resolved top-tier items and the operator decision recorded"
  key_links:
    - from: "deletions"
      to: "tsc + full suite"
      via: "no dangling import after removal"
      pattern: "tsc -b --noEmit"
---

<objective>
Resolve the top tier of the TECHDEBT-13 inventory (D-01: resolve the top tier regardless of theme). Delete confirmed-dead code, clear the scheduler lint warnings and the stale eslint-disable, and resolve the two operator-dependent items (recordFeedView / trajectoryAnalyzer fate; scheduler-log preference) via a decision checkpoint. Below-top-tier items stay in the inventory, formally re-accepted (handled in Plan 03), and are NOT touched here.

Purpose: Pay down the high-priority, confirmed-dead debt deliberately, keeping the suite + tsc green and not regressing any load-bearing invariant.
Output: Deleted dead files, lint-clean scheduler logging, an updated inventory recording the resolutions + the operator decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-CONTEXT.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-PATTERNS.md
# The scored inventory from Plan 03 — drives the worklist
@.planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md

<interfaces>
<!-- Verified facts (live app/src, 2026-05-20). Re-grep before each deletion to confirm still 0. -->
- usePlanner: state/usePlanner.ts:19 — @deprecated Phase 26 D-22, 0 call sites. NOTE: usePlannerAutoGen (state/usePlannerAutoGen.ts) is LIVE and DISTINCT — do not touch it.
- ConnectionPostScreen: screens/ConnectionPostScreen.tsx:27 — 0 call sites, not routed in App.tsx. Verify it exports no type/util consumed elsewhere before deleting.
- scheduler.service.ts console.log: lines 76,84,113,137,172,178,190 (7). scheduler.native.ts: lines 106,111 (2). eslint rule (eslint.config.js:41): allow only ['warn','error']. console.info still warns → use console.warn.
- PodcastScreen.tsx:102 — stale `// eslint-disable-next-line @typescript-eslint/no-unused-vars` above a destructure that already uses the underscore-prefix exemption. Delete the disable line only.
- trajectoryAnalyzer.service.ts: imported by plannerAutoGen.service.ts (service LIVE); only recordFeedView export (line 63) is dead. tests/services/trajectoryAnalyzer.test.mjs uses a pure-helper mirror and does NOT import recordFeedView — deleting recordFeedView needs no test change.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision: resolve recordFeedView fate + scheduler logging preference</name>
  <decision>Resolve the two operator-dependent inventory items: (A) the fate of `recordFeedView` / the trajectory-analytics export, and (B) the scheduler logging preference.</decision>
  <context>
    Plan 03's inventory marks these DECISION-PENDING. They cannot be auto-resolved because they depend on product intent, not code facts.

    (A) `recordFeedView` in `trajectoryAnalyzer.service.ts:63` has zero call sites, but the surrounding service is live (imported by `plannerAutoGen.service.ts`). It may be (i) abandoned dead code, or (ii) a planned analytics seam for the Phase 55 recommendation/feed work (TUNE-02). RESEARCH.md OQ#1 flagged this for operator input.

    (B) `scheduler.service.ts` / `scheduler.native.ts` emit 9 `console.log` calls that violate the no-console lint rule (only warn/error allowed). They are useful device-debugging diagnostics for podcast trigger timing. RESEARCH.md OQ#2.
  </context>
  <options>
    <option id="a-delete-warn">
      <label>A: delete recordFeedView (+ its dead-only storage keys if unused) · B: convert scheduler console.log → console.warn</label>
      <pros>Removes confirmed-dead export now; clears all 9 scheduler lint warnings while preserving the diagnostic value via console.warn.</pros>
      <cons>If trajectory analytics IS planned for Phase 55, the export must be re-added then.</cons>
    </option>
    <option id="b-keep-warn">
      <label>A: keep recordFeedView, re-accept with a "planned for Phase 55 TUNE-02" rationale · B: console.log → console.warn</label>
      <pros>Preserves the analytics seam if Phase 55 will wire it; still clears scheduler lint.</pros>
      <cons>Leaves a dead export carried into Phase 55 with no current consumer.</cons>
    </option>
    <option id="c-delete-silence">
      <label>A: delete recordFeedView · B: silence scheduler logs entirely (remove the console.log calls)</label>
      <pros>Cleanest production logging; smallest log surface.</pros>
      <cons>Loses the device-debug diagnostics the operator may rely on for podcast timing.</cons>
    </option>
  </options>
  <resume-signal>Select: a-delete-warn, b-keep-warn, or c-delete-silence (and note any per-item override).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Delete confirmed-dead code and clear the scheduler + eslint-disable warnings per the inventory + decision</name>
  <read_first>
    - .planning/phases/54-code-quality-bugs-tech-debt/54-TECH-DEBT-INVENTORY.md (the Top Tier FIX worklist from Plan 03)
    - app/src/state/usePlanner.ts (deprecated header; deletion target)
    - app/src/state/usePlannerAutoGen.ts (the LIVE distinct hook — confirm you are NOT deleting this)
    - app/src/screens/ConnectionPostScreen.tsx (unrouted; check for any unique exported type/util before deleting)
    - app/src/App.tsx (confirm no ConnectionPostScreen route)
    - app/src/services/scheduler.service.ts and app/src/services/scheduler.native.ts (console.log call sites)
    - app/src/screens/PodcastScreen.tsx (stale eslint-disable at ~line 102)
    - app/src/services/trajectoryAnalyzer.service.ts (recordFeedView at ~line 63; only if the decision is delete)
    - app/eslint.config.js (the no-console allow list)
  </read_first>
  <action>
    Apply the Top Tier FIX worklist from the inventory, plus the checkpoint decision:

    1. Re-grep to confirm `usePlanner` still has 0 call sites in `app/src` + `app/tests` (excluding usePlannerAutoGen matches), then delete `app/src/state/usePlanner.ts` and any now-orphaned type-only export it owned. Use `\busePlanner\b` word-boundary grep so `usePlannerAutoGen` does not match.

    2. Re-grep to confirm `ConnectionPostScreen` has 0 references and no App.tsx route, verify it owns no type/util consumed elsewhere, then delete `app/src/screens/ConnectionPostScreen.tsx`.

    3. Per checkpoint part B: replace the 9 scheduler `console.log` calls (scheduler.service.ts lines 76,84,113,137,172,178,190 + scheduler.native.ts 106,111) with `console.warn` (option a/b) OR remove them (option c). console.info is NOT lint-allowed — never use it.

    4. Delete the stale `// eslint-disable-next-line @typescript-eslint/no-unused-vars` at PodcastScreen.tsx:102 (the underscore-prefixed destructure is already exempt by config). Do not change the destructure itself.

    5. Per checkpoint part A: if the decision is delete, remove `recordFeedView` from trajectoryAnalyzer.service.ts AND any storage key (e.g. FEED_VIEWS_KEY) used ONLY by it — re-grep each key to confirm it has no other reader before removing. If the decision is keep, add a one-line rationale comment at the export ("retained for Phase 55 TUNE-02 recommendation wiring") and leave it. Do NOT touch the live parts of trajectoryAnalyzer.service.ts that plannerAutoGen.service.ts depends on.

    Per RESEARCH.md Pitfall 2: before deleting any symbol, confirm (a) no test asserts its presence, (b) CLAUDE.md does not mention it as load-bearing, (c) it is not referenced in debug/resolved/ as an important path. None of usePlanner/ConnectionPostScreen/recordFeedView are load-bearing per the call-site audit, but re-verify.

    Finally update `54-TECH-DEBT-INVENTORY.md`: flip the resolved items' Decision cells from FIX/DECISION-PENDING to RESOLVED (with the commit-level note) and record the operator's checkpoint selection.
  </action>
  <verify>
    <automated>cd app && tsc -b --noEmit && npm test && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && tsc -b --noEmit` exits 0 (no dangling import after deletions).
    - `cd app && npm test` exits 0 (full suite green).
    - `grep -rn "\busePlanner\b" app/src app/tests` returns no matches (file deleted; usePlannerAutoGen unaffected).
    - `grep -rn "ConnectionPostScreen" app/src app/tests` returns no matches.
    - `grep -c "console\.log" app/src/services/scheduler.service.ts app/src/services/scheduler.native.ts` returns 0 for both files.
    - `grep -n "eslint-disable-next-line @typescript-eslint/no-unused-vars" app/src/screens/PodcastScreen.tsx` returns no match.
    - `cd app && npm run lint` reports fewer warnings than the 28-warning baseline AND 0 errors.
    - `app/src/state/usePlannerAutoGen.ts` and `app/src/screens/PlannerScreen.tsx` are unchanged (`git diff --name-only` does not list them).
    - The inventory's resolved items are marked RESOLVED and the operator decision is recorded.
  </acceptance_criteria>
  <done>The top-tier dead code is deleted, scheduler/eslint warnings are cleared per the operator decision, suite + tsc are green, and the inventory records the resolutions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan deletes dead code and adjusts logging; it adds no external input path or I/O surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | N/A | (dead-code deletion) | accept | No high/medium threats. The only risk is accidentally deleting a load-bearing or live symbol during cleanup. |
| T-54-04-DEL | Tampering | usePlanner / ConnectionPostScreen / recordFeedView deletion | mitigate | Word-boundary re-grep before each deletion (confirms 0 references and that the LIVE usePlannerAutoGen / live trajectoryAnalyzer paths are untouched); tsc + full suite must stay green; Pitfall-2 load-bearing check (test/CLAUDE.md/debug-resolved) before removal. |
</threat_model>

<verification>
- `cd app && tsc -b --noEmit` and `cd app && npm test` both exit 0.
- `cd app && npm run lint` shows 0 errors and fewer warnings than the 28-warning baseline.
- usePlanner and ConnectionPostScreen have 0 remaining references; usePlannerAutoGen + PlannerScreen unchanged.
- The inventory records the resolved items and the operator decision.
</verification>

<success_criteria>
- TECHDEBT-13 (resolution half): the top tier of the scored inventory is resolved (confirmed-dead code deleted, scheduler/eslint warnings cleared), the operator-dependent items are decided and applied, and below-top-tier items remain re-accepted in the inventory. Suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/54-code-quality-bugs-tech-debt/54-04-SUMMARY.md` when done.
</output>
