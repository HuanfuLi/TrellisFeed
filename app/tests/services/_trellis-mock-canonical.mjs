/**
 * Stub for canonical-knowledge.service.ts in trellis tests.
 * Returns empty tree so buildTrellisState can run without the full dep chain.
 */

export function buildAnchorReflectionTree() {
  return [];
}
