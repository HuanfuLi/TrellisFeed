---
phase: 35-fix-the-dynamic-system-prompt-issue
verified: 2026-04-29
status: in-progress
score: 0/8 must-haves checked (status flips to verified during phase close-out as Plans 01/02/03 land)
re_verification: false
gaps: []
---

# Phase 35 Verification

**Phase:** 35-fix-the-dynamic-system-prompt-issue
**Status:** in-progress (status flips to `verified` during phase close-out as plans 01/02/03 land)
**Audited:** 2026-04-29

## Scope confirmation

Phase 35's code-modifying scope is strictly `app/src/state/useQuestions.ts` `askStreaming` Pass 1 + Pass 2 per CONTEXT.md decision **D-05**. This document is the project-wide receipt that the scope decision was correct: every other `chatStream` / `chatCompletion` call site in the codebase is intentionally one-shot (no multi-turn KV-cache prefix to protect) or is the Phase 23 multi-step classification descent which is already append-only by construction.

A future contributor inspecting any non-`useQuestions` chatStream/chatCompletion call site can find the audit row, see the one-shot classification + cost-benefit reasoning, and avoid wasting time "consistency-fixing" it.

## Project-wide chatStream / chatCompletion audit

Enumeration source (re-runnable):

```bash
grep -rnE "chatStream\(|chatCompletion\(" app/src --include="*.ts" --include="*.tsx" | sort
```

The grep returned **28 lines across 14 files** on 2026-04-29 (HEAD `10c40a32`). Two of those lines are the function declarations in `app/src/providers/llm/index.ts:61` and `:71` (the API surface itself, not call sites) so the effective call-site count is **26**.

### Primary audit table

| #   | Site                                                  | File:Line                                            | Classification                                | Why                                                                                                                                                                                                                                                | Phase 35 action                                                                                                                                                |
| --- | ----------------------------------------------------- | ---------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `useQuestions.ts` `askStreaming` Pass 1               | `app/src/state/useQuestions.ts:175`                  | **multi-turn / append-only (IN SCOPE)**       | History spread (`...historyMessages` line 178) carries prior conversation; KV-cache prefix coverage requires byte-stable system prompt. Static `systemPrompt` constructed at line 146 contains identity + safety + `WEB_SEARCH_TOOL_PROMPT` only. | **REFACTORED — Plan 01 LANDED.** Dynamic candidate-context lives in tail `assistantContextMessage` (declared line 162, used at line 179). Source-readable in HEAD. |
| 2   | `useQuestions.ts` `askStreaming` Pass 2 (web search)  | `app/src/state/useQuestions.ts:235`                  | **multi-turn / append-only (IN SCOPE)**       | Same `askStreaming` closure; Pass 1's prefix is still warm in provider cache when Pass 2 fires moments later. Spread of `...historyMessages` at line 238 + same `assistantContextMessage` at line 239.                                              | **REFACTORED — Plan 01 LANDED.** Same closure variable used at the same array position as Pass 1 — Pass 1 → Pass 2 prefix continuity preserved (D-02).         |
| 3   | `AskScreen.tsx` session-title generation              | `app/src/screens/AskScreen.tsx:86`                   | **one-shot**                                  | `[system, user]` only; produces a single chat-session title from `userMessage` + `aiReply.slice(0, 400)`; no history; result is a string saved to `ChatSession.title`.                                                                          | **NO CHANGE.** No multi-turn cache benefit; cost-benefit doesn't justify test-and-refactor.                                                                    |
| 4   | `concept-feed.service.ts` text-art headline gen       | `app/src/services/concept-feed.service.ts:706`       | **one-shot**                                  | `[{ role: 'user', content: prompt }]` only — no system message, no history. Generates a single headline string per text-art post.                                                                                                                | **NO CHANGE.** Out of scope; one-shot.                                                                                                                         |
| 5   | `concept-feed.service.ts` daily post batch generation | `app/src/services/concept-feed.service.ts:937`       | **one-shot**                                  | `[system, user]` only; system prompt has byte-stable editorial directive; user content is the per-batch `buildGenerationPrompt(...)` which IS dynamic (per-day knowledge context) but call is one-shot so no cache prefix to protect.            | **NO CHANGE.** Dynamic content in user message, not system. One-shot — no multi-turn KV cache benefit. (Deferred per CONTEXT.md `<deferred>` rationale.)       |
| 6   | `concept-feed.service.ts` suggestion topics gen       | `app/src/services/concept-feed.service.ts:1022`      | **one-shot**                                  | `[system, user]` only; per-post LLM call to expand suggestion-card topics. System prompt static; user prompt dynamic but one-shot.                                                                                                                | **NO CHANGE.** One-shot.                                                                                                                                       |
| 7   | `concept-feed.service.ts` session-post batch          | `app/src/services/concept-feed.service.ts:1732`      | **one-shot**                                  | `[system, user]` only; system prompt static editorial directive; user prompt is the session-context generation prompt with `avoidClause` interpolation. One-shot per Ask session close.                                                          | **NO CHANGE.** Dynamic content in user message; one-shot.                                                                                                      |
| 8   | `concept-feed.service.ts` connection-essay stream     | `app/src/services/concept-feed.service.ts:1875`      | **one-shot**                                  | `yield* chatStream([system, user], ...)`; `system` and `prompt` constructed inline per call; no history.                                                                                                                                          | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 9   | `concept-feed.service.ts` discover-essay stream       | `app/src/services/concept-feed.service.ts:1993`      | **one-shot**                                  | `yield* chatStream([system, user], ...)`; same shape as #8 for "discover concept" essay generation.                                                                                                                                                | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 10  | `planner.service.ts` `extractSignals`                 | `app/src/services/planner.service.ts:263`            | **one-shot**                                  | `[system, user]` only; `EXTRACT_SIGNALS_PROMPT` is a module-level const (already byte-stable); user message is the per-checkin `content`. One LLM call per check-in.                                                                              | **NO CHANGE.** One-shot. System prompt is already a const so even if this WERE multi-turn it would be byte-stable, but it isn't multi-turn.                    |
| 11  | `planner.service.ts` `generateDiscoverTitle`          | `app/src/services/planner.service.ts:372`            | **one-shot**                                  | `[system, user]` only; per-discover-card title generation. Topic interpolated into user message, not system.                                                                                                                                       | **NO CHANGE.** One-shot.                                                                                                                                       |
| 12  | `podcast.service.ts` daily podcast script gen         | `app/src/services/podcast.service.ts:204`            | **one-shot**                                  | `[system, user]` only; system prompt is a static literal ("Write a 90-second spoken podcast recap..."); user message is the per-day question summary lines.                                                                                       | **NO CHANGE.** One-shot.                                                                                                                                       |
| 13  | `post-essay.service.ts` `generateEssayMeta`           | `app/src/services/post-essay.service.ts:46`          | **one-shot**                                  | `[system, user]` only; non-streaming follow-up after `bodyMarkdown` streaming completes. Generates `whyCare` / `takeaway` / `quickAskPrompts` JSON.                                                                                                | **NO CHANGE.** One-shot.                                                                                                                                       |
| 14  | `post-essay.service.ts` `generateStandardEssay`       | `app/src/services/post-essay.service.ts:88`          | **one-shot**                                  | `yield* chatStream([system, user], ...)`; static system prompt, dynamic per-post user content. One stream per post-detail open.                                                                                                                   | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 15  | `post-essay.service.ts` `generateVideoEssay`          | `app/src/services/post-essay.service.ts:117`         | **one-shot**                                  | `yield* chatStream([system, user], ...)`; static system prompt, dynamic per-video user content (transcript or description fallback). One stream per video-post open.                                                                              | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 16  | `post-essay.service.ts` `generateNewsEssay`           | `app/src/services/post-essay.service.ts:146`         | **one-shot**                                  | `yield* chatStream([system, user], ...)`; static system prompt, dynamic per-news-item user content (Tavily snippets). One stream per news-post open. Deferred-to-streamer pattern (Phase 32.1 CLAUDE.md "News post pipeline").                       | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 17  | `post-essay.service.ts` `generateTextArtEssay`        | `app/src/services/post-essay.service.ts:169`         | **one-shot**                                  | `yield* chatStream([system, user], ...)`; static system prompt for "social media educator" voice; dynamic per-post user content. One stream per text-art post open.                                                                              | **NO CHANGE.** One-shot streaming.                                                                                                                             |
| 18  | `post-context-qa.service.ts` `askStreaming`           | `app/src/services/post-context-qa.service.ts:52`     | **one-shot**                                  | `yield* chatStream([system, user], ...)`; system prompt is a static one-liner ("You are a contextual learning companion..."); user content is `buildContextPrompt(context, userQuestion)` — single Q per post-detail Ask invocation, no history threaded. | **NO CHANGE.** One-shot streaming. Note: this is post-detail follow-up Q&A, distinct from chat Ask in `useQuestions.askStreaming`.                              |
| 19  | `flashcard.service.ts` flashcard extraction           | `app/src/services/flashcard.service.ts:199`          | **one-shot**                                  | `[system, user]` only; system prompt is the static "knowledge extraction assistant" directive; user message is the session transcript. One LLM call per session-close.                                                                            | **NO CHANGE.** One-shot.                                                                                                                                       |
| 20  | `question.service.ts` legacy `ask` (non-streaming)    | `app/src/services/question.service.ts:230`           | **multi-turn shape — but DEAD-CODE-ish**      | Spreads `...historyMessages` at line 233. `useQuestions.ts:82` calls it via the legacy `ask` callback, but **AskScreen uses `askStreaming` exclusively** (per `AskScreen.tsx:15` comment + grep on `screens/`). The legacy `ask` callback is exposed in `useQuestions` return at line 341 but no screen calls it.                                                                                          | **NO CHANGE — see Deferred section.** Path is currently unreachable from any screen. If reactivated, apply Phase 35 discipline. Documented as deferred.        |
| 21  | `canonical-knowledge.service.ts` reorg LLM call       | `app/src/services/canonical-knowledge.service.ts:1652` | **one-shot (with auto-retry continuation)** | `[system, user]` initial call; on parse failure ONE retry with `[...messagesForReorg, assistant: raw, user: feedback]` (line 1668). The retry IS append-only relative to the first call — but it's a single retry, not a multi-turn conversation. | **NO CHANGE.** Append-only retry pattern is already correct (mirrors Phase 23 discipline at smaller scale).                                                    |
| 22  | `canonical-knowledge.service.ts` reorg retry          | `app/src/services/canonical-knowledge.service.ts:1668` | **append-only continuation**                  | Same `messagesForReorg` array + `[assistant: raw, user: feedback]` appended — pure prefix extension of #21. System prompt at index 0 is byte-identical to #21.                                                                                    | **NO CHANGE.** Already append-only.                                                                                                                            |
| 23  | `canonical-knowledge.service.ts` legacy `classifyAndAnchor` | `app/src/services/canonical-knowledge.service.ts:1223` | **one-shot (legacy fallback)**            | `[system, user]` only; system prompt has dynamic `treeContext` interpolation but call is one-shot. Used as the fallback when `classifyAndAnchorIncremental` fails (line 1059, 1100, 1108, 1124, 1159, 1187).                                       | **NO CHANGE.** One-shot fallback.                                                                                                                              |
| 24  | `question-filter.service.ts` `isOffTopicByLLM`        | `app/src/services/question-filter.service.ts:81`     | **one-shot**                                  | `[system, user]` only; system prompt has dynamic `contextInstructions` (prior-question/answer hint) interpolated when present. One LLM call per question-asked event.                                                                              | **NO CHANGE.** One-shot. Note: dynamic system content is intentional — instruction varies per call by design.                                                  |
| 25  | `youtube.service.ts` video-summary fallback           | `app/src/services/youtube.service.ts:374`            | **one-shot**                                  | `[system, user]` only; system prompt static; user message is video title + transcript-or-description. One call per video-fetch with-no-cached-summary.                                                                                            | **NO CHANGE.** One-shot.                                                                                                                                       |
| 26  | `providers/llm/index.ts` `testLLMConnection`          | `app/src/providers/llm/index.ts:364`                 | **one-shot**                                  | `[{ role: 'user', content: 'Say "ok".' }]` only — no system message at all. Used by Settings → Test Connection.                                                                                                                                   | **NO CHANGE.** One-shot.                                                                                                                                       |

> **Audit row count: 26.** CONTEXT.md D-05 named 8 sites as the floor; the grep enumeration found 18 additional one-shot sites (all extras welcome — they reinforce the scope decision). No row contains a placeholder; every row cites a concrete file:line from the grep output.
>
> **Coverage check vs. CONTEXT.md D-05 (the named eight):**
>
> 1. `useQuestions.ts` ✓ rows 1-2
> 2. `concept-feed.service.ts` ✓ rows 4-9 (six call sites — D-05 said "multiple", confirmed)
> 3. `planner.service.ts` ✓ rows 10-11 (two call sites — D-05 said "one-shot", confirmed)
> 4. `podcast.service.ts` ✓ row 12
> 5. `post-essay.service.ts` ✓ rows 13-17 (five call sites — D-05 said "one-shot", confirmed)
> 6. `post-context-qa.service.ts` ✓ row 18
> 7. `flashcard.service.ts` ✓ row 19
> 8. `canonical-knowledge.service.ts` ✓ rows 21-23 + the incremental-pipeline descent rows in the subsidiary table below
> 9. `AskColumn.tsx:86` ✓ row 3 (CONTEXT.md cites it as `AskScreen.tsx:86`)

### Subsidiary audit: `canonical-knowledge.service.ts` classification descent (Phase 23 append-only)

The classification descent in `classifyAndAnchorIncremental` (lines 1007-1192) is the only **multi-step LLM pipeline** in the codebase outside of the Ask chat. Phase 23 established the append-only pattern there. Phase 35 verifies the pattern is intact:

| Step                | File:Line                                                | Append-only? | Notes                                                                                                                                                                                                                                                                                                       |
| ------------------- | -------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| init                | `app/src/services/canonical-knowledge.service.ts:1041-1044` | n/a          | `messages: PipelineMessage[]` initialized with `[{role: 'system', content: PIPELINE_SYSTEM_PROMPT}, {role: 'user', content: '<question>'}]`. `PIPELINE_SYSTEM_PROMPT` is a module-level const (byte-stable across all three steps).                                                                              |
| branch (step 1)     | `app/src/services/canonical-knowledge.service.ts:1047, 1056, 1063` | ✓            | `messages.push({role: 'user', content: buildStepPrompt('branch', branches)})` (line 1047) → `runStepWithRetry(messages, ...)` (line 1056) → `messages.push({role: 'assistant', content: step1.rawResponse})` (line 1063). Pure append; system stays at index 0.                                                |
| new-branch combined | `app/src/services/canonical-knowledge.service.ts:1087-1094` | ✓            | When step 1 picks `isNew`, append `{role: 'user', content: buildNewBranchClusterAnchorPrompt(branchName)}` (line 1087-1090) → `chatCompletion(messages, ...)` (line 1094). Pure append. System still at index 0.                                                                                                  |
| cluster (step 2)    | `app/src/services/canonical-knowledge.service.ts:1118, 1122, 1127` | ✓            | `messages.push({role: 'user', content: buildStepPrompt('cluster', clusters)})` (line 1118) → `runStepWithRetry(messages, ...)` (line 1122) → `messages.push({role: 'assistant', content: step2.rawResponse})` (line 1127). Pure append.                                                                          |
| anchor (step 3)     | `app/src/services/canonical-knowledge.service.ts:1153, 1157` | ✓            | `messages.push({role: 'user', content: buildStepPrompt('anchor', ...)})` (line 1153) → `runStepWithRetry(messages, ...)` (line 1157). Pure append. (No assistant push after step 3 because the result is consumed immediately.)                                                                                  |

**Verdict:** Phase 23 append-only pattern is **fully intact**. Each step pushes onto the same `messages` array; the system message at index 0 is `PIPELINE_SYSTEM_PROMPT` (byte-stable const) and is never re-interpolated. No Phase 35 changes needed in `canonical-knowledge.service.ts`.

Phase 23 commit reference: `.planning/PROJECT.md` line 52 — "Phase 23 complete (2026-04-09): Incremental Classification Pipeline + Ask Rate Limiter — replaced single-call classification with 3-step branch→cluster→anchor pipeline using append-only messages for KV cache efficiency."

## Phase 35 must-haves verification

| #   | Must-have                                                                                                              | Source-of-truth                                                                                                                  | Status                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 1   | `useQuestions.ts` system prompt is byte-stable across turns (no `formatCandidateContextPack` inside any `role: 'system'` content) | source read of `useQuestions.ts:146-152` post-Plan-01                                                                            | ☑ VERIFIED — systemPrompt at line 146 is `[identity, safety, WEB_SEARCH_TOOL_PROMPT].join('\n')`; `formatCandidateContextPack` appears only in (a) the import (line 7), (b) comments (lines 141, 159), (c) the tail-assistant message variable (line 162) — never inside a `role: 'system'` element. |
| 2   | `assistantContextMessage` declared exactly once, referenced in both Pass 1 and Pass 2 chatStream arrays                | `grep -nE "assistantContextMessage" app/src/state/useQuestions.ts`                                                                | ☑ VERIFIED — declared line 162, referenced at line 179 (Pass 1) and line 239 (Pass 2). Single closure variable, two consumption sites, identical position relative to history + user-content. |
| 3   | `applyLocaleDirective` integration unchanged (first message in both arrays is still `role: 'system'`)                  | source read of `useQuestions.ts:175-181` and `:235-249` + `app/src/providers/llm/locale-directive.ts`                            | ☑ VERIFIED — both Pass 1 (line 177) and Pass 2 (line 237) start with `{ role: 'system', content: systemPrompt }` as the FIRST element. `locale-directive.ts` unchanged in Phase 35 scope. |
| 4   | `WEB_SEARCH_TOOL_PROMPT` remains in the static system prompt                                                            | source read of `useQuestions.ts:15-28` + systemPrompt construction at line 146                                                   | ☑ VERIFIED — `WEB_SEARCH_TOOL_PROMPT` is the third element joined into `systemPrompt` at line 149.                                                                                                  |
| 5   | `useQuestions-system-prompt-stability.test.mjs` green                                                                  | `node --test app/tests/state/useQuestions-system-prompt-stability.test.mjs` exits 0                                              | ☐ PENDING — test file exists at `app/tests/state/useQuestions-system-prompt-stability.test.mjs` (7,979 bytes, mtime 2026-04-29 08:46) per Plan 02. Must be re-run on green CI after all Wave 1 plans land; status flips to ☑ at phase close-out. |
| 6   | CLAUDE.md Phase 35 load-bearing section present                                                                        | `grep -c "Ask-chat system prompt — byte-stable across turns" CLAUDE.md`                                                          | ☐ PENDING Plan 03 — current `grep -c` returns 0 on CLAUDE.md (root). Plan 03 lands the section.                                                                                                       |
| 7   | TypeScript clean                                                                                                       | `cd app && tsc -b --noEmit` exits 0                                                                                              | ☐ PENDING — re-run during phase close-out regression sweep. Plan 01 source-grep evidence (no orphan imports of `formatCandidateContextPack`, both Pass 1 + Pass 2 fully reference variables in scope) suggests clean. |
| 8   | Test baseline preserved                                                                                                | `cd app && npm test` shows same pass/fail count as pre-Plan-01 baseline plus the 5 newly-passing assertions in Plan 02            | ☐ PENDING — re-run during phase close-out regression sweep.                                                                                                                                          |

> Status table reconciliation: 4/8 verified statically from source on 2026-04-29; 4/8 pending the close-out regression sweep + Plan 03 CLAUDE.md landing. The audit is INDEPENDENT of whether Plan 02 + Plan 03 have landed — Plans 01 source-state is verified and the must-haves table is now ready to flip during close-out.

## Empirical KV-cache verification (deferred — optional ad-hoc)

Per CONTEXT.md `<specifics>`, the planner-research-phase ad-hoc cache-hit observation (3-turn Anthropic conversation showing `cache_read_input_tokens > 0` on turn 2) is documented as a NICE-TO-HAVE for the verification phase, **not a blocker** for Phase 35 close-out.

If executed, the result format would be a fenced block here:

```
Status: NOT-RUN
```

Rationale for marking NOT-RUN as acceptable: The Phase 35 fix is structural (byte-stable system prompt + tail assistant message). Empirical cache-hit verification on a paid provider depends on (a) operator-furnished API key in a non-test environment, (b) a multi-turn chat session in production traffic, and (c) provider response-header inspection. The structural source-reading test (Plan 02) gives sufficient regression protection without the empirical run.

## Cross-cutting confirmation

| Confirmation                                                                                                       | Source                                                                                              | Status                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| No call-site changes at `AskScreen.tsx:289, :294` (askStreaming closure variable is private)                        | source read of `AskScreen.tsx:285-295`                                                              | ☑ VERIFIED — `priorMessages = (currentMessages ?? sessionRef.current.messages).slice(0, -1)` then `await askStreaming(userContent, ..., sessionContext, priorMessages, webSearchEnabled)`. Argument order matches `useQuestions.ts:38` typedef. No call-site edit needed for Phase 35. |
| Schema unchanged — no `SessionMessage` / `ChatSession` / `ChatMessage` field changes                                | `git diff main..HEAD -- app/src/types/index.ts app/src/providers/llm/index.ts`                      | ☑ EXPECTED — Phase 35 explicitly does not touch types per CONTEXT.md "Out of scope". Re-run during close-out for confirmation.                                                                                                                  |
| Abort plumbing intact (Phase 27 D-22) — `useQuestions-locale-abort.test.mjs` green                                  | `node --test app/tests/state/useQuestions-locale-abort.test.mjs` exits 0                            | ☐ PENDING — re-run during close-out regression sweep. The Plan 01 refactor preserves `abortController` + `signal` plumbing at lines 131-134, 183, 251.                                                                                            |
| Pre-checks (`aiConsentGiven`, `isConfigured`, monthly rate limit) preserved                                          | source read of `useQuestions.ts:99-125`                                                             | ☑ VERIFIED — gates at lines 102, 110, 118 unmodified vs. pre-Plan-01.                                                                                                                                                                            |
| Tool-pattern stripping during Pass 1 streaming preserved                                                             | source read of `useQuestions.ts:186-192`                                                            | ☑ VERIFIED — `TOOL_PATTERN` strip + display-cleaning at line 190 unchanged.                                                                                                                                                                       |

## Confirmed non-goals (deferred items from CONTEXT.md)

These items were explicitly out of scope for Phase 35; they are recorded here so future contributors can find the cost-benefit reasoning without re-deriving it from CONTEXT.md.

| #   | Deferred item                                                       | Source                              | Why deferred                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Append-only history invariant test                                   | CONTEXT.md `<deferred>`             | A behavioral test asserting `priorMessages` from `AskScreen.tsx:288` is always a strict prefix of `current.messages.slice(0, -1)`. Out of scope (about history-array invariants, not system-prompt stability).                                                                |
| 2   | Fix dynamic system prompts at one-shot call sites                    | CONTEXT.md `<deferred>`             | `concept-feed`, `planner`, `podcast`, `post-essay` interpolate dynamic content into their system prompts. They are all one-shot — KV-cache benefit is negligible. Not worth the test-and-refactor cost. **Per audit rows 4-19 above, the one-shot classification is confirmed.** |
| 3   | Anthropic `cache_control` explicit markers                            | CONTEXT.md `<deferred>`             | Phase 35's reordering enables IMPLICIT prefix caching on all providers; explicit markers could squeeze more out of Anthropic specifically. Future optimization phase if telemetry shows residual cache misses.                                                              |
| 4   | Auto-memory entry alongside CLAUDE.md note                           | CONTEXT.md `<deferred>`             | Phase 32.1 lesson #8 ("documentation in three places") suggests a parallel auto-memory entry. Optional; may land during Plan 03 or as a follow-up.                                                                                                                            |
| 5   | Reactivate-or-delete `question.service.ts:230` legacy non-streaming `ask` | This audit row 20 (NEW finding)     | The legacy `questionService.ask` path is multi-turn-shaped (spreads `historyMessages` at line 233) but no screen calls it (AskScreen comment at line 15 is explicit; grep confirms). If/when reactivated, apply Phase 35 discipline. If left dormant, candidate for deletion in a future hygiene phase. |

## Closure

Phase 35 ships when all rows in the **Phase 35 must-haves verification** table are ☑. As of 2026-04-29 audit time:

- ☑ 4/8 verified statically (rows 1-4)
- ☐ 4/8 pending close-out regression sweep + Plan 03 CLAUDE.md landing (rows 5-8)

The empirical KV-cache observation row (deferred section) is OPTIONAL and does not block close-out — its absence is documented as expected.

Audit row 20 (the legacy `questionService.ask` finding) is a **NEW deferred item** surfaced during this audit. CONTEXT.md did not name it; the grep enumeration uncovered it. Documented in the Confirmed non-goals table for future hygiene work.

---

*Phase: 35-fix-the-dynamic-system-prompt-issue*
*Verification authored: 2026-04-29*
*Last updated: 2026-04-29 (initial authoring)*
