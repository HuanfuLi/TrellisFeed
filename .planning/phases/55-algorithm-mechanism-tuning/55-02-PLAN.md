---
phase: 55-algorithm-mechanism-tuning
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/src/providers/embedding/index.ts
  - app/src/services/question.service.ts
  - app/src/services/canonical-knowledge.service.ts
  - app/src/services/question-filter.service.ts
  - app/src/screens/settings/SettingsAIScreen.tsx
autonomous: true
requirements: [TUNE-01]
must_haves:
  truths:
    - "embedText returns a cached vector for an identical (provider, model, text) within a session — no second network call"
    - "Changing the embedding provider/model invalidates the session cache so stale-dimensionality vectors are never returned"
    - "A single ask embeds the bare question content at most once across filter rawVec, retrieval queryEmbedding, and classify pre-check"
  artifacts:
    - path: "app/src/providers/embedding/index.ts"
      provides: "in-memory session embed cache + clearEmbedCache + getCachedEmbedding"
      exports: ["embedText", "clearEmbedCache", "getCachedEmbedding", "cosine"]
    - path: "app/src/screens/settings/SettingsAIScreen.tsx"
      provides: "cache invalidation on embedding model/provider change"
      contains: "clearEmbedCache"
  key_links:
    - from: "app/src/services/question-filter.service.ts"
      to: "app/src/providers/embedding/index.ts"
      via: "embedText (rawVec) hits cache"
      pattern: "embedText"
    - from: "app/src/services/canonical-knowledge.service.ts"
      to: "app/src/providers/embedding/index.ts"
      via: "preCheckAnchorMatch embedText hits cache"
      pattern: "embedText"
    - from: "app/src/screens/settings/SettingsAIScreen.tsx"
      to: "app/src/providers/embedding/index.ts"
      via: "clearEmbedCache on model change"
      pattern: "clearEmbedCache"
---

<objective>
Add an in-memory, session-lived embedding cache to `embedText` and thread the bare-content query vector through the filter, retrieval, and classify pre-check so the same string is embedded at most once per ask (D-07). Add cache invalidation on embedding model/provider change (D-08 / Pitfall 5).

Purpose: Resolves the folded `2026-05-07-fix-cosine-similarity-threshold-cache-miss` todo. Eliminates the known 2-3x duplicate embed of the current question within a single ask, which also makes threshold tuning (plan 55-03) deterministic and cheap to exercise.
Output: Cache + invalidation in the embedding provider; `embed-cache.test.mjs` (scaffolded in 55-01) goes green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md
@.planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md
@app/src/providers/embedding/index.ts

<interfaces>
<!-- From app/src/providers/embedding/index.ts (verified) -->
- `export function cosine(a: number[], b: number[]): number` (line 15)
- `export async function embedText(text: string, config: EmbeddingConfig): Promise<number[]>` (line 110) — switch over config.provider: google | local | lmstudio | (default openAIEmbed)
- Lines 1-11: the FILTER-03 / D-13 bracketing-exemption comment — MUST be preserved verbatim.

<!-- Duplicate embed call sites (verified, from 55-RESEARCH §"Embedding Cache") -->
- question-filter.service.ts:173 — filter rawVec on bare `content`
- question.service.ts:253 — pre-LLM retrieval embed on bare `content`
- canonical-knowledge.service.ts:728 — preCheckAnchorMatch on bare `question.content` (only when question.embeddingVector empty)

<!-- EmbeddingConfig key fields used for the cache key -->
- config.provider, config.model (both required in the djb2 key per Pitfall 5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the in-memory embed cache + invalidation API to the embedding provider</name>
  <files>app/src/providers/embedding/index.ts</files>
  <read_first>
    - app/src/providers/embedding/index.ts (the full file — preserve the lines 1-11 FILTER-03 exemption comment verbatim; the existing embedText switch dispatch)
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("app/src/providers/embedding/index.ts" section — the exact cache addition pattern, djb2 key including provider:model:text)
    - app/tests/providers/embed-cache.test.mjs (the scaffold this task turns green — match its expected export names and behavior)
  </read_first>
  <behavior>
    - Test 1: `embedText('hello world', cfg)` called twice with the same provider+model -> network/dispatch invoked exactly once; second call returns the cached vector.
    - Test 2: same text, different `config.model` -> cache miss -> dispatch invoked twice (key includes model).
    - Test 3: `getCachedEmbedding('test', cfg)` returns a vector after a prior `embedText('test', cfg)`; returns undefined when not cached.
    - Test 4: `clearEmbedCache()` empties the cache so a subsequent `embedText` re-dispatches.
  </behavior>
  <action>
    Apply the D-07 cache pattern from 55-PATTERNS.md. Rename the existing provider switch to a private `_embedTextUncached(text, config)`. Add a module-closure `const _embedCache = new Map<string, number[]>()`. Add `_djb2CacheKey(text, config)` building the raw string as `${config.provider}:${config.model}:${text}` then djb2-hashing (the browser-safe inline hash — no node:crypto, no SubtleCrypto). Rewrite `embedText` to check the cache, dispatch on miss, store, return. Export `clearEmbedCache(): void` (clears the Map) and `getCachedEmbedding(text, config): number[] | undefined`. Preserve the lines 1-11 FILTER-03 exemption comment verbatim and the existing `cosine` export. Add the SECURITY NOTE comment from the pattern explaining the provider+model key requirement (Pitfall 5).
  </action>
  <verify>
    <automated>cd app && node --test tests/providers/embed-cache.test.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `embed-cache.test.mjs` passes all of its assertions (the 55-01 scaffold guard, if any, is removed by this task).
    - `index.ts` exports `embedText`, `clearEmbedCache`, `getCachedEmbedding`, and still exports `cosine`.
    - The cache key string literal includes both `config.provider` and `config.model` (grep: `provider}:${.*model` or equivalent).
    - The FILTER-03 exemption comment block at the top of the file is unchanged (still present verbatim).
    - `cd app && ./node_modules/.bin/tsc -b --noEmit` is clean for this file.
  </acceptance_criteria>
  <done>embedText caches per (provider,model,text); clearEmbedCache + getCachedEmbedding exported; embed-cache.test.mjs green; FILTER-03 comment preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline hand-off + model-change invalidation across the three call sites</name>
  <files>app/src/services/question.service.ts, app/src/services/canonical-knowledge.service.ts, app/src/services/question-filter.service.ts, app/src/screens/settings/SettingsAIScreen.tsx</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Embedding Cache + Pipeline Hand-off" — the call-site table and the "filter runs first, so line 253 + line 728 become cache hits" ordering note; Pitfall 5)
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("SettingsAIScreen.tsx" -> saveEmbedding + clearEmbedCache on model change)
    - app/src/services/question.service.ts (the embedText call ~line 253 — confirm it embeds bare content)
    - app/src/services/canonical-knowledge.service.ts (preCheckAnchorMatch embedText ~line 728)
    - app/src/services/question-filter.service.ts (layer2Embedding rawVec ~line 173 — do NOT touch the dual-vector contextVec logic)
    - app/src/screens/settings/SettingsAIScreen.tsx (saveEmbedding handler ~line 50)
  </read_first>
  <action>
    The hand-off is automatic once the cache exists: the filter's `rawVec` embed (question-filter.service.ts:173) runs first in `evaluateQuestion`, so the later retrieval embed (question.service.ts:253) and pre-check embed (canonical-knowledge.service.ts:728) become cache hits for the same bare `content` string. Verify each of these three sites calls `embedText(content, ...)` (bare content, no priorAnswer prefix) so they share a cache key — do NOT add a priorAnswer prefix to any of them (the contextVec dual-vector path in the filter is separate and unchanged). If any site builds a non-bare-content string for the bare-content embed, leave the dual-vector contextVec alone but ensure the bare path is genuinely bare. No structural rewrite — only confirm/adjust the embedded text so the cache hits.
    In SettingsAIScreen.tsx: import `clearEmbedCache` from the embedding provider and call it inside the existing embedding save handler when `provider` or `model` changed (compare prev vs next), per Pitfall 5. Do not clear on unrelated setting changes.
  </action>
  <verify>
    <automated>cd app && ./node_modules/.bin/tsc -b --noEmit 2>&1 | tail -10 && grep -n "clearEmbedCache" app/src/screens/settings/SettingsAIScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsAIScreen.tsx` imports and calls `clearEmbedCache()` only inside the embedding-config save path, guarded by a provider/model change comparison.
    - The three bare-content embed sites (filter rawVec, retrieval, pre-check) all embed the SAME bare `content` string (no priorAnswer prefix on the bare path) — confirmed by reading each site.
    - The filter's dual-vector `contextVec` path (priorAnswer-prefixed) is unchanged — `tests/services/filter-classifier.unit.test.mjs` still passes (run it).
    - `tsc -b --noEmit` is clean.
  </acceptance_criteria>
  <done>The three bare-content embeds share a cache key (filter runs first -> retrieval + pre-check are hits); model change clears the cache; dual-vector filter untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question -> filter | Untrusted input embedded for malicious/off-topic classification |
| settings change -> cache | Model/provider switch can poison the cache with wrong-dimensionality vectors if not invalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02a | Tampering | embed cache key | mitigate | Key includes `provider:model:text` (djb2). Pitfall 5: model change calls `clearEmbedCache()` so a stale vector from model A is never returned for model B (would silently corrupt malicious cosine scoring). |
| T-55-02b | Spoofing | filter malicious path via cache | mitigate | The bare-content embed (used by the malicious cosine path) is never prefixed with priorAnswer; the cache shares only the bare-content key. Dual-vector buried-payload defense (CLAUDE.md) is preserved — verified by leaving the contextVec path untouched and re-running filter-classifier.unit.test.mjs. |
| T-55-02c | Information Disclosure | session Map | accept | Cache is in-memory, session-lived (cleared on reload); no persistence of arbitrary strings (D-08). No new disclosure surface. |
</threat_model>

<verification>
- `embed-cache.test.mjs` green.
- `filter-classifier.unit.test.mjs` still green (dual-vector not regressed).
- `tsc -b --noEmit` clean.
</verification>

<success_criteria>
- Same (provider,model,text) embeds once per session; model change invalidates.
- Single ask embeds bare content at most once across the three sites.
- No regression to the dual-vector filter.
</success_criteria>

<output>
Create `.planning/phases/55-algorithm-mechanism-tuning/55-02-SUMMARY.md` when done.
</output>
