---
phase: 55-algorithm-mechanism-tuning
plan: 04
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/src/services/concept-feed.service.ts
  - app/tests/services/like-boost.test.mjs
autonomous: true
requirements: [TUNE-02]
must_haves:
  truths:
    - "A liked concept gets 2x derived-list multiplicity (8 entries vs 4) — surfacing more in the feed"
    - "A non-liked, non-important concept keeps its base 4 entries"
    - "Liked-concept boosting does NOT starve due-for-review concepts — they remain present in the derived list"
    - "The like-boost reuses the existing buildConceptBatch multiplicity lever — no fourth list, no new pipeline stage, derived list stays append-only"
    - "STYLE_WEIGHTS and trajectoryAnalyzer weights are verified by behavior tests + dev instrumentation; constants kept unless real drift is shown, each with a rationale comment"
    - "D-14: a like boosts the concept's derived-list multiplicity via the existing 4->8 importance/overdue lever — no new list — and must not starve due-for-review concepts"
    - "D-15: STYLE_WEIGHTS and trajectoryAnalyzer weights are verify-and-keep — instrumented and behavior-tested, only re-tuned if real drift is shown, each value keeping a rationale comment"
  artifacts:
    - path: "app/src/services/concept-feed.service.ts"
      provides: "like -> multiplicity boost in buildConceptBatch"
      contains: "isBoosted"
    - path: "app/tests/services/like-boost.test.mjs"
      provides: "TUNE-02 like-boost + pipeline-invariant tests"
      contains: "isBoosted"
  key_links:
    - from: "app/src/services/concept-feed.service.ts"
      to: "engagementService.getLikedPostIds"
      via: "resolve liked postIds -> conceptIds before the anchor loop"
      pattern: "getLikedPostIds"
    - from: "app/src/services/concept-feed.service.ts"
      to: "postHistoryService.getPosts"
      via: "map liked postId -> conceptId"
      pattern: "conceptId"
---

<objective>
Hook the existing "like" signal into the derived-list multiplicity lever in `buildConceptBatch` (D-14): a liked concept is treated like an important/overdue concept and gets `BASE_ENTRIES_PER_CONCEPT * 2` entries — no new list, no new pipeline stage. Add behavior tests proving liked concepts surface more without starving due-for-review concepts, and verify-and-keep the `STYLE_WEIGHTS` + `trajectoryAnalyzer` weights with tests + dev instrumentation (D-15).

Purpose: `engagementService.liked[]` was a recorded recommendation signal that never influenced the feed. D-14 connects it to the existing importance-doubling lever, the same mechanism the 3-list pipeline already uses. The verify-and-keep covers TUNE-02's "tested and tuned against expected behavior" for the already-tuned weights.
Output: like-boost in concept-feed; `like-boost.test.mjs` green; STYLE_WEIGHTS/trajectory behavior asserted; D-02 instrumentation logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md
@.planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md
@app/src/services/concept-feed.service.ts
@app/src/services/engagement.service.ts

<interfaces>
<!-- buildConceptBatch multiplicity (verified, concept-feed.service.ts lines 832-854) -->
- `const BASE_ENTRIES_PER_CONCEPT = 4` (line 832 — DO NOT CHANGE)
- isImportant = easeFactor < 1.5 OR leafState ∈ {dying, falling, dead}; count = isImportant ? 8 : 4
- buildConceptBatch filters by `dailyReadService.getExploredAnchors()` BEFORE the anchor loop (load-bearing — keep)

<!-- engagement.service.ts (verified) -->
- `getLikedPostIds()` -> liked postIds[]
- `getPinnedIds()` returns `new Set([...saved, ...liked, ...])` — liked posts survive postHistory purge (Pitfall 6 already handled)

<!-- DailyPost.conceptId maps a postId -> anchor id; postHistoryService.getPosts() resolves served posts -->

<!-- D-02 dev instrumentation pattern: import.meta.env?.DEV gate (style-assignment.ts lines 113-118) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the like -> multiplicity boost in buildConceptBatch</name>
  <files>app/src/services/concept-feed.service.ts</files>
  <read_first>
    - CLAUDE.md §"Concept Feed Generation Pipeline" (the 3-list pipeline — like-boost hooks the EXISTING multiplicity lever; NO fourth list; derived list append-only; re-explained 5+ times)
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("concept-feed.service.ts" — the exact like-boost block: resolve likedPostIds->conceptIds before the loop, isLiked check, isBoosted = isImportant || isLiked, count = isBoosted ? BASE*2 : BASE, D-02 logging)
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Like-Signal -> Derived-List Multiplicity" — data flow, decay/no-decay rationale, starvation analysis, Pitfall 6 pinning)
    - app/src/services/concept-feed.service.ts (buildConceptBatch lines 832-854; confirm engagementService + postHistoryService import availability)
    - app/src/services/engagement.service.ts (getLikedPostIds, getPinnedIds)
    - app/tests/services/like-boost.test.mjs (the 55-01 scaffold this turns green)
  </read_first>
  <behavior>
    - A concept whose post was liked -> count = 8 (BASE * 2).
    - A non-liked, non-important concept -> count = 4 (BASE).
    - A concept that is BOTH liked and important -> count = 8 (boost is OR, not additive — no starvation amplification beyond the existing worst case).
    - Due-for-review (overdue/important) concepts remain present in the returned conceptIds even when other concepts are liked.
  </behavior>
  <action>
    Apply the D-14 block from 55-PATTERNS.md. BEFORE the anchor loop, resolve liked postIds -> conceptIds once: `const likedPostIds = new Set(engagementService.getLikedPostIds())`; iterate `postHistoryService.getPosts()` collecting `p.conceptId` where `likedPostIds.has(p.id)` into a `likedConceptIds` Set. Inside the per-anchor loop, after the existing `isImportant` computation, add `const isLiked = likedConceptIds.has(anchor.id)`, `const isBoosted = isImportant || isLiked`, `const count = isBoosted ? BASE_ENTRIES_PER_CONCEPT * 2 : BASE_ENTRIES_PER_CONCEPT`. Add a rationale comment that the like reuses the SAME lever (no new list) and that boost is OR not additive so worst-case multiplicity is unchanged (A4 starvation note). Add the D-02 dev-gated `import.meta.env?.DEV` instrumentation logging per-anchor `count`/`isLiked`/`isImportant` and totals `likedConceptIds.size`/`conceptIds.length`.
    Keep the existing `exploredIds` filter before the loop. Do NOT call `appendToDerivedList` or splice the derived list inside buildConceptBatch — it only returns conceptIds for the caller to append (append-only invariant).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/like-boost.test.mjs 2>&1 | tail -15 && ./node_modules/.bin/tsc -b --noEmit 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `like-boost.test.mjs` passes: `isBoosted = isImportant || isLiked` present; `BASE_ENTRIES_PER_CONCEPT * 2` used for boosted; buildConceptBatch does NOT call `appendToDerivedList` or `derivedList.splice` (negative assertion green).
    - `BASE_ENTRIES_PER_CONCEPT` is still `4` (unchanged).
    - The `exploredIds` filter still runs before the anchor loop.
    - Existing pipeline tests stay green: `node --test tests/services/derived-list.test.mjs tests/services/refill-queue-integration.test.mjs`.
    - `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Liked concepts get 2x multiplicity via the existing lever; OR-not-additive (no starvation); append-only + explored-filter invariants intact; like-boost.test.mjs green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify-and-keep STYLE_WEIGHTS + trajectoryAnalyzer with behavior tests + instrumentation</name>
  <files>app/src/services/concept-feed.service.ts, app/tests/services/like-boost.test.mjs</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Signal mechanisms" D-15 verify-and-keep; "Validation Architecture" rows for STYLE_WEIGHTS sum=1.0 and stratified ±1)
    - app/src/services/style-assignment.ts (STYLE_WEIGHTS lines 9-16; the existing DEV instrumentation block lines 113-118)
    - app/src/services/trajectoryAnalyzer.service.ts (aggregateSignals weights + rationale comments)
    - app/tests/services/style-assignment.test.mjs and style-assignment-stratified.test.mjs (existing — DO NOT duplicate; reference)
  </read_first>
  <action>
    This is D-15 verify-and-keep, NOT retune. Confirm `STYLE_WEIGHTS` and `trajectoryAnalyzer.aggregateSignals` weights each carry a rationale comment; add one where missing (do not change values unless the instrumentation reveals real drift — if it does, document the before/after + reason in the SUMMARY). Add a small behavior assertion section to `like-boost.test.mjs` (or confirm the existing style-assignment tests already cover): STYLE_WEIGHTS sum ≈ 1.0 and the realized-mix instrumentation exists. Add the D-02 dev-gated realized-style-mix-vs-target log if not already present (the existing style-assignment.ts DEV block at lines 113-118 likely covers this — extend rather than duplicate). The deliverable is evidence the weights behave correctly, not new constants.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/style-assignment.test.mjs tests/services/style-assignment-stratified.test.mjs tests/services/like-boost.test.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `STYLE_WEIGHTS` and `trajectoryAnalyzer` weights each have a rationale comment (added if missing).
    - Behavior coverage exists for STYLE_WEIGHTS sum and stratified allocation (existing tests green; new assertions added only if a gap exists).
    - If any weight changed, the SUMMARY documents the instrumented drift evidence + before/after; otherwise the SUMMARY states "verified, kept" with the evidence.
    - All referenced tests green.
  </acceptance_criteria>
  <done>STYLE_WEIGHTS + trajectory weights verified-and-kept with rationale comments + behavior tests + realized-mix instrumentation; any change is evidence-backed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| like signal -> feed multiplicity | Engagement state influences which concepts surface (no external/untrusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04a | Denial of Service (self-starvation) | buildConceptBatch multiplicity | mitigate | Boost is OR (`isImportant || isLiked`), not additive — worst-case multiplicity is `BASE*2`, identical to the pre-existing all-important case (A4). Behavior test asserts due-for-review concepts remain present. No new starvation vector. |
| T-55-04b | Tampering (pipeline drift) | 3-list concept-feed pipeline | mitigate | Like-boost reuses the existing multiplicity lever only; negative test asserts no `appendToDerivedList`/`splice` inside buildConceptBatch; explored-filter + append-only invariants preserved (CLAUDE.md load-bearing). |
</threat_model>

<verification>
- like-boost.test.mjs, derived-list.test.mjs, refill-queue-integration.test.mjs, style-assignment*.test.mjs green.
- tsc -b --noEmit clean.
</verification>

<success_criteria>
- Liked concepts surface 2x via the existing lever without starving due concepts.
- 3-list pipeline invariants intact.
- STYLE_WEIGHTS/trajectory verified-and-kept with rationale + tests.
</success_criteria>

<output>
Create `.planning/phases/55-algorithm-mechanism-tuning/55-04-SUMMARY.md` when done. Record whether any STYLE_WEIGHTS/trajectory constant changed and the instrumented evidence.
</output>
