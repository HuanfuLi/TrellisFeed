---
phase: 55-algorithm-mechanism-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/providers/embed-cache.test.mjs
  - app/tests/services/filter-golden-fixtures.test.mjs
  - app/tests/services/storage-migration.test.mjs
  - app/tests/services/like-boost.test.mjs
  - app/package.json
  - app/package-lock.json
autonomous: false
requirements: [TUNE-01, TUNE-02]
must_haves:
  truths:
    - "The four Wave-0 test files exist and run under `node --test` (red/skipped is acceptable pre-implementation)"
    - "`@sqlite.org/sqlite-wasm` is installed and its OO1 OPFS API resolves in `npm run dev` (or the spike records a fallback verdict)"
    - "The migration go/no-go decision is recorded for the executor of plan 55-05"
  artifacts:
    - path: "app/tests/providers/embed-cache.test.mjs"
      provides: "TUNE-01 embed-cache hit/miss + pipeline hand-off scaffold"
      contains: "embed-cache"
    - path: "app/tests/services/filter-golden-fixtures.test.mjs"
      provides: "TUNE-01 labeled golden corpus scaffold"
      contains: "GOLDEN_FIXTURES"
    - path: "app/tests/services/storage-migration.test.mjs"
      provides: "migration boot-hydration + delete-guard + Float32 BLOB codec scaffold"
      contains: "storage-migration"
    - path: "app/tests/services/like-boost.test.mjs"
      provides: "TUNE-02 like-boost multiplicity scaffold"
      contains: "like-boost"
  key_links:
    - from: "app/package.json"
      to: "@sqlite.org/sqlite-wasm"
      via: "dependency entry"
      pattern: "@sqlite.org/sqlite-wasm"
---

<objective>
Create the Wave 0 test scaffolds for all four new phase test files and run the SQLite-WASM backend spike that gates the storage migration (plan 55-05).

Purpose: The Nyquist contract requires every later task to have a real `<automated>` target. This plan creates those test files first (red/skipped is fine — implementation plans turn them green) and de-risks Open Question 1 (does `@sqlite.org/sqlite-wasm` + `opfs-sahpool` actually resolve in `npm run dev`?) BEFORE plan 55-05 commits to that backend.
Output: Four `.test.mjs` scaffolds + installed `@sqlite.org/sqlite-wasm` + a recorded go/no-go verdict (browser SQLite vs LocalStorageBackend-fallback) for the migration executor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-algorithm-mechanism-tuning/55-CONTEXT.md
@.planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md
@.planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md
@.planning/phases/55-algorithm-mechanism-tuning/55-VALIDATION.md
@app/tests/services/filter-classifier.unit.test.mjs
@app/tests/services/classification-dedup.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four Wave-0 test scaffolds</name>
  <files>app/tests/providers/embed-cache.test.mjs, app/tests/services/filter-golden-fixtures.test.mjs, app/tests/services/storage-migration.test.mjs, app/tests/services/like-boost.test.mjs</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("Test File Patterns" + "Shared Patterns" — verbatim scaffolds and the localStorage shim)
    - .planning/phases/55-algorithm-mechanism-tuning/55-VALIDATION.md ("Wave 0 Requirements" + the per-task verification map)
    - app/tests/services/filter-classifier.unit.test.mjs (localStorage shim lines 29-43, loader registration, import.meta.url import shape)
    - app/tests/services/classification-dedup.test.mjs (source-reading assertion pattern)
  </read_first>
  <action>
    Create all four test files using the exact scaffolds in 55-PATTERNS.md "Test File Patterns". Reuse the localStorage shim from filter-classifier.unit.test.mjs lines 29-43 verbatim in every file that imports a service. Each file is a scaffold that establishes the test names and assertion shapes the implementation plans (55-02 cache, 55-03 thresholds, 55-04 like, 55-05 migration) will satisfy.
    - `embed-cache.test.mjs`: tests "same text + model -> cache hit, fetch called once", "different model -> cache miss", "getCachedEmbedding returns hit after embedText call". Stub `globalThis.fetch` with a deterministic vector and a call counter as in the pattern. These imports (`embedText`, `clearEmbedCache`, `getCachedEmbedding`) do not exist yet — gate the body behind a try/catch import or mark the suite as expected-red so the file PARSES and runs; the 55-02 executor removes the guard once the exports land. Add a top-of-file comment: "Wave 0 scaffold (55-01); turned green by 55-02".
    - `filter-golden-fixtures.test.mjs`: register `_filter-classifier-mock-loader.mjs`, define `GOLDEN_FIXTURES` with the three seed cases from 55-RESEARCH.md (off-topic greeting, verbatim jailbreak -> malicious, on-topic learning question), loop `evaluateQuestion` over them. Comment: "fixtures ARE the D-03 evidence bar; 55-03 populates more from browser instrumentation".
    - `storage-migration.test.mjs`: source-reading assertions per the pattern — Float32 BLOB round-trip (inline the `vectorToBase64`/`base64ToVector` codec in the test for now), `if (existing.length > 0) return` delete-guard present in question.service.ts source, `GRAPH_UPDATED` emitted in hydrate block, `class WASMSQLiteBackend` present in db.service.ts source. The last three are source-reading and will be RED until 55-05; comment: "turned green by 55-05".
    - `like-boost.test.mjs`: source-reading assertions on concept-feed.service.ts — `isBoosted = isImportant || isLiked`, `BASE_ENTRIES_PER_CONCEPT * 2` for boosted, and a negative assertion that buildConceptBatch does NOT call `appendToDerivedList` or `derivedList.splice`. RED until 55-04; comment: "turned green by 55-04".
    Do NOT implement any production code in this task. Scaffolds only.
  </action>
  <verify>
    <automated>cd app && node --test tests/providers/embed-cache.test.mjs tests/services/filter-golden-fixtures.test.mjs tests/services/storage-migration.test.mjs tests/services/like-boost.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the paths in `files_modified`.
    - `node --test` PARSES and EXECUTES all four files (no SyntaxError, no unhandled import rejection that aborts the runner). Failing/red assertions are acceptable and expected for the source-reading checks that depend on later plans.
    - Each file contains its sentinel string: `embed-cache`, `GOLDEN_FIXTURES`, `storage-migration`, `like-boost` respectively.
    - No production source file under `app/src/` is modified by this task.
  </acceptance_criteria>
  <done>Four scaffolds run under node --test; red assertions point at the exact invariants later plans must satisfy; zero src/ changes.</done>
</task>

<task type="auto">
  <name>Task 2: Install @sqlite.org/sqlite-wasm and run the OPFS resolution spike</name>
  <files>app/package.json, app/package-lock.json</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Package Legitimacy Audit", "Standard Stack" -> Browser SQLite Backend Selection, "Open Questions" -> #1, "Common Pitfalls" -> Pitfall 1 & 2)
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("db.service.ts" -> WASMSQLiteBackend pattern, the opfs-sahpool OO1 API shape)
    - app/src/services/db.service.ts (DBBackend interface lines 14-19, getDB factory line 182-194)
    - app/vite.config.ts (current dev server + optimizeDeps config)
  </read_first>
  <action>
    Install the research-approved official package: `npm install @sqlite.org/sqlite-wasm` (run from `app/`). Per the Package Legitimacy Audit it is the official SQLite-project package (github.com/sqlite/sqlite-wasm) — APPROVED, no legitimacy checkpoint needed. Do NOT install `wa-sqlite` (flagged [SUS] in the audit) or `sql.js`.
    Then write a throwaway spike script (e.g. `app/scripts/sqlite-wasm-spike.mjs` or an inline node check) that: dynamically imports `@sqlite.org/sqlite-wasm`, calls the default `sqlite3InitModule({ print, printErr })`, and confirms the OO1 + OpfsDb (or opfs-sahpool) API surface that the WASMSQLiteBackend pattern in 55-PATTERNS.md depends on is reachable. Because OPFS requires a browser secure context (not available in bare Node), the durable verification is in `npm run dev`: start the dev server, open the app in Chrome, and in DevTools confirm `navigator.storage?.getDirectory` exists and `new sqlite3.oo1.OpfsDb('/trellis.sqlite3')` does not throw. Capture the verdict.
    Record in the SUMMARY a clear go/no-go for plan 55-05: either (a) GO — opfs-sahpool resolves, WASMSQLiteBackend is viable; or (b) NO-GO — OPFS unavailable in the dev environment, 55-05 must keep the LocalStorageBackend fallback path as the active browser backend and gate WASMSQLiteBackend behind the try/catch fallback already in the pattern. Delete the throwaway spike script before completing (do not leave dead scripts).
  </action>
  <verify>
    <automated>cd app && node -e "import('@sqlite.org/sqlite-wasm').then(m => { if (typeof m.default !== 'function') { console.error('FAIL: default export not a function'); process.exit(1); } console.log('OK: @sqlite.org/sqlite-wasm default export resolves'); }).catch(e => { console.error('FAIL', e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `app/package.json` dependencies contains `@sqlite.org/sqlite-wasm` and `app/package-lock.json` records it.
    - The automated check prints "OK: @sqlite.org/sqlite-wasm default export resolves" and exits 0 (the package imports and exposes the init module).
    - `wa-sqlite` and `sql.js` are NOT added to package.json.
    - The throwaway spike script is removed; no new file under `app/scripts/` remains.
    - The SUMMARY records the explicit GO / NO-GO verdict (browser SQLite vs LocalStorageBackend-fallback) for plan 55-05, including which Chrome version / context was used.
  </acceptance_criteria>
  <done>Official sqlite-wasm package installed and importable; OPFS resolution verified in npm run dev with a recorded go/no-go that 55-05 consumes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms OPFS availability + migration go/no-go verdict</name>
  <files>(none — verification checkpoint)</files>
  <action>Checkpoint only — no code. The operator verifies the OPFS resolution spike result in the browser and records the migration go/no-go that plan 55-05 consumes. See what-built / how-to-verify below.</action>
  <what-built>Installed `@sqlite.org/sqlite-wasm` and ran the OPFS resolution spike in `npm run dev`. The verdict determines whether plan 55-05 uses the WASMSQLiteBackend as the primary browser backend or keeps LocalStorageBackend as the active path with WASM gated behind the OPFS try/catch fallback.</what-built>
  <how-to-verify>
    1. From `app/`, run `npm run dev` and open the app in a recent Chrome (108+).
    2. Open DevTools console. Confirm `navigator.storage?.getDirectory` is a function (OPFS available, secure context on localhost).
    3. Exercise the app briefly (ask a question, scroll the feed) and confirm no console errors mentioning "No such VFS: opfs-sahpool" or "The requested storage is not available".
    4. Confirm the spike's recorded verdict (GO browser-SQLite vs NO-GO fallback) matches what you observe.
  </how-to-verify>
  <verify><human-check>Operator confirms OPFS availability and the recorded GO/NO-GO verdict.</human-check></verify>
  <resume-signal>Type "approved — GO" or "approved — NO-GO (fallback)" or describe the OPFS error observed.</resume-signal>
  <done>Operator has confirmed the OPFS availability verdict; plan 55-05 has a definitive backend decision (WASM vs fallback).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build | New dependency `@sqlite.org/sqlite-wasm` enters the dependency tree (supply chain) |
| browser -> OPFS | WASM SQLite reads/writes the Origin Private File System (dev-only target this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SC | Tampering | npm install `@sqlite.org/sqlite-wasm` | mitigate | Use ONLY the official SQLite-project package (github.com/sqlite/sqlite-wasm), APPROVED in 55-RESEARCH Package Legitimacy Audit; reject the [SUS] `wa-sqlite` re-publish and `sql.js`. No legitimacy checkpoint required for the approved official package. |
| T-55-01 | Information Disclosure | OPFS-backed SQLite file | accept | Local-first app, single-user device/dev browser; no PII leaves the device; OPFS is origin-scoped. Low risk. |
</threat_model>

<verification>
- All four scaffolds run under `node --test` (red allowed).
- `@sqlite.org/sqlite-wasm` imports successfully via the automated check.
- Human checkpoint confirms OPFS availability verdict for 55-05.
</verification>

<success_criteria>
- Four Wave-0 test files created and executable.
- Official sqlite-wasm package installed; no [SUS]/extra packages added.
- GO/NO-GO migration verdict recorded for plan 55-05.
</success_criteria>

<output>
Create `.planning/phases/55-algorithm-mechanism-tuning/55-01-SUMMARY.md` when done. The SUMMARY MUST record the GO/NO-GO browser-SQLite verdict consumed by plan 55-05.
</output>
