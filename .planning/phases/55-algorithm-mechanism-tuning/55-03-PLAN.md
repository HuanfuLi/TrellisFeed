---
phase: 55-algorithm-mechanism-tuning
plan: 03
type: execute
wave: 3
depends_on: ["55-02"]
files_modified:
  - app/src/types/index.ts
  - app/src/services/settings.service.ts
  - app/src/services/question-filter.service.ts
  - app/src/services/canonical-knowledge.service.ts
  - app/src/screens/settings/SettingsAIScreen.tsx
  - app/tests/services/filter-golden-fixtures.test.mjs
  - app/tests/services/classification-dedup.test.mjs
autonomous: true
requirements: [TUNE-01, TUNE-02]
must_haves:
  truths:
    - "The dead single `similarityThreshold = 0.65` slider is replaced by three labeled per-threshold knobs (off-topic, malicious, anchor-dedup) gated behind a master debugEnabled toggle"
    - "In production (debugEnabled false/undefined) every threshold read falls back to its hardcoded constant"
    - "The malicious threshold is clamped to [0.78, 0.85] in BOTH the slider min/max AND the service read path — it cannot be detuned to reopen the buried-payload evasion surface"
    - "The anchor-dedup threshold knob actually drives preCheckAnchorMatch (the 0.65 dead-slider discrepancy is resolved)"
    - "Golden fixtures classify off-topic / on-topic / malicious correctly at the chosen thresholds and are frozen as the regression bar"
    - "D-04: thresholds are settings-driven during tuning, then finalized to the hardcoded constant with the debug control hidden from released users"
    - "D-05: the dead single 0.65 slider is resolved into labeled per-threshold knobs (off-topic 0.75, malicious 0.82, anchor-dedup 0.82) and the anchor-dedup knob actually drives preCheckAnchorMatch"
    - "D-06: the malicious threshold is clamped to the 0.78-0.85 band even in debug so the buried-payload evasion surface cannot be reopened"
  artifacts:
    - path: "app/src/types/index.ts"
      provides: "EmbeddingDebugConfig per-threshold knobs (optional additive)"
      contains: "maliciousThreshold"
    - path: "app/src/services/question-filter.service.ts"
      provides: "getActiveThresholds with malicious clamp"
      contains: "0.78"
    - path: "app/tests/services/filter-golden-fixtures.test.mjs"
      provides: "frozen labeled corpus (D-03 evidence bar)"
      contains: "GOLDEN_FIXTURES"
  key_links:
    - from: "app/src/services/canonical-knowledge.service.ts"
      to: "settings.embeddingDebug.anchorDedupThreshold"
      via: "preCheckAnchorMatch reads live knob when debugEnabled"
      pattern: "anchorDedupThreshold"
    - from: "app/src/screens/settings/SettingsAIScreen.tsx"
      to: "settings.embeddingDebug.maliciousThreshold"
      via: "slider min=0.78 max=0.85"
      pattern: "0.78"
---

<objective>
Resolve the threshold source-of-truth discrepancy (D-04/D-05) and lock the security band (D-06): replace the dead single `similarityThreshold = 0.65` slider with three labeled, dev-gated per-threshold knobs (off-topic 0.75, malicious 0.82, anchor-dedup 0.82), wire the anchor-dedup knob into `preCheckAnchorMatch`, clamp the malicious knob to 0.78-0.85 in both UI and service, and freeze the golden-fixture corpus as the durable evidence bar (D-03).

Purpose: The `settings.embeddingDebug.similarityThreshold = 0.65` slider is silently disconnected from all three real thresholds — tuning it does nothing. This plan makes the knobs real for dev tuning, hides them in release, prevents the operator from detuning the malicious threshold into the buried-payload evasion surface, and bakes the tuning evidence into regression tests.
Output: per-threshold debug UI + service reads; malicious clamp; populated golden fixtures; anchor-dedup band test added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md
@.planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md
@.planning/phases/55-algorithm-mechanism-tuning/55-VALIDATION.md
@app/src/services/question-filter.service.ts
@app/src/services/canonical-knowledge.service.ts

<interfaces>
<!-- Threshold constants (verified, 55-RESEARCH §Threshold Audit) -->
- question-filter.service.ts:82 `export const OFF_TOPIC_SIMILARITY_THRESHOLD = 0.75`
- question-filter.service.ts:89 `export const MALICIOUS_SIMILARITY_THRESHOLD = 0.82` (security band 0.78-0.85)
- canonical-knowledge.service.ts:51 `const ANCHOR_PRE_CHECK_SIMILARITY_THRESHOLD = 0.82` (DISCONNECTED from settings 0.65)

<!-- EmbeddingDebugConfig current shape (types/index.ts lines 271-274) -->
- `interface EmbeddingDebugConfig { similarityThreshold: number; showScores: boolean }`

<!-- SettingsAIScreen single-slider block (lines 312-349) reads embeddingDebug.similarityThreshold -->
<!-- settingsService.getSync() is the synchronous read path used by services -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EmbeddingDebugConfig + wire per-threshold reads in both services (with malicious clamp)</name>
  <files>app/src/types/index.ts, app/src/services/settings.service.ts, app/src/services/question-filter.service.ts, app/src/services/canonical-knowledge.service.ts</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("types/index.ts EmbeddingDebugConfig extension", "question-filter.service.ts getActiveThresholds", "canonical-knowledge.service.ts debug-knob consumption")
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Threshold Audit" -> proposed EmbeddingDebugConfig; "Common Pitfalls" -> Pitfall 3 (shape-change migration on the kept settings key))
    - CLAUDE.md §"Question filter — dual-vector scoring" and §"Classification dedup — embedding pre-check" (the load-bearing 0.78-0.85 band and dual-vector invariant)
    - app/src/types/index.ts (EmbeddingDebugConfig lines 271-274)
    - app/src/services/settings.service.ts (defaultSettings embeddingDebug ~line 28, load() shape handling)
    - app/src/services/question-filter.service.ts (constants lines 82/89, layer2Embedding threshold comparison ~lines 212-225)
    - app/src/services/canonical-knowledge.service.ts (constant line 51, preCheckAnchorMatch cosine comparison)
  </read_first>
  <action>
    Extend `EmbeddingDebugConfig` additively per D-05: keep legacy `similarityThreshold` + `showScores`; add optional `debugEnabled?: boolean`, `offTopicThreshold?: number`, `maliciousThreshold?: number`, `anchorDedupThreshold?: number`. Optional/additive — no normalize framework (CLAUDE.md feedback_no_normalize_for_optional_fields). In settings.service.ts `load()` add the Pitfall-3 shape-change defaulting: if the new fields are undefined on a stored config, default offTopic 0.75 / malicious 0.82 / anchorDedup 0.82 / debugEnabled false. Do NOT reconnect the old 0.65 `similarityThreshold` to anchor dedup.
    In question-filter.service.ts add `getActiveThresholds(embDebug)`: when `debugEnabled !== true`, return the hardcoded `OFF_TOPIC_SIMILARITY_THRESHOLD` / `MALICIOUS_SIMILARITY_THRESHOLD`; when debug, return `offTopicThreshold ?? const` and `Math.min(0.85, Math.max(0.78, maliciousThreshold ?? const))` — the D-06 clamp in the SERVICE read path. Use the returned values in the existing threshold comparisons. Do NOT touch the rawVec/contextVec dual-vector construction.
    In canonical-knowledge.service.ts at the preCheckAnchorMatch comparison, read `settingsService.getSync().embeddingDebug`: when debugEnabled use `Math.min(0.85, Math.max(0.78, anchorDedupThreshold ?? const))` (anchor band also kept inside the empirical 0.78-0.85 per CLAUDE.md), else the hardcoded constant. The pre-check must STILL run before the tree descent (do not remove it).
    Add the D-02 dev-gated would-flip-distance instrumentation in layer2Embedding per the pattern (gated `import.meta.env?.DEV && debugEnabled`).
  </action>
  <verify>
    <automated>cd app && ./node_modules/.bin/tsc -b --noEmit 2>&1 | tail -10 && node --test tests/services/filter-classifier.unit.test.mjs tests/services/classification-dedup.test.mjs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `EmbeddingDebugConfig` has the four new optional fields; `similarityThreshold` and `showScores` retained.
    - `getActiveThresholds` clamps malicious to `Math.min(0.85, Math.max(0.78, ...))` in the service read path (grep both `0.78` and `0.85` in the function).
    - canonical-knowledge.service.ts reads `anchorDedupThreshold` and clamps it; the pre-check still runs before `buildStepPrompt('branch')` (classification-dedup.test.mjs still green).
    - In production mode (debugEnabled false) all three reads return the hardcoded constants — assert via a unit test or source read.
    - `filter-classifier.unit.test.mjs` Test 18d (dual-vector buried-payload) still green; `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Three per-threshold knobs drive the services when debugEnabled; production uses constants; malicious + anchor clamped in-service; dual-vector + pre-check invariants intact.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the single slider with three dev-gated knobs in SettingsAIScreen</name>
  <files>app/src/screens/settings/SettingsAIScreen.tsx</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-PATTERNS.md ("SettingsAIScreen.tsx" — the replacement block with master debugEnabled MaterialSwitch + three SettingRow range inputs, malicious min=0.78 max=0.85)
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Open Questions" #3 — clamp in both UI and service)
    - app/src/screens/settings/SettingsAIScreen.tsx (single-slider block lines 312-349; saveEmbeddingDebug handler)
    - app/src/screens/settings/SettingsShared.tsx (SettingRow, MaterialSwitch shapes)
    - CLAUDE.md §i18n Workflow ("Adding a new UI string" — any user-visible string needs all 4 bundles; but dev-only debug labels behind debugEnabled may stay English per discretion — document the choice)
  </read_first>
  <action>
    Replace the dead single-slider block (lines ~320-338) with: a master "Debug mode" `MaterialSwitch` bound to `embeddingDebug.debugEnabled`, and — rendered only when `debugEnabled` is true — three `SettingRow` range inputs per the 55-PATTERNS.md block: off-topic (min 0.60 max 0.95 step 0.01), malicious (min 0.78 max 0.85 step 0.01 — D-06 UI clamp), anchor-dedup (min 0.78 max 0.85 step 0.01 — clamped to the CLAUDE.md dedup band). Each onChange spreads into embeddingDebug and calls the existing `saveEmbeddingDebug`. This is the D-04 "hide the debug control in release" affordance: the knobs are inert/hidden unless debugEnabled is toggled on. These are dev-only debug labels — keep them English (do NOT add to en.json/zh/es/ja per the discretion note); document this in the SUMMARY so the Phase 56 i18n sweep does not flag them.
    Do not delete the legacy `similarityThreshold` field from the type; just stop rendering its slider.
  </action>
  <verify>
    <automated>cd app && ./node_modules/.bin/tsc -b --noEmit 2>&1 | tail -8 && grep -n "min={0.78}\|maliciousThreshold\|debugEnabled\|anchorDedupThreshold" app/src/screens/settings/SettingsAIScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The single `similarityThreshold` slider is no longer rendered; three labeled knobs + a `debugEnabled` MaterialSwitch are present.
    - The malicious knob input has `min={0.78}` and `max={0.85}` (UI clamp matching the service clamp).
    - The anchor-dedup knob input has `min={0.78}` and `max={0.85}` (mirrors the malicious clamp; CLAUDE.md §"Classification dedup" empirical band).
    - The three knobs only render when `embeddingDebug.debugEnabled` is true.
    - No new keys were added to any of `en/zh/es/ja.json` (dev-only labels stay English) — `node --test tests/locales/bundle-parity.test.mjs` still green.
    - `tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Three dev-gated per-threshold knobs replace the dead slider; malicious AND anchor-dedup UI-clamped 0.78-0.85; debug controls hidden in release; bundle parity intact.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the golden-fixture corpus + add the anchor-dedup band test</name>
  <files>app/tests/services/filter-golden-fixtures.test.mjs, app/tests/services/classification-dedup.test.mjs</files>
  <read_first>
    - .planning/phases/55-algorithm-mechanism-tuning/55-RESEARCH.md ("Code Examples" -> golden-fixture structure; "Validation Architecture" -> the golden test rows; the verbatim-jailbreak case)
    - .planning/phases/55-algorithm-mechanism-tuning/55-VALIDATION.md (Manual-Only: operator drives the dev slider, interesting cases are frozen here)
    - app/tests/services/filter-golden-fixtures.test.mjs (the 55-01 scaffold)
    - app/tests/services/classification-dedup.test.mjs (existing threshold-band test pattern to extend)
    - app/tests/services/_filter-classifier-mock-loader.mjs (the mock loader the golden test registers)
  </read_first>
  <action>
    Populate `filter-golden-fixtures.test.mjs` with the labeled corpus that captures the chosen thresholds as the D-03 evidence bar. Seed with the three research cases plus the security-critical buried-payload case: a benign 240-char preamble followed by a verbatim jailbreak must classify `malicious` (mirrors filter-classifier Test 18d intent at the golden level). Each fixture is `{ input, expectedLabel, note }`; the note records the captured cosine/rationale. These are the values the operator selected during the Manual-Only browser tuning session — the SUMMARY must note that the corpus is the durable artifact, not the live tuning session.
    In `classification-dedup.test.mjs` add the band test from 55-RESEARCH "Code Examples": assert `ANCHOR_PRE_CHECK_SIMILARITY_THRESHOLD` parsed from source is in `[0.78, 0.85]` (the existing file lacks this exact band assertion per the validation map).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/filter-golden-fixtures.test.mjs tests/services/classification-dedup.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `filter-golden-fixtures.test.mjs` contains at least the four labeled cases (off-topic greeting, on-topic learning question, verbatim jailbreak -> malicious, benign-preamble+verbatim-jailbreak -> malicious) and all pass at the chosen thresholds.
    - `classification-dedup.test.mjs` has a passing assertion that the anchor-dedup threshold source value is within `[0.78, 0.85]`.
    - Both files run green under `node --test`.
  </acceptance_criteria>
  <done>Golden corpus frozen as the regression bar (incl. the buried-payload case); anchor-dedup band asserted; both tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question -> filter classifier | Untrusted input scored against off-topic + malicious corpora |
| operator debug knob -> threshold read | A debug slider can move a security-relevant threshold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | malicious cosine threshold | mitigate | D-06: malicious clamped to [0.78, 0.85] in BOTH the slider (min=0.78 max=0.85) AND `getActiveThresholds` (`Math.min(0.85, Math.max(0.78, ...))`). Operator cannot detune below the band that re-opens the buried-payload evasion surface. Golden fixture asserts a benign-preamble+verbatim-jailbreak stays `malicious`. |
| T-55-03a | Elevation of Privilege | per-threshold debug knobs in release | mitigate | D-04: knobs render only when `embeddingDebug.debugEnabled === true`; services read live knobs only when debugEnabled; production falls back to hardcoded constants. Casual/release users never see or drive the security band. |
| T-55-03b | Tampering | anchor-dedup threshold | mitigate | Anchor-dedup knob also clamped to [0.78, 0.85] in-service (CLAUDE.md empirical band); pre-check still runs before tree descent so dedup is not bypassed. |
| T-55-03c | Spoofing | dual-vector buried-payload | mitigate | The contextVec/rawVec dual-vector construction is untouched; malicious always scored on the raw bare-content vector. Verified by re-running filter-classifier.unit.test.mjs Test 18d. |
</threat_model>

<verification>
- filter-classifier.unit.test.mjs (incl. Test 18d), classification-dedup.test.mjs, filter-golden-fixtures.test.mjs all green.
- bundle-parity.test.mjs green (no new locale keys).
- tsc -b --noEmit clean.
</verification>

<success_criteria>
- Three real, dev-gated, clamped per-threshold knobs replace the dead 0.65 slider.
- Anchor-dedup discrepancy resolved; pre-check intact.
- Malicious clamped in UI + service; golden corpus frozen.
</success_criteria>

<output>
Create `.planning/phases/55-algorithm-mechanism-tuning/55-03-SUMMARY.md` when done. Note the chosen final threshold values + that the golden corpus is the durable tuning artifact, and that the dev-only debug labels intentionally stay English.
</output>
