---
phase: 53-engagement-guardrails-provider-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/helpers/memory-localstorage.mjs
autonomous: true
requirements: [PRIVACY-01]
must_haves:
  truths:
    - "A test can install a Map-backed localStorage before importing a leaf service that calls localStorage.getItem/setItem"
    - "Seeding a sentinel via the shim and reading it back returns the seeded value"
    - "The helper is importable under node --test without pulling heavy/JSON-import-attribute deps"
    - "D-03: this shim enables the tests-and-structural-assertion enforcement approach (no runtime scrubber) by letting goldens seed the private localStorage keys"
  artifacts:
    - path: "app/tests/helpers/memory-localstorage.mjs"
      provides: "Leaf-safe in-memory localStorage shim for privacy goldens"
      contains: "makeMemoryLocalStorage"
  key_links:
    - from: "tests/providers/privacy-payload-tts.test.mjs"
      to: "tests/helpers/memory-localstorage.mjs"
      via: "import makeMemoryLocalStorage"
      pattern: "memory-localstorage"
---

<objective>
Create a tiny, leaf-safe, Map-backed in-memory `localStorage` shim helper that the PRIVACY-01 payload goldens use to seed private services (engagement / collections / graph-edit-journal) before importing the provider modules under test. `node --test` has no DOM, so the private services' `localStorage.getItem/setItem` calls would otherwise throw.

Purpose: Wave 0 support file. The two payload goldens (Plan 53-02) seed sentinel private data into localStorage keys, then drive a provider flow and assert the captured fetch body excludes the sentinels. Those goldens cannot run without this shim.
Output: `app/tests/helpers/memory-localstorage.mjs` exporting `makeMemoryLocalStorage()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-RESEARCH.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-VALIDATION.md

<interfaces>
<!-- The inline shim pattern already used in the repo. The new helper formalizes
     this into a reusable module. Executor should mirror this exact shape. -->

From app/tests/services/graph-command-service.reload-survival.test.mjs (inline precedent):
```
const storage = new Map();
globalThis.localStorage = {
  getItem: (k) => storage.has(k) ? storage.get(k) : null,
  setItem: (k, v) => storage.set(k, String(v)),
};
```

Private services this shim must satisfy (they call getItem/setItem; some also call removeItem/clear):
- app/src/services/engagement.service.ts — key `trellis_engagement_v1`
- app/src/services/collection.service.ts — key `trellis_collections_v1`
- app/src/services/graph-edit-journal.service.ts — key `trellis_graph_edit_log`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the leaf-safe Map-backed localStorage shim helper</name>
  <files>app/tests/helpers/memory-localstorage.mjs</files>
  <read_first>
    - app/tests/services/graph-command-service.reload-survival.test.mjs (inline shim precedent at lines 14-17 — copy this shape, formalize as a module export)
    - app/src/services/collection.service.ts (confirm it calls getItem/setItem; check for removeItem/clear usage so the shim covers every method the private services touch)
    - app/src/services/engagement.service.ts (same — confirm the localStorage surface area used)
    - app/src/services/graph-edit-journal.service.ts (same — confirm getItem/setItem usage on `trellis_graph_edit_log`)
  </read_first>
  <action>
    Create `app/tests/helpers/memory-localstorage.mjs` (a plain `.mjs` module, NOT a `.test.mjs` file — the `test:main` glob `find tests -name '*.test.mjs'` must NOT pick it up as a test). Export a named function `makeMemoryLocalStorage()` that returns an object backed by a private `Map`, implementing the full Web Storage surface the private services call: `getItem(k)` returns the stored string or `null` when absent; `setItem(k, v)` stores `String(v)`; `removeItem(k)` deletes the key; `clear()` empties the Map; and a `length` getter plus `key(i)` for completeness. Keep it dependency-free (no imports at all) so it stays leaf-safe under `node --test`. Do NOT auto-install onto `globalThis` inside the helper — return the object and let the importing golden assign `globalThis.localStorage = makeMemoryLocalStorage()` BEFORE its dynamic `import()` of the module under test (the goldens control install ordering). Add a top-of-file comment noting: (a) why it exists (node --test has no DOM; private services call localStorage), (b) that it must be installed before importing the provider/service module under test, (c) that it is NOT a test file.
  </action>
  <verify>
    <automated>cd app && node -e "import('./tests/helpers/memory-localstorage.mjs').then(m => { const ls = m.makeMemoryLocalStorage(); ls.setItem('k','SECRET-SENTINEL'); if (ls.getItem('k') !== 'SECRET-SENTINEL') throw new Error('roundtrip failed'); if (ls.getItem('absent') !== null) throw new Error('absent must be null'); ls.removeItem('k'); if (ls.getItem('k') !== null) throw new Error('removeItem failed'); ls.setItem('a','1'); ls.clear(); if (ls.getItem('a') !== null) throw new Error('clear failed'); console.log('OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/tests/helpers/memory-localstorage.mjs` exists and exports a named `makeMemoryLocalStorage`.
    - The verify node one-liner prints `OK` and exits 0: getItem roundtrips a seeded value, returns `null` for absent keys, `removeItem` deletes, and `clear` empties.
    - Filename ends in `.mjs` but NOT `.test.mjs` (running `cd app && node --test tests/helpers/memory-localstorage.mjs` is NOT expected; the file must be ignored by the `find tests -name '*.test.mjs'` glob — confirm by `find tests -name '*.test.mjs' | grep memory-localstorage` returning nothing).
    - The module has zero `import` statements (leaf-safe).
  </acceptance_criteria>
  <done>`makeMemoryLocalStorage()` returns a working Map-backed Storage object; importable under node with no deps; not matched by the test glob.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → module under test | The shim is the seam that lets a golden seed private localStorage data before the provider module reads it. Incorrect shim behavior could mask a real leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | privacy goldens (downstream, Plan 53-02) | mitigate | This shim provides the seam those goldens require; a faithful Web Storage shim ensures sentinels are actually present so a leak would be detectable. Roundtrip verified in this plan. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — this plan adds one dependency-free `.mjs` helper using only Node built-ins. |
</threat_model>

<verification>
`cd app && node --test tests/helpers/` is a no-op (no test files there); the helper is validated by the node one-liner in Task 1's verify. The downstream goldens (Plan 53-02) are the real consumers and exercise this shim end-to-end.
</verification>

<success_criteria>
- `app/tests/helpers/memory-localstorage.mjs` exists, exports `makeMemoryLocalStorage`, is dependency-free, and is not matched by the `test:main` glob.
- A seed/read/remove/clear roundtrip passes.
</success_criteria>

<output>
Create `.planning/phases/53-engagement-guardrails-provider-privacy/53-01-SUMMARY.md` when done.
</output>
