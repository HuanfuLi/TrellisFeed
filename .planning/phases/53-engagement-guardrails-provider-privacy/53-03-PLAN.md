---
phase: 53-engagement-guardrails-provider-privacy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/providers/privacy-callsite-structural.test.mjs
  - app/tests/learn-04-no-pushy-mechanics.test.mjs
autonomous: true
requirements: [PRIVACY-01, LEARN-04]
must_haves:
  truths:
    - "Provider chokepoint files do not import the three private services"
    - "Prompt-bearing provider call-sites do not read the private services, EXCEPT reorganizeMindmap which is an allowlisted, documented exception for the graph-edit-journal only"
    - "If any non-reorg provider call-site newly reads the graph-edit-journal, the structural test fails"
    - "No streak/leaderboard/stop-cue/mandated-goal/public-like construct exists in src/"
    - "The hidden `liked` engagement signal and the reward/fruit-credit/confetti harvest mechanic do NOT trip the LEARN-04 guard"
    - "D-01: phase is narrowed to this guardrail test + the PRIVACY-01 tests, no UI — the absence of coercive constructs this guard enforces is the realized scope"
    - "D-02: the non-pushy principle's deliverable is the *absence* of coercive mechanics, enforced by the LEARN-04 negative-invariant test in this plan"
    - "D-03: enforcement is tests + structural source-read assertion, not a runtime scrubber"
    - "D-04: the structural assertion protects the field inventory — collections, engagement, graph-edit-journal"
    - "D-05: negative-invariant source guard modelled on tests/components/InfoFlow.video-tap-emit.test.mjs"
    - "D-06: hidden `liked` signal and reward/fruit-credit/confetti are allowed and must not trip the guard"
    - "D-07: the graph-edit-journal is readable only by reorganizeMindmap (scoped exception); any other reader fails the structural assertion"
  artifacts:
    - path: "app/tests/providers/privacy-callsite-structural.test.mjs"
      provides: "Structural source-read assertion incl. reorg scoped exception (PRIVACY-01)"
      contains: "reorganizeMindmap"
    - path: "app/tests/learn-04-no-pushy-mechanics.test.mjs"
      provides: "Negative-invariant guard for non-pushy stance (LEARN-04)"
      contains: "streak"
  key_links:
    - from: "tests/providers/privacy-callsite-structural.test.mjs"
      to: "src/providers/llm/index.ts + prompt call-sites"
      via: "readFileSync + regex over source bytes"
      pattern: "readFileSync"
    - from: "tests/learn-04-no-pushy-mechanics.test.mjs"
      to: "src/**/*.{ts,tsx}"
      via: "recursive readFileSync + word-boundary regex"
      pattern: "readFileSync"
---

<objective>
Lock the two structural guarantees that need no runtime (pure source-byte reads, so heavy-import loadability is irrelevant):

1. PRIVACY-01 structural assertion — provider chokepoints and prompt-bearing call-sites do not import/read the private services (engagement / collections / graph-edit-journal), WITH a documented scoped exception (CONTEXT.md D-07): `reorganizeMindmap` in `canonical-knowledge.service.ts` is the ONLY allowlisted reader of the graph-edit-journal (Phase 48 GRAPH-04 — preserves manual graph edits across reorg). The test must FAIL if any OTHER call-site reads the journal.

2. LEARN-04 negative-invariant guard — `src/` contains no streak counter, leaderboard, public-like display/count, stop-cue interstitial, or mandated-goal mechanic. Must NOT false-positive on the allowed hidden `liked` signal or the intended reward/fruit-credit/confetti harvest loop.

Purpose: Success Criteria 1 (structural half of D-03) and 2 (the guardrail test).
Output: `app/tests/providers/privacy-callsite-structural.test.mjs`, `app/tests/learn-04-no-pushy-mechanics.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-CONTEXT.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-RESEARCH.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-VALIDATION.md

<interfaces>
<!-- Files the structural test reads (source bytes only — no import). VERIFIED in 53-RESEARCH.md. -->

Provider chokepoints (must NOT import any private service):
  src/providers/llm/index.ts
  src/providers/llm/locale-directive.ts
  src/providers/llm/user-content-bracketing.ts
  src/providers/tts/index.ts

Prompt-bearing call-sites (must NOT read private services — read bytes, no import needed):
  src/state/useQuestions.ts
  src/services/podcast.service.ts
  src/services/post-essay.service.ts
  src/services/flashcard.service.ts
  src/services/post-context-qa.service.ts
  src/services/concept-feed.service.ts  (reads engagement getDismissedAnchorIds — IDs used to FILTER, never prompt-interpolated; see note below)
  src/services/canonical-knowledge.service.ts  (THE EXCEPTION — reorganizeMindmap)

Private service identifiers to match (from 53-RESEARCH.md structural pattern):
  engagement.service | collection.service | graph-edit-journal

Reorg exception, VERIFIED:
  src/services/canonical-knowledge.service.ts
    :25  import { graphEditJournal } from './graph-edit-journal.service.ts';
    :26  import { phraseJournalEntry } from './graph-edit-journal-phrasing.ts';
    :1650-1654  reorganizeMindmap reads graphEditJournal.list() and injects phraseJournalEntry(entry) into the reorg system prompt.

LEARN-04 token inventory (from 53-RESEARCH.md — all forbidden counts = 0 today):
  FORBIDDEN (word-boundary): \bstreak\b, \bleaderboard\b, stopCue|stop-cue|stop cue, dailyGoal|daily goal|mandatedGoal, publicLike|public like|likeCount|likesCount
  ALLOWED (must NOT trip): liked / likePost / isLiked / getLikedPosts (14 files); trellisCreditsService / fruit_credits / confetti harvest; dailyReadService / "explored" / vine progress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structural call-site assertion with reorg scoped exception</name>
  <files>app/tests/providers/privacy-callsite-structural.test.mjs</files>
  <read_first>
    - app/tests/providers/llm-bracketing.test.mjs (lines 214-294 — the readFileSync + regex source-assertion seam; `resolve(here, '../../src/...')` path construction)
    - app/src/providers/llm/index.ts (confirm it imports NONE of the private services today)
    - app/src/providers/tts/index.ts (same)
    - app/src/services/canonical-knowledge.service.ts (lines 25-26 imports + 1650-1654 — confirm the journal read happens inside `reorganizeMindmap` and nowhere else in the file that reaches a non-reorg prompt; this is the allowlisted exception)
    - app/src/services/concept-feed.service.ts (confirm its engagement read is `getDismissedAnchorIds()` used as a filter, NOT prompt-interpolated — so it may import engagement.service for IDs; the structural test must not falsely fail it. Decide the matching strategy below accordingly.)
  </read_first>
  <action>
    Create `app/tests/providers/privacy-callsite-structural.test.mjs` modeled on `llm-bracketing.test.mjs:214`. Use `readFileSync(resolve(here, '../../src/<rel>'), 'utf-8')` (derive `here` from `fileURLToPath(import.meta.url)` + `dirname` as the analog does).

    Sub-test A — provider chokepoints: for each of `providers/llm/index.ts`, `providers/llm/locale-directive.ts`, `providers/llm/user-content-bracketing.ts`, `providers/tts/index.ts`, assert the source does NOT match `/engagement\.service|collection\.service|graph-edit-journal/`, message: "<rel> must not import a private user-data service (PRIVACY-01)".

    Sub-test B — graph-edit-journal scoped exception (D-07): assert that `src/services/canonical-knowledge.service.ts` IS allowed to reference `graph-edit-journal` (it is the documented exception — do NOT fail it), but that NO OTHER prompt-bearing call-site reads the journal. Concretely: define `JOURNAL_READERS_ALLOWED = ['services/canonical-knowledge.service.ts']`; for every other prompt-bearing call-site file (`state/useQuestions.ts`, `services/podcast.service.ts`, `services/post-essay.service.ts`, `services/flashcard.service.ts`, `services/post-context-qa.service.ts`, `services/concept-feed.service.ts`), assert the source does NOT match `/graph-edit-journal/`, message naming PRIVACY-01 + the D-07 exception. Add a clear comment block above sub-test B explaining the exception and its reason verbatim from D-07 ("reorganizeMindmap intentionally injects graph-edit-journal entries into the reorg prompt — Phase 48 GRAPH-04 — to stop the LLM undoing manual edits; this is the ONLY allowlisted reader; any other reader is a leak"). Also assert positively that `canonical-knowledge.service.ts` DOES reference `graphEditJournal` AND that the reference co-occurs with `reorganizeMindmap` in the file (e.g. both substrings present), so the exception stays anchored to the reorg function and a future move of the journal read into a different function in the same file is at least flagged for review — keep this a soft positive assertion documented in the message.

    Sub-test C — collections/engagement at prompt call-sites: for the prompt-bearing call-sites that should NOT read collections at all, assert source does NOT match `/collection\.service/`. For `engagement.service`: `concept-feed.service.ts` legitimately imports it for `getDismissedAnchorIds()` (ID-filter, not prompt). Do NOT blanket-ban engagement import there. Instead assert the narrow leak shape is absent: `concept-feed.service.ts` source must NOT contain a prompt-interpolation of liked/saved content — assert it does NOT match `getLikedPosts|getSavedPosts|likedPosts` (the ID-filter path uses `getDismissedAnchorIds`, not these). For the other call-sites (`useQuestions.ts`, `podcast.service.ts`, `post-essay.service.ts`, `flashcard.service.ts`, `post-context-qa.service.ts`) assert source does NOT match `/engagement\.service/`. Each assertion carries a PRIVACY-01 message.

    Do NOT import any source module — read bytes only.
  </action>
  <verify>
    <automated>cd app && node --test tests/providers/privacy-callsite-structural.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/providers/privacy-callsite-structural.test.mjs` exits 0 against the current codebase.
    - Sub-test A asserts all four provider chokepoint files are free of `engagement.service|collection.service|graph-edit-journal`.
    - Sub-test B allowlists exactly `services/canonical-knowledge.service.ts` for `graph-edit-journal` and fails if any of the other six listed call-sites references `graph-edit-journal`. The exception + Phase 48 GRAPH-04 reason is documented in code comments and assertion messages.
    - Sub-test C bans `collection.service` at all prompt call-sites; bans `engagement.service` at the five non-feed call-sites; for `concept-feed.service.ts` permits the `getDismissedAnchorIds` ID-filter import but bans `getLikedPosts|getSavedPosts|likedPosts` prompt leakage.
    - The test reads source via `readFileSync` only (no `import` of the source modules).
    - A self-check: temporarily appending `import { graphEditJournal } from './graph-edit-journal.service.ts'` to one of the banned files would make the test red (the executor may reason about this; do not commit such a change).
  </acceptance_criteria>
  <done>Structural assertion green; chokepoints clean; journal read allowlisted to reorg only; collections/engagement leak shapes banned at prompt call-sites with the documented D-07 exception.</done>
</task>

<task type="auto">
  <name>Task 2: LEARN-04 non-pushy negative-invariant guard</name>
  <files>app/tests/learn-04-no-pushy-mechanics.test.mjs</files>
  <read_first>
    - app/tests/components/InfoFlow.video-tap-emit.test.mjs (the negative-invariant source-read guard pattern to mirror: each `it()` reads source, asserts `matches.length === 0` with a descriptive rationale message)
    - 53-RESEARCH.md "LEARN-04 Forbidden / Allowed Token Inventory" (the exact forbidden word-boundary patterns and the allowed constructs that must NOT trip the guard)
  </read_first>
  <action>
    Create `app/tests/learn-04-no-pushy-mechanics.test.mjs` mirroring `InfoFlow.video-tap-emit.test.mjs`. Implement a small recursive `collectSourceFiles(dir)` that walks `resolve(here, '../src')` collecting `*.ts` and `*.tsx` (skip `node_modules`, `*.test.*`, `*.d.ts`). For each forbidden construct, one `it()` that scans every collected file and asserts zero matches, with a message explaining WHY it is forbidden and citing "LEARN-04, rescoped 2026-05-20: Trellis is reward-based, not mandate-based, never pushy". Forbidden patterns (word-boundary / phrase, case-insensitive): `/\bstreak\b/i`; `/\bleaderboard\b/i`; `/stopCue|stop-cue|stop cue/i`; `/dailyGoal|daily goal|mandatedGoal/i`; `/publicLike|public like|likeCount|likesCount/i`. Use word-boundary / specific constructs — NEVER a bare `/liked/` or `/like/` substring (would false-positive on the allowed hidden engagement signal across 14 files). Add an explicit POSITIVE guard-against-regression `it()` that asserts the guard does NOT match the allowed constructs by including a tiny inline fixture string containing `liked`, `getLikedPosts`, `isLiked`, `trellisCreditsService`, `fruit_credits`, `confetti`, `dailyReadService`, `explored` and asserting none of the forbidden regexes match it — this documents that the allowed reward/engagement vocabulary is intentionally NOT banned. On match failure, the message must list the offending file paths.
  </action>
  <verify>
    <automated>cd app && node --test tests/learn-04-no-pushy-mechanics.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/learn-04-no-pushy-mechanics.test.mjs` exits 0 against the current codebase (all forbidden counts are 0 today per research).
    - Five forbidden-construct `it()` blocks exist (streak, leaderboard, stop-cue, mandated/daily-goal, public-like/like-count), each using word-boundary or phrase patterns — NO bare `liked`/`like` substring.
    - A positive regression-guard `it()` confirms the allowed vocabulary (`liked`, `getLikedPosts`, `isLiked`, `trellisCreditsService`, `fruit_credits`, `confetti`, `dailyReadService`, `explored`) does NOT match any forbidden regex.
    - Each forbidden assertion's failure message explains the LEARN-04 rescope rationale and lists offending file paths.
    - The guard scans `src/**/*.{ts,tsx}` recursively via `readFileSync`, skipping `*.test.*` and `*.d.ts`.
  </acceptance_criteria>
  <done>LEARN-04 guard green; forbidden coercive constructs proven absent; allowed reward/engagement vocabulary proven not to trip the guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code → outbound provider payload | A future code change could wire a private-service read into a prompt-bearing call-site, leaking private data on the next request. |
| product design → engagement mechanics | A future change could add a coercive mechanic (streak/leaderboard/stop-cue/mandated goal) contrary to the locked non-pushy stance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02 | Information Disclosure | provider chokepoints + prompt-bearing call-sites | mitigate | Structural source-read test fails if any chokepoint imports a private service or any non-reorg call-site reads the graph-edit-journal/collections/engagement-prompt-leak. Documents the D-07 reorg scoped exception explicitly. |
| T-53-03 | Engagement ethics (non-STRIDE) | `src/**` engagement mechanics | mitigate | LEARN-04 negative-invariant guard fails if a coercive construct (streak/leaderboard/stop-cue/mandated-goal/public-like) is introduced; allowed hidden `liked` + reward loop explicitly exempted to avoid false positives. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — two `.test.mjs` files using only Node built-ins (`node:fs`, `node:path`, `node:test`, `node:assert`). |
</threat_model>

<verification>
`cd app && node --test tests/providers/privacy-callsite-structural.test.mjs tests/learn-04-no-pushy-mechanics.test.mjs` — both green. Picked up automatically by `npm run test:main`.
</verification>

<success_criteria>
- Structural assertion green: chokepoints free of private-service imports; journal read allowlisted to `reorganizeMindmap` only; other call-sites free of journal/collection/engagement-prompt leaks.
- LEARN-04 guard green: zero forbidden constructs; allowed reward/engagement vocabulary not tripped.
</success_criteria>

<output>
Create `.planning/phases/53-engagement-guardrails-provider-privacy/53-03-SUMMARY.md` when done.
</output>
