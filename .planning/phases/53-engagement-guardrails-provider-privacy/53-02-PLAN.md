---
phase: 53-engagement-guardrails-provider-privacy
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - app/tests/providers/privacy-payload-tts.test.mjs
  - app/tests/providers/privacy-payload-llm.test.mjs
autonomous: true
requirements: [PRIVACY-01]
must_haves:
  truths:
    - "A TTS synthesize call, with private services seeded with sentinels, produces an outbound body whose `input` field contains no sentinel"
    - "An LLM chatCompletion call for openAI, claude, and gemini, with private services seeded with sentinels, produces an outbound body whose message/content fields contain no sentinel"
    - "Both goldens capture the real outbound fetch body without making a network call"
    - "D-03: these payload goldens are the test-based half of the tests-and-structural-assertion enforcement (no runtime scrubber)"
    - "D-04: goldens seed and assert exclusion of the full field inventory — tags/collections (trellis_collections_v1), engagement saved/liked/etc (trellis_engagement_v1), graph correction logs (trellis_graph_edit_log)"
  artifacts:
    - path: "app/tests/providers/privacy-payload-tts.test.mjs"
      provides: "TTS payload privacy golden (PRIVACY-01)"
      contains: "SECRET"
    - path: "app/tests/providers/privacy-payload-llm.test.mjs"
      provides: "LLM payload privacy golden across 3 cloud providers (PRIVACY-01)"
      contains: "gemini"
  key_links:
    - from: "tests/providers/privacy-payload-tts.test.mjs"
      to: "src/providers/tts/index.ts"
      via: "dynamic import of synthesize after fetch+localStorage shims"
      pattern: "providers/tts/index.ts"
    - from: "tests/providers/privacy-payload-llm.test.mjs"
      to: "src/providers/llm/index.ts"
      via: "dynamic import of chatCompletion after fetch+localStorage shims"
      pattern: "providers/llm/index.ts"
---

<objective>
Lock PRIVACY-01 with two fetch-stub golden tests proving private user data never reaches the outbound provider request body. Each golden seeds the three private services with unique sentinel strings, installs the Plan 53-01 localStorage shim and a fetch capture shim, drives the leaf provider chokepoint (`synthesize` for TTS; `chatCompletion` for each of openAI/claude/gemini for LLM), and asserts the JSON-stringified captured request body contains none of the sentinels.

Purpose: Success Criterion 1 — "Provider-bound LLM and TTS payload tests confirm tags, saved/liked/history, and graph correction logs are excluded from outbound provider requests by default." These are the representative-flow goldens of D-03.
Output: `app/tests/providers/privacy-payload-tts.test.mjs`, `app/tests/providers/privacy-payload-llm.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-RESEARCH.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-VALIDATION.md
@.planning/phases/53-engagement-guardrails-provider-privacy/53-01-SUMMARY.md

<interfaces>
<!-- Outbound payload field names the goldens assert against. From 53-RESEARCH.md
     "Exact outbound payload shapes" — VERIFIED by direct source read. -->

TTS (src/providers/tts/index.ts, synthesize :44):
  body = { model, input: <text>, voice, speed }   // only content field is `input`
  URL: ${base}/v1/audio/speech

OpenAI/local (src/providers/llm/index.ts, openAICompletion :157):
  body = { model, messages: [{role,content}...], max_tokens, stream, response_format? }
  URL: ${base}/v1/chat/completions
  NOTE: native local path uses CapacitorHttp (not window.fetch). In Node, Capacitor.isNativePlatform() is false → cloud fetch path runs. Assert the cloud path.

Claude (claudeCompletion :206):
  body = { model, max_tokens, system: <system content|undefined>, messages: [{role,content}...] }
  URL: https://api.anthropic.com/v1/messages

Gemini (toGeminiPayload :273):
  body = { contents:[{role, parts:[{text}]}...], systemInstruction?, generationConfig }
  URL: ${GEMINI_BASE}/models/${model}:generateContent

Private localStorage keys to seed (from src/services/*, key names VERIFIED):
  trellis_collections_v1   (collection.service.ts — tags/bookmarks)
  trellis_engagement_v1    (engagement.service.ts — saved/liked/dismissed/savedPodcasts)
  trellis_graph_edit_log   (graph-edit-journal.service.ts — graph correction logs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TTS payload privacy golden</name>
  <files>app/tests/providers/privacy-payload-tts.test.mjs</files>
  <read_first>
    - app/tests/providers/tts-locale.test.mjs (lines 27-54 — the canonical fetch-stub seam: fakeFetch capturing `JSON.parse(init.body)`, window/globalThis/URL shims, dynamic import AFTER shims; also the i18next.init + bindI18nLeaf preamble that `synthesize` needs because `resolveVoice` reads locale)
    - app/tests/helpers/memory-localstorage.mjs (the Plan 53-01 shim — import `makeMemoryLocalStorage`)
    - app/src/providers/tts/index.ts (confirm `synthesize` signature, the `input` body field, and that `resolveVoice` reads i18n locale not a private service)
    - app/src/services/collection.service.ts (confirm the exact JSON shape stored under `trellis_collections_v1` so the seeded sentinel is shaped like real data)
  </read_first>
  <action>
    Create `app/tests/providers/privacy-payload-tts.test.mjs` modeled on `tts-locale.test.mjs:27-54`. Preamble: `i18next.init({lng:'en',fallbackLng:'en',resources:{en:{translation:{}},zh:{translation:{}},es:{translation:{}},ja:{translation:{}}}})` then `bindI18nLeaf(i18next.t.bind(i18next), () => i18next.language)` (copy verbatim from the analog — `synthesize`'s `resolveVoice` needs it). Install `globalThis.localStorage = makeMemoryLocalStorage()` BEFORE the dynamic import. Seed three sentinels into the real localStorage keys, shaped to match each service's stored JSON: `trellis_collections_v1` with a collection named `SECRET-TAG-SENTINEL`; `trellis_engagement_v1` with a liked/saved entry whose id or note is `SECRET-ENGAGEMENT-SENTINEL`; `trellis_graph_edit_log` with a rename entry whose node title is `SECRET-JOURNAL-SENTINEL`. Install the fakeFetch capture shim (captures `JSON.parse(init.body)` into `captured`, returns the minimal `{ok,status,blob,text}` Response from the analog), set `globalThis.window.fetch`, `globalThis.fetch`, and `globalThis.URL.createObjectURL`. Dynamic-import `synthesize` from `../../src/providers/tts/index.ts` AFTER all shims. Call `await synthesize('Recap of spaced repetition.', baseTtsConfig)` with a `baseTtsConfig` mirroring the analog's `baseConfig` (provider openai, apiKey, baseUrl, voice, speed). Assert `captured.input === 'Recap of spaced repetition.'` (proves capture worked) AND `!JSON.stringify(captured).includes('SECRET-TAG-SENTINEL')`, `...includes('SECRET-ENGAGEMENT-SENTINEL')`, `...includes('SECRET-JOURNAL-SENTINEL')` are all false, each with a message naming PRIVACY-01 and the field. Do NOT import any heavy prompt-bearing service.
  </action>
  <verify>
    <automated>cd app && node --test tests/providers/privacy-payload-tts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/providers/privacy-payload-tts.test.mjs` exits 0 with all assertions passing.
    - The test seeds all three keys (`trellis_collections_v1`, `trellis_engagement_v1`, `trellis_graph_edit_log`) with the exact sentinels `SECRET-TAG-SENTINEL`, `SECRET-ENGAGEMENT-SENTINEL`, `SECRET-JOURNAL-SENTINEL`.
    - A positive assertion confirms capture worked (`captured.input` equals the synthesized text) so the negative assertions cannot pass vacuously.
    - The file installs `globalThis.localStorage` via `makeMemoryLocalStorage()` from the Plan 53-01 helper BEFORE the dynamic import of `synthesize`.
    - No import of `useQuestions`, `podcast.service`, `concept-feed.service`, or `canonical-knowledge.service` (would fail node --test load).
  </acceptance_criteria>
  <done>TTS golden green; outbound `input` proven free of all three private sentinels with a non-vacuous capture assertion.</done>
</task>

<task type="auto">
  <name>Task 2: LLM payload privacy golden across openAI / claude / gemini</name>
  <files>app/tests/providers/privacy-payload-llm.test.mjs</files>
  <read_first>
    - app/tests/providers/tts-locale.test.mjs (lines 27-54 — same fetch-stub seam)
    - app/tests/providers/llm-bracketing.test.mjs (how it imports and calls the LLM chokepoint; how it constructs a ChatMessage[] and an LLMConfig per provider)
    - app/tests/helpers/memory-localstorage.mjs (import `makeMemoryLocalStorage`)
    - app/src/providers/llm/index.ts (confirm `chatCompletion` signature; per-provider body builders openAICompletion :157, claudeCompletion :206, toGeminiPayload :273; confirm cloud path uses global fetch and that `Capacitor.isNativePlatform()` is false under Node so the cloud branch runs)
  </read_first>
  <action>
    Create `app/tests/providers/privacy-payload-llm.test.mjs` modeled on the same fetch-stub seam. For the streaming caveat: since this golden cares about the REQUEST body, use `chatCompletion` (non-stream). The fake `json()` MUST return a UNION response shape that all three providers can parse WITHOUT throwing after the body is captured — `claudeCompletion` reads `data.content[0].text`, `openAICompletion` reads `data.choices[0].message.content`, and the gemini builder reads `data.candidates[0].content.parts[0].text`. Return one object satisfying all three: `{ ok, status, async text(){return ''}, async json(){return { choices:[{message:{content:''}}], content:[{text:''}], candidates:[{content:{parts:[{text:''}]}}] }} }`. (Plan-checker WARNING 1: the OpenAI-only stub throws an uncaught rejection on the claude sub-test because `data.content` is undefined.) Prefer the non-stream path to keep the stub minimal. Install `globalThis.localStorage = makeMemoryLocalStorage()` and seed the SAME three sentinels into `trellis_collections_v1`, `trellis_engagement_v1`, `trellis_graph_edit_log` as Task 1. Install the fakeFetch capture shim. Dynamic-import `chatCompletion` from `../../src/providers/llm/index.ts` AFTER shims. Build a representative `messages` array (a system message + a user message containing benign content like `What is spaced repetition?`). Run THREE sub-tests (one per provider) — openAI, claude, gemini — each with an `LLMConfig` for that provider (apiKey, baseUrl/model as the analog does). After each call: a positive capture assertion that the user content appears in the stringified body (proves capture + the right field), then assert `JSON.stringify(captured)` does NOT include any of the three sentinels, each with a PRIVACY-01 message naming the provider. Per provider, assert against the provider-specific content location is unnecessary — stringify-the-whole-body is sufficient and matches the research's golden strategy. Do NOT import any heavy prompt-bearing service; build the `messages` array inline.
  </action>
  <verify>
    <automated>cd app && node --test tests/providers/privacy-payload-llm.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/providers/privacy-payload-llm.test.mjs` exits 0.
    - Three provider sub-tests run: openAI, claude, gemini (provider names appear in the test/it descriptions).
    - Each sub-test seeds the same three sentinels and asserts none appear in the captured body; plus a non-vacuous positive assertion that the benign user content IS present in the captured body.
    - Installs the Plan 53-01 localStorage shim before the dynamic import of `chatCompletion`.
    - No import of `useQuestions`, `podcast.service`, `concept-feed.service`, or `canonical-knowledge.service`.
  </acceptance_criteria>
  <done>LLM golden green across all three cloud providers; outbound message content proven free of all three private sentinels with non-vacuous capture assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device localStorage → provider fetch body | Private user data lives in `trellis_*` localStorage keys; the threat is it being interpolated into an outbound LLM/TTS request and leaving the device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | `providers/tts/index.ts` `synthesize`, `providers/llm/index.ts` `chatCompletion` | mitigate | Golden tests seed sentinel private data into all three protected keys and assert the captured outbound body excludes them. Non-vacuous capture assertions prevent false-green. Covers the representative TTS + LLM (3 providers) flows per D-03. |
| T-53-02 | Information Disclosure | future code adding a private-data read to a prompt | transfer | Out of this plan's scope — covered by the structural source-read assertion in Plan 53-03. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — two `.test.mjs` files using only Node built-ins + the in-repo shim. |
</threat_model>

<verification>
`cd app && node --test tests/providers/privacy-payload-tts.test.mjs tests/providers/privacy-payload-llm.test.mjs` — both green. Per VALIDATION.md, `npm run test:main` at wave merge picks both up automatically (they match the `find tests -name '*.test.mjs'` glob).
</verification>

<success_criteria>
- Both goldens green.
- TTS `input` and LLM message content provably exclude `SECRET-TAG-SENTINEL`, `SECRET-ENGAGEMENT-SENTINEL`, `SECRET-JOURNAL-SENTINEL`.
- Capture assertions are non-vacuous (benign content present), so the negative assertions are meaningful.
</success_criteria>

<output>
Create `.planning/phases/53-engagement-guardrails-provider-privacy/53-02-SUMMARY.md` when done.
</output>
