---
phase: 32-v1-4-verification-close-out-and-uat-retest
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md
  - .planning/phases/29-final-polishment/29-VALIDATION.md
  - .planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md
  - .planning/STATE.md
  - .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md
autonomous: true
requirements: [D-10, D-11, D-12]  # D-12 enforces: 31-VALIDATION.md MUST NOT appear in any commit touched by this plan (already validated — hands off). Task 03-03 Step 3 + acceptance criteria check git log to confirm.
gap_closure: true

must_haves:
  truths:
    - "28-VALIDATION.md frontmatter changes status:draft -> validated, nyquist_compliant:false -> true, wave_0_complete:false -> true, adds validated: 2026-04-18"
    - "29-VALIDATION.md frontmatter changes status:draft -> validated, nyquist_compliant:false -> true, wave_0_complete:false -> true, adds validated: 2026-04-18"
    - "30-VALIDATION.md frontmatter flipped ONLY IF 30-VERIFICATION.md has status: passed AND zero BLOCKED rows (per D-11 gate)"
    - "If 30-VERIFICATION.md is gaps_found, 30-VALIDATION.md stays draft and task logs the reason AND explicitly reports outcome: PASS-AND-HELD"
    - "31-VALIDATION.md is NOT TOUCHED (already validated per D-12) — enforced by git log --stat check in task 03-03"
    - "32-VALIDATION.md updated to status: validated, nyquist_compliant: true, wave_0_complete: true to reflect Phase 32 completion"
    - "STATE.md records Phase 32 as COMPLETE with close-out date"
    - "Preserved frontmatter fields (phase, slug, created) unchanged in all flips"
  artifacts:
    - path: ".planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 28"
      contains: "status: validated"
    - path: ".planning/phases/29-final-polishment/29-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 29"
      contains: "status: validated"
    - path: ".planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 30 (gated on 30-VERIFICATION passing clean)"
      contains: "status: validated OR still-draft (if gap_found)"
    - path: ".planning/STATE.md"
      provides: "Phase 32 close-out record"
      contains: "Phase 32"
  key_links:
    - from: "30-VERIFICATION.md status field (from plan 02)"
      to: "30-VALIDATION.md flip decision"
      via: "D-11 gate — only flip if status: passed + zero BLOCKED rows"
      pattern: "status: passed"
    - from: "Plan 03 commit's git log --stat"
      to: "31-VALIDATION.md (must NOT appear)"
      via: "D-12 hands-off enforcement — 31-VALIDATION.md was already flipped pre-Phase-32"
      pattern: "31-VALIDATION.md absent"

---

<objective>
Flip VALIDATION.md frontmatter on Phases 28 and 29 (pure doc drift — VERIFICATION already passed), conditionally flip Phase 30 VALIDATION.md (gated on 30-VERIFICATION.md passing clean per D-11), confirm Phase 31 VALIDATION.md remains untouched (D-12 hands-off), update STATE.md with Phase 32 completion, and flip 32-VALIDATION.md itself to reflect Phase 32 close-out.

Purpose: Close D-10 + D-11 + D-12 from 32-CONTEXT.md. Without these flips, `/gsd:audit-milestone v1.4` reports phases 28/29/30 as `nyquist: draft` even though VERIFICATION passed — the milestone cannot close. D-12 is enforced by a git log check: the plan 03 commit MUST NOT touch 31-VALIDATION.md.

Output:
- `.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md` — 3-field flip + 1 field added
- `.planning/phases/29-final-polishment/29-VALIDATION.md` — 3-field flip + 1 field added
- `.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md` — same flip IF D-11 gate passes, else documented hold
- `.planning/STATE.md` — Phase 32 COMPLETE record
- `.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md` — own flip
- Atomic commit: "docs(32-03): flip VALIDATION.md status on phases 28/29/30 + record Phase 32 close-out"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-CONTEXT.md
@.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-RESEARCH.md
@.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md
@.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md
@.planning/phases/29-final-polishment/29-VALIDATION.md
@.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md
@.planning/phases/31-curiosity-feed-redesign-post-lifecycle-and-display/31-VALIDATION.md
@.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md
@.planning/phases/31-curiosity-feed-redesign-post-lifecycle-and-display/31-VERIFICATION.md

<interfaces>
<!-- Exact frontmatter transformations per 32-RESEARCH.md Pattern 4 -->

### 28-VALIDATION.md flip

**Before** (lines 1-8):
```yaml
    ---
phase: 28
slug: ui-ux-polish-from-audit-findings
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
    ---
```

**After**:
```yaml
    ---
phase: 28
slug: ui-ux-polish-from-audit-findings
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-16
validated: 2026-04-18
    ---
```

Fields changed: status, nyquist_compliant, wave_0_complete. Field added: validated.
Fields preserved: phase, slug, created.

### 29-VALIDATION.md flip

**Before**: identical shape to 28 above (phase/slug swapped).
**After**: identical transformation — all 3 flips + 1 add.

### 30-VALIDATION.md flip (D-11 GATED)

**D-11 Gate condition** (from 32-CONTEXT.md + RESEARCH.md):
- `test -f .planning/phases/30-*/30-VERIFICATION.md` MUST return 0 (file exists from plan 02)
- `grep -c "^status: passed$" .planning/phases/30-*/30-VERIFICATION.md` MUST return 1
- `grep -cE "BLOCKED|UNVERIFIED|PARTIAL" .planning/phases/30-*/30-VERIFICATION.md` MUST return 0

If ALL 3 gate conditions pass → flip 30-VALIDATION.md same as 28/29.
If ANY gate fails → 30-VALIDATION.md stays draft, task log records the reason with `outcome: PASS-AND-HELD`.

**Before**: identical shape (phase: 30, slug: redesign-curiosity-feed-...).
**After** (if gate passes): status:validated, nyquist_compliant:true, wave_0_complete:true + validated:2026-04-18.

### 31-VALIDATION.md — DO NOT TOUCH (per D-12)

Already at status:validated, nyquist_compliant:true, validated:2026-04-18, re_audited:2026-04-18. Zero changes. Verified by `git log -1 --stat` which MUST show zero hits for "31-VALIDATION.md". Acceptance criterion in task 03-03 enforces this.

### 32-VALIDATION.md self-flip

**Before**:
```yaml
    ---
phase: 32
slug: v1-4-verification-close-out-and-uat-retest
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-18
    ---
```

**After**:
```yaml
    ---
phase: 32
slug: v1-4-verification-close-out-and-uat-retest
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-18
validated: 2026-04-18
    ---
```

### STATE.md record

Append (or update) a Phase 32 completion line. Use the session-record tool if available:
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state record-session \
  --phase 32 \
  --status complete \
  --note "v1.4 verification close-out + UAT retest — phases 28/29/30 VALIDATION flipped, 30 & 31 VERIFICATION written, 31-UAT retested"
```

If the tool is unavailable, edit STATE.md directly to append under the "Phase Status" or equivalent tracking section a line like:
```
Phase 32: COMPLETE (2026-04-18) — v1.4 verification close-out: 30 & 31 VERIFICATION.md written, 28/29/30 VALIDATION.md flipped, 31-UAT retest green
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-01: Flip 28-VALIDATION.md + 29-VALIDATION.md frontmatter (unconditional per D-10)</name>
  <files>
    .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md,
    .planning/phases/29-final-polishment/29-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md (lines 1-8 — current frontmatter)
    - .planning/phases/29-final-polishment/29-VALIDATION.md (lines 1-8 — current frontmatter)
    - .planning/phases/31-curiosity-feed-redesign-post-lifecycle-and-display/31-VALIDATION.md (reference shape — already validated; this is the target shape)
    - .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-RESEARCH.md (Pattern 4 — exact field-by-field transformation)
    - .planning/phases/28-ui-ux-polish-from-audit-findings/28-VERIFICATION.md (confirms 30/30 pass — basis for nyquist_compliant:true flip per D-10)
    - .planning/phases/29-final-polishment/29-VERIFICATION.md (confirms 10/10 pass — basis for nyquist_compliant:true flip per D-10)
  </read_first>
  <action>
    Edit the frontmatter (lines 1-8) of `.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md`:

    - Change line 4: `status: draft` -> `status: validated`
    - Change line 5: `nyquist_compliant: false` -> `nyquist_compliant: true`
    - Change line 6: `wave_0_complete: false` -> `wave_0_complete: true`
    - After line 7 (`created: 2026-04-16`), insert new line 8: `validated: 2026-04-18`
    - The closing `---` shifts to line 9

    All other content in the file below the frontmatter (lines 9-117 originally — all the validation strategy body) MUST stay exactly as-is. Only touch lines 1-8.

    Justification per D-10: 28-VERIFICATION.md already shows status:passed with 30/30 decisions verified. Changing VALIDATION to match is pure documentation drift correction. wave_0_complete flips from false to true because all Wave 0 test files (`tests/lib/swipe-tab-logic.test.mjs`, `tests/components/BottomNavigation.slide.test.mjs`, etc.) exist and pass per 28-VERIFICATION.md evidence.

    Then apply the IDENTICAL transformation to `.planning/phases/29-final-polishment/29-VALIDATION.md`:
    - Same 3-field flip
    - Same 1-field addition (`validated: 2026-04-18`)

    Justification per D-10: 29-VERIFICATION.md shows 10/10 truths verified + all requirements SATISFIED. wave_0_complete flips because `tests/screens/post-detail-abort.test.mjs` exists and passes (10/10) per 29-VERIFICATION.md line 43.

    Do NOT commit yet — commit happens in task 03-03 after all flips in this plan are grouped atomically.
  </action>
  <verify>
    <automated>bash -c 'count1=$(grep -c "^status: validated$" /Users/Code/EchoLearn/.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md); count2=$(grep -c "^status: validated$" /Users/Code/EchoLearn/.planning/phases/29-final-polishment/29-VALIDATION.md); [ "$count1" = "1" ] && [ "$count2" = "1" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: validated$" .planning/phases/28-*/28-VALIDATION.md` returns 1
    - `grep -c "^status: validated$" .planning/phases/29-*/29-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/phases/28-*/28-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/phases/29-*/29-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/phases/28-*/28-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/phases/29-*/29-VALIDATION.md` returns 1
    - `grep -c "^validated: 2026-04-18$" .planning/phases/28-*/28-VALIDATION.md` returns 1
    - `grep -c "^validated: 2026-04-18$" .planning/phases/29-*/29-VALIDATION.md` returns 1
    - `grep -c "^phase: 28$" .planning/phases/28-*/28-VALIDATION.md` returns 1 (preserved)
    - `grep -c "^phase: 29$" .planning/phases/29-*/29-VALIDATION.md` returns 1 (preserved)
    - `grep -c "^created: 2026-04-16$" .planning/phases/28-*/28-VALIDATION.md` returns 1 (preserved)
    - `grep -c "^created: 2026-04-16$" .planning/phases/29-*/29-VALIDATION.md` returns 1 (preserved)
    - Body content below frontmatter unchanged — `wc -l` of both files equals original + 1 (only `validated:` line added)
    - The automated `<verify>` command exits 0 (equality check via bash `[ "$count1" = "1" ] && [ "$count2" = "1" ]`)
  </acceptance_criteria>
  <done>28-VALIDATION.md and 29-VALIDATION.md frontmatter flipped to validated/nyquist_compliant:true/wave_0_complete:true + validated:2026-04-18 line added. Body content untouched. Preserved fields (phase, slug, created) intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-02: Flip 30-VALIDATION.md frontmatter CONDITIONALLY on D-11 gate</name>
  <files>.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md</files>
  <read_first>
    - .planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md (MUST EXIST from plan 02 — this plan's gate depends on its frontmatter)
    - .planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md (lines 1-8 — current frontmatter)
    - .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-CONTEXT.md (D-11 gate definition)
    - .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-RESEARCH.md (Pattern 4 — 30-VALIDATION shape; D-11 gate grep)
  </read_first>
  <action>
    **Step 1 — Evaluate D-11 gate.** Run these 3 checks and record outputs:

    ```bash
    # Gate check 1: file exists
    test -f /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md && echo "GATE-1-PASS" || echo "GATE-1-FAIL"

    # Gate check 2: status: passed in frontmatter
    grep -c "^status: passed$" /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md

    # Gate check 3: zero BLOCKED/UNVERIFIED/PARTIAL rows
    grep -cE "BLOCKED|UNVERIFIED|PARTIAL" /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md
    ```

    Decision logic:
    - If gate check 1 returns `GATE-1-PASS` AND gate check 2 returns `1` AND gate check 3 returns `0` -> **FLIP 30-VALIDATION.md** (proceed to Step 2) and record `outcome: PASS-AND-FLIPPED`
    - If any gate check fails -> **HOLD 30-VALIDATION.md** in draft and record the reason with `outcome: PASS-AND-HELD` (proceed to Step 3)

    **Step 2 (gate passed) — Flip 30-VALIDATION.md frontmatter:**

    Same transformation as task 03-01:
    - Line 4: `status: draft` -> `status: validated`
    - Line 5: `nyquist_compliant: false` -> `nyquist_compliant: true`
    - Line 6: `wave_0_complete: false` -> `wave_0_complete: true`
    - After line 7 (`created: 2026-04-17`), insert: `validated: 2026-04-18`
    - Body content (lines 9-82 originally) unchanged

    Justification per D-11: 30-VERIFICATION.md was written in plan 02 with status:passed and zero BLOCKED/PARTIAL rows, so the Nyquist contract is now satisfiable and the VALIDATION.md flip is warranted. wave_0_complete flips because Phase 30's wave-0 test file (`tests/services/daily-read.service.test.mjs` per 30-VALIDATION.md line 53) exists and passes.

    Record in task log:
    ```
    outcome: PASS-AND-FLIPPED
    gate_check_1: GATE-1-PASS
    gate_check_2: 1 (status: passed present)
    gate_check_3: 0 (no BLOCKED/UNVERIFIED/PARTIAL)
    ```

    **Step 3 (gate failed) — HOLD:**

    Leave 30-VALIDATION.md unchanged. Record in task log and 32-03-SUMMARY.md:
    ```
    outcome: PASS-AND-HELD
    reason: 30-VERIFICATION.md status = gaps_found | gate check N returned X
    gate_check_1: <value>
    gate_check_2: <value>
    gate_check_3: <value>
    fix: Go back to plan 32-02 and address the gap in 30-VERIFICATION.md, then rerun plan 32-03 task 03-02.
    ```

    The commit in task 03-03 will still proceed for 28/29/32/STATE flips — just without 30-VALIDATION.md changes.

    Do NOT flip 30-VALIDATION.md under any circumstance if the gate fails. The integrity of the Nyquist-compliance signal depends on this strict gating per 32-RESEARCH.md Pitfall 4.
  </action>
  <verify>
    <automated>bash -c 'test -f /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VERIFICATION.md && grep -E "^status: (draft|validated)$" /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md'</automated>
  </verify>
  <acceptance_criteria>
    - Task log includes an explicit `outcome:` field set to either `PASS-AND-FLIPPED` or `PASS-AND-HELD (reason: ...)` (per NOTE 10 fix — outcome MUST be reported, not implicit)
    - EITHER: gate passed + outcome=PASS-AND-FLIPPED -> all four of `^status: validated$`, `^nyquist_compliant: true$`, `^wave_0_complete: true$`, `^validated: 2026-04-18$` each return 1 hit in 30-VALIDATION.md via grep; OR
    - Gate failed + outcome=PASS-AND-HELD -> 30-VALIDATION.md unchanged (grep `^status: draft$` still returns 1) AND task log records the specific gate check that failed
    - In either outcome: `grep -c "^phase: 30$" 30-VALIDATION.md` returns 1 (preserved)
    - In either outcome: `grep -c "^created: 2026-04-17$" 30-VALIDATION.md` returns 1 (preserved)
    - No scenario leaves 30-VALIDATION.md in a half-flipped state (e.g., status:validated but nyquist_compliant:false)
  </acceptance_criteria>
  <done>30-VALIDATION.md either fully flipped with outcome=PASS-AND-FLIPPED (all 3 fields + added validated line) if 30-VERIFICATION.md passed clean, OR unchanged with outcome=PASS-AND-HELD and documented hold reason. No half-flipped state. D-11 gate respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-03: Flip 32-VALIDATION.md + update STATE.md + commit all VALIDATION flips atomically (enforce D-12 hands-off)</name>
  <files>
    .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md (lines 1-8 — current frontmatter)
    - .planning/STATE.md (read to determine where to record Phase 32 completion — offset 0 limit 100 is enough for the header/phase tracker section)
    - .planning/phases/31-curiosity-feed-redesign-post-lifecycle-and-display/31-VALIDATION.md (confirm NOT touched per D-12 — `git diff --stat` MUST NOT include this file; verified in acceptance criteria)
  </read_first>
  <action>
    **Step 1 — Flip 32-VALIDATION.md frontmatter:**

    Edit `.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md` lines 1-8:
    - Line 4: `status: draft` -> `status: validated`
    - Line 5: `nyquist_compliant: false` -> `nyquist_compliant: true`
    - Line 6: `wave_0_complete: false` -> `wave_0_complete: true`
    - After line 7 (`created: 2026-04-18`), insert: `validated: 2026-04-18`

    Justification: Phase 32 itself is now complete — plan 32-01 delivered the UAT retest + dropdown fix, 32-02 delivered 30 & 31 VERIFICATION.md. 32-VALIDATION.md Wave 0 was already declared a no-op per its own line 56. All per-task verification map entries are green.

    **Step 2 — Update STATE.md with Phase 32 completion record:**

    Try using the gsd-tools session record command first:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state record-session \
      --phase 32 \
      --status complete \
      --note "v1.4 verification close-out: 30 & 31 VERIFICATION.md written, 28/29/30 VALIDATION.md flipped, 31-UAT retest green + dropdown fixed (UAT-31-11)"
    ```

    If the tool is unavailable or fails, edit STATE.md directly. Find the phase-status tracking section (or create one if missing) and add/update a line:
    ```
    Phase 32: COMPLETE (2026-04-18) — v1.4 verification close-out & UAT retest. 30 & 31 VERIFICATION.md written (22+47 decision audit). 28/29/30 VALIDATION.md flipped to validated/nyquist_compliant:true. 31-UAT retest green on 7 of 8 failed tests + UAT-31-11 dropdown fixed. Phase 33 handles deferred TD-04/05/06 + tsc hygiene.
    ```

    If STATE.md has a structured section for phases (look for "Phase" headings or a table), place the update in the appropriate position. Preserve all other STATE.md content.

    **Step 3 — Enforce D-12 hands-off rule (31-VALIDATION.md MUST NOT be in this commit).**

    Before staging, confirm 31-VALIDATION.md is not in the working tree diff:
    ```bash
    cd /Users/Code/EchoLearn
    git status --short | grep -c "31-VALIDATION.md"
    ```
    Expected: 0. If non-zero, stop and investigate before committing.

    **Step 4 — Commit all flips atomically:**

    Determine which files actually changed:
    ```bash
    cd /Users/Code/EchoLearn
    git status --short | grep -E "VALIDATION|STATE"
    ```

    Expected output:
    - `M .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md`
    - `M .planning/phases/29-final-polishment/29-VALIDATION.md`
    - `M .planning/phases/30-redesign-...-credits/30-VALIDATION.md` (OR NOT if D-11 gate failed in task 03-02)
    - `M .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md`
    - `M .planning/STATE.md`
    - **NO** entry for 31-VALIDATION.md (D-12 hands-off)

    Stage exactly these files (not `git add -A`):
    ```bash
    git add \
      .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md \
      .planning/phases/29-final-polishment/29-VALIDATION.md \
      .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md \
      .planning/STATE.md

    # Only add 30-VALIDATION.md if task 03-02 flipped it (D-11 gate passed, outcome=PASS-AND-FLIPPED)
    if [ "$(grep -c '^status: validated$' /Users/Code/EchoLearn/.planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md)" = "1" ]; then
      git add .planning/phases/30-redesign-curiosity-feed-as-scroll-progress-bar-with-daily-reading-quota-credits/30-VALIDATION.md
    fi
    ```

    Commit using a HEREDOC so formatting is preserved:
    ```bash
    git commit -m "$(cat <<'EOF'
    docs(32-03): flip VALIDATION.md status on phases 28/29/30 + record Phase 32 close-out

    Per 32-CONTEXT D-10: 28-VALIDATION.md and 29-VALIDATION.md were stale
    drafts — VERIFICATION already passed (28: 30/30, 29: 10/10). Flips to
    status:validated + nyquist_compliant:true + wave_0_complete:true + adds
    validated:2026-04-18 line.

    Per D-11: 30-VALIDATION.md flipped ONLY if 30-VERIFICATION.md wrote up
    clean in 32-02 (status:passed, zero BLOCKED/PARTIAL rows). Commit will
    include 30-VALIDATION.md only when task 03-02 outcome=PASS-AND-FLIPPED.
    If outcome=PASS-AND-HELD, 30-VALIDATION.md stays draft and the reason
    is documented in 32-03-SUMMARY.md.

    Per D-12: 31-VALIDATION.md NOT TOUCHED — already validated. Verified by
    the pre-commit git status check (zero hits).

    Also flips 32-VALIDATION.md to reflect Phase 32's own completion and
    records Phase 32 COMPLETE in STATE.md.

    Closes 32-CONTEXT D-10, D-11, D-12.
    EOF
    )"
    ```

    **Step 5 — Final sanity check.** Run:
    ```bash
    # All flipped phases now validated (28/29/32 unconditional; 30 conditional; 31 was already validated)
    grep -c "^status: validated$" .planning/phases/28-*/28-VALIDATION.md
    grep -c "^status: validated$" .planning/phases/29-*/29-VALIDATION.md
    grep -c "^status: validated$" .planning/phases/32-*/32-VALIDATION.md

    # 30 either validated (gate pass) or still draft (gate fail) — grep to confirm EXACTLY one of those
    grep -E "^status: (draft|validated)$" .planning/phases/30-*/30-VALIDATION.md

    # D-12 enforcement: 31-VALIDATION.md was NOT touched in this commit (expect 0 hits)
    git log -1 --stat | grep -c "31-VALIDATION.md"
    ```

    Document all four sanity-check results in 32-03-SUMMARY.md.
  </action>
  <verify>
    <automated>bash -c 'count28=$(grep -c "^status: validated$" /Users/Code/EchoLearn/.planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md); count29=$(grep -c "^status: validated$" /Users/Code/EchoLearn/.planning/phases/29-final-polishment/29-VALIDATION.md); count32=$(grep -c "^status: validated$" /Users/Code/EchoLearn/.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md); [ "$count28" = "1" ] && [ "$count29" = "1" ] && [ "$count32" = "1" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: validated$" .planning/phases/28-*/28-VALIDATION.md` returns 1
    - `grep -c "^status: validated$" .planning/phases/29-*/29-VALIDATION.md` returns 1
    - `grep -c "^status: validated$" .planning/phases/32-*/32-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/phases/32-*/32-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/phases/32-*/32-VALIDATION.md` returns 1
    - `grep -c "^validated: 2026-04-18$" .planning/phases/32-*/32-VALIDATION.md` returns 1
    - `grep -E "^status: (draft|validated)$" .planning/phases/30-*/30-VALIDATION.md` returns exactly 1 line (one of the two states, not both)
    - `grep -c "Phase 32" .planning/STATE.md` returns >= 1 (Phase 32 recorded)
    - `git log -1 --stat | grep -c "31-VALIDATION.md"` returns 0 (D-12 respected — 31 not touched)
    - `git log -1 --name-only | grep -c "VALIDATION.md"` returns 3 or 4 (28, 29, 32, optionally 30)
    - `git log -1 --name-only | grep -c "STATE.md"` returns 1
    - Single atomic commit contains all the flips
    - The automated `<verify>` command exits 0 (equality checks via bash on all three 28/29/32 counts)
  </acceptance_criteria>
  <done>32-VALIDATION.md flipped to validated/nyquist_compliant:true/wave_0_complete:true + validated:2026-04-18. STATE.md records Phase 32 COMPLETE. Atomic commit touches 28/29/32 VALIDATION (always) + 30 VALIDATION (if D-11 gate passed) + STATE.md. 31-VALIDATION.md untouched per D-12 (enforced by git log check).</done>
</task>

</tasks>

<verification>
Plan-level verification after all three tasks complete:

```bash
# 28, 29, 32 VALIDATION flipped unconditionally per D-10 + self-flip
[ "$(grep -c '^status: validated$' .planning/phases/28-ui-ux-polish-from-audit-findings/28-VALIDATION.md)" = "1" ]
[ "$(grep -c '^status: validated$' .planning/phases/29-final-polishment/29-VALIDATION.md)" = "1" ]
[ "$(grep -c '^status: validated$' .planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-VALIDATION.md)" = "1" ]

# nyquist_compliant: true in all four (28, 29, 31 already, 32 new)
[ "$(grep -c '^nyquist_compliant: true$' .planning/phases/28-*/28-VALIDATION.md)" = "1" ]
[ "$(grep -c '^nyquist_compliant: true$' .planning/phases/29-*/29-VALIDATION.md)" = "1" ]
[ "$(grep -c '^nyquist_compliant: true$' .planning/phases/32-*/32-VALIDATION.md)" = "1" ]

# validated date added to all three
[ "$(grep -c '^validated: 2026-04-18$' .planning/phases/28-*/28-VALIDATION.md)" = "1" ]
[ "$(grep -c '^validated: 2026-04-18$' .planning/phases/29-*/29-VALIDATION.md)" = "1" ]
[ "$(grep -c '^validated: 2026-04-18$' .planning/phases/32-*/32-VALIDATION.md)" = "1" ]

# D-11 gate: 30-VALIDATION either validated (gate pass) OR draft (gate fail) — exactly one
grep -c -E "^status: (draft|validated)$" .planning/phases/30-*/30-VALIDATION.md

# D-12: 31-VALIDATION NOT touched in last commit
[ "$(git log -1 --stat | grep -c '31-VALIDATION.md')" = "0" ]

# STATE.md updated
grep -q "Phase 32" .planning/STATE.md

# Preserved fields intact across all flipped files
[ "$(grep -c '^phase: 28$' .planning/phases/28-*/28-VALIDATION.md)" = "1" ]
[ "$(grep -c '^phase: 29$' .planning/phases/29-*/29-VALIDATION.md)" = "1" ]
[ "$(grep -c '^phase: 32$' .planning/phases/32-*/32-VALIDATION.md)" = "1" ]
[ "$(grep -c '^created: 2026-04-16$' .planning/phases/28-*/28-VALIDATION.md)" = "1" ]
[ "$(grep -c '^created: 2026-04-16$' .planning/phases/29-*/29-VALIDATION.md)" = "1" ]
[ "$(grep -c '^created: 2026-04-18$' .planning/phases/32-*/32-VALIDATION.md)" = "1" ]
```
</verification>

<success_criteria>
- D-10: 28 + 29 VALIDATION.md flipped unconditionally (both have status: validated, nyquist_compliant: true, wave_0_complete: true, validated: 2026-04-18)
- D-11: 30 VALIDATION.md flipped IFF 30-VERIFICATION.md wrote up clean (status: passed + 0 BLOCKED/PARTIAL). Else draft held + documented reason with outcome: PASS-AND-HELD.
- D-12: 31 VALIDATION.md untouched (verified by git log --stat returning 0 hits)
- 32 VALIDATION.md self-flipped to reflect Phase 32 close-out
- STATE.md records Phase 32 COMPLETE
- Task 03-02 reports explicit outcome field (PASS-AND-FLIPPED or PASS-AND-HELD)
- Single atomic commit captures all 4 or 5 file changes
- `/gsd:audit-milestone v1.4` re-run now shows phases 28/29/30 (if gate passed) as nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/32-v1-4-verification-close-out-and-uat-retest/32-03-SUMMARY.md` citing:
- D-10: 28 + 29 VALIDATION.md flipped (status:validated + nyquist:true + wave_0:true + validated:2026-04-18)
- D-11: 30 VALIDATION.md flip outcome (PASS-AND-FLIPPED → flipped; or PASS-AND-HELD → held with reason)
- D-12: 31 VALIDATION.md confirmed untouched via git log -1 --stat (zero hits)
- 32-VALIDATION.md self-flip recorded
- STATE.md update mechanism (session-record tool used, or direct edit)
- Link to atomic commit SHA
- Milestone v1.4 status: ready for `/gsd:audit-milestone v1.4` re-run (expect gaps_found → passed)
</output>
</output>
