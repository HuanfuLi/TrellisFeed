---
phase: 49-graph-correction-ui
plan: 05
type: execute
wave: 3
depends_on:
  - 49-01
  - 49-02
  - 49-03
  - 49-04
files_modified:
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/src/locales/i18n.d.ts
  - app/tests/screens/GraphScreen.reload-survival.test.mjs
  - .planning/phases/49-graph-correction-ui/49-VALIDATION.md
autonomous: false
requirements:
  - GRAPHUI-03

must_haves:
  truths:
    - "en.json has a complete graph.correction.* subtree with ALL ~41 keys per 49-CONTEXT.md D-17"
    - "zh.json, es.json, ja.json each have a graph.correction.* subtree with the IDENTICAL key set as en.json"
    - "bundle-parity.test.mjs passes after the locale bundles are updated"
    - "i18n.d.ts module-augmentation includes the new graph.correction.* keys for type-safe t() calls"
    - "GraphScreen.reload-survival.test.mjs proves a graphCommandService.rename commit + simulated reload + remount reflects the new title in state (GRAPHUI-03)"
    - "End-to-end manual UAT checklist exists for operator sign-off (haptics, magnetic snap feel, Header positioning, Capacitor build)"
    - "After full suite passes, 49-VALIDATION.md frontmatter `nyquist_compliant` is flipped to `true` (W-5)"
  artifacts:
    - path: "app/src/locales/en.json"
      provides: "Canonical EN bundle for all Phase 49 keys"
      contains: "graph.correction"
    - path: "app/src/locales/zh.json"
      provides: "Simplified Chinese translations"
      contains: "graph.correction"
    - path: "app/src/locales/es.json"
      provides: "Spanish translations"
      contains: "graph.correction"
    - path: "app/src/locales/ja.json"
      provides: "Japanese translations"
      contains: "graph.correction"
    - path: "app/src/locales/i18n.d.ts"
      provides: "Module augmentation for type-safe t() keys"
    - path: "app/tests/screens/GraphScreen.reload-survival.test.mjs"
      provides: "GRAPHUI-03 regression — Phase 48 patchQuestion durability proven through the UI surface"
    - path: ".planning/phases/49-graph-correction-ui/49-VALIDATION.md"
      provides: "Frontmatter `nyquist_compliant: true` after full suite is green (W-5)"
  key_links:
    - from: "app/src/locales/{zh,es,ja}.json"
      to: "app/src/locales/en.json"
      via: "Sonnet subagent run per app/scripts/translate-locales.md"
      pattern: "graph.correction"
    - from: "app/tests/locales/bundle-parity.test.mjs"
      to: "all 4 locale bundles"
      via: "Existing test asserts identical key sets"
      pattern: "bundle-parity"
---

<objective>
Finalize the i18n surface for Phase 49 by:
1. Reconciling all `graph.correction.*` keys across en.json (canonical) from Plans 49-01 through 49-04.
2. Running the Sonnet subagent per `app/scripts/translate-locales.md` to generate zh.json, es.json, ja.json translations for the new keys.
3. Updating `app/src/locales/i18n.d.ts` module augmentation so `t('graph.correction.X')` is type-safe.
4. Writing the GRAPHUI-03 reload-survival regression test (proves Phase 48's patchQuestion durability is exposed correctly through Plan 49's UI).
5. Producing the end-of-phase operator UAT checklist for sign-off on manual-only behaviors (haptics, magnetic snap, Header positioning, Capacitor mobile build).
6. **W-5: After the full suite is green, FLIP `nyquist_compliant: true` in `.planning/phases/49-graph-correction-ui/49-VALIDATION.md` frontmatter.**

Purpose: GRAPHUI-03 explicitly states "corrected graph survives navigation away/back/app reload." Phase 48's command service already guarantees durability (writes through `questionService.patchQuestion` → localStorage); this plan adds the through-the-UI regression test plus the i18n + UAT cleanup needed to ship the phase.

Output: 4 fully-translated locale bundles, type-safe i18n keys, a passing reload-survival test, a human-readable UAT checklist, and the VALIDATION.md frontmatter flipped to compliant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-graph-correction-ui/49-CONTEXT.md
@.planning/phases/49-graph-correction-ui/49-RESEARCH.md
@.planning/phases/49-graph-correction-ui/49-PATTERNS.md
@.planning/phases/49-graph-correction-ui/49-VALIDATION.md

@app/src/locales/en.json
@app/src/locales/zh.json
@app/src/locales/es.json
@app/src/locales/ja.json
@app/src/locales/i18n.d.ts
@app/scripts/translate-locales.md
@app/tests/locales/bundle-parity.test.mjs

@.planning/phases/49-graph-correction-ui/49-01-PLAN.md
@.planning/phases/49-graph-correction-ui/49-02-PLAN.md
@.planning/phases/49-graph-correction-ui/49-03-PLAN.md
@.planning/phases/49-graph-correction-ui/49-04-PLAN.md

<interfaces>
<!-- Reconciliation map: which plan added which keys to en.json as stubs. Plan 49-05 ensures the final EN values are correct + complete + parity-checked across all 4 bundles. -->

From 49-CONTEXT.md D-17 (the canonical key inventory — ~41 keys including branchNotEditable):
```
graph.correction.actions.{rename, move, merge, detach, prune, delete, undo, close}
graph.correction.pickMode.{move, merge, cancel, invalidTarget}
graph.correction.rename.{placeholder, save, tooLong, empty}
graph.correction.merge.{title, willBeRemoved, willKeep, body, footer, cancel, confirm}
graph.correction.delete.{title, bodyWithChildren, bodyEmpty, cancel, confirm}
graph.correction.toast.{renamed, moved, merged, detachedNewAnchor, detachedSameAnchor, pruned, deleted, undone, nothingToUndo, dropInvalid, reorgInProgress, rootNotEditable, branchNotEditable}
graph.correction.reorgPaused
```

Total: 41 keys.

From `app/src/services/question.service.ts:573` (CONFIRMED):
```typescript
// Returns Question[] DIRECTLY — NOT ServiceResult<Question[]>. Never `.data ?? []`.
getAll(opts?: { includeFlagged?: boolean }): Question[];
```

B-2 RULE: All `questionService.getAll(...)` sites in this plan's reload-survival test use `questionService.getAll({ includeFlagged: true })` (or default opts where flagged exclusion is intentional). NEVER `.data ?? []`.

CLAUDE.md i18n rule (load-bearing):
- en.json is canonical; zh/es/ja are translations generated via Sonnet subagent (app/scripts/translate-locales.md).
- Every PR adds canonical EN value + runs Sonnet subagent + bundle-parity.test.mjs blocks merges where key sets diverge.
- Proper nouns (Trellis, OpenAI, Claude, etc.) do NOT translate.
- Interpolation placeholders ({{title}}, {{n}}, etc.) stay verbatim across all 4 locales.
</interfaces>

<load_bearing>
- 49-CONTEXT.md D-17 — Phase 49 adds NEW namespace `graph.correction.*` (NOT overloading `graph.toast.*` / `graph.selected.*`).
- 49-VALIDATION.md §"Sampling continuity" — i18n bundle parity must be green AFTER Sonnet translation step (subagent runs as a Wave-3 task before bundle-parity check).
- 49-PATTERNS.md §"locales/en.json" — add `graph.correction.*` as a peer subtree alongside existing `graph.toast.*`. Interpolation uses `{{name}}` syntax (double-brace) matching existing keys.
- 49-RESEARCH.md R18 — GRAPHUI-03 reload survival: Phase 48's `patchQuestion` writes to localStorage; Phase 49 inherits this for FREE. The regression test verifies the through-UI path.
- CLAUDE.md §"i18n Workflow (Phase 27+)" — EN-first + Sonnet subagent + bundle-parity test.
- CLAUDE.md §"What NOT to translate" — proper nouns; system prompts; cross-locale branded labels; provider/model identifiers; static content blurbs (HomeScreen MILESTONE_POOL — not relevant here).
- This plan is `autonomous: false` because the Sonnet subagent invocation is a human-mediated step (operator runs `claude --prompt-file app/scripts/translate-locales.md` three times — once per locale). Auto-execution skips this and relies on the operator to run.
- W-4 — Plan 49-05's bash exploration in en.json reconciliation is trimmed. Single simple verify command per the `<verify>` block; no shell loops.
- W-5 — After full suite is green, this plan EXPLICITLY flips `nyquist_compliant: true` in `49-VALIDATION.md` frontmatter via Task 4.
</load_bearing>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile en.json graph.correction.* canonical bundle (W-4 simplified verify)</name>
  <files>app/src/locales/en.json, app/src/locales/i18n.d.ts</files>
  <action>
    Audit `app/src/locales/en.json` for the `graph.correction.*` namespace. Each prior plan (49-01 through 49-04) added stub keys when implementing their feature. This task verifies completeness against the D-17 canonical list and finalizes the EN values.

    W-4: Skip the shell-loop exploration; use the single Node script in `<verify>` to enumerate + assert. The canonical-key list lives in the verify command — no separate shell loop.

    Finalize the EN values. The canonical reference values (English) — operator-typed for clarity + concision:
    ```json
    {
      "graph": {
        "correction": {
          "actions": {
            "rename": "Rename",
            "move": "Move",
            "merge": "Merge with…",
            "detach": "Re-classify",
            "prune": "Prune",
            "delete": "Delete",
            "undo": "Undo",
            "close": "Close"
          },
          "pickMode": {
            "move": "Tap a cluster to move \"{{title}}\" into it",
            "merge": "Tap an anchor to merge \"{{title}}\" into",
            "cancel": "Cancel",
            "invalidTarget": "Not a valid target"
          },
          "rename": {
            "placeholder": "Enter new title…",
            "save": "Save",
            "tooLong": "Title must be 100 characters or fewer",
            "empty": "Title cannot be empty"
          },
          "merge": {
            "title": "Merge anchors?",
            "willBeRemoved": "Will be removed",
            "willKeep": "Will keep",
            "body": "Merging will move {{n}} Q&As under \"{{survivorTitle}}\" and remove the \"{{loserTitle}}\" anchor.",
            "footer": "Survivor's title and cluster are preserved. This can be undone.",
            "cancel": "Cancel",
            "confirm": "Merge"
          },
          "delete": {
            "title": "Delete \"{{title}}\" permanently?",
            "bodyWithChildren": "{{count}} Q&As will be re-parented to the cluster \"{{parentCluster}}\". This can be undone within the last 10 graph edits.",
            "bodyEmpty": "This anchor has no Q&As. Deleting removes it from the map.",
            "cancel": "Cancel",
            "confirm": "Delete"
          },
          "toast": {
            "renamed": "Renamed to \"{{title}}\"",
            "moved": "\"{{title}}\" moved to \"{{target}}\"",
            "merged": "Merged \"{{loserTitle}}\" into \"{{survivorTitle}}\"",
            "detachedNewAnchor": "\"{{qaTitle}}\" re-anchored under \"{{newAnchorTitle}}\"",
            "detachedSameAnchor": "\"{{qaTitle}}\" stayed under \"{{anchorTitle}}\" (best match)",
            "pruned": "\"{{title}}\" pruned",
            "deleted": "\"{{title}}\" deleted",
            "undone": "Undone: {{summary}}",
            "nothingToUndo": "Nothing to undo",
            "dropInvalid": "Not a valid drop target",
            "reorgInProgress": "Reorganize in progress — try again in a moment",
            "rootNotEditable": "Root node — not editable",
            "branchNotEditable": "Branch rename coming soon"
          },
          "reorgPaused": "Reorganizing — manual corrections paused"
        }
      }
    }
    ```

    Merge this canonical subtree into `app/src/locales/en.json`. Where prior plans added stubs, REPLACE them with these final values (the stubs may have used placeholder text). Preserve all existing keys outside the `graph.correction.*` namespace.

    Update `app/src/locales/i18n.d.ts` module augmentation. Read the existing pattern (from Phase 27); add the new graph.correction.* keys to the type augmentation so `t('graph.correction.X')` is type-safe.

    Verify: `cd app && npx tsc -b --noEmit` passes. `cd app && node --test tests/locales/missing-key.test.mjs` passes (existing fallback test). `cd app && node --test tests/locales/bundle-parity.test.mjs` is EXPECTED TO FAIL at this stage because zh/es/ja have not been translated yet — that's fine; Task 2 closes the parity gap.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node -e "const j = require('./src/locales/en.json'); const required = ['actions.rename','actions.move','actions.merge','actions.detach','actions.prune','actions.delete','actions.undo','actions.close','pickMode.move','pickMode.merge','pickMode.cancel','pickMode.invalidTarget','rename.placeholder','rename.save','rename.tooLong','rename.empty','merge.title','merge.willBeRemoved','merge.willKeep','merge.body','merge.footer','merge.cancel','merge.confirm','delete.title','delete.bodyWithChildren','delete.bodyEmpty','delete.cancel','delete.confirm','toast.renamed','toast.moved','toast.merged','toast.detachedNewAnchor','toast.detachedSameAnchor','toast.pruned','toast.deleted','toast.undone','toast.nothingToUndo','toast.dropInvalid','toast.reorgInProgress','toast.rootNotEditable','toast.branchNotEditable','reorgPaused']; for (const k of required) { const path = k.split('.'); let cur = j.graph?.correction; for (const p of path) { if (cur == null) { console.error('MISSING graph.correction.' + k); process.exit(1); } cur = cur[p]; } if (cur == null) { console.error('MISSING graph.correction.' + k); process.exit(1); } } console.log('All ' + required.length + ' keys present'); " &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <done>en.json contains all 41 keys with canonical English values; i18n.d.ts module augmentation extended; `tsc -b --noEmit` passes; no remaining stub placeholders from Plans 49-01..04.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs Sonnet subagent for zh/es/ja translations</name>
  <what-built>
    en.json now contains the complete canonical `graph.correction.*` subtree (41 keys). The Sonnet subagent at `app/scripts/translate-locales.md` is the project's standard tool for translating new keys into zh/es/ja per CLAUDE.md i18n workflow.
  </what-built>
  <how-to-verify>
    Operator runs the Sonnet subagent THREE times (once per non-EN locale) and merges the output into the respective bundle files:

    1. Run for zh.json:
       ```bash
       cd app && claude --prompt-file scripts/translate-locales.md --input '{"sourceLocale": "en", "targetLocale": "zh", "namespace": "graph.correction"}'
       ```
       Or follow the manual instructions inside `scripts/translate-locales.md` if it's not a CLI-invocable prompt file.

    2. Same for es.json (target locale: "es") and ja.json (target locale: "ja").

    3. Review each output for:
       - Proper nouns NOT translated (Trellis, Q&A → Q&A or 问答 per existing convention in zh.json — match what the existing graph.* namespace does).
       - Interpolation placeholders `{{name}}` preserved verbatim across all locales.
       - Length sanity check (Spanish typically ~20% longer than English; if any translation balloons by >50%, it likely picked the wrong term).

    4. After merging into zh.json / es.json / ja.json, run:
       ```bash
       cd app && node --test tests/locales/bundle-parity.test.mjs
       ```
       Must be GREEN.

    5. Run the full locale suite:
       ```bash
       cd app && node --test tests/locales/ && npx tsc -b --noEmit
       ```
       Must be GREEN.

    If any locale has missing or extra keys vs. en.json, the parity test will surface the diff; the operator fixes by re-running the subagent on the missing keys.
  </how-to-verify>
  <resume-signal>Type "translations merged and bundle-parity green" once all 3 locales are populated and the parity test passes; OR "translations rejected" with specific revisions needed.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write GRAPHUI-03 reload-survival regression test (B-2 getAll signature)</name>
  <files>app/tests/screens/GraphScreen.reload-survival.test.mjs</files>
  <behavior>
    - Test 1 (rename survives reload): seed questionService with an anchor named "Test Anchor"; call `await graphCommandService.rename(anchorId, "Renamed Anchor")`; reload localStorage state (clear in-memory caches, re-init questionService from localStorage); assert that `questionService.getAll().find(q => q.id === anchorId).title === "Renamed Anchor"`. (B-2: getAll returns Question[] directly; no `.data` destructure. `includeFlagged` not needed here — the anchor is not flagged.)
    - Test 2 (move survives reload): seed an anchor under cluster A; call `graphCommandService.move(anchorId, clusterB.id)`; simulate reload; assert anchor's parentId === clusterB.id.
    - Test 3 (delete survives reload): seed an anchor; call `graphCommandService.delete(anchorId)`; simulate reload; assert anchor is gone from `questionService.getAll()`.
    - Test 4 (journal survives reload): commit a rename; simulate reload; assert `graphEditJournal.list().length === 1` AND the entry's command is correct.
    - Test 5 (multiple commits replay correctly): rename A → move B → delete C → reload → all three reflected; journal has 3 entries in order.
  </behavior>
  <action>
    Fill in the Wave-0 scaffold `app/tests/screens/GraphScreen.reload-survival.test.mjs` per 49-RESEARCH.md R18.

    The test does NOT mount the GraphScreen UI — it exercises the data layer + service layer that the UI depends on. Reload simulation = clear in-memory caches + re-read from localStorage.

    B-2 RULE: All `questionService.getAll(...)` calls in this file return `Question[]` directly. NEVER use `.data ?? []`. Use `{ includeFlagged: true }` only when the test surface explicitly includes flagged questions (most reload-survival tests don't need it — the rename/move/delete don't flag anything).

    Recommended pattern:
    ```ts
    import { test } from 'node:test';
    import assert from 'node:assert/strict';
    import { questionService } from '../../src/services/question.service';
    import { graphCommandService } from '../../src/services/graph-command.service';
    import { graphEditJournal } from '../../src/services/graph-edit-journal.service';

    function simulateReload() {
      // Force re-hydration from localStorage:
      // 1. Clear in-memory caches in services that maintain them (questionService likely has a _cache field)
      // 2. Re-invoke load() / init() on the service
      questionService.reload(); // or whatever the service exposes
      graphEditJournal.reload();
    }

    test('GRAPHUI-03 reload survival', async (t) => {
      await t.test('rename survives reload', async () => {
        // arrange: seed anchor
        const seed = await questionService.create({ /* ... */ title: 'Test Anchor', isAnchorNode: true });
        // act
        const result = await graphCommandService.rename(seed.id, 'Renamed Anchor');
        assert.ok(result.success);
        simulateReload();
        // assert — B-2: getAll returns Question[] directly
        const after = questionService.getAll().find(q => q.id === seed.id);
        assert.equal(after?.title, 'Renamed Anchor');
      });
      // ... etc for tests 2-5
    });
    ```

    If `questionService` does not expose a `reload()` method, the test uses a different reload-simulation strategy:
    - Save the relevant `localStorage` keys to variables, clear localStorage, restore them, force questionService to re-read. The test environment uses a localStorage shim (Phase 48 tests have a pattern for this); reference `app/tests/services/refill-mutex.test.mjs` or any Phase 48 service test.
    - Alternative: use `vi.resetModules()` equivalent via Node's `import.meta.resolve` + dynamic re-import. Cleanest is whatever Phase 48 used in `graph-command.service.test.mjs` (read that file for the pattern).

    The test uses a temp localStorage namespace (e.g., `trellis_test_${randomId}`) so it doesn't pollute the real app's data.
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/GraphScreen.reload-survival.test.mjs &amp;&amp; ! grep -E "questionService\\.getAll\\([^)]*\\)\\.data" app/tests/screens/GraphScreen.reload-survival.test.mjs</automated>
  </verify>
  <done>All 5 reload-survival tests pass; the test uses a temp namespace; cleanup runs after each test; proves GRAPHUI-03 through the UI's write path (graphCommandService); the test would FAIL if Phase 48's patchQuestion durability regressed; B-2 enforced: no `.data` destructure on questionService.getAll calls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator UAT — manual-only behaviors + flip nyquist_compliant (W-5)</name>
  <what-built>
    Phase 49 ships the gesture engine, correction card, confirm dialogs, undo button, soft-prune snackbar, detach toast variants, and pick-mode banner. The following behaviors are MANUAL-ONLY per 49-VALIDATION.md §"Manual-Only Verifications":
    - Magnetic snap "feels right" at default 0.5× scale on a real Android device.
    - Haptic feedback fires on long-press + drop-onto-valid-target.
    - Drag does not fight MindElixir's pan/zoom.
    - Pick-mode banner doesn't break Header positioning.
    - Operator review of prune toast type (`'info'` — W-6) — confirm or request `'success'`.
  </what-built>
  <how-to-verify>
    Build the Capacitor Android (or iOS) app and exercise this checklist on a real device:

    **Magnetic snap**
    1. Open Graph tab; ensure ≥5 anchors visible at default 0.5× zoom.
    2. Long-press an anchor; wait for haptic + ghost appears.
    3. Slowly drag toward a different anchor. Confirm:
       - Halo activates within ~32px of target center.
       - Ghost snaps to the target.
    4. If feel is off, note the band that should be re-tuned (24-48px is acceptable).

    **Haptic feedback**
    5. Long-press a node; confirm light tap haptic at the 480ms mark.
    6. Drag + drop on a valid cluster; confirm medium tap haptic at drop.

    **Drag vs. MindElixir pan/zoom**
    7. Pinch-zoom out to ~0.3×.
    8. Long-press a deep anchor; drag toward a different cluster.
    9. Confirm: gesture commits without the map snapping back to default scale.

    **Pick-mode banner + Header positioning**
    10. Long-press an anchor → Move row → banner appears below Header.
    11. Swipe to next tab (Planner) and back to Graph.
    12. Confirm: Header stays in place; banner is still visible OR resets cleanly (Plan 49-02 + 49-04 always-mounted reset effect nulls pickMode on /graph leave).

    **Pick-mode original-coord restore (W-2)**
    13. Long-press an anchor in the upper-left of the visible map → Move row → banner appears → tap Cancel in banner.
    14. Confirm: the CorrectionCard reappears at THE ORIGINAL anchor position (upper-left), NOT at screen-center.

    **Reorganize gate**
    15. Trigger a Reorganize (from the existing button); during the reorg, attempt long-press → confirm card shows the paused row.
    16. Attempt drag → confirm toast "Reorganize in progress — try again in a moment" and ghost does NOT mount.
    17. Confirm Undo button is grayed during reorg.
    18. After reorg completes (REORG_COMPLETED toast), confirm all controls re-enable.

    **Detach two-emit correlation (B-1)**
    19. Long-press a Q&A leaf → Detach row → wait for classifier.
    20. Confirm: toast surfaces re-anchored OR same-anchor variant.
    21. If a slow LLM (>5s) is used: the toast may not appear (silent timeout fallback per B-1); the map still updates correctly.

    **Undo summary toast (B-5)**
    22. Rename an anchor → tap Undo corner button → confirm toast reads "Undone: rename '<new>' → '<old>'" (the operator-facing `summary`), NOT a bare verb literal like "Undone: rename".

    **Prune toast type review (W-6)**
    23. Prune an anchor → confirm the snackbar appears with type `'info'` (current default).
    24. Operator decides: accept `'info'` OR file a follow-up to switch to `'success'`. Record the decision in the SUMMARY.

    **All translations**
    25. Switch app to zh / es / ja in Settings.
    26. Repeat the long-press → correction card flow; confirm action labels render in the selected locale + interpolated titles render correctly.
    27. Confirm proper nouns like "Trellis" are NOT translated.

    **W-5 — Final step: flip nyquist_compliant**
    28. After ALL 27 manual checks above pass AND the full suite (`cd app && node --test tests/ && npx tsc -b --noEmit`) is green:
        - Edit `.planning/phases/49-graph-correction-ui/49-VALIDATION.md` frontmatter and flip `nyquist_compliant: false` → `nyquist_compliant: true`.
        - Commit with message `docs(49): flip nyquist_compliant after UAT sign-off`.
  </how-to-verify>
  <resume-signal>Type "UAT passed + nyquist_compliant flipped" once all 28 steps are complete, OR "UAT failed: [specific issues]" listing the regressions found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sonnet subagent → locale bundle | Operator-mediated step; subagent output is reviewed before merge (CLAUDE.md i18n rule) |
| reload-survival test → questionService localStorage | Test uses a temp namespace to avoid polluting real app data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-22 | Tampering | Translated bundle replacing proper nouns | mitigate | Operator manually reviews per CLAUDE.md "What NOT to translate" list; proper nouns (Trellis, OpenAI, etc.) must stay verbatim |
| T-49-23 | Information Disclosure | Test localStorage namespace bleeding into real app | mitigate | reload-survival test uses `trellis_test_${randomId}` namespace + cleanup after each test |
| T-49-24 | Repudiation | Sonnet hallucinating an interpolation placeholder | mitigate | bundle-parity.test.mjs catches missing keys; manual review catches placeholder rename (e.g., `{{name}}` → `{{nombre}}` would fail i18next interpolation) |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — only existing i18next + react-i18next |
</threat_model>

<verification>
- `cd app && node --test tests/locales/bundle-parity.test.mjs` — green AFTER Task 2 completes
- `cd app && node --test tests/locales/missing-key.test.mjs` — green
- `cd app && node --test tests/screens/GraphScreen.reload-survival.test.mjs` — green
- `cd app && node --test tests/` — full suite green
- `cd app && npx tsc -b --noEmit` — green
- Operator UAT checklist completed with sign-off
- `.planning/phases/49-graph-correction-ui/49-VALIDATION.md` frontmatter has `nyquist_compliant: true` (W-5)
</verification>

<success_criteria>
- en.json has all 41 canonical keys in `graph.correction.*` subtree.
- zh.json, es.json, ja.json each mirror en.json's `graph.correction.*` key set exactly (bundle-parity test green).
- i18n.d.ts module augmentation makes all new keys type-safe.
- Proper nouns (Trellis, OpenAI, Claude, etc.) are NOT translated.
- Interpolation placeholders preserved verbatim across all 4 locales.
- GRAPHUI-03 reload-survival regression test passes (5 sub-tests for rename / move / delete / journal / multi-commit replay).
- Operator UAT manual checks all pass (haptics, magnetic snap, drag vs. pan/zoom, pick-mode banner Header positioning, original-coord restore per W-2, reorg gate, detach two-emit per B-1, undo summary per B-5, prune toast type per W-6, all locales).
- nyquist_compliant flag in 49-VALIDATION.md flipped to `true` after sign-off (W-5).
</success_criteria>

<output>
Create `.planning/phases/49-graph-correction-ui/49-05-SUMMARY.md` when done with:
- Final key count per locale bundle (en/zh/es/ja).
- Any keys the Sonnet subagent rejected or needed manual override (proper nouns, tricky interpolations).
- reload-survival test approach used (which reload simulation strategy from the options listed).
- UAT outcome summary: which manual checks passed, which surfaced regressions (and where they were fixed), magnetic snap radius final tuning if any.
- Operator decision on prune toast type (W-6): kept `'info'` OR switched to `'success'`.
- Confirmation that `nyquist_compliant: true` was flipped in 49-VALIDATION.md frontmatter (W-5).
- Phase 49 closure status: ROADMAP.md Phase 49 entry can be checked off; STATE.md updated with the closure.
</output>
