---
phase: 34-v1-4-close-out-verification-debt-and-cleanup
plan: 06
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - app/src/services/post-store.service.ts
  - app/src/components/InfoFlow.tsx
autonomous: true
requirements:
  - SEAM-2-tail
must_haves:
  truths:
    - "app/src/services/post-store.service.ts is DELETED (file does not exist)"
    - "ImmersiveInfoFlow function and ImmersiveInfoFlowProps interface are DELETED from app/src/components/InfoFlow.tsx (zero matches for ImmersiveInfoFlow in the file)"
    - "Other InfoFlow.tsx exports (InlineInfoFlow at line 1001, InfoFlowPreview at line 1193, ConnectionCard at line 687, MilestoneCard at line 777) are PRESERVED"
    - "tsc -b --noEmit exits 0 after both deletions (no orphan-type errors from removed imports/types)"
    - "VineProgress dead-prop fold (D-15) is documented as NO-OP — VineProgressProps already lacks explored/total/isComplete per 34-RESEARCH.md Q7"
    - "post-store.service.ts had zero consumers in src/ and tests/ (verified pre-deletion)"
    - "ImmersiveInfoFlow had zero consumers outside InfoFlow.tsx (verified pre-deletion)"
  artifacts:
    - path: "app/src/services/post-store.service.ts"
      provides: "DELETED — file removed"
      contains: ""
    - path: "app/src/components/InfoFlow.tsx"
      provides: "ImmersiveInfoFlow removed; InlineInfoFlow / InfoFlowPreview preserved"
      contains: "InlineInfoFlow"
  key_links:
    - from: "InfoFlow.tsx remaining exports (InlineInfoFlow, InfoFlowPreview)"
      to: "consumers in HomeScreen.tsx + other screens"
      via: "ESM import"
      pattern: "InlineInfoFlow\\|InfoFlowPreview"
---

<objective>
Close SEAM-2-tail by deleting two orphan exports verified to have zero consumers (per 34-RESEARCH.md Q7 grep evidence):

1. **`app/src/services/post-store.service.ts`** — entire file. Zero consumers across `app/src/` and `app/tests/`.
2. **`ImmersiveInfoFlow` function + `ImmersiveInfoFlowProps` interface** in `app/src/components/InfoFlow.tsx` (lines 808-990). Zero consumers; missing D-29 video-stop-on-swipe wiring (footgun if revived).

Document the **D-15 dead-prop fold** as a NO-OP: 34-RESEARCH.md Q7 confirmed `VineProgressProps` no longer contains `explored`, `total`, `isComplete` — those props were already removed in a prior phase (likely Phase 31 D-01/D-02 redesign). The Plan 34-06 SUMMARY records this as NO-OP per D-15 (`fold into Wave 3 Seam 2 sweep`); no edit to `VineProgress.tsx` or `HomeScreen.tsx` needed.

Purpose: Closes the SEAM-2-tail gap (audit findings show Phase 33 plan 33-01 only deleted ConceptProgressCard — these two orphans remained). Test baseline must hold at 449 pass / 27 fail or improve.

Output: One file deleted; one file edited (~180 lines removed from InfoFlow.tsx); tsc clean; test baseline preserved.

**Wave 3 sequencing:** Per 34-CONTEXT.md D-07, Wave 3 runs after Wave 1 (test fixes) so we have a clean test baseline to compare against post-deletion. Plans 34-01/02 must complete before this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md
@.planning/v1.4-INTEGRATION-CHECK-v2.md

<deletion_targets>
**Target 1: `app/src/services/post-store.service.ts`**
- Pre-deletion grep evidence (34-RESEARCH.md Q7):
  ```
  grep -rn "post-store\|postStoreService\|postStore" app/src/
  → only matches inside post-store.service.ts itself
  ```
- Action: delete entire file.

**Target 2: `ImmersiveInfoFlow` in `app/src/components/InfoFlow.tsx`**
- File total lines: 1286
- Targeted region:
  - `interface ImmersiveInfoFlowProps {` at line 808
  - `}` closing the interface at line 813
  - `export function ImmersiveInfoFlow(...)` at line 815
  - `}` closing the function at line 990
- Pre-deletion grep evidence (34-RESEARCH.md Q7):
  ```
  grep -rn "ImmersiveInfoFlow" app/src/
  → only matches inside InfoFlow.tsx (interface + export)
  ```
- After deletion the file will be ~180 lines shorter. Following exports remain intact:
  - `MilestoneCard` (lines 777-806) — internal helper
  - `InlineInfoFlow` (lines 1001-1186) — exported, has consumers
  - `InfoFlowPreview` (lines 1193-1286) — exported, has consumers
  - `ConnectionCard` (lines 687-765) — internal helper
  - `BlindboxItem` type, `conceptCardPropsEqual` helper, `pickConnectionColors` helper

**Target 3 (DOCUMENT-ONLY): D-15 VineProgress dead-prop fold**
- 34-RESEARCH.md Q7 confirms current `VineProgressProps` interface in `app/src/components/VineProgress.tsx`:
  ```ts
  interface VineProgressProps {
    mode: 'inline' | 'compact';
    concepts: Array<{ id: string; name: string; explored: boolean }>;
    onConceptTap?: (conceptId: string) => void;
    onHistoryTap?: () => void;
  }
  ```
  No `explored` / `total` / `isComplete` props on the interface (those were removed in a prior phase). The D-15 fold is a NO-OP — document in the SUMMARY, don't edit code.

**Pitfall avoidance (per 34-RESEARCH.md Pitfall 4):** When deleting `ImmersiveInfoFlow`, do NOT remove top-level imports (`useTranslation`, `useRef`, `useState`, `useEffect`) since `InlineInfoFlow` and other components in the file use them. Only remove the interface + function body.
</deletion_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete app/src/services/post-store.service.ts</name>
  <files>app/src/services/post-store.service.ts</files>
  <read_first>
    - app/src/services/post-store.service.ts (read entire file to confirm contents)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q7 grep evidence)
  </read_first>
  <action>
    1. Pre-deletion verification — run from repo root:
       ```
       grep -rn "post-store\|postStoreService\|postStore" app/src/ app/tests/
       ```
       Expected: only matches inside `post-store.service.ts` itself. If any non-self consumers found, **HALT** and report — do not delete.

    2. Delete the file:
       ```
       rm app/src/services/post-store.service.ts
       ```

    3. Run from `app/`:
       ```
       npx tsc -b --noEmit
       ```
       Expected: exit 0. If non-zero, investigate orphan type imports referencing the deleted module.

    4. Run from `app/`:
       ```
       node --test tests/**/*.test.mjs 2>&1 | grep -E "^# (pass|fail)"
       ```
       Expected: pass count >= prior baseline (449 after Plan 34-01); fail count <= prior baseline.
  </action>
  <verify>
    <automated>test ! -f app/src/services/post-store.service.ts && echo DELETED</automated>
    Expected: `DELETED`.

    <automated>grep -rn "post-store\|postStoreService\|postStore" app/src/ app/tests/ | wc -l</automated>
    Expected: `0`.

    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -3</automated>
    Expected: no errors; exit code 0.
  </verify>
  <acceptance_criteria>
    - `app/src/services/post-store.service.ts` does NOT exist after this task.
    - `grep -rn "post-store\|postStoreService\|postStore" app/src/ app/tests/` returns zero matches.
    - `cd app && npx tsc -b --noEmit` exits 0.
    - `cd app && npm test 2>&1 | grep -E "^# (pass|fail)"` shows pass >= 449 and fail <= 27 (test baseline preserved or improved post-Plan 34-01).
  </acceptance_criteria>
  <done>
    File deleted; zero consumers; tsc clean; test baseline preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete ImmersiveInfoFlow + ImmersiveInfoFlowProps from app/src/components/InfoFlow.tsx</name>
  <files>app/src/components/InfoFlow.tsx</files>
  <read_first>
    - app/src/components/InfoFlow.tsx (read lines 800-1000 to scope the deletion; confirm function ends at line 990)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q7 + Pitfall 4)
  </read_first>
  <action>
    1. Pre-deletion verification:
       ```
       grep -rn "ImmersiveInfoFlow" app/src/ app/tests/
       ```
       Expected: only matches inside `app/src/components/InfoFlow.tsx`. If any consumer exists, **HALT** and report.

    2. Identify exact deletion range. From `read_first`:
       - Line 808: `interface ImmersiveInfoFlowProps {`
       - Line 813: `}` (interface closing brace)
       - Lines ~813-814: blank/whitespace
       - Line 815: `export function ImmersiveInfoFlow({ items, onOpenConnection, onClose, onOpenPost }: ImmersiveInfoFlowProps) {`
       - Line 990: `}` (function closing brace)

       Use the `Edit` tool or `Read` + `Write` to remove lines 808-990 inclusive. The next non-deleted line is the start of `InlineInfoFlow` near line 999/1001 (`}` of preceding helper, then `export function InlineInfoFlow`). Keep all subsequent content intact.

    3. Verify NO top-level imports/types are removed. Per 34-RESEARCH.md Pitfall 4: `useTranslation`, `useRef`, `useState`, `useEffect`, `IntersectionObserver`, `DailyPost`, `InfoFlowItem` are used by other functions in the file.

    4. Run from `app/`:
       ```
       npx tsc -b --noEmit
       ```
       Expected: exit 0. If errors mention `ImmersiveInfoFlow` or `ImmersiveInfoFlowProps`, those are the only references (no consumers); the error must be inside `InfoFlow.tsx` itself — meaning a self-reference inside the deleted block leaked. Re-check deletion bounds.

    5. Run from `app/`:
       ```
       node --test tests/**/*.test.mjs 2>&1 | grep -E "^# (pass|fail)"
       ```
       Expected: pass count >= prior baseline; fail count <= prior baseline.

    6. Verify InfoFlow.tsx still exports the surviving functions:
       ```
       grep -E "^export function (InlineInfoFlow|InfoFlowPreview)" app/src/components/InfoFlow.tsx
       ```
       Expected: 2 matches.
  </action>
  <verify>
    <automated>grep -c "ImmersiveInfoFlow" app/src/components/InfoFlow.tsx</automated>
    Expected: `0`.

    <automated>grep -c "^export function InlineInfoFlow" app/src/components/InfoFlow.tsx</automated>
    Expected: `1` (preserved).

    <automated>grep -c "^export function InfoFlowPreview" app/src/components/InfoFlow.tsx</automated>
    Expected: `1` (preserved).

    <automated>grep -rn "ImmersiveInfoFlow" app/src/ app/tests/ | wc -l</automated>
    Expected: `0`.

    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -3</automated>
    Expected: no errors; exit code 0.

    <automated>cd app && node --test tests/**/*.test.mjs 2>&1 | grep -E "^# (pass|fail)"</automated>
    Expected: pass >= 449 (or better post-Plan 34-01); fail <= 27.
  </verify>
  <acceptance_criteria>
    - `grep -c "ImmersiveInfoFlow" app/src/components/InfoFlow.tsx` returns `0`.
    - `grep -c "ImmersiveInfoFlowProps" app/src/components/InfoFlow.tsx` returns `0`.
    - `grep -c "^export function InlineInfoFlow" app/src/components/InfoFlow.tsx` returns `1`.
    - `grep -c "^export function InfoFlowPreview" app/src/components/InfoFlow.tsx` returns `1`.
    - `grep -c "^export function MilestoneCard\|^function MilestoneCard" app/src/components/InfoFlow.tsx` returns `>= 1` (helper preserved).
    - `cd app && npx tsc -b --noEmit` exits 0.
    - `cd app && npm test 2>&1 | grep -E "^# (pass|fail)"` shows pass >= 449 and fail <= 27.
    - File is ~180 lines shorter than pre-deletion (was 1286 lines; expect ~1106 lines).
  </acceptance_criteria>
  <done>
    ImmersiveInfoFlow + ImmersiveInfoFlowProps deleted; InlineInfoFlow / InfoFlowPreview / MilestoneCard preserved; tsc clean; test baseline preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document D-15 VineProgress dead-prop fold as NO-OP (no code change)</name>
  <files>.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-06-SUMMARY.md</files>
  <read_first>
    - app/src/components/VineProgress.tsx (confirm VineProgressProps no longer has explored/total/isComplete)
    - app/src/screens/HomeScreen.tsx (confirm `<VineProgress>` call sites pass only mode/concepts/onConceptTap/onHistoryTap)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q7 — D-15 NO-OP finding)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md (D-15 wording)
  </read_first>
  <action>
    Append the D-15 NO-OP finding to the SUMMARY for Plan 34-06 (which will be created by the SUMMARY step at end of execution). Specifically, the SUMMARY must contain a section like:

    ```markdown
    ## D-15 Dead-Prop Fold — NO-OP

    34-CONTEXT.md D-15 directs that we "fold into Wave 3 (Seam 2 sweep) the VineProgress explored/total/isComplete dead-prop cleanup". 34-RESEARCH.md Q7 verified against current source that:

    - `app/src/components/VineProgress.tsx` `VineProgressProps` interface contains exactly four fields: `mode`, `concepts`, `onConceptTap?`, `onHistoryTap?`. NO `explored`, `total`, or `isComplete` fields exist.
    - `app/src/screens/HomeScreen.tsx` `<VineProgress>` call sites pass only the four valid props.

    Therefore D-15 is a **NO-OP** — those props were already removed in a prior phase (likely Phase 31 D-01/D-02 redesign). No edit to VineProgress.tsx or HomeScreen.tsx is needed in Plan 34-06.

    This NO-OP determination is also documented in `30-VERIFICATION.md` (Plan 34-03 row for D-XX) and `31-VERIFICATION.md` (Plan 34-04 row for D-XX referencing the redesign).

    Evidence:
    - `grep -c "explored:\\|total:\\|isComplete:" app/src/components/VineProgress.tsx` → 0 matches in `VineProgressProps` interface (the `explored: boolean` inside `concepts` array entry is a different field — that one IS expected).
    - `grep -E "<VineProgress" app/src/screens/HomeScreen.tsx` shows props passed: confirms no dead props.
    ```

    During Plan 34-06's SUMMARY creation, include this section verbatim. No code change is required for this task.

    To make this task verifiable as a "task action" in execute-plan: run the grep checks now and confirm the NO-OP holds.
  </action>
  <verify>
    <automated>grep -E "interface VineProgressProps" app/src/components/VineProgress.tsx -A 8 | grep -cE "^\s+(explored|total|isComplete):"</automated>
    Expected: `0` (no top-level dead props in the interface; `explored: boolean` inside the `concepts` array entry is on a nested type and won't match this regex which requires interface-direct fields).

    <automated>grep -c "<VineProgress" app/src/screens/HomeScreen.tsx</automated>
    Expected: `>= 1` (call site exists, just to confirm we're checking the right component).

    <automated>grep -E "<VineProgress" app/src/screens/HomeScreen.tsx -A 5 | grep -cE "(explored|total|isComplete)\s*=\s*\{"</automated>
    Expected: `0` (no call site passes any of the dead props).
  </verify>
  <acceptance_criteria>
    - `VineProgressProps` interface contains exactly the 4 expected fields (mode, concepts, onConceptTap, onHistoryTap) per direct source read.
    - HomeScreen.tsx `<VineProgress>` call sites do NOT pass `explored=`, `total=`, or `isComplete=` props.
    - The Plan 34-06 SUMMARY (created at end of execution) explicitly documents D-15 as NO-OP with the grep evidence shown.
    - No code changes to `VineProgress.tsx` or `HomeScreen.tsx` from this task.
  </acceptance_criteria>
  <done>
    D-15 NO-OP determination confirmed via grep; SUMMARY documents the finding; no code changes made.
  </done>
</task>

</tasks>

<verification>
- `app/src/services/post-store.service.ts` deleted.
- `ImmersiveInfoFlow` + `ImmersiveInfoFlowProps` removed from `InfoFlow.tsx` (~180 lines smaller).
- `InlineInfoFlow`, `InfoFlowPreview`, `MilestoneCard`, `ConnectionCard` PRESERVED.
- `cd app && npx tsc -b --noEmit` exits 0.
- `cd app && npm test` shows pass >= 449 (post-Plan 34-01 baseline) and fail <= 27.
- D-15 documented as NO-OP in SUMMARY (no code change to VineProgress/HomeScreen).
- Both deletion files commit as part of Wave 5 Commit 5 (Phase 34 execution artifacts).
</verification>

<success_criteria>
- 2 orphan exports deleted (post-store.service.ts file + ImmersiveInfoFlow function/interface).
- D-15 dead-prop cleanup confirmed as already-done (NO-OP).
- Test baseline preserved at 449/27 or better.
- tsc clean.
- File-level surgical edits — no unrelated changes.
</success_criteria>

<output>
After completion, create `.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-06-SUMMARY.md` documenting:
- Files deleted: post-store.service.ts (full file) + InfoFlow.tsx lines 808-990 (ImmersiveInfoFlowProps + ImmersiveInfoFlow).
- Pre/post `wc -l app/src/components/InfoFlow.tsx` line count.
- Test baseline before/after (pass/fail counts).
- D-15 NO-OP determination with grep evidence.
- Citations to 34-RESEARCH.md Q7 and Pitfall 4.
</output>
