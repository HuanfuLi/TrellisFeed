---
phase: 55.1-device-test-bug-fixes
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/screens/HomeScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
autonomous: false
gap_closure: true
requirements: [BUGFIX-07]
user_setup: []

must_haves:
  truths:
    - "The 'Nothing new today' empty-state card no longer contradicts a populated feed below it."
    - "If reworded, the new copy exists in all 4 locale bundles (en/zh/es/ja) with identical key sets (bundle-parity passes)."
    - "A truly empty feed still shows an appropriate empty/caught-up message."
  artifacts:
    - path: "app/src/screens/HomeScreen.tsx"
      provides: "Corrected gating/copy at the empty-state card (line ~959)"
    - path: "app/src/locales/en.json"
      provides: "Canonical copy (reworded keys if the rewrite option is chosen)"
  key_links:
    - from: "HomeScreen empty-state card gate"
      to: "dailyPosts.length / conceptQuota"
      via: "corrected conditional"
      pattern: "conceptQuota === 0"
---

<objective>
GAP-D (BUGFIX-07, device-test follow-on folded into 55.1): the "Nothing new today / Check back later"
empty-state card renders ABOVE a populated curiosity feed (HomeScreen.tsx:959, gated on
`conceptQuota === 0 && dailyPosts.length > 0 && questions.length > 0`). The copy contradicts the
visible MasonryFeed.

Purpose: fix is a copy/logic decision — either hide the card when the feed is non-empty, OR reword to
distinguish "no NEW concepts today" from "empty feed". If reworded, the new copy MUST be localized in all
4 bundles (no runtime LLM translation; bundle-parity test enforced).
Output: corrected HomeScreen gating/copy; 4-bundle parity if reworded; on-device screenshot UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/2026-05-21-empty-state-coexists-with-populated-feed.md

CLAUDE.md invariants this plan MUST preserve:
- "i18n Workflow" — runtime LLM translation is PROHIBITED. Any reworded string lands canonical EN in
  en.json AND human/Sonnet-subagent-generated translations in zh/es/ja IN THE SAME change.
  `bundle-parity.test.mjs` blocks divergent key sets; `missing-key.test.mjs` asserts EN fallback.
  Do NOT translate proper nouns (Trellis, etc.).
- The empty-state card is the D-17 design element; the conditional currently lives at HomeScreen.tsx:959.

<interfaces>
From app/src/screens/HomeScreen.tsx:958-980 — current gate and copy:
```
{conceptQuota === 0 && dailyPosts.length > 0 && questions.length > 0 && (
  ... t('home.feed.emptyTitle') / t('home.feed.emptyBody') ...
)}
```
The MasonryFeed renders below at ~line 1055. `conceptQuota === 0` means "no NEW concepts due today";
`dailyPosts.length > 0` means the feed HAS content (review/existing posts).
From app/src/locales/en.json (home.feed namespace) — existing keys:
```
"emptyTitle": "Nothing new today",
"emptyBody": "Check back later for fresh concepts to explore.",
"allExplored": "All caught up! Great work today.",
```
All 4 bundles (en/zh/es/ja) share the home.feed namespace; key sets MUST stay identical.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Decision — hide vs reword the empty-state card (GAP-D)</name>
  <decision>How to resolve the contradictory empty-state card: HIDE it when the feed is non-empty, or REWORD it to distinguish "no new concepts" from "empty feed"?</decision>
  <context>
    At HomeScreen.tsx:959 the "Nothing new today" card shows even when `dailyPosts.length > 0`, so it sits
    above a populated MasonryFeed and reads as a false "empty feed" message. The original intent was
    "no new concepts today, but here's your existing feed." Picking the messaging direction is an operator
    call (per the GAP-D todo). The rewrite option requires 4-bundle localization; the hide option is a
    pure logic change with no copy work.
  </context>
  <options>
    <option id="hide">
      <name>Hide the card when the feed has content</name>
      <pros>Pure logic change; no new copy; no localization needed; removes the contradiction entirely.</pros>
      <cons>Loses the "you're caught up on new concepts" signal — the user gets no explicit acknowledgment that there are no NEW concepts today.</cons>
    </option>
    <option id="reword">
      <name>Reword to "caught up on new concepts — here's more to revisit"</name>
      <pros>Keeps the positive "all caught up on new concepts" signal AND makes sense above review content.</pros>
      <cons>Requires new EN copy + zh/es/ja translations in the same change (bundle-parity enforced); slightly more work.</cons>
    </option>
  </options>
  <resume-signal>Select: hide or reword (and, if reword, confirm/adjust the EN wording).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply the chosen fix at HomeScreen.tsx:959 (+ 4-bundle copy if rewording)</name>
  <read_first>
    - app/src/screens/HomeScreen.tsx (lines 945-981 — the gate + copy block)
    - app/src/locales/en.json (home.feed namespace — canonical)
    - app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json (home.feed namespace)
    - app/scripts/translate-locales.md (Sonnet-subagent translation prompt, if rewording)
    - CLAUDE.md "i18n Workflow" (EN-first, all 4 bundles same change, proper nouns not translated)
  </read_first>
  <action>
    IF the decision was HIDE: change the conditional at HomeScreen.tsx:959 so the card does NOT render when
    `dailyPosts.length > 0` — i.e. only show the empty/caught-up card on a genuinely empty feed
    (`dailyPosts.length === 0`) under the existing other conditions. Ensure a truly empty feed still shows
    a sensible message (reuse `home.feed.emptyTitle/emptyBody` or `home.feed.allExplored` as appropriate).
    No locale changes needed in this branch (no new keys).

    IF the decision was REWORD: keep the card visible above the populated feed but change its copy to the
    operator-approved wording. Add/replace the canonical EN values in en.json's `home.feed` namespace
    (e.g. new keys like `caughtUpTitle` / `caughtUpBody`, or repurpose emptyTitle/emptyBody — choose one
    and keep key sets consistent). Generate zh/es/ja translations (run the Sonnet subagent per
    `app/scripts/translate-locales.md` once per locale) and add them under the SAME key in all 3 bundles.
    Do NOT translate "Trellis" or other proper nouns. Keep interpolation placeholders verbatim. Point
    `t('...')` in HomeScreen at the chosen keys. Either way, keep all 4 bundles' key sets identical.
  </action>
  <verify>
    <automated>cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs && tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - HIDE branch: `grep -n "conceptQuota === 0" app/src/screens/HomeScreen.tsx` shows the card no longer
      renders when `dailyPosts.length > 0` (read the JSX to confirm the card is gated on an empty feed);
      no locale key churn.
    - REWORD branch: the new/changed key exists in ALL of en/zh/es/ja under home.feed; en value is the
      approved EN copy; zh/es/ja are non-empty translations (proper nouns untouched); HomeScreen `t('...')`
      references the new key.
    - `node --test tests/locales/bundle-parity.test.mjs` passes (identical key sets across 4 bundles).
    - `node --test tests/locales/missing-key.test.mjs` passes (EN fallback intact).
    - `tsc -b --noEmit` clean (no broken t() keys).
    - DEVICE/SCREENSHOT UAT below is the visual gate.
  </acceptance_criteria>
  <done>Empty-state contradiction resolved per the operator decision; bundle parity + tsc pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Screenshot UAT — empty-state no longer contradicts feed (GAP-D)</name>
  <what-built>
    GAP-D: the "Nothing new today" card no longer contradicts a populated feed — either hidden when the
    feed has content, or reworded (localized in all 4 bundles) to read sensibly above review content.
  </what-built>
  <how-to-verify>
    On a device/emulator (or in the browser build), reproduce the GAP-D screenshot state: a day with
    `conceptQuota === 0` (no new concepts due) but a populated curiosity feed.
    1. Open the Home screen. EXPECT (hide): no "Nothing new today" card above the populated feed.
       EXPECT (reword): the card reads as "caught up on new concepts, here's more to revisit" and makes
       sense above the feed — no contradiction.
    2. Switch locale to zh, es, ja (if reword): EXPECT the copy is translated, no missing-key fallback
       artifacts, layout not broken by length.
    3. Force a genuinely empty feed (no posts): EXPECT an appropriate empty/caught-up message still shows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the remaining copy/layout issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static UI copy → render | No untrusted input; all strings are bundled, hand-authored/dev-translated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55.1D-01 | Tampering | i18n bundle parity | mitigate | bundle-parity + missing-key tests block divergent/empty keys |
| T-55.1D-02 | Tampering | runtime-LLM-translation prohibition | mitigate | Translations are dev-time bundles; no chatCompletion/chatStream for UI copy |
| T-55.1D-SC | Tampering | npm installs | accept | No package installs; source + JSON only |
</threat_model>

<verification>
- HomeScreen empty-state no longer contradicts a populated feed (per chosen option).
- `bundle-parity.test.mjs` + `missing-key.test.mjs` pass; `tsc -b --noEmit` clean.
- On-device/screenshot UAT confirms the corrected state across locales.
</verification>

<success_criteria>
GAP-D closed: empty-state card fixed (hide or localized reword), 4-bundle parity preserved, UAT approved.
</success_criteria>

<output>
Create `.planning/phases/55.1-device-test-bug-fixes/55.1-08-SUMMARY.md` when done.
</output>
