---
phase: 55.1
slug: device-test-bug-fixes
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-21
---

# Phase 55.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `node --test` + esbuild tsx loader |
| **Config file** | none — per-file `.test.mjs` (pattern at `app/tests/canonical-knowledge.test.mjs`) |
| **Quick run command** | `cd app && node --test tests/<file>.test.mjs` |
| **Full suite command** | `cd app && npm test` |
| **Estimated runtime** | ~quick: <5s per file · full suite: minutes |

---

## Sampling Rate

- **After every task commit:** Run `cd app && node --test tests/<the-touched-file>.test.mjs`
- **After every plan wave:** Run `cd app && npm test`
- **Before `/gsd:verify-work`:** Full suite green + `tsc -b --noEmit` clean + on-device UAT for BUGFIX-03/04 and the rapid-switch UAT for BUGFIX-01
- **Max feedback latency:** ~5 seconds (single-file quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 55.1-01-xx | 01 | 1 | BUGFIX-01 | Info-disclosure (wrong-session leak) | Answer persists to originating session only; switch aborts in-flight stream | unit + source-guard | `node --test tests/screens/AskScreen.session-binding.test.mjs` | ❌ W0 | ⬜ pending |
| 55.1-02-xx | 02 | 1 | BUGFIX-02 | Tampering (provider-truncated content persisted) | Empty/fragment regen rejected; non-empty `textArtContent` never overwritten with shorter | unit (pure helper) | `node --test tests/services/text-art-no-truncate-on-regen.test.mjs` | ❌ W0 | ⬜ pending |
| 55.1-02-xx | 02 | 1 | BUGFIX-02 | Tampering | Gemini text-art path raises token budget / sets thinkingConfig | source-guard | `node --test tests/providers/gemini-text-art-budget.test.mjs` | ❌ W0 | ⬜ pending |
| 55.1-02-xx | 02 | 1 | BUGFIX-02 | — | Tightener rejects empty/fragment input | unit | `node --test tests/services/text-art-tightener.test.mjs` (extend) | ✅ extend | ⬜ pending |
| 55.1-03-xx | 03 | 1 | BUGFIX-03 | — | `useKeyboard` has hysteresis/debounce so transient resize toggles don't reverse the nav spring | source-guard + pure fn | `node --test tests/components/BottomNavigation.slide.test.mjs` (extend) | ✅ extend | ⬜ pending |
| 55.1-04-xx | 04 | 1 | BUGFIX-04 | — | Send fires on pointerdown with preventDefault; Enter-key submit still works | source-guard | `node --test tests/components/ChatInput.send-on-pointerdown.test.mjs` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/screens/AskScreen.session-binding.test.mjs` — BUGFIX-01 (extract a pure `resolvePersistTarget`/origin-id-match helper; source-guard for abort-on-switch)
- [ ] `tests/services/text-art-no-truncate-on-regen.test.mjs` — BUGFIX-02 persist-merge guard (never overwrite good content with shorter)
- [ ] `tests/providers/gemini-text-art-budget.test.mjs` — BUGFIX-02 Gemini budget/thinkingConfig source-guard
- [ ] `tests/components/ChatInput.send-on-pointerdown.test.mjs` — BUGFIX-04
- [ ] Extend `tests/services/text-art-tightener.test.mjs` — reject empty/fragment input
- [ ] Extend `tests/components/BottomNavigation.slide.test.mjs` — assert hysteresis/debounce in `useKeyboard`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Nav bar does not flicker up/down on keyboard open | BUGFIX-03 | Device-only: WebView mid-animation resize cadence is not reproducible in Node | On Android device/emulator, open Ask screen, tap input, observe bottom nav slides down once smoothly (no oscillation) |
| Send sends on first tap with keyboard open | BUGFIX-04 | Device-only: Android WebView blur-eats-tap behavior | On device, focus input + type, tap Send once with keyboard open — message sends without a second tap |
| Rapid ask → new-session → ask → new-session never cross-renders | BUGFIX-01 | Timing-dependent repro; device confirms the abort path | On device, send a long-generating question, immediately new-chat + send again ×3; confirm no prior answer appears under a later session |
| Text-art posts keep full text after provider→Gemini + locale switch | BUGFIX-02 | Needs a live Gemini key + real generation | With Gemini configured, switch provider then locale; confirm existing text-art posts retain full sentences |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
