---
phase: 55.1-device-test-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ChatInput.tsx
  - app/src/screens/AskScreen.tsx
  - app/src/state/useKeyboard.ts
  - app/src/state/chatinput-offset.ts
  - app/tests/state/chatinput-offset.test.mjs
autonomous: false
gap_closure: true
requirements: [BUGFIX-05]
user_setup: []

must_haves:
  truths:
    - "Opening the keyboard on the Ask screen animates the input bar smoothly to its keyboard-open position (no instant teleport)."
    - "The nav-bar instant-hide fix (commit 6fb7b325) is preserved — the nav still hides on focusin, no oscillation."
    - "ChatInput input keeps minWidth:0; root-overflow both-axes clip and SwipeTabContainer resize/keyboard invariants are unchanged."
  artifacts:
    - path: "app/src/state/chatinput-offset.ts"
      provides: "Pure helper resolving the ChatInput translateY offset from visualViewport geometry"
      contains: "export function resolveChatInputOffset"
    - path: "app/tests/state/chatinput-offset.test.mjs"
      provides: "Executing unit tests for resolveChatInputOffset"
    - path: "app/src/components/ChatInput.tsx"
      provides: "ChatInput animated reposition wired to the offset, minWidth:0 preserved"
  key_links:
    - from: "ChatInput.tsx reposition style"
      to: "resolveChatInputOffset"
      via: "visualViewport-driven offset with CSS transition"
      pattern: "resolveChatInputOffset"
---

<objective>
GAP-A (BUGFIX-03 follow-on): the Ask-screen text input bar "teleports" to its keyboard-open
position with no animation. Make the reposition smooth WITHOUT reintroducing the nav-bar flicker
that was already fixed (focus-driven instant hide, commit 6fb7b325) and WITHOUT regressing the
load-bearing keyboard/overflow invariants in CLAUDE.md.

Purpose: device UAT round 2 accepted the nav fix but flagged the input teleport as a remaining
polish defect. The reflow is native (Android adjustResize shrinks the WebView and the 100dvh
flex-column reflows instantly — AskScreen.tsx:600 column, ChatInput at line 922).
Output: a smooth, animated input reposition; one new pure helper + executing test; mandatory
on-device UAT acceptance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55.1-device-test-bug-fixes/55.1-VERIFICATION.md
@.planning/todos/pending/2026-05-21-empty-state-coexists-with-populated-feed.md

CLAUDE.md load-bearing invariants this plan MUST preserve (read them):
- "ChatInput flex shrink" — `<input>` keeps `minWidth:0` alongside `flex:1`; Send button ≤ 44px.
- "Root overflow clip — both axes" — `html, body { overflow: hidden }`, App-root `overflowX:'hidden'`,
  `scrollLeft = 0` reset in SwipeTabContainer.onFocusOut. Never rely on body scroll; no `window.scrollTo`.
- "SwipeTabContainer resize + keyboard" — `resync()` early-returns on unchanged width; do NOT install
  `@capacitor/keyboard` with `resize:'none'`.
- "Header positioning" — do NOT add `transform`/`will-change`/`filter`/`contain`/`perspective` to any
  Header ancestor. The reposition animation must NOT introduce these on an ancestor of a Header.

<interfaces>
From app/src/state/keyboard-hysteresis.ts (already implemented — Phase 55.1 BUGFIX-03):
```
export const MIN_KEYBOARD_HEIGHT = 150;   // open threshold (heightDelta)
export const CLOSE_KEYBOARD_HEIGHT = 80;  // close threshold (heightDelta)
export function nextKeyboardState(prev, ev): KeyboardNavState;  // focus-driven instant-hide state machine
```
From app/src/state/useKeyboard.ts: the single `handle(kind)` reads `viewport.height`,
`viewportState.baselineHeight`, computes `heightDelta = baselineHeight - currentHeight`, and the hook
returns only a boolean `keyboardOpen`. The `window.visualViewport` object is the geometry source.
From app/src/screens/AskScreen.tsx:600: root is
`height: calc(100dvh - var(--safe-area-top) - var(--bottom-nav-height))`, `display:flex`,
`flexDirection:column`, `overflow:hidden`; ChatInput is the last flex child at line 922.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract resolveChatInputOffset pure helper + executing test (Wave 0 scaffold)</name>
  <read_first>
    - app/src/state/keyboard-hysteresis.ts (pure-helper module pattern + threshold constants)
    - app/src/state/useKeyboard.ts (viewport geometry: viewport.height, baselineHeight, heightDelta)
    - app/tests/components/BottomNavigation.slide.test.mjs (executing + source-guard test pattern)
  </read_first>
  <action>
    Create `app/src/state/chatinput-offset.ts` (React-free, mirrors the keyboard-hysteresis.ts module
    style so it unit-tests under `node --test` without importing react). Export
    `resolveChatInputOffset({ keyboardHeight, isKeyboardOpen })` returning the pixel translateY the
    ChatInput should sit at to stay pinned above the keyboard while the WebView reflow is in flight.
    Inputs are derived from `window.visualViewport` geometry only — do NOT read DOM layout. Define the
    contract: when `isKeyboardOpen` is false return 0; when true return 0 as well IF the host relies on
    adjustResize to lift the bar (the offset helper exists to optionally smooth the transition delta, not
    to double-move the bar). The helper's real job: expose a single, testable mapping so the component can
    apply a CSS `transition` to the bar's transform and let the value settle, rather than the bar jumping.
    Keep the math deterministic and documented. Add `app/tests/state/chatinput-offset.test.mjs` with
    executing assertions: closed → 0; open with a given keyboardHeight → the documented value; idempotent
    on repeat calls. Follow the `text-art-fragment.test.mjs` executing-test style.
  </action>
  <verify>
    <automated>cd app && node --test tests/state/chatinput-offset.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/state/chatinput-offset.ts` exists, exports `resolveChatInputOffset`, imports no react.
    - `node --test tests/state/chatinput-offset.test.mjs` passes (≥3 executing assertions).
    - `grep -n "import .*react" app/src/state/chatinput-offset.ts` returns nothing.
  </acceptance_criteria>
  <done>Pure helper + executing test exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply a CSS transition to the ChatInput reposition; preserve all keyboard/overflow invariants</name>
  <read_first>
    - app/src/components/ChatInput.tsx (the bar being repositioned; minWidth:0 invariant at the input)
    - app/src/screens/AskScreen.tsx (lines 599-600 root flex column; line 922 ChatInput mount)
    - app/src/state/useKeyboard.ts (the focus-driven instant-hide machine — do not alter its contract)
    - CLAUDE.md sections: "ChatInput flex shrink", "Root overflow clip — both axes",
      "SwipeTabContainer resize + keyboard", "Header positioning"
  </read_first>
  <action>
    Make the input bar's reposition animate instead of teleport. Preferred minimal approach: add a CSS
    `transition` on `transform` (and/or `bottom`) to the ChatInput's own wrapper element so that when the
    WebView's adjustResize shrinks the viewport and the bar re-anchors, the visible motion is eased rather
    than instant. Drive the transform from `resolveChatInputOffset` using `window.visualViewport` height
    (subscribe in a small effect inside ChatInput.tsx or pass `keyboardOpen` from the existing
    `useKeyboard()` consumer in AskScreen — do NOT add a second viewport listener if the existing hook
    already exposes the needed signal; prefer reusing it). Constraints: (a) the `transition` MUST live on
    the ChatInput wrapper itself, NOT on an ancestor of a Header (Header-ancestor `transform`/`will-change`
    is forbidden by CLAUDE.md); (b) keep `minWidth:0` on the `<input>` and Send button padding 8px (≤44px);
    (c) do NOT touch `html, body { overflow:hidden }`, App-root `overflowX:hidden`, or the SwipeTabContainer
    `resync()`/`onFocusOut` logic; (d) do NOT install `@capacitor/keyboard`. If the eased transform would
    fight adjustResize (two animation owners), prefer animating only the residual delta or using a short
    duration (~150-200ms ease-out) so the settle is imperceptible. Do NOT regress the nav instant-hide:
    `useKeyboard`'s `nextKeyboardState` focusin path stays untouched.
  </action>
  <verify>
    <automated>cd app && node --test tests/components/ChatInput.flex-shrink.test.mjs tests/components/ChatInput.send-on-pointerdown.test.mjs tests/layout/root-horizontal-clip.test.mjs tests/components/SwipeTabContainer.resize-guard.test.mjs tests/components/BottomNavigation.slide.test.mjs && tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "minWidth: 0" app/src/components/ChatInput.tsx` ≥ 1 (input invariant intact).
    - `grep -c "transition" app/src/components/ChatInput.tsx` ≥ 1 AND the transition is on the ChatInput
      wrapper (not an ancestor of a Header — verify by reading the JSX, no `transform`/`will-change` added
      to AskScreen's root flex column or Header parents).
    - `grep -c "@capacitor/keyboard" app/src/state/useKeyboard.ts` returns 0.
    - `grep -v '^#' app/src/index.css | grep -c "overflow: hidden"` ≥ 1 (root clip intact).
    - `tsc -b --noEmit` clean; all listed invariant tests pass.
    - DEVICE-ONLY (automated tests are source-guards): on-device UAT below is the acceptance gate.
  </acceptance_criteria>
  <done>The input bar reposition is eased; all invariant source-guards + tsc pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: On-device UAT — smooth ChatInput reposition (GAP-A)</name>
  <what-built>
    GAP-A: the Ask-screen ChatInput now animates to its keyboard-open position via a CSS transition
    driven off visualViewport geometry, instead of teleporting. The nav instant-hide (commit 6fb7b325)
    and all CLAUDE.md keyboard/overflow/ChatInput invariants are preserved (source-guarded).
  </what-built>
  <how-to-verify>
    On a physical Android device (or the configured emulator) running the Capacitor build:
    1. Navigate to the Ask screen and tap the text input to open the keyboard.
       EXPECT: the input bar rises smoothly (eased) to sit above the keyboard — it does NOT instantly
       teleport. The bottom-nav still hides in a single motion with no up/down oscillation.
    2. Close the keyboard (back button / tap away). EXPECT: the bar eases back down; nav re-appears.
    3. Repeat open/close 3-5 times rapidly. EXPECT: no flicker, no leftover offset, bar always lands
       flush above the keyboard or at rest.
    4. Check Planner and Settings screens' keyboard interactions are unaffected (useKeyboard is global).
    If residual teleport remains, the transition duration / offset mapping in chatinput-offset.ts may
    need device tuning — note the observed behavior in the resume signal.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the residual motion issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView ↔ React layout | visualViewport resize events drive layout; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55.1A-01 | Tampering | CLAUDE.md keyboard/overflow invariants | mitigate | Source-guard tests assert minWidth:0, root-overflow clip, no @capacitor/keyboard, no Header-ancestor transform |
| T-55.1A-SC | Tampering | npm installs | accept | No package installs in this plan; all source-only changes |
</threat_model>

<verification>
- `cd app && node --test tests/state/chatinput-offset.test.mjs` passes.
- All CLAUDE.md invariant guard tests pass; `tsc -b --noEmit` clean.
- Mandatory on-device UAT confirms smooth reposition with no nav flicker regression.
</verification>

<success_criteria>
GAP-A closed: input bar animates smoothly on keyboard open/close, nav instant-hide preserved, all
load-bearing invariants intact (source-guarded), on-device UAT approved.
</success_criteria>

<output>
Create `.planning/phases/55.1-device-test-bug-fixes/55.1-05-SUMMARY.md` when done.
</output>
