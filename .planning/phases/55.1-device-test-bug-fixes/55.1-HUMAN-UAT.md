---
status: partial
phase: 55.1-device-test-bug-fixes
source: [55.1-VERIFICATION.md]
started: 2026-05-21T19:16:38Z
updated: 2026-05-21T19:16:38Z
---

## Current Test

Round 3 — gap-closure plans 55.1-05..08 implemented + merged (all automated tests green). Awaiting on-device re-test of GAP-A..D (Tests 5–8 below).

## Tests

### 1. BUGFIX-01 — Cross-session response binding (rapid-switch race)
expected: On device, rapidly ask a question → New Chat → ask again → New Chat (repeat 3×). No prior session's streaming answer ever renders under a different session's question. Switching session aborts the in-flight stream.
result: passed (device UAT 2026-05-21)

### 2. BUGFIX-02 — Text-art survives provider/locale switch (live Gemini)
expected: With a Gemini thinking model configured and text-art posts already generated, switch provider to Gemini and/or change locale (incl. zh/ja). Existing text-art posts keep full sentences — no collapse to a few words / single token, no API error from `thinkingConfig.thinkingBudget:0`. Short CJK headlines are NOT discarded.
result: passed (device UAT 2026-05-21, round 2 — confirmed).

### 3. BUGFIX-03 — No bottom-nav flicker on keyboard open
expected: On the Ask screen (Android), tap the input. The input island raises smoothly and the bottom-navigation bar does NOT oscillate up/down during the keyboard transition (single settle, no flicker).
result: nav-bar behavior PASSED after the focus-driven instant-hide re-fix (commit 6fb7b325). PARTIAL overall — re-test surfaced a new issue: the text input bar "teleports" to its keyboard-open position with no animation (native adjustResize reflow). Tracked as GAP-A for gap closure.

### 4. BUGFIX-04 — Send fires on first tap with keyboard open
expected: On the Ask screen with the keyboard open, tap Send once. The message sends on the FIRST tap (not consumed by keyboard dismissal), fires exactly once, and the keyboard stays open.
result: passed (device UAT 2026-05-21)

### 5. GAP-A (BUGFIX-05) — ChatInput animates smoothly to keyboard-open position (no teleport)
expected: On the Ask screen (Android), tap the text input. The input bar should SLIDE/settle into its keyboard-open position over a short ease (~0.18s), not instantly jump/teleport. The bottom nav must still hide cleanly with no flicker (BUGFIX-03 must not regress). If a residual jump remains, note it — device knobs are transition `0.18s` and settle map `keyboardHeight/12` (cap 24px).
result: pending (round 3)

### 6. GAP-B (BUGFIX-05) — Planner trellis stays smooth on a large graph
expected: On device with a large/production-scale trellis graph, open the Planner and scroll/interact. No perceptible lag. (Measured fix: `buildTrellisState` 60.87ms → 6.72ms at N=3000 by batching blossom-date writes to a single flush.) Leaf states (bud/green/yellow/falling/blossom/fruit) must look identical to before — outputs are unchanged, only the write path was optimized.
result: pending (round 3)

### 7. GAP-C (BUGFIX-06) — First LLM response after cold start is fast
expected: Force-quit the app, relaunch, and ask a question. The FIRST roundtrip should no longer take ~1 min. (Measured fix: the cold filter-corpus embed — 6875ms, 89.6% of first-ask cost — is now pre-warmed at boot, fire-and-forget, so the first ask pays 0 corpus embeds.) Filtering must still work correctly (off-topic/malicious questions still handled); offline / no-API-key launch must not error.
result: pending (round 3)

### 8. GAP-D (BUGFIX-07) — "Nothing new today" no longer shows above a populated feed
expected: On Home with a populated curiosity feed (posts visible), the "Nothing new today / Check back later" card must NOT appear (operator decision: HIDE). The card should appear ONLY when the feed is genuinely empty. Matches the round-2 screenshot scenario — the contradiction must be gone.
result: pending (round 3)

## Summary

total: 8
passed: 3
issues: 0
pending: 4
skipped: 0
blocked: 0

_Round 1+2 (BUGFIX-01..04): 3 passed, BUGFIX-03 partial→fixed. Round 3 (GAP-A..D): 4 pending device re-test._

## Gaps

Device UAT round 2: BUGFIX-01/02/04 confirmed passed; BUGFIX-03 nav flicker fixed. Remaining work folded into Phase 55.1 gap closure (see 55.1-VERIFICATION.md `## Gaps`):
- GAP-A — ChatInput "teleports" on keyboard open (no reposition animation). BUGFIX-03 follow-on.
- GAP-B — Planner trellis lag at production scale.
- GAP-C — ~1 min cold-start first LLM response.
- GAP-D — "Nothing new today" empty-state coexists with populated feed (`HomeScreen.tsx:959`).
