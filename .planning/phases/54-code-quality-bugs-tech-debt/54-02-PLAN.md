---
phase: 54-code-quality-bugs-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/screens/HomeScreen.empty-questions-no-error.test.mjs
  - app/tests/services/concept-feed-bonus-cap.test.mjs
  - app/tests/services/walker-empty-derived-list.test.mjs
  - .planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md
autonomous: true
requirements: [QUALITY-01]
must_haves:
  truths:
    - "The whole-codebase bug audit findings are recorded with a fix/log disposition for each cluster"
    - "The anchors=0 first-time-user path is proven NOT to set generationError"
    - "generateMorePosts with allExplored=true and bonusCap=0 returns [] without crashing"
    - "walkDerivedList with len=0 returns [] without a false error"
  artifacts:
    - path: ".planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md"
      provides: "Recorded findings for the 5 RESEARCH.md risk clusters + broader sweep, each with fix-or-log disposition"
    - path: "app/tests/screens/HomeScreen.empty-questions-no-error.test.mjs"
      provides: "Regression guard for the questions.length===0 no-error edge"
    - path: "app/tests/services/concept-feed-bonus-cap.test.mjs"
      provides: "Regression guard for the bonusCap=0 / allExplored cap edge"
    - path: "app/tests/services/walker-empty-derived-list.test.mjs"
      provides: "Regression guard for the empty-derived-list walker edge"
  key_links:
    - from: "54-BUG-AUDIT.md"
      to: "regression tests"
      via: "each confirmed bug fix cites the test that observes its corrected behavior"
      pattern: "QUALITY-01"
---

<objective>
Execute the QUALITY-01 whole-codebase bug audit (D-04: services + screens + hooks, breadth over a load-bearing-only focus). Audit the 5 risk clusters RESEARCH.md identified plus a broader sweep, record findings, fix confirmed bugs, and cover each fix with a regression test that observes the corrected RUNTIME behavior (NOT a source-string grep) per the CLAUDE.md "tests must guard the LIVE code path" lesson.

Purpose: Surface and fix real logic/edge/race bugs accumulated across v1.4–v1.6 before the rewards work begins, with durable regression coverage.
Output: A bug-audit findings doc + new behavior-observing regression tests for the highest-risk concept-feed edges, with any confirmed bug fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-CONTEXT.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-PATTERNS.md

# Audit targets — the highest-risk surfaces (RESEARCH.md §Bug-Audit Surfaces)
@app/src/services/concept-feed.service.ts
@app/src/services/post-queue.service.ts
@app/src/screens/HomeScreen.tsx
@app/src/services/daily-read.service.ts
@app/src/services/question-filter.service.ts

# Test analogs (PATTERNS.md) — follow these exactly
@app/tests/services/bonus-post-cap.test.mjs
@app/tests/screens/HomeScreen.exploredAnchors-resync.test.mjs
@app/tests/services/trajectoryAnalyzer.test.mjs

<interfaces>
<!-- Key facts the executor needs; from RESEARCH.md + PATTERNS.md. No codebase exploration needed for these. -->
- ESLint console rule (app/eslint.config.js:41): 'no-console': ['warn', { allow: ['warn', 'error'] }] — info/log/debug all warn.
- generationError gate: HomeScreen.tsx:223 — `if (posts.length === 0 && questions.length > 0 && !warmStartHadPostsRef.current) setGenerationError(true)`. The `questions.length > 0` clause is the guard that suppresses a false error for first-time users.
- bonus cap gate: concept-feed.service.ts:1698-1710 — allExplored = anchors.length > 0 && anchors.every(explored); cap fires when totalServed >= totalGenerated + bonusCap. anchors.length===0 means allExplored is false.
- walker: post-queue.service.ts:301 — maxSteps = Math.max(count * 2, len). With len=0 → Math.max(16,0)=16; loop over empty list returns [].
- question-filter dual-vector (CLAUDE.md load-bearing): malicious scored against RAW content vector; off-topic/on-topic against contextualized vector. When priorAnswer empty, contextVec aliases rawVec. DO NOT regress this.
- Test import note: concept-feed.service.ts + canonical-knowledge.service.ts have transitive i18n (locales/en.json) deps that block direct import under `node --test`. Use the inline-algorithm pure-helper pattern (mirror the gate as a pure function in the test) — see bonus-post-cap.test.mjs.
- localStorage polyfill needed for any test importing settings.service.ts — see bonus-post-cap.test.mjs lines 26-33.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the whole-codebase bug audit and record findings (QUALITY-01, D-04)</name>
  <read_first>
    - .planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md (§Bug-Audit Surfaces — the 5 clusters; §Common Pitfalls)
    - app/src/services/concept-feed.service.ts (Cluster 1: anchors=0, bonusCap=0, dequeue partial, walker len=0)
    - app/src/services/post-queue.service.ts (Cluster 1: walkDerivedList maxSteps, dequeue)
    - app/src/screens/HomeScreen.tsx (Cluster 2: always-mounted resync completeness incl. trellisCreditsService balance)
    - app/src/services/daily-read.service.ts (Cluster 3: getConceptQuota with empty questions, isCreditAwarded)
    - app/src/services/question-filter.service.ts (Cluster 4: dual-vector correctness, AbortSignal, null-corpus default)
    - app/src/services/canonical-knowledge.service.ts (Cluster 5: classification pre-check; backfill on empty store)
  </read_first>
  <action>
    Per D-04 do a breadth sweep (services + screens + hooks) anchored on the 5 RESEARCH.md clusters: (1) concept-feed pipeline edge cases (anchors=0, bonusCap=0, dequeue partial, walker len=0), (2) always-mounted-screen resync completeness — explicitly check whether `trellisCreditsService.getTotal()` for the harvest-credit display is re-read on `/home` navigation (RESEARCH.md open question #3) and whether `suggestedMoveCount` can go stale, (3) vine/leaf state edges (getConceptQuota with empty questions, creditAwardedRef freshness), (4) question-filter dual-vector correctness + pre-aborted AbortSignal handling + all-null-corpus default-to-on-topic, (5) scheduler daily-flag reset + generatePodcast error containment.

    Author `.planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md` recording, per cluster: what was checked, whether a bug was confirmed, and the disposition — FIX (this plan), LOG (note for a future phase), or NOT-A-BUG (correct as designed, with the reasoning).

    Apply Pitfall 5 (RESEARCH.md): do NOT tune any cosine threshold or numeric band (Phase 55) and do NOT touch UI spacing/animation/nav (Phase 56) — if found, LOG it, do not fix. Apply the Security Domain note: never bypass `normalizeAnchorName()` on any classification path and never collapse the question-filter dual-vector.

    The `trellisCreditsService` resync gap: RESEARCH.md says it is needed before Phase 58 and may be in scope here as a QUALITY-01 finding. Decide its disposition in the audit doc — if the harvest-credit display can show a stale balance after navigating away and back while it changed off-screen, that is a confirmed bug → FIX in Task 2 by extending the existing `[location.pathname]==='/home'` resync effect (the canonical pattern); if no current screen reads a mutable credit balance, LOG it as a Phase 57/58 invariant. State which, with evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md && grep -qi "cluster 1" .planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md && echo AUDIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md` exists.
    - The doc covers all 5 RESEARCH.md clusters (grep finds "Cluster 1" through "Cluster 5" or equivalent headings) plus a broader-sweep section.
    - Each cluster has an explicit disposition keyword (FIX / LOG / NOT-A-BUG) with reasoning.
    - The `trellisCreditsService` resync question (RESEARCH.md OQ#3) has a stated disposition with evidence.
    - No cosine threshold or UI/nav change appears in this plan's diff (those are Phase 55/56).
  </acceptance_criteria>
  <done>The bug-audit findings doc enumerates all 5 clusters + a broader sweep, each with a FIX/LOG/NOT-A-BUG disposition and the trellisCreditsService disposition resolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add behavior-observing regression tests for the concept-feed edges + fix any confirmed bug</name>
  <read_first>
    - app/tests/services/bonus-post-cap.test.mjs (inline-algorithm pure-helper analog + localStorage polyfill — PATTERNS.md)
    - app/tests/screens/HomeScreen.exploredAnchors-resync.test.mjs (region-slice source-reading analog — only if a runtime test is infeasible)
    - app/src/services/concept-feed.service.ts (the bonus-cap gate at ~1698-1710; buildConceptBatch empty-anchors return)
    - app/src/services/post-queue.service.ts (walkDerivedList maxSteps at ~301)
    - app/src/screens/HomeScreen.tsx (generationError gate at ~223)
    - .planning/phases/54-code-quality-bugs-tech-debt/54-BUG-AUDIT.md (the findings from Task 1 — drives which fixes are needed)
  </read_first>
  <behavior>
    - concept-feed-bonus-cap.test.mjs: with anchors=[{id:'a-1'}], exploredAnchorIds=['a-1'], totalServed=5, totalGenerated=5, bonusCap=0 → the cap helper returns true (capped, returns []); with anchors=[] (first-time user) → allExplored is false → NOT capped; with anchors present but none explored → NOT capped. Mirror the gate as a pure helper (i18n-dep blocks direct import).
    - walker-empty-derived-list.test.mjs: a pure mirror of walkDerivedList's maxSteps termination — with len=0 and count=8, maxSteps=Math.max(16,0)=16 and the walk over an empty list returns [] (no infinite loop, no thrown error). Also assert maxSteps regression guard: with count=16, len=4 → Math.max(32,4)=32 (NOT len*2=8), preserving the Phase 36 GAP-B fix.
    - HomeScreen.empty-questions-no-error: prove the generationError gate requires `questions.length > 0`, so a first-time user (questions.length===0, empty feed) does NOT see an error state. If a runtime harness is infeasible (HomeScreen has heavy transitive deps), use the region-slice source-reading pattern asserting the `questions.length > 0` clause is present in the gate region — this matches the existing exploredAnchors-resync guard's approach for screen-level invariants.
  </behavior>
  <action>
    Write the three regression tests above, preferring runtime behavior assertions (pure-helper mirrors of the live algorithm) for the two service edges per the VALIDATION.md "observe corrected behavior at runtime, not source presence" rule. Use the source-reading region-slice pattern ONLY for the HomeScreen screen-level guard where a runtime render is impractical.

    If Task 1's audit confirmed a real bug (e.g., the trellisCreditsService stale-balance resync, or any other FIX-disposition finding), implement the minimal fix now: for an always-mounted-screen resync gap, extend HomeScreen's existing `[location.pathname] === '/home'` effect with the missing setter — NEVER add a duplicate effect or a new event (CLAUDE.md: "extend the existing one", "one signal per semantic event"). Add a regression test observing the corrected behavior. If Task 1 found NO additional bug beyond the three edge guards, that is an acceptable outcome — record it in the SUMMARY and ship the three guards.

    Do not modify the concept-feed walker or bonus-cap source unless the audit confirmed those edges are actually broken — RESEARCH.md indicates they are currently correct, so the tests are pinning guards, not fix-drivers.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/concept-feed-bonus-cap.test.mjs tests/services/walker-empty-derived-list.test.mjs tests/screens/HomeScreen.empty-questions-no-error.test.mjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/concept-feed-bonus-cap.test.mjs tests/services/walker-empty-derived-list.test.mjs tests/screens/HomeScreen.empty-questions-no-error.test.mjs` exits 0.
    - `cd app && npm test` exits 0 (full suite green, no regressions).
    - `cd app && tsc -b --noEmit` exits 0.
    - If a bug was fixed, the diff shows the fix AND a test asserting the corrected behavior; if HomeScreen was touched, `grep -c "location.pathname" app/src/screens/HomeScreen.tsx` shows no NEW duplicate resync effect was added (the existing effect was extended).
    - The question-filter dual-vector guard test still passes: `cd app && node --test tests/services/filter-classifier.unit.test.mjs` exits 0 (no regression to the load-bearing dual-vector).
  </acceptance_criteria>
  <done>Three behavior-observing regression tests pass, the full suite is green, and any audit-confirmed bug is fixed with a corresponding regression test (or the SUMMARY records that no additional bug was found).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds tests and (at most) extends an existing resync effect; it processes no new external input and crosses no new I/O boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | N/A | (audit + tests) | accept | No high/medium threats — the audit reads code and adds tests. The one tampering-adjacent risk is accidentally regressing a load-bearing security invariant during cleanup. |
| T-54-02-DV | Tampering | question-filter dual-vector | mitigate | Acceptance criteria require `filter-classifier.unit.test.mjs` to pass; the plan explicitly forbids collapsing the dual-vector or bypassing `normalizeAnchorName()`. |
</threat_model>

<verification>
- `cd app && npm test` exits 0 and `cd app && tsc -b --noEmit` exits 0.
- The three new regression test files pass.
- `54-BUG-AUDIT.md` exists and covers all 5 clusters + broader sweep with dispositions.
- No cosine-threshold or UI/nav change is present (Phase 55/56 boundary respected).
</verification>

<success_criteria>
- QUALITY-01: whole-codebase audit performed and recorded; confirmed bugs fixed; the three highest-risk concept-feed edges are pinned by behavior-observing regression tests; full suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/54-code-quality-bugs-tech-debt/54-02-SUMMARY.md` when done.
</output>
