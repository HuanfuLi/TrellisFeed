---
phase: 54-code-quality-bugs-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/vine-chip-not-clearing-after-force-new-day.md
  - .planning/debug/feed-not-auto-populating-after-force-new-day.md
  - .planning/debug/resolved/vine-chip-not-clearing-after-force-new-day.md
  - .planning/debug/resolved/feed-not-auto-populating-after-force-new-day.md
  - .planning/todos/pending/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md
  - .planning/todos/done/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md
autonomous: true
requirements: [QUALITY-02, QUALITY-03, TECHDEBT-14]
must_haves:
  truths:
    - "Both Force-New-Day debug writeups are in .planning/debug/resolved/ with accurate fix/verification fields"
    - "The auto-gen podcast pending todo is closed (moved to done) with the device-verified disposition recorded"
    - "The full test suite passes and tsc is clean, formally re-confirming TECHDEBT-14"
  artifacts:
    - path: ".planning/debug/resolved/vine-chip-not-clearing-after-force-new-day.md"
      provides: "Resolved vine-chip debug writeup with fix location HomeScreen.tsx:667-672"
    - path: ".planning/debug/resolved/feed-not-auto-populating-after-force-new-day.md"
      provides: "Resolved feed-repopulation debug writeup with fix location SettingsDataScreen.tsx:118-127"
    - path: ".planning/todos/done/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md"
      provides: "Closed podcast verification todo with D-07 device-verified disposition"
  key_links:
    - from: "resolved debug writeups"
      to: "existing regression tests"
      via: "fix/verification fields cite the guarding test files"
      pattern: "exploredAnchors-resync|force-new-day"
---

<objective>
Close out the three already-satisfied requirements: QUALITY-02 (two carried-over Force-New-Day debug sessions, both fixes already shipped in v1.5), QUALITY-03 (auto-gen podcast, device-verified by operator per D-07), and TECHDEBT-14 (test suite already fully green, stale buildFallbackPosts test already fixed).

Per RESEARCH.md, none of these require re-implementing a fix. The work is verification-of-shipped-state + documentation hygiene + file moves.

Purpose: Formally resolve the carried-over debug threads and the deferred-test-failure requirement so the phase's clean baseline is recorded, not silently assumed.
Output: Two debug writeups moved to resolved/ with accurate fields, the podcast todo closed, and a recorded re-acceptance that the suite + tsc are green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-CONTEXT.md
@.planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md

# Debug writeups being resolved
@.planning/debug/vine-chip-not-clearing-after-force-new-day.md
@.planning/debug/feed-not-auto-populating-after-force-new-day.md
# Workflow analog — how a prior session was moved to resolved/ with updated fields
@.planning/debug/resolved/force-new-day-wipes-saved-liked.md
# The carried-over todo to close
@.planning/todos/pending/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify both Force-New-Day fixes via existing regression tests, then resolve the writeups (QUALITY-02)</name>
  <read_first>
    - app/src/screens/HomeScreen.tsx (the [location.pathname]==='/home' resync effect ~lines 667-672)
    - app/src/screens/settings/SettingsDataScreen.tsx (handleForceNewDay ~lines 78-127, the trellis_daily_posts mutation)
    - app/tests/screens/HomeScreen.exploredAnchors-resync.test.mjs (existing guard, vine-chip fix)
    - app/tests/screens/SettingsDataScreen.force-new-day.test.mjs (existing guard, feed-repopulation fix)
    - .planning/debug/vine-chip-not-clearing-after-force-new-day.md (writeup to update)
    - .planning/debug/feed-not-auto-populating-after-force-new-day.md (writeup to update)
    - .planning/debug/resolved/force-new-day-wipes-saved-liked.md (workflow analog: how fix/verification fields are filled before moving)
  </read_first>
  <action>
    Per D-06: both fixes are ALREADY in production code — do NOT re-implement. First prove they work by running the two existing regression tests. Then update each debug writeup's frontmatter/body so the `status`, `root_cause`, `fix`, and `verification` fields are accurate (currently the fix field reads "(not applied — find_root_cause_only mode)").

    For vine-chip writeup: set status to resolved; fix = HomeScreen.tsx:667-672 — the `[location.pathname] === '/home'` effect calls `setExploredAnchors(dailyReadService.getExploredAnchors())` and re-reads `creditAwardedRef.current = dailyReadService.isCreditAwarded()`; verification = `app/tests/screens/HomeScreen.exploredAnchors-resync.test.mjs` (passing).

    For feed-repopulation writeup: set status to resolved; root_cause = handleForceNewDay only rolled back trellis_post_queue.date, not the daily-posts cache, so loadCache() date-rejection never fired; fix = SettingsDataScreen.tsx:118-127 — symmetric mutation of `trellis_daily_posts.date` to yesterday; verification = `app/tests/screens/SettingsDataScreen.force-new-day.test.mjs` (passing).

    Then move both files from `.planning/debug/` into `.planning/debug/resolved/` (use git mv to preserve history). Confirm the live storage keys are `trellis_*` (not legacy `echolearn_*`) before citing them — RESEARCH.md storage-key audit confirms `trellis_post_queue` and `trellis_daily_posts`.

    Do NOT touch HomeScreen.tsx or SettingsDataScreen.tsx source — they already carry the fixes and are guarded; editing them risks regressing a load-bearing always-mounted-resync invariant documented in CLAUDE.md.
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/HomeScreen.exploredAnchors-resync.test.mjs tests/screens/SettingsDataScreen.force-new-day.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/screens/HomeScreen.exploredAnchors-resync.test.mjs tests/screens/SettingsDataScreen.force-new-day.test.mjs` exits 0.
    - `ls .planning/debug/resolved/vine-chip-not-clearing-after-force-new-day.md .planning/debug/resolved/feed-not-auto-populating-after-force-new-day.md` both succeed.
    - `ls .planning/debug/vine-chip-not-clearing-after-force-new-day.md .planning/debug/feed-not-auto-populating-after-force-new-day.md` both fail (files moved out).
    - `grep -l "not applied" .planning/debug/resolved/vine-chip-not-clearing-after-force-new-day.md .planning/debug/resolved/feed-not-auto-populating-after-force-new-day.md` returns nothing (placeholder fix text replaced).
    - No diff to `app/src/screens/HomeScreen.tsx` or `app/src/screens/settings/SettingsDataScreen.tsx` (`git diff --name-only` does not list either).
  </acceptance_criteria>
  <done>Both debug writeups are in resolved/ with accurate root_cause/fix/verification citing the existing passing tests; no source code was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Close the auto-gen podcast todo with the device-verified disposition (QUALITY-03)</name>
  <read_first>
    - .planning/todos/pending/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md (the todo to close)
    - .planning/phases/54-code-quality-bugs-tech-debt/54-CONTEXT.md (D-07 disposition: device-verified working 2026-05-20)
  </read_first>
  <action>
    Per D-07: the operator device-verified auto-gen podcast working on 2026-05-20, so QUALITY-03 is satisfied with NO diagnostics build and NO fix work. Close the carried-over todo by moving it from `.planning/todos/pending/` to `.planning/todos/done/` (use git mv; create the `done/` directory if it does not exist). Append a short disposition note to the todo body recording: closed per Phase 54 D-07 — auto-gen podcast device-verified working by operator 2026-05-20; no defects found; no code change required.

    Optional light source sanity-check ONLY (must NOT gate the phase, per D-07): confirm `scheduler.service.ts` catches errors from `generatePodcast` so a failed generation cannot crash the scheduler poll loop, and that the daily flag `trellis_scheduler_podcast_done` is reset on date-mismatch rather than being permanent. If anything is obviously broken, note it as a QUALITY-01 finding for Plan 02's audit — do NOT fix it here. If nothing is broken, record "light sanity-check: no issues" in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md && echo MOVED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/done/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md` exists.
    - `.planning/todos/pending/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md` no longer exists.
    - `grep -i "D-07\|device-verified" .planning/todos/done/2026-05-09-inspect-auto-gen-podcast-working-or-not-and-debug.md` returns a match (disposition recorded).
    - No source files under `app/src/` are modified by this task.
  </acceptance_criteria>
  <done>The podcast todo is in done/ with the D-07 device-verified disposition recorded; no podcast code was changed.</done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm the green suite + clean tsc baseline for TECHDEBT-14</name>
  <read_first>
    - .planning/phases/54-code-quality-bugs-tech-debt/54-RESEARCH.md (Test Suite & Type Check Baseline: 1,620 passing, 0 skip, stale buildFallbackPosts test already fixed)
    - app/tests/concept-feed.test.mjs (the file whose buildFallbackPosts import was already removed — confirm no stale import remains)
  </read_first>
  <action>
    Per D-08 and RESEARCH.md: the suite is already fully green (1,471 + 149 tests, 0 failures, 0 skipped) and the stale `buildFallbackPosts` test contract was already fixed in concept-feed.test.mjs. TECHDEBT-14's success criterion ("full suite + tsc green") is a verification gate, not new code work.

    Run the full suite and the typecheck to formally re-confirm the baseline. Confirm there are zero `.skip` calls and zero `@ts-ignore`/`@ts-expect-error` directives (RESEARCH.md confirmed this). Record in the SUMMARY the formal re-acceptance: TECHDEBT-14 is satisfied — suite green, tsc clean, no stale-test repair needed, no intentional gaps requiring a rationale comment. Do NOT add or modify any test or source file unless the suite is actually red — if red, that is a QUALITY-01 finding (route to Plan 02), not a TECHDEBT-14 fix here.
  </action>
  <verify>
    <automated>cd app && npm test && tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npm test` exits 0 (full suite green).
    - `cd app && tsc -b --noEmit` exits 0 (no type errors).
    - `grep -rn "test\.skip\|describe\.skip\|it\.skip" app/tests/` returns no matches.
    - `grep -rn "@ts-ignore\|@ts-expect-error" app/src/` returns no matches.
    - SUMMARY records the TECHDEBT-14 formal re-acceptance (suite green, tsc clean, no repair needed).
  </acceptance_criteria>
  <done>Full suite and tsc are confirmed green; TECHDEBT-14 is formally re-accepted with that evidence recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundary. It moves planning documents, closes a todo, and runs the existing test suite. No new external input path, no new I/O surface, no untrusted data crosses any boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | N/A | (cleanup-only plan) | accept | No high/medium threats. This plan only relocates `.planning/` markdown files and runs read-only verification commands; it adds no attack surface, no new I/O boundary, and no untrusted input path. The explicit instruction NOT to edit HomeScreen.tsx / SettingsDataScreen.tsx preserves the load-bearing always-mounted-resync and dual-key Force-New-Day invariants. |
</threat_model>

<verification>
- `cd app && npm test` exits 0 and `cd app && tsc -b --noEmit` exits 0.
- The two debug writeups live in `.planning/debug/resolved/` with accurate fix/verification fields.
- The podcast todo is in `.planning/todos/done/` with the D-07 disposition.
- No `app/src/` source files were modified by this plan.
</verification>

<success_criteria>
- QUALITY-02: both Force-New-Day debug sessions are resolved (writeups moved to resolved/, fixes proven by existing passing tests).
- QUALITY-03: podcast todo closed with the device-verified disposition; no diagnostics build performed.
- TECHDEBT-14: suite + tsc formally re-confirmed green; no stale-test repair needed.
</success_criteria>

<output>
Create `.planning/phases/54-code-quality-bugs-tech-debt/54-01-SUMMARY.md` when done.
</output>
