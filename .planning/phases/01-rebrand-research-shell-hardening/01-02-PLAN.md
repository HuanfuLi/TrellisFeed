---
phase: 01-rebrand-research-shell-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/db.service.ts
  - app/src/main.tsx
  - app/src/services/settings.service.ts
  - app/src/services/daily-read.service.ts
  - app/src/services/session.service.ts
  - app/src/services/concept-feed.service.ts
  - app/src/services/filter-corpus.service.ts
  - app/src/services/imageGeneration.service.ts
  - app/src/lib/cold-start-profiler.ts
  - app/src/services/legacy-migration.service.ts
  - app/tests/services/legacy-migration.test.mjs
  - app/tests/services/storage-namespace.test.mjs
  - app/tests/services/storage-migration.test.mjs
autonomous: true
requirements: [SHELL-02]
must_haves:
  truths:
    - "The primary IndexedDB database is named `questiontrace` and every retained app-owned localStorage/sessionStorage key uses the `questiontrace_*` namespace (SHELL-02, SHELL-02 no-migration rule)."
    - "New `research_records`, `research_upload_queue`, and `research_metadata` object stores exist under the questiontrace DB and round-trip through the dbQuery/dbExecute seam."
    - "No boot-time read-forward migration runs; old `trellis`/`echolearn` data is orphaned, never copied to the new namespace."
    - "The Node fallback (LocalStorageBackend) and IndexedDBBackend still implement the identical SQL subset for the new stores."
  artifacts:
    - app/tests/services/storage-namespace.test.mjs
    - app/src/services/db.service.ts
  key_links:
    - "SHARED_DDL research tables ↔ TABLE_NAMES object-store derivation ↔ IDB_VERSION bump onupgradeneeded"
    - "renamed key constants ↔ every reader/writer of that store"
---

<objective>
Rename the storage namespace from the legacy brand to `questiontrace` with NO migration framework (old data orphaned by design), and add the three durable research object stores (`research_records`, `research_upload_queue`, `research_metadata`) that Plans 06/07/09 build on. Remove the boot-time legacy-key read-forward migration entirely. Implements SHELL-02 and the ROADMAP no-migration success criterion.

Purpose: A clean, single-namespace durable store is the foundation for interaction logging and the upload queue.
Output: Renamed DB + keys, new research DDL stores, deleted legacy migration, and a behavioral namespace test through the dbQuery seam.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- IndexedDB stores `research_records`, `research_upload_queue`, `research_metadata` (new DDL rows in `SHARED_DDL`)
- `app/tests/services/storage-namespace.test.mjs` (new test file)
- Renamed constants (same names, new values): `IDB_NAME='questiontrace'`, `PREFIX='questiontrace_db_'`, `IDB_VERSION=2`, `FILTER_CORPUS_CACHE_KEY`, `COLD_START_PROFILE_KEY`, image-cache prefixes
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-CONTEXT.md
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@CLAUDE.md
@app/src/services/db.service.ts
@app/tests/services/storage-migration.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the DB namespace, add research stores, and delete the legacy migration</name>
  <files>app/src/services/db.service.ts, app/src/main.tsx, app/src/services/legacy-migration.service.ts, app/tests/services/legacy-migration.test.mjs</files>
  <read_first>
    - app/src/services/db.service.ts (IDB_NAME, IDB_VERSION, PREFIX, SHARED_DDL, TABLE_NAMES, onupgradeneeded, LEGACY_HEAVY_KEYS, clearAllTables, console.info tags)
    - app/src/main.tsx (migrateLegacyKeys import + call)
    - app/src/services/legacy-migration.service.ts (to be deleted)
    - CLAUDE.md § "Heavy stores live in IndexedDB" and § "LocalStorageBackend and IndexedDBBackend must implement the same SQL subset"
    - RESEARCH § "Add local research stores through the existing DB schema" and § Runtime State Inventory
  </read_first>
  <action>
    In `db.service.ts`: set `IDB_NAME` to `questiontrace`, `PREFIX` to `questiontrace_db_`, and bump `IDB_VERSION` to `2` (the version bump is what fires `onupgradeneeded` to create the new stores; the existing loop already creates any missing store from TABLE_NAMES). Append three rows to `SHARED_DDL` using ONLY the supported SQL subset (first column is the PK/row key): `research_records (id TEXT PRIMARY KEY, kind TEXT NOT NULL, revision INTEGER NOT NULL, data TEXT NOT NULL)`, `research_upload_queue (id TEXT PRIMARY KEY, data TEXT NOT NULL)`, `research_metadata (id TEXT PRIMARY KEY, data TEXT NOT NULL)`. Add matching `DELETE FROM research_records` / `research_upload_queue` / `research_metadata` lines to `clearAllTables`. Update the two `console.info`/`console.warn` backend-active log tags to read `[QuestionTrace]`.
    Leave `LEGACY_HEAVY_KEYS` and `clearLegacyHeavyLocalStorageKeys` in place UNCHANGED — they only DELETE orphaned legacy heavy keys at boot (quota reclamation, never a read-forward), which is consistent with the no-migration orphaning rule. Do not repoint any reader to the old namespace.
    In `main.tsx`: remove the `migrateLegacyKeys` import and its call (the EchoLegacy→legacy read-forward is incompatible with the no-migration decision — RESEARCH § Deprecated/outdated). Delete `app/src/services/legacy-migration.service.ts` and its test `app/tests/services/legacy-migration.test.mjs`.
    Do NOT touch App.tsx here (Plan 04 owns the App.tsx hydration edit).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/storage-namespace.test.mjs tests/services/storage-migration.test.mjs && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `db.service.ts` source: `IDB_NAME` value is `questiontrace`, `IDB_VERSION` is `2`, `PREFIX` is `questiontrace_db_`.
    - `SHARED_DDL` contains the three research tables; `TABLE_NAMES` (derived) therefore includes them.
    - `main.tsx` no longer imports or calls `migrateLegacyKeys`; `legacy-migration.service.ts` and its test no longer exist.
    - `npm run lint` reports no new errors (no dangling import of the deleted service).
  </acceptance_criteria>
  <done>Primary DB is `questiontrace`, research stores exist, and the boot-time legacy migration is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rename all retained per-service storage-key owners to the questiontrace namespace</name>
  <files>app/src/services/settings.service.ts, app/src/services/daily-read.service.ts, app/src/services/session.service.ts, app/src/services/concept-feed.service.ts, app/src/services/filter-corpus.service.ts, app/src/services/imageGeneration.service.ts, app/src/lib/cold-start-profiler.ts</files>
  <read_first>
    - app/src/services/settings.service.ts (`STORAGE_KEY = 'trellis_settings'`)
    - app/src/services/daily-read.service.ts (`STORAGE_KEY = 'trellis_daily_read'`)
    - app/src/services/session.service.ts (`ACTIVE_ID_KEY = 'trellis_active_session'`)
    - app/src/services/concept-feed.service.ts (`CONNECTION_POSTS_KEY = 'trellis_connection_posts'`, sessionStorage)
    - app/src/services/filter-corpus.service.ts (exported `FILTER_CORPUS_CACHE_KEY = 'trellis_filter_corpus_emb_v1'`)
    - app/src/services/imageGeneration.service.ts (`IDB_NAME = 'trellis_images'`, `CACHE_KEY_PREFIX = 'img-cache-'`, `CACHE_META_KEY = 'img-cache-meta'`)
    - app/src/lib/cold-start-profiler.ts (exported `COLD_START_PROFILE_KEY = 'trellis_cold_start_profile'`)
    - app/tests/services/storage-migration.test.mjs (allowlists `ACTIVE_ID_KEY` by name, not value — safe to rename the value)
  </read_first>
  <action>
    Change each storage-key VALUE (not the constant name) to the `questiontrace_*` namespace, one edit per owner: `questiontrace_settings`, `questiontrace_daily_read`, `questiontrace_active_session`, `questiontrace_connection_posts`, `questiontrace_filter_corpus_emb_v1`, image DB `questiontrace_images`, image cache prefix `questiontrace_img_cache_`, image meta `questiontrace_img_cache_meta`, `questiontrace_cold_start_profile`. This is a namespace-wide rename (RESEARCH § Rebrand and Prune Sweep Plan item 3 — generic `img-cache-*` keys become `questiontrace_*` too). Do NOT rename `REORG_SNAPSHOT_KEY` in canonical-knowledge (Plan 10 deletes that reorg residue) and do NOT rename the `trellis_dev_mode` key in SettingsDataScreen (Plan 09 deletes that screen). Preserve `LEGACY_HEAVY_KEYS` in db.service.ts (those legacy names are the delete-list, intentionally left).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/storage-namespace.test.mjs tests/services/settings-locale.test.mjs tests/services/daily-read.service.test.mjs && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Exported `FILTER_CORPUS_CACHE_KEY` and `COLD_START_PROFILE_KEY` both start with `questiontrace_`.
    - `settings.service.ts`, `daily-read.service.ts`, `session.service.ts`, `concept-feed.service.ts`, `imageGeneration.service.ts` each define their retained key/DB in the `questiontrace_*` namespace.
    - Existing settings/daily-read tests still pass (behavior unchanged; only the storage key name moved).
  </acceptance_criteria>
  <done>Every retained active storage owner reads/writes under `questiontrace_*`; the two owners scheduled for deletion are intentionally left for Plans 09/10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the storage-namespace behavioral test (through dbQuery)</name>
  <files>app/tests/services/storage-namespace.test.mjs, app/tests/services/storage-migration.test.mjs</files>
  <read_first>
    - app/tests/services/storage-migration.test.mjs (localStorage shim pattern; source-read vs dbQuery guidance)
    - CLAUDE.md § "Testing this area" (false-green risk — assert through dbQuery, not the in-memory mirror)
    - app/src/services/db.service.ts (dbExecute/dbQuery exports)
  </read_first>
  <behavior>
    - INSERT OR REPLACE a row into `research_records` via `dbExecute`, then `dbQuery('SELECT * FROM research_records')` returns it (round-trip through the real backend seam, LocalStorageBackend under Node).
    - Same round-trip for `research_upload_queue` and `research_metadata`.
    - Source assertion: `IDB_NAME` resolves to `questiontrace`; the exported `FILTER_CORPUS_CACHE_KEY`/`COLD_START_PROFILE_KEY` start with `questiontrace_`; no retained active owner defines a `trellis_`-prefixed key (LEGACY_HEAVY_KEYS delete-list and comments excluded).
  </behavior>
  <action>
    Create `app/tests/services/storage-namespace.test.mjs` with the localStorage shim from storage-migration.test.mjs. Add durable round-trip assertions for all three research stores THROUGH `dbExecute`/`dbQuery` (not the mirror). Add a source-scan assertion that reads the renamed owner files and asserts each retained active key is `questiontrace_`-prefixed; scope the negative check with `grep`-style filtering that excludes comment lines and the `LEGACY_HEAVY_KEYS` array so the intentional legacy delete-list does not trip the gate. Import the exported cache-key constants directly to assert their prefix.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/storage-namespace.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The three research stores round-trip through `dbQuery` (behavioral, not source-read).
    - The namespace source assertions pass and would fail if any retained owner reverted to the legacy prefix.
    - `npm test` discovers and passes the new file.
  </acceptance_criteria>
  <done>The namespace rename and research-store durability are pinned by a behavioral gate that reads through the persistence seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app code → durable local store | Renamed namespace and new research stores hold participant records before upload; a schema/version mistake loses durability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | IndexedDB schema/version | high | mitigate | Bump `IDB_VERSION` so `onupgradeneeded` creates the new stores; behavioral dbQuery round-trip test guards store creation. |
| T-01-05 | Information disclosure | orphaned legacy `trellis`/`echolearn` data | low | accept | No read-forward migration; old data is orphaned by design (ROADMAP no-migration decision); boot delete-sweep reclaims legacy heavy keys. |
| T-01-06 | Denial of service | SQL subset drift between backends | medium | mitigate | New DDL uses only INSERT OR REPLACE / SELECT * / PK DELETE (subset supported by both backends); CLAUDE.md parity rule honored. |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green.
- Manual UAT (device/browser): confirm a fresh install opens the `questiontrace` IndexedDB database (DevTools → Application → IndexedDB) with the research stores present, and that old `trellis` data is not read.
</verification>

<success_criteria>
Single `questiontrace` namespace across DB + retained keys; research stores durable through the seam; no migration framework; gates green.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-02-SUMMARY.md` when done.
</output>
