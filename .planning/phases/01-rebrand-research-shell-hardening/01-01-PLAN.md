---
phase: 01-rebrand-research-shell-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/index.html
  - app/capacitor.config.ts
  - app/ios/App/App/Info.plist
  - app/android/app/src/main/res/values/strings.xml
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/phase1/rebrand-surfaces.test.mjs
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "The system/home-screen app name reads QuestionTrace on iOS, Android, web title, and Capacitor appName (D-05)."
    - "No user-facing 'Trellis' string remains in the four active locale bundles or the shipped web/native display surfaces."
    - "iOS bundle id com.huanfuli.trellis and Android applicationId/namespace com.trellis.app are byte-for-byte unchanged."
    - "Onboarding/consent/privacy copy describes the research shell (post-scoped Q&A + logged study signals uploaded to a research collector), not a no-backend local-only app (D-06)."
  artifacts:
    - app/tests/phase1/rebrand-surfaces.test.mjs
  key_links:
    - "app_name resource strings ↔ native launcher/home-screen label"
    - "locale bundle key parity ↔ bundle-parity.test.mjs gate"
---

<objective>
Rebrand every user-facing display surface from the legacy product brand to QuestionTrace: web title, Capacitor `appName`, iOS/Android display names, and all four locale bundles — while leaving native bundle identifiers untouched. Replace the legacy local-only privacy/consent copy with research-shell copy that matches the actual behavior (records upload to a research collector). Implements SHELL-01 and locked decisions D-05 (QuestionTrace is the system app name; in-app pages use neutral titles) and D-06 (English default; four switchable UI languages; curated content stays English).

Purpose: A stable branded shell is the prerequisite for participant installs and for every later phase.
Output: Rebranded display/native/locale surfaces + a regression test pinning the rename and the preserved bundle identifiers.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `app/tests/phase1/rebrand-surfaces.test.mjs` (new test file)
- Display string value `QuestionTrace` in `app/index.html` `<title>`, `capacitor.config.ts` `appName`, `Info.plist` `CFBundleDisplayName`, `strings.xml` `app_name`/`title_activity_main`
</artifacts_this_phase_produces>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rebrand-research-shell-hardening/01-CONTEXT.md
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@CLAUDE.md
@app/tests/locales/bundle-parity.test.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand web + native display surfaces (preserve bundle identifiers)</name>
  <files>app/index.html, app/capacitor.config.ts, app/ios/App/App/Info.plist, app/android/app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/index.html (current `<title>Trellis</title>`)
    - app/capacitor.config.ts (`appId: 'com.trellis.app'`, `appName: 'Trellis'`)
    - app/ios/App/App/Info.plist (CFBundleDisplayName, CFBundleName, NSMicrophoneUsageDescription)
    - app/android/app/src/main/res/values/strings.xml (app_name, title_activity_main, package_name, custom_url_scheme)
    - CLAUDE.md § "iOS device build, gestures, and app assets" (bundle-id asymmetry rule) and the Naming-residue note
  </read_first>
  <action>
    Change every DISPLAY name to `QuestionTrace`, leaving every IDENTIFIER unchanged (per the CLAUDE.md load-bearing bundle-id rule, D-05):
    - `app/index.html`: set `<title>` to `QuestionTrace`.
    - `app/capacitor.config.ts`: set `appName` to `QuestionTrace`. Do NOT change `appId` — it stays `com.trellis.app` (Android applicationId depends on it; renaming orphans installed data).
    - `app/ios/App/App/Info.plist`: set `CFBundleDisplayName` string to `QuestionTrace`. Leave `CFBundleName` and the Xcode `PRODUCT_BUNDLE_IDENTIFIER` (`com.huanfuli.trellis`) untouched. Remove the stale `NSMicrophoneUsageDescription` key AND its `<string>` value — voice recording was pruned in Phase 0 and no live native capability needs the microphone (RESEARCH Runtime State Inventory §3; prune_report Leftover Dead Context).
    - `app/android/app/src/main/res/values/strings.xml`: set `app_name` and `title_activity_main` to `QuestionTrace`. Leave `package_name` and `custom_url_scheme` as `com.trellis.app`.
    Do not run `npx cap sync` here — native project regeneration is a manual UAT step recorded in the phase verification section, not an executor action.
  </action>
  <verify>
    <automated>cd app && node --test tests/phase1/rebrand-surfaces.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `app/index.html` contains `<title>QuestionTrace</title>` and no `<title>` containing the legacy brand.
    - `capacitor.config.ts` has `appName: 'QuestionTrace'` and still has `appId: 'com.trellis.app'`.
    - `Info.plist` `CFBundleDisplayName` value is `QuestionTrace`; the `NSMicrophoneUsageDescription` key is absent; `PRODUCT_BUNDLE_IDENTIFIER com.huanfuli.trellis` still present in the Xcode project.
    - `strings.xml` `app_name` and `title_activity_main` are `QuestionTrace`; `package_name` and `custom_url_scheme` are still `com.trellis.app`.
  </acceptance_criteria>
  <done>Display surfaces read QuestionTrace; iOS/Android identifiers and package paths are byte-for-byte unchanged; the microphone usage string is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand the four locale bundles and replace local-only privacy copy with research-shell copy</name>
  <files>app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json</files>
  <read_first>
    - app/src/locales/en.json (canonical — keys under `onboarding.*`, `settings.about.version`, `settings.data.privacyBlurb`, `onboarding.consent.*` rowNoServer/intro/title, and any `Trellis`/`trellis`-bearing display value)
    - app/src/locales/zh.json, es.json, ja.json (same keys, translated values)
    - CLAUDE.md § "i18n Workflow" — EN-first, four-bundle parity, proper-noun rules, `bundle-parity.test.mjs` gate
    - app/tests/locales/bundle-parity.test.mjs and app/tests/locales/missing-key.test.mjs
  </read_first>
  <action>
    In all four bundles together (EN-first, key parity preserved), replace every user-visible DISPLAY value of the legacy brand `Trellis` with `QuestionTrace` (the proper noun is NOT translated — same English token in zh/es/ja values), including onboarding welcome title/intro, the About version label (`QuestionTrace v1.0.0`), and any screen copy still naming the old brand. Then rewrite the privacy/consent copy that currently asserts "no server ever receives your data / there is no backend" to accurately describe the research shell per RESEARCH § State of the Art: interaction records and question/answer records are stored on-device first and uploaded to a research data collector; the app works offline and retries. Keep curated content, questions, and answers English-only regardless of UI language (D-06) — do not add content-translation copy. Do NOT rename storage keys or namespaces here (that is Plan 02). Leave unused legacy namespaces in place — Plan 10 removes dead locale namespaces; removing keys here would break parity mid-plan.
    Note: the leaf-state debug namespace (`settings.developer.trellisDevMode*`) is dead-UI residue removed in Plan 10; do not rebrand values you are about to delete — leave them for the sweep.
  </action>
  <verify>
    <automated>cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs tests/phase1/rebrand-surfaces.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `bundle-parity.test.mjs` and `missing-key.test.mjs` pass (identical key sets across en/zh/es/ja).
    - The rebrand-surfaces test's locale assertion finds zero active user-facing occurrences of the legacy brand in `en.json` outside the leaf-debug namespace slated for Plan-10 deletion.
    - `en.json` `onboarding.consent.rowNoServer` (or its successor key) no longer claims data is never transmitted; it describes upload to a research collector.
    - `settings.about.version` reads `QuestionTrace v1.0.0` in all four bundles.
  </acceptance_criteria>
  <done>All four bundles name QuestionTrace on visible surfaces, privacy copy matches the collector behavior, and parity gates are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author rebrand-surfaces regression test</name>
  <files>app/tests/phase1/rebrand-surfaces.test.mjs</files>
  <read_first>
    - app/tests/locales/bundle-parity.test.mjs (test style: node:test, fs.readFileSync of source/config, assert.match)
    - app/index.html, app/capacitor.config.ts, app/android/app/src/main/res/values/strings.xml, app/ios/App/App/Info.plist
  </read_first>
  <behavior>
    - Reads index.html/capacitor.config.ts/strings.xml/Info.plist and asserts each display surface equals `QuestionTrace`.
    - Asserts `appId 'com.trellis.app'` present in capacitor.config.ts; `com.huanfuli.trellis` present in the Xcode project file; `package_name`/`custom_url_scheme` still `com.trellis.app`.
    - Asserts `NSMicrophoneUsageDescription` is absent from Info.plist.
    - Reads en.json and asserts no active user-facing legacy-brand display value remains outside the allowlisted leaf-debug namespace.
  </behavior>
  <action>
    Create `app/tests/phase1/rebrand-surfaces.test.mjs` (create the `app/tests/phase1/` directory). Use `node:test` + `node:assert/strict`, reading files with `fs.readFileSync` relative to `import.meta.url` (mirror bundle-parity.test.mjs). Assert the display-name equalities and the identifier-preservation invariants above. For the en.json legacy-brand scan, load JSON, walk string values, and allowlist only the `settings.developer` leaf-debug namespace (removed in Plan 10). Reference the acceptance identifiers by variable, not by embedding a full forbidden-token list as a written source comment.
  </action>
  <verify>
    <automated>cd app && node --test tests/phase1/rebrand-surfaces.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at `app/tests/phase1/rebrand-surfaces.test.mjs` and is discovered by `npm test`.
    - All assertions pass against the Task 1/2 edits.
    - Test fails if any display surface reverts to the legacy brand OR if a preserved identifier is altered.
  </acceptance_criteria>
  <done>The rename and the identifier-preservation invariants are locked by an automated gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → native package | Display strings and bundle identifiers are read by the OS installer; a wrong identifier orphans installed data/signing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | bundle identifiers (iOS/Android) | high | mitigate | rebrand-surfaces.test.mjs asserts `com.huanfuli.trellis` and `com.trellis.app` are unchanged; only display strings mutate. |
| T-01-02 | Information disclosure | privacy/consent copy | medium | mitigate | Replace false "no server receives data" copy so consent text matches the actual collector upload (accurate disclosure to participants). |
| T-01-03 | Repudiation | stale microphone permission string | low | accept | Removing the unused NSMicrophoneUsageDescription reduces an unjustified capability prompt; no residual risk. |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green after edits.
- Manual UAT (native): run `npx cap sync`, inspect the iOS/Android diff, and confirm the launcher/home-screen label reads QuestionTrace with no identifier change.
</verification>

<success_criteria>
No user-facing legacy-brand string remains on active display/native/locale surfaces; bundle identifiers unchanged; parity + rebrand-surfaces gates green.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-01-SUMMARY.md` when done.
</output>
