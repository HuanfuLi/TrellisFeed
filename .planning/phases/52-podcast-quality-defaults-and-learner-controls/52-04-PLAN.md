---
phase: 52-podcast-quality-defaults-and-learner-controls
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - app/src/services/podcast-view-model.ts
  - app/src/screens/PodcastScreen.tsx
  - app/src/services/podcast.service.ts
  - app/tests/services/podcast-view-model.test.mjs
autonomous: true
requirements:
  - PODCAST-03   # restores: cached/regenerated podcasts honor options + identity; freshly generated podcast plays
user_setup: []

must_haves:
  truths:
    - "A freshly generated podcast plays back: audioRef binds to the just-generated id, play does NOT hit the !audio early-return."
    - "Exactly one of {podcast player, 'No podcast for today' empty state} renders at a time."
    - "A freshly generated podcast with unchanged chips yields isDirty === false (no phantom Regenerate CTA)."
    - "Old podcasts (no today podcast) are reached via History; the main player only shows TODAY's podcast."
  artifacts:
    - path: "app/src/services/podcast-view-model.ts"
      provides: "Pure leaf functions deriveSelectedPodcast + isEmptyStateVisible + a single source-of-truth gate, unit-testable without jsdom/Audio/IndexedDB."
      contains: "export function deriveSelectedPodcast"
    - path: "app/tests/services/podcast-view-model.test.mjs"
      provides: "Behavioral tests for the three UAT missing: fields (mutual exclusion, fresh-play binding, isDirty===false)."
      contains: "deriveSelectedPodcast"
  key_links:
    - from: "app/src/screens/PodcastScreen.tsx"
      to: "app/src/services/podcast-view-model.ts"
      via: "import { deriveSelectedPodcast, isEmptyStateVisible }"
      pattern: "deriveSelectedPodcast"
    - from: "app/src/screens/PodcastScreen.tsx (player gate ~line 627)"
      to: "app/src/screens/PodcastScreen.tsx (empty-state gate ~line 773)"
      via: "single mutually-exclusive condition keyed on the same derived source"
      pattern: "isEmptyStateVisible"
---

<objective>
Close GAP-4 (blocker) and GAP-3 (major) from the Phase 52 UAT. These two are tightly coupled: both stem from the `selected` derivation at `PodcastScreen.tsx:104-106` falling back to a stale `podcasts[0]`, and the player-gate (line 627, keyed on `selected`) being divergent from the empty-state gate (line 773, keyed on `todayPodcast`).

Full diagnosis: `.planning/debug/phase-52-podcast-playback-and-dual-render.md`.

Purpose: A freshly generated podcast must actually play, and the screen must never show the player and the "No podcast for today" empty state simultaneously.

Output:
- New pure leaf module `app/src/services/podcast-view-model.ts` holding the selection + render-gate logic, so the LIVE path is unit-testable (the existing source-read-only test gave false confidence and missed both bugs — CLAUDE.md best-practice #2).
- PodcastScreen.tsx rewired to consume the leaf module; deterministic select-on-generate; reconciled isDirty hash.
- podcast.service.ts echoes back the resolved concept-id list so the screen's currentHash matches the service's optionsHash.
- New behavioral test file covering the three UAT `missing:` items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UAT.md
@.planning/debug/phase-52-podcast-playback-and-dual-render.md

# The screen + service being fixed (read fully before editing)
@app/src/screens/PodcastScreen.tsx
@app/src/services/podcast.service.ts
@app/src/state/usePodcast.ts

# Leaf-module exemplar (no JSON / lib/date / react-i18next imports) — the new
# podcast-view-model.ts MUST follow this same discipline so node --test can import it.
@app/src/services/podcast-prompt.ts

# Behavioral-test analog: pure-logic unit test via dynamic import (NO jsdom — the
# project has no React render harness; behavioral tests target extracted pure helpers).
@app/tests/services/podcast-options.test.mjs

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Use directly — no exploration. -->

From app/src/types/index.ts:
```typescript
export interface DailyPodcast {
  id: string;
  date: string;
  questionIds: string[];
  script: string;
  duration?: number;
  status: PodcastStatus;          // 'pending' | 'generating' | 'ready' | 'failed'
  progress?: number;
  error?: string;
  createdAt: number;
  options?: PodcastOptions;
  optionsHash?: string;
}
export interface PodcastOptions { length: PodcastLength; style: PodcastStyle; }
```

Current stale derivation (PodcastScreen.tsx:103-106) — THE BUG:
```typescript
const todayPodcast = getPodcastForDate(today());
const selected: DailyPodcast | null = selectedId
  ? (podcasts.find((p) => p.id === selectedId) ?? null)
  : (todayPodcast ?? podcasts[0] ?? null);   // ← podcasts[0] fallback is the dual-render + wrong-blob trigger
```

Current divergent gates:
- Player (line 627):  `selected && selected.status === 'ready'`
- Chip Card (line 563): `(!todayPodcast || todayPodcast.status === 'pending' || todayPodcast.status === 'failed' || (selected && selected.status === 'ready'))`
- Empty state (line 773): `(!todayPodcast || todayPodcast.status === 'pending' || todayPodcast.status === 'failed')`

Service optionsHash source (podcast.service.ts:163-179) — the list the screen must match:
```typescript
// Uses provided conceptIds; else getDueForReview(date); else getRecent(5) fallback.
const conceptIdList = questions.map((q) => q.id);
const optionsHash = computeOptionsHash(conceptIdList, locale, resolvedOptions);
```
The completed DailyPodcast already stores `questionIds: conceptIdList` AND `optionsHash`. So the screen can recompute its hash over `selected.questionIds` (the SERVICE-resolved list) instead of `todayConceptIds` (the screen's divergent SM-2+planner list).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure podcast-view-model leaf module + behavioral tests (RED→GREEN)</name>
  <files>app/src/services/podcast-view-model.ts, app/tests/services/podcast-view-model.test.mjs</files>
  <behavior>
    Three pure functions, no React / no Audio / no IndexedDB / no i18n imports (leaf-module rule — mirror podcast-prompt.ts header).

    1. deriveSelectedPodcast({ selectedId, podcasts, todayPodcast }): DailyPodcast | null
       - If selectedId set AND found in podcasts → that podcast (History selection wins).
       - Else → todayPodcast ?? null.  (CRITICAL: NO podcasts[0] fallback in the main-player path. The History sub-view handles browsing old podcasts via explicit selectedId.)
       - Test: selectedId='old-1' present → returns old-1.
       - Test: selectedId=null, todayPodcast=undefined, podcasts=[staleReady] → returns null (NOT staleReady). This is the GAP-3 mutual-exclusion fix.
       - Test: selectedId=null, todayPodcast=todayReady → returns todayReady.

    2. isEmptyStateVisible({ todayPodcast }): boolean
       - true when !todayPodcast || status==='pending' || status==='failed'.
       - Test: undefined → true; pending → true; failed → true; ready → false; generating → false.

    3. isPlayerVisible({ selected }): boolean
       - true when selected != null && selected.status === 'ready'.
       - MUTUAL-EXCLUSION INVARIANT TEST (the GAP-3 missing: field): for the state {selectedId:null, todayPodcast:undefined, podcasts:[staleReady]} the derived `selected` is null, so isPlayerVisible===false AND isEmptyStateVisible===true → NOT both true. Assert `!(isPlayerVisible(...) && isEmptyStateVisible(...))` for this exact state.

    4. isDirty({ selected, currentHash }): boolean
       - true when selected?.optionsHash is set AND !== currentHash.
       - FRESH-PLAY missing: field test — model the fresh-generation reconciliation: build a podcast with questionIds=['c1','c2'] and optionsHash = computeOptionsHash(['c1','c2'],'en',{length:'standard',style:'conversational'}); recompute currentHash over THE SAME selected.questionIds with the same chips/locale; assert isDirty===false (no phantom Regenerate). Then change a chip and assert isDirty===true.
       - Import computeOptionsHash from ./podcast-prompt (allowed — it is also a pure leaf).
  </behavior>
  <action>
    Create `app/src/services/podcast-view-model.ts` as a PURE leaf module. Copy the leaf-module header style from `podcast-prompt.ts:1-28` (state explicitly: no JSON imports, no lib/date, no react-i18next, no Audio/IndexedDB — so `node --test` can import it and bundle-parity.test.mjs chain stays intact). Type-only import of DailyPodcast/PodcastOptions/SupportedLocale from `../types`. May import `computeOptionsHash` from `./podcast-prompt` (it is itself a leaf).

    Implement deriveSelectedPodcast, isEmptyStateVisible, isPlayerVisible, isDirty exactly per the behavior block. deriveSelectedPodcast MUST NOT contain a `podcasts[0]` fallback — that omission is the GAP-3/GAP-4 root-cause fix.

    Provide a small helper `computeCurrentHashForSelected(selected, locale, options)` that hashes over `selected.questionIds` (the SERVICE-resolved list) — this is what reconciles the screen hash with the service hash (GAP-4 isDirty loop fix). When selected is null, hash over `[]`.

    Write `app/tests/services/podcast-view-model.test.mjs` following the dynamic-import + node:test pattern of `podcast-options.test.mjs`. Cover every Test bullet above. These tests are the behavioral regression coverage the UAT `missing:` fields demand — they must exercise the imported functions, NOT regex source reads.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/podcast-view-model.test.mjs</automated>
  </verify>
  <done>All podcast-view-model.test.mjs tests pass. The module exports deriveSelectedPodcast, isEmptyStateVisible, isPlayerVisible, isDirty, computeCurrentHashForSelected. No podcasts[0] fallback anywhere in the module. grep confirms no forbidden imports: `grep -E "from '\.\./locales|from '\.\./lib/date|react-i18next|new Audio|indexedDB" app/src/services/podcast-view-model.ts` returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire PodcastScreen to the view-model + deterministic select-on-generate</name>
  <files>app/src/screens/PodcastScreen.tsx</files>
  <action>
    Import the new helpers: `import { deriveSelectedPodcast, isEmptyStateVisible, isPlayerVisible, computeCurrentHashForSelected } from '../services/podcast-view-model';`

    1. GAP-3/GAP-4 selection fix — replace the `selected` derivation (lines 104-106) with:
       `const selected = deriveSelectedPodcast({ selectedId, podcasts, todayPodcast });`
       This removes the `podcasts[0]` fallback for the MAIN view. (The All-Podcasts sub-view already sets `selectedId` explicitly on tap at line 397, so History still reaches old podcasts.)

    2. GAP-3 mutual exclusion — gate the player block (line 627) on `isPlayerVisible({ selected })` and the empty/generate block (line 773) on `isEmptyStateVisible({ todayPodcast })`. These are now derived from the same single source so they cannot both be true: when todayPodcast is undefined, selected is null → player hidden, empty shown. When todayPodcast is ready, selected is it → player shown, empty hidden. Update the chip Card gate (line 563) to `isEmptyStateVisible({ todayPodcast }) || isPlayerVisible({ selected })` so its meaning is unchanged but it reads from the same helpers (GAP-2 will move/collapse this card in plan 52-05 — leave its position here).

    3. GAP-4 deterministic select-on-generate — after a generation completes, the player must bind to the just-generated TODAY podcast, not a stale id. The `usePodcast` hook already updates `podcasts` on PODCAST_GENERATION_COMPLETED. Add a `useEffect` (CLAUDE.md no-refresh reactive re-read) subscribing to `PODCAST_GENERATION_COMPLETED` via `eventBus` that calls `setSelectedId(e.payload.id)` ONLY when `e.payload.date === today()`. This guarantees the audio-wiring effect (lines 200-239) re-runs with selected.id === the new podcast id, so `getAudioPath(selected.id)` returns the fresh blob and play no longer hits the `!audio` branch. Reuse the existing PODCAST_GENERATION_COMPLETED signal — do NOT introduce a new event type (CLAUDE.md one-signal-per-event).

    4. GAP-4 isDirty reconciliation — replace the currentHash useMemo (lines 113-122) body so it hashes over `selected?.questionIds ?? []` (the SERVICE-resolved list stored on the completed podcast) instead of `todayConceptIds`. Use `computeCurrentHashForSelected(selected, getCurrentLocale() as SupportedLocale, { length: selectedLength, style: selectedStyle })`. Keep `isDirty = !!cachedHash && cachedHash !== currentHash`. Now a freshly generated, unchanged-chips podcast yields isDirty===false because both sides hash over the identical id list. Note: when the user actively changes a chip, the recomputed hash diverges and the Regenerate CTA correctly appears.

    5. The Regenerate (line 728) and Generate (line 790) call sites currently pass `todayConceptIds`. Keep passing `todayConceptIds` to generatePodcast (the service re-resolves and echoes the list back onto the completed podcast — Task 3 ensures the screen reads back from selected.questionIds, closing the loop). Do NOT change the generate args.

    Preserve all existing inline-style + CSS-variable conventions. Do not touch the audio-wiring effect body, handlePlayPause, or the playback-rate button.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && node --test tests/screens/PodcastScreen.options.test.mjs tests/screens/PodcastScreen.routeFilter.test.mjs</automated>
  </verify>
  <done>tsc clean. PodcastScreen no longer contains the `podcasts[0]` fallback (`grep -n "podcasts\[0\]" app/src/screens/PodcastScreen.tsx` returns nothing). Player/empty gates read from isPlayerVisible/isEmptyStateVisible. A PODCAST_GENERATION_COMPLETED subscriber sets selectedId to the today podcast id. currentHash is computed over selected.questionIds. Existing PodcastScreen.options + routeFilter tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Service echoes the resolved concept-id list (close the hash loop)</name>
  <files>app/src/services/podcast.service.ts</files>
  <action>
    The completed DailyPodcast already persists `questionIds: conceptIdList` (line 282) and `optionsHash` (line 289) — these are computed over the SERVICE-resolved question set (including the getRecent(5) fallback at lines 173-176). Verify this is intact and that `optionsHash` is computed from the SAME `conceptIdList` that is written to `questionIds`. They already are (lines 178-179, 282, 289) — confirm and do NOT regress.

    The only required change: ensure the `pod` shell written at the START of generation (lines 197-205, status 'generating') ALSO carries `questionIds: conceptIdList`. It already does (line 200). Confirm the GENERATING-state object does not carry a stale optionsHash from a prior options set — it must NOT set optionsHash on the generating shell (it is only set on the completed object at line 289), so a mid-flight podcast never reports a misleading hash. This is already the case; add a one-line comment at line 197 documenting that questionIds on both the generating shell and the completed object MUST be the same conceptIdList so PodcastScreen's currentHash (computed over selected.questionIds) reconciles with optionsHash. No behavioral change if already correct.

    Add a guard test reference: the invariant is "completed.optionsHash === computeOptionsHash(completed.questionIds, locale, completed.options)". This is structurally guaranteed by lines 178-179/282/288-289 — do not refactor it apart.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/podcast-options.test.mjs && grep -c "questionIds: conceptIdList" app/src/services/podcast.service.ts</automated>
  </verify>
  <done>podcast-options.test.mjs passes. `grep -c "questionIds: conceptIdList" app/src/services/podcast.service.ts` returns ≥2 (generating shell + completed object both use the same list). optionsHash is computed once over conceptIdList and reused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB blob → Audio element | Audio blob URL fetched from IDB and bound to a new Audio(); local-origin only, no untrusted input |
| No new external input | This plan adds no network/provider calls; pure client-side state-derivation fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Tampering | podcast-view-model selection logic | mitigate | Pure functions with unit tests pin the selection + gate behavior; no podcasts[0] fallback prevents binding to wrong blob |
| T-52-04-02 | Information disclosure | None | accept | No PII, no provider tokens touched; local-first state only |
| T-52-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run from `app/`:
```bash
node --test tests/services/podcast-view-model.test.mjs
node --test tests/services/podcast-options.test.mjs
node --test tests/screens/PodcastScreen.options.test.mjs tests/screens/PodcastScreen.routeFilter.test.mjs
npx tsc -b --noEmit
npm test
```
- New view-model behavioral tests pass (mutual exclusion, fresh-play binding, isDirty===false).
- No `podcasts[0]` fallback remains in PodcastScreen.tsx.
- Full suite stays green (was 1406/1406 pre-UAT; this plan adds tests, fixes none-breaking).
</verification>

<success_criteria>
- GAP-4 closed: deriveSelectedPodcast + select-on-generate ensure audioRef binds to the freshly generated today podcast; isDirty reconciliation removes the phantom permanent-dirty loop.
- GAP-3 closed: isPlayerVisible and isEmptyStateVisible are mutually exclusive on a single derived source; player only shows TODAY's podcast; old podcasts reached via History.
- Behavioral regression tests exist on the LIVE path (extracted pure helpers), not regex source-reads.
- `npm test` green; `tsc -b --noEmit` clean.
</success_criteria>

<output>
Create `.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-04-SUMMARY.md` when done.
</output>
