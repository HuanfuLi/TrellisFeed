---
phase: 52-podcast-quality-defaults-and-learner-controls
plan: 05
type: execute
wave: 2
gap_closure: true
depends_on:
  - 52-04
  - 52-06
files_modified:
  - app/src/types/index.ts
  - app/src/services/podcast-prompt.ts
  - app/src/screens/PodcastScreen.tsx
  - app/src/screens/settings/SettingsFeaturesScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/services/podcast-prompt.test.mjs
  - app/tests/services/podcast-options.test.mjs
  - app/tests/screens/PodcastScreen.options.test.mjs
autonomous: true
requirements:
  - PODCAST-02   # bounded length/style controls — operator revises D-01 (4→3 lengths) during UAT
user_setup: []

must_haves:
  truths:
    - "The Length × Style config panel reads cleanly without chip overflow (3 lengths × 3 styles)."
    - "The 'brief' length is removed everywhere (type, LENGTH_MAP, i18n, settings dropdown, tests)."
    - "The style label 'Review Drill' is renamed to 'Review' (display label only — the enum value 'review' is unchanged)."
    - "The config panel is a collapsible section, collapsed by default, positioned below the player / no-podcast panel and above Knowledge Today."
  artifacts:
    - path: "app/src/types/index.ts"
      provides: "PodcastLength = 'standard' | 'deep' | 'extended' (brief dropped)."
      contains: "export type PodcastLength"
    - path: "app/src/services/podcast-prompt.ts"
      provides: "LENGTH_MAP without the 'brief' entry."
      contains: "LENGTH_MAP"
    - path: "app/src/screens/PodcastScreen.tsx"
      provides: "LENGTH_CHIPS without 'brief'; collapsible config section repositioned below player/empty-state."
      contains: "LENGTH_CHIPS"
  key_links:
    - from: "app/src/screens/PodcastScreen.tsx LENGTH_CHIPS"
      to: "app/src/types/index.ts PodcastLength"
      via: "satisfies readonly PodcastLength[]"
      pattern: "satisfies readonly PodcastLength"
    - from: "app/src/locales/{en,zh,es,ja}.json"
      to: "bundle-parity.test.mjs"
      via: "identical key sets after removing podcast.options.brief in all four"
      pattern: "options"
---

<objective>
Close GAP-1 (minor, operator design revision) and GAP-2 (minor, operator design revision) from the Phase 52 UAT.

GAP-1: LENGTH (4 chips) + STYLE (3 chips) wrap to two lines. Operator-directed fix: REMOVE the `brief` length (leaving standard / deep / extended — 3 lengths) and rename the style DISPLAY label "Review Drill" → "Review" (the enum value stays `review`). This REVISES locked decision D-01 (operator chose 4 lengths over researcher's 3); the operator re-decided during UAT and is the decision authority. Default length stays `standard`.

GAP-2: The Length × Style config panel should become an expandable section, COLLAPSED by default (length/style is a low-frequency action), and REPOSITIONED below the podcast player / "no podcast yet" panel and above the Knowledge Today panel (it currently sits at the top above the player).

Purpose: tidy the podcast screen per operator UAT direction.

Output:
- `brief` removed from PodcastLength type, podcast-prompt.ts LENGTH_MAP, PodcastScreen LENGTH_CHIPS, SettingsFeaturesScreen length dropdown, and the `podcast.options.brief` key in all 4 locales.
- "Review Drill" → "Review" label value in all 4 locales (key `podcast.options.review` unchanged).
- Wave-0 tests updated: prompt test (drop brief case + assertion → 3×3=9 combos), options test, screen test REQUIRED_OPTION_KEYS (drop 'brief').
- PodcastScreen config card converted to a collapsed-by-default expandable section repositioned below the player/empty-state, above Knowledge Today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UAT.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UI-SPEC.md

# Files edited (read fully — note 52-04 will have already rewired the gates here)
@app/src/screens/PodcastScreen.tsx
@app/src/services/podcast-prompt.ts
@app/src/screens/settings/SettingsFeaturesScreen.tsx

# Tests that hard-code the 4-length matrix and the brief key — MUST be updated in lockstep
@app/tests/services/podcast-prompt.test.mjs
@app/tests/screens/PodcastScreen.options.test.mjs

<interfaces>
<!-- Current state the executor must change. -->

PodcastLength (types/index.ts:207) — CHANGE TO drop 'brief':
```
export type PodcastLength = 'brief' | 'standard' | 'deep' | 'extended';   // → 'standard' | 'deep' | 'extended'
```

LENGTH_CHIPS (PodcastScreen.tsx:22) — drop 'brief':
```
const LENGTH_CHIPS = ['brief', 'standard', 'deep', 'extended'] as const satisfies readonly PodcastLength[];
```

LENGTH_MAP (podcast-prompt.ts:47-56) — drop the 'brief' entry (keep standard/deep/extended). Record<PodcastLength,string> will tsc-enforce the exact 3 keys after the type change.

SettingsFeaturesScreen.tsx:82 — drop the brief option:
```
{ value: 'brief', label: t('podcast.options.brief') },   // ← remove
```

Locale podcast.options (all 4) — REMOVE the "brief" key, RENAME "review" value:
- en: "review": "Review Drill" → "Review"
- zh: "review": "复习训练" → "复习"
- es: "review": "Repaso intensivo" → "Repaso"
- ja: "review": "復習特訓" → "復習"
(Keep all other keys. The `brief` key is removed from all 4 simultaneously so bundle-parity stays green.)

Tests asserting the 4-length matrix (MUST update):
- podcast-prompt.test.mjs:61 `const LENGTHS = ['brief', 'standard', 'deep', 'extended'];` → drop 'brief'
- podcast-prompt.test.mjs:89-93 the `it('brief contains 150 words target', ...)` block → DELETE
- podcast-prompt.test.mjs:119 `buildPodcastPrompt(lines, { length: 'brief', ... })` → change to a surviving length (e.g. 'standard')
- podcast-prompt.test.mjs:14 header comment "4 lengths × 3 styles (12 cases)" → "3 lengths × 3 styles (9 cases)"
- podcast-options.test.mjs:47 `computeOptionsHash([], 'en', { length: 'brief', ... })` → change to a surviving length (e.g. 'standard')
- PodcastScreen.options.test.mjs:96-107 REQUIRED_OPTION_KEYS array → drop 'brief'

Current config-panel JSX in PodcastScreen.tsx is the Card at lines 558-624 (gated, after 52-04, on `isEmptyStateVisible(...) || isPlayerVisible(...)`). The player Card is lines 627-770; the empty/generate Card is lines 773-795; the generating Card is 797-805; Knowledge Today starts at line 807.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop 'brief' from type + prompt + settings + chips, rename Review label across 4 locales</name>
  <files>app/src/types/index.ts, app/src/services/podcast-prompt.ts, app/src/screens/settings/SettingsFeaturesScreen.tsx, app/src/screens/PodcastScreen.tsx, app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json</files>
  <action>
    Implements GAP-1 (operator revision of D-01). Default length stays 'standard' — confirm `settings.podcast.defaultLength ?? 'standard'` fallbacks remain correct (they are).

    1. types/index.ts:207 — `export type PodcastLength = 'standard' | 'deep' | 'extended';`. Update the doc comment from "four bounded length presets" to "three bounded length presets (Phase 52 UAT GAP-1: operator dropped 'brief')".
    2. podcast-prompt.ts — delete the `'brief': '...'` entry from LENGTH_MAP (lines 48-49). Keep standard/deep/extended. The `Record<PodcastLength, string>` type now requires exactly the 3 surviving keys — tsc enforces no leftover/missing key.
    3. SettingsFeaturesScreen.tsx — remove the `{ value: 'brief', label: t('podcast.options.brief') }` option (line 82). Leave standard/deep/extended.
    4. PodcastScreen.tsx — `LENGTH_CHIPS = ['standard', 'deep', 'extended'] as const satisfies readonly PodcastLength[];` (line 22).
    5. All 4 locale bundles — in `podcast.options`: DELETE the `"brief"` key entirely; CHANGE the `"review"` VALUE only (key stays `review`): en "Review", zh "复习", es "Repaso", ja "復習". Do NOT translate/alter any other key. The `brief` key must be removed from all 4 in this same task so `bundle-parity.test.mjs` never sees divergent key sets. Do NOT touch settings.fields.* keys.

    Honor CLAUDE.md i18n rule: all 4 bundles change in the same plan/PR. en.json is canonical; the zh/es/ja "Review" equivalents above are the human-reviewed values (short single words, fit narrow chips). Proper nouns unaffected.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs</automated>
  </verify>
  <done>tsc clean. PodcastLength has exactly 3 members. LENGTH_MAP has 3 entries. No 'brief' in SettingsFeaturesScreen or LENGTH_CHIPS. bundle-parity + missing-key pass. Verify locales: no `podcast.options.brief` in any of the 4 bundles, and `podcast.options.review` value is the renamed short label in all 4.</done>
</task>

<task type="auto">
  <name>Task 2: Update Wave-0 tests to the 3-length matrix (drop brief assertions)</name>
  <files>app/tests/services/podcast-prompt.test.mjs, app/tests/services/podcast-options.test.mjs, app/tests/screens/PodcastScreen.options.test.mjs</files>
  <action>
    These tests are the Wave-0 source-read/behavioral guards that hard-code the old 4-length matrix. They MUST be updated in lockstep with Task 1 or they go red. Per CLAUDE.md best-practice #2, tests guard the LIVE path — so they must reflect the new 3-length reality.

    1. podcast-prompt.test.mjs:
       - Line 61: `const LENGTHS = ['standard', 'deep', 'extended'];` (drop 'brief'). The 3×3 for-loop now produces 9 combos.
       - Lines 89-93: DELETE the entire `it('brief contains 150 words target', ...)` block.
       - Line 119: change `{ length: 'brief', style: 'review' }` → `{ length: 'standard', style: 'review' }` (the coverage substring test does not depend on length).
       - Line 14 header comment: "4 lengths × 3 styles (12 cases)" → "3 lengths × 3 styles (9 cases)".
    2. podcast-options.test.mjs:
       - Line 47: `computeOptionsHash([], 'en', { length: 'brief', style: 'focused' })` → use `{ length: 'standard', style: 'focused' }` and ensure the paired line 48 still differs (`{ length: 'deep', ... }`) so the not-equal assertion holds. (standard vs deep differ → assertion intact.)
    3. PodcastScreen.options.test.mjs:
       - Lines 96-107 REQUIRED_OPTION_KEYS: remove `'brief',`. Keep lengthLabel, standard, deep, extended, styleLabel, focused, conversational, review, regenerateWithNew.

    Do NOT weaken any assertion beyond removing the dropped 'brief' references. Do NOT add `== 0` grep gates.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/podcast-prompt.test.mjs tests/services/podcast-options.test.mjs tests/screens/PodcastScreen.options.test.mjs</automated>
  </verify>
  <done>All three test files pass with no 'brief' references remaining: `grep -rn "'brief'\|\"brief\"\|length: 'brief'" app/tests/services/podcast-prompt.test.mjs app/tests/services/podcast-options.test.mjs app/tests/screens/PodcastScreen.options.test.mjs` returns nothing. Prompt matrix is 9 combos.</done>
</task>

<task type="auto">
  <name>Task 3: Collapsible config section, repositioned below player/empty-state, above Knowledge Today</name>
  <files>app/src/screens/PodcastScreen.tsx</files>
  <action>
    Implements GAP-2. Operate on the config Card currently at lines 558-624 (after 52-04 it is gated on `isEmptyStateVisible({ todayPodcast }) || isPlayerVisible({ selected })`).

    1. Add collapse state: `const [showConfig, setShowConfig] = useState(false);` (collapsed by default — low-frequency action). Place near the other useState declarations (~line 44).
    2. Convert the Card into an expandable section: a header row with the section label (reuse `t('podcast.options.lengthLabel')` is wrong here — instead add a single section header). Add ONE new i18n key for the section heading: `podcast.options.configHeading` = "Length & style" (en) + translations: zh "长度与风格", es "Duración y estilo", ja "長さとスタイル", added to all 4 bundles in podcast.options. The header is a button that toggles `showConfig` with a chevron affordance (use the already-imported `ChevronRight` rotated, or import `ChevronDown` from lucide-react — follow the existing collapsible/inline-style patterns; inline styles + CSS variables only, no Tailwind). When collapsed, render only the header; when expanded, render the existing Length + Style chip groups below it.
    3. REPOSITION: move this entire config section so it renders AFTER the player Card (currently ends line 770) and the empty/generate Card (ends line 795) and the generating Card (ends 805), and BEFORE the Knowledge Today block (starts line 807). Keep the same gate condition (`isEmptyStateVisible(...) || isPlayerVisible(...)`) so it appears in the same states, just lower on the page. Do not duplicate it — there must be exactly one config section in the JSX.
    4. The chip onClick handlers, selectedLength/selectedStyle state, the LENGTH_CHIPS/STYLE_CHIPS maps, and the isDirty/Regenerate wiring from 52-04 are unchanged — only the wrapping/position/collapse changes.

    Honor CLAUDE.md no-refresh + always-mounted-screen rules: showConfig is ephemeral local UI state, no service read needed. Honor `feedback_tile_simplicity_preference` spirit — fewer always-visible controls, cleaner hierarchy. Inline styles with CSS variables (`--surface`, `--surface-variant`, `--border`, `--radius-xl`, `--muted-foreground`).

    Add the `configHeading` key to all 4 locale bundles in this task (en/zh/es/ja) to keep bundle-parity green. Update PodcastScreen.options.test.mjs REQUIRED_OPTION_KEYS to include `'configHeading'`? — NO, leave the test list as the GAP-1 set; add a small dedicated source-read assertion instead in Task 2's file is already closed. Simplest: since adding a new required key would diverge from the test, just add `configHeading` to all 4 bundles (bundle-parity asserts parity, not membership of a fixed list) — that keeps bundle-parity green without touching the test. Confirm bundle-parity passes after adding it to all 4.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && node --test tests/locales/bundle-parity.test.mjs tests/screens/PodcastScreen.options.test.mjs tests/screens/PodcastScreen.routeFilter.test.mjs</automated>
  </verify>
  <done>tsc clean. PodcastScreen has a `showConfig` state defaulting to false, a toggle header, and the chip groups render only when expanded. The config section JSX is positioned after the player/empty/generating cards and before Knowledge Today (verify by source order: the `showConfig` block's index in the file is greater than the empty-state Card index and less than the Knowledge Today `BookOpen` heading index). `podcast.options.configHeading` exists in all 4 bundles. bundle-parity + PodcastScreen tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM prompt assembly (podcast-prompt.ts) | LENGTH_MAP strings feed the system prompt; static, no untrusted input |
| No new external input | Pure UI/label/enum changes; no network calls added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Tampering | PodcastLength enum + LENGTH_MAP | mitigate | tsc Record<PodcastLength,string> enforces exact key set; prompt test 9-combo matrix pins coverage constraint survives the drop |
| T-52-05-02 | Denial of service | i18n bundle parity | mitigate | bundle-parity.test.mjs blocks divergent key sets; brief removed + configHeading added across all 4 simultaneously |
| T-52-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Run from `app/`:
```bash
npx tsc -b --noEmit
node --test tests/services/podcast-prompt.test.mjs tests/services/podcast-options.test.mjs
node --test tests/screens/PodcastScreen.options.test.mjs tests/screens/PodcastScreen.routeFilter.test.mjs
node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs
npm test
```
- 'brief' fully removed; PodcastLength has 3 members; LENGTH_MAP has 3 entries.
- "Review Drill" → "Review" in all 4 locales; key `review` unchanged.
- Config section is collapsible (collapsed default) and repositioned below player/empty-state, above Knowledge Today.
- Full suite green.
</verification>

<success_criteria>
- GAP-1 closed: 3 lengths × 3 styles, no chip overflow; "Review" label; D-01 revision applied everywhere (type, prompt, settings, chips, locales, tests).
- GAP-2 closed: collapsible config section, collapsed by default, repositioned below the player/no-podcast panel and above Knowledge Today.
- `npm test` green; `tsc -b --noEmit` clean; bundle-parity green.
</success_criteria>

<output>
Create `.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-05-SUMMARY.md` when done.
</output>
