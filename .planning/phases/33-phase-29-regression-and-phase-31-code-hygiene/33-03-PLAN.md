---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 03
type: execute
wave: 2
depends_on: [33-01, 33-02]
files_modified:
  - app/src/services/trellis-state.service.ts
  - app/src/services/concept-feed.service.ts
  - app/src/screens/PlannerScreen.tsx
  - app/src/components/trellis/TrellisStatusPanel.tsx
  - app/src/components/trellis/TrellisLeaf.tsx
  - app/tests/services/trellis-state.test.mjs
  - app/tests/components/trellis-tooltip-copy.test.mjs
  - app/tests/services/concept-batch-filter.test.mjs
autonomous: true
requirements: [TD-06]
must_haves:
  truths:
    - "LeafState type union reads 'bud | green | dying | falling | dead | blossom | fruit' — 'yellow' and 'fallen' no longer members"
    - "'falling' literal retained in the type union (per D-12 — internal-only gradation, UI never exposes it)"
    - "computeLeafState returns 'dying' for 1d overdue input and 'dead' for 14d overdue / ease<1.5 input"
    - "concept-feed.service.ts weak-concept predicate reads 'leaf === dying || leaf === falling || leaf === dead' — previously dead 'dying' literal (pre-rename: 'yellow') is now valid under the new union"
    - "TrellisLeaf.tsx STATE_COLOR and STATE_VEIN Records have renamed keys: dying and dead replace yellow and fallen (4 keys total)"
    - "tsc -b --noEmit exit 0 after the rename"
    - "Badge.tsx remains untouched — 'yellow' there is a CSS color name, not LeafState"
    - "PodcastScreen.tsx:220 remains untouched — 'yellow' there is a UI status color, not LeafState"
    - "Single atomic commit contains the rename — git log --follow traces cleanly"
  artifacts:
    - path: "app/src/services/trellis-state.service.ts"
      provides: "LeafState type union with renamed literals; computeLeafState returning renamed literals; dev-mode seeds updated"
      must_contain: "'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit'"
      must_not_contain: "'yellow'"
    - path: "app/src/components/trellis/TrellisLeaf.tsx"
      provides: "STATE_COLOR and STATE_VEIN Records keyed by new LeafState names"
      must_contain: "dying:"
      must_contain_second: "dead:"
  key_links:
    - from: "app/src/services/concept-feed.service.ts leaf === 'dying' predicate"
      to: "trellis-state.service.ts LeafState type"
      via: "TypeScript type-checking"
      pattern: "leaf === 'dying' \\|\\| leaf === 'falling' \\|\\| leaf === 'dead'"
---

<objective>
Unify the internal LeafState vocabulary with the user-facing design vocabulary. Per D-10/D-11/D-12/D-13/D-15:

- Rename `'yellow'` → `'dying'` across the entire codebase's LeafState contexts.
- Rename `'fallen'` → `'dead'` across the entire codebase's LeafState contexts.
- KEEP `'falling'` as-is (internal-only 7-13d-overdue gradation; UI never exposes it).
- Use ONE atomic commit per D-15 so `git log --follow` traces the rename cleanly.

**Scope boundaries:**
- DO NOT touch `app/src/components/ui/Badge.tsx` — `'yellow'` there is a CSS color palette member (`type BadgeColor`), not a LeafState.
- DO NOT touch `app/src/screens/PodcastScreen.tsx:220` — `'yellow'` there is a UI status color returned by `statusColor`, not a LeafState.
- DO NOT mix this commit with Plan 33-01 (TD-05) or 33-02 (TD-04) work.
- DO include code COMMENTS in the rename (prevents future grep confusion).
- **EXPLICITLY OUT OF SCOPE — `app/src/components/trellis/TrellisTooltip.tsx`** does not exist in the working tree (filesystem-verified 2026-04-19). The pinned test `app/tests/components/trellis-tooltip-copy.test.mjs` imports from that non-existent path and has been failing with `ERR_MODULE_NOT_FOUND` since BEFORE Phase 33. Restoring `TrellisTooltip.tsx` (or re-pointing the test at a different component) is a pre-existing defect NOT in Phase 33's scope — it is deferred to v1.5 planning (mirrors the D-07/D-08 deferral pattern). This plan renames the test-file arguments for symmetry with the new LeafState vocabulary, but the test will continue to fail at `ERR_MODULE_NOT_FOUND` after the rename. That is expected and NOT a rename regression.

**Locator strategy (per revision):** This plan uses `grep -n` to locate every edit site dynamically rather than relying on fixed line numbers. Line numbers cited below are APPROXIMATIONS from the planner-time baseline — the executor MUST grep-verify each site before editing because subsequent commits may shift line numbers.

Purpose: D-06 via the LeafState rename — TD-06 dead-branch tsc error (originally at concept-feed.service.ts:745, pre-renamed to `'yellow'` in commit 760fa4f8, now at line ~655 per re-baseline) becomes valid automatically once `'dying'` enters the type union.
Output: one atomic commit; `tsc -b --noEmit` stays exit 0; `grep "'yellow'" app/src/services/trellis-state.service.ts` returns 0; pre-existing Node-25 trellis test failures stay unchanged (no new trellis regression); test failure set stays within the pre-rename baseline signatures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@app/src/services/trellis-state.service.ts
@app/src/components/trellis/TrellisLeaf.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Rename LeafState type union + computeLeafState + dev-mode seeds in trellis-state.service.ts</name>
  <read_first>
    - app/src/services/trellis-state.service.ts (entire file — small; read fully)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #1 (exact line-by-line diff expected)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Code Example 1 (before/after snippet)
  </read_first>
  <action>
    Step 0 — Grep-locate every edit site in this file before editing:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'\|'fallen'" app/src/services/trellis-state.service.ts
    ```
    Expected: 6-8 hits (type union + 2 computeLeafState returns + 3-5 dev-seed literals). Record the actual line numbers printed; the line numbers below are approximations.

    Open `app/src/services/trellis-state.service.ts` and apply these precise edits via content-based match (not line-number-based):

    Edit A — the type union declaration (approx line 9):
    - BEFORE: `export type LeafState = 'bud' | 'green' | 'yellow' | 'falling' | 'fallen' | 'blossom' | 'fruit';`
    - AFTER:  `export type LeafState = 'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit';`

    Edit B — computeLeafState return value for 14d overdue / ease<1.5 (approx line 88):
    - BEFORE: `if (maxOverdue >= 14 || aggregateEase < 1.5) return 'fallen';`
    - AFTER:  `if (maxOverdue >= 14 || aggregateEase < 1.5) return 'dead';`

    Edit C — computeLeafState return value for 1d overdue (approx line 90):
    - BEFORE: `if (maxOverdue >= 1) return 'yellow';`
    - AFTER:  `if (maxOverdue >= 1) return 'dying';`

    Edit D — dev-mode seed entries (approx lines 113-121). Every occurrence of `state: 'yellow'` becomes `state: 'dying'`; every `state: 'fallen'` becomes `state: 'dead'`. Do NOT touch `state: 'falling'` (retained per D-12). Do NOT touch `state: 'green'`, `'bud'`, `'blossom'`, `'fruit'`.

    Use the `Edit` tool with each literal replacement as a SEPARATE call. Because `'yellow'` and `'fallen'` only appear in LeafState contexts in this file (Step 0 grep confirms), global replace is safe — but make edits one string at a time and re-verify with `grep -n` after each.

    Update any comments that reference the old vocabulary in this file for alignment with code (e.g., if there's a comment mentioning "yellow" or "fallen" as LeafState states, update to "dying" / "dead"). Scan the whole file for any such comments and update.

    Grep-verify post-edit:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'" app/src/services/trellis-state.service.ts
    # must return nothing
    grep -n "'fallen'" app/src/services/trellis-state.service.ts
    # must return nothing
    grep -n "'dying'" app/src/services/trellis-state.service.ts
    # must return at least 3 hits (union + computeLeafState + >=1 seed)
    grep -n "'dead'" app/src/services/trellis-state.service.ts
    # must return at least 2 hits (union + computeLeafState + maybe seed)
    grep -n "'falling'" app/src/services/trellis-state.service.ts
    # must still return at least 2 hits (retained per D-12)
    ```

    Do NOT commit — Task 3.5 makes the single atomic commit covering all rename touchpoints.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "'yellow'\|'fallen'" app/src/services/trellis-state.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'yellow'" app/src/services/trellis-state.service.ts` returns `0`.
    - `grep -c "'fallen'" app/src/services/trellis-state.service.ts` returns `0`.
    - `grep -c "'dying'" app/src/services/trellis-state.service.ts` returns at least `3`.
    - `grep -c "'dead'" app/src/services/trellis-state.service.ts` returns at least `2`.
    - `grep -c "'falling'" app/src/services/trellis-state.service.ts` returns at least `2` (retained).
    - Type union line exactly reads: `export type LeafState = 'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit';`
  </acceptance_criteria>
  <done>trellis-state.service.ts renamed; downstream consumers will be updated next.</done>
</task>

<task type="auto">
  <name>Task 3.2: Update concept-feed.service.ts and PlannerScreen.tsx LeafState string comparisons</name>
  <read_first>
    - app/src/services/concept-feed.service.ts (locate the single LeafState comparison via grep — approximate line 655 post-760fa4f8)
    - app/src/screens/PlannerScreen.tsx (locate lines ~46-47 with `leafState === 'fallen'` and `leafState === 'yellow' || leafState === 'falling'` via grep)
  </read_first>
  <action>
    Step 1 — Grep-locate all edit sites in both files before editing:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'\|'fallen'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx
    ```
    Expected hits (at revision-time baseline):
    - `app/src/services/concept-feed.service.ts:655:        isImportant = leaf === 'yellow' || leaf === 'falling' || leaf === 'fallen';`
    - `app/src/screens/PlannerScreen.tsx:46:  const deadNodes = layout.nodes.filter((n) => n.leafState === 'fallen');`
    - `app/src/screens/PlannerScreen.tsx:47:  const dyingNodes = layout.nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');`

    Line numbers may shift after Plans 33-01 and 33-02 commits; use the current grep output as the source of truth, not the plan's approximations.

    Step 2 — In `app/src/services/concept-feed.service.ts`, replace the predicate line (content-based match):
    - BEFORE: `isImportant = leaf === 'yellow' || leaf === 'falling' || leaf === 'fallen';`
    - AFTER:  `isImportant = leaf === 'dying' || leaf === 'falling' || leaf === 'dead';`

    Note: this file previously triggered TD-06's TS2367 (dead `'dying'` branch); post-rename the literal is valid. `'falling'` stays unchanged.

    Step 3 — In `app/src/screens/PlannerScreen.tsx`, replace the two filter predicates (content-based match):
    - BEFORE: `const deadNodes = layout.nodes.filter((n) => n.leafState === 'fallen');`
    - AFTER:  `const deadNodes = layout.nodes.filter((n) => n.leafState === 'dead');`
    - BEFORE: `const dyingNodes = layout.nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');`
    - AFTER:  `const dyingNodes = layout.nodes.filter((n) => n.leafState === 'dying' || n.leafState === 'falling');`

    Step 4 — Grep-verify:
    ```
    cd /Users/Code/EchoLearn
    grep -n "leaf === 'yellow'\|leaf === 'fallen'\|leafState === 'yellow'\|leafState === 'fallen'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx
    # must return zero hits

    grep -n "leaf === 'dying'\|leafState === 'dying'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx
    # must return at least 2 hits
    ```

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "'yellow'\|'fallen'" app/src/services/concept-feed.service.ts app/src/screens/PlannerScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'yellow'" /Users/Code/EchoLearn/app/src/services/concept-feed.service.ts` returns `0`.
    - `grep -c "'fallen'" /Users/Code/EchoLearn/app/src/services/concept-feed.service.ts` returns `0`.
    - `grep -c "leafState === 'yellow'\|leafState === 'fallen'" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx` returns `0`.
    - `grep -c "leafState === 'dying'\|leafState === 'dead'" /Users/Code/EchoLearn/app/src/screens/PlannerScreen.tsx` returns at least `2`.
  </acceptance_criteria>
  <done>concept-feed.service.ts + PlannerScreen.tsx aligned with new LeafState vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3.3: Update TrellisStatusPanel.tsx and TrellisLeaf.tsx (and SKIP-if-absent branch for TrellisTooltip.tsx)</name>
  <read_first>
    - app/src/components/trellis/TrellisStatusPanel.tsx (first 60 lines — grep-locate dyingNodes/deadNodes predicates)
    - app/src/components/trellis/TrellisLeaf.tsx (STATE_COLOR/STATE_VEIN + withDecay section — grep-locate via `'yellow'`/`'fallen'`)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #1
  </read_first>
  <action>
    Step 0 — Grep-locate every edit site across the two trellis-component files before editing:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'\|'fallen'\|yellow:\|fallen:" app/src/components/trellis/TrellisStatusPanel.tsx app/src/components/trellis/TrellisLeaf.tsx
    ```
    Expected hits (at revision-time baseline) — use actual grep output as source of truth:
    - `TrellisStatusPanel.tsx:44: const dyingNodes = nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');`
    - `TrellisStatusPanel.tsx:45: const deadNodes = nodes.filter((n) => n.leafState === 'fallen');`
    - `TrellisLeaf.tsx:73: yellow: '#F9A825',` (STATE_COLOR Record key)
    - `TrellisLeaf.tsx:75: fallen: '#8D6E63',` (STATE_COLOR Record key)
    - `TrellisLeaf.tsx:83: yellow: '#F57F17',` (STATE_VEIN Record key)
    - `TrellisLeaf.tsx:85: fallen: '#5D4037',` (STATE_VEIN Record key)
    - `TrellisLeaf.tsx:516: function withDecay(leaf: React.ReactNode, state: 'yellow' | 'falling' | 'fallen'): React.ReactNode {`
    - `TrellisLeaf.tsx:517: const extraRotation = state === 'falling' ? 20 : state === 'fallen' ? -15 : 0;`
    - `TrellisLeaf.tsx:518: const opacity = state === 'fallen' ? 0.75 : 1;`
    - `TrellisLeaf.tsx:525: {state === 'fallen' && (`

    Line numbers may shift after earlier Plan-33 commits; use the current grep output as the source of truth.

    Step 1 — `app/src/components/trellis/TrellisStatusPanel.tsx`. Content-based edits:
    - BEFORE: `const dyingNodes = nodes.filter((n) => n.leafState === 'yellow' || n.leafState === 'falling');`
    - AFTER:  `const dyingNodes = nodes.filter((n) => n.leafState === 'dying' || n.leafState === 'falling');`
    - BEFORE: `const deadNodes = nodes.filter((n) => n.leafState === 'fallen');`
    - AFTER:  `const deadNodes = nodes.filter((n) => n.leafState === 'dead');`

    Step 2 — `app/src/components/trellis/TrellisLeaf.tsx`. Apply these content-based edits (approx lines in parens; USE ACTUAL GREP OUTPUT):

    - (approx L73, STATE_COLOR Record key): `yellow: '#F9A825',` → `dying: '#F9A825',`
    - (approx L75, STATE_COLOR Record key): `fallen: '#8D6E63',` → `dead: '#8D6E63',`
    - (approx L83, STATE_VEIN Record key): `yellow: '#F57F17',` → `dying: '#F57F17',`
    - (approx L85, STATE_VEIN Record key): `fallen: '#5D4037',` → `dead: '#5D4037',`
    - (approx L516, withDecay fn signature): `function withDecay(leaf: React.ReactNode, state: 'yellow' | 'falling' | 'fallen'): React.ReactNode {` → `function withDecay(leaf: React.ReactNode, state: 'dying' | 'falling' | 'dead'): React.ReactNode {`
    - (approx L517): `const extraRotation = state === 'falling' ? 20 : state === 'fallen' ? -15 : 0;` → `const extraRotation = state === 'falling' ? 20 : state === 'dead' ? -15 : 0;`
    - (approx L518): `const opacity = state === 'fallen' ? 0.75 : 1;` → `const opacity = state === 'dead' ? 0.75 : 1;`
    - (approx L525): `{state === 'fallen' && (` → `{state === 'dead' && (`

    Also update comments in the file referring to the old vocabulary so that comments stay aligned with code. Search for the text strings via grep and update wherever found:
    - `grep -n "Yellow/falling/fallen" app/src/components/trellis/TrellisLeaf.tsx` — if present, replace "Yellow/falling/fallen" with "Dying/falling/dead".
    - `grep -n "yellow, falling, fallen" app/src/components/trellis/TrellisLeaf.tsx` — if present, replace "yellow, falling, fallen" with "dying, falling, dead".

    Step 3 — `app/src/components/trellis/TrellisTooltip.tsx` — **SKIP-IF-ABSENT branch:**

    First verify whether the file exists in the working tree (filesystem-authoritative check):
    ```
    ls app/src/components/trellis/TrellisTooltip.tsx 2>&1
    ```

    Expected at revision time (2026-04-19): the `ls` command prints `ls: app/src/components/trellis/TrellisTooltip.tsx: No such file or directory`. In that case:

    - DO NOT create the file. It is explicitly out of scope for Phase 33 (see objective's Scope boundaries section).
    - DO NOT add it to the commit.
    - Document the fact in a single SKIP note to be included in Task 3.5's commit message body. Record this string for use later: `SKIP: trellis/TrellisTooltip.tsx absent from working tree — file restoration deferred to v1.5 per Phase 33 scope boundary`.
    - Continue directly to Step 4.

    If the `ls` command unexpectedly prints a real file path (the file was restored between revision-time and execute-time), THEN apply the originally-planned edits to that file:
    - Grep-locate: `grep -n "'yellow'\|'fallen'\|yellow:\|fallen:" app/src/components/trellis/TrellisTooltip.tsx`
    - For each `case 'yellow':` → `case 'dying':`
    - For each `case 'fallen':` → `case 'dead':`
    - For any object-literal keys `yellow: ...` / `fallen: ...` in LeafState-to-copy Records, rename to `dying` / `dead`.
    - Add the file to the atomic commit in Task 3.5 (also update the frontmatter `files_modified` list at that point).

    Step 4 — Global grep sweep to confirm no stray `'yellow'`/`'fallen'` remains in trellis code (excluding Badge.tsx and PodcastScreen.tsx which are intentionally untouched):

    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/src/components/trellis/ app/src/screens/PlannerScreen.tsx app/src/services/trellis-state.service.ts app/src/services/concept-feed.service.ts
    # must return zero hits
    ```

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/src/components/trellis/ app/src/screens/PlannerScreen.tsx app/src/services/trellis-state.service.ts app/src/services/concept-feed.service.ts | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Above grep sweep returns `0` total hits.
    - `grep -c "dying:" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx` returns at least `2` (STATE_COLOR + STATE_VEIN keys).
    - `grep -c "dead:" /Users/Code/EchoLearn/app/src/components/trellis/TrellisLeaf.tsx` returns at least `2`.
    - TrellisLeaf.tsx `withDecay` signature reads: `state: 'dying' | 'falling' | 'dead'`.
    - If `TrellisTooltip.tsx` is absent (the expected case at revision time), SKIP note recorded for the commit message body; no error.
  </acceptance_criteria>
  <done>Trellis components aligned; TrellisTooltip.tsx skipped (absent file, out of scope); awaiting test updates + atomic commit.</done>
</task>

<task type="auto">
  <name>Task 3.4: Update test fixtures — trellis-state.test.mjs, trellis-tooltip-copy.test.mjs, concept-batch-filter.test.mjs</name>
  <read_first>
    - app/tests/services/trellis-state.test.mjs (whole file — grep-locate `'yellow'`/`'fallen'` hits; approx lines 40-65 per RESEARCH.md Finding #1)
    - app/tests/components/trellis-tooltip-copy.test.mjs (whole file — grep-locate resolveHealthCopy args and the LeafState enumeration)
    - app/tests/services/concept-batch-filter.test.mjs (grep-locate the one comment referencing "dying/falling/fallen")
  </read_first>
  <action>
    Step 0 — Grep-locate every edit site before editing:
    ```
    cd /Users/Code/EchoLearn && grep -n "'yellow'\|'fallen'" app/tests/services/trellis-state.test.mjs app/tests/components/trellis-tooltip-copy.test.mjs && grep -n "dying/falling/fallen" app/tests/services/concept-batch-filter.test.mjs
    ```

    Step 1 — `app/tests/services/trellis-state.test.mjs` (content-based edits; approx line numbers are for orientation only):

    - test-name string (approx L44): `'computeLeafState returns yellow for 1-7 day overdue child'` → `'computeLeafState returns dying for 1-7 day overdue child'`
    - assertion (approx L49): `assert.equal(computeLeafState(anchor, [child]), 'yellow');` → `assert.equal(computeLeafState(anchor, [child]), 'dying');`
    - assertion (approx L58): `assert.equal(computeLeafState(anchor, [bad, good]), 'fallen');` → `assert.equal(computeLeafState(anchor, [bad, good]), 'dead');`
    - Scan ENTIRE file for any remaining `'yellow'` or `'fallen'` strings; rename each using content-based match.

    Step 2 — `app/tests/components/trellis-tooltip-copy.test.mjs` (content-based edits):

    - `assert.equal(mod.resolveHealthCopy('yellow', 2, 0), '...')` → `assert.equal(mod.resolveHealthCopy('dying', 2, 0), '...')` (preserve the third-position string literal — that's the pinned expected copy)
    - `assert.equal(mod.resolveHealthCopy('fallen', 3, 0), '...')` → `assert.equal(mod.resolveHealthCopy('dead', 3, 0), '...')`
    - LeafState enumeration: `const expected = ['bud', 'green', 'yellow', 'falling', 'fallen', 'blossom', 'fruit'];` → `const expected = ['bud', 'green', 'dying', 'falling', 'dead', 'blossom', 'fruit'];`

    **IMPORTANT — pre-existing failure note:** `trellis-tooltip-copy.test.mjs` imports from `app/src/components/trellis/TrellisTooltip.tsx`, which does NOT exist in the working tree (verified 2026-04-19). That test has been failing with `ERR_MODULE_NOT_FOUND` since BEFORE Phase 33. This rename is a symmetry follow-up to the LeafState vocabulary change; the test WILL CONTINUE to fail with the SAME `ERR_MODULE_NOT_FOUND` signature after the rename. The `ERR_MODULE_NOT_FOUND` signature is a pre-existing issue — NOT a rename regression. The Task 3.5 baseline-comparison logic enumerates failure signatures and treats this as expected, not new. Restoring the tooltip component (or re-pointing the test) is out of scope (see Plan 33-03 objective Scope boundaries).

    Step 3 — `app/tests/services/concept-batch-filter.test.mjs`:

    - Rationale/provenance: this test file ALREADY landed in commit `9486799a test(32.1-02): commit existing concept-batch + daily-generation-cap + starter-posts tests` (NOT in the Plan 33-00 WIP flush — the file was already tracked at the start of Phase 33). It is on disk and editable now; this comment-rename is a consistency follow-up to the LeafState vocabulary change.
    - Edit: locate the comment referencing `"ease < 1.5 or dying/falling/fallen"` via `grep -n "dying/falling/fallen" app/tests/services/concept-batch-filter.test.mjs` and replace it with `"ease < 1.5 or dying/falling/dead"`.

    Step 4 — Grep sweep across `app/tests/`:
    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/tests/
    # Any remaining hits should be investigated — expected: zero LeafState-context hits
    ```

    If any hit remains, inspect whether it's a genuine LeafState reference (rename) or incidental text (leave).

    Do NOT commit yet.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -rn "'yellow'\|'fallen'" app/tests/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'yellow'\\|'fallen'" /Users/Code/EchoLearn/app/tests/` returns `0` hits.
    - `grep -c "'dying'" /Users/Code/EchoLearn/app/tests/services/trellis-state.test.mjs` returns at least `1`.
    - `grep -c "'dead'" /Users/Code/EchoLearn/app/tests/services/trellis-state.test.mjs` returns at least `1`.
    - `grep -c "resolveHealthCopy('dying'" /Users/Code/EchoLearn/app/tests/components/trellis-tooltip-copy.test.mjs` returns at least `1`.
    - `grep -c "resolveHealthCopy('dead'" /Users/Code/EchoLearn/app/tests/components/trellis-tooltip-copy.test.mjs` returns at least `1`.
    - `grep "dying/falling/dead" /Users/Code/EchoLearn/app/tests/services/concept-batch-filter.test.mjs` returns a hit.
  </acceptance_criteria>
  <done>Test fixtures renamed; ready for verification + commit.</done>
</task>

<task type="auto">
  <name>Task 3.5: Capture pre-rename baseline, verify tsc + build, run post-rename test diff, then make the single atomic commit</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #8 (test baseline notes)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Pitfall #4 (pre-existing trellis import-attribute failures)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Pitfall #5 (TrellisTooltip.tsx note)
  </read_first>
  <action>
    Step 1 — Run tsc:
    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit
    ```
    Must exit 0 (no errors). If any TS2367 or type-incompatibility error surfaces, investigate — likely a missed LeafState callsite.

    Step 2 — Global grep sweep across the whole `app/` tree. Confirm only Badge.tsx and PodcastScreen.tsx still contain `'yellow'` (both intentional per D-13 scope boundaries):

    ```
    cd /Users/Code/EchoLearn && grep -rn "'yellow'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js"
    ```

    Expected output: ONLY hits in `app/src/components/ui/Badge.tsx` and `app/src/screens/PodcastScreen.tsx`. If any hit appears outside those two files, investigate and fix.

    ```
    cd /Users/Code/EchoLearn && grep -rn "'fallen'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js"
    ```

    Expected output: zero hits.

    ```
    cd /Users/Code/EchoLearn && grep -rn "'falling'" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" --include="*.js" | wc -l
    ```

    Expected: at least 3 (retained per D-12).

    Step 3 — **Capture pre-rename baseline, then run post-rename suite and diff (authoritative baseline-relative tolerance per revision 2):**

    First, if NOT already captured earlier in the plan, stash your working-tree rename changes and capture the pre-rename baseline:
    ```
    cd /Users/Code/EchoLearn && git stash push -m "phase-33-03-pre-rename-stash" -- \
      app/src/services/trellis-state.service.ts \
      app/src/services/concept-feed.service.ts \
      app/src/screens/PlannerScreen.tsx \
      app/src/components/trellis/TrellisStatusPanel.tsx \
      app/src/components/trellis/TrellisLeaf.tsx \
      app/tests/services/trellis-state.test.mjs \
      app/tests/components/trellis-tooltip-copy.test.mjs \
      app/tests/services/concept-batch-filter.test.mjs

    cd /Users/Code/EchoLearn/app && npm test 2>&1 | tee /tmp/phase-33-pre-rename-baseline.log
    FAIL_COUNT_BASELINE=$(grep -cE "code: 'ERR_|AssertionError \\[ERR_ASSERTION\\]" /tmp/phase-33-pre-rename-baseline.log)
    grep -oE "code: 'ERR_[A-Z_]+'|AssertionError \\[ERR_ASSERTION\\]" /tmp/phase-33-pre-rename-baseline.log | sort -u > /tmp/phase-33-baseline-signatures.txt
    echo "BASELINE_FAIL_COUNT=$FAIL_COUNT_BASELINE"
    cat /tmp/phase-33-baseline-signatures.txt

    cd /Users/Code/EchoLearn && git stash pop
    ```

    Record `$FAIL_COUNT_BASELINE` and the signature set. Typical revision-time baseline (observed 2026-04-19): 32 failures across 5 signatures — `ERR_IMPORT_ATTRIBUTE_MISSING`, `ERR_MODULE_NOT_FOUND`, `ERR_UNKNOWN_FILE_EXTENSION`, `ERR_ASSERTION`, `AssertionError [ERR_ASSERTION]`.

    Now run the post-rename suite:
    ```
    cd /Users/Code/EchoLearn/app && npm test 2>&1 | tee /tmp/phase-33-post-rename.log
    FAIL_COUNT_POST=$(grep -cE "code: 'ERR_|AssertionError \\[ERR_ASSERTION\\]" /tmp/phase-33-post-rename.log)
    grep -oE "code: 'ERR_[A-Z_]+'|AssertionError \\[ERR_ASSERTION\\]" /tmp/phase-33-post-rename.log | sort -u > /tmp/phase-33-post-rename-signatures.txt
    echo "POST_FAIL_COUNT=$FAIL_COUNT_POST"
    cat /tmp/phase-33-post-rename-signatures.txt
    ```

    **Authoritative tolerance (per revision 2) — baseline-relative, NOT a fixed numeric band:**

    - **PASS** if BOTH: `$FAIL_COUNT_POST ≤ $FAIL_COUNT_BASELINE` (no new failures introduced) AND every signature in `/tmp/phase-33-post-rename-signatures.txt` is ALSO present in `/tmp/phase-33-baseline-signatures.txt` (signature set is a subset — no new failure kinds).
    - **HALT** if EITHER: `$FAIL_COUNT_POST > $FAIL_COUNT_BASELINE` (new failure appeared) OR any signature in the post set is missing from the baseline set (new failure kind — rename missed a callsite).

    Diff check (one-liner):
    ```
    comm -23 /tmp/phase-33-post-rename-signatures.txt /tmp/phase-33-baseline-signatures.txt
    # must be empty — any output = NEW signature = HALT
    if [ "$FAIL_COUNT_POST" -gt "$FAIL_COUNT_BASELINE" ]; then echo "HALT: fail count rose from $FAIL_COUNT_BASELINE to $FAIL_COUNT_POST"; else echo "PASS: fail count $FAIL_COUNT_POST ≤ baseline $FAIL_COUNT_BASELINE"; fi
    ```

    Rationale: the pre-Phase-33 working tree has multiple orthogonal pre-existing failure signatures (Node-25 JSON import-attributes, a missing TrellisTooltip.tsx producing ERR_MODULE_NOT_FOUND, a missing podcast.service producing ERR_MODULE_NOT_FOUND, tsx-import extension errors, feed-strategy assertion failures). Pinning the tolerance to a single signature (`ERR_IMPORT_ATTRIBUTE_MISSING` only) would falsely HALT on pre-existing failures. The only correct PASS gate is "no new failures vs. captured baseline".

    Step 4 — Run vite build to confirm no build regression:
    ```
    cd /Users/Code/EchoLearn/app && npx vite build 2>&1 | tail -5
    ```
    Expected: clean build in ~3s.

    Step 5 — Stage + commit the 8 files atomically per D-15 (TrellisTooltip.tsx absent, per Task 3.3 SKIP branch):
    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/services/trellis-state.service.ts \
      app/src/services/concept-feed.service.ts \
      app/src/screens/PlannerScreen.tsx \
      app/src/components/trellis/TrellisStatusPanel.tsx \
      app/src/components/trellis/TrellisLeaf.tsx \
      app/tests/services/trellis-state.test.mjs \
      app/tests/components/trellis-tooltip-copy.test.mjs \
      app/tests/services/concept-batch-filter.test.mjs

    git commit -m "$(cat <<'EOF'
    refactor(trellis): rename LeafState literals yellow->dying, fallen->dead per design vocabulary (TD-06)

    Unify internal LeafState vocabulary with user-facing design labels.
    UI already used "Dying" / "Dead" (TrellisStatusPanel filters); the literal
    type members lagged.

    Per Phase 33 D-10/D-11/D-12/D-13/D-15:
    - 'yellow' -> 'dying' (1-6d overdue)
    - 'fallen' -> 'dead'  (14d+ overdue or ease<1.5)
    - 'falling' RETAINED — internal-only 7-13d gradation, UI never exposes it

    Edits (atomic per D-15):
    - trellis-state.service.ts: LeafState type union + computeLeafState return
      values + dev-mode seed array entries + aligned comments.
    - concept-feed.service.ts: weak-concept predicate (leaf === 'dying' || ...
      'falling' || 'dead'). This was the TD-06 TS2367 site (dead 'dying'
      branch pre-renamed to 'yellow' in 760fa4f8); the new literal is now
      valid.
    - PlannerScreen.tsx: dyingNodes / deadNodes filter predicates.
    - trellis/TrellisStatusPanel.tsx: dyingNodes / deadNodes filter predicates.
    - trellis/TrellisLeaf.tsx: STATE_COLOR + STATE_VEIN Record keys; withDecay
      signature; 3 literal comparisons; aligned comments.
    - tests/services/trellis-state.test.mjs: rename test names + assertion values.
    - tests/components/trellis-tooltip-copy.test.mjs: rename resolveHealthCopy
      args + LeafState enumeration.
    - tests/services/concept-batch-filter.test.mjs: comment-only alignment.

    SKIP: trellis/TrellisTooltip.tsx absent from working tree — file
    restoration deferred to v1.5 per Phase 33 scope boundary. The pinned
    test trellis-tooltip-copy.test.mjs continues to fail with
    ERR_MODULE_NOT_FOUND (pre-existing, NOT a rename regression).

    Intentionally NOT renamed per D-13 scope:
    - Badge.tsx 'yellow' — CSS color palette, independent of trellis.
    - PodcastScreen.tsx:220 'yellow' — UI status color, not LeafState.

    Locale bundles unchanged — planner.trellis.dying / planner.trellis.dead
    keys already exist with correct copy (D-14).

    tsc -b --noEmit: exit 0. vite build: clean. Test delta vs. pre-rename
    baseline: fail count NOT increased, signature set IS a subset of baseline
    (per /tmp/phase-33-pre-rename-baseline.log and /tmp/phase-33-post-rename.log).
    Pre-existing failure kinds (ERR_IMPORT_ATTRIBUTE_MISSING,
    ERR_MODULE_NOT_FOUND, ERR_UNKNOWN_FILE_EXTENSION, ERR_ASSERTION) all carry
    through from v1.3/1.4 WIP state per Pitfall #4/#5.
    EOF
    )"
    ```

    Step 6 — Final verify:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    # must be empty
    git log -1 --format=%s
    # must start with "refactor(trellis): rename LeafState"
    git log -1 --stat | grep -c "app/"
    # must be >= 8 (8 changed files)
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit && echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b --noEmit` exits 0 with empty output.
    - Global grep of `'yellow'` in `app/src/` / `app/tests/` returns hits ONLY in `Badge.tsx` and `PodcastScreen.tsx`.
    - Global grep of `'fallen'` in `app/src/` / `app/tests/` returns zero hits.
    - `grep -c "'falling'"` across same scope returns at least 3 (retained).
    - Post-rename test run: `FAIL_COUNT_POST ≤ FAIL_COUNT_BASELINE` AND the post-rename signature set is a SUBSET of the baseline signature set (no new signatures introduced). HALT criteria (fail count increased OR new signature appeared) must NOT trigger.
    - `npx vite build` exits 0.
    - `git log -1 --format=%s` starts with `refactor(trellis): rename LeafState`.
    - `git log -1 --stat` shows all 8 files in the single commit.
    - Commit body contains the SKIP note referencing absent TrellisTooltip.tsx.
    - `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>LeafState rename complete in one atomic commit; tsc clean; post-rename test signatures ⊆ pre-rename baseline (no new regressions).</done>
</task>

</tasks>

<verification>
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
- `cd /Users/Code/EchoLearn/app && npx vite build` completes cleanly.
- Global `grep "'yellow'\|'fallen'"` across `app/src` + `app/tests` returns ONLY the 2 intentional Badge.tsx / PodcastScreen.tsx `'yellow'` hits (no `'fallen'` at all).
- Post-rename `npm test` fail count ≤ pre-rename baseline fail count AND post-rename signature set ⊆ pre-rename signature set.
- Single commit covers all 8 files — `git log --follow app/src/services/trellis-state.service.ts` traces cleanly.
</verification>

<success_criteria>
- 8 files renamed in one atomic commit (TrellisTooltip.tsx absent, explicitly skipped per objective Scope boundaries).
- tsc clean, vite build clean.
- No new trellis test regressions (signature set subset + fail count ≤ baseline).
- Badge.tsx + PodcastScreen.tsx confirmed untouched.
- `git log --follow` on the renamed files shows a single refactor commit.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-03-SUMMARY.md` with the commit SHA, exhaustive rename site list, tsc output, vite build output, the `FAIL_COUNT_BASELINE` / `FAIL_COUNT_POST` numbers, and the baseline vs. post-rename signature diff (should be empty).
</output>
