---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 01
type: execute
wave: 1
depends_on: [33-00]
files_modified:
  - app/src/components/ConceptProgressCard.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
autonomous: true
requirements: [TD-05]
must_haves:
  truths:
    - "ConceptProgressCard.tsx no longer exists on disk"
    - "4 orphan i18n keys (home.feed.title, home.feed.complete, home.feed.progress, home.feed.progressCompact) removed from all 4 locale bundles in ONE atomic commit"
    - "bundle-parity.test.mjs continues to pass after the deletion"
    - "VineProgress's still-in-use home.feed.* keys (vineComplete, vineProgress, allExplored, suggestionTitle, scrollToTop, creditToast, emptyTitle, emptyBody, loadingTitle, feedbackPrompt, generationErrorTitle, generationErrorBody, generationErrorRetry) remain present in all 4 locale bundles"
  artifacts:
    - path: "app/src/components/ConceptProgressCard.tsx"
      provides: "Deleted — was orphaned by Phase 31 D-01 VineProgress replacement"
      state: "absent"
    - path: "app/src/locales/en.json"
      provides: "home.feed group with 4 orphan keys removed; all other keys retained"
      must_not_contain: "home.feed.title, home.feed.complete, home.feed.progress, home.feed.progressCompact"
  key_links:
    - from: "tests/locales/bundle-parity.test.mjs"
      to: "identical key sets across en/zh/es/ja"
      via: "node --test"
      pattern: "bundle.*parity.*pass"
---

<objective>
Close TD-05 partial orphan sweep: delete the orphaned `ConceptProgressCard.tsx` component file (researcher-verified zero consumers) and the 4 i18n keys it was the sole consumer of (`home.feed.title`, `home.feed.complete`, `home.feed.progress`, `home.feed.progressCompact`) from all 4 locale bundles. Deletion must be atomic per CLAUDE.md's bundle-parity rule.

**Scope boundaries (per D-07, D-08):** Do NOT delete `post-store.service.ts` or `ImmersiveInfoFlow` — both deferred to v1.5.

Purpose: D-06 + D-09 — reduce dead code surface; keep bundle-parity test green at every commit boundary.
Output: ConceptProgressCard.tsx absent; 4 keys absent from each of 4 locale bundles; bundle-parity test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@app/src/components/ConceptProgressCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Delete ConceptProgressCard.tsx and remove 4 orphan i18n keys from all 4 locale bundles in one atomic commit</name>
  <read_first>
    - app/src/components/ConceptProgressCard.tsx (must confirm zero consumers before deleting)
    - app/src/locales/en.json (locate the `home.feed` block, confirm the 4 keys are present and identify retained siblings)
    - app/src/locales/zh.json
    - app/src/locales/es.json
    - app/src/locales/ja.json
    - app/tests/locales/bundle-parity.test.mjs (the regression guard)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #2 (grep evidence + exact key list)
    - CLAUDE.md i18n workflow (bundle-parity rule: 4 locales move together, never split)
  </read_first>
  <action>
    Step 1 — Grep-verify zero external consumers of the 4 orphan keys (exclude ConceptProgressCard.tsx itself since we're about to delete it):

    ```
    cd /Users/Code/EchoLearn && grep -rn "home\.feed\.\(title\|complete\|progress\|progressCompact\)" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" | grep -v "ConceptProgressCard.tsx"
    ```

    Expected: zero output. If ANY hit appears, HALT — there's an undiscovered consumer; escalate.

    Step 2 — Also grep-verify zero consumers of the component itself:

    ```
    cd /Users/Code/EchoLearn && grep -rn "ConceptProgressCard\|CompactProgressBar" app/src/ app/tests/ --include="*.ts" --include="*.tsx" --include="*.mjs" | grep -v "ConceptProgressCard.tsx"
    ```

    Expected: zero output. If ANY hit appears, HALT.

    Step 3 — Delete the component file:

    ```
    rm /Users/Code/EchoLearn/app/src/components/ConceptProgressCard.tsx
    ```

    Step 4 — Apply all 4 locale edits in one batch, then Steps 5-6 validate. For EACH locale (en.json, zh.json, es.json, ja.json) use the `Edit` tool to:
    - Locate the `"feed": { ... }` block inside the `"home"` namespace.
    - Delete these 4 key/value pairs: `"title": "..."`, `"complete": "..."`, `"progress": "..."`, `"progressCompact": "..."`.
    - Preserve every other key in the block (especially `vineComplete`, `vineProgress`, `allExplored`, `suggestionTitle`, `scrollToTop`, `creditToast`, `emptyTitle`, `emptyBody`, `loadingTitle`, `feedbackPrompt`, `generationErrorTitle`, `generationErrorBody`, `generationErrorRetry`).
    - Preserve valid JSON (trailing comma handling — the key before your deletions may become the last key in the block).

    Apply all 4 locale edits sequentially before moving to Step 5. Do NOT run parity test between edits — Step 6 is the single validation gate for all 4 locales together.

    Reference values removed (per Finding #2):
    - en.json: `"title": "Today's Concepts"`, `"complete": "All caught up!"`, `"progress": "{{explored}} of {{total}} explored"`, `"progressCompact": "{{explored}}/{{total}}"`
    - zh.json: `"title": "今日概念"`, `"complete": "全部看完了!"`, `"progress": "已探索 {{explored}}/{{total}}"`, `"progressCompact": "{{explored}}/{{total}}"`
    - es.json: `"title": "Conceptos de hoy"`, `"complete": "Todo al dia!"`, `"progress": "{{explored}} de {{total}} explorados"`, `"progressCompact": "{{explored}}/{{total}}"`
    - ja.json: `"title": "今日のコンセプト"`, `"complete": "すべて完了!"`, `"progress": "{{explored}}/{{total}} 探索済み"`, `"progressCompact": "{{explored}}/{{total}}"`

    Step 5 — Validate JSON parse across all 4 bundles:
    ```
    cd /Users/Code/EchoLearn && node -e "for (const l of ['en','zh','es','ja']) { JSON.parse(require('fs').readFileSync('app/src/locales/'+l+'.json','utf8')); console.log(l+' OK'); }"
    ```
    All four must print `OK`.

    Step 6 — Run bundle-parity + missing-key tests:
    ```
    cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs
    ```
    Both must pass.

    Step 7 — Stage + commit atomically:
    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/components/ConceptProgressCard.tsx \
      app/src/locales/en.json \
      app/src/locales/zh.json \
      app/src/locales/es.json \
      app/src/locales/ja.json

    git commit -m "$(cat <<'EOF'
    refactor(feed): delete orphaned ConceptProgressCard.tsx + its 4 dead i18n keys (TD-05)

    Phase 31 D-01 replaced ConceptProgressCard (sticky card) with VineProgress (SVG
    vine). The card file and its 4 sole-consumer i18n keys remained on disk until
    now. Per Phase 33 D-06 + D-09:

    - Delete app/src/components/ConceptProgressCard.tsx entirely (both exports:
      ConceptProgressCard, CompactProgressBar — zero consumers confirmed via grep).
    - Delete 4 home.feed.* keys from all 4 locale bundles (atomic commit per
      CLAUDE.md bundle-parity rule):
        home.feed.title, home.feed.complete, home.feed.progress, home.feed.progressCompact

    Retained per D-07/D-08: post-store.service.ts, ImmersiveInfoFlow export.
    Deferred to v1.5 milestone planning.

    bundle-parity.test.mjs continues to pass.
    EOF
    )"
    ```

    Step 8 — Confirm git status clean + verify:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f /Users/Code/EchoLearn/app/src/components/ConceptProgressCard.tsx` exits 0.
    - `grep -c '"title"' /Users/Code/EchoLearn/app/src/locales/en.json` drops by exactly 1 vs. pre-state (only `home.feed.title` removed from that particular key name at that level).
    - `grep -rn "home\.feed\.\(title\|complete\|progress\|progressCompact\)" /Users/Code/EchoLearn/app/src/ /Users/Code/EchoLearn/app/tests/` returns zero hits.
    - `grep -rn "ConceptProgressCard\|CompactProgressBar" /Users/Code/EchoLearn/app/` returns zero hits.
    - `node --test tests/locales/bundle-parity.test.mjs` reports `fail 0`.
    - All 4 locale files parse as valid JSON.
    - `git status --porcelain` is empty after commit.
    - `git log -1 --format=%s` starts with `refactor(feed): delete orphaned ConceptProgressCard.tsx`.
  </acceptance_criteria>
  <done>ConceptProgressCard.tsx + 4 orphan keys deleted in one commit; bundle parity preserved; working tree clean.</done>
</task>

</tasks>

<verification>
- Full bundle-parity + missing-key test: `cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs` — both pass.
- tsc clean: `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` — exit 0 (the deletion removes an unused file so tsc remains clean).
- Grep sweep: zero hits for deleted symbols and deleted keys across `app/`.
</verification>

<success_criteria>
- ConceptProgressCard.tsx deleted.
- 4 orphan i18n keys removed from all 4 locale bundles.
- bundle-parity.test.mjs passes.
- One atomic commit captures both deletions.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-01-SUMMARY.md` with commit SHA, files deleted, key diff, and bundle-parity test output.
</output>
