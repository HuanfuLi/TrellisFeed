---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/components/ScrollToTopFAB.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/src/screens/PlannerScreen.tsx
  - app/src/screens/SettingsScreen.tsx
  - app/src/screens/settings/SettingsDataScreen.tsx
  - app/src/screens/settings/SettingsFeaturesScreen.tsx
  - app/src/services/daily-read.service.ts
  - app/src/services/post-history.service.ts
  - app/tests/concept-quota.test.mjs
  - app/tests/services/concept-batch-filter.test.mjs
  - app/tests/services/daily-generation-cap.test.mjs
  - app/tests/services/starter-posts.test.mjs
autonomous: true
requirements: [WAVE-4-WIP]
gap_closure: false
must_haves:
  truths:
    - "All 12 Wave-4 follow-up working-tree files commit cleanly in ONE atomic chore commit (per D-29)"
    - "Per-file diff review (D-30) confirms zero debug logs, no commented-out blocks, no leaked secrets, no half-implemented features, no unrelated experiments"
    - "Targeted test suite (D-31: concept-quota + concept-batch-filter + daily-generation-cap + starter-posts + bundle-parity) passes BEFORE the flush commit lands"
    - "After the commit, `git status --porcelain` is empty AND `npx tsc -b --noEmit` exits 0 — Wave 1 plans are unblocked"
    - "Commit subject is exactly `chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)` (per D-29)"
    - "i18n bundle parity holds: any new keys added in Wave 4 appear in all 4 locale bundles (CLAUDE.md: en/zh/es/ja move together)"
  artifacts:
    - path: "app/src/components/ScrollToTopFAB.tsx"
      provides: "Wave 4 follow-up edits committed (verify diff content; no behavioral expectation beyond clean diff review)"
      state: "committed"
    - path: "app/src/locales/en.json"
      provides: "Wave 4 i18n key additions committed in lock-step with the other 3 bundles"
      state: "committed"
    - path: "app/src/locales/zh.json"
      provides: "Mirrored Wave 4 i18n key additions"
      state: "committed"
    - path: "app/src/locales/es.json"
      provides: "Mirrored Wave 4 i18n key additions"
      state: "committed"
    - path: "app/src/locales/ja.json"
      provides: "Mirrored Wave 4 i18n key additions"
      state: "committed"
    - path: "app/src/screens/PlannerScreen.tsx"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/src/screens/SettingsScreen.tsx"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/src/screens/settings/SettingsDataScreen.tsx"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/src/screens/settings/SettingsFeaturesScreen.tsx"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/src/services/daily-read.service.ts"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/src/services/post-history.service.ts"
      provides: "Wave 4 follow-up edits committed"
      state: "committed"
    - path: "app/tests/concept-quota.test.mjs"
      provides: "Test updates committed; suite continues to pass"
      state: "committed"
    - path: "app/tests/services/concept-batch-filter.test.mjs"
      provides: "Test file is tracked (was untracked or recently tracked); all assertions pass"
      state: "committed-or-already-tracked"
    - path: "app/tests/services/daily-generation-cap.test.mjs"
      provides: "Test file is tracked; all assertions pass"
      state: "committed-or-already-tracked"
    - path: "app/tests/services/starter-posts.test.mjs"
      provides: "Test file is tracked; all assertions pass"
      state: "committed-or-already-tracked"
  key_links:
    - from: "git working tree"
      to: "single chore commit on main branch"
      via: "explicit `git add <file-list>` then `git commit -m \"chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)\"`"
      pattern: "chore\\(v1\\.4\\): flush Wave 4 follow-up WIP"
    - from: "targeted test suite"
      to: "all 5 specified test files exit 0"
      via: "node --test"
      pattern: "fail 0"
    - from: "tsc baseline"
      to: "exit 0 after commit (matches pre-commit baseline)"
      via: "npx tsc -b --noEmit"
      pattern: "exit code 0"
---

<objective>
Phase 33 v2 prerequisite: re-flush the 12 working-tree files left over from Phase 32.1 Wave 4 (Bug A through Bug 10 fixes + i18n keys + UI polish). This must land BEFORE Plans 33-06 (perf memoization) or 33-07 (cosmetic polish) execute, otherwise their edits will collide with stale uncommitted work.

This plan mirrors the structure of `33-00-SUMMARY.md`: a single atomic `chore(v1.4)` commit covering all 12 files after a per-file diff review confirms each diff is safe to land. Per CONTEXT.md decisions D-29/D-30/D-31.

Purpose:
- D-29: Single chore commit with the exact specified subject line.
- D-30: Per-file diff review (same policy as D-20) — HALT if any diff contains debug logs, commented-out blocks, half-implemented work, or unrelated experiments.
- D-31: All targeted tests must pass at the flush boundary; do NOT skip a failing test — pause and fix root cause first.

Output: working tree clean (`git status --porcelain | wc -l` returns 0); single commit subject matches D-29 spec; tsc still exit 0; targeted tests still green.

**Scope boundary:** This plan ONLY commits the existing Wave 4 working-tree changes. It does NOT modify behavior, refactor code, or add new features. If a per-file review surfaces any issue, HALT and escalate — do not "fix while flushing".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Per-file diff review (D-30 policy gate)</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-29, D-30, D-31 — re-read in full before reviewing diffs)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md (mirror this structure for the new commit)
    - CLAUDE.md "Best practices learned in Phase 32.1" section, especially rule #1 ("Search for dead code BEFORE assuming") and rule #5 ("Hardcoded fallbacks vs. defer-to-streamer")
  </read_first>
  <action>
    Step 1 — Confirm working-tree state matches D-29's expected file set:

    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    ```

    Expected output (12 lines, modulo ordering):
    ```
     M app/src/components/ScrollToTopFAB.tsx
     M app/src/locales/en.json
     M app/src/locales/es.json
     M app/src/locales/ja.json
     M app/src/locales/zh.json
     M app/src/screens/PlannerScreen.tsx
     M app/src/screens/SettingsScreen.tsx
     M app/src/screens/settings/SettingsDataScreen.tsx
     M app/src/screens/settings/SettingsFeaturesScreen.tsx
     M app/src/services/daily-read.service.ts
     M app/src/services/post-history.service.ts
     M app/tests/concept-quota.test.mjs
    ?? app/tests/services/concept-batch-filter.test.mjs
    ?? app/tests/services/daily-generation-cap.test.mjs
    ?? app/tests/services/starter-posts.test.mjs
    ```

    NOTE: per CONTEXT.md, the 3 untracked test files MAY have already been tracked by a prior commit (e.g., `9486799a` per 33-00 SUMMARY). Re-check: if `git status --porcelain` shows fewer than 3 `??` lines for those files, that's expected — they're already in the tree. Reduce the staging set accordingly in Task 5.3.

    If the working tree contains ADDITIONAL files not in the D-29 list (e.g., `.planning/STATE.md` or anything else), HALT and escalate — do not silently broaden the flush scope.

    Step 2 — For each file in the D-29 list, run `git diff` (or `git diff --no-index /dev/null <file>` for untracked) and apply the D-30 review checklist:

    For each MODIFIED file:
    ```
    cd /Users/Code/EchoLearn && git diff app/src/components/ScrollToTopFAB.tsx
    cd /Users/Code/EchoLearn && git diff app/src/locales/en.json
    cd /Users/Code/EchoLearn && git diff app/src/locales/zh.json
    cd /Users/Code/EchoLearn && git diff app/src/locales/es.json
    cd /Users/Code/EchoLearn && git diff app/src/locales/ja.json
    cd /Users/Code/EchoLearn && git diff app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && git diff app/src/screens/SettingsScreen.tsx
    cd /Users/Code/EchoLearn && git diff app/src/screens/settings/SettingsDataScreen.tsx
    cd /Users/Code/EchoLearn && git diff app/src/screens/settings/SettingsFeaturesScreen.tsx
    cd /Users/Code/EchoLearn && git diff app/src/services/daily-read.service.ts
    cd /Users/Code/EchoLearn && git diff app/src/services/post-history.service.ts
    cd /Users/Code/EchoLearn && git diff app/tests/concept-quota.test.mjs
    ```

    For each UNTRACKED file (only if still listed `??` after Step 1):
    ```
    cd /Users/Code/EchoLearn && git diff --no-index /dev/null app/tests/services/concept-batch-filter.test.mjs
    cd /Users/Code/EchoLearn && git diff --no-index /dev/null app/tests/services/daily-generation-cap.test.mjs
    cd /Users/Code/EchoLearn && git diff --no-index /dev/null app/tests/services/starter-posts.test.mjs
    ```

    Per-file D-30 checklist (HALT and escalate on ANY hit):
    1. **Debug logs:** Any `console.log` (other than dev-mode-gated `if (import.meta.env.DEV) console.log/warn/error`)? `console.error` with stack-trace dumping unrelated to error-handling? `debugger;` statements?
    2. **Commented-out blocks:** Multi-line `// ...` or `/* ... */` blocks containing actual code (not just doc comments)?
    3. **Secret leakage:** Hard-coded API keys (`sk-...`, `pk-...`, anything matching `[A-Za-z0-9]{30,}` in a string literal that looks like a token)? `localhost` URLs that should be relative?
    4. **Half-implemented features:** New `TODO` / `FIXME` / `XXX` comments? Unused imports added then not consumed? Functions added without callers? Type annotations like `any` introduced without justification?
    5. **Unrelated experiments:** Refactors that go beyond what the file's stated Wave 4 disposition would imply (per CONTEXT.md table at lines 107-120)?

    If the diff for any file fails the checklist:
    - DO NOT modify the file in this plan.
    - DO NOT use `git stash` or `git checkout --` (destructive — preserves nothing).
    - HALT, return to the orchestrator with: filename, offending content excerpt, and recommendation (revert in working tree by hand, or split the file out into a separate plan for individual triage).

    Step 3 — Capture the per-file disposition in plan output (will be referenced in 33-05-SUMMARY.md). For each file, record one of:
    - `clean — proceed with flush`
    - `clean (already tracked, no-op)` — for the 3 test files if they're not actually untracked
    - `HALT — <reason>` — if any D-30 violation found
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` lists exactly the 12 files in D-29 (modulo the 3 test files possibly already tracked — reduce expectation accordingly).
    - For every file, `git diff` (or `git diff --no-index /dev/null <file>`) was executed and reviewed against the D-30 checklist.
    - Zero files trigger any of the 5 D-30 violation classes; OR if any file does, the orchestrator received a HALT with details and Task 5.2 / 5.3 were NOT executed.
    - No new files appear in the working tree beyond the D-29 list (i.e., this task did not modify any source).
  </acceptance_criteria>
  <done>All 12 (or fewer, if tests already tracked) Wave 4 files reviewed; clean disposition recorded for each; ready for Task 5.2 test gate.</done>
</task>

<task type="auto">
  <name>Task 5.2: Run targeted test suite (D-31 gate)</name>
  <read_first>
    - app/tests/concept-quota.test.mjs (read in full to understand what's being tested)
    - app/tests/services/concept-batch-filter.test.mjs
    - app/tests/services/daily-generation-cap.test.mjs
    - app/tests/services/starter-posts.test.mjs
    - app/tests/locales/bundle-parity.test.mjs
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-31 verbatim)
    - CLAUDE.md "Best practices learned in Phase 32.1" rule #2 ("Tests must guard the LIVE code path")
  </read_first>
  <action>
    Step 1 — Run the D-31 targeted test suite as a single command so the report is one cohesive output block:

    ```
    cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | tail -20
    ```

    Capture the output. Expected outcome (per D-31 + Finding #7 in RESEARCH.md):
    - `concept-quota.test.mjs` — 8 tests pass.
    - `concept-batch-filter.test.mjs` — 8 tests pass.
    - `daily-generation-cap.test.mjs` — 11 tests pass.
    - `starter-posts.test.mjs` — 9 tests pass.
    - `bundle-parity.test.mjs` — 2 tests pass.
    - Aggregate: `pass 38 / fail 0` (or close to it — exact pass count may shift slightly if a suite has been edited; the load-bearing assertion is `fail 0`).

    Step 2 — If `fail` count > 0:
    - Do NOT proceed to Task 5.3.
    - Do NOT delete or skip the failing test.
    - Investigate the root cause: read the failing test source, read the live code it asserts on, identify whether the assertion is stale (test needs update) or the live code regressed (code needs fix).
    - Fix the root cause via Edit (either the test OR the code, depending on diagnosis), re-run the suite, and only proceed to Task 5.3 once `fail 0` holds.
    - Record the fix in this plan's eventual SUMMARY ("Pre-flush fix" subsection).

    Step 3 — Tsc clean check (ensures the WIP files compile):

    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    ```

    Expected: `tsc-exit=0`. If non-zero, STOP and diagnose — the WIP introduced a type regression that must be fixed before flush.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | grep -E "^# (tests|pass|fail)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - The aggregate `fail` count from the targeted suite is exactly `0`.
    - The aggregate `pass` count is at least `30` (covers the 5 suites; allows minor drift if a single test was added/removed in WIP).
    - `npx tsc -b --noEmit` exits 0 (captured stdout shows `tsc-exit=0`).
    - If any failure was found, a root-cause fix landed BEFORE Task 5.3 began (recorded in the eventual SUMMARY).
  </acceptance_criteria>
  <done>D-31 targeted test gate passes; tsc clean; flush is safe to commit.</done>
</task>

<task type="auto">
  <name>Task 5.3: Stage explicit file list and commit atomically</name>
  <read_first>
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md (D-29 commit subject — must match exactly)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-00-SUMMARY.md (commit-message style reference)
  </read_first>
  <action>
    Step 1 — Stage explicit file list. Do NOT use `git add .` or `git add -A` (CLAUDE.md `<git_safety>` and the orchestrator's instruction explicitly forbid these because they can sweep up unrelated files like `.planning/STATE.md`).

    Use one `git add` invocation with the full file list:

    ```
    cd /Users/Code/EchoLearn && git add \
      app/src/components/ScrollToTopFAB.tsx \
      app/src/locales/en.json \
      app/src/locales/zh.json \
      app/src/locales/es.json \
      app/src/locales/ja.json \
      app/src/screens/PlannerScreen.tsx \
      app/src/screens/SettingsScreen.tsx \
      app/src/screens/settings/SettingsDataScreen.tsx \
      app/src/screens/settings/SettingsFeaturesScreen.tsx \
      app/src/services/daily-read.service.ts \
      app/src/services/post-history.service.ts \
      app/tests/concept-quota.test.mjs \
      app/tests/services/concept-batch-filter.test.mjs \
      app/tests/services/daily-generation-cap.test.mjs \
      app/tests/services/starter-posts.test.mjs
    ```

    If any of the 3 test files were determined "already tracked" in Task 5.1, omit them from this list (their `git add` would be a no-op but cleaner to skip).

    Step 2 — Verify the staging set matches what's expected (no surprises):

    ```
    cd /Users/Code/EchoLearn && git diff --cached --name-only
    ```

    Output should list exactly the files staged in Step 1. If anything else appears (e.g., `.planning/STATE.md`), unstage it:
    ```
    cd /Users/Code/EchoLearn && git reset HEAD <unexpected-file>
    ```

    Step 3 — Commit with the EXACT subject from D-29 via heredoc to preserve formatting:

    ```
    cd /Users/Code/EchoLearn && git commit -m "$(cat <<'EOF'
    chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)

    Second batch of working-tree changes after fe4a2387 — the Phase 32.1 Wave 4
    Bug A-D + Bug 7-10 fixes + i18n keys for the new features. Per Phase 33
    CONTEXT D-29/D-30/D-31:

    - Per-file diff review (D-30) confirmed clean: no debug logs, no commented-out
      blocks, no secret leakage, no half-implemented features, no unrelated
      experiments.
    - Targeted test suite (D-31) green: concept-quota, concept-batch-filter,
      daily-generation-cap, starter-posts, and bundle-parity all pass.
    - tsc -b --noEmit exits 0.

    Files (12, per D-29):
    - app/src/components/ScrollToTopFAB.tsx
    - app/src/locales/{en,zh,es,ja}.json (Wave 4 i18n keys; bundle parity preserved)
    - app/src/screens/PlannerScreen.tsx
    - app/src/screens/SettingsScreen.tsx
    - app/src/screens/settings/SettingsDataScreen.tsx
    - app/src/screens/settings/SettingsFeaturesScreen.tsx
    - app/src/services/daily-read.service.ts
    - app/src/services/post-history.service.ts
    - app/tests/concept-quota.test.mjs
    - app/tests/services/concept-batch-filter.test.mjs (track if previously untracked)
    - app/tests/services/daily-generation-cap.test.mjs (track if previously untracked)
    - app/tests/services/starter-posts.test.mjs (track if previously untracked)

    Unblocks Plans 33-06 (perf memoization) and 33-07 (cosmetic polish).
    EOF
    )"
    ```

    Step 4 — Verify the commit landed cleanly:

    ```
    cd /Users/Code/EchoLearn && git log -1 --format='%H %s'
    cd /Users/Code/EchoLearn && git status --porcelain
    ```

    Expected:
    - `git log -1` shows the new commit SHA + the EXACT subject line `chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)`.
    - `git status --porcelain` is empty (zero lines of output).

    Step 5 — Final tsc + targeted test re-verify (sanity gate after the commit, post-rewrite of file timestamps):

    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/locales/bundle-parity.test.mjs 2>&1 | tail -8
    ```

    Both must still show clean state (tsc-exit=0, fail 0).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && git log -1 --format='%s' && echo "---" && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` returns exactly: `chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)`.
    - `git status --porcelain | wc -l | tr -d ' '` returns `0` (working tree clean).
    - `git diff HEAD~1 --name-only` lists exactly the files from D-29 (allowing for the 3 test files being already tracked).
    - `git diff --cached --name-only` returns nothing (no staged changes left over).
    - `npx tsc -b --noEmit` exits 0 after the commit.
    - The targeted test suite (concept-quota + concept-batch-filter + daily-generation-cap + starter-posts + bundle-parity) still reports `fail 0` after the commit.
    - `git add .` and `git add -A` were NOT used (verifiable via Bash invocation history — only explicit file paths in the `git add` argument list).
  </acceptance_criteria>
  <done>Wave 4 WIP flushed in single atomic chore commit; working tree clean; downstream Plans 33-06/33-07 unblocked.</done>
</task>

</tasks>

<verification>
- Working tree clean: `cd /Users/Code/EchoLearn && git status --porcelain | wc -l | tr -d ' '` returns `0`.
- Commit subject exact: `cd /Users/Code/EchoLearn && git log -1 --format='%s'` returns `chore(v1.4): flush Wave 4 follow-up WIP (Phase 32.1 cleanup)`.
- Targeted tests green: `cd /Users/Code/EchoLearn/app && node --test tests/concept-quota.test.mjs tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/locales/bundle-parity.test.mjs` reports `fail 0`.
- tsc clean: `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
- Bundle parity preserved: locale JSON files all have identical key sets across en/zh/es/ja (subsumed by bundle-parity.test.mjs above).
</verification>

<success_criteria>
- All 12 (or fewer if tests already tracked) Wave 4 follow-up files committed in ONE atomic chore commit per D-29.
- D-30 per-file diff review applied; no policy violations.
- D-31 targeted test gate passed.
- Working tree clean post-commit; tsc clean; targeted tests still green.
- Plans 33-06 and 33-07 unblocked (their depends_on: [33-05] is satisfied).
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-05-SUMMARY.md` with:
- Commit SHA + exact subject line
- Per-file disposition table (mirroring 33-00-SUMMARY.md format)
- Targeted test output (pass/fail counts)
- tsc exit code post-commit
- Any pre-flush fixes that landed (with file + reason)
- Confirmation that Wave 1 plans (33-06, 33-07) are unblocked
</output>
