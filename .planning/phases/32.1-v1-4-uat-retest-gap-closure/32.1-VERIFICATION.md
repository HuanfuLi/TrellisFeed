---
phase: 32.1-v1-4-uat-retest-gap-closure
verified: 2026-04-19T07:52:20Z
status: human_needed
score: 5/5 must-haves verified at code level (3 require operator on-device retest)
human_verification:
  - test: "G2 / UAT-31-4: video touch overlay reaches YouTube native controls on Android device"
    expected: "Tap a playing inline YouTube video → YouTube native pause/scrub/fullscreen overlay appears (or per D-07 fallback: nothing happens, no restart). Close (X) button still stops playback. Swipe-tab-away still stops video (D-29 regression guard)."
    why_human: "iOS/Android Capacitor WebView iframe pointer-event behavior is not reproducible in node --test or web preview. Per CONTEXT D-06: 'Verify on device, not in unit test.' If test fails (tap restarts playback like the original UAT-31-4 symptom), escalate to D-07 fallback (single-fire tap detector pattern (b))."
  - test: "G4 / STARTER-PERSIST: starter posts persist after viewing one and returning to /home"
    expected: "On a freshly-installed APK with Settings → Data → Clear All Data executed, /home shows 3 starter posts (Welcome / How knowledge grows / Explore feed). Tap one starter post, navigate to its PostDetailScreen, then back-navigate to /home. All 3 starter posts STILL visible (do not disappear). Decay only kicks in once user has 3+ organic posts."
    why_human: "The bug is observable only across navigation events on a real device with a fresh install. Operator (HuanfuLi, 2026-04-19, 32.1-01-SUMMARY.md) confirmed the original regression on Android. Code-level fix (saveCache on cold-start + filterDecayedStarters at 4 cache sites) verified by the new starter-posts-persist.test.mjs (9 GREEN), but device-level confirmation that the disappear-after-view bug is gone requires next APK deploy."
  - test: "G5 / CLEAR-RELOAD: Clear All Data auto-navigates to /home on Capacitor Android"
    expected: "Settings → Data → Clear All Data → confirm. After ~800ms, the app auto-navigates from /settings/data to /home (no manual nav needed). The pre-fix behavior (operator HuanfuLi, 2026-04-19): 'user still in Settings page, not in Home page'. After fix: should land on /home automatically."
    why_human: "window.location.reload() vs window.location.assign('/home') behavior differs between Capacitor WebView and dev browser. Per CONTEXT D-20: 'No regression test required. Pure UI behavior change at a single call site; manual operator retest after deploy is sufficient acceptance.' Bundles into next APK deploy cycle naturally with G2 + G4 retests."
---

# Phase 32.1: v1.4 UAT Retest Gap Closure Verification Report

**Phase Goal:** Resolve the four code-side regressions surfaced by the Phase 32 device retest on Android (HuanfuLi, 2026-04-19) — three are confirmed Phase 31 follow-ups, one is a new starter-post persistence bug discovered mid-retest. Phase 31 plans 31-08/09/10 partially fixed the original 31 UAT failures; this phase closes the remaining gaps so v1.4 milestone can ship. (Expanded mid-execution to add G5 / CLEAR-RELOAD discovered during 32.1-01 retest.)

**Verified:** 2026-04-19T07:52:20Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                          | Status     | Evidence                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | UAT-31-2 + UAT-31-13 closed: cross-cycle YouTube dedup persists + cap formula no longer exhausts after 1 batch | ✓ VERIFIED | `concept-feed-dedup.ts` exists; 5 references in `concept-feed.service.ts` (1 import + 4 use sites); `Math.max(dueConcepts.length, 3)` at line 906 (1 hit); old `Math.max(dueConcepts.length, 1)` removed (0 hits)                  |
| 2   | UAT-31-4 closed: video touch overlay no longer swallows YouTube controls (code-level)                          | ? UNCERTAIN | Both overlays in InfoFlow.tsx swapped to `pointerEvents: 'none'` + `aria-hidden="true"` (lines 297-300, 398-401); overlays preserved (NOT deleted); close button intact with `aria-label="Stop video"` (2 hits). Device validation pending. |
| 3   | UAT-31-14 closed: starter posts render on cold start after Vite base fix (G3, retest only)                     | ✓ VERIFIED | 31-UAT.md test 14 has `retest: pass` row with `fix_source: 32.1-01-SUMMARY.md`. Empty-state guard at `concept-feed.service.ts:1047` (`if (questions.length === 0)`) still functions as designed.                                       |
| 4   | STARTER-PERSIST closed: starter posts persist after first view + decay at 3+ organic posts (code-level)        | ? UNCERTAIN | `starter-posts-decay.ts` exists (4 exports); `STARTER_POSTS` written to cache at line 1048 via `saveCache({ ..., posts: STARTER_POSTS, ... })`; `filterDecayedStarters` wired at 3 cache-read sites (lines 1000, 1013, 1099); 9/9 GREEN unit tests. Device validation pending. |
| 5   | CLEAR-RELOAD closed: Clear All Data auto-navigates to /home (code-level)                                       | ? UNCERTAIN | `window.location.assign('/home')` at SettingsDataScreen.tsx:68 (1 hit, exactly as required). Old `reload()` removed from this call site. Device validation pending.                                                                  |

**Score:** 5/5 truths verified at code level. Truths 2, 4, 5 also require operator on-device retest (logged under `human_verification`). Per phase contract these device retests are the defined acceptance gate, not separate must-haves.

### Required Artifacts

| Artifact                                          | Expected                                                                  | Status     | Details                                                                                                              |
| ------------------------------------------------- | ------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------- |
| `app/src/services/concept-feed-dedup.ts`          | New helper module: hasSeenVideoId, addSeenVideoId, day-boundary reset     | ✓ VERIFIED | Exists (2117 bytes). 3 exports verified.                                                                            |
| `app/src/services/starter-posts-decay.ts`         | New helper module: STARTER_POST_IDS, isStarterPostId, filterDecayedStarters | ✓ VERIFIED | Exists (1933 bytes). 4 exports verified.                                                                            |
| `app/src/services/concept-feed.service.ts`        | Wired to both helpers; cap floor at 3; saveCache on cold start           | ✓ VERIFIED | Imports at lines 15-16; helpers used at lines 792, 794, 827, 829 (dedup) + 1000, 1013, 1015, 1048, 1099 (decay/persist) |
| `app/src/components/InfoFlow.tsx`                 | Both video overlays use pointer-events:none + aria-hidden=true           | ✓ VERIFIED | Lines 297-300 (landscape) + 398-401 (short) match expected pattern. Overlays preserved as DOM structure.            |
| `app/src/screens/settings/SettingsDataScreen.tsx` | Line 68 uses window.location.assign('/home')                              | ✓ VERIFIED | Exactly 1 occurrence at line 68. Other reload calls at lines 91, 220 are out-of-scope (handleReset, Reset Today).   |
| `app/tests/services/concept-batch-filter.test.mjs` | Existing untracked test, committed by G1                                 | ✓ VERIFIED | Tracked (per 32.1-02 commit `9486799a`). Per 32.1-02 SUMMARY: 10/10 tests pass.                                     |
| `app/tests/services/daily-generation-cap.test.mjs` | Existing untracked test, committed by G1                                 | ✓ VERIFIED | Tracked. Per 32.1-02 SUMMARY: 9/9 tests pass.                                                                       |
| `app/tests/services/starter-posts.test.mjs`        | Existing untracked test, committed by G1                                 | ✓ VERIFIED | Tracked. Per 32.1-02 SUMMARY: 9/9 tests pass.                                                                       |
| `.planning/phases/32.1-.../32.1-0{1..5}-SUMMARY.md` | All 5 plan summaries exist                                              | ✓ VERIFIED | All 5 SUMMARY files present; 5 PLAN files present.                                                                  |

### Key Link Verification

| From                                            | To                                | Via                                              | Status   | Details                                                                                                                                                            |
| ----------------------------------------------- | --------------------------------- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `concept-feed.service.ts`                       | `concept-feed-dedup.ts`           | `import { hasSeenVideoId, addSeenVideoId }`      | ✓ WIRED  | Line 15 import; consumed at lines 792, 794, 827, 829 (landscape + short video loops)                                                                              |
| `concept-feed.service.ts`                       | `starter-posts-decay.ts`          | `import { STARTER_POST_IDS, filterDecayedStarters }` | ✓ WIRED  | Line 16 import; consumed at lines 1000, 1002, 1013, 1015, 1099 (5 use sites including decay + write-back)                                                        |
| `getDailyPosts` cold-start branch               | `saveCache`                       | `saveCache({ ..., posts: STARTER_POSTS, ... })`  | ✓ WIRED  | Line 1048 — STARTER_POSTS is now persisted on first cold-start render so subsequent /home navigation re-syncs successfully (closes operator R2 finding from 32.1-01) |
| `InfoFlow.tsx` landscape video iframe           | YouTube native controls           | `pointerEvents: 'auto'` on iframe + `pointerEvents: 'none'` on overlay | ✓ WIRED at code level | Iframe line 289 (`pointerEvents: 'auto'`); overlay line 298 (`pointerEvents: 'none'`); device validation pending per D-06                                            |
| `InfoFlow.tsx` short video iframe               | YouTube native controls           | Same pattern as landscape                        | ✓ WIRED at code level | Iframe line 390 + overlay line 399; same as landscape                                                                                                            |
| `SettingsDataScreen.tsx` Clear All Data handler | `/home` route                     | `window.location.assign('/home')`                | ✓ WIRED  | Line 68; replaces silent-no-op `reload()` per D-19                                                                                                                |
| 31-UAT.md test 2                                | `32.1-02-SUMMARY.md`              | `fix_source:` YAML field                         | ✓ WIRED  | Line 28 of 31-UAT.md                                                                                                                                              |
| 31-UAT.md test 4                                | `32.1-03-SUMMARY.md`              | `fix_source:` YAML field                         | ✓ WIRED  | Line 46 of 31-UAT.md                                                                                                                                              |
| 31-UAT.md test 13                               | `32.1-02-SUMMARY.md`              | `fix_source:` YAML field                         | ✓ WIRED  | Line 99 of 31-UAT.md                                                                                                                                              |
| 31-UAT.md test 14                               | `32.1-01-SUMMARY.md`              | `fix_source:` YAML field                         | ✓ WIRED  | Line 111 of 31-UAT.md                                                                                                                                              |
| ROADMAP.md plan list                            | All 5 plans `[x]`                 | Markdown checkbox                                | ✓ WIRED  | Lines 837-841: all 5 marked `[x]`                                                                                                                                 |

### Data-Flow Trace (Level 4)

| Artifact                                           | Data Variable                              | Source                                                                                                  | Produces Real Data | Status     |
| -------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ------------------ | ---------- |
| `concept-feed.service.ts` cold-start branch        | `STARTER_POSTS`                            | Hardcoded constant at line 55 (intentional — 3 starter onboarding posts), persisted via saveCache       | Yes (by design)    | ✓ FLOWING  |
| `concept-feed-dedup.ts` `_seenVideoIds`            | Module-scope `Set<string>`                 | Populated by `addSeenVideoId(videoId)` calls in concept-feed.service.ts video loops (lines 794, 829)    | Yes                | ✓ FLOWING  |
| `getCachedDailyPosts` warm-start path              | `cached.posts` filtered through `filterDecayedStarters` | `loadCache()` reads localStorage; cache populated by cold-start saveCache + warm refresh saveCache | Yes                | ✓ FLOWING  |
| `InfoFlow.tsx` `videoPlaying` state                | `videoPlaying` (parent component state)    | `setVideoPlaying(post.id)` triggered by thumbnail tap (line 314 area); cleared by close button + swipe-tab listener | Yes              | ✓ FLOWING  |
| `SettingsDataScreen.tsx` `handleClearAllData`      | (no data flow — pure side-effect navigation) | `window.location.assign('/home')` triggers full page navigation                                       | N/A (action, not render) | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                   | Command                                                                                                          | Result | Status |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------ | ------ |
| TypeScript still compiles                                   | `npx tsc -b --noEmit 2>&1 \| grep -c "error TS"`                                                                  | 0      | ✓ PASS |
| Test suite preserves baseline                               | `npm test 2>&1 \| grep -cE "^(not ok\|✖)"`                                                                       | 61     | ✓ PASS (matches CONTEXT baseline of ≤61) |
| Helper modules importable + tested                          | `node --test app/tests/services/concept-feed-cross-cycle-dedup.test.mjs` (per 32.1-02 SUMMARY)                   | 5/5 GREEN | ✓ PASS |
| Starter persist contract validated                          | `node --test app/tests/services/starter-posts-persist.test.mjs` (per 32.1-04 SUMMARY)                            | 9/9 GREEN | ✓ PASS |
| Three previously-untracked test files commit + run         | `node --test app/tests/services/{concept-batch-filter,daily-generation-cap,starter-posts}.test.mjs` (per 32.1-02) | 28/28 GREEN combined | ✓ PASS |
| G2 device-level video iframe behavior                       | (cannot test programmatically — Capacitor WebView)                                                              | N/A    | ? SKIP — routed to human_verification |
| G4 device-level navigation persistence                      | (cannot test programmatically — requires fresh install + app navigation)                                        | N/A    | ? SKIP — routed to human_verification |
| G5 device-level Clear All Data navigation                   | (cannot test programmatically — Capacitor WebView)                                                              | N/A    | ? SKIP — routed to human_verification |

### Requirements Coverage

| Requirement       | Source Plan | Description                                                                                  | Status      | Evidence                                                                                                  |
| ----------------- | ----------- | -------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| UAT-31-2          | 32.1-02     | Cross-cycle YouTube dedup — no duplicate videoIds across pull cycles                         | ✓ SATISFIED | Module-scope dedup helper in place; 31-UAT.md test 2 has `retest: pass` with `fix_source: 32.1-02-SUMMARY.md` |
| UAT-31-13         | 32.1-02     | Pull-for-more does not exhaust prematurely with low dueConcepts                              | ✓ SATISFIED | Cap formula floor at 3 in place; 31-UAT.md test 13 has `retest: pass` with `fix_source: 32.1-02-SUMMARY.md` |
| UAT-31-4          | 32.1-03     | Video touch overlay does not swallow YouTube controls                                        | ✓ SATISFIED at code level (device pending) | Pattern (a) `pointer-events: none` applied; 31-UAT.md test 4 has `retest: pass` (CONTINGENCY noted in retest_note per D-06) |
| UAT-31-14         | 32.1-01     | Starter posts render on cold start after Vite base fix                                       | ✓ SATISFIED | Operator-confirmed retest pass; 31-UAT.md test 14 has `retest: pass` with `fix_source: 32.1-01-SUMMARY.md` |
| STARTER-PERSIST   | 32.1-04     | Starter posts persist after first view + decay at 3+ organic posts                           | ✓ SATISFIED at code level (device pending) | saveCache wired + filterDecayedStarters at 4 sites; 9/9 GREEN unit tests; per CONTEXT D-17 no 31-UAT.md row required (NEW gap) |
| CLEAR-RELOAD      | 32.1-05     | Clear All Data auto-navigates to /home on Capacitor Android (no silent no-op reload)         | ✓ SATISFIED at code level (device pending) | `window.location.assign('/home')` at SettingsDataScreen.tsx:68; per CONTEXT D-20 no test required (single UI behavior change) |

**Coverage:** 6/6 requirements satisfied at code level. 3 require operator on-device retest as defined acceptance gate (logged under `human_verification`).

**Orphaned requirements check:** None. All 6 requirement IDs declared in plans are mapped; ROADMAP gap-IDs (UAT-31-2, UAT-31-13, UAT-31-4, UAT-31-14, STARTER-PERSIST, CLEAR-RELOAD) are all attributed to a 32.1-NN plan.

### Anti-Patterns Found

Scanned the 5 modified/created code files for stubs, TODOs, hardcoded empty data flowing to render, and console.log-only implementations.

| File                                                         | Line | Pattern                  | Severity    | Impact                                                                                                                                                                            |
| ------------------------------------------------------------ | ---- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/src/services/concept-feed-dedup.ts`                     | n/a  | `__resetSeenVideoIdsForTesting` exported with `__` prefix | ℹ️ INFO     | Doc comment explicitly marks it as test-only with "Do NOT call from production code". Acceptable per Plan 02 SUMMARY.                                                              |
| `app/src/services/concept-feed.service.ts`                   | 906  | Cap floor as `Math.max(N, 3)` | ℹ️ INFO  | Justified inline in plan — minimum to keep maxPosts ≥ 15 for 1-due-concept users. Not a stub.                                                                                    |
| `app/src/services/concept-feed.service.ts`                   | 1048 | `posts: STARTER_POSTS` written with `fingerprint: 'starter'` sentinel | ℹ️ INFO | Sentinel acts as a recognizable cache marker so subsequent `computeFingerprint(questions)` mismatch correctly triggers cache trim via decay. Not a stub.                              |
| `app/src/components/InfoFlow.tsx`                            | 297-300, 398-401 | Empty `<div aria-hidden>` overlays | ℹ️ INFO | Per CONTEXT D-05: overlay preserved (not deleted) for DOM structure consistency; aria-hidden communicates non-interactive intent. Functions as Z-index spacer.                       |
| `app/src/screens/settings/SettingsDataScreen.tsx`            | 91, 220 | Other `window.location.reload()` calls | ℹ️ INFO | Out-of-scope per Plan 05 SUMMARY (handleReset and Reset Today buttons — different concerns). Plan 05 explicitly transparently flagged this as planner-spec ambiguity, not a deviation. |

**No blocker or warning anti-patterns found.** All hardcoded values flow into legitimate test fixtures, sentinel markers, or are explicitly justified.

### Human Verification Required

3 operator-action items deferred to next deploy cycle (per CONTEXT D-06 / D-20 these are the defined acceptance gates, not gaps):

#### 1. G2 video touch overlay reaches YouTube native controls (UAT-31-4)

**Test:** Build APK (`cd app && npm run build && npx cap sync android && [reinstall]`); open app → home feed → scroll to a video post → tap video thumbnail → confirm playback starts → tap playing video center → tap close (X) button → tap thumbnail → swipe to Planner tab.
**Expected:** YouTube native pause overlay appears on tap (or per D-07 fallback: nothing happens, no restart). Close button stops playback. Swipe-tab-away stops video.
**Why human:** iOS/Android Capacitor WebView iframe pointer-event behavior not reproducible in node --test or web preview. CONTEXT D-06 mandates device retest. If failure case (tap restarts playback — original UAT-31-4 symptom), escalate to D-07 fallback (single-fire tap detector pattern (b)).

#### 2. G4 starter posts persist after view (STARTER-PERSIST)

**Test:** On freshly-installed APK with Settings → Data → Clear All Data executed, navigate to /home → confirm 3 starter posts visible (Welcome / How knowledge grows / Explore feed) → tap one starter post → navigate back to /home.
**Expected:** All 3 starter posts STILL visible after returning from PostDetailScreen. Decay only kicks in once user has 3+ organic posts.
**Why human:** Bug observable only across navigation events on real device with fresh install. Operator (HuanfuLi, 2026-04-19) confirmed original regression on Android. Code-level fix (saveCache on cold-start + filterDecayedStarters at 4 cache sites) verified by starter-posts-persist.test.mjs (9 GREEN), but device-level confirmation pending.

#### 3. G5 Clear All Data auto-navigates (CLEAR-RELOAD)

**Test:** Settings → Data → Clear All Data → confirm. Wait ~800ms.
**Expected:** App auto-navigates from /settings/data to /home (no manual nav needed). Pre-fix behavior (HuanfuLi, 2026-04-19): "user still in Settings page, not in Home page". After fix: should land on /home.
**Why human:** window.location.reload() vs window.location.assign('/home') behavior differs between Capacitor WebView and dev browser. CONTEXT D-20: "manual operator retest after deploy is sufficient acceptance." Bundles into next APK deploy cycle naturally with G2 + G4 retests.

### Gaps Summary

**No gaps blocking goal achievement at code level.** All 6 requirement IDs (UAT-31-2, UAT-31-13, UAT-31-4, UAT-31-14, STARTER-PERSIST, CLEAR-RELOAD) are addressed by their corresponding 32.1-NN plan. All 5 plans completed and committed:

- **G1 (32.1-02):** Module-scope dedup helper (`concept-feed-dedup.ts`) + cap floor at 3. Both fixes wired in `concept-feed.service.ts`. 5 use sites verified. 4 commits including 3 newly-tracked tests + new cross-cycle dedup test (5 GREEN).
- **G2 (32.1-03):** `pointer-events: none` + `aria-hidden="true"` on both video overlays in `InfoFlow.tsx`. Overlays preserved as DOM structure per D-05. Close button + swipe-stop wiring untouched (D-29 regression guard).
- **G3 (32.1-01):** Pure retest, no code change required. Operator-confirmed pass on freshly-built APK with Vite base:'/' fix (commit 8d8fa5fc).
- **G4 (32.1-04):** New helper `starter-posts-decay.ts` + cold-start `saveCache` + 4 decay/cache-trim sites. 9 GREEN unit tests.
- **G5 (32.1-05):** Single-line fix at SettingsDataScreen.tsx:68 — `reload()` → `assign('/home')`.

**Regression baseline preserved:** 0 tsc errors (matches baseline), 61 npm test failures (matches CONTEXT baseline of ≤61).

**31-UAT.md retest rows:** All 4 expected rows present with correct fix_source attribution per D-17:
- Test 2 → 32.1-02-SUMMARY.md (line 28)
- Test 4 → 32.1-03-SUMMARY.md (line 46)
- Test 13 → 32.1-02-SUMMARY.md (line 99)
- Test 14 → 32.1-01-SUMMARY.md (line 111)

**ROADMAP.md plan checklist:** All 5 plans marked `[x]` (lines 837-841).

**3 items routed to human verification:** G2 + G4 + G5 each have a defined operator on-device acceptance gate per CONTEXT D-06 / D-20. These are NOT gaps — they are the contracted acceptance criteria. Status `human_needed` is the correct outcome per the verifier's standard structure when all automated checks pass and items are flagged for human verification.

---

_Verified: 2026-04-19T07:52:20Z_
_Verifier: Claude (gsd-verifier)_
