---
phase: 56-ui-polish-documentation
plan: 05
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - Documents/UI_AUDIT_REPORT.md
  - Documents/CHANGELOG_4_05.md
  - Documents/CHANGELOG_SUMMARY_4_05.md
  - Documents/CHANGELOG_4_16.md
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/v1.3-INTEGRATION-CHECK.md
  - .planning/v1.3-MILESTONE-AUDIT.md
  - CLAUDE.md
autonomous: false
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "Every doc the operator approved for archival is MOVED (git mv) to its Legacy/milestones destination — no doc is deleted (D-08 stale docs move to Legacy/, never deleted)"
    - "Documents/UI_AUDIT_REPORT.md (Apr-16) is in Documents/Legacy/ and no longer at the Documents/ top level (D-03)"
    - "CLAUDE.md is corrected ONLY for the operator-approved drift directions recorded in 56-TRIAGE.md (DR-01, DR-02, any confirmed C-*) — no silent auto-correction (D-09)"
    - "The CLAUDE.md brand-history note matches db.service.ts IDB_NAME and the yesterday-queue note matches post-queue.service.ts"
    - "No load-bearing security documentation is removed from CLAUDE.md"
  artifacts:
    - path: "Documents/Legacy/UI_AUDIT_REPORT.md"
      provides: "Archived Apr-16 audit report (moved, not deleted)"
      contains: ""
    - path: "CLAUDE.md"
      provides: "Drift-corrected load-bearing doc per approved directions"
      contains: "Trellis"
  key_links:
    - from: "stale Documents/ + .planning/ docs"
      to: "Documents/Legacy/ and .planning/milestones/"
      via: "git mv (rename, never delete)"
      pattern: "rename"
    - from: "CLAUDE.md brand-history + yesterday-queue notes"
      to: "db.service.ts IDB_NAME / post-queue.service.ts key"
      via: "operator-approved corrected wording in 56-TRIAGE.md"
      pattern: "IndexedDB|trellis"
---

<objective>
Execute the operator-APPROVED DOCS-01 doc archival and DOCS-02 CLAUDE.md drift corrections from 56-TRIAGE.md. DOCS-01 is autonomous (move stale docs to Legacy via git mv — never delete, D-08). DOCS-02 is confirm-first (apply ONLY the operator-approved drift resolution directions recorded in 56-TRIAGE.md, then pause for the operator to confirm the rendered CLAUDE.md diff — CLAUDE.md is never silently auto-corrected, D-09). This plan is `autonomous: false` because of the DOCS-02 confirm gate.

Purpose: reconcile the documentation surface with reality without losing history (archive, don't delete) and without unilaterally editing the load-bearing CLAUDE.md (operator approves each correction).

Output: stale docs relocated under Documents/Legacy/ and .planning/milestones/; CLAUDE.md corrected for the approved drifts only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-ui-polish-documentation/56-TRIAGE.md
@.planning/phases/56-ui-polish-documentation/56-FINDINGS.md
@.planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md
@.planning/phases/56-ui-polish-documentation/56-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive approved stale docs to Legacy (DOCS-01)</name>
  <files>Documents/UI_AUDIT_REPORT.md, Documents/CHANGELOG_4_05.md, Documents/CHANGELOG_SUMMARY_4_05.md, Documents/CHANGELOG_4_16.md, .planning/v1.1-MILESTONE-AUDIT.md, .planning/v1.3-INTEGRATION-CHECK.md, .planning/v1.3-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md (the F-D* doc-staleness verdicts the operator APPROVED: which docs are archive-to-Legacy vs keep-live vs update)
    - .planning/phases/56-ui-polish-documentation/56-FINDINGS.md § DOCS-01 doc-staleness inventory (the keep-live / archive / update marks the triage approved)
    - .planning/phases/56-ui-polish-documentation/56-UI-SPEC.md § "Documentation Contract (DOCS-01, DOCS-02)" (move-to-Legacy-never-delete; updated-not-archived allowed per-doc)
    - the Documents/Legacy/ directory listing and the .planning/milestones/ directory listing (confirm the existing archive-destination convention before moving)
  </read_first>
  <action>
    For each doc the operator marked archive-to-Legacy APPROVED in 56-TRIAGE.md, MOVE it with `git mv` — NEVER `rm`/delete (D-08, preserves history). Use the established destinations confirmed by the directory listings:
    - Documents/UI_AUDIT_REPORT.md → Documents/Legacy/UI_AUDIT_REPORT.md (D-03 locked: this Apr-16 report is the explicit first archive target).
    - Documents/CHANGELOG_4_05.md → Documents/Legacy/CHANGELOG_4_05.md (only if APPROVED; note Documents/Legacy/CHANGELOG_4_02.md already exists, mirror the convention).
    - Documents/CHANGELOG_SUMMARY_4_05.md → Documents/Legacy/CHANGELOG_SUMMARY_4_05.md (only if APPROVED).
    - Documents/CHANGELOG_4_16.md → Documents/Legacy/CHANGELOG_4_16.md (only if APPROVED).
    - .planning/v1.1-MILESTONE-AUDIT.md → .planning/milestones/v1.1-MILESTONE-AUDIT.md (mirror the existing v1.4/v1.5/v1.6 milestone-file pattern already in .planning/milestones/).
    - .planning/v1.3-INTEGRATION-CHECK.md → .planning/milestones/v1.3-INTEGRATION-CHECK.md (only if APPROVED).
    - .planning/v1.3-MILESTONE-AUDIT.md → .planning/milestones/v1.3-MILESTONE-AUDIT.md (only if APPROVED).

    Move ONLY docs the operator approved for archival. Leave keep-live docs (Documents/CHANGELOG_5_20.md, Documents/EMAIL-DRAFT-PROFESSOR.md, Documents/LANDING-VIDEO-SCRIPT.md) untouched. For any doc the operator marked update-not-archive (e.g. a stale-but-live doc such as the .planning/codebase/CONVENTIONS.md "Tailwind CSS" line, IF approved-update), apply the approved edit instead of moving — do not move a doc marked update. Do NOT delete any file under any circumstance. If a target file already exists at the Legacy destination, stop and surface it rather than overwriting.
  </action>
  <verify>
    <automated>test -f Documents/Legacy/UI_AUDIT_REPORT.md && test ! -f Documents/UI_AUDIT_REPORT.md && ! git status --porcelain Documents/ .planning/ | grep -qE '^ ?D '</automated>
  </verify>
  <acceptance_criteria>
    - Documents/Legacy/UI_AUDIT_REPORT.md exists; Documents/UI_AUDIT_REPORT.md no longer at the Documents/ top level (D-03).
    - Every other doc approved-for-archival exists at its Legacy/milestones destination and is gone from its old location.
    - No doc was deleted: `git status --porcelain` shows renames (R) / additions, NOT deletions (no ` D ` entries) for Documents/ and .planning/.
    - Keep-live docs (CHANGELOG_5_20.md, EMAIL-DRAFT-PROFESSOR.md, LANDING-VIDEO-SCRIPT.md) are unchanged.
    - Only docs marked APPROVED in 56-TRIAGE.md were moved.
  </acceptance_criteria>
  <done>All operator-approved stale docs are archived via git mv to Legacy/milestones with history preserved; no doc deleted; keep-live docs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply operator-approved CLAUDE.md drift corrections (DOCS-02, confirm-first per D-09)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md § CLAUDE.md CORRECTIONS (for each DRIFTED claim: the operator-approved resolution direction AND the exact approved corrected wording)
    - .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md (DR-01, DR-02, confirmed C-* drift rows with file:line evidence)
    - CLAUDE.md § "Brand history" note (the SQLite/'echolearn' claim — DR-01) and § "Concept Feed Generation Pipeline" → "Yesterday-queue snapshot" line (the STORAGE_KEY_YESTERDAY claim — DR-02)
    - CLAUDE.md § "Video post completion signals" (the Detector D origin allowlist + enablejsapi=1 security documentation — must NOT be removed) and § "Question filter — RAW-ARGMAX gate" (security documentation — must NOT be removed)
    - app/src/services/db.service.ts (IDB_NAME='trellis' — the actual value the DR-01 corrected wording must match)
    - app/src/services/post-queue.service.ts (SQLITE_ROW_ID_YESTERDAY='queue_yesterday' in IndexedDB — the actual value the DR-02 corrected wording must match)
  </read_first>
  <action>
    Apply ONLY the operator-approved drift corrections recorded in 56-TRIAGE.md § CLAUDE.md CORRECTIONS, using the EXACT approved corrected wording for each DRIFTED claim — no paraphrase, no scope expansion. Per D-09, CLAUDE.md is high-stakes/load-bearing and is never silently auto-corrected: only edit claims that carry an operator-approved corrected wording in 56-TRIAGE.md.
    - DR-01: replace the stale brand-history note ("SQLite connection name 'echolearn' in db.service.ts") with the approved IndexedDB wording matching app/src/services/db.service.ts IDB_NAME='trellis'.
    - DR-02: replace the stale yesterday-queue note (STORAGE_KEY_YESTERDAY='trellis_post_queue_yesterday' written by load() in localStorage) with the approved wording matching the actual IndexedDB key in app/src/services/post-queue.service.ts.
    - Any confirmed C-* drift (e.g. function-name drift) only if 56-TRIAGE.md records an approved correction for it.
    Do NOT edit any claim the operator marked as a code-regression-to-investigate (those defer to a code task, not a doc edit). Do NOT remove any load-bearing security documentation: the Detector D YouTube postMessage origin allowlist (youtube.com / youtube-nocookie.com), the enablejsapi=1 requirement, and the Question-filter RAW-ARGMAX malicious-gate section MUST remain present. After applying, pause for the operator to confirm the rendered diff before the wave is considered complete (the human-check below).
  </action>
  <verify>
    <automated>grep -qi "indexeddb" CLAUDE.md && [ "$(grep -c "enablejsapi=1" CLAUDE.md)" -ge 1 ] && [ "$(grep -c "youtube-nocookie" CLAUDE.md)" -ge 1 ] && [ "$(grep -c "RAW-ARGMAX" CLAUDE.md)" -ge 1 ]</automated>
    <human-check>Operator reviews `git diff CLAUDE.md`: every hunk maps to an operator-approved correction in 56-TRIAGE.md § CLAUDE.md CORRECTIONS (DR-01 brand-history → IndexedDB/'trellis'; DR-02 yesterday-queue key → IndexedDB; any approved C-*). No claim marked code-regression-to-investigate was doc-edited. The Detector D origin allowlist, enablejsapi=1, and RAW-ARGMAX sections are still present. Operator confirms "approved" or names corrections to adjust.</human-check>
  </verify>
  <acceptance_criteria>
    - Every CLAUDE.md hunk in `git diff CLAUDE.md` maps to an operator-approved correction recorded in 56-TRIAGE.md (no un-approved edit).
    - DR-01: CLAUDE.md brand-history note matches db.service.ts IDB_NAME='trellis' (IndexedDB, not SQLite); `grep -qi "indexeddb" CLAUDE.md` finds the corrected note.
    - DR-02: CLAUDE.md yesterday-queue note matches post-queue.service.ts (no stale localStorage-write claim).
    - No load-bearing security documentation removed: `grep -c "enablejsapi=1" CLAUDE.md`, `grep -c "youtube-nocookie" CLAUDE.md`, `grep -c "RAW-ARGMAX" CLAUDE.md` each return ≥1.
    - No CLAUDE.md edit was applied to a claim the operator marked as a code-regression-to-investigate (those defer to a code task, not a doc edit).
  </acceptance_criteria>
  <done>CLAUDE.md is corrected only for operator-approved drifts with the exact approved wording, the operator has confirmed the diff, and all load-bearing security documentation remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

File-system doc moves + CLAUDE.md text edits only. No application code, network, or data flow changes. No new trust boundary introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Repudiation | Doc history preservation | mitigate | D-08: archival is `git mv` (rename), never delete — history is preserved and the move is auditable in `git status` as a rename, not a deletion. |
| T-56-09 | Tampering | CLAUDE.md load-bearing security documentation | mitigate | D-09 confirm-first gate: only operator-approved corrected wording from 56-TRIAGE.md is applied, and a blocking human-check confirms the diff. The Detector D YouTube postMessage origin allowlist, `enablejsapi=1`, and the Question-filter RAW-ARGMAX malicious-gate sections MUST remain present (grep-asserted ≥1) — a drift correction must never strip load-bearing security documentation. |
</threat_model>

<verification>
- Doc archival: `git status --porcelain Documents/ .planning/` shows renames, no deletions; Documents/Legacy/UI_AUDIT_REPORT.md exists; Documents/UI_AUDIT_REPORT.md absent at top level.
- CLAUDE.md: `grep -c "enablejsapi=1" CLAUDE.md`, `grep -c "youtube-nocookie" CLAUDE.md`, `grep -c "RAW-ARGMAX" CLAUDE.md` each ≥1; `git diff CLAUDE.md` hunks all map to approved corrections.
- No app/src change in this plan: `git status --porcelain app/src` clean.
</verification>

<success_criteria>
- All operator-approved stale docs archived via git mv (none deleted); keep-live docs untouched.
- CLAUDE.md corrected only for operator-approved drifts; load-bearing security documentation intact.
</success_criteria>

<output>
Create `.planning/phases/56-ui-polish-documentation/56-05-SUMMARY.md` when done.
</output>
