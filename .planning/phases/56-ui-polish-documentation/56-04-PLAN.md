---
phase: 56-ui-polish-documentation
plan: 04
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - app/src/screens/QuestionDetailScreen.tsx
  - app/src/screens/SavedScreen.tsx
  - app/src/screens/CollectionDrillInScreen.tsx
autonomous: true
requirements: [POLISH-03]

must_haves:
  truths:
    - "Every operator-APPROVED back-button mismatch is reconciled: the visual Header backTo destination and the Android hardware/gesture back destination resolve to the same logical parent (D-07 Android hardware/gesture back-button consistency across screens)"
    - "No per-screen Capacitor backButton listener is added — the single global App.tsx handler stays the only one"
    - "Header portal-vs-in-tree split is intact — no transform/will-change/filter/contain/perspective added to any Header ancestor"
    - "Only paths marked APPROVED/ADDED in 56-TRIAGE.md are touched"
  artifacts:
    - path: "app/src/screens/QuestionDetailScreen.tsx"
      provides: "Reconciled back destination (visual backTo aligned with hardware history pop)"
      contains: "backTo"
  key_links:
    - from: "Header backTo on a sub-screen"
      to: "Android hardware back (window.history.back via App.tsx listener)"
      via: "both resolve to the same logical parent"
      pattern: "backTo|navigate"
---

<objective>
Execute the operator-APPROVED POLISH-03 navigation / Android back-button consistency fixes from 56-TRIAGE.md. Reconcile each approved sub-screen where the visual Header `backTo` destination disagrees with the Android hardware/gesture back destination (which pops `window.history` via the single global App.tsx `backButton` listener), per the UI-SPEC Navigation & Back-Button Contract acceptance rubric (Outlet/Header parity: visual back-arrow and hardware back must go to the SAME destination). Touch ONLY the paths marked APPROVED/ADDED in 56-TRIAGE.md.

Purpose: D-07's top concern — back-button consistency across sub-screen Outlets. The audit (56-01) identified two concrete mismatch candidates (QuestionDetailScreen `backTo="/ask"` vs history; SavedScreen `backTo="/home"` vs history); the operator triaged them in 56-02. This plan fixes only what was approved, without regressing the load-bearing Header / overflow / resync invariants.

Output: reconciled back destinations in the approved subset of QuestionDetailScreen.tsx, SavedScreen.tsx, and (if approved) CollectionDrillInScreen.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-ui-polish-documentation/56-TRIAGE.md
@.planning/phases/56-ui-polish-documentation/56-UI-SPEC.md
@.planning/phases/56-ui-polish-documentation/56-RESEARCH.md
@.planning/phases/56-ui-polish-documentation/56-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile approved back-button mismatches (POLISH-03)</name>
  <files>app/src/screens/QuestionDetailScreen.tsx, app/src/screens/SavedScreen.tsx, app/src/screens/CollectionDrillInScreen.tsx</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-TRIAGE.md (which F-N* navigation findings are APPROVED; for each, the operator-chosen reconciliation direction — align backTo to history, or align history to a named parent, or leave as-is)
    - .planning/phases/56-ui-polish-documentation/56-UI-SPEC.md § "Navigation & Back-Button Contract" (acceptance rubric: rule 4 Outlet/Header parity, rule 3 no wrong paths, rule 6 settings sub-page back)
    - .planning/phases/56-ui-polish-documentation/56-RESEARCH.md § "Route Map" + § "Back-Button Audit Patterns" (the Back-Button Pattern Comparison table; the two flagged mismatch rows for QuestionDetailScreen and SavedScreen; the App.tsx:414-425 global handler description)
    - .planning/phases/56-ui-polish-documentation/56-PATTERNS.md § "Android Back-Button Handler" (single global App.tsx listener rule; back-button consistency concerns 1-3) and § "Header portal-vs-in-tree split"
    - CLAUDE.md § "Header positioning (Phase 32.1)" (portal-vs-in-tree; do not add transform/will-change/filter/contain/perspective to a Header ancestor)
    - app/src/screens/QuestionDetailScreen.tsx (current Header backTo="/ask" usage; entry paths /ask → /posts/:id → /ask/:id producing the mismatch)
    - app/src/screens/SavedScreen.tsx (current Header backTo="/home" usage)
    - app/src/screens/CollectionDrillInScreen.tsx (current Header backTo="/saved" usage — verify only if APPROVED in triage)
    - app/src/App.tsx (lines ~413-425 global backButton listener — read but DO NOT modify; per-screen listeners are forbidden)
    - app/src/components/ui/Header.tsx (backTo prop → navigate(backTo) named-route behavior)
    - app/tests/components/SwipeTabContainer.resize-guard.test.mjs (load-bearing guard — must stay green)
  </read_first>
  <action>
    Apply ONLY the F-N* navigation reconciliations the operator marked APPROVED in 56-TRIAGE.md, using the per-finding direction recorded there. Do NOT touch any path not APPROVED. The contract goal (UI-SPEC rule 4) is that a sub-screen's visual Header back-arrow and the Android hardware/gesture back resolve to the SAME logical parent.

    For each approved finding, apply the operator-recorded direction:
    - QuestionDetailScreen.tsx (F-N candidate): the visual back currently uses Header `backTo="/ask"` (a named route) while hardware back pops `window.history` — these disagree when the user arrived via `/ask` → `/posts/:id` → `/ask/:id` (hardware back lands on `/posts/:id`, visual goes to `/ask`). Apply the triage-approved reconciliation: EITHER change the Header to use the history-pop back affordance so visual and hardware agree (matching the `navigate(-1)` inline pattern PostDetailScreen/AnchorDetailScreen use), OR keep the named `/ask` parent and ensure the entry navigations into `/ask/:id` are `replace:true` so history's prior entry IS `/ask` — whichever direction 56-TRIAGE.md records. Do not invent a third direction.
    - SavedScreen.tsx (F-N candidate): currently `backTo="/home"`; SavedScreen is reachable from the HomeScreen feed but hardware back pops to `history[-1]`. Apply the triage-approved direction (align visual back to history pop, or confirm `/home` is always the correct logical parent and leave as-is if the operator marked it accept-as-correct).
    - CollectionDrillInScreen.tsx: only touch if 56-TRIAGE.md marks its `backTo="/saved"` finding APPROVED; otherwise leave unchanged.

    HARD INVARIANTS (automatic FAIL if violated): do NOT add a per-screen Capacitor `App.addListener('backButton')` — the single global listener in App.tsx is the only one (per 56-PATTERNS.md "Android Back-Button Handler"). Do NOT add `transform`/`will-change`/`filter`/`contain`/`perspective` to any Header ancestor while editing these screens (Header portal-vs-in-tree split). Do NOT modify App.tsx's global handler. Do NOT introduce a scroll region at the page root or remove any overflow-clip / scrollLeft-reset. If a reconciliation requires changing an entry navigation to `replace:true`, change it only at the approved call site and verify it does not exclude a screen the user must be able to hardware-back into.
  </action>
  <verify>
    <automated>cd app && tsc -b --noEmit && node --test tests/components/SwipeTabContainer.resize-guard.test.mjs tests/layout/root-horizontal-clip.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - For each APPROVED finding, the cited screen's visual back destination and the hardware/history back destination resolve to the same logical parent (source assertion: the exact backTo value and/or the exact navigate target match the operator-recorded direction in 56-TRIAGE.md).
    - No new `App.addListener('backButton')` appears in any screen file: `grep -rn "addListener('backButton'" app/src/screens` returns no results (the only such listener stays in App.tsx).
    - No Header ancestor in the touched files gained `transform`/`will-change`/`filter`/`contain`/`perspective` (grep the touched files).
    - App.tsx global handler is unmodified: `git status --porcelain app/src/App.tsx` shows no changes.
    - Findings the operator marked accept-as-correct or did NOT approve are left unchanged.
    - `cd app && tsc -b --noEmit` is clean and `node --test tests/components/SwipeTabContainer.resize-guard.test.mjs tests/layout/root-horizontal-clip.test.mjs` passes.
  </acceptance_criteria>
  <done>Every operator-APPROVED back-button mismatch is reconciled so visual and hardware back agree; the single global back handler and Header portal split are intact; tsc and load-bearing guard tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Navigation back-destination reconciliation only — visual `backTo` and history-pop targets aligned within the existing react-router-dom + single Capacitor `backButton` handler. No auth, no network, no new data flow, no new attack surface introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06 | (none — no new attack surface) | Sub-screen back destinations | accept | Pure client-side navigation reconciliation. No trust boundary crossed. Invariant to preserve: the single global App.tsx backButton listener (no per-screen listeners — teardown race) and the Header portal-vs-in-tree split. |
| T-56-07 | Tampering | PostDetailScreen / YouTubeEmbed (only IF a nav fix incidentally touches them) | mitigate | These files are NOT in this plan's scope; if any approved reconciliation requires editing a video-bearing screen, the Detector D YouTube postMessage origin allowlist (youtube.com / youtube-nocookie.com) and `enablejsapi=1` MUST remain intact (CLAUDE.md § "Video post completion signals"). Do not relax either. |
</threat_model>

<verification>
- Per task: `cd app && tsc -b --noEmit && node --test tests/components/SwipeTabContainer.resize-guard.test.mjs tests/layout/root-horizontal-clip.test.mjs tests/components/ChatInput.flex-shrink.test.mjs`.
- Per-wave merge: `cd app && npm test && tsc -b --noEmit`.
</verification>

<success_criteria>
- All operator-APPROVED POLISH-03 back-button reconciliations applied; nothing un-approved touched.
- Visual back-arrow and Android hardware back agree on destination for every fixed sub-screen (UI-SPEC rule 4).
- Single global back handler + Header portal split intact; full suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/56-ui-polish-documentation/56-04-SUMMARY.md` when done.
</output>
