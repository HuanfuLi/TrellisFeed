---
phase: 56-ui-polish-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-ui-polish-documentation/56-FINDINGS.md
  - .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, DOCS-01, DOCS-02]

must_haves:
  truths:
    - "A scored findings list exists covering all 18 screens at equal depth across the 6 polish pillars (D-01 hybrid discovery — agent audit precedes operator triage; D-02 all screens at equal depth)"
    - "Every existing animation keyframe is classified compositor-safe vs jank, with a remove-vs-simplify proposal for each jank candidate"
    - "Every route + entry point is walked and each back-button path is marked match / mismatch with evidence (D-06 full route-map audit; D-07 Android hardware/gesture back-button consistency is the top concern)"
    - "A CLAUDE.md drift report lists each load-bearing claim as verified-correct or drifted, with file:line evidence and a proposed resolution direction"
    - "A doc-staleness inventory marks each Documents/ and .planning/ doc as keep-live / archive-to-Legacy / update"
  artifacts:
    - path: ".planning/phases/56-ui-polish-documentation/56-FINDINGS.md"
      provides: "Scored polish/animation/navigation findings + doc-staleness inventory for operator triage"
      contains: "POLISH-01"
    - path: ".planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md"
      provides: "CLAUDE.md drift report with per-claim verification and proposed resolution"
      contains: "DR-01"
  key_links:
    - from: "56-FINDINGS.md"
      to: "56-02-PLAN.md operator triage"
      via: "each finding has a stable ID the operator approves/cuts"
      pattern: "F-[0-9]"
---

<objective>
Produce the candidate-findings artifacts that gate every Phase 56 fix. This is a READ-ONLY audit wave — it modifies NO source code and creates ONLY two report files. Three concurrent audit streams (POLISH-01 visual, POLISH-02 animation, POLISH-03 navigation) plus a doc-staleness inventory feed one consolidated `56-FINDINGS.md`. The DOCS-02 mechanical drift check produces a separate `56-CLAUDE-DRIFT-REPORT.md`.

Purpose: D-01 mandates hybrid discovery — agents audit, the operator triages, agents fix only approved items. No fix may land before the Wave-2 triage checkpoint. This plan creates the inputs that checkpoint consumes.

Output: `56-FINDINGS.md` (scored findings with stable IDs) and `56-CLAUDE-DRIFT-REPORT.md` (per-claim drift verification).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-ui-polish-documentation/56-CONTEXT.md
@.planning/phases/56-ui-polish-documentation/56-RESEARCH.md
@.planning/phases/56-ui-polish-documentation/56-UI-SPEC.md
@.planning/phases/56-ui-polish-documentation/56-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual + animation + navigation audit → 56-FINDINGS.md</name>
  <files>.planning/phases/56-ui-polish-documentation/56-FINDINGS.md</files>
  <read_first>
    - .planning/phases/56-ui-polish-documentation/56-UI-SPEC.md (Animation Contract, Navigation & Back-Button Contract, 6-Pillar rubric, Spacing Scale, Typography, Color)
    - .planning/phases/56-ui-polish-documentation/56-RESEARCH.md (Screen Inventory, Route Map, Animation Inventory, Jank Candidates, Back-Button Audit Patterns, Color Drift Candidates)
    - .planning/phases/56-ui-polish-documentation/56-PATTERNS.md (Load-Bearing Invariants, Jank Candidate fix patterns, prefers-reduced-motion gap)
    - app/src/index.css (all keyframes — read the inline-defined bodies the research left unverified: fruit-fly, vineLoadingPulse, saved-card-in)
    - All 18 screen files enumerated in 56-RESEARCH.md Screen Inventory, plus app/src/components/InfoFlow.tsx, app/src/components/trellis/TrellisStatusPanel.tsx, app/src/components/ui/Header.tsx, app/src/App.tsx
  </read_first>
  <action>
    Walk all 18 screens (5 swipe roots: HomeScreen, AskScreen, PlannerScreen, GraphScreen, SettingsScreen; plus PostDetailScreen, QuestionDetailScreen, AnchorDetailScreen, ClusterDetailScreen, CollectionDrillInScreen, ReviewScreen, PodcastScreen, SavedScreen, OnboardingScreen, and the 4 settings sub-screens) at EQUAL depth per D-02 — no high-traffic prioritization. Do a FRESH audit per D-03; do NOT consult the Apr-16 Documents/UI_AUDIT_REPORT.md.

    Produce a single 56-FINDINGS.md with four sections, each finding given a stable ID:
    - POLISH-01 visual findings (IDs F-V01, F-V02, …): score each against the 6 pillars (Copywriting, Visual hierarchy, Color, Typography, Spacing, Motion). For Color, flag the hardcoded-hex drift candidates the research found (PlannerScreen.tsx '#4CAF50' line 214, '#66BB6A' line 264; InfoFlow.tsx '#f59e0b' lines 392/608, '#ffffff' lines 734/752; Flashcard.tsx '#FF8A65'/'#FFD54F'/'#9CCC65'/'#558B2F' lines 25-28; CollectionPickerSheet.tsx '#fff' lines 414/452) and decide token-replacement vs accept-as-functional. Check dark-mode parity and es-locale text overflow. Cite file:line for each finding.
    - POLISH-02 animation findings (IDs F-A01, …): classify EVERY keyframe (the 23 in index.css + the inline-defined ones: status-glow, fruit-fly, vineLoadingPulse, saved-card-in, bounce, pulse, toast-in/out, btn-spin) as compositor-safe (transform/opacity only) or jank (animates box-shadow/filter/width/height/etc on a per-frame path). For each JANK candidate (glow-pulse, aha-pulse, glow-ring, status-glow), write a one-line remove-vs-simplify proposal per D-05 for the operator to decide. Flag the missing @media (prefers-reduced-motion: reduce) block in index.css as F-A-RM.
    - POLISH-03 navigation findings (IDs F-N01, …): walk every route in the App.tsx route tree + every entry point (swipe tabs, sub-screen Outlets, Header backTo, event-bus navigations heal→/review and replant→/posts/anchor-post-{id}, the * catch-all). For each sub-screen mark visual-back destination vs hardware-back (history.back) destination as MATCH or MISMATCH with the entry path that triggers the mismatch. Give special attention per D-07 to QuestionDetailScreen (backTo="/ask" vs history) and SavedScreen (backTo="/home" vs history). Confirm SettingsScreen's missing [location.pathname] resync is intentional or a finding. Check all replace:true navigations.
    - DOCS-01 doc-staleness inventory (IDs F-D01, …): mark each file in Documents/ and .planning/ top-level + .planning/codebase/ as keep-live / archive-to-Legacy / update, citing date and reason. Pre-seed from the research Document Inventory (UI_AUDIT_REPORT.md → Legacy per D-03; CHANGELOG_4_05/SUMMARY_4_05/4_16 → Legacy; CHANGELOG_5_20, EMAIL-DRAFT-PROFESSOR, LANDING-VIDEO-SCRIPT → keep; .planning/v1.1-MILESTONE-AUDIT.md, v1.3-INTEGRATION-CHECK.md, v1.3-MILESTONE-AUDIT.md → .planning/milestones/; CONVENTIONS.md "Tailwind CSS" line → operator decision update-vs-note).

    Bias per the locked specifics: if a feed-tile (InfoFlow.tsx) finding arises, propose SIMPLIFICATION (fewer signals) not added chips. Do NOT propose any new motion, micro-interaction, streak/goal/nudge, or redesign. Every finding must be operator-triageable (approve/cut/add) — write them as discrete, scoped, ID'd items, not prose.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-ui-polish-documentation/56-FINDINGS.md && grep -q "F-V01" .planning/phases/56-ui-polish-documentation/56-FINDINGS.md && grep -q "F-A" .planning/phases/56-ui-polish-documentation/56-FINDINGS.md && grep -q "F-N" .planning/phases/56-ui-polish-documentation/56-FINDINGS.md && grep -q "F-D" .planning/phases/56-ui-polish-documentation/56-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 56-FINDINGS.md exists with four ID-prefixed sections (F-V*, F-A*, F-N*, F-D*).
    - All 18 screens appear by name in the visual section.
    - Every JANK animation candidate (glow-pulse, aha-pulse, glow-ring, status-glow) has a remove-vs-simplify proposal line; F-A-RM flags the missing prefers-reduced-motion block.
    - Every sub-screen in the Route Map has a MATCH/MISMATCH back-button verdict citing the entry path.
    - No source files were modified (this is read-only): `git status --porcelain app/src` shows no changes.
    - No finding proposes new motion, a redesign, or pushy mechanics.
  </acceptance_criteria>
  <done>56-FINDINGS.md is a complete, ID'd, operator-triageable candidate list across POLISH-01/02/03 and the DOCS-01 doc inventory; no app source touched.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md drift report → 56-CLAUDE-DRIFT-REPORT.md</name>
  <files>.planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md</files>
  <read_first>
    - CLAUDE.md (all load-bearing sections, especially Brand history note, Concept Feed Pipeline numeric defaults + Yesterday-queue snapshot, Video post Detector D, Header positioning, Root overflow clip, SwipeTabContainer, ChatInput, Question filter)
    - .planning/phases/56-ui-polish-documentation/56-RESEARCH.md (CLAUDE.md Drift Verification table — DR-01, DR-02, V-01..V-15, C-01..C-04)
    - app/src/services/db.service.ts (IDB_NAME, stale-key purge list)
    - app/src/services/post-queue.service.ts (SQLITE_ROW_ID_YESTERDAY, MAX_QUEUE_SIZE, REFILL_THRESHOLD, walker maxSteps)
  </read_first>
  <action>
    Mechanically verify each load-bearing CLAUDE.md claim against current code using grep/source-reading per D-09 (this is a REPORT only — make NO edit to CLAUDE.md or to code). Produce 56-CLAUDE-DRIFT-REPORT.md as a table with columns: claim ID, CLAUDE.md claim (quoted), actual code value, file:line evidence, verdict (DRIFTED / VERIFIED), proposed resolution direction (stale-doc-update vs code-regression), and a confirm-required flag.

    Confirm the two known drifts with fresh grep evidence: DR-01 (Brand history "SQLite connection name 'echolearn' in db.service.ts" vs actual IDB_NAME='trellis' at db.service.ts:190 — no SQLite, migrated to IndexedDB in Phase 55) and DR-02 ("STORAGE_KEY_YESTERDAY='trellis_post_queue_yesterday' written by postQueueService.load()" vs actual SQLITE_ROW_ID_YESTERDAY='queue_yesterday' in IndexedDB at post-queue.service.ts:29; the old localStorage key appears only in db.service.ts:351 stale-key purge list, deleted not written).

    Re-verify and record as VERIFIED the no-drift claims (MAX_QUEUE_SIZE=32, REFILL_THRESHOLD=24, walker maxSteps=Math.max(count*2,len), enablejsapi=1 count ≥1, ANCHOR_PRE_CHECK_SIMILARITY_THRESHOLD=0.82, MALICIOUS_FLOOR_MIN/MAX=0.35/0.70, OFF_TOPIC_MARGIN=0.02, html/body overflow:hidden, App root overflowX, onFocusOut scrollLeft reset, resync width-gate, ChatInput minWidth:0, USER_ACK_BEFORE_GRAPH_CONTEXT, Header portal split).

    Investigate the candidate drifts C-01 (postQueueService.load() vs normalizeState() function-name drift), C-02 (BASE_ENTRIES_PER_CONCEPT location), C-03 (Concept Feed Pipeline function-name drift post-Phase-55), and record each as DRIFTED or VERIFIED with evidence. Do NOT verify C-04 news-pipeline claims (out of polish scope) — note it as deferred.

    Each DRIFTED row must carry a confirm-required flag because D-09 forbids silent auto-correction. State a recommended direction (most are stale-doc-update) but mark it as awaiting operator approval.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md && grep -q "DR-01" .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md && grep -q "DR-02" .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md && grep -qi "trellis" .planning/phases/56-ui-polish-documentation/56-CLAUDE-DRIFT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 56-CLAUDE-DRIFT-REPORT.md exists with a per-claim table including DR-01, DR-02, the V-* verified claims, and C-01..C-03 verdicts.
    - DR-01 row cites db.service.ts:190 IDB_NAME='trellis' as evidence; DR-02 row cites post-queue.service.ts:29 and db.service.ts:351.
    - Every DRIFTED row has a confirm-required flag and a proposed resolution direction.
    - CLAUDE.md is unmodified: `git status --porcelain CLAUDE.md` shows no changes.
    - No app source modified.
  </acceptance_criteria>
  <done>56-CLAUDE-DRIFT-REPORT.md is a complete confirm-first drift report ready for the Wave-2 operator approval gate; neither CLAUDE.md nor code edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a read-only audit wave. It produces two Markdown report files under `.planning/` and modifies no application code, no network surface, no data flow. No new trust boundary is introduced.

| Boundary | Description |
|----------|-------------|
| (none new) | Audit reads source + docs; writes only planning reports. No runtime code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | Audit accuracy of security-adjacent invariants | mitigate | The drift report MUST record (not weaken) the Detector D YouTube postMessage origin allowlist (youtube.com / youtube-nocookie.com) and the enablejsapi=1 requirement as VERIFIED invariants — flag any erosion as a code-regression, never propose relaxing them. |
| T-56-02 | Information disclosure | npm/pip/cargo installs | accept | No package installs in this phase; no audit table required. |
</threat_model>

<verification>
- `cd app && node --test tests/layout/root-horizontal-clip.test.mjs tests/components/ChatInput.flex-shrink.test.mjs tests/components/SwipeTabContainer.resize-guard.test.mjs` stays green (this wave changes no source, so it must remain green unchanged).
- Both report artifacts exist and carry their required IDs.
</verification>

<success_criteria>
- 56-FINDINGS.md and 56-CLAUDE-DRIFT-REPORT.md exist with the structures above.
- No app/src file and no CLAUDE.md edit in this wave (`git status --porcelain app/src CLAUDE.md` clean except the new planning reports).
</success_criteria>

<output>
Create `.planning/phases/56-ui-polish-documentation/56-01-SUMMARY.md` when done.
</output>
