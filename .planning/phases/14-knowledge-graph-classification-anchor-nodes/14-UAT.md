---
status: diagnosed
phase: 14-knowledge-graph-classification-anchor-nodes
source: [14-01-SUMMARY.md, 14-02-SUMMARY.md, 14-03-SUMMARY.md]
started: 2026-03-29T20:00:00Z
updated: 2026-03-29T20:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  1. Close any running 'npm run dev' processes.
  2. Clear browser local storage (for localhost:5173).
  3. Run 'npm run dev' in the /app directory.
  4. Open the app in the browser.
  5. The app should load the home/ask screen without errors.
result: pass

### 2. Two-Call Classification & Anchor Creation
expected: |
  1. Ask a question (e.g., "How does photosynthesis work?").
  2. Wait for the answer to complete.
  3. Behind the scenes (check console or wait a few seconds), the second classification call should fire.
  4. Navigate to the Graph screen.
  5. A concept anchor node (e.g., "Photosynthesis") should be created under the appropriate cluster.
result: issue
reported: "No, I don't see the second call for classification. I see: First call for answering questions, second call for generating title, third call for generating flashcards, fourth call for generating posts. When I checked Graph page, it is not classified as expected: All concepts are placed under 1 chunk under 1 branch: Root(Knowledge) -> General Concepts -> Open questions -> Questions (in short title generated by AI second call)."
severity: major

### 3. Mindmap Anchor Rendering (Collapsed)
expected: |
  1. Open the Graph screen.
  2. Locate a concept anchor node.
  3. It should be rendered as a leaf node with a title like "Concept Name (1)".
  4. It should have a '+' icon indicating it is collapsed and has children.
result: issue
reported: "No. It is not rendered as a brief concept at all, it is still rendered as short title of question, such as 'The Feynman technic in simple terms'. Also, there is no '+' icon at all, cannot expand/collapse."
severity: major

### 4. Anchor Expansion & Q&A Children
expected: |
  1. Tap the '+' icon on a concept anchor node in the Graph.
  2. It should expand to reveal the Q&A node(s) attached to it.
  3. Tapping the '-' icon should collapse it back.
result: issue
reported: "Not at all. There is no '+' icon, anchor nodes are not interactove at all, cannot expand/collapse. Probably the use of Mind-Elixir is wrong."
severity: major

### 5. Anchor Detail Panel
expected: |
  1. Tap on the label of a concept anchor node (not the toggle).
  2. The bottom detail panel should appear.
  3. It should display "CONCEPT ANCHOR — N Q&As" at the top.
  4. The "View details" chevron should NOT be present.
  5. Clicking the panel should NOT navigate to the Q&A detail screen.
result: issue
reported: "No, the bottom detail panel is not showing CONCEPT ANCHOR and N Q&As. It seems the concept anchor is totally mis-implemented. Huge gap here. View Details chevron still present. Clicking the panel navigated to Q&A detail screen."
severity: major

### 6. Backward Compatibility (Legacy Q&As)
expected: |
  1. Ensure there are existing Q&A nodes in local storage that do not have a parentId (legacy data).
  2. Open the Graph screen.
  3. These legacy Q&A nodes should still appear as direct leaves under their respective clusters, just like before Phase 14.
result: pass

## Summary

total: 6
passed: 2
issues: 4
pending: 0
skipped: 0

## Gaps

## Gaps

- truth: |
    1. Ask a question (e.g., "How does photosynthesis work?").
    2. Wait for the answer to complete.
    3. Behind the scenes (check console or wait a few seconds), the second classification call should fire.
    4. Navigate to the Graph screen.
    5. A concept anchor node (e.g., "Photosynthesis") should be created under the appropriate cluster.
  status: failed
  reason: "User reported: No, I don't see the second call for classification. I see: First call for answering questions, second call for generating title, third call for generating flashcards, fourth call for generating posts. When I checked Graph page, it is not classified as expected: All concepts are placed under 1 chunk under 1 branch: Root(Knowledge) -> General Concepts -> Open questions -> Questions (in short title generated by AI second call)."
  severity: major
  test: '2'
  root_cause: "askStreaming in useQuestions.ts completely misses the classifyAndAnchor call that was added to ask in question.service.ts. Because askStreaming is the primary entrypoint used by AskScreen, second-call classification never fires."
  artifacts:
    - path: "app/src/state/useQuestions.ts"
      issue: "Missing classifyAndAnchor call inside askStreaming"
  missing:
    - "Add classifyAndAnchor call to askStreaming when !question.flagged"

- truth: |
    1. Open the Graph screen.
    2. Locate a concept anchor node.
    3. It should be rendered as a leaf node with a title like "Concept Name (1)".
    4. It should have a '+' icon indicating it is collapsed and has children.
  status: failed
  reason: "User reported: No. It is not rendered as a brief concept at all, it is still rendered as short title of question, such as 'The Feynman technic in simple terms'. Also, there is no '+' icon at all, cannot expand/collapse."
  severity: major
  test: '3'
  root_cause: "Downstream of Test 2. Because classifyAndAnchor never fires, Anchor nodes are never created. The GraphScreen therefore renders all Q&As as legacyNodes (which don't have children or a '+' icon) instead of anchors."
  artifacts:
    - path: "app/src/screens/GraphScreen.tsx"
      issue: "Graph falls back to legacyNodes rendering because anchor nodes never exist"
  missing:
    - "Fix classifyAndAnchor trigger in useQuestions to resolve this"

- truth: |
    1. Tap the '+' icon on a concept anchor node in the Graph.
    2. It should expand to reveal the Q&A node(s) attached to it.
    3. Tapping the '-' icon should collapse it back.
  status: failed
  reason: "User reported: Not at all. There is no '+' icon, anchor nodes are not interactove at all, cannot expand/collapse. Probably the use of Mind-Elixir is wrong."
  severity: major
  test: '4'
  root_cause: "Downstream of Test 2. See Test 3 root cause."
  artifacts:
    - path: "app/src/screens/GraphScreen.tsx"
      issue: "No anchors exist to be expanded"
  missing:
    - "Fix classification trigger"

- truth: |
    1. Tap on the label of a concept anchor node (not the toggle).
    2. The bottom detail panel should appear.
    3. It should display "CONCEPT ANCHOR — N Q&As" at the top.
    4. The "View details" chevron should NOT be present.
    5. Clicking the panel should NOT navigate to the Q&A detail screen.
  status: failed
  reason: "User reported: No, the bottom detail panel is not showing CONCEPT ANCHOR and N Q&As. It seems the concept anchor is totally mis-implemented. Huge gap here. View Details chevron still present. Clicking the panel navigated to Q&A detail screen."
  severity: major
  test: '5'
  root_cause: "Downstream of Test 2. The detail panel for Anchor-specific properties doesn't show because the node isn't an anchor."
  artifacts:
    - path: "app/src/screens/GraphScreen.tsx"
      issue: "Detail panel logic checks isAnchorNode which is false for legacy Q&As"
  missing:
    - "Fix classification trigger"
