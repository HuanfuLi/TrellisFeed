# PROJECT: Trellis (formerly EchoLearn)

## What This Is

Trellis is an AI-powered personalized learning platform designed to facilitate non-linear knowledge acquisition through AI-driven content generation, visual knowledge mapping, and spaced repetition. It bridges the gap between passive content consumption and active, long-term learning.

The platform prioritizes a high-quality, native-first mobile experience built with React 19, TypeScript 5.9, Vite 7, Tailwind CSS 4, and Capacitor 8, combining local-first privacy with seamless AI integration (OpenAI, Claude, Gemini, local endpoints like LM Studio).

**Brand history:** Renamed EchoLearn → Trellis on 2026-05-07 (commit `9e5d1f38`). On-disk directory and SQLite connection name `'echolearn'` are intentionally preserved for backwards compatibility. localStorage keys migrated `echolearn_*` → `trellis_*` via `legacy-migration.service.ts` runtime migration.

## Core Value

Enable learners to transform fragmented information into structured knowledge through AI-driven Q&A, visual mapping, and adaptive spaced repetition — all while maintaining complete local-first privacy.

## Current Milestone: v1.5 Curiosity Feed v2 + Tech-Debt Hardening

**Goal:** Ship the second-iteration curiosity feed (Pinterest-style masonry layout, richer essays, source diversity, engagement signals) on top of a fully-hardened codebase that closes all v1.4 carry-overs plus a broader hygiene sweep.

**Target features:**

*A. Curiosity Feed v2 — quality*
- Pinterest-style masonry feed layout (variable-height tiles, 2-column flow, visual mosaic)
- Richer post body essays (longer / tighter source grounding / citation render polish)
- Source diversity (multi-domain, multi-media per concept, no near-duplicates, source quality scoring)
- Engagement signals (like / save / dismiss, social-proof affordances, content-quality feedback loop) — local-only annotations to preserve local-first privacy

*B. Tech-Debt Hardening — broader hygiene*
- v1.4 carry-overs: i18n leaf-module refactor (closes 10 carried test failures), VALIDATION drift cleanup (34/35), ROADMAP plan-list polish (36-14 + 36-15 bullets), 33-HUMAN-UAT-1/2 device retest, CLAUDE.md `echolearn_*` doc-drift cleanup, YouTube landscape-listed-as-short bug
- Broader hygiene: tsc strictness audit, dependency-version sweep, dead-code sweep, perf profiling, project-wide TODO/FIXME triage, sweep of operator-note bugs

## Previous Milestones

- **v1.0** — phases 5-6 (foundation UI polish, question quality evaluation)
- **v1.1** — phases 7-9 (image-forward feed, post detail, infinite scroll)
- **v1.2** — phases 10-19 (Planner auto-suggestions, knowledge graph classification, token optimization, feed expansion, web search)
- **v1.3 (pre-release)** — phases 20-27 (orchestration, streaming, swipe nav, incremental classification, Trellis, harvest actions, i18n/L10n)
- **v1.4** — phases 28-36 (UI/UX polish + curiosity feed redesign + Ask-chat KV-cache prefix preservation + concept feed pipeline gap closure + EchoLearn → Trellis rebrand) — see `.planning/milestones/v1.4-MILESTONE-AUDIT.md`

## Key Decisions

- **Local-First Privacy:** All user data persists locally via localStorage/SQLite. No backend required.
- **LLM Flexibility:** Support multiple providers (OpenAI, Claude, Gemini, local endpoints like LM Studio).
- **Visual-First UX:** Post feeds emphasize images and hooks (questions/stories) to drive engagement.
- **Adaptive Recommendations:** Planner logic respects user trajectory, review performance, and engagement patterns.
- **Concept feed pipeline (v1.4 / Phase 36):** Three-list architecture — Daily Concept List (anchor nodes filtered by SM-2 due dates) → Derived List (append-only, weighted, persistent across refills with cyclePosition) → Queue (32-max cyclic walker, 4 posts per swipe, refill threshold 16, Promise-mutex guarded). Lazy-skip explored anchors at walk time; stratified style allocation (largest-remainder + Fisher-Yates); spreadByConcept mixer before spreadByStyle. See CLAUDE.md "Concept Feed Generation Pipeline" load-bearing section.
- **Ask-chat KV-cache prefix (v1.4 / Phase 35):** System prompt is byte-stable across turns (identity + safety + WEB_SEARCH_TOOL_PROMPT only); per-turn graph context lives in tail-position assistant message after history; USER_ACK_BEFORE_GRAPH_CONTEXT user message inserted between for strict-alternation chat templates (Qwen via LM Studio). See CLAUDE.md "Ask-chat system prompt — byte-stable across turns" load-bearing section.
- **Always-mounted screen state-resync principle (v1.4 / Phase 36-14):** SwipeTabContainer mounts all 5 first-level screens once at boot; any screen reading from a service whose state can change while another screen is in foreground MUST add a `[location.pathname]` `useEffect` to re-sync on navigation. HomeScreen is the canonical pattern (vine progress + warm-start re-fallback). Documented in CLAUDE.md.
- **Leaf-module pattern for testability (v1.4 / Phase 36):** Services that pull `src/locales/index.ts` transitively can't load under `node --test` (Vite-only `import.meta.env.DEV`). Pure-logic helpers extracted into leaf modules (`feed-spread.ts`, `refill-mutex.ts`) so tests can exercise behavior without dragging in the i18n chain. Pattern carried to v1.5 for systemic i18n cleanup.
- **Test runner architecture (v1.4 audit):** `npm test` chains `test:main; test:actions`. The latter registers `_actions-mock-loader.mjs` via `--import` for trellis-actions tests (heavy LLM/SQLite/i18n stubs). Cannot register globally because that breaks `web-search.test.mjs` (needs real settings.service).

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---

**Phase 14 complete (2026-03-29):** Knowledge Graph Classification & Anchor Nodes — dedicated second LLM classification call, concept anchor nodes (isAnchorNode), Q&A attachment via parentId, mindmap renders anchors as collapsed leaves with expand/retract Q&A children. GRAPH-01 through GRAPH-06 validated.

**Phase 23 complete (2026-04-09):** Incremental Classification Pipeline + Ask Rate Limiter — replaced single-call classification with 3-step branch→cluster→anchor pipeline using append-only messages for KV cache efficiency. Added configurable monthly rate limiter with Settings UI, inline warning/error banners, and send button disable at limit.

**Phase 26 complete (2026-04-15):** Trellis Harvest Panel + Node Actions + Suggested Moves Refactor — Planner now shows a flat 3-column status panel (Dying | Fruit | Dead) with Fruit as a centered harvest button (direct tap, fly-to-counter + confetti, credits persisted). Suggested Moves refactored to be trellis-health-driven (dead → dying → filtered autoGen); heal, re-plant, and prune actions live directly on the rows. Re-plant reuses the "Learn as Post" flow (fire-and-forget, bumps node to dying state). PrunedSection archive at page bottom. UX simplified from the originally-planned bottom-sheet design per user feedback (D-09/D-14/D-17 voided, D-13 modified — see `.planning/phases/26-*/26-ADDENDUM.md`). Also landed cross-cutting fix: BottomNavigation tap = instant transport (reverted from Phase 22's animated jump), finger-swipe keeps the spring; all 5 first-level screens always mounted + always visible.

**Phase 27 complete (2026-04-16):** i18n/L10n support — 4 locales shipped (EN canonical + Simplified Chinese + Spanish + Japanese). `i18next` + `react-i18next` with synchronous init in `main.tsx`; 602 flattened keys across all 4 bundles (ZH 92.7% / ES 90.2% / JA 92.4% leaf coverage — remainder are intentional EN retention for proper nouns, provider names, LLM system prompts per D-07). Centralized locale injection: LLM provider prepends `applyLocaleDirective` to system prompts (D-12), TTS maps locale→voice (D-13), YouTube forwards `hl`/`regionCode` (D-14), Tavily web-search stays English by design (D-15), dates use `Intl.DateTimeFormat` (D-11). User flow: onboarding Language step (4-option script picker with cross-locale `Language / 语言 / Idioma / 言語` header, D-18), Settings locale switcher with instant swap (D-19), mid-stream LLM abort on `LOCALE_CHANGED` event (D-22 — `AbortController` shared across Pass 1/2 streams), locale-specific CSS font stacks via `:root[data-locale]` overrides (D-23). Workflow codified: root `CLAUDE.md` requires syncing all 4 bundles when adding UI strings; `app/scripts/translate-locales.md` is the Sonnet subagent template for future re-translations (D-09). Dev-time translation only — runtime LLM translation is prohibited (D-07). Operator UAT walkthrough approved (D-24).

**Milestone v1.3 (pre-release) complete (2026-04-16):** Phases 20-27 archived as milestone v1.3. Audit: `.planning/v1.3-MILESTONE-AUDIT.md`. Phase archives moved to `.planning/milestones/v1.3-phases/`. Phase 28 queued as first phase of v1.4. Four phases carry `human_needed` verification status (20, 21, 22, 26) — automated checks pass; visual/timing/feel QA on device is the pending work if v1.4 decides to close those gaps.

**Phase 35 complete (2026-04-29, empirically closed via UAT round 2):** Ask-chat system-prompt stability — moved per-turn `formatCandidateContextPack` interpolation out of the system role into a tail-position assistant message in both Pass 1 and Pass 2 of `useQuestions.askStreaming`. Provider KV-cache prefix now covers `[system, ...history]` across chat turns instead of breaking at the first dynamic byte. UAT round 1 surfaced a CONTEXT.md D-08 risk: Qwen 3.5 via LM Studio rejected the assistant-before-user shape with "No user query found in messages" jinja error. Plan 35-05 inserted a constant byte-stable `USER_ACK_BEFORE_GRAPH_CONTEXT` user message between history and the assistant context, restoring user→assistant→user alternation while preserving cache properties (constant ack stays inside the byte-stable prefix). UAT round 2 confirmed all 3 tests pass on Qwen via LM Studio. Source-reading invariant test (`useQuestions-system-prompt-stability.test.mjs`, 6/6 green) prevents future drift on both invariants. CLAUDE.md gained a new "Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)" section with strict-alternation rationale documenting the LM Studio incident. Phase 32.1 lesson #8 (documentation in three places: inline comment + test + CLAUDE.md) applied. Project-wide chatStream/chatCompletion audit confirmed 24/26 call sites are intentionally one-shot; Phase 23 classification descent in `canonical-knowledge.service.ts` remains append-only. Closes the public self-disclosure in `LabPresentation/SCRIPTS.md` slide 4.7 — line can flip from "I haven't shipped the fix yet" to past tense.

**Phase 36 complete (2026-05-06, verifier 13/13 must-haves + 14/14 gap-closure must-haves after UAT round 1):** Curiosity-feed randomness/weights gap closure — closed four design-drift gaps between live code and CLAUDE.md "Concept Feed Generation Pipeline" load-bearing section. (GAP-1) Derived list promoted from per-call ad-hoc array to persistent `derivedList: string[]` field in `QueueState` localStorage payload, with append-only semantics across same-day refill cycles. (GAP-2) New `walkDerivedList(count, exploredIds)` cyclic walker advances a persisted `cyclePosition` index, lazy-skipping explored conceptIds via `dailyReadService.getExploredAnchors()` at walk time (not array splicing — the index-stable approach avoids cyclePosition corruption). (GAP-3) i.i.d. style sampling replaced with Hamilton's largest-remainder allocation + Fisher-Yates shuffle in `style-assignment.ts`, guaranteeing `round(N×w_style)` of each available style ±1 per N-entry batch — small batches no longer starve image/news/video/short. (GAP-4) New `spreadByConcept` mixer (in new leaf module `feed-spread.ts` — workaround for the i18n JSON-import-attribute chain that prevented test imports through `concept-feed.service.ts`) wires into `enqueueInterleaved` BEFORE the existing `spreadByStyle`. Two-branch placement algorithm (skip-position formula for dominant buckets, half-stride for balanced) provably keeps max-run ≤ 2 even when one important anchor monopolizes 6 of 8 entries. (GAP-6) CLAUDE.md doc-sync: `MAX_QUEUE_SIZE = 32` documented, `appendToDerivedList` cross-referenced, lazy-skip described, GAP-1/3/4 struck from "Known divergences" with closure annotations. GAP-5 (variable-count vs strict-4 per swipe) intentionally out of scope — generateMorePosts default is 4 and the only caller passes 4, so the loose contract is empirically tight. Phase 33 fixes preserved: `dueAnchors` filter at concept-feed.service.ts:798 + `allExplored` cap-gate at 1265-1267 verified intact. 33 new passing tests across 4 files (derived-list 10, style-assignment-stratified 10, spread-by-concept 7, refill-queue-integration 6); npm test baseline preserved at 422 pass / 26 pre-existing JSON-import-attribute fail. Plan 36-00 wrote Wave-0 RED tests FIRST (per Phase 32.1 lesson #2 — tests must guard the LIVE code path), Waves 1-3 flipped them to GREEN. Two checker-iteration-1 blockers landed correctly: (a) appendToDerivedList uses seed-once-before-loop `existing` variable with NO in-loop mutation so first-call multiplicity (8 entries for an important anchor) is preserved while across-call dedup still works, (b) integration-smoke imports STYLE_WEIGHTS from style-assignment.ts (drift-proof, negative-grep enforced) instead of redeclaring inline.

**Phase 36 gap closure (2026-05-06, verifier 14/14):** UAT round 1 surfaced 3 gaps after the initial 13/13 close-out — (GAP-A blocker) cold-start empty feed because HomeScreen's useEffect overwrote `getYesterdayQueue()` warm-start state with `setDailyPosts([])` AND fired `setGenerationError(true)`; (GAP-B major Phase 36 regression) `walkDerivedList` `maxSteps = len * 2` cap silently truncated 16-entry walk requests to 8 entries on single-anchor lists, defeating Phase 36-01 stratification (text-art pinned at 50% floor); (GAP-C major pre-existing) video and short posts had ZERO completion-signal paths because YouTube iframes lacked `enablejsapi=1` and shorts never navigated to PostDetailScreen. Gap-closure plans 36-06/07/08 closed all three across two waves: (06) `useRef(dailyPosts.length > 0)` snapshot pattern (StrictMode-safe, no nested setState); (07) `Math.max(count * 2, len)` plus regression-sentinel test for the missed N=16 case; (08) `enablejsapi=1` on all 3 iframe srcs + Detector D postMessage listener (origin allowlist + ENDED + 80% heartbeat) + InfoFlow short tap-to-play emit. 17 new GREEN tests (4 warm-start guard + 3 walker + 6 video detector + 4 short tap emit), Phase 33 grep sentinels intact, Phase 35 byte-stable system prompt invariant intact. Lessons: (1) integration tests with `count << maxSteps` give false confidence — Plan 36-04's smoke at `walkDerivedList(2, 4-entry)` missed the cap defect; the regression sentinel `walkDerivedList(16, 4-entry)` is now the standing guard. (2) Pre-existing drift gaps (A + C) surfaced ONLY because gap-closure was scoped tightly enough to bring them into UAT focus — no new code wrote them. (3) Phase 32.1 lesson #2 ("tests must guard the LIVE code path") proved its weight twice in one phase: 36-00's RED-first discipline caught Phase 36-03's would-be regressions, but the smoke-only N didn't catch GAP-B until UAT.

**Phase 36 gap closure round 2 (2026-05-07, verifier 16/16):** UAT round 2 (after dev-only "Force new day" affordance landed) surfaced GAP-D — the snapshot Plan 36-06 read on cold-start was being destroyed by the very next `save()` because both halves wrote to the same localStorage key. Plan 36-09 split storage into `STORAGE_KEY_LIVE` + `STORAGE_KEY_YESTERDAY` so the snapshot is durable across multiple cold-start mounts of a new day. Plan 36-10 added the dev-only `import.meta.env.DEV`-gated "Force new day" SettingRow under Settings → Data → Developer with one-tap localStorage rollback + `postQueueService.loadQueue()` reload + `navigate('/home')` so future GAP-A/D regressions can be retested without waiting for a real midnight rollover. UAT round 2 retest 3/3 pass on Qwen via LM Studio.

**Phase 36 gap closure round 3 (2026-05-07, verifier 17/17):** UAT round 3 surfaced 5 sub-issues from the Force-New-Day path: (a) vine progress chip not cleared after rollover (`dailyReadService.loadState`'s self-reset depends on real `today()` advancing — dev button cannot trigger it); (b) empty cold-start feed (Plan 36-09's `STORAGE_KEY_YESTERDAY` snapshot was orphaned — preserved but no consumer pushed it back into the live queue); (c) style imbalance on cold-start (yesterday's leftover queue is structurally minority-style-biased because plurality text-art gets popped first); (d) double-rollover regressed to served posts (`loadCache()` was date-independent); (e) queue not auto-topping during rapid swipe (boolean mutex `_queueRefillRunning` made `await refillQueue()` callers no-op when in-flight). Three round-3 plans closed all five: (11) `loadCache()` rejects when `cached.date !== today()` + `load()` rehydrates `_state.posts/derivedList/cyclePosition` from yesterday's snapshot + `spreadByConcept`+`spreadByStyle` re-interleave restores style balance; (12) Promise-mutex via leaf module `refill-mutex.ts` (await-on-in-flight instead of bail) + `REFILL_THRESHOLD` 12 → 16 (forward-looking buffer for planned double-column feed); (13) `handleForceNewDay` calls `dailyReadService.reset()` + reverts the now-redundant dual-cache hack from commit `6a90224a`. 17 new GREEN tests across 5 files (post-queue-rehydrate 5, concept-feed-cache-date 3, refill-mutex 9, force-new-day Test 5+6, post-queue threshold-test rewrite). Pre-wave npm test baseline: 35 fail / 459 pass; post-wave: 26 fail / 469 pass — wave net-improved by 9 tests with zero new failures.

**Phase 36 gap closure round 4 (2026-05-07, verifier 16/16):** UAT round 4 surfaced 2 of round 3's 5 sub-issues regressing — both (a) vine chip and (b) feed auto-populate failed at runtime DESPITE Plan 36-13 + Plan 36-11's source-reading and unit tests passing. Two interaction bugs invisible to unit tests: (1) HomeScreen's `exploredAnchors` `useState` initializer is mount-frozen and the only post-mount setter is wired to `CONCEPT_EXPLORED`; HomeScreen is one of 5 always-mounted SwipeTabContainer slots so `navigate('/home')` doesn't remount it; `dailyReadService.reset()` writes localStorage but emits no event-bus signal — persistence cleared, React state stale. (2) Plan 36-13's revert of the `echolearn_daily_posts.date` mutation framed it as a "redundant dual-cache hack" — but `loadCache()`'s `parsed.date !== today()` rejection only fires when the wall clock advances, which the dev button cannot do; so the cache stayed valid, `getDailyPosts()` short-circuited on cache-hit, and Plan 36-11's rehydrated `_state.posts` was unreachable. Two round-4 plans closed both: (14) HomeScreen sibling `[location.pathname]` effect re-syncs `exploredAnchors` + `creditAwardedRef.current` after Force New Day; same effect bundle widens the existing nav effect with tier-1 (`getCachedDailyPosts`) → tier-2 (`getYesterdayQueue`) warm-start fallback so yesterday's UNSERVED queue surfaces on `/home` navigation. (15) `handleForceNewDay` reinstates the `echolearn_daily_posts.date = yesterday` mutation alongside the existing post-queue mutation; Test 6 inverted in place from negative-assertion to positive (anchor-pair extracted). 7 new GREEN tests in 36-14's two new files; Test 6 inversion + 5 preserved tests in 36-15. Pre-wave npm test baseline: 26 fail / 469 pass; post-wave: 26 fail / 476 pass — wave net-improved by 7 tests with zero new failures. Lessons surfaced: (i) source-reading tests asserting "the call exists" can ship false-confidence — Plan 36-13's Test 5 passed on `dailyReadService.reset()` presence but missed the consumer-side staleness; both 36-14 test files use anchor-pair extraction so the assertion captures the actual containing scope (the meta-rule pattern from Plan 36-13's negative regression test). (ii) the "dev button must mimic every wall-clock side effect" pattern (round 3) extends to "every always-mounted screen consuming that service must re-sync on navigation" (round 4) — single source of asymmetry, two layers of defense. CLAUDE.md got a new bullet documenting this principle under "Concept Feed Generation Pipeline → Numeric defaults".

**Milestone v1.4 complete (2026-05-08):** Phases 28-36 archived as milestone v1.4. Audit: `.planning/milestones/v1.4-MILESTONE-AUDIT.md` (status: tech_debt — 16 of 26 v1.3-carried test failures closed; 10 architectural carry-overs to v1.5). Phase archives moved to `.planning/milestones/v1.4-phases/`. Test baseline at close: 570 / 560 pass / 10 fail / tsc + vite clean. Audit session same day closed SEAM-11 (npm test script split into `test:main` + `test:actions`) and deleted obsolete `trellis-tooltip-copy.test.mjs` (4 tests). v1.5 first wave should pick up i18n leaf-module refactor (closes the 10 carried failures), 34-VALIDATION drift flip, 35-VALIDATION status normalize, ROADMAP plan-list polish (36-14 + 36-15 bullets), 33-HUMAN-UAT-1/2 device retest, and CLAUDE.md `echolearn_*` doc-drift cleanup.

**Phase 37 complete (2026-05-09, verifier 5/5 + UAT 1/1):** v1.5 Wave 0 — i18n Leaf-Module Refactor. Introduced `src/lib/i18n-leaf.ts` shim (60 lines, 3 exports — `t`, `getCurrentLocale`, `bindI18nLeaf`) breaking the `ERR_IMPORT_ATTRIBUTE_MISSING` chain that prevented services importing `'../locales'` from loading under Node 25 native `node --test`. Production wires the live i18next singleton at boot via `bindI18nLeaf` in `main.tsx`; tests get identity defaults with zero setup. 9 source files migrated (5 Tier 1+2 services: flashcard / podcast / question / scheduler / session; 4 Tier 3 leaves: youtube-locale-url / lib/date / providers/llm/locale-directive / providers/tts/index) — each via atomic per-file commit (D-03), Tier 3 paired with their existing test updates in the SAME commit (Pitfall 1 mitigation — `bindI18nLeaf(i18next.t.bind(i18next), () => i18next.language)` placed AFTER `await i18next.init({...})` AND BEFORE the dynamic `await import(...)` of the module under test). Source-reading invariant test (`tests/services/leaf-imports.test.mjs`, 4 assertions) guards against future drift — no service/lib/provider file (except the leaf itself + main.tsx + locales/) may import `'../locales'` or `'i18next'` directly. D-07 (Phase 27 load-bearing) prohibition on runtime LLM translation preserved verbatim in `providers/llm/locale-directive.ts` with append-only Phase 37 footnote noting the leaf is the indirection layer. 11 atomic commits across 3 plans (2+5+5) + 3 metadata commits. Test baseline: pre-Phase-37 558/548/10 (test:main) + 16/14/2 (test:actions) → post-Phase-37 562/559/3 + 16/14/2; 7 of 10 hold-out failures CLOSED at Plan 37-02 Task 3 (`question.service.ts` migration — chain root was deeper than RESEARCH predicted because `flashcard.service.ts` transitively imports `question.service.ts`); the remaining 3 hold-out failures flipped to pre-existing latent issues (`concept-feed.test.mjs` extensionless `youtube.service` import + 2 date-dependent assertions in `trellis-state` / `trellis-layout`) all explicitly D-08 out-of-scope and recommended for Phase 38. tsc -b --noEmit exits 0. Locale switch UAT (EN→ZH→ES→JA + boot smoke) confirmed live by operator. Lessons: (i) RESEARCH § Open Question A's claim that Node 25 native ESM auto-resolves extensionless `.ts` imports under `node --test` was empirically wrong — all 9 service-file shim imports use `'../lib/i18n-leaf.ts'` with explicit `.ts` extension, matching the existing per-file convention. (ii) Cast `as any` at the `bindI18nLeaf` site in `main.tsx` to bridge i18next's auto-generated literal-key-union type (from `i18n.d.ts` module augmentation) and the leaf's intentionally-generic `TFn` signature — single-line cast preserves the regex contract and erases compile-time narrowness only; runtime byte-stable. (iii) Source-reading invariant tests can false-positive on the leaf's own docstring if it contains literal forbidden import substrings — de-collide doc text rather than weakening the regex.

**Phase 38 complete (2026-05-09, verifier 5/5 + UAT 2/2 on iOS+Android):** v1.5 Wave 0 carry-over cleanup — closed all 5 v1.4 doc/QA carry-overs (TECHDEBT-02 through TECHDEBT-06). Three parallel-safe plans landed in one wave: (38-01 doc cleanup) flipped 34/35-VALIDATION frontmatter to `status: validated`, named plans 36-14 + 36-15 inside the v1.4-ROADMAP Phase 36 entry, annotated PITFALLS.md echolearn references with `(historical: pre-2026-05-07 brand)` brand-history notes, and brought the starter-posts test fixture into parity with production STARTER_POSTS (5 atomic commits, zero source-code changes). (38-02 YouTube short removal) eliminated the entire `'short'` post-type structurally — deleted `probePortrait` classifier, dropped `sourceType: 'short'` + `presentationStyle: 'short'` from the union, rebalanced `STYLE_WEIGHTS` (video absorbed short's 0.10 → video: 0.20; sum=1.0 invariant preserved), generalized Phase 36 GAP-C tap-emit so all videos use thumbnail-as-tap-target with hybrid interaction (D-02b: thumbnail tap = inline play + `markExplored` + `CONCEPT_EXPLORED` emit; title/teaser tap = navigate to PostDetailScreen), added CSS `aspectRatio: 'auto 16 / 9'` for landscape/portrait flexibility (D-02a: zero new state, no extra render pass), amended CLAUDE.md GAP-C section (retitled "Video post completion signals (Phase 36 GAP-C, generalized in Phase 38 — load-bearing)" with detector inventory + Why-both subsection + Rules 1/3/4 rewritten for the video-only world). Strategy C atomic commit ordering (types + immediate consumers in commit 1) kept tsc -b --noEmit at exit 0 between every commit. New invariant test `youtube-no-short-classification.test.mjs` (4 assertions: probePortrait absent + sourceType 'short' absent + presentationStyle 'short' absent + STYLE_WEIGHTS shape including sum=1.0) is the standing regression guard. 8 tasks across 10 atomic commits + 1 metadata commit; renamed `InfoFlow.short-tap-emit.test.mjs` → `InfoFlow.video-tap-emit.test.mjs` via `git mv` (4/4 green; single-emit semantic preserved across the migration). (38-03 device UAT scaffold) created `38-HUMAN-UAT.md` mirroring Phase 37's canonical shape with the 2 deferred Phase 33 tests (touch-target feel + React.memo behavioral correctness, iOS+Android matrix per D-03b, sub-checkpoints documented inline per D-03c); operator built APK and ran both tests on physical iOS + Android devices on 2026-05-09 — both passed; status flipped to `complete` with 2× `result: pass`. Test baseline: pre-Phase-38 562/559/3 (test:main) + 16/14/2 (test:actions) → post-Phase-38 566/564/2 + 16/16/0 — net +6 passing main cases AND +2 passing action cases vs Phase 37 baseline; both remaining test:main fails are pre-existing `concept-feed.test.mjs` ERR_MODULE_NOT_FOUND + date-dependent `trellis-layout` assertion (Phase 37 STATE.md identified, D-08 out-of-scope, future Phase 45 candidates). Out-of-scope finding filed for v1.5 backlog: GraphScreen mindmap drag latency on Android Chromium WebView (recently introduced regression, perceptible-but-usable, warm-up smooths it out — surfaced during the device UAT session) — captured at `.planning/notes/2026-05-09-graphscreen-drag-lag-android.md` for `/gsd:discuss-phase` triage in a later v1.5 phase (likely Phase 44/45 or its own perf phase); does NOT block Phase 38 close-out (Phase 33 deferral set is the 2 specific tests that passed). Lessons: (i) STYLE_WEIGHTS rebalance MUST preserve sum=1.0; the invariant test asserts both no `short:` key AND sum within 1e-9 tolerance — first attempt's regex over-matched the trailing `// Phase 38: absorbed short's 0.10` comment producing sum=1.1; corrected by anchoring on key-value pairs after stripping line comments. (ii) D-02b hybrid interaction was simpler to implement as card-level `handleActivate` + thumbnail `e.stopPropagation()` than as two separate click handlers — matches existing structure, single-emit semantic enforced by the renamed test. (iii) Parallel executors can accidentally capture sibling-agent staged writes (Plan 38-02 Task 3's commit `01d870e5` swept up 4 sibling 38-01 finalization writes); future protocol: explicit `git reset HEAD` of unrelated indexed paths before per-task commits when running concurrently. (iv) `gsd-tools requirements mark-complete` updates checkboxes but not the traceability matrix table rows — recurring drift caught at both 38-01 and 38-03 close; future fix candidate.

**Phase 38 gap-closure addendum (Plan 38-04, 2026-05-09):** Two pre-existing fresh-install UX bugs surfaced during the Plan 38-03 device UAT session were closed in a same-day gap-closure plan: (Bug A — text-art locale staleness) `concept-feed.service.ts` cached `textArtContent` without a locale tag, so welcome posts (STARTER_POSTS) generated under one locale stayed in cache forever — Chinese text-art rendered for English-locale users on devices where `detectInitialLocale` initially returned `zh`. Fix: subscribe to `LOCALE_CHANGED`, strip `textArtContent` from cached posts, reset `_textArtBgRunning` so the next render path re-fires generation under the new locale. Self-heals on next swipe-for-more / cold-boot. (Bug B — mock flashcard seeds) `flashcard.service.ts:makeSeedCards()` persisted 5 hardcoded English flashcards (Marx / quantum / backprop / supervised / thermodynamics) on first launch — Trellis's local-first personalized-learning framing makes pre-canned mock content a content/value mismatch. Fix: delete `makeSeedCards`; `loadAll()` returns `[]` on first launch. Plan 38-04 ran as a separate Wave 2 gap-closure plan (`gap_closure: true` frontmatter); 2 atomic fix commits + 1 metadata commit; test baseline preserved (566/564/2 + 16/16/0). Lessons: (i) Stale-state writes from a long-running parallel executor can revert operator commits made mid-stream — Plan 38-04's metadata commit reverted operator's earlier `phase complete` updates to PROJECT.md/REQUIREMENTS.md/38-03-SUMMARY.md; orchestrator restored via `git checkout HEAD --` before the next commit. Future parallel-executor protocol: re-read STATE.md immediately before metadata writes, not at agent boot. (ii) Lazy-cache fields with locale-sensitive content need an explicit invalidation event — the locale directive injection at LLM-call time was working correctly; the bug was that the result was persisted into a cache with no invalidation hook.

_Last updated: 2026-05-09 — Phase 38 complete (TECHDEBT-02 through TECHDEBT-06 closed + Plan 38-04 gap-closure fixed 2 fresh-install UX bugs; v1.5 Wave 0 fully done). v1.5 progress: 2 of 9 phases done; Wave 1 next (Phase 39 engagement service + Phase 40 source diversity, parallel-safe)._

**Phase 39 complete (2026-05-09, verifier 5/5):** v1.5 Wave 1 — Engagement Service + Walker Extension. Foundation leaf service `engagement.service.ts` (single localStorage key `trellis_engagement_v1`, shape `{ saved: string[], liked: string[], dismissed: string[] }`) covers ENGAGE-01/02/03 (save/dismiss/like) with full API surface (savePost / getSavedPosts / removeSavedPost / likePost / unlikePost / isLiked / dismissAnchor / getDismissedAnchorIds / undismissAnchor / getPinnedIds / reset) — leaf-module pattern intact (no JSON imports, no `lib/date.ts`, no `react-i18next`). Walker `walkDerivedList(count, exploredIds, dismissedIds)` extended with REQUIRED positional 3rd arg (no default) — lazy-skip only (no splice; cyclePosition uncorrupted), Phase 36 GAP-B `Math.max(count * 2, len)` headroom math + comment block preserved. Two new event-bus signals: `ANCHOR_DISMISSED { anchorId }` (walker subscribes; one-signal-per-semantic-event per CLAUDE.md rule 6) and `ENGAGEMENT_CHANGED { kind, id }` (UI subscribes; payload-discriminates-kind mirrors `GRAPH_UPDATED` precedent). `post-history.service.ts:purgeExpired()` filter extended to honor `engagementService.getPinnedIds()` so saved+liked posts are pinned against 7-day rolling retention (D-04). Defense-in-depth anti-wire enforcement (D-06): behavioral test case 6 in `engagement.service.test.mjs` (asserts `dismissAnchor` emits exactly 1 ANCHOR_DISMISSED + 0 CONCEPT_EXPLORED) AND source-reading scan in `engagement-anti-wire.test.mjs` (forbids any function emitting both events). Saves/likes persist across day boundaries (no `date` field in `EngagementState`, no date-mismatch reset in `loadState()`); dismisses only reset via undismiss or full `reset()` (Clear-All-Data). 8 atomic per-file commits + 1 fix commit + 1 metadata commit; sole walker call site updated at `concept-feed.service.ts:1209`. Test baseline: pre-Phase-39 566/564/2 + 16/16/0 → post-Phase-39 583/581/2 + 16/16/0 (net +17 main passing, both remaining fails are the same Phase 37 carry-overs — `concept-feed.test.mjs` ERR_MODULE_NOT_FOUND + `trellis-layout` date-dependent assertion). tsc -b --noEmit exits 0. v1.5 progress: 3 of 9 phases done.

**Phase 40 complete (2026-05-09, verifier 4/4):** v1.5 Wave 1 — Source Diversity Leaf Module. Foundation leaf service `source-diversity.service.ts` (513 lines, parallel-safe peer to Phase 39's `engagement.service.ts`) covers CONTENT-02 leaf-module surface — singleton `sourceDiversityService` with 5 functions (`filterForDiversity`, `recordServedDomain`, `scoreSource`, `getUsedDomains`, `reset`) backed by a session-scoped `usedByAnchor: Map<string, Set<string>>`. Bundled quality table `DOMAIN_TIERS` (219 entries, all values in [0.0, 0.95]) is hand-curated per D-03 editorial line (peer-reviewed/academic/reputable journalism at top; SEO/AI farms blocked; mainstream outlets receive equal score regardless of partisan lean) and is loaded into `_tierMap: Map<string, number>` once at module-init for O(1) lookup. URL normalization uses a hand-rolled 12-entry `MULTI_SEGMENT_TLDS` PSL slice (`co.uk` / `co.jp` / `com.au` / `co.nz` / `org.uk` / `ac.uk` / `edu.au` / `gov.uk` / `co.kr` / `com.br` / `gob.mx` / `ac.nz`) — `extractDomain` is defensive (try/catch `new URL`, returns `undefined` on malformed input) and `normalizeHost` collapses subdomains to registrable root. `UNKNOWN_DOMAIN_SCORE = 0.5` declared as named const. Two-pass re-rank in `filterForDiversity` (Pass A: unseen domains, score-desc; Pass B: seen domains, score-desc; concatenated; preserves Tavily order via V8 stable sort tie-break). Best-of-the-bad fallback (D-06) fires ONLY when ALL inputs were malformed URLs (every result's `extractDomain` returned undefined) — explicitly NOT a "no unseen results" handler; valid-but-seen results still surface via Pass B. Defense-in-depth anti-wire enforcement: behavioral suite (16 cases — 7 covering filter semantics including the "valid-but-seen → Pass B, fallback does NOT fire" distinction, 2 round-trip + idempotence for the session Map, 7 covering scoreSource bounds + DOMAIN_TIERS bounds + extractDomain defenses) AND source-reading scan in `source-diversity-anti-wire.test.mjs` (4 assertions: no `\basync\s` keyword anywhere, no `fetch(`, no `chatStream(`, no `chatCompletion(` — only import is `import type { WebSearchResult }`). The async-absence pattern (vs substring `await` window-scan) was the planner's deliberate choice per RESEARCH §8 pitfall 4 / Phase 39 SUMMARY's window-fragility lesson. Synchronous O(N) scan suitable for direct invocation inside `refillQueue`'s mutex hold (Phase 41's wiring job). 4 atomic per-task commits (`feat` Task 1 + `test` Tasks 2/3 + `docs` close-out). PHASE 41 BOUNDARY DISCIPLINE held strictly — zero edits to `concept-feed.service.ts` or `web-search.service.ts`; no `recordServedDomain` call sites added; no Tavily `maxResults` widening; no citation rendering touched. CONTENT-02 traced as `◐ Partial` (leaf shipped; Phase 41 will wire it into Tavily news branch + multi-snippet essay grounding). Test baseline: pre-Phase-40 583/581/2 + 16/16/0 → post-Phase-40 603/601/2 + 16/16/0 (net +20 main passing — 16 behavioral + 4 anti-wire); both remaining test:main fails are the same Phase 37 carry-overs. tsc -b --noEmit exits 0. Per-task auto-fix deviation (Rule 2 — proactive docstring de-collision against acceptance grep patterns) folded into Task 1's commit; identical pattern + fix to Plan 39-01's lesson (iii). v1.5 progress: 4 of 9 phases done; Phase 41 unblocked (consumes both Phase 39 walker dismissedIds extension + Phase 40 source-diversity leaf at the same `refillQueue` seam).

**Phase 41 complete (2026-05-09, verifier 7/7):** v1.5 Wave 2 — Pipeline Wiring + Essay Depth. Two plans landed sequentially (Wave 2 sequencing was a planning revision because both plans touched non-overlapping line regions of `concept-feed.service.ts` and parallel writes would have risked file corruption; logical scopes stayed disjoint). (41-01 source-diversity wiring) `sourceDiversityService` wired into both news call sites in `concept-feed.service.ts` — creation loop (~:1093) and pre-fetch loop (~:1296) each call `getUsedDomains → excludeDomains+maxResults:3 → filterForDiversity → recordServedDomain` after `posts.push` of the news entry. `WebSearchOptions.excludeDomains?: string[]` added to `web-search.service.ts:17`; conditional Tavily `exclude_domains` body field set at :54-55 (snake_case per Tavily REST API). `sourceDiversityService.reset()` installed inside `loadCache()`'s date-mismatch branch as the day-boundary regenerative reset. Tavily `maxResults` widened 1→3 to make `filterForDiversity` rerank meaningful (operator chose conservative 3 over recommended 5 per CONTEXT D-02 — matches `sources.slice(0, 3)` invariant downstream). SC-1 integration test confirms walker dismissedIds path skips dismissed concepts (Phase 39 wire untouched at :1234-1235 per Plan 41-01 Task 2 step 4 explicit invariant). CONTENT-02 closed (`◐ Partial` → `✓ Complete`). 5 atomic commits + 3 new test files (12 + 7 + 4 cases). (41-02 essay depth + citation rendering) `EssayOptions.depth?: 'standard' \| 'deep'` knob added to `post-essay.service.ts:14`; all 4 generators (standard 200-350 / video 200-400 / news 150-250 / text-art 80-120) compute `const depth = options?.depth ?? 'standard'` and select word-count instruction; deep band 350-600 referenced in all 4 (lines 99, 135, 175, 217). `bodyMarkdownDeep?: string` additive field on BOTH `EssayContent` AND `PostSnapshot` per RESEARCH Pitfall 9 (schema additivity preserved across cache snapshot boundary — old cached essays stay valid). `patchPostEssayInCache` extended for depth-aware selective merge with truthiness guards (only touches the variant the caller streamed). News grounding widened to `sources.slice(0, 3).map(s => snippet).join('\n\n')` at :163-169; LLM news prompt explicitly instructs `[^1]/[^2]/[^3]` footnote markers (markdown footnote syntax that remark-gfm parses into `<sup>` + `<section className="footnotes">`). `generateEssayMeta` body slice cap raised 2000→4000 chars at :61 (also bumped video transcript to 4000 at :125). PostDetailScreen.tsx essay `useEffect` D-08 abort-chain audit landed: 3 pre-call guards `if (abortController.signal.aborted) return` at :314/:327/:338 immediately preceding the for-await openers for connection/discover/generatePostEssay branches; 4 signal-arg passes at :320/:331/:339/:350. `generateConnectionPost` + `generateDiscoverPost` extended with trailing `options?: { signal?: AbortSignal }` parameter (per CONTEXT discretion-delegated mirror of `generatePostEssay`); chatStream calls thread `signal: options?.signal` at :1855/:1978. `Markdown.tsx` ReactMarkdown gained `components={{ sup, a, section }}` overrides (citation chip styling) — `<a>` discriminates footnote refs/backrefs by `data-footnote-ref`/`data-footnote-backref` (DOM-clobber-prefix-safe per RESEARCH Pitfall 4); `<section>` discriminates by `className.includes('footnotes')`. Sanitize schema sup-attr SPREAD fix per RESEARCH Pitfall 4: `sup: [...(defaultSchema.attributes?.['sup'] ?? []), 'dataCite', 'style']` extends the GitHub default rather than replacing it. CONTENT-01 + CONTENT-03 + CONTENT-04 all closed. 7 atomic commits + 3 new test files (11 + 10 + 8 cases) + 2 Rule-3 in-test-iteration regex/scoping auto-fixes folded into the source-modifying commits. Test baseline: pre-Phase-41 603/601/2 (test:main) + 16/16/0 (test:actions) → post-Phase-41 657/655/2 + 16/16/0 — net +52 main passing across 6 new test files (52/52 green); both remaining test:main fails unchanged (Phase 37 carry-overs — `concept-feed.test.mjs` ERR_MODULE_NOT_FOUND + `getVineColor` date-dependent assertion; both deferred to Phase 45 per CLAUDE.md anti-pattern audit). tsc -b --noEmit exits 0. 3 manual-only items flagged for operator UAT (visual citation chip rendering, source-diversity rotation observable across same-anchor refills, deep-dive essay length feel) per VALIDATION.md but explicitly NOT blockers (visual quality + LLM-soft-instruction outcomes). Lessons: (i) when both plans touch the same file at non-overlapping line regions, sequence them (Wave N → Wave N+1) rather than parallelize — file-write coordination overhead exceeds the modest parallel speedup; planning checker caught this on iteration 1, surgical wave-bump revision restored correctness. (ii) Schema additivity on cache snapshots is a quiet load-bearing rule — `bodyMarkdownDeep?` had to land on BOTH `EssayContent` (live) AND `PostSnapshot` (persisted) in the same commit; Pitfall 9 caught this before it could ship as a serialization bug. (iii) Sanitize-schema spread vs replace: `sup: ['dataCite', 'style']` would have replaced the GitHub default attribute set, removing default `<sup>` allowances — `[...(defaultSchema.attributes?.['sup'] ?? []), 'dataCite', 'style']` extends instead; both 41-02 SC-5(c) test and CLAUDE.md note this Pitfall-4 regression guard. v1.5 progress: 5 of 9 phases done; Wave 2 fully landed; Phase 42 (Pinterest masonry) and Phase 43 (engagement UI + Deep dive button) unblocked.

**Phase 42 complete (2026-05-10, verifier 8/8 + UAT 10/12 manual + 1 gap-closed):** v1.5 Wave 3 — Pinterest-style 2-column Masonry Feed Layout. New `MasonryFeed.tsx` component implements height-accumulating JS split (each tile drops into the currently shorter column at append time and stays there — no `column-count`/`break-inside`, no per-append column shuffle) with `columnHeightsRef` + `tileColumnAssignmentsRef` keyed on stable post id; per-style height estimates (image / text-art / video / news / suggestion) feed Pass 1 comparator pre-measure, Pass 2 measures real DOM after first paint and corrects only if the estimate diverged. framer-motion entrance animations on leaf tiles only, wrapped in `<MotionConfig reducedMotion="user">` (RESEARCH Pitfall 1: framer-motion v12 does NOT auto-respect prefers-reduced-motion). `VineBloomCard` end-of-content celebration replaces the bare `noMorePosts` toast — 13 `home.celebration.*` keys across 4 locales (en/zh/es/ja); CTAs Heal/Re-plant/Open Planner reuse `trellisActionsService` verbatim (Pitfall 3 — zero new methods). Queue defaults bumped (`MAX_QUEUE_SIZE` 16→32, refill threshold 16→24, swipe pop 4→8 — masonry half-width tiles consume twice as fast as the prior single-column InlineInfoFlow). Feed video tiles became navigation-only (Phase 42 UAT-7+8 inline-play removal — operator rejected play-icon overlay; Detector D in PostDetailScreen is now the sole feed-level video signal). `FeedPostImage.tsx` corner-shape ownership moved to parent ConceptCard (`overflow: hidden` + `borderRadius: 8px`) — own borderRadius removed per UAT-11 round 2 to fix double-rounding gradient gap. Plan 42-08 gap-closure (UAT-4): `ReviewScreen.tsx:299` fail-open `Boolean(filteredItems && filteredItems.length > 0)` flipped to `filteredItems !== null` so an anchor with 0 extracted flashcards no longer silently dumps the user into today's full SM-2 queue — new anchor-scoped empty branch with `{{title}}` interpolation across 4 locales; pre-existing latent bug (NOT a Phase 42 regression — same fail-open exists in PlannerScreen heal/replant) that the always-on celebration UX promoted to user-visible. 8 plans landed; 40 Phase 42-specific tests pass (21 MasonryFeed structural + 16 screen/gap + 3 locale parity); tsc -b --noEmit exits 0. Test baseline: pre-Phase-42 657/655/2 → post-Phase-42 709/704/5 — net +47 passing main cases; the 5 fails are stale prior-phase invariants needing constants update (concept-feed `walkDerivedList(16,...)` + post-queue `needsRefill` 16-threshold both reference the OLD constants from before Phase 42's 16→24 bump, plus 3 unrelated pre-existing assertions — all queued for Phase 45 hygiene). Operator follow-ups recorded but explicitly NOT Phase 42 blockers: F-1 vine illustration polish ("vine is super ugly"), F-2 feed dominated by news+video (text-art absent — separate `/gsd:debug` queued), seed flashcards from QA records on `heal()` so anchor-empty state fires less often (plan-gated). Lessons: (i) Phase 42's contract (layout/structure) was not an illustration-polish phase — operator F-1 "vine is super ugly" rightly captured as a separate UI polish phase rather than expanding 42's scope. (ii) Promotion-of-latent-bug is a real Phase shape — Plan 42-08 is the canonical example: ReviewScreen's fail-open `isFiltered` boolean shipped before Phase 42 but was hidden because PlannerScreen's heal flow rarely triggered the "0 matching cards" condition; promoting heal into the always-on VineBloomCard surface made it user-visible. Frame the gap as "phase surfaced", not "phase regressed". v1.5 progress: 6 of 9 phases done; Phase 43 (engagement UI consuming MASONRY-rendered cards) unblocked.

**Phase 43 complete (2026-05-11, verifier 6/6):** v1.5 Wave 4 — Engagement UI. Surfaced Phase 39's leaf engagement service into the user-facing app across 8 plans / 4 waves (Wave 0 shared infra → Wave 1 four parallel feature plans → Wave 2 two wiring plans → Wave 3 close-out). ENGAGE-01 + ENGAGE-02 + ENGAGE-03 + CONTENT-01 closed; ENGAGE-04 ("N connections in your graph" micro-label) explicitly descoped via DS-01 on operator framing "tiles already too rich; we should try to simplify instead" — captured in ROADMAP italicized SC-4 + REQUIREMENTS Out-of-Scope row with reopen-path documented. (43-01 shared infra) `useLongPress.ts` hook extracted from `ChatMessage.tsx:119-140` canonical 480ms pattern (pointer events + `setTimeout`-cancel-on-move + no-contextmenu invariant); `BottomSheet.tsx` gained a `compact` prop for the 3-row engagement menu shape; 14 new i18n keys × 4 locales (`engagement.menu.{save,unsave,like,unlike,dismiss}`, `engagement.toast.{saved,unsaved,liked,unliked,dismissed}`, `saved.{title,tabs.saved,tabs.liked,empty.saved,empty.liked}`, `postDetail.deepDive.{button,restore,toggleStandard,toggleDeep}`); 9 Wave-0 test scaffolds landed first per Phase 32.1 lesson #2 — subsequent plans flipped them from skip stubs to filled assertions. (43-02 TS-01) trimmed the "NEWS" presentation-style chip from news tile cards + removed `infoFlow.newsTag` from 4 locale bundles (paired negative+positive source-reading invariant test silently guards against re-add). (43-03 LongPressMenu + MasonryFeed) new `LongPressMenu.tsx` (141 lines, wraps `BottomSheet` with `compact=true`, 3 rows with state-aware Save/Unsave + Like/Unlike label flips reading `engagementService.isSaved/isLiked` at open time, anti-wire invariant — zero `CONCEPT_EXPLORED` / `eventBus.emit` / `dailyReadService.markExplored` in the menu); `MasonryFeed.tsx` extended with `TileWrapper` extraction + `useLongPress` per-tile binding + corner-icon overlay (icon is read-only, never a tap target — anti-wire enforced) + `AnimatePresence` column wrapping for LP-05 fade-ALL-same-anchor on dismiss (filter by `sourceQuestionIds[0] !== anchorId`, not just tapped tile). (43-04 SavedScreen) new `/saved` sub-route with Saved | Liked tabs mirroring `PostHistoryScreen` compact-card layout; backed by `engagementService.getSavedPosts()` / `getLikedPosts()`; subscribes to `ENGAGEMENT_CHANGED` for in-place resync; per-tab empty states (Bookmark / Heart icons + i18n heading + body); `Header backTo="/home"` preserves Phase 32.1 portal split. (43-05 PostDetail Deep Dive) Deep Dive full-width button positioned below body + above takeaway, gated on `!isStreamingOnEnter && (post.bodyMarkdown || streamingBody) && !post.bodyMarkdownDeep && !isStreamingDeep`; dedicated `deepAbortControllerRef` (separate from existing essay controller); tap fires `generatePostEssay({ depth: 'deep', signal })`; during stream renders `Markdown(streamingDeep)` in body slot with "Restore standard" link above; Restore aborts deep controller, resets `streamingDeep=''` + `activeVariant='standard'`; once `bodyMarkdownDeep` is cached the segmented Standard | Deep toggle appears (pure client-side `setActiveVariant`, no re-stream). DD-05 abort contract extended `3→16 pre-call guards + 4→6 signal-arg passes` across all 4 essay paths (standard + deep variants) — both `patchPostEssayInCache` call sites now guarded. (43-06 HomeScreen wiring) top-right Bookmark icon navigates to `/saved` (layout-stable — masonry first-tile position unchanged); HomeScreen now hosts `LongPressMenu` with `{menuOpen, menuPostId, menuAnchorId}` state and bumps `engagementVersion` on `ENGAGEMENT_CHANGED` for corner-icon re-render; **dual-effect dismiss handling** reaffirms Phase 36-14 sibling-effects canonical pattern — Effect A (deps `[]`, lines 567-574) subscribes `ANCHOR_DISMISSED` and filters `dailyPosts` in-place by `sourceQuestionIds[0] !== anchorId` for immediate fade-out while on `/home`; Effect B (deps `[location.pathname]`, lines 584-591) re-reads `engagementService.getDismissedAnchorIds()` on navigation and filters in-place; both paths filter in-place — neither calls `conceptFeedService.getDailyPosts()` (no refetch). (43-07 Force-New-Day) `handleForceNewDay` calls `engagementService.reset()` at line 138, ordered AFTER `dailyReadService.reset()` (line 134) and BEFORE the success toast (line 139); source-reading invariant test enforces ordering. (43-08 close-out) `43-PHASE-SUMMARY.md` (176 lines) aggregates per-plan references + 10-item final invariant audit; STATE / ROADMAP / VALIDATION close-out edits; `nyquist_compliant: true` flipped + per-task map populated with 26 rows. Plan deviation worth flagging: 43-06 plan body referenced `engagementService.getDismissedAnchors()` but the actual Phase 39 method is `getDismissedAnchorIds()` — corrected in both source and test (Rule-1 auto-fix). Test baseline: pre-Phase-43 709/704/5 (test:main) + 16/16/0 (test:actions) → post-Phase-43 770/765/5 + 16/16/0 — net +61 main passing across new Wave-0 scaffolds + filled assertions (5 pre-existing failures unchanged — same Phase 37 + Phase 42 carry-overs queued for Phase 45). tsc -b --noEmit exits 0; `npm run build` exits 0. Lessons: (i) Operator's "tiles already too rich" framing is **tile-specific, not codebase-wide** — Phase 43 simultaneously trimmed tile signals (TS-01 NEWS chip out, ENGAGE-04 N-connections descoped) AND leaned the opposite direction on dedicated surfaces (Saved | Liked tabs over private-only Liked, Standard | Deep segmented toggle over append-both, replace-in-place during deep stream over additive append); tiles minimal, dedicated screens + PostDetail interactive + state-rich. (ii) Wave-0 test scaffolds landed FIRST then filled in subsequent plans cleanly separates contract-defining-work from contract-fulfilling-work — 43-01's 9 skip stubs all converted to passing tests by 43-08 close-out. (iii) Dual-effect canonical pattern (Effect A stable listener `[]` + Effect B navigation re-read `[location.pathname]`) is now Phase 36-14's load-bearing legacy — 43-06 mirrors it exactly for ANCHOR_DISMISSED with identical sibling-effects layout. v1.5 progress: 7 of 9 phases done; Phase 44 (dependency-version sweep) + Phase 45 (code-quality / dead-code / pre-existing carry-over closure) remain — parallel-safe Wave 5 to close the milestone.

**Phase 43 gap-closure round 2 (2026-05-12, verifier 13/13):** Re-verify UAT surfaced two regressions in the Wave-4 ship: Test 4 (dismissing one tile failed to fade sibling tiles sharing the same anchor — and refresh didn't recover; severity: major) and Test 12 (Force-New-Day produced React duplicate-key DEV warnings on the home feed; severity: blocker). Two parallel-safe gap plans landed under Wave 5 with disjoint file surfaces. (43-14 dismiss-filter-at-read-boundary) Centralized the dismiss filter at the concept-feed cache-read boundary instead of relying on HomeScreen Effect A alone — new `applyDismissedFilter(posts: DailyPost[])` private helper in `concept-feed.service.ts` (line 240) builds a `Set` from `engagementService.getDismissedAnchorIds()` and filters by `post.sourceQuestionIds[0]`. Wired into 3 cache-read sites: `getCachedDailyPosts` (line 1638), `getDailyPosts` cache-hit branch (line 1523), `getDailyPosts` fingerprint-mismatch same-day branch (line 1548). Drain branch (line 1515-1525) deliberately UNTOUCHED — those posts come from the walker which already dismiss-skips at `post-queue.service.ts:389` (Phase 39 D-07 contract preserved). Effect A remains as live-fade-animation layer for AnimatePresence; Effect B remains as `[location.pathname]` defense-in-depth. 12/12 new tests (`concept-feed-dismiss-filter.test.mjs` + `HomeScreen.dismiss-resync.test.mjs`). (43-15 force-new-day-dedup) Diagnosed root cause: after Force-New-Day the same yesterday-snapshot posts lived in TWO stores simultaneously — `STORAGE_KEY_YESTERDAY` snapshot (read by `getYesterdayQueue()`) AND `_state.posts` rehydrated by `load()` date-mismatch from same `parsed.posts`. HomeScreen warm-start tier-2 seeded 8 from the snapshot; `loadNextBatch` then popped 8 from the live queue; first-8 of each overlapped ~75%. Both approaches landed per UAT root_cause defense-in-depth guidance: **Approach A (structural)** — new `postQueueService.removeByIds(ids: string[])` (line 270) splices warm-start seed IDs out of `_state.posts` so the two stores become mutually exclusive; persists via `save()`; does NOT touch `STORAGE_KEY_YESTERDAY`, `totalServed`, `derivedList`, or `cyclePosition`. HomeScreen's tier-2 fallback dispatches removeByIds on tier === 'yesterday' via a mount-once `warmStartTierRef` flag. **Approach B (render-boundary)** — new `infiniteScrollService.seedSeen(ids)` primes the dedup Set; `HomeScreen.handleLoad` concat at line 340-341 now uses explicit Set-based id-dedup so any future regression can't produce duplicate keys. 18/18 new tests (`post-queue-remove-by-id.test.mjs` + `HomeScreen.force-new-day-dedup.test.mjs`). Both rounds preserved Phase 36-11 stale-cache rejection AND Phase 36-14 tier-2 warm-start re-fallback AND Phase 36 GAP-B walker `Math.max(count * 2, len)` AND walker dismiss-skip AND `spreadByConcept`→`spreadByStyle` order AND numeric defaults (`MAX_QUEUE_SIZE=32`, refill threshold = 24, `loadNextBatch limit=8`). 10 atomic commits + UAT/debug closure commit + phase-complete commit; tsc -b --noEmit exits 0; `npm run build` exits 0 (1.73s). 67/67 prior-phase regression tests (derived-list, refill-queue-integration, spread-by-concept, refill-mutex, useQuestions system-prompt-stability, video-completion-detectors, warm-start-refallback, stratified-sampling) confirmed green. Lessons: (i) When centralizing a filter that previously lived only at the screen-render layer, the fix belongs at the **read boundary** — multiple unfiltered service reads (4 in HomeScreen) will always race the screen-layer filter; pushing the filter into the cache-read functions makes screen layers passive consumers and structurally eliminates the race. (ii) When a single state event causes the same data to be persisted to two stores simultaneously (here: snapshot + rehydrated queue from one `parsed.posts` payload), defense-in-depth must combine **structural** dedup (remove from one store after seeding from the other) AND **render-boundary** dedup (Set-based concat safety net) — fixing only one leaves an unobserved second path. (iii) When a UAT root_cause comment explicitly proposes both Approach A (primary) AND Approach B (fallback), prefer landing both — the disjoint test surfaces detect different regression classes (structural changes break A's tests; render-path concat-order changes break B's tests). v1.5 progress: 7 of 9 phases done; Phase 44 + 45 next (parallel-safe Wave 5 to close the milestone).

**Phase 44 complete (2026-05-13, verifier 10/10):** v1.5 Wave 5 dependency-version sweep closed TECHDEBT-08. Safe in-major target ranges landed for Capacitor `^8.3.3`, React/React DOM `^19.2.6`, i18next `^26.1.0`, react-i18next `^17.0.7`, react-router-dom `^7.15.0`, Tailwind/Vite plugin `^4.3.0`, `@types/react` `^19.2.14`, `typescript-eslint` `^8.59.3`, `eslint-plugin-react-hooks` `^7.1.1`, and `eslint-plugin-react-refresh` `^0.5.2`; npm regenerated `package-lock.json`. Held-back majors remain documented for future work: Vite 8, TypeScript 6.0, ESLint 10, lucide-react 1.x, framer-motion to motion, @vitejs/plugin-react 6, @types/node 25, and globals 17. Plan 44-02 added `@capacitor/ios@^8.3.3` as a justified deviation because the repo already has `app/ios/` and exact `npx cap sync` could not succeed without the matching iOS platform package; Android sync produced no `app/android` diff, while iOS Podfile outputs updated. Verification baseline: `npm test` exits 0 via existing `test:main; test:actions` script, with `test:main` still at the known 844/839/5 post-Phase-43 failure set and `test:actions` 16/16 green; `npm run lint`, `npx tsc -b --noEmit`, `npm run build`, and `npx cap sync` exit 0. `npm audit --audit-level=high` remains the pre-existing 5 high / 0 critical-new baseline. Manual smoke passed all five dependency-risk surfaces (`locale-switch`, `ask-streaming`, `queue-refill`, `saved-route-navigation`, `android-sync-sanity`). `44-VALIDATION.md` is signed off with `nyquist_compliant: true`; `44-PHASE-SUMMARY.md` and `44-VERIFICATION.md` preserve the durable audit trail. Phase 45 remains pending for broader hygiene, dead-code cleanup, audit remediation, and stale test invariant fixes.

**Phase 45 complete (2026-05-13, verifier 5/5):** v1.5 Wave 5 code-quality sweep closed TECHDEBT-07, TECHDEBT-09, TECHDEBT-10, TECHDEBT-11, and TECHDEBT-12. The phase produced durable audit artifacts for TypeScript strictness/lint (`45-TSC-AUDIT.md`), TODO/suppression triage (`45-TODO-TRIAGE.md`), dead-code and removed-feature residue (`45-DEAD-CODE-SWEEP.md`), operator/debug notes (`45-OPERATOR-NOTES.md`), performance (`45-PERF-AUDIT.md`), final verification (`45-VERIFY.md`), and phase rollup (`45-PHASE-SUMMARY.md`). Stale no-console disables and source-reading constants were fixed; direct `concept-feed.service.ts` local imports now use `.ts` suffixes for Node test compatibility. The remaining `npm run test:main` failure is a known-deferred stale `buildFallbackPosts` test contract, narrowed and documented after the original import blocker was fixed. GraphScreen Android drag lag was verified on `emulator-5554` and closed with a scoped MindElixir-container layer/touch mitigation plus source-reading guard; first-paint and masonry broad rewrites remain deferred without P0/P1 trace evidence. Final verification: `tsc`, lint, build, `test:actions`, targeted phase suites, and 845/846 `test:main` cases match the recorded baseline.

**Phase 46 complete (2026-05-13, verifier 8/8):** v1.5 milestone-audit gap closure closed CONTENT-03 on the queued-news prefetch path. `PreFetchCache.news` now stores filtered top-source arrays, direct and queued-prefetch news paths share `selectNewsTopSources`, and `mapNewsSourcesToNewsMeta` maps up to three sources with stable 1-based indexes before `generateNewsEssay` consumes `sources.slice(0, 3)`. The targeted CONTENT-03 regression passes 17/17 and final build/lint/test evidence is recorded in `46-VERIFY.md`; the only aggregate test caveat remains the known-deferred `tests/concept-feed.test.mjs` `buildFallbackPosts` baseline from Phase 45. Milestone v1.5 is ready for re-audit and archive.

---
*Last updated: 2026-05-13 — Phase 46 complete (verifier 8/8): v1.5 audit blocker CONTENT-03 closed for queued-news prefetch multi-source grounding. Milestone v1.5 is ready for re-audit and archive.*
