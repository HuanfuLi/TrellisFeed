---
phase: 04-study-infrastructure-pilot
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/research-consent.service.ts
  - app/src/types/index.ts
  - app/src/services/settings.service.ts
  - app/src/screens/OnboardingScreen.tsx
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/phase4/onboarding-consent.test.mjs
  - app/tests/phase1/consent-gate.test.mjs
  - app/tests/phase1/consented-prewarm.test.mjs
  - app/tests/services/interaction-log.service.test.mjs
autonomous: true
requirements: [STUDY-01]
must_haves:
  truths:
    - "hasAffirmativeResearchConsent requires researchConsentGiven === true AND researchConsentVersion === RESEARCH_CONSENT_VERSION AND onboardingCompleted === true; a legacy install with only aiConsentGiven true is routed back to onboarding"
    - "The consent step visibly enumerates all five §14.3 disclosures: in-app interaction logging; AI questions/answers stored and uploaded for research; pre/post oral responses recorded externally and transcribed; anonymization per protocol; withdrawal per protocol"
    - "Onboarding remains exactly three steps (welcome → language → consent) with no topic-selection step and no participant API-key entry"
    - "Consent copy exists in all four locale bundles with identical key sets (bundle-parity green)"
    - "aiConsentGiven remains a separate LLM-provider gate and is still set by onboarding completion (D-05)"
  artifacts:
    - "app/src/services/research-consent.service.ts (RESEARCH_CONSENT_VERSION + version-gated gate)"
    - "app/src/screens/OnboardingScreen.tsx (five-item consent step)"
    - "app/src/locales/{en,zh,es,ja}.json (onboarding.consent.item* keys)"
    - "app/tests/phase4/onboarding-consent.test.mjs"
  key_links:
    - "settingsService preferences → hasAffirmativeResearchConsent → resolveParticipantRoute / interactionLog storeAndEnqueue / App session-start (one shared gate, many consumers)"
    - "OnboardingScreen handleContinue writes researchConsentGiven/researchConsentVersion/researchConsentGivenAt in the same preferences write as onboardingCompleted"
  prohibitions:
    - "No topic-selection UI anywhere in onboarding (D-03)"
    - "No participant API-key entry step (D-05)"
    - "No in-app audio capture, microphone permission, or recording plugin (D-06) — the oral-test disclosure describes an EXTERNAL researcher-run recording"
    - "No runtime LLM translation of consent copy — all four bundles are build-time authored"
    - "No inference of research consent from a legacy aiConsentGiven flag"
---

<objective>
Harden onboarding into the STUDY-01 consent instrument (D-04): replace the single-boolean consent inference with a versioned research-consent preference, render the five §14.3 disclosures on the existing consent step, and land the copy in all four locale bundles in this same plan.

Purpose: the current gate equates onboardingCompleted + aiConsentGiven with research consent, and the current copy discloses neither external oral recording, anonymization, nor withdrawal — insufficient for an IRB-ready instrument (D-14). Versioning forces installs holding stale copy back through consent.
Output: version-gated consent service, five-item consent UI, four synchronized locale bundles, executable gate tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-study-infrastructure-pilot/04-CONTEXT.md
@.planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md
@app/src/services/research-consent.service.ts
@app/src/screens/OnboardingScreen.tsx
@app/scripts/translate-locales.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (04-04):**
- `app/src/services/research-consent.service.ts` → `export const RESEARCH_CONSENT_VERSION = 1`; `hasAffirmativeResearchConsent()` version-gated
- `app/src/types/index.ts` → `AppPreferences.researchConsentGiven?: boolean`, `researchConsentVersion?: number`, `researchConsentGivenAt?: string`
- `app/src/services/settings.service.ts` → default `researchConsentGiven: false`
- `app/src/screens/OnboardingScreen.tsx` → five §14.3 disclosure rows; handleContinue writes the three research-consent preference fields
- Locale keys (all four bundles): `onboarding.consent.itemLogging`, `itemQaStorage`, `itemOralRecording`, `itemAnonymization`, `itemWithdrawal` (replacing `rowLocal`/`rowCloud`/`rowNoServer`, removed identically from all four)
- `app/tests/phase4/onboarding-consent.test.mjs` (new)

**Rest of phase:** wire contract/migration/validation (04-01); Worker ingest + four-file export (04-02); client recommendation projection (04-03 — its tests build affirmative-consent fixtures against THIS plan's version gate); D-15 docs + STUDY-02 traceability (04-05); pilot protocol + operator gate (04-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Versioned research consent — service, preferences, defaults, gate consumers</name>
  <files>app/src/services/research-consent.service.ts, app/src/types/index.ts, app/src/services/settings.service.ts, app/tests/phase4/onboarding-consent.test.mjs, app/tests/phase1/consented-prewarm.test.mjs, app/tests/services/interaction-log.service.test.mjs</files>
  <read_first>
    - app/src/services/research-consent.service.ts (entire 16-line file — current gate reads onboardingCompleted + aiConsentGiven)
    - app/src/types/index.ts lines 118-131 (AppPreferences shape and doc-comment style)
    - app/src/services/settings.service.ts lines 35-50 (preference defaults block)
    - app/src/App.tsx lines 290-345 (gate consumers: ParticipantRouteGate, startResearchSession, boot reconciliation, prewarm — signatures must not change)
    - app/tests/services/interaction-log.service.test.mjs and app/tests/phase1/consented-prewarm.test.mjs (how existing tests construct affirmative-consent preference fixtures — these fixtures need the new fields)
  </read_first>
  <behavior>
    - Gate false when preferences are `{onboardingCompleted: true, aiConsentGiven: true}` only (legacy install)
    - Gate false when researchConsentGiven true but researchConsentVersion is absent or !== RESEARCH_CONSENT_VERSION (stale copy)
    - Gate true when onboardingCompleted true + researchConsentGiven true + researchConsentVersion === RESEARCH_CONSENT_VERSION (aiConsentGiven not consulted)
    - resolveParticipantRoute returns 'onboarding' for a bound install failing the gate (re-consent path), 'participant' when passing
    - interactionLog.record refuses to persist (returns unpersisted) under a legacy-only preference state
  </behavior>
  <action>
    In `app/src/services/research-consent.service.ts`: add `export const RESEARCH_CONSENT_VERSION = 1;` and rewrite `hasAffirmativeResearchConsent()` to read `{ onboardingCompleted, researchConsentGiven, researchConsentVersion }` from `settingsService.getSync().preferences` and return the three-way conjunction from the behavior list. Per D-04, do NOT read aiConsentGiven here — it remains the separate LLM-provider gate consumed by prewarm/Ask paths. `resolveParticipantRoute` logic is unchanged (it already delegates to the gate).

    In `app/src/types/index.ts` `AppPreferences`: add `researchConsentGiven?: boolean`, `researchConsentVersion?: number`, `researchConsentGivenAt?: string` with doc comments citing §14.3/D-04. In `app/src/services/settings.service.ts` defaults: add `researchConsentGiven: false` beside the existing `aiConsentGiven: false` default (version/timestamp stay undefined until granted).

    Create `app/tests/phase4/onboarding-consent.test.mjs` (create the tests/phase4/ directory) with executable service assertions for every behavior bullet, driving real `settingsService` preference writes per the fixture style of the existing service tests — assert gate transitions through the service, not by regexing the service source. Include the interactionLog refusal case by composing the real interaction-log factory with a legacy-only preference state.

    Update existing fixtures that construct affirmative consent (grep app/tests for `aiConsentGiven` in preference-fixture objects — at minimum tests/phase1/consented-prewarm.test.mjs and tests/services/interaction-log.service.test.mjs): add `researchConsentGiven: true, researchConsentVersion: 1` so they satisfy the new gate. Do not weaken any assertion; only extend fixture state. Then run the full app suite and repair any remaining fixture that legitimately constructed affirmative consent the old way (upload-queue, phase2, screen tests may also touch it).
  </action>
  <verify>
    <automated>cd app && node --test tests/phase4/onboarding-consent.test.mjs tests/phase1/consent-gate.test.mjs tests/services/interaction-log.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/phase4/onboarding-consent.test.mjs` passes with legacy-flag, stale-version, and current-version cases in TAP output
    - `cd app && npm test` fully green after fixture updates
    - `cd app && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Research consent is an explicit, versioned, independently stored preference; every downstream consumer (routes, logging, session, uploads) inherits the stricter gate through the one shared function.</done>
</task>

<task type="auto">
  <name>Task 2: Five-item §14.3 consent step + canonical EN copy</name>
  <files>app/src/screens/OnboardingScreen.tsx, app/src/locales/en.json, app/tests/phase4/onboarding-consent.test.mjs, app/tests/phase1/consent-gate.test.mjs</files>
  <read_first>
    - app/src/screens/OnboardingScreen.tsx (entire file — Step machine, consent-step row rendering with icon + t() rows, handleContinue preferences write)
    - app/src/locales/en.json onboarding.consent namespace (lines ~630-638: title/intro/rowLocal/rowCloud/rowNoServer/consent/continue)
    - app/tests/phase1/consent-gate.test.mjs (source-regex assertions on OnboardingScreen that must stay true: aiConsentGiven true write, no skip handler)
    - docs/research_system_design.md §14.3 (the five consent items — authoritative wording basis)
  </read_first>
  <action>
    In `app/src/locales/en.json` under `onboarding.consent`: keep `title`, `intro`, `consent`, `continue`; REMOVE `rowLocal`, `rowCloud`, `rowNoServer`; ADD five keys with this canonical EN copy —
    `itemLogging`: "Your interactions inside QuestionTrace (posts you open, time on posts, taps, questions, saves) are logged for research.";
    `itemQaStorage`: "Questions you ask the AI and the answers you receive are stored and securely uploaded for research analysis.";
    `itemOralRecording`: "Your spoken answers in the pre-test and post-test interviews are audio-recorded by the researcher and transcribed for analysis.";
    `itemAnonymization`: "Your study data is stored under a neutral participant ID and anonymized according to the study protocol.";
    `itemWithdrawal`: "You may withdraw from the study at any time as described in the study protocol; contact the research team to withdraw.".
    Update `consent` to: "I have read the five points above and agree to take part in this study." and `intro` to: "Before you begin, please review what taking part in this study involves:". Keep the wording generic where protocol-specific ("as described in the study protocol") — final withdrawal contact wording is study-owner-approved via the 04-06 preflight checklist; the app must not invent an IRB determination.

    In `app/src/screens/OnboardingScreen.tsx`: replace the three data-flow rows in the consent step with the five disclosure rows (map over the five t() keys, reusing the existing row layout/inline-style pattern; pick five lucide icons, e.g. Activity, MessageCircle, Mic, UserRound, LogOut — the Mic icon ILLUSTRATES the external recording disclosure; no recording capability, permission, or plugin is added). In `handleContinue`, extend the existing `set('preferences', {...})` object with `researchConsentGiven: true, researchConsentVersion: RESEARCH_CONSENT_VERSION, researchConsentGivenAt: new Date().toISOString()` (import RESEARCH_CONSENT_VERSION from '../services/research-consent.service'); keep `aiConsentGiven: true` (D-05 — study-build LLM, consent-only). The Step union stays exactly 'welcome' | 'language' | 'consent' — no topic step (D-03), no key-entry step (D-05).

    Extend `app/tests/phase4/onboarding-consent.test.mjs` with thin source invariants on OnboardingScreen (this is JSX, so source assertions are the sanctioned form per 04-RESEARCH.md Wave 0 note): the five `onboarding.consent.item*` t() keys are each referenced; the Step type literal remains the exact three-member union; `researchConsentVersion: RESEARCH_CONSENT_VERSION` appears in the preferences write; no `apiKey` or topic-selection token appears in the onboarding source. Update `app/tests/phase1/consent-gate.test.mjs` only if its existing regexes reference the removed row keys.
  </action>
  <verify>
    <automated>cd app && node --test tests/phase4/onboarding-consent.test.mjs tests/phase1/consent-gate.test.mjs && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/phase4/onboarding-consent.test.mjs` passes with the five-key source assertions
    - `grep -c "onboarding.consent.item" app/src/screens/OnboardingScreen.tsx` returns 5 or more
    - `cd app && npx tsc -b --noEmit` exits 0 and `npm run lint` reports 0 errors
  </acceptance_criteria>
  <done>The consent step enumerates all five §14.3 disclosures with canonical EN copy, grants versioned research consent on continue, and onboarding's three-step shape is test-pinned.</done>
</task>

<task type="auto">
  <name>Task 3: zh/es/ja consent translations + bundle parity</name>
  <files>app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json</files>
  <read_first>
    - app/src/locales/en.json onboarding.consent namespace (the Task 2 canonical copy — source of truth)
    - app/scripts/translate-locales.md (the translation-prompt workflow and review rules)
    - app/tests/locales/bundle-parity.test.mjs (the gate that fails on any key-set divergence)
    - CLAUDE.md "i18n Workflow" (proper nouns untranslated; placeholders verbatim; Spanish ~20% longer; remove keys identically across all four)
  </read_first>
  <action>
    Following the EN-first workflow: translate the new/changed `onboarding.consent` values (itemLogging, itemQaStorage, itemOralRecording, itemAnonymization, itemWithdrawal, intro, consent) into zh.json, es.json, and ja.json, and remove `rowLocal`/`rowCloud`/`rowNoServer` from all three (matching the EN removal so all four bundles keep identical key sets). Keep "QuestionTrace" and "AI" untranslated in every locale. Register-match each locale's existing onboarding strings (zh: plain 你/请 register; es: usted-neutral informational register as used elsewhere in the bundle — check existing onboarding strings; ja: です/ます polite form). These are consent disclosures — translate meaning precisely, especially "audio-recorded by the researcher and transcribed" (external recording, not the app) and "withdraw ... as described in the study protocol". Translation happens at build time in this task — the app never invokes an LLM for UI copy at runtime.
  </action>
  <verify>
    <automated>cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs` passes (identical key sets across en/zh/es/ja)
    - `node -e "for (const l of ['zh','es','ja']) { const b=require('./app/src/locales/'+l+'.json'); const c=b.onboarding.consent; for (const k of ['itemLogging','itemQaStorage','itemOralRecording','itemAnonymization','itemWithdrawal']) if(!c[k]) process.exit(1); } console.log('locales ok')"` run from repo root prints "locales ok"
    - Each translated itemOralRecording value names the recording as performed externally/by the researcher (human-review check recorded in the SUMMARY)
  </acceptance_criteria>
  <done>All four bundles carry the five-item consent copy with identical key sets; parity and missing-key gates are green in the same plan that introduced the EN strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant→consent gate | Consent state controls all research persistence and participant routes |
| legacy install→new build | Old preference state must not satisfy the new disclosure requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Elevation of privilege | legacy aiConsentGiven bypass | high | mitigate | Version-gated conjunction; explicit legacy-state test routes back to onboarding (Task 1) |
| T-04-17 | Repudiation | unverifiable consent grant | medium | mitigate | researchConsentGivenAt timestamp + researchConsentVersion persisted at grant time |
| T-04-18 | Information disclosure | undisclosed collection | high | mitigate | Five §14.3 items rendered and test-pinned; logging/upload gate shares the same function, so nothing persists pre-consent |
| T-04-19 | Spoofing | machine-translated consent drift | medium | mitigate | Build-time human-reviewed translations only; runtime LLM translation prohibited; parity gate blocks divergence |
</threat_model>

<verification>
From app/: `npm test` fully green, `npm run lint` 0 errors, `npx tsc -b --noEmit` clean. Locale parity + missing-key green. In-browser/device rendering of the consent step in all four locales is deferred to phase UAT (04-VALIDATION.md Manual-Only table).
</verification>

<success_criteria>
- Wave 0 gap "app/tests/phase4/onboarding-consent.test.mjs" closed with executable gate assertions plus thin JSX source invariants
- Required Executable Assertion 8 from 04-RESEARCH.md implemented as passing tests
- STUDY-01 consent surface is IRB-ready per D-04/D-14 (five disclosures, versioned acceptance, four locales)
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-04-SUMMARY.md` when done.
</output>
