---
phase: 04-study-infrastructure-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/research-wire-contract.v1.json
  - research-backend/migrations/0004_recommendations.sql
  - research-backend/src/validation.ts
  - research-backend/test/validation.test.mjs
  - research-backend/test/migration-schema.test.mjs
autonomous: true
requirements: [STUDY-03]
must_haves:
  truths:
    - "The shared wire contract v1 declares a recommendation record kind with the exact eight-value strategy allowlist, without changing the contract version string"
    - "A D1 migration exists that creates a recommendations table whose columns mirror the wire record plus server identity and received_at, with condition/strategy/positivity CHECK constraints"
    - "Backend parseIngest accepts a valid recommendation wire record and rejects extras, identity fields, unknown strategies, non-finite scores, non-positive batch positions, and ambiguous event/QA/recommendation shapes"
  artifacts:
    - "shared/research-wire-contract.v1.json (recommendation metadata block)"
    - "research-backend/migrations/0004_recommendations.sql"
    - "research-backend/src/validation.ts (parseRecommendation + dispatch)"
    - "research-backend/test/migration-schema.test.mjs"
  key_links:
    - "shared/research-wire-contract.v1.json is imported by BOTH research-backend/src/validation.ts and app/src/services/research-wire-contract.ts — additions must be additive and the version string must stay 'research-ingest-v1'"
    - "parseIngest dispatch order: kind-bearing records must be checked before the eventType/revision branches or recommendation records fall through to the final throw"
  prohibitions:
    - "No §14.2-excluded field (location, contacts, clipboard, device/app telemetry, free-form payload) is added to any wire allowlist"
    - "No new npm package is installed (Phase 4 needs none per 04-RESEARCH.md)"
    - "No wire contract version bump — additive extension only"
    - "No userId/condition/topicId accepted from the client wire (forbiddenIdentityFields stays enforced)"
---

<objective>
Lock the additive recommendation export contract (D-09): extend the shared wire contract with recommendation kind/strategy metadata, create the D1 `recommendations` table migration, and teach backend validation to accept exactly one new record shape while rejecting everything else.

Purpose: STUDY-03 requires recommendations (reasonText, strategy, contributing trace IDs, served batch order) to reach the researcher export. This plan is the contract layer both the Worker (plan 04-02) and the client (plan 04-03) build against, so it ships first (backend-first additive rollout per 04-RESEARCH.md Pattern 5).
Output: shared contract metadata, migration 0004, backend validation branch, and executable contract tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-study-infrastructure-pilot/04-CONTEXT.md
@.planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md
@shared/research-wire-contract.v1.json
@research-backend/src/validation.ts
@research-backend/migrations/0001_init.sql
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (04-01):**
- `shared/research-wire-contract.v1.json` → new `recommendation` metadata block: `kind: "recommendation"`, `strategies` (8 values), `limits` (`reasonText: 2048`, `contributorArrayItems: 64`, `componentScoreKeys: 32`, `componentScoreKeyLength: 64`)
- `research-backend/migrations/0004_recommendations.sql` → D1 table `recommendations` (columns: id, user_id, condition, topic_id, session_id, batch_id, batch_seq, batch_position, post_id, generated_at, strategy, score, reason_text, contributing_question_ids, contributing_concept_ids, contributing_post_ids, component_scores, received_at) + index `recommendations_session_order` on `(user_id, session_id, batch_seq, batch_position)`
- `research-backend/src/validation.ts` → `RECOMMENDATION_FIELDS` set, `RECOMMENDATION_STRATEGIES` set, `parseRecommendation()` function, extended `parseIngest` dispatch
- `research-backend/test/migration-schema.test.mjs` (new)

**Rest of phase (for reference):** Worker ingest branch + `bindRecommendationInsert` + admin status count + export selects (04-02); `RECOMMENDATION_COLUMNS`/`PARTICIPANT_COLUMNS`/4-entry `buildExportZip` + `recommendations.csv`/`participants.csv` (04-02); `RecommendationResearchRecord` type, client wire branch, `UploadRecord` union extension, `app/src/services/recommendation-research.service.ts` with `projectRecommendationResearchRecords()`/`captureRecommendationResearch()`, post-ready-batch hook, App boot projection (04-03); `RESEARCH_CONSENT_VERSION`, version-gated `hasAffirmativeResearchConsent()`, `AppPreferences.researchConsentGiven/researchConsentVersion/researchConsentGivenAt`, five-item consent step, locale keys `onboarding.consent.itemLogging|itemQaStorage|itemOralRecording|itemAnonymization|itemWithdrawal` in 4 bundles (04-04); D-15 doc wording updates (04-05); `docs/pilot_protocol.md` (04-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add recommendation metadata to the shared wire contract and create the D1 migration</name>
  <files>shared/research-wire-contract.v1.json, research-backend/migrations/0004_recommendations.sql, research-backend/test/migration-schema.test.mjs</files>
  <read_first>
    - shared/research-wire-contract.v1.json (current version/limits/forbiddenIdentityFields — additions must not disturb existing keys)
    - research-backend/migrations/0001_init.sql (CHECK-constraint and column-naming conventions to mirror)
    - research-backend/migrations/0002_install_tokens.sql (index-naming convention)
    - .planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md "Pattern 2: Additive Authenticated Worker Ingest" (authoritative column list and constraint rules)
    - app/src/domain/content.types.ts lines 106-122 (RecommendationStrategy union — the 8 exact strategy strings — and Recommendation field names)
  </read_first>
  <action>
    Per D-09, extend `shared/research-wire-contract.v1.json` with one additive top-level key `recommendation` containing: `"kind": "recommendation"`; `"strategies": ["topic_baseline", "quality_baseline", "diversity_baseline", "continue", "deepen", "contrast", "bridge", "echo"]` (must exactly match the RecommendationStrategy union in app/src/domain/content.types.ts:106); and `"limits": { "reasonText": 2048, "contributorArrayItems": 64, "componentScoreKeys": 32, "componentScoreKeyLength": 64 }`. Do NOT change `version`, `routes`, `limits`, or `forbiddenIdentityFields` — the app-side contract module throws on version mismatch and freezes existing limits, so the change must be purely additive.

    Create `research-backend/migrations/0004_recommendations.sql` with `CREATE TABLE IF NOT EXISTS recommendations` using exactly these columns and constraints: `id TEXT PRIMARY KEY`; `user_id TEXT NOT NULL`; `condition TEXT NOT NULL CHECK (condition IN ('control', 'experimental'))`; `topic_id TEXT NOT NULL`; `session_id TEXT NOT NULL`; `batch_id TEXT NOT NULL`; `batch_seq INTEGER NOT NULL CHECK (batch_seq > 0)`; `batch_position INTEGER NOT NULL CHECK (batch_position > 0)`; `post_id TEXT NOT NULL`; `generated_at TEXT NOT NULL`; `strategy TEXT NOT NULL CHECK (strategy IN (...))` listing the same 8 strategies; `score REAL NOT NULL`; `reason_text TEXT NOT NULL`; `contributing_question_ids TEXT`; `contributing_concept_ids TEXT`; `contributing_post_ids TEXT`; `component_scores TEXT` (all four JSON-text columns, nullable); `received_at TEXT NOT NULL`. Add `CREATE INDEX IF NOT EXISTS recommendations_session_order ON recommendations(user_id, session_id, batch_seq, batch_position)` — non-unique on purpose: `INSERT OR IGNORE` must never silently discard a row via an unrelated uniqueness rule (04-RESEARCH.md Pattern 2). The serving timestamp is export-derived from feed_impression events, never a stored column in this table.

    Create `research-backend/test/migration-schema.test.mjs` (node:test + assert/strict, matching the header style of research-backend/test/validation.test.mjs): read `migrations/0004_recommendations.sql` with node:fs, and assert via regex/string checks: the table name; each of the 18 column names above; the condition CHECK lists exactly control and experimental; the strategy CHECK lists all 8 strategies; batch_seq and batch_position have `> 0` CHECKs; `id TEXT PRIMARY KEY` is present; the index name and its 4-column ordering; and the stored schema contains no serving-timestamp column (assert the only timestamp columns are generated_at and received_at). Also assert cross-file consistency: parse `shared/research-wire-contract.v1.json` and check its `recommendation.strategies` array equals the 8 strategies named in the migration's strategy CHECK.
  </action>
  <verify>
    <automated>cd research-backend && node --test test/migration-schema.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/migration-schema.test.mjs` passes from research-backend/
    - `node -e "const c=require('./shared/research-wire-contract.v1.json'); if(c.version!=='research-ingest-v1')process.exit(1); if(c.recommendation.strategies.length!==8)process.exit(1); console.log('contract ok')"` run from repo root prints "contract ok"
    - `git diff shared/research-wire-contract.v1.json` shows only an added `recommendation` key (existing keys byte-identical)
    - Existing backend suite still green: `cd research-backend && npm test`
  </acceptance_criteria>
  <done>Shared contract carries the recommendation kind/strategies/limits metadata additively; migration 0004 defines the recommendations table + index exactly as specified; the schema assertion test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend validation — parseRecommendation with exact allowlist and ambiguity rejection</name>
  <files>research-backend/src/validation.ts, research-backend/test/validation.test.mjs</files>
  <read_first>
    - research-backend/src/validation.ts (entire file — parseEvent/parseQuestionAnswer/parseIngest patterns, assertAllowedFields/assertString/assertTimestamp/assertStringArray helpers to reuse)
    - research-backend/test/validation.test.mjs (existing test structure and fixture style to extend)
    - shared/research-wire-contract.v1.json (recommendation metadata added in Task 1 — read the values, do not re-derive)
    - .planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md "Pattern 1" wire-shape table (field-by-field validation rules)
  </read_first>
  <behavior>
    - A valid recommendation record (kind, id, batchId, sessionId, batchSeq, batchPosition, postId, generatedAt, strategy, score, reasonText, plus optional contributing arrays and componentScores) parses and is returned with kind 'recommendation'
    - Records with any non-allowlisted key (including userId, condition, topicId) throw ValidationError
    - strategy outside the 8-value allowlist throws; score NaN/Infinity/-Infinity/non-number throws; batchSeq or batchPosition zero, negative, fractional, or non-number throws
    - reasonText empty or longer than contract recommendation.limits.reasonText throws; contributing arrays with >64 items, empty-string items, or items longer than contract.limits.postId throw; componentScores non-object, >32 keys, key longer than 64 chars, or non-finite value throws
    - Ambiguous shapes throw: a record carrying kind together with eventType, or kind together with revision, or kind with any value other than the recommendation constant
    - Existing event and question/answer parsing behavior is unchanged (all pre-existing tests stay green)
  </behavior>
  <action>
    In `research-backend/src/validation.ts`: add `const RECOMMENDATION_FIELDS = new Set(['kind','id','batchId','sessionId','batchSeq','batchPosition','postId','generatedAt','strategy','score','reasonText','contributingQuestionIds','contributingConceptIds','contributingPostIds','componentScores'])` and `export const RECOMMENDATION_STRATEGIES = new Set(contract.recommendation.strategies)`.

    Add `function parseRecommendation(record)` following the parseEvent/parseQuestionAnswer style: `assertAllowedFields(record, RECOMMENDATION_FIELDS, 'Recommendation record')`; assertString id/batchId/sessionId with maxLength `contract.limits.id`; assert `batchSeq` and `batchPosition` with `Number.isSafeInteger(v) && v > 0` (distinct error messages); assertString postId with `contract.limits.postId`; assertTimestamp generatedAt; require `RECOMMENDATION_STRATEGIES.has(record.strategy)`; require `typeof record.score === 'number' && Number.isFinite(record.score)`; assertString reasonText with maxLength `contract.recommendation.limits.reasonText`; for each optional contributing array reuse the bounded-array rule but with the tighter bounds: array length <= `contract.recommendation.limits.contributorArrayItems`, each item a non-empty string <= `contract.limits.postId` (write a small local helper rather than loosening the existing assertStringArray); for optional componentScores require a plain object (reuse isRecord), <= `contract.recommendation.limits.componentScoreKeys` keys, each key a non-empty string <= `contract.recommendation.limits.componentScoreKeyLength`, each value a finite number. Require `record.kind === contract.recommendation.kind`. Return `{ ...record }` (kind already present).

    Extend the `parseIngest` per-record dispatch: BEFORE the existing eventType/revision branches, add — if `Object.hasOwn(record, 'kind')`: throw ValidationError if the record also has `eventType` or `revision` (ambiguous shape), throw if `record.kind !== 'recommendation'`, else return `parseRecommendation(record)`. Leave the existing event/QA branches and the final throw untouched.

    Extend `research-backend/test/validation.test.mjs` with a helper that builds a valid recommendation fixture and tests covering every behavior bullet above, including a boundary-accept case (batchPosition 1, score 0, reasonText at exactly the limit) and identity-field rejection for each of userId, condition, topicId.
  </action>
  <verify>
    <automated>cd research-backend && node --test test/validation.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd research-backend && node --test test/validation.test.mjs` passes with new recommendation cases visible in the TAP output
    - `cd research-backend && npm test` passes (no regression in ingest/export/admin/cors suites — worker changes come in plan 04-02, so ingest of kind-bearing records through the Worker is NOT expected to succeed yet; only pure validation is in scope)
    - A fixture with `{ kind: 'recommendation', eventType: 'app_open', ... }` is rejected with a ValidationError (ambiguity test present in the file)
  </acceptance_criteria>
  <done>parseIngest recognizes exactly one new kind with a positive field allowlist, strict numeric/strategy/text bounds, identity rejection, and ambiguity rejection; all backend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/v1/ingest | Untrusted participant device submits recommendation wire records |
| shared contract file | Single source of truth consumed by two runtimes (app + Worker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | parseRecommendation / RECOMMENDATION_FIELDS | high | mitigate | Positive field allowlist + exact strategy set + finite-number checks; identity fields rejected by allowlist absence (Task 2 tests) |
| T-04-02 | Information disclosure | wire allowlist | high | mitigate | No §14.2 category field exists in RECOMMENDATION_FIELDS; free-form payload keys rejected (assertAllowedFields) |
| T-04-03 | Denial of service | oversized arrays/objects | medium | mitigate | contributorArrayItems=64, componentScoreKeys=32, reasonText=2048 bounds enforced pre-insert; request stays under existing 256 KiB cap |
| T-04-04 | Tampering | contract version drift | medium | mitigate | Version string unchanged; app module throws on mismatch; migration-schema test asserts strategy parity between contract and SQL |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed in this phase (04-RESEARCH.md "Installation: none"); no install task exists in any Phase 4 plan |
</threat_model>

<verification>
From research-backend/: `npm test` fully green. From repo root: `git diff --stat` touches only the five files in frontmatter. Contract JSON parses and keeps version 'research-ingest-v1'.
</verification>

<success_criteria>
- Wave 0 gap "Extend research-backend/test/validation.test.mjs" closed with executable allowlist/bounds/identity/ambiguity assertions
- Wave 0 gap "Migration/schema assertion for the recommendations D1 table and indexes" closed by migration-schema.test.mjs
- Plans 04-02 and 04-03 can build against a frozen, tested contract
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-01-SUMMARY.md` when done.
</output>
