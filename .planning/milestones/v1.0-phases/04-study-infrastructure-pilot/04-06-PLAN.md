---
phase: 04-study-infrastructure-pilot
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - docs/pilot_protocol.md
autonomous: false
requirements: [STUDY-05, STUDY-04, RQ-03]
must_haves:
  truths:
    - "docs/pilot_protocol.md exists as an executable operator checklist covering preflight, per-participant run, export audit, issue loop, and exit gate"
    - "The protocol names the pool re-freeze as a hard preflight precondition: the packaged pilot-v1-20260717 pool predates the typed-graph exporter and the app fails with POOL_INVALID until the operator re-freezes offline"
    - "The protocol enforces backend-first rollout order: apply migration 0004 to live D1 and deploy the Worker BEFORE distributing a client build that emits recommendation records"
    - "The protocol defines the external oral-test workflow: exact user_id filenames, three assessment stages, the §13.4 behavioral/rubric field set, §13.5 normalization formulas, and blind scoring before condition join (§13.6)"
    - "The protocol contains a manual UAT evidence template and a release-blocker issue-log format"
    - "A blocking operator checkpoint gates phase completion on live migration/deploy/smoke evidence"
  artifacts:
    - "docs/pilot_protocol.md"
  key_links:
    - "participants.csv.user_id ↔ external audio/transcript/rubric filenames (the STUDY-04/RQ-03 join)"
    - "0004_recommendations.sql → wrangler d1 migrations apply → wrangler deploy → /admin status recommendation count → /admin/export.zip four files (the live rollout chain)"
  prohibitions:
    - "No mutation of data/content_pool_v1 — pool corrections are a new version via the offline pipeline (D-12)"
    - "No in-app audio capture, oral-test screens, or prompt rendering (D-06/D-07); oral prompts live in this external protocol doc, not the app (D-08)"
    - "No secrets (enrollment credential, admin password, install tokens, Cloudflare credentials) recorded in the protocol, issue log, or repo"
    - "No claim that STUDY-05 is complete before the operator confirms a 3–5-user end-to-end run with issue closure (D-13)"
    - "No condition labels on rater-facing materials before blind scoring is locked (§13.6)"
---

<objective>
Deliver the STUDY-05 pilot instrument's operating manual (D-13) and the operator gate that makes the deployment real: a written pilot protocol/checklist covering enrollment, condition seeding, oral-test administration (D-06/D-07/D-08), export audit, and the issue loop — plus a blocking checkpoint for the live D1 migration + Worker deploy + smoke test that must precede any client distribution.

Purpose: D-14 defines IRB-ready as app + docs; the 3–5-person run is operator work a code phase cannot perform, so the phase ships the instrument, the protocol, and hard evidence gates. This plan also carries the STUDY-04/RQ-03 external contract: the scoring sheet fields and formulas that make oral-explanation quality measurable from the export join.
Output: docs/pilot_protocol.md + operator checkpoint evidence.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md
@.planning/phases/04-study-infrastructure-pilot/04-VALIDATION.md
@docs/research_system_design.md
@research-backend/wrangler.jsonc
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (04-06):**
- `docs/pilot_protocol.md` — operator protocol with sections: Preflight, Enrollment & Condition Seeding, Per-Participant Run, Oral Assessment (external), External Scoring Sheet Contract, Export Audit, UAT Evidence Template, Issue Log & Exit Gate
- Operator checkpoint evidence (live migration/deploy/smoke) recorded in 04-06-SUMMARY.md

**Rest of phase:** wire contract/migration/validation (04-01); Worker ingest + four-file export with `recommendations.csv`/`participants.csv` (04-02); client projection service (04-03); versioned five-item consent in four locales (04-04); STUDY-02 traceability + D-15 doc corrections (04-05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/pilot_protocol.md — executable operator checklist</name>
  <files>docs/pilot_protocol.md</files>
  <read_first>
    - .planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md "Pilot Protocol / Checklist Requirements" (authoritative section-by-section content: preflight, per-participant, export audit, issue loop) and "STUDY-04 / RQ-03 Boundary and Analysis Contract" (the external scoring-sheet field table and normalization formulas)
    - docs/research_system_design.md §13.2–§13.6 (oral assessment stages, scoring dimensions, normalization, blind scoring), §14.2 (do-not-collect audit list), §21 (final success review items)
    - research-backend/src/export.ts (final RECOMMENDATION_COLUMNS/PARTICIPANT_COLUMNS from 04-02 — the audit section must name the real headers)
    - research-backend/migrations/0004_recommendations.sql (migration the preflight applies)
    - .planning/phases/04-study-infrastructure-pilot/04-VALIDATION.md "Manual-Only Verifications" (rows the UAT template must cover)
  </read_first>
  <action>
    Create `docs/pilot_protocol.md` as a checkbox-driven operator document with these sections and concrete content (research framing rules from docs/SCOPE.md apply: "post-centered graph-memory feed orchestration", never "AI learning feed"):

    1. **Preflight** — (a) Pool re-freeze: confirm the packaged pool has been re-frozen offline with the typed Phase 3 exporter so the manifest carries `sources.json`, `global_edges.json`, and `ranking_features.json`; regenerate the packaged projection and confirm `cd app && npm run build` passes. State explicitly: the current `pilot-v1-20260717` manifest predates the typed exporter and the app intentionally fails with POOL_INVALID until then; never edit `data/content_pool_v1` in place — corrections are a NEW pool version (D-12). (b) Backend-first rollout: `cd research-backend && npx wrangler d1 migrations apply <database> --remote` (the operator supplies the live database name bound as `DB` in wrangler.jsonc) then `npx wrangler deploy`; then smoke: authenticated `/admin` shows a recommendation count row; `/admin/export.zip` downloads with exactly four CSV files. Only AFTER this may a client build with the recommendation projection be installed on devices (04-RESEARCH.md Pattern 5: an old backend permanently quarantines the new record kind as server_rejected). (c) Seed 3–5 numeric `study_accounts` rows with the single frozen topic and an explicit planned control/experimental allocation recorded outside participant-facing materials (D-01/D-02). (d) Study-owner approval of final consent/withdrawal language and the oral-test prompts (04-RESEARCH.md Open Question 1). (e) Secrets rule: enrollment credential, admin password, and Cloudflare credentials are configured privately and never written into this checklist, the issue log, or the repo.

    2. **Per-Participant Run** — numbered steps per 04-RESEARCH.md: assign exact `userId`; resolve/bind once via ResearchSetupScreen; verify no topic picker, no condition disclosure, no API-key entry; administer and externally record Pretest A (general verbal baseline) and Pretest B (domain baseline) with files named `<userId>_pre_verbal` / `<userId>_pre_domain`; complete welcome→language→consent verifying all five §14.3 disclosures render in the participant's locale; exercise Home, Post Detail, suggested + typed Ask, AI answer, source click, save/not-interested, at least two recommendation batches, and reason expansion; include an offline interval + app restart to exercise durable upload retry; end/resume sessions across at least two days; administer and record the post-test as `<userId>_post_domain`. Note: Ask behavior/quality must be indistinguishable between conditions (§6.5–§6.6).

    3. **External Scoring Sheet Contract (STUDY-04/RQ-03)** — a table of required columns per assessment row: exact `user_id`; `assessment_stage` in {pre_verbal, pre_domain, post_domain}; recording/transcript filename; behavioral fields (speaking duration, word count, examples, distinct claims, concept mentions); the eight §13.4 rubric dimensions; blind rater code + per-rater scores for inter-rater reliability (§13.6). Then the §13.5 formulas verbatim: DomainResponseWordCount / GeneralBaselineWordCount; PostDomainRubricScore − PreDomainRubricScore; PostWordCount / GeneralBaselineWordCount. Rule: raters score by neutral user_id only; condition joins from participants.csv strictly AFTER scores are locked. Rule: spreadsheet imports must treat user_id as text (leading zeros preserved) and filenames must use the exact ID string.

    4. **Export Audit** — download the authenticated ZIP; verify exactly four files with the exact headers from export.ts; every displayed recommendation has a row; served_at matches first feed_impression; Q/A rows are latest-revision; reason-view events link to recommendation IDs; both conditions and the single topic present; participants.csv includes every seeded account. §14.2 exclusion review checklist: confirm no column carries screen/audio data, out-of-app usage, geolocation, contacts, other-app names, clipboard, keystroke timing, or free-form device payloads.

    5. **UAT Evidence Template** — a fill-in table matching 04-VALIDATION.md Manual-Only rows: device onboarding/consent in all four locales, seeded-account bind smoke, live deploy smoke, per-participant checklist completion, export audit result — each with evidence column (screenshot/file/row-count) and pass/fail.

    6. **Issue Log & Exit Gate** — issue table columns: issue ID, participant ID (only when necessary), condition, app/backend/pool version, steps, expected/actual, severity, privacy/experimental-validity flag, owner, fix reference, retest result. No question text, audio, or secrets in the log. Release blockers (any one blocks IRB-ready): missing export row, cross-account record, a control row carrying contributing trace IDs, Ask asymmetry between conditions, consent disclosure missing, pool import error, failed user_id join. Exit gate: 3–5 users completed end-to-end, all blockers closed with retest, affected suites re-run green, and the §21 review items confirmed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('docs/pilot_protocol.md','utf8'); const req=['POOL_INVALID','wrangler d1 migrations apply','wrangler deploy','participants.csv','pre_verbal','pre_domain','post_domain','GeneralBaselineWordCount','blind','study_accounts','recommendations.csv','server_rejected']; const missing=req.filter(t=>!p.includes(t)); if(missing.length){console.error('missing:',missing);process.exit(1)} console.log('protocol ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "protocol ok" (all twelve anchor tokens present)
    - docs/pilot_protocol.md contains all six numbered sections with markdown checkboxes in Preflight and Per-Participant Run
    - The scoring-sheet table lists all eight §13.4 rubric dimensions (concept coverage, relationship understanding, stance comparison, counterargument awareness, evidence/example use, transfer, explanatory clarity, overall understanding depth)
    - `grep -c "secret\|credential" docs/pilot_protocol.md` returns 1 or more (the secrets-handling rule is stated) while no actual secret value appears
    - `git diff --stat` shows only docs/pilot_protocol.md changed
  </acceptance_criteria>
  <done>An operator can run the full pilot from this document alone: preconditions, ordered rollout, per-participant script, external oral workflow with a complete scoring contract, export audit, and a blocker-driven exit gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator gate — live D1 migration, Worker deploy, smoke evidence</name>
  <files>.planning/phases/04-study-infrastructure-pilot/04-06-SUMMARY.md</files>
  <read_first>
    - docs/pilot_protocol.md Preflight section (written in Task 1 — the checkpoint instructions must quote its exact commands)
    - research-backend/wrangler.jsonc (the DB binding; the live database name is operator-supplied)
    - .planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md "Pattern 5: Backend-First Additive Rollout" (why this gate blocks client distribution)
  </read_first>
  <action>Pause execution and present the checkpoint below to the operator. This is the schema-push gate: migration 0004 must be applied to the LIVE D1 database and the Worker deployed BEFORE any pilot device receives the client build from 04-03 (an old backend permanently quarantines the new record kind). The executor performs no deployment itself — live credentials are operator-controlled. Record the operator's evidence (migration listing, deploy confirmation, four-file export listing, pool re-freeze status) verbatim in the SUMMARY.</action>
  <what-built>Plans 04-01/04-02 created migration 0004_recommendations.sql and the four-file export; plan 04-03 made the client emit recommendation records. All local suites are green, but the LIVE Cloudflare D1 database and Worker are operator-controlled — the schema change must be applied and deployed remotely before any pilot device receives the new client build, or recommendation uploads will be permanently quarantined as server_rejected.</what-built>
  <how-to-verify>
    Following docs/pilot_protocol.md Preflight section (b):
    1. `cd research-backend && npx wrangler d1 migrations apply <your-database-name> --remote` — confirm migration 0004 is listed as applied.
    2. `npx wrangler deploy` — confirm the Worker version updates.
    3. Open `/admin` with the admin credentials: the status page must show a recommendations count (0 is expected before any pilot traffic).
    4. Download `/admin/export.zip`: it must contain exactly four files — behavioral-events.csv, question-answer-records.csv, recommendations.csv, participants.csv — with parseable headers.
    5. Report the pool re-freeze status (protocol Preflight (a)): re-frozen and `npm run build` green, or still pending. A pending re-freeze does NOT block this backend gate but blocks pilot start.
  </how-to-verify>
  <resume-signal>Type "deployed" with the export file list (and pool re-freeze status), or describe what failed. The pilot run itself (3–5 users, issue loop, STUDY-05 closure) proceeds afterward via the protocol and is confirmed during phase verification.</resume-signal>
  <verify>
    <human-check>Operator confirms: migration 0004 applied to live D1; Worker deployed; /admin status shows a recommendations count; /admin/export.zip contains exactly the four expected CSV files; pool re-freeze status reported.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator resume message includes the wrangler migrations listing showing 0004 applied and a deploy confirmation
    - Operator resume message lists the four export.zip member filenames exactly (behavioral-events.csv, question-answer-records.csv, recommendations.csv, participants.csv)
    - Pool re-freeze status (done / pending) is stated explicitly
    - All three evidence items are transcribed into 04-06-SUMMARY.md
  </acceptance_criteria>
  <done>Operator evidence (deploy + four-file export smoke + pool re-freeze status) is recorded in 04-06-SUMMARY.md; the backend-first rollout order is proven before client distribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs→operator ops | The protocol drives real deployment, enrollment, and data-handling actions |
| researcher files→raters | External audio/transcripts cross to blind scorers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Information disclosure | secrets in protocol/issue log | high | mitigate | Explicit secrets rule in Preflight; issue-log column set excludes free text secrets/question text; acceptance check confirms rule present without secret values |
| T-04-24 | Availability | client shipped before backend | high | mitigate | Ordered Preflight + blocking Task 2 gate with export-file evidence before device distribution |
| T-04-25 | Information disclosure / scoring bias | condition leak to raters | medium | mitigate | Neutral user_id filenames; condition join only after locked scores; rule stated in scoring contract and export audit |
| T-04-26 | Tampering | pool edited in place to fix pilot issues | high | mitigate | Protocol states corrections create a new pool version via the offline pipeline; frozen-cutover guard test remains in CI |
| T-04-27 | Repudiation | pilot claimed complete without evidence | medium | mitigate | UAT evidence template + exit gate require per-item evidence and issue closure before STUDY-05 is claimable (D-13) |
</threat_model>

<verification>
Task 1 anchor-token check green; Task 2 checkpoint resolved with deploy + four-file export evidence recorded in the SUMMARY. Phase-level: the pilot run and issue closure are confirmed later via /gsd-verify-work against the protocol's UAT template (manual-only per 04-VALIDATION.md).
</verification>

<success_criteria>
- Wave 0 gap "Manual UAT evidence template in docs/pilot_protocol.md" closed
- STUDY-05 instrument + protocol delivered per D-13; IRB-ready definition per D-14 satisfied on the app+docs side
- STUDY-04/RQ-03 external analysis contract (stages, fields, formulas, blinding) documented and joinable to participants.csv
- Live backend gate passed with evidence, enforcing the backend-first rollout order
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-06-SUMMARY.md` when done (include the operator's deploy/smoke evidence and pool re-freeze status).
</output>
