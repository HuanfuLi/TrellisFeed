---
phase: 04-study-infrastructure-pilot
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements: [STUDY-02]
must_haves:
  truths:
    - "The seeded-assignment chain is verified green end to end on the final phase code: D1 study_accounts → /v1/install/resolve → studyContextService.bindOnce (immutable) → recommendationService condition branch → interactionLog condition snapshot"
    - "REQUIREMENTS.md STUDY-01/STUDY-02/STUDY-04 wording matches the locked decisions: one topic (D-01), researcher-assigned condition (D-02), out-of-band oral capture (D-06)"
    - "ROADMAP.md Phase 4 success criteria 1 and 3 no longer claim three topics, topic-stratified randomization, or in-app capture"
    - "PROJECT.md no longer proposes three study topics"
  artifacts:
    - "Updated .planning/REQUIREMENTS.md"
    - "Updated .planning/ROADMAP.md (Phase 4 success criteria)"
    - "Updated .planning/PROJECT.md"
  key_links:
    - "worker.ts resolveAccount SELECT condition, topic_id FROM study_accounts → bindOnce research_metadata write → recommendationService.getRequired() branch → interactionLog identity snapshot"
  prohibitions:
    - "No randomizer, topic picker, condition setter, or any new assignment code — STUDY-02 is verification, not build (D-02)"
    - "No edits outside the three .planning documents"
    - "No weakening of the stale requirement into less than the locked decisions deliver — wording is CORRECTED to the decisions, not simplified"
---

<objective>
Certify STUDY-02 on the final phase code (D-02: researcher-assigned, persisted, immutable condition driving ranker + logging) via the existing executable suites, and execute the D-15 documentation alignment: correct the stale three-topic / randomization / in-app-capture wording in REQUIREMENTS.md, ROADMAP.md, and PROJECT.md to the locked one-topic, researcher-assigned, out-of-band reality.

Purpose: the assignment architecture already satisfies STUDY-02 — what is missing is proof on the final integrated code and planning documents that stop contradicting the locked decisions (04-RESEARCH.md: stale wording "must not drive implementation").
Output: recorded traceability evidence in the SUMMARY + three corrected planning documents.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-study-infrastructure-pilot/04-CONTEXT.md
@.planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (04-05):**
- D-15 wording corrections in `.planning/REQUIREMENTS.md` (STUDY-01, STUDY-02, STUDY-04 lines), `.planning/ROADMAP.md` (Phase 4 success criteria 1 and 3), `.planning/PROJECT.md` (three-topics proposed decision)
- STUDY-02 traceability evidence table in `04-05-SUMMARY.md`

**Rest of phase:** wire contract/migration/validation (04-01); Worker ingest + four-file export with `recommendations.csv`/`participants.csv` (04-02); client projection service + outbox extension (04-03); versioned five-item consent in four locales (04-04); `docs/pilot_protocol.md` + operator deployment gate (04-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run and record the STUDY-02 assignment traceability chain</name>
  <files>.planning/phases/04-study-infrastructure-pilot/04-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md "STUDY-02 Verification, Not Build" (the five verified chain links and the exact suites that cover them)
    - app/src/services/study-context.service.ts (bindOnce immutability semantics to describe accurately in evidence)
    - research-backend/src/worker.ts resolveAccount + handleInstallResolve (server-authoritative assignment source)
  </read_first>
  <action>
    Execute the traceability run on the final integrated phase code (this plan runs in wave 3, after 04-02/04-03/04-04):

    1. `cd app && node --test tests/services/study-context.service.test.mjs tests/services/interaction-log.service.test.mjs tests/services/recommendation.service.test.mjs`
    2. `cd research-backend && node --test test/validation.test.mjs test/ingest.test.mjs`

    All suites must pass. Then write a traceability evidence table into the plan SUMMARY mapping each STUDY-02 clause to its verifying artifact: (a) "assignment is researcher-set and server-authoritative" → worker.ts resolveAccount selects only condition/topic_id from study_accounts and handleInstallResolve requires the enrollment credential; (b) "persisted immutably on-device" → study-context suite's identical-repeat-allowed / identity-change-rejected cases through the dbQuery seam; (c) "drives ranker" → recommendation.service suite's condition-branch and control-isolation (throwing personal-loader) cases; (d) "drives logging condition field" → interaction-log suite's identity-snapshot and caller-identity-rejection cases; (e) "no client identity on the wire" → backend validation identity-rejection cases. Record exact test counts and command output summaries. Do NOT add code, new test files, or an assignment service — per D-02 this requirement is satisfied by the existing architecture; the deliverable is recorded proof.

    Note in the SUMMARY that the live seeded-account smoke check (real D1 row → device bind) is an operator step in docs/pilot_protocol.md (04-06), not automatable from the repository.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/study-context.service.test.mjs tests/services/interaction-log.service.test.mjs tests/services/recommendation.service.test.mjs && cd ../research-backend && node --test test/validation.test.mjs test/ingest.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both commands above exit 0 with all tests passing
    - 04-05-SUMMARY.md contains a five-row traceability table mapping STUDY-02 clauses (a)-(e) to named test files/cases
    - `git status --porcelain -- app research-backend` shows no modifications from this task (verification-only)
  </acceptance_criteria>
  <done>STUDY-02 is certified green on final phase code with recorded, clause-level evidence; no assignment code was added or changed.</done>
</task>

<task type="auto">
  <name>Task 2: D-15 documentation alignment — REQUIREMENTS, ROADMAP, PROJECT</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/04-study-infrastructure-pilot/04-CONTEXT.md D-01, D-02, D-03, D-06, D-07, D-15 (the exact decisions the wording must match)
    - .planning/REQUIREMENTS.md (STUDY-01 through STUDY-05 + RQ-03 lines and the Traceability table)
    - .planning/ROADMAP.md Phase 4 section (success criteria 1-4)
    - .planning/PROJECT.md (locate the proposed-decisions / three-topics wording before editing)
  </read_first>
  <action>
    Apply scoped edits (surgical Edit-style replacements — never rewrite whole files; every other line stays byte-identical):

    1. `.planning/REQUIREMENTS.md`:
       - STUDY-01: replace "participant chooses one of three topics" wording with: onboarding covers welcome, language, and §14.3 consent; the single study topic is bound silently by the server per the seeded account (D-01/D-03); LLM setup remains study-build-provided with consent only (D-05). Keep the §7.1/§13 anchors.
       - STUDY-02: replace "topic-stratified randomization" with: condition is researcher-assigned per seeded D1 `study_accounts` row, resolved by `/v1/install/resolve`, bound once immutably on-device, and drives ranker + logging `condition` (D-02). Keep the §6.5 anchor.
       - STUDY-04: replace in-app capture phrasing with: pre/post oral tests are administered and recorded EXTERNALLY by the researcher (D-06/D-07); files are named by exact `user_id`; the app/export contribution is the `participants.csv` join key; §13.4 scoring dimensions and §13.5 normalization are supported by the export + external protocol. Keep the §13 anchor.
    2. `.planning/ROADMAP.md` Phase 4 "Success Criteria": rewrite criterion 1 to: participant completes onboarding (welcome, language, §14.3 consent, study-provided LLM); the single topic is server-bound with no topic choice; researcher-assigned condition is persisted and drives ranker + logging (D-01/D-02/D-03). Rewrite criterion 3 to: pre/post oral explanations are captured and transcribed via the external researcher protocol (D-06); the export's `participants.csv` join key links them; §13.4 scoring and §13.5 normalization are supported by exported data plus the external scoring sheet. Criteria 2 and 4 stay as-is.
    3. `.planning/PROJECT.md`: update the proposed-decision wording that references three study topics to the locked single-topic instrument (D-01), preserving surrounding formatting and any decision-table structure.

    Constraint: corrections must state what the system NOW delivers under D-01..D-08 — do not soften "IRB-ready", export completeness, or oral-assessment measurability. Do not mark any STUDY requirement checkbox complete; completion states are owned by the verifier.
  </action>
  <verify>
    <automated>grep -n "researcher-assigned" .planning/REQUIREMENTS.md && grep -n "participants.csv" .planning/ROADMAP.md && node -e "const fs=require('fs'); const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const road=fs.readFileSync('.planning/ROADMAP.md','utf8'); if(/one of three topics/.test(req)||/topic-stratified randomization/.test(req)||/topic-stratified randomization/.test(road)) process.exit(1); console.log('wording ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above exits 0 and prints "wording ok" (retired phrases absent from REQUIREMENTS.md and the ROADMAP Phase 4 criteria; the replacement wording must describe the new reality in its own words and must not quote the retired phrases)
    - `grep -n "study_accounts" .planning/REQUIREMENTS.md` finds the STUDY-02 line (researcher-assignment wording names the seeded table)
    - `grep -c "D-0" .planning/REQUIREMENTS.md` returns 1 or more (decision IDs cited in the corrected lines)
    - `git diff --stat` shows changes only in .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md
    - No STUDY-0x checkbox flipped from `[ ]` to `[x]` in this task
  </acceptance_criteria>
  <done>All three planning documents describe the locked one-topic, researcher-assigned, out-of-band instrument; no stale wording can mislead verification or future phases (D-15 complete).</done>
</task>

</tasks>

<!-- planner-discipline-allow: one of three topics -->
<!-- planner-discipline-allow: topic-stratified randomization -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→future agents | Planning documents drive later implementation and verification decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Repudiation | requirement/decision divergence | medium | mitigate | D-15 corrections cite decision IDs inline so audits trace wording to locked decisions |
| T-04-21 | Tampering | doc edit reintroducing dropped scope | medium | mitigate | Verify command proves retired phrases are gone; prohibitions forbid new assignment/topic code; verification-only Task 1 proves no source changed |
| T-04-22 | Spoofing | claiming STUDY-02 built rather than verified | low | accept | SUMMARY records the chain as pre-existing architecture with test evidence; honest framing per D-02 |
</threat_model>

<verification>
Task 1 commands green with zero source modifications; Task 2 wording checks green; `git status` shows only the three .planning documents changed.
</verification>

<success_criteria>
- STUDY-02 clause-level traceability evidence recorded on final phase code
- D-15 fully executed: REQUIREMENTS.md, ROADMAP.md Phase 4 criteria, PROJECT.md aligned with D-01/D-02/D-03/D-06/D-07
- No new assignment code, randomizer, or topic UI introduced anywhere
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-05-SUMMARY.md` when done (include the five-row STUDY-02 traceability table from Task 1).
</output>