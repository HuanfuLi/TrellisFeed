---
phase: 04-study-infrastructure-pilot
plan: 07
type: execute
wave: 4
depends_on: ["04-06"]
files_modified:
  - research-backend/wrangler.jsonc
  - research-backend/test/deployment-config.test.mjs
  - docs/pilot_protocol.md
  - .planning/phases/04-study-infrastructure-pilot/04-UAT.md
  - .planning/phases/04-study-infrastructure-pilot/04-VERIFICATION.md
  - .planning/debug/phase4-android-webview-cors.md
  - .planning/debug/resolved/phase4-android-webview-cors.md
autonomous: false
gap_closure: true
requirements: [STUDY-03, STUDY-05]
must_haves:
  truths:
    - "The tracked canonical Wrangler config supplies the exact approved non-secret origin allowlist to question-trace-research-collector; wildcard, suffix, and reflected-origin policies are forbidden"
    - "Enrollment and admin credentials remain Worker secrets and never appear under tracked vars or in test, protocol, plan, summary, UAT, or debug evidence"
    - "A regression test loads the canonical deployment config itself and proves both public endpoints grant exact-origin preflights for every approved origin while an unapproved origin remains denied"
    - "The deployment protocol cannot pass on origin-less admin/export smoke alone: it requires uploaded-version binding inspection and real-origin OPTIONS checks for /v1/install/resolve and /v1/ingest"
    - "After an operator-authorized canonical redeploy, the 100%-traffic Worker version visibly carries RESEARCH_ALLOWED_ORIGINS and both live Android-WebView preflights return 204 with the exact https://localhost ACAO grant"
    - "A freshly cleared API 36.1 installation binds seeded account 1001 and uploads a real research record through the canonical Worker using ADB only, without a fetch stub, browser devtools injection, or desktop Computer Use"
    - "The CORS UAT gap and debug session are marked resolved only after the live deploy and fresh-device ingest evidence pass; the separate 3-5-person pilot remains human-needed"
  artifacts:
    - path: "research-backend/wrangler.jsonc"
      provides: "Canonical Worker configuration with the exact non-secret approved origin binding"
    - path: "research-backend/test/deployment-config.test.mjs"
      provides: "Executable deployment-config and public-preflight regression"
    - path: "docs/pilot_protocol.md"
      provides: "Deploy gate requiring config tests, version-binding inspection, and real WebView-origin preflights"
    - path: ".planning/phases/04-study-infrastructure-pilot/04-UAT.md"
      provides: "Fresh API 36.1 enrollment and ingest retest evidence"
  key_links:
    - from: "research-backend/wrangler.jsonc vars.RESEARCH_ALLOWED_ORIGINS"
      to: "research-backend/src/worker.ts allowedOrigins/corsOrigin"
      via: "the canonical deployment uploads the exact comma-delimited allowlist consumed by the existing fail-closed runtime"
      pattern: "RESEARCH_ALLOWED_ORIGINS"
    - from: "research-backend/test/deployment-config.test.mjs"
      to: "research-backend/wrangler.jsonc"
      via: "the test reads the tracked config rather than synthesizing the binding independently"
      pattern: "wrangler.jsonc"
    - from: "docs/pilot_protocol.md"
      to: "question-trace-research-collector uploaded version"
      via: "post-deploy binding inspection plus OPTIONS /v1/install/resolve and OPTIONS /v1/ingest with Origin https://localhost"
      pattern: "https://localhost"
    - from: "fresh API 36.1 ResearchSetupScreen"
      to: "canonical /v1/install/resolve and /v1/ingest"
      via: "real Capacitor WebView requests from https://localhost, driven entirely through adb"
      pattern: "adb"
  prohibitions:
    - statement: "Do not weaken or replace worker.ts fail-closed exact-origin behavior unless new executable evidence proves the implementation itself is wrong"
      status: unresolved
    - statement: "Do not use '*' origins, regex/suffix matching, reflected origins, Access-Control-Allow-Credentials, or put enrollment/admin credentials in tracked vars"
      status: unresolved
    - statement: "Do not deploy a duplicate Worker, change the D1 binding, apply migrations, mutate participant tables, or delete live records as part of this CORS repair"
      status: unresolved
    - statement: "Do not use a debug fetch stub, Chrome DevTools override, Computer Use, emulator window clicks, or a live AI provider during the fresh-device retest"
      status: unresolved
    - statement: "Do not mark STUDY-05 or the 3-5-person internal pilot complete from a single seeded-account infrastructure smoke"
      status: unresolved
---

<objective>
Close the Phase 4 Android WebView CORS blocker by restoring the required exact origin allowlist to the tracked canonical Worker config, pinning that deploy contract in tests and protocol gates, redeploying only after operator approval, and rerunning enrollment plus real ingest on a fresh API 36.1 installation through ADB.

Purpose: the Worker implementation is correctly fail-closed, but the Phase 4 canonical redeploy omitted its non-secret origin binding. Synthetic unit-test environments and origin-less admin/export smoke could not detect that configuration drift.
Output: tracked config + deployment regression + hardened deploy protocol + live version/preflight evidence + resolved fresh-device UAT record.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@CLAUDE.md
@AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-study-infrastructure-pilot/04-CONTEXT.md
@.planning/phases/04-study-infrastructure-pilot/04-UAT.md
@.planning/debug/phase4-android-webview-cors.md
@.planning/phases/04-study-infrastructure-pilot/04-06-PLAN.md
@.planning/phases/04-study-infrastructure-pilot/04-06-SUMMARY.md
@research-backend/wrangler.jsonc
@research-backend/src/worker.ts
@research-backend/test/cors-auth.test.mjs
@docs/pilot_protocol.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore and pin the canonical exact-origin deployment contract</name>
  <files>research-backend/wrangler.jsonc, research-backend/test/deployment-config.test.mjs</files>
  <read_first>
    - research-backend/wrangler.jsonc (canonical Worker name and D1 binding; this is the config used by plain wrangler deploy)
    - research-backend/src/worker.ts (allowedOrigins, corsOrigin, withCors, PUBLIC_PATHS; preserve the current exact-match/fail-closed behavior)
    - research-backend/test/cors-auth.test.mjs (existing synthetic-env CORS unit coverage and exact response headers)
    - .planning/debug/phase4-android-webview-cors.md (version 16 binding evidence, version 17 omission, exact intended origin set)
    - research-backend/package.json (node:test discovery includes every test/*.test.mjs file)
  </read_first>
  <behavior>
    - "The canonical config declares exactly capacitor://localhost,http://localhost,http://localhost:5173,https://localhost after trimming and set comparison; no wildcard or extra origin is accepted."
    - "The tracked vars object contains RESEARCH_ALLOWED_ORIGINS only; RESEARCH_ADMIN_PASSWORD, RESEARCH_ENROLLMENT_CREDENTIAL, install tokens, and credential values are absent from the config."
    - "For each configured origin and each of /v1/install/resolve and /v1/ingest, invoking the real Worker with the config-derived binding returns 204, echoes only that exact origin, sets Vary: Origin, POST/OPTIONS methods, and Content-Type/Authorization headers, and performs no D1 work."
    - "A near-match/disallowed origin returns 403 with no Access-Control-Allow-Origin, and an environment missing the binding remains fail-closed."
  </behavior>
  <action>
    RED: add `test/deployment-config.test.mjs` that loads the tracked `wrangler.jsonc`, asserts the canonical Worker name and DB binding are preserved, compares the configured origin set to the four exact approved origins, rejects wildcard/duplicates/extras, and asserts credentials do not appear in tracked vars or serialized config. Derive the Worker test environment's `RESEARCH_ALLOWED_ORIGINS` from the loaded config, then exercise OPTIONS against both public paths for every approved origin plus a disallowed near-match and missing-binding case. This must fail against the current config because the vars block is absent.

    GREEN: add only the non-secret canonical binding under `vars` in `wrangler.jsonc` with the exact comma-delimited value `capacitor://localhost,http://localhost,http://localhost:5173,https://localhost`. Preserve Worker name `question-trace-research-collector`, main, compatibility date, and D1 name/id byte-for-byte. Do not edit `worker.ts`: its exact-match, no-credentials, fail-closed implementation is correct. Do not copy from or depend on ignored `wrangler.deploy.local.jsonc` at runtime.
  </action>
  <verify>
    <automated>cd research-backend &amp;&amp; node --test test/deployment-config.test.mjs test/cors-auth.test.mjs &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - The new test fails before the config change and passes afterward while reading the tracked canonical config.
    - All approved origins and both public endpoint preflights are executed; disallowed and absent-binding paths remain 403 without ACAO.
    - `worker.ts` is unchanged and the full backend suite passes with zero failures/skips.
    - No secret name/value is added to tracked vars; only the explicitly non-secret origin allowlist is committed.
  </acceptance_criteria>
  <done>A plain canonical wrangler deploy can no longer silently omit the WebView origin contract, and CI proves config-to-runtime linkage.</done>
</task>

<task type="auto">
  <name>Task 2: Make real-origin preflights part of the pilot deployment gate</name>
  <files>docs/pilot_protocol.md</files>
  <read_first>
    - docs/pilot_protocol.md Section 1.2 (current backend-first deploy and origin-less admin/export smoke)
    - research-backend/test/deployment-config.test.mjs (the command and exact config contract established in Task 1)
    - .planning/debug/phase4-android-webview-cors.md "Root Cause" and "Suggested Fix Direction" (why the old gate was false-green)
    - .planning/phases/04-study-infrastructure-pilot/04-06-SUMMARY.md Consolidation addendum (canonical Worker identity; do not recreate the deleted duplicate)
  </read_first>
  <action>
    Amend protocol §1.2 without changing unrelated pilot/scoring content. Before deploy, require `npm test` (thereby executing the config contract) and an operator check that the target is exactly `question-trace-research-collector`; retain migration/admin/export gates. Immediately after `npx wrangler deploy`, require inspection of the 100%-traffic uploaded version and confirmation that the non-secret `RESEARCH_ALLOWED_ORIGINS` binding exists with the four exact approved origins, while credential bindings are named as secrets and their values are never printed or recorded.

    Add copy/paste-safe real-origin preflight checks for both `/v1/install/resolve` and `/v1/ingest`, sending `Origin: https://localhost`, `Access-Control-Request-Method: POST`, and the endpoint's required request headers. The gate requires HTTP 204, ACAO exactly `https://localhost`, `Vary: Origin`, allowed methods `POST, OPTIONS`, and the existing allowed-header contract. Also require one disallowed-origin negative check to remain 403 with no ACAO. State explicitly that origin-less `/admin` and export smoke cannot substitute for these checks, wildcard is forbidden, and credentials remain Worker secrets. Use a placeholder for the canonical base URL only if the existing protocol does not already name it; never insert a credential.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('docs/pilot_protocol.md','utf8');const required=['deployment-config.test.mjs','question-trace-research-collector','RESEARCH_ALLOWED_ORIGINS','https://localhost','/v1/install/resolve','/v1/ingest','OPTIONS','204','Access-Control-Allow-Origin','wildcard','Worker secrets'];const missing=required.filter(x=&gt;!p.includes(x));if(missing.length){console.error(missing);process.exit(1)}console.log('deploy CORS gate ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Protocol deploy gate names config regression, canonical Worker, uploaded-version binding inspection, both public endpoint preflights, exact 204/ACAO expectations, and a negative origin check.
    - Existing authenticated admin and exact four-file export smoke remain required.
    - The protocol contains no real credential/token and does not instruct an operator to print secret values.
  </acceptance_criteria>
  <done>A deploy cannot be accepted merely because origin-less operational endpoints work; the participant WebView boundary is an explicit release gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Authorize the canonical Worker redeploy and live seeded-account smoke</name>
  <files>.planning/phases/04-study-infrastructure-pilot/04-07-SUMMARY.md</files>
  <read_first>
    - research-backend/wrangler.jsonc (exact canonical Worker target and non-secret binding to be uploaded)
    - docs/pilot_protocol.md Section 1.2 (post-deploy inspection/preflight gate from Task 2)
    - .planning/debug/phase4-android-webview-cors.md (current production failure and version 17 evidence)
  </read_first>
  <action>
    Pause immediately before any live Cloudflare write. Present the exact scope: one `npx wrangler deploy` to the existing `question-trace-research-collector`, no migration/D1 data mutation/duplicate Worker/secret update; then read-only version inspection and OPTIONS probes. Also disclose that the subsequent fresh Android smoke will resolve seeded account 1001 and upload test research rows to the existing pilot database. The agent performs all CLI and ADB work after authorization; the operator only needs to intervene if Wrangler authentication is unavailable, the emulator cannot start, or the existing private app build environment is missing. Never request a secret value in chat.
  </action>
  <what-built>The tracked canonical config now restores the exact non-secret origin allowlist, its deployment linkage is covered by executable tests, and the pilot protocol requires uploaded-version plus real-origin preflight evidence.</what-built>
  <how-to-verify>
    Review the staged diff and test results. Confirm authorization for: (1) redeploying only `question-trace-research-collector`; (2) performing read-only binding/status and live OPTIONS checks; and (3) using seeded account 1001 for a fresh API 36.1 enrollment/ingest smoke that creates test rows. No credential values will be displayed, stored in tracked files, or requested in chat.
  </how-to-verify>
  <resume-signal>Type "authorized" to permit the scoped deploy and live ADB smoke, or describe the constraint. If Wrangler requires login, authenticate locally and then type "authorized"; do not paste credentials.</resume-signal>
  <verify>
    <human-check>Operator explicitly authorizes the named Worker redeploy and seeded-account live smoke, with any required local authentication completed privately.</human-check>
  </verify>
  <acceptance_criteria>
    - Authorization names or clearly refers to the existing canonical Worker and the live seeded-account smoke.
    - No deployment or live participant-data write occurs before authorization.
    - Missing authentication causes a pause for local login, never a request for secret text.
  </acceptance_criteria>
  <done>The executor has bounded authority for exactly one canonical redeploy and the corresponding live preflight/device verification.</done>
</task>

<task type="auto">
  <name>Task 4: Redeploy the canonical Worker and prove the uploaded binding at the live boundary</name>
  <files>research-backend/wrangler.jsonc, docs/pilot_protocol.md</files>
  <read_first>
    - research-backend/wrangler.jsonc (must still name question-trace-research-collector with the approved origin vars)
    - docs/pilot_protocol.md Section 1.2 (execute the exact new deploy gate)
    - .planning/phases/04-study-infrastructure-pilot/04-06-SUMMARY.md Consolidation addendum (canonical D1/Worker identity and deleted duplicate)
  </read_first>
  <action>
    After Task 3 authorization, run the targeted/full backend tests once more, then deploy from `research-backend` with plain `npx wrangler deploy`. Stop on any config/test/deploy mismatch; do not fall back to the ignored local config, create another Worker, change secrets, or edit `worker.ts` to make a smoke pass.

    Query Wrangler deployment status and inspect the newly uploaded 100%-traffic version's bindings. Record only version ID, timestamp, Worker name, DB binding name/database name, presence and exact non-secret value of `RESEARCH_ALLOWED_ORIGINS`, and the presence (not values) of credential secret bindings. Fail the gate if the origin binding is absent/different or the new version is not serving canonical traffic.

    Run live OPTIONS probes with `Origin: https://localhost` against both `/v1/install/resolve` and `/v1/ingest`, including POST and required-header preflight headers. Assert 204 and exact ACAO/Vary/method/header values. Probe a disallowed near-match against both paths and assert 403 with no ACAO. Origin-less admin/export evidence from 04-06 is supplementary, not a substitute. Record sanitized commands/results in 04-07-SUMMARY.md; never record secret values.
  </action>
  <verify>
    <automated>cd research-backend &amp;&amp; node --test test/deployment-config.test.mjs test/cors-auth.test.mjs &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - Exactly the canonical Worker is redeployed and its new version serves 100% traffic.
    - Version inspection shows the exact four-origin non-secret binding and no credential values are printed or persisted.
    - Both live `https://localhost` preflights return 204 with exact ACAO; disallowed near-matches return 403 without ACAO.
    - No D1 mutation, migration, duplicate Worker, secret change, wildcard, or Worker fail-closed code change occurs.
  </acceptance_criteria>
  <done>The live canonical Worker demonstrably carries and enforces the same exact-origin contract as the tracked config.</done>
</task>

<task type="auto">
  <name>Task 5: Rerun fresh API 36.1 enrollment and real ingest through ADB, then refresh reports</name>
  <files>.planning/phases/04-study-infrastructure-pilot/04-UAT.md, .planning/debug/phase4-android-webview-cors.md, .planning/phases/04-study-infrastructure-pilot/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-study-infrastructure-pilot/04-UAT.md Test 19 and Gaps (preserve prior failure report while adding retest evidence)
    - .planning/debug/phase4-android-webview-cors.md (diagnosed session to resolve only after real-device success)
    - .planning/phases/04-study-infrastructure-pilot/04-VERIFICATION.md (remove stale device/CORS blockers but retain the real 3-5-person pilot as human-needed)
    - app/package.json and app/capacitor.config.ts (production build/sync and Android package identity)
    - app/src/screens/ResearchSetupScreen.tsx and app/src/services/research-client.service.ts (UI labels and canonical real-fetch path; do not modify)
    - docs/pilot_protocol.md Sections 1.2 and 2 (fresh enrollment/consent/activity steps)
  </read_first>
  <action>
    Use command-line ADB only so the operator can continue using the desktop. Do not use Computer Use, focus/resize the emulator window, or inject a fetch stub. Confirm exactly one API 36.1 emulator/device target, build/sync/assemble the current Android app using the existing private local environment without printing it, install the debug APK, and clear `com.trellis.app` application data for a genuinely fresh state. Clear logcat, launch via `adb shell monkey`, inspect UI with `adb shell uiautomator dump`, and drive taps/text/keyevents through ADB.

    Enter seeded account `1001` on the real ResearchSetupScreen and submit it. Prove the app advances without "could not be prepared" or CORS errors, then complete the current versioned research consent and create an explicit study interaction that enters the real upload outbox. Allow retry/reconciliation to settle, relaunch once to prove the binding persists, and use logcat plus a read-only remote D1 count/query scoped to account 1001 and the retest time window to prove at least one new record reached `/v1/ingest`. Do not call a live AI provider; a feed/post interaction is sufficient. Record emulator API, APK/build identity, Worker version, timestamps, UI hierarchy assertions, sanitized log evidence, and remote row-count delta. Do not delete any live row without separate explicit authorization.

    On success, update UAT Test 19 to `pass` with `previous_result: issue`, preserve the original reported failure, add live preflight/device/ingest resolution evidence, change totals to 19 passed/0 issues, and resolve the gap. Change the debug session to resolved and move it to `.planning/debug/resolved/phase4-android-webview-cors.md` using a safe same-workspace move. Refresh 04-VERIFICATION.md as a re-verification: remove the stale CORS/device blocker and any already-resolved pool/DB-hygiene statements contradicted by current evidence, but keep status `human_needed` and the 3-5-person internal pilot truth unsatisfied until real participants complete the protocol. If any enrollment or ingest assertion fails, leave UAT/debug diagnosed and report the exact new evidence; do not manufacture a pass.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; node scripts/package-content-pool.mjs --check &amp;&amp; npx cap sync android &amp;&amp; cd android &amp;&amp; .\gradlew.bat assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - ADB, not desktop Computer Use, performs the complete fresh API 36.1 interaction; no fetch stub/devtools override exists.
    - Account 1001 binds through the live canonical Worker, onboarding/consent completes, restart preserves the binding, and no CORS/Origin-not-allowed error appears.
    - A read-only remote query proves a new real research row for account 1001 after the retest began.
    - App tests/lint/build, pool package check, Capacitor sync, and Android debug assembly pass.
    - UAT and debug are resolved only with evidence; 04-VERIFICATION accurately remains human-needed solely for genuine remaining operator/participant work and does not falsely claim the 3-5-person pilot complete.
  </acceptance_criteria>
  <done>The original Android CORS blocker is closed at the actual WebView enrollment and ingest boundary, with durable evidence and honest remaining pilot status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked Wrangler config → uploaded Worker version | Required non-secret runtime bindings can be lost when a deployment replaces the version binding set. |
| WebView Origin → public Worker endpoints | Participant enrollment and upload require exact-origin CORS without widening browser access. |
| operator authorization → live Cloudflare state | Redeployment and seeded-account test writes are external-state changes requiring explicit bounded approval. |
| ADB-driven app → live D1 evidence | A fresh installation must prove the real participant transport rather than a stubbed UI path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-28 | Tampering / availability | canonical Worker deployment vars | high | mitigate | Track exact non-secret origins, config-derived regression, uploaded-version binding inspection. |
| T-04-29 | Information disclosure | Worker credential configuration/evidence | high | mitigate | Credentials remain secrets; config test rejects credential vars; reports record binding presence only. |
| T-04-30 | Spoofing / information disclosure | permissive CORS repair | high | mitigate | Exact four-origin set, no wildcard/reflection/credentials, negative near-match tests locally and live. |
| T-04-31 | Repudiation | origin-less deployment smoke | medium | mitigate | Both real public OPTIONS paths and exact response headers become protocol evidence. |
| T-04-32 | Repudiation | stubbed Android acceptance | high | mitigate | Fresh data clear, ADB-only real UI path, logcat, restart, and remote D1 row delta. |
| T-04-33 | Elevation of privilege / accidental mutation | live repair scope creep | high | mitigate | Blocking authorization, canonical Worker-only deploy, no migrations/secret changes/D1 deletion. |
</threat_model>

<verification>
- `cd research-backend && node --test test/deployment-config.test.mjs test/cors-auth.test.mjs && npm test`
- Operator-authorized `cd research-backend && npx wrangler deploy`, followed by deployment/version binding inspection.
- Live exact-origin and disallowed-origin OPTIONS probes for both `/v1/install/resolve` and `/v1/ingest`.
- `cd app && npm test && npm run lint && npm run build && node scripts/package-content-pool.mjs --check && npx cap sync android`
- `cd app/android && .\gradlew.bat assembleDebug`
- Fresh API 36.1 ADB enrollment/consent/activity/restart with logcat and remote D1 row-delta evidence; no Computer Use or debug stub.
</verification>

<success_criteria>
- Canonical tracked config, CI contract, uploaded version, and live preflight behavior agree on the same exact origin allowlist.
- Fresh Android WebView enrollment and ingest succeed through production source and the canonical Worker.
- The Phase 4 CORS UAT gap is resolved with reproducible evidence while the genuine 3-5-person pilot remains honestly human-needed.
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-07-SUMMARY.md` when done, including sanitized local test, operator authorization, deployed version/binding, live preflight, ADB, logcat, remote row-delta, and report-refresh evidence. Never include credentials or secret values.
</output>
