---
phase: 04-study-infrastructure-pilot
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-04"]
files_modified:
  - app/src/types/research.ts
  - app/src/types/index.ts
  - app/src/services/research-wire-contract.ts
  - app/src/services/upload-queue.service.ts
  - app/src/services/recommendation-research.service.ts
  - app/src/services/recommendation.service.ts
  - app/src/App.tsx
  - app/tests/services/recommendation-research.service.test.mjs
  - app/tests/services/upload-queue.service.test.mjs
autonomous: true
requirements: [STUDY-03]
must_haves:
  truths:
    - "Every recommendation in a ready batch is projected into research_records (kind 'recommendation', revision 1) with batchId/sessionId/batchSeq/1-based batchPosition, verified through dbQuery — never the in-memory mirror"
    - "Projection is idempotent and crash-recoverable: a boot-time run converges a batch persisted before the projection ever ran, and a second run adds zero rows"
    - "Recommendation records ride the existing outbox: enqueue, batching, ACK receipt, quarantine, and reconciliation all work for the new kind"
    - "The wire record strips userId/condition/topicId and excludes batch status and diversityCounters"
    - "Projection only runs with affirmative research consent; without it, zero research_records rows are written"
    - "Control-condition projected/wire rows carry no contributing question/concept/post IDs, and the control-ranker isolation tests stay green"
  artifacts:
    - "app/src/services/recommendation-research.service.ts (projectRecommendationResearchRecords, captureRecommendationResearch)"
    - "app/src/types/research.ts (RecommendationResearchRecord)"
    - "app/tests/services/recommendation-research.service.test.mjs"
  key_links:
    - "recommendation_batches + recommendations (IndexedDB via dbQuery) → research_records → research_upload_queue → POST /v1/ingest"
    - "recommendation.service buildBatch ready-save success → error-isolated lazy import of captureRecommendationResearch (matches the interaction-log→graph-memory hook pattern)"
    - "App.tsx boot: projectRecommendationResearchRecords before reconcileResearchOutbox so the outbox reconciler sees projected rows"
  prohibitions:
    - "No ranker or ranking-support module imports the projection service — projection is strictly one-way, downstream of persisted output"
    - "No batch status or diversityCounters field on the wire or in research_records data"
    - "No direct read of the in-memory mirror for durability assertions — tests assert through dbQuery"
    - "No second network client or retry queue — the existing outbox is the only transport"
---

<objective>
Build the client half of STUDY-03 (D-09): a crash-recoverable, consent-gated projection that flattens ready recommendation batches from the two Phase 3 IndexedDB stores into durable `research_records`, and extend the wire contract + upload queue so the new kind rides the existing delivery pipeline to the backend landed in 04-02.

Purpose: recommendation reasons, strategies, trace IDs, and served batch order currently exist only on-device; this makes them backend-authoritative without inventing new transport.
Output: RecommendationResearchRecord type, client wire branch, outbox union extension, recommendation-research.service.ts, post-commit + boot hooks, and dbQuery-verified tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-study-infrastructure-pilot/04-RESEARCH.md
@.planning/phases/04-study-infrastructure-pilot/04-PATTERNS.md
@app/src/services/recommendation.repository.ts
@app/src/services/interaction-log.service.ts
@app/src/services/upload-queue.service.ts
@app/src/services/research-wire-contract.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**This plan (04-03):**
- `app/src/types/research.ts` → `export interface RecommendationResearchRecord` (kind: 'recommendation', id, userId, condition, topicId, batchId, sessionId, batchSeq, batchPosition, postId, generatedAt, strategy, score, reasonText, contributingQuestionIds?, contributingConceptIds?, contributingPostIds?, componentScores?)
- `app/src/types/index.ts` → re-export of RecommendationResearchRecord (if the file uses named re-exports)
- `app/src/services/research-wire-contract.ts` → RECOMMENDATION_FIELDS wire allowlist + kind-dispatched validation branch in `toResearchWireRecord`
- `app/src/services/upload-queue.service.ts` → `UploadRecord` union widened to include RecommendationResearchRecord
- `app/src/services/recommendation-research.service.ts` (new) → `projectRecommendationResearchRecords(): Promise<number>`, `captureRecommendationResearch(): Promise<void>`
- `app/src/services/recommendation.service.ts` → error-isolated lazy capture hook after ready-batch persistence
- `app/src/App.tsx` → boot projection call inside the consent-gated reconciliation block
- `app/tests/services/recommendation-research.service.test.mjs` (new); extended `app/tests/services/upload-queue.service.test.mjs`

**Rest of phase:** shared contract + migration + backend validation (04-01); Worker ingest + four-file export (04-02); versioned §14.3 consent (04-04); D-15 docs + STUDY-02 traceability (04-05); pilot protocol + operator gate (04-06).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecommendationResearchRecord type + client wire branch + outbox union</name>
  <files>app/src/types/research.ts, app/src/types/index.ts, app/src/services/research-wire-contract.ts, app/src/services/upload-queue.service.ts, app/tests/services/upload-queue.service.test.mjs</files>
  <read_first>
    - app/src/services/research-wire-contract.ts (entire file — LocalRecord union, EVENT_FIELDS/QA_FIELDS, assertString/assertTimestamp/assertStringArray/assertOnlyFields helpers, identity-strip destructure in toResearchWireRecord)
    - app/src/types/research.ts and app/src/types/index.ts lines 1-15 (how research types are re-exported)
    - app/src/domain/content.types.ts lines 106-122 (RecommendationStrategy + Recommendation fields)
    - app/src/services/upload-queue.service.ts (UploadRecord union, recordRevision, enqueue, sendBatch quarantine semantics)
    - app/tests/services/upload-queue.service.test.mjs (existing fixture/stub-fetch patterns to extend)
    - app/tests/services/leaf-imports.test.mjs (import-graph constraints that may forbid a types/research → domain import)
    - shared/research-wire-contract.v1.json (recommendation metadata block added by 04-01)
  </read_first>
  <behavior>
    - toResearchWireRecord on a RecommendationResearchRecord returns a wire object without userId/condition/topicId and with kind 'recommendation' preserved
    - The wire branch rejects: extra keys, strategy outside contractJson.recommendation.strategies, non-finite score, non-positive/non-integer batchSeq or batchPosition, empty or over-limit reasonText, contributing arrays over 64 items or with invalid items, componentScores with over 32 keys / over-64-char keys / non-finite values
    - Enqueuing a recommendation record persists it in research_upload_queue (dbQuery-visible) and a successful flush POSTs a body whose record carries kind 'recommendation' and no identity fields, then deletes the queue row after ACK
    - A single recommendation record rejected with HTTP 422 lands in research_upload_quarantine with reason server_rejected (poison-row behavior unchanged for the new kind)
    - reconcileResearchOutbox re-enqueues a kind-'recommendation' research_records row that has no delivery receipt
  </behavior>
  <action>
    In `app/src/types/research.ts`: define `RecommendationResearchRecord` with the exact fields listed in the artifacts section. For the strategy field, prefer `import type { RecommendationStrategy } from '../domain/content.types.ts'` (type-only, erased at runtime); if tests/services/leaf-imports.test.mjs forbids that edge, inline the eight-literal union ('topic_baseline' | 'quality_baseline' | 'diversity_baseline' | 'continue' | 'deepen' | 'contrast' | 'bridge' | 'echo') with a comment naming RecommendationStrategy as the source of truth. In `app/src/types/index.ts`, add RecommendationResearchRecord to the existing named re-export from './research' if re-exports are named (check line 9); skip if it uses a wildcard.

    In `app/src/services/research-wire-contract.ts`: widen `LocalRecord` to include RecommendationResearchRecord; note `ResearchWireRecord = Omit<LocalRecord, 'userId' | 'condition' | 'topicId'>` then covers the new kind automatically. Add `const RECOMMENDATION_FIELDS = new Set(['kind','id','batchId','sessionId','batchSeq','batchPosition','postId','generatedAt','strategy','score','reasonText','contributingQuestionIds','contributingConceptIds','contributingPostIds','componentScores'])` and `const RECOMMENDATION_STRATEGIES = new Set<string>(contractJson.recommendation.strategies)`. In `toResearchWireRecord`, add a new FIRST branch: `if (candidate.kind === 'recommendation')` → `assertOnlyFields(candidate, RECOMMENDATION_FIELDS)`; assertString id/batchId/sessionId (RESEARCH_WIRE_LIMITS.id); positive-safe-integer checks on batchSeq/batchPosition; assertString postId (RESEARCH_WIRE_LIMITS.postId); assertTimestamp generatedAt; strategy membership in RECOMMENDATION_STRATEGIES; `typeof score === 'number' && Number.isFinite(score)`; assertString reasonText with max `contractJson.recommendation.limits.reasonText`; the three contributing arrays optional with a local bounded-array helper (≤ contractJson.recommendation.limits.contributorArrayItems items, each a non-empty string ≤ RESEARCH_WIRE_LIMITS.postId); componentScores optional plain object (≤ componentScoreKeys keys, key length ≤ componentScoreKeyLength, finite numeric values). Throw ResearchWireValidationError('invalid_record') on any failure. The existing eventType/QA branches and the trailing singleton-size check stay untouched. Mirror the exact bounds used by backend parseRecommendation (04-01) — client/backend parity comes from both reading contractJson.

    In `app/src/services/upload-queue.service.ts`: widen `type UploadRecord = UserInteractionEvent | QuestionAnswerRecord | RecommendationResearchRecord` and add the type import. No logic change: `recordRevision` already returns 1 for records without a revision field, and parse/reconcile paths are shape-agnostic.

    Extend `app/tests/services/upload-queue.service.test.mjs` with the recommendation-kind delivery, quarantine, and reconciliation cases from the behavior list, following the file's existing stub-fetch and dbQuery assertion patterns. Build fixtures with a helper returning a full valid RecommendationResearchRecord (experimental condition with contributing IDs, and a control variant without).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/upload-queue.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/upload-queue.service.test.mjs` passes with recommendation delivery/ACK, 422-quarantine, and reconcile cases in TAP output
    - A test asserts the POSTed wire body for a recommendation record has no userId, condition, or topicId keys and DOES have kind 'recommendation'
    - `cd app && npx tsc -b --noEmit` exits 0 (union widening compiles across all consumers)
  </acceptance_criteria>
  <done>The new record kind validates, strips identity, and travels the existing durable outbox end to end under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recommendation-research.service.ts — dbQuery projection with crash reconciliation</name>
  <files>app/src/services/recommendation-research.service.ts, app/tests/services/recommendation-research.service.test.mjs</files>
  <read_first>
    - app/src/services/interaction-log.service.ts lines 92-150 (persistRecord INSERT OR REPLACE into research_records + consent-gated storeAndEnqueue — the exact analog per 04-PATTERNS.md)
    - app/src/services/recommendation.repository.ts (row shapes: recommendation_batches has id/user_id/session_id/data; recommendations has id/user_id/data; payloads are JSON in data)
    - app/src/domain/graph.types.ts lines 98-110 (RecommendationBatch: recommendationIds order, status 'building'|'ready', diversityCounters, seq)
    - app/src/services/research-consent.service.ts (hasAffirmativeResearchConsent — version-gated after plan 04-04)
    - app/tests/services/upload-queue.service.test.mjs and app/tests/services/interaction-log.service.test.mjs (Node test harness setup for dbQuery-backed services: LocalStorageBackend fallback, settings fixtures)
  </read_first>
  <behavior>
    - projectRecommendationResearchRecords: reads all recommendation_batches via dbQuery, skips status 'building', enumerates batch.recommendationIds in order, reads each recommendation row via dbQuery, and for each missing research_records id writes {kind:'recommendation', revision:1} whose data JSON is the Recommendation fields plus batchId, sessionId, batchSeq (= batch.seq), batchPosition (= index + 1); enqueues each new record with triggerFlush false; returns the count of newly projected rows
    - A recommendation id referenced by a ready batch but missing from the recommendations store is skipped without failing the run (converges on a later reconciliation)
    - Crash window converges: saving a ready batch through recommendationRepository.saveBatch with NO projection call, then running projectRecommendationResearchRecords (simulated boot), yields exactly one research_records row per recommendation; an immediate second run returns 0
    - Without affirmative research consent the function returns 0 and writes nothing (dbQuery on research_records shows no recommendation rows)
    - Projected data JSON never contains batch status or diversityCounters keys
    - Control-condition fixtures project rows whose data has no contributing ID arrays, and toResearchWireRecord of that projected record succeeds
  </behavior>
  <action>
    Create `app/src/services/recommendation-research.service.ts` importing only: `dbExecute, dbQuery` from './db.service.ts', `hasAffirmativeResearchConsent` from './research-consent.service.ts', `enqueue, flushPendingUploads` from './upload-queue.service.ts', and the RecommendationResearchRecord / Recommendation / RecommendationBatch types. Export:

    1. `projectRecommendationResearchRecords(): Promise<number>` implementing the behavior above. Query patterns: `SELECT * FROM recommendation_batches` then per-id `SELECT * FROM recommendations WHERE id = ?`; existence check via `SELECT * FROM research_records WHERE id = ?`; write via `INSERT OR REPLACE INTO research_records (id, kind, revision, data) VALUES (?, 'recommendation', 1, ?)` mirroring interaction-log persistRecord. Build the record by explicit field picks from the parsed Recommendation (id, userId, condition, topicId, postId, generatedAt, strategy, score, reasonText, and the three contributing arrays + componentScores only when present) — do NOT object-spread the whole parsed payload, so foreign/legacy keys can never leak onto the wire.
    2. `captureRecommendationResearch(): Promise<void>` = await projectRecommendationResearchRecords(); if the count > 0, `void flushPendingUploads().catch(() => {})`. This is the post-commit hook entry; boot uses the bare projection because App.tsx already reconciles + flushes.

    Create `app/tests/services/recommendation-research.service.test.mjs` (node:test + assert/strict, harness setup copied from the existing service tests): seed through `recommendationRepository.saveBatch` (real repository writes), set affirmative-consent preferences via the settings fixture pattern used by interaction-log tests (must satisfy the 04-04 version-gated fields: researchConsentGiven true + current RESEARCH_CONSENT_VERSION + onboardingCompleted true), then assert every behavior bullet THROUGH dbQuery on research_records and research_upload_queue. Include: order preservation (batchPosition matches recommendationIds index + 1 for a shuffled-score batch), building-batch exclusion, idempotent second run, missing-recommendation-row skip, consent-off zero-write, no-status/no-diversityCounters keys in stored data JSON, and control-fixture wire-shape success.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/recommendation-research.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/recommendation-research.service.test.mjs` passes; durability assertions use dbQuery (no assertion reads an in-memory array or module state for persistence proof)
    - The crash-reconciliation test performs: saveBatch → project (boot) → assert N rows → project again → assert return value 0 and row count unchanged
    - `cd app && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Ready batches project deterministically, idempotently, and only under consent, with exposure order preserved and internal serving state excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the capture hooks — post-ready-batch + App boot — and prove control isolation holds</name>
  <files>app/src/services/recommendation.service.ts, app/src/App.tsx, app/tests/services/recommendation-research.service.test.mjs</files>
  <read_first>
    - app/src/services/recommendation.service.ts lines 312-366 (buildBatch: the ready saveBatch success point where the hook attaches; RecommendationServiceDependencies for injectability)
    - app/src/services/interaction-log.service.ts lines 124-141 (loadGraphMemory lazy-import + error-isolation hook — the pattern to replicate)
    - app/src/App.tsx lines 317-345 (boot hydration block: registerRetryTriggers → consent check → reconcileResearchOutbox → flushPendingUploads)
    - app/tests/services/recommendation.service.test.mjs (dependency-injection test style; control-isolation throwing-spy tests that MUST stay green)
    - app/tests/phase1/consent-gate.test.mjs (source-regex assertions on App.tsx that must not break)
  </read_first>
  <action>
    In `app/src/services/recommendation.service.ts`: add an optional dependency `captureResearch?: () => Promise<void>` to RecommendationServiceDependencies with a default that lazily imports the projection service — `() => import('./recommendation-research.service.ts').then((m) => m.captureRecommendationResearch())` — mirroring the interaction-log loadGraphMemory pattern. In `buildBatch`, immediately after the ready-batch `saved.success` path (before returning success), fire it error-isolated and non-blocking: `void this.dependencies.captureResearch().catch(() => {})` with a comment noting boot projection is the convergence path for any miss. The hook must run strictly AFTER saveBatch resolves successfully — projection is one-way, downstream of persisted output, and must never influence materialization, scoring, or batch content (DEC-control-no-question-history).

    In `app/src/App.tsx`: inside the existing `if (hasAffirmativeResearchConsent())` boot block, insert `await projectRecommendationResearchRecords();` on the line BEFORE `await reconcileResearchOutbox();` (static import from './services/recommendation-research.service'). This ordering lets the existing outbox reconciler pick up any projected-but-unqueued rows in the same boot pass. Do not touch registerRetryTriggers, session handlers, or route gates.

    Extend `app/tests/services/recommendation-research.service.test.mjs` with two hook tests: (1) construct a RecommendationService with an injected captureResearch spy, drive beginSession to a ready batch (reuse the fixture dependencies from recommendation.service.test.mjs), and assert the spy fired exactly once after the batch was dbQuery-visible; (2) inject a captureResearch that rejects and assert beginSession still returns success (error isolation). Then run the existing control-isolation suites unchanged as the regression proof.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/recommendation-research.service.test.mjs tests/services/recommendation.service.test.mjs tests/phase1/consent-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/recommendation-research.service.test.mjs tests/services/recommendation.service.test.mjs tests/phase1/consent-gate.test.mjs` passes — including the pre-existing control-ranker throwing-personal-loader tests, unmodified
    - `grep -rn "recommendation-research" app/src/services/ranking/` returns no matches (rankers never import the projection service)
    - `cd app && npm test` fully green and `npm run lint` reports 0 errors
  </acceptance_criteria>
  <done>Ready batches trigger capture automatically, boot converges crash windows, and the experimental-isolation invariants are proven intact by the untouched Phase 3 test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB→wire | On-device recommendation data crosses into upload payloads |
| projection→rankers | The exporter must never feed back into ranking inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Experiment contamination (Tampering) | control condition vs projection | high | mitigate | One-way post-persistence hook; no ranking module imports the service (grep gate, Task 3); control fixtures carry no contributing IDs (Task 2 test); Phase 3 throwing-spy suite kept green |
| T-04-12 | Information disclosure | wire payload | high | mitigate | Explicit field picks (no spread) + client allowlist branch strips identity and rejects extras; status/diversityCounters never projected |
| T-04-13 | Information disclosure | consent bypass | high | mitigate | projectRecommendationResearchRecords returns 0 before any write when hasAffirmativeResearchConsent() is false (Task 2 test) |
| T-04-14 | Availability | client ships before backend | high | mitigate | depends_on 04-01; live rollout ordering enforced by the 04-06 operator gate (backend migrate/deploy before client distribution) |
| T-04-15 | Repudiation | lost recommendation rows on crash | medium | mitigate | Idempotent boot projection + outbox reconciliation converge every crash window (Task 2 crash test) |
</threat_model>

<verification>
From app/: `npm test` fully green, `npm run lint` 0 errors, `npx tsc -b --noEmit` clean. `git diff --stat` touches only frontmatter files.
</verification>

<success_criteria>
- Wave 0 gaps "app/tests/services/recommendation-research.service.test.mjs" and "Extend app/tests/services/upload-queue.service.test.mjs" closed
- Required Executable Assertions 1, 2, 3 (client half), and 7 from 04-RESEARCH.md implemented as passing tests
- Recommendation data survives device loss once uploaded — the D-09 chain is complete client-side
</success_criteria>

<output>
Create `.planning/phases/04-study-infrastructure-pilot/04-03-SUMMARY.md` when done.
</output>
