---
phase: 01-rebrand-research-shell-hardening
plan: 05
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - research-backend/package.json
  - research-backend/src/worker.ts
  - research-backend/src/export.ts
  - research-backend/src/admin.ts
  - research-backend/test/export.test.mjs
  - research-backend/test/admin-auth.test.mjs
autonomous: false
requirements: [LOG-01]
user_setup:
  - service: cloudflare
    why: "Deploy the Worker + D1 to a fixed study URL and set the admin password as a server secret (D-14)."
    env_vars:
      - name: RESEARCH_ADMIN_PASSWORD
        source: "`wrangler secret put RESEARCH_ADMIN_PASSWORD` — server-side only, never in the app or repo (D-14)."
    dashboard_config:
      - task: "Apply migration 0001 to the bound D1 database and deploy the Worker"
        location: "`wrangler d1 migrations apply` + `wrangler deploy`"
must_haves:
  truths:
    - "`GET /admin` and `GET /admin/export.zip` require the server-only management password (HTTP Basic over HTTPS) and have no edit/delete routes (D-14, D-11)."
    - "The admin page shows upload-health only: total records and last-received time; it renders participant text safely (escaped/text nodes), never as raw HTML."
    - "`GET /admin/export.zip` returns one ZIP containing exactly two entries — `behavioral-events.csv` and `question-answer-records.csv` — aggregating all accounts (D-15)."
    - "CSV cells are formula-escaped (leading =, +, -, @) to prevent spreadsheet injection."
  artifacts:
    - research-backend/src/export.ts
    - research-backend/src/admin.ts
  key_links:
    - "Basic-auth guard ↔ /admin* routes ↔ RESEARCH_ADMIN_PASSWORD Worker secret"
    - "D1 SELECT ↔ CSV escaping ↔ fflate two-entry ZIP"
---

<objective>
Complete the backend collector: a password-protected researcher page showing upload health and a single downloadable ZIP archive of two CSV files (behavioral events + question/answer records) aggregating all accounts. Then deploy the Worker + D1 to a fixed URL via a human checkpoint. Implements the D-14 (password-protected researcher webpage, server-only password) and D-15 (upload-health + two-file archive) halves of the collection loop; server side of LOG-01.

Purpose: Only a protected server page may aggregate all participants; the app never sees other accounts' data.
Output: admin auth guard, CSV/ZIP export with injection-safe output, unit tests, and a deploy checkpoint.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `research-backend/src/export.ts` (`toCsv`, `escapeCsvCell`, `buildExportZip`)
- `research-backend/src/admin.ts` (`requireAdminAuth`, `renderStatusPage`)
- Routes `GET /admin`, `GET /admin/export.zip`
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSV escaping + two-entry ZIP export (fflate)</name>
  <files>research-backend/package.json, research-backend/src/export.ts, research-backend/test/export.test.mjs</files>
  <read_first>
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Generate the required aggregate archive in the backend" and § "Known Threat Patterns" (CSV formula execution)
  </read_first>
  <behavior>
    - `escapeCsvCell(value)` prefixes a `'` (or wraps) when the cell begins with `=`, `+`, `-`, or `@`, and quotes/escapes embedded quotes, commas, and newlines.
    - `toCsv(rows, columns)` produces a header row + escaped data rows in stable column order.
    - `buildExportZip(events, qa)` returns bytes of a ZIP with exactly two entries: `behavioral-events.csv` and `question-answer-records.csv`.
  </behavior>
  <action>
    Install the audited `fflate@0.8.3` (approved [OK] in the RESEARCH audit) as a research-backend dependency. Write `src/export.ts` with `escapeCsvCell`, `toCsv`, and `buildExportZip` using `fflate` `strToU8` + `zipSync` for the two named entries. Author `test/export.test.mjs` asserting: a cell starting with `=cmd()` is neutralized; a cell with a comma/quote/newline is correctly quoted; `buildExportZip` output unzips (via `fflate` `unzipSync`) to exactly the two expected filenames with the expected header rows.
  </action>
  <verify>
    <automated>cd research-backend && node --test test/export.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - A formula-leading cell (`=`, `+`, `-`, `@`) is escaped so it is not spreadsheet-executable (asserted).
    - `buildExportZip` yields a ZIP whose entry names are exactly `behavioral-events.csv` and `question-answer-records.csv`.
    - Round-trip unzip recovers both CSVs with correct headers.
  </acceptance_criteria>
  <done>Export produces an injection-safe two-file archive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Basic-auth admin guard + status page + export route</name>
  <files>research-backend/src/admin.ts, research-backend/src/worker.ts, research-backend/test/admin-auth.test.mjs</files>
  <read_first>
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Pattern 4: Minimal Worker Boundary", § Security Domain (V2/V4), § "Known Threat Patterns" (HTML injection)
    - research-backend/src/worker.ts (existing route dispatch from Plan 03)
  </read_first>
  <behavior>
    - `requireAdminAuth(request, env)` returns 401 with `WWW-Authenticate: Basic` unless the request carries the correct password from `env.RESEARCH_ADMIN_PASSWORD`; missing/incorrect credentials never reach data.
    - `GET /admin` (authed) returns an HTML status page with total behavioral-event count, total Q/A-record count, and last `received_at`; participant text is emitted via escaping/text nodes with `Content-Type: text/html; charset=utf-8`.
    - `GET /admin/export.zip` (authed) streams the two-file ZIP with `content-disposition: attachment` and `cache-control: no-store`.
    - There are no write/delete/edit admin routes.
  </behavior>
  <action>
    Write `src/admin.ts` with `requireAdminAuth` (constant-time-ish compare against `env.RESEARCH_ADMIN_PASSWORD`; the password is a Worker secret, never in the bundle/app — D-14) and `renderStatusPage` (health counts only, HTML-escaped). Wire `GET /admin` and `GET /admin/export.zip` into `src/worker.ts`, both behind `requireAdminAuth`, aggregating across all accounts via bound D1 SELECTs (D-15). Do not add any mutating admin route (D-11/D-14). Author `test/admin-auth.test.mjs` asserting: no/incorrect credentials → 401 and no DB read; correct credentials → 200 status page; a Q/A record containing `<script>` is rendered escaped, not as live HTML.
  </action>
  <verify>
    <automated>cd research-backend && node --test test/admin-auth.test.mjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - A request without valid Basic credentials to `/admin` or `/admin/export.zip` returns 401 and performs no data query (asserted).
    - The status page shows counts + last-received only and escapes participant text (`<script>` fixture rendered inert).
    - No admin route mutates or deletes data.
  </acceptance_criteria>
  <done>The researcher page is password-gated, read-only, and injection-safe.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Deploy Worker + D1 to the fixed study URL and set the admin secret</name>
  <files>research-backend/wrangler.jsonc</files>
  <action>Human-only deploy step (operator holds the Cloudflare credentials/domain). Provision D1, apply migration 0001, set `RESEARCH_ADMIN_PASSWORD` as a Worker secret, deploy, and record the fixed public URL out-of-repo for the app build's `VITE_RESEARCH_API_BASE_URL`. No secret/URL/account map is committed.</action>
  <what-built>The Worker (ingest + resolve + admin + export) and D1 migration are complete and unit-tested locally.</what-built>
  <how-to-verify>
    Deploy to the operator's Cloudflare account (only the operator has the credentials/domain — RESEARCH Open Question 3):
    1. Create/confirm the D1 database and bind it as `DB` in `wrangler.jsonc`.
    2. `wrangler d1 migrations apply <db>` to create the four tables; provision the real numeric `study_accounts` rows out-of-repo (never commit them).
    3. `wrangler secret put RESEARCH_ADMIN_PASSWORD` (server-only).
    4. `wrangler deploy`; record the fixed public URL out-of-repo. Set the app build's `VITE_RESEARCH_API_BASE_URL` to this URL at package build time (Plan 04 config; not committed).
    5. Smoke-test: `GET /admin` prompts for Basic auth; a sample `POST /v1/ingest` returns `acknowledgedIds`; `GET /admin/export.zip` downloads a two-CSV ZIP.
  </how-to-verify>
  <resume-signal>Type "deployed" once the fixed URL responds and the admin page + export work, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| researcher → /admin* | Only the server-side password may cross; participants must never reach aggregate data. |
| D1 record text → HTML/CSV output | Participant-authored text must be neutralized before rendering or export. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Elevation of privilege | /admin* access | high | mitigate | HTTP Basic against `RESEARCH_ADMIN_PASSWORD` Worker secret; 401 before any data read (admin-auth test asserts). |
| T-01-16 | Tampering | CSV formula execution | high | mitigate | `escapeCsvCell` neutralizes `=`/`+`/`-`/`@` cells (export test asserts). |
| T-01-17 | Tampering | HTML injection on status page | high | mitigate | Escape participant text / text nodes; `text/html; charset=utf-8`; `<script>` fixture rendered inert. |
| T-01-18 | Information disclosure | admin password in app bundle | high | mitigate | Password is a Worker secret only; app holds only the non-secret base URL (D-14). |
| T-01-19 | Repudiation | destructive admin action | medium | accept | No write/delete admin routes exist (D-11/D-14); read-only surface by design. |
</threat_model>

<verification>
- `cd research-backend && npm test` green (validation + ingest + export + admin-auth).
- Post-deploy smoke test recorded in the deploy checkpoint.
</verification>

<success_criteria>
Password-protected researcher page shows upload health and exports a two-CSV ZIP across all accounts; output is injection-safe; deployed to a fixed URL with the password as a server secret.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-05-SUMMARY.md` when done.
</output>
