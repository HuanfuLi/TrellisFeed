---
phase: 01-rebrand-research-shell-hardening
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app/src/types/research.ts
  - app/src/types/index.ts
  - app/src/services/research-config.ts
  - app/src/services/study-context.service.ts
  - app/src/screens/ResearchSetupScreen.tsx
  - app/src/screens/ResearchDiagnosticsScreen.tsx
  - app/src/App.tsx
  - app/tests/services/study-context.service.test.mjs
autonomous: true
requirements: [SHELL-03]
must_haves:
  truths:
    - "An immutable `ResearchIdentity { userId, condition: 'control'|'experimental', topicId, boundAt }` is bound once during researcher-led install and readable synchronously app-wide (SHELL-03, D-01)."
    - "There is no public mutator, logout, condition switch, or clear-data action; `bindOnce` rejects when a different identity already exists (D-02, D-03)."
    - "First participant render is gated on research-identity hydration; when unbound, the app redirects to the researcher setup screen instead of the feed (D-01)."
    - "Non-secret research build config (`apiBaseUrl`, optional PIN digest) validates at startup; no secret is read from a VITE_ variable (D-12)."
  artifacts:
    - app/src/types/research.ts
    - app/src/services/study-context.service.ts
    - app/src/services/research-config.ts
  key_links:
    - "study_accounts resolve response ↔ bindOnce ↔ IndexedDB research_metadata identity row"
    - "App.tsx hydration gate ↔ setup-required redirect ↔ getRequired() readers"
---

<objective>
Introduce the immutable study-context layer: the `ResearchIdentity` types, a bind-once service reading the durable identity, the non-secret research config module, and the App.tsx hydration gate that redirects to a researcher setup screen until identity is bound. This makes `"control" | "experimental"` assignable (via researcher-led install) and readable app-wide so downstream services can branch on it — SHELL-03 and locked decisions D-01/D-02/D-03/D-12.

Purpose: A hydrated immutable identity (not a mutable settings boolean) guarantees every later log/record carries the correct condition and cannot be flipped by a participant.
Output: research types, study-context service, research-config, setup screen, a diagnostics stub screen (filled in Plan 09), App.tsx gate, and event-union additions.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `app/src/types/research.ts`: `StudyCondition`, `ResearchIdentity`, `InteractionEventType`, `UserInteractionEvent`, `QuestionAnswerRecord`
- `app/src/services/study-context.service.ts`: `studyContextService` (`hydrate`, `bindOnce`, `getRequired`, `getOptional`, `isBound`)
- `app/src/services/research-config.ts`: `researchConfig`, `resolveResearchConfig`
- `app/src/screens/ResearchSetupScreen.tsx`, `app/src/screens/ResearchDiagnosticsScreen.tsx` (stub)
- New `AppEvent` members `RESEARCH_IDENTITY_BOUND`, `UPLOAD_STATUS_CHANGED`
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-CONTEXT.md
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@app/src/App.tsx
@app/src/services/settings.service.ts
@app/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research types + non-secret research-config + event-union additions</name>
  <files>app/src/types/research.ts, app/src/services/research-config.ts, app/src/types/index.ts</files>
  <read_first>
    - app/src/types/index.ts (interface/type style; `AppEvent` union at ~line 474)
    - app/src/services/generation-config.ts (pure resolve-config pattern analog)
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Pattern 1"/"Pattern 2"/"Pattern 5"
    - CLAUDE.md § i18n (proper nouns) and § event-bus (one event per semantic action)
  </read_first>
  <action>
    Create `app/src/types/research.ts` exporting: `StudyCondition = 'control' | 'experimental'`; `ResearchIdentity { userId: string; condition: StudyCondition; topicId: string; boundAt: string }`; the 16-member `InteractionEventType` union (app_open, feed_impression, post_open, post_close, source_click, video_play, video_progress, question_suggestion_click, question_submit, ai_answer_view, save_post, not_interested, recommendation_reason_view, notification_received, notification_open, session_end); `UserInteractionEvent { id; userId; condition; topicId; timestamp; eventType; postId?; questionId?; recommendationId?; durationMs? }` with NO open payload field; and `QuestionAnswerRecord { id; revision; userId; condition; topicId; postId; questionId; questionText; questionSource: 'typed'|'suggested_question'; submittedAt; answerText?; answerViewedAt? }` (per D-07/D-08 — question text lives here, never as event payload). Create `app/src/services/research-config.ts` with `resolveResearchConfig()` reading only `import.meta.env.VITE_RESEARCH_API_BASE_URL` and optional `VITE_RESEARCH_PIN_SHA256` (a digest, not a raw PIN), validating the base URL is present and non-secret; export a `researchConfig` accessor. Add `RESEARCH_IDENTITY_BOUND` (payload `{ userId: string; condition: StudyCondition; topicId: string }`) and `UPLOAD_STATUS_CHANGED` (payload `{ pending: number; lastSuccessAt: string | null }`) to the `AppEvent` union in `types/index.ts`.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `types/research.ts` exports all five symbols; `UserInteractionEvent` has no `payload`/`sourceUrl`/`route`/`device` field.
    - `research-config.ts` reads no VITE_ variable that could carry a secret (only base URL + PIN digest); throws/flags when the base URL is missing.
    - `AppEvent` includes `RESEARCH_IDENTITY_BOUND` and `UPLOAD_STATUS_CHANGED`; `tsc -b` passes.
  </acceptance_criteria>
  <done>Type contracts and non-secret config exist; the interaction schema forbids prohibited fields at the type level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Immutable study-context service (bind-once, hydrate, read-only)</name>
  <files>app/src/services/study-context.service.ts, app/tests/services/study-context.service.test.mjs</files>
  <read_first>
    - app/src/services/settings.service.ts (sync-read pattern) and app/src/services/engagement.service.ts (hydrate-from-SQLite pattern)
    - app/src/services/db.service.ts (dbQuery/dbExecute against research_metadata)
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Pattern 1: Immutable Study Context"
  </read_first>
  <behavior>
    - `hydrate()` loads the identity from `research_metadata` (id `identity`) into a sync-readable mirror; idempotent.
    - `getRequired()` throws when unbound; `getOptional()` returns null when unbound; `isBound()` reflects state.
    - `bindOnce(identity)` writes durably (dbExecute INSERT OR REPLACE) and emits `RESEARCH_IDENTITY_BOUND`; calling it again with a DIFFERENT userId/condition/topic REJECTS (throws) and does not overwrite.
    - There is no exported setter/clear/logout.
  </behavior>
  <action>
    Create `app/src/services/study-context.service.ts` as `studyContextService` with `hydrate`, `getRequired`, `getOptional`, `isBound`, and `bindOnce`. Persist the identity JSON to the `research_metadata` store (row id `identity`) via `dbExecute`, and read back through `dbQuery` on hydrate. `bindOnce` must reject a conflicting re-bind (idempotent for an identical identity, throw for a different one) — enforcing D-02/D-03 (no condition switch, permanent binding). Emit `RESEARCH_IDENTITY_BOUND` on first successful bind. Author `app/tests/services/study-context.service.test.mjs` (localStorage shim + dbQuery seam) asserting: unbound `getRequired` throws; `bindOnce` persists and `getRequired().condition` is readable after re-hydrate; a conflicting second `bindOnce` throws; there is no exported mutator to change condition.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/study-context.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - After `bindOnce` + fresh `hydrate` (new mirror), `getRequired().condition` returns the bound value through the dbQuery seam (behavioral, not mirror-only).
    - A second `bindOnce` with a different condition throws and leaves the original identity intact.
    - The module exports no function that mutates condition/userId/topic after bind.
  </acceptance_criteria>
  <done>Condition is bound once, durable, sync-readable app-wide, and immutable to participant flows.</done>
</task>

<task type="auto">
  <name>Task 3: Researcher setup screen, diagnostics stub, and App.tsx hydration gate</name>
  <files>app/src/screens/ResearchSetupScreen.tsx, app/src/screens/ResearchDiagnosticsScreen.tsx, app/src/App.tsx</files>
  <read_first>
    - app/src/App.tsx (hydrateAllFromSQLite, `hydrated` gate, router children, settings sub-routes at lines 223-226)
    - app/src/screens/settings/SettingsDataScreen.tsx (screen/Card/Header component style analog)
    - app/src/services/research-config.ts (apiBaseUrl for the resolve call)
  </read_first>
  <action>
    Create `app/src/screens/ResearchSetupScreen.tsx`: a minimal researcher-only bind flow that accepts the neutral numeric account ID, POSTs it to `${researchConfig.apiBaseUrl}/v1/install/resolve`, and on success calls `studyContextService.bindOnce({ userId, condition, topicId, boundAt })` (D-01). No condition/topic UI — those come from the server (D-02). Create a MINIMAL `app/src/screens/ResearchDiagnosticsScreen.tsx` stub (renders a placeholder; PIN gate + diagnostics land in Plan 09) so its route can be registered now. In `App.tsx`: call `studyContextService.hydrate()` inside `hydrateAllFromSQLite` (awaited with the other hydrates); add routes `/research-setup` → ResearchSetupScreen and `/research-diagnostics` → ResearchDiagnosticsScreen; and change `HomeRedirect` (or add a gate) so that when `studyContextService.isBound()` is false the app navigates to `/research-setup` instead of the feed. REMOVE the four settings sub-route entries (`settings/ai`, `settings/content`, `settings/features`, `settings/data`) and their four screen imports from App.tsx (their screen files are deleted in Plan 09; removing the routes/imports here decouples that deletion). Preserve the SwipeTabContainer, header portal, and overflow invariants (CLAUDE.md).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` awaits `studyContextService.hydrate()` before first render and routes to `/research-setup` when identity is unbound.
    - Routes `/research-setup` and `/research-diagnostics` are registered; the four `settings/{ai,content,features,data}` routes and their imports are gone from App.tsx.
    - `tsc -b` and `lint` pass (no dangling imports of the not-yet-deleted sub-screens).
  </acceptance_criteria>
  <done>The app gates on research identity, exposes the researcher bind flow, and no longer routes to the removed settings sub-screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant UI → condition state | A participant must not be able to read/flip their condition; identity is bound by the researcher and immutable. |
| build env → app bundle | VITE_ config is bundled into client source; it must never carry a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | condition mutation | high | mitigate | Immutable identity; no exported mutator; `bindOnce` rejects conflicting re-bind (study-context test asserts). |
| T-01-13 | Information disclosure | secret in VITE_ config | high | mitigate | research-config reads only base URL + PIN digest; no key/password variable; enforced by review + naming. |
| T-01-14 | Elevation of privilege | participant reaching feed unbound | medium | mitigate | App.tsx gate redirects to setup-required until `isBound()` is true. |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green.
- Manual UAT: fresh install lands on `/research-setup`; entering a (mocked) account binds identity and reveals the feed; relaunch keeps identity (no re-setup); no participant-facing condition control exists.
</verification>

<success_criteria>
`control`/`experimental` is bound once and readable app-wide; the app gates on it; no secret in client config; gates green.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-04-SUMMARY.md` when done.
</output>
