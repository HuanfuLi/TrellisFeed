---
phase: 01-rebrand-research-shell-hardening
plan: 13
type: execute
wave: 8
depends_on: ["01-11", "01-12"]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/scripts/run-tests.mjs
  - app/src/components/ui/BottomSheet.tsx
  - app/src/components/ChatInput.tsx
  - app/src/lib/locale.ts
  - app/src/lib/cold-start-profiler.ts
  - app/src/screens/HomeScreen.tsx
  - app/src/screens/OnboardingScreen.tsx
  - app/src/services/concept-feed.service.ts
  - app/src/services/canonical-knowledge.service.ts
  - app/src/services/filter-corpus.service.ts
  - app/src/services/question-filter.service.ts
  - app/src/services/question.service.ts
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/components/BottomSheet.test.mjs
  - app/tests/components/ChatInput.flex-shrink.test.mjs
  - app/tests/components/ChatInput.send-on-pointerdown.test.mjs
  - app/tests/components/bottom-sheet-no-bare-autofocus.test.mjs
  - app/tests/services/post-history-fallback.test.mjs
  - app/tests/services/post-history.test.mjs
  - app/tests/services/settings-locale.test.mjs
  - app/tests/services/storage-namespace.test.mjs
  - app/tests/phase1/rebrand-surfaces.test.mjs
  - app/tests/phase1/pruned-residue.test.mjs
  - .planning/phases/01-rebrand-research-shell-hardening/01-TEST-BASELINE.md
  - .planning/phases/01-rebrand-research-shell-hardening/01-DEPLOYMENT-SMOKE.md
autonomous: false
gap_closure: true
requirements: [SHELL-01, SHELL-04, LOG-01, RQ-01]
must_haves:
  truths:
    - "WR-05/D-06: a fresh install starts in English even when navigator/device locale is Chinese, Spanish, or Japanese; only an explicit participant selection changes and persists UI language immediately."
    - "WR-06/SHELL-01: no active participant-facing string, starter post, href/mail subject, locale value, web/native display name, or screen exposes the Trellis brand; native bundle identifiers remain unchanged."
    - "WR-07/SHELL-04: the orphan cold-start profiler, its pinning test contract, and unused retired AI/data/developer/live-fetch/clear-data locale copy are removed after call-site proof; no pruned feature is reintroduced."
    - "The six existing BottomSheet/ChatInput/post-history failures are classified with non-attribution evidence where applicable and then fixed minimally; no failure is accepted as a baseline exemption."
    - "The exact cross-platform `npm test`, `npm run lint`, `npm run build`, backend full tests, CORS/auth integration, consent/privacy forbidden-field guards, and native sync all finish with zero failures/errors."
    - "Final real-device smoke proves consent-first use, numeric-only setup, English default, offline persistence/automatic补传, authenticated cloud receipt, PIN diagnostics, and protected two-CSV export without recording any secret/URL/ID."
  artifacts:
    - app/scripts/run-tests.mjs
    - .planning/phases/01-rebrand-research-shell-hardening/01-TEST-BASELINE.md
    - .planning/phases/01-rebrand-research-shell-hardening/01-DEPLOYMENT-SMOKE.md
  key_links:
    - "fresh default settings locale=en -> i18n bootstrap -> onboarding selected locale -> explicit language action only"
    - "active UI literals/attributes + locale values + starter posts -> broadened rebrand-surfaces regression"
    - "locale call-site reachability -> four-bundle identical deletion -> parity/missing-key tests"
    - "cross-platform test discovery runner -> npm test -> zero-failure phase completion gate"
---

<objective>
Close the remaining branded-shell/protocol gaps and finish Phase 1 with a genuine zero-failure gate. Remove active legacy branding and orphaned retired copy/code, enforce English on a fresh install, repair the six brittle/stale source-contract tests without weakening their load-bearing invariants, make `npm test` cross-platform, and complete the final signed-device collection smoke.

Purpose: Phase 1 must leave a clean research shell and trustworthy green baseline for Phase 2—not a nominal completion that carries known failures or participant-visible residue.
Output: active-surface rebrand and residue cleanup; deterministic English default; robust/cross-platform tests; attribution record; all automated gates green; sanitized signed-device smoke evidence.
</objective>

<artifacts_this_phase_produces>
Artifacts this phase produces:
- `app/scripts/run-tests.mjs` — OS-independent recursive `*.test.mjs` discovery and Node test-process exit propagation used by `npm test`.
- `.planning/phases/01-rebrand-research-shell-hardening/01-TEST-BASELINE.md` — factual last-modified/non-attribution record for the six failures plus their final repaired status.
- Expanded `rebrand-surfaces.test.mjs` and `pruned-residue.test.mjs` guards for active JSX attributes, starter content, locale reachability, and orphan files.
- Updated `.planning/.../01-DEPLOYMENT-SMOKE.md` with sanitized signed-build/offline closeout evidence.
- Removed artifact `app/src/lib/cold-start-profiler.ts` and, if no live caller remains, the unused `@capacitor/device` package/function.
</artifacts_this_phase_produces>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.planning/phases/01-rebrand-research-shell-hardening/01-CONTEXT.md
@.planning/phases/01-rebrand-research-shell-hardening/01-UI-SPEC.md
@.planning/phases/01-rebrand-research-shell-hardening/01-VERIFICATION.md
@.planning/phases/01-rebrand-research-shell-hardening/01-REVIEW.md
@CLAUDE.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete active-surface rebrand and call-site-proven residue removal</name>
  <files>app/src/screens/HomeScreen.tsx, app/src/services/concept-feed.service.ts, app/src/services/canonical-knowledge.service.ts, app/src/services/filter-corpus.service.ts, app/src/services/question-filter.service.ts, app/src/services/question.service.ts, app/src/lib/cold-start-profiler.ts, app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json, app/tests/services/storage-namespace.test.mjs, app/tests/phase1/rebrand-surfaces.test.mjs, app/tests/phase1/pruned-residue.test.mjs</files>
  <read_first>
    - app/src/screens/HomeScreen.tsx (two active `mailto:` Trellis feedback subjects and whether the action belongs on the participant surface)
    - app/src/services/concept-feed.service.ts (`STARTER_POSTS` participant-visible legacy titles/bodies)
    - app/src/lib/cold-start-profiler.ts and repository-wide import search (currently test-only)
    - app/tests/services/storage-namespace.test.mjs (currently pins the orphan profiler)
    - app/src/locales/en.json plus `rg` call-site inventory of every `t('...')` key under retired settings/live-fetch/data-clear namespaces
    - app/tests/phase1/rebrand-surfaces.test.mjs (currently scans native surfaces and English locale only)
    - app/tests/phase1/pruned-residue.test.mjs (current narrow token list)
    - docs/prune_report.md and CLAUDE.md retained-vs-pruned boundaries
  </read_first>
  <action>
    RED: broaden rebrand coverage to active participant-facing TSX attributes and transitional starter-post strings, while explicitly allowlisting only the unchanged native IDs and internal/historical semantic identifiers that are not rendered. Add residue assertions proving the profiler has no production importer and that retired participant settings/live-fetch/clear-data locale families have no live call site before deletion.

    GREEN: remove the feedback mail actions entirely if the call-site review confirms they are outside the locked participant surface; otherwise replace only their visible subject with QuestionTrace. Rewrite every active starter post title/body that currently says Trellis to QuestionTrace/neutral research-shell wording without polishing or expanding the temporary generated-feed feature. Rename active console diagnostic prefixes to `[QuestionTrace]` where this is a literal-only change; do not rename native IDs or data-model fields whose semantics/history require stability.

    Delete `cold-start-profiler.ts` because it has no production caller, and remove its import/owner expectation from storage-namespace tests. For locale cleanup, first build a concrete key-to-call-site inventory; remove only keys proven unused and tied to deleted AI/data/developer settings, clear-data controls, YouTube/live web search, or other §15.3 surfaces, identically across en/zh/es/ja. Preserve any key with a live retained call site and preserve key parity. Do not reintroduce settings screens, live fetch, data clear, graph UI, or other pruned functions.
  </action>
  <acceptance_criteria>
    - No participant-visible active source or attribute—including starter posts and feedback actions—contains the legacy brand; unchanged native IDs still pass exact assertions.
    - Orphan profiler file and its test-only pin are absent; no live import is broken.
    - Every removed locale key has documented zero call sites and is removed from all four bundles; parity and missing-key tests pass.
    - Transitional feed algorithms, `GRAPH_UPDATED`, canonical anchoring, ChatInput, and daily-read exploration behavior remain unchanged.
    - Rebrand/residue tests fail on reintroduction of an active brand string or retired UI family without falsely matching comments/history/required IDs.
  </acceptance_criteria>
  <verify>
    <automated>cd app &amp;&amp; node --test tests/phase1/rebrand-surfaces.test.mjs tests/phase1/pruned-residue.test.mjs tests/services/storage-namespace.test.mjs tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make English the deterministic fresh-install default</name>
  <files>app/src/screens/OnboardingScreen.tsx, app/src/lib/locale.ts, app/package.json, app/package-lock.json, app/tests/services/settings-locale.test.mjs, app/tests/phase1/consent-gate.test.mjs</files>
  <read_first>
    - app/src/screens/OnboardingScreen.tsx (current async `detectDeviceLocale()` override)
    - app/src/locales/index.ts (`settingsService` saved locale passed to `detectInitialLocale`)
    - app/src/lib/locale.ts (`detectInitialLocale`, `detectDeviceLocale`, Capacitor Device import)
    - app/src/services/settings.service.ts (fresh `preferences.locale='en'` default)
    - app/tests/services/settings-locale.test.mjs
    - app/package.json and package-lock entry for `@capacitor/device`
    - CONTEXT D-06 and 01-UI-SPEC (language switch remains immediate; content remains English)
  </read_first>
  <action>
    RED: add a test fixture with non-English `navigator.language`/native-device detection and no stored settings, asserting i18n/onboarding still selects and persists `en`. Keep existing tests proving a previously saved participant locale wins and language changes remain immediate.

    GREEN: remove the onboarding effect that automatically replaces the fresh `en` selection with the device locale. Initialize from the persisted supported locale when it truly exists, otherwise `en`; do not infer the default from navigator/device. Retain the four explicit language buttons and immediate `i18n.changeLanguage` + event-bus update. If the call-site sweep confirms `detectDeviceLocale` and `@capacitor/device` are now unused, remove the function/import and dependency/lock entry cleanly; keep `normalizeLocale` and any live saved-preference behavior.
  </action>
  <acceptance_criteria>
    - Fresh install uses English under zh/es/ja device or navigator locales.
    - Existing saved zh/es/ja locale still restores, and explicit selection changes the UI immediately without restart.
    - Curated/user/answer content is not translated by this change.
    - No unused native Device import/dependency remains if it has zero callers.
  </acceptance_criteria>
  <verify>
    <automated>cd app &amp;&amp; node --test tests/services/settings-locale.test.mjs tests/phase1/consent-gate.test.mjs tests/locales/bundle-parity.test.mjs &amp;&amp; npx tsc -b --noEmit &amp;&amp; npm run lint</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repair all six source-contract failures and make npm test cross-platform</name>
  <files>app/package.json, app/scripts/run-tests.mjs, app/src/components/ui/BottomSheet.tsx, app/src/components/ChatInput.tsx, app/tests/components/BottomSheet.test.mjs, app/tests/components/ChatInput.flex-shrink.test.mjs, app/tests/components/ChatInput.send-on-pointerdown.test.mjs, app/tests/components/bottom-sheet-no-bare-autofocus.test.mjs, app/tests/services/post-history-fallback.test.mjs, app/tests/services/post-history.test.mjs, .planning/phases/01-rebrand-research-shell-hardening/01-TEST-BASELINE.md</files>
  <read_first>
    - Current full-suite output and the six failures named in 01-VERIFICATION.md/01-REVIEW.md
    - app/src/components/ui/BottomSheet.tsx and tests/components/BottomSheet.test.mjs (styles are co-located; 200-character comment window is brittle)
    - app/src/components/ChatInput.tsx and its two tests (live input has required `flex:1`, `minWidth:0`, pointerdown send; tests assume exact indentation)
    - app/tests/components/bottom-sheet-no-bare-autofocus.test.mjs (Windows falls through from `rg` to unavailable `grep`)
    - app/src/services/post-essay.service.ts and post-history-fallback.test.mjs (live function uses valid CRLF/one-line try blocks; test uses brittle newline terminator)
    - app/src/services/post-history.service.ts, settings.service.ts, and post-history.test.mjs (test seeds retired `trellis_settings`)
    - app/package.json (`npm test` currently uses Unix `$(find ...)`)
    - `git log --follow`/`git blame` for each failing test and target, plus Phase-1 summaries, to establish attribution before edits
  </read_first>
  <action>
    First write `01-TEST-BASELINE.md` with the six exact failing test names, last-modified commits for test/target files, whether Phase 1 touched the behavior, and the concrete observed cause. Record that BottomSheet/ChatInput failures are source-parser brittleness if history confirms it; record Phase-1 attribution for the stale post-history settings key if confirmed. This evidence is explanatory only—every failure must still be repaired.

    Fix minimally without weakening the protected behaviors:
    - BottomSheet: test structural co-location within the same inline style object rather than an arbitrary 200-character distance; keep `overflowY`, `overscrollBehavior`, WebKit momentum, portal, bottom anchor, and rounded mask unchanged.
    - ChatInput: locate the JSX input with whitespace-tolerant parsing/regex and still assert `flex:1`, `minWidth:0`, shared submit function, pointerdown preventDefault, Enter submit, and disabled guard. Change production only if an invariant is actually missing.
    - BottomSheet consumers: replace shelling out to platform `rg/grep` with recursive Node `fs` traversal so Windows and POSIX execute the same assertion.
    - Post essay: use brace-aware or newline-style-independent function extraction and retain unconditional patches to both durable stores.
    - Post history: seed `questiontrace_settings` (or call the service API) and keep the behavioral null-retention assertion.

    Add `app/scripts/run-tests.mjs` that recursively discovers sorted `app/tests/**/*.test.mjs`, spawns `process.execPath --test` with argument-array semantics, inherits stdio, and returns the child exit code. Point `npm test`/`test:main` at it so the canonical command works in PowerShell and POSIX without shell substitution. Do not hide, filter, retry, or mark any failure allowed.
  </action>
  <acceptance_criteria>
    - 01-TEST-BASELINE documents factual attribution and ends with all six repaired; it does not create an exemption.
    - Every load-bearing BottomSheet/ChatInput/post-history invariant remains asserted with OS/format-tolerant test logic.
    - `npm test` discovers the same complete set on Windows/POSIX and propagates any single failing test as non-zero.
    - The formerly failing six tests pass, and the complete suite has zero failures.
  </acceptance_criteria>
  <verify>
    <automated>cd app &amp;&amp; npm test</automated>
  </verify>
</task>

<task type="checkpoint:human-action">
  <name>Checkpoint: Run final all-green gates and signed-device end-to-end study smoke</name>
  <files>.planning/phases/01-rebrand-research-shell-hardening/01-DEPLOYMENT-SMOKE.md</files>
  <read_first>
    - .planning/phases/01-rebrand-research-shell-hardening/01-DEPLOYMENT-SMOKE.md (Plan 11 sanitized server/browser evidence)
    - .planning/phases/01-rebrand-research-shell-hardening/01-TEST-BASELINE.md
    - .planning/phases/01-rebrand-research-shell-hardening/01-UI-SPEC.md
    - app/capacitor.config.ts and CLAUDE.md native ID/header/gesture/overflow invariants
    - app/src/screens/ResearchDiagnosticsScreen.tsx (expected PIN/status/export-only behavior)
  </read_first>
  <action>
    Executor first runs every automated closeout command below and stops on any failure. Then, with private build variables supplied only in the shell/CI secret store, create the research build and run `npx cap sync`; inspect native diffs to ensure only expected web assets/display/plugin changes and unchanged bundle IDs.

    On a real participant-style phone install, verify in order: first screen defaults to English; researcher enters only a neutral numeric account; setup leads to consent, direct participant deep links cannot bypass it, and central count remains unchanged before consent; affirmative consent unlocks Feed/Saved/Settings; Settings shows numeric ID + language only and changes language immediately; go offline, create multiple behavioral/Q&A records, kill the app, relaunch offline, restore connectivity without another participant action, and observe pending drain automatically; hidden diagnostics requires PIN and exposes only pending/last success/Export Data; server admin remains password-protected and two-CSV export contains the new rows. Exercise a backlog larger than one batch or the automated equivalent, and confirm quarantine/recovery status if a controlled invalid fixture is available.

    Update `01-DEPLOYMENT-SMOKE.md` with PASS/FAIL labels, UTC timestamp, platform/build version, native-sync review, pre-consent-zero, offline-kill-resume-drain, diagnostics, protected export, and cleanup confirmation. Never record URL/origin strings, D1/Worker IDs, numeric account, token/header, password/PIN, enrollment credential, or provider key. Remove temporary study rows/fixtures after verification.
  </action>
  <acceptance_criteria>
    - App `npm test`, lint, build, TypeScript and backend tests are 0-failure/0-error; no six-failure baseline remains.
    - CORS/auth/collision/consent/privacy forbidden-field tests pass; classified source scans confirm no forbidden fields or actual secrets.
    - `npx cap sync` succeeds and native bundle identifiers remain exact.
    - Real device proves consent-first, English-first, offline kill/resume automatic upload, minimal diagnostics, and protected two-CSV aggregation.
    - Sanitized smoke evidence contains no sensitive values and all temporary fixtures are removed.
  </acceptance_criteria>
  <verify>
    <automated>cd app &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx tsc -b --noEmit &amp;&amp; cd ../research-backend &amp;&amp; npm test</automated>
    <manual>Run private-config build + `npx cap sync`, inspect native identifiers/diff, complete signed-device flow, and approve sanitized `01-DEPLOYMENT-SMOKE.md`.</manual>
  </verify>
</task>

</tasks>

<threat_model>
## ASVS L1 Security Enforcement

**Policy:** OWASP ASVS L1 baseline; `block_on: high`. Any unresolved high-severity privacy, credential, export, or test-gate issue blocks Phase 1 completion.

### Assets
- Participant-facing research framing and blinded account/condition experience.
- Fresh-install protocol consistency (English + affirmative consent).
- Local/offline and cloud research data completeness.
- Native identifiers/signing continuity.
- Integrity of the full automated completion gate.

### Trust boundaries
- Source/locales/starter posts -> participant-visible shell.
- Device locale -> persisted UI preference.
- Local outbox/recovery -> deployed collector/admin export.
- Test discovery/source contracts -> CI completion decision.
- Private build configuration -> signed package and smoke evidence.

### Threats, mitigations, verification
| ID | ASVS | Threat | Severity | Mitigation | Verification |
|---|---|---|---|---|---|
| P13-T1 | V8 Data Protection | retired brand/copy misstates study or exposes removed controls | high | active-surface scan + call-site-proven four-locale deletion | rebrand/residue/parity tests |
| P13-T2 | V4/V8 | device locale changes initial protocol exposure | medium | persisted preference or English only; remove auto-detect override | non-English-device fresh test |
| P13-T3 | V7 Error Handling | test runner omits tests or accepts known failures | high | Node recursive discovery, child exit propagation, zero-exemption baseline | inject-failure check + full npm test |
| P13-T4 | V8 | private endpoint/token/ID enters build log/evidence | high | shell/CI-only injection and sanitized evidence schema | classified secret scan + human review |
| P13-T5 | V4/V5 | signed WebView differs from Node/curl CORS/auth result | high | real signed-device consent/offline/upload/admin smoke | checkpoint evidence |
| P13-T6 | V8 | cleanup breaks load-bearing feed/data/header/input behavior | high | call-site scan, positive invariants, full build/test/native sync | pruned-residue + full suite + native diff |
</threat_model>

<verification>
- Focused task tests pass after RED/GREEN work.
- Canonical final gate is exactly: `cd app && npm test && npm run lint && npm run build && npx tsc -b --noEmit`, with zero failures/errors.
- Backend gate: `cd research-backend && npm test`, including auth/CORS/collision/CSV tests.
- Privacy guards prove no pre-consent rows and no sourceUrl/feedPosition/route/device/keystroke/arbitrary payload/identity wire fields.
- Classified scans find no actual endpoint, credentials, token, PIN, password, D1/Worker ID, or participant ID in tracked artifacts.
- Native sync + signed-device end-to-end smoke is required; curl-only proof is insufficient.
</verification>

<success_criteria>
The Phase-1 shell is English-first, consent-first, fully QuestionTrace-branded on active surfaces, free of the identified orphan residue, and backed by a genuine all-green cross-platform test/build/backend gate plus sanitized real-device collection evidence.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-13-SUMMARY.md` when complete, then rerun Phase-1 verification; it may pass only with zero incomplete plans and zero test failures.
</output>
