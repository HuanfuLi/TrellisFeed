---
phase: 01-rebrand-research-shell-hardening
plan: 06
type: tdd
wave: 3
depends_on: ["01-02", "01-04"]
files_modified:
  - app/src/services/upload-queue.service.ts
  - app/src/services/research-metadata.service.ts
  - app/tests/services/upload-queue.service.test.mjs
autonomous: true
requirements: [LOG-01]
must_haves:
  truths:
    - "Every record is written to a durable queue envelope BEFORE any upload is attempted; a failed/aborted/5xx upload leaves envelopes intact for retry (D-13)."
    - "A single-flighted flush sends a bounded batch (≤100 envelopes / ≤256 KiB) and deletes ONLY the server-acknowledged envelope ids; a lost response after commit is safe because re-send is idempotent."
    - "Flush is triggered on enqueue, browser `online`, and Capacitor app-resume; `navigator.onLine` is only a retry trigger, never proof of delivery."
    - "Pending count and last-successful-upload time are durable in `research_metadata` and broadcast via `UPLOAD_STATUS_CHANGED` for always-mounted screens to re-read (CLAUDE.md no-refresh)."
  artifacts:
    - app/src/services/upload-queue.service.ts
    - app/src/services/research-metadata.service.ts
  key_links:
    - "enqueue durable write ↔ serialized flush ↔ acknowledgedIds-only deletion"
    - "research_metadata pending/lastSuccess ↔ UPLOAD_STATUS_CHANGED ↔ diagnostics screen"
---

<objective>
Build the durable at-least-once upload queue and the research-metadata store: persist-before-send, a single-flighted bounded flush against `POST /v1/ingest`, ACK-only deletion, retry triggers on enqueue/online/resume, and durable upload-health metadata surfaced via an event. Client side of LOG-01 and locked decision D-13 (offline-durable, retry-until-acknowledged).

Purpose: A participant action must never be lost to offline/response-loss; only a server ACK removes a queued record.
Output: upload-queue.service.ts, research-metadata.service.ts, and a full TDD suite through the dbQuery seam with mocked fetch.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `app/src/services/upload-queue.service.ts` (`enqueue`, `flushPendingUploads`, `getPendingCount`, `registerRetryTriggers`)
- `app/src/services/research-metadata.service.ts` (`getLastSuccessfulUploadAt`, `setLastSuccessfulUploadAt`, `getPendingCount`)
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@app/src/services/post-queue.service.ts
@app/src/services/db.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Durable upload queue + research metadata (RED→GREEN→REFACTOR)</name>
  <files>app/src/services/upload-queue.service.ts, app/src/services/research-metadata.service.ts, app/tests/services/upload-queue.service.test.mjs</files>
  <read_first>
    - app/src/services/post-queue.service.ts (batch/dequeue + IndexedDB write-through analog)
    - app/src/services/db.service.ts (dbExecute/dbQuery; supported SQL subset — SELECT *, INSERT OR REPLACE, PK DELETE; sort in JS)
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Pattern 3: Durable At-Least-Once Upload with Idempotent ACK" and § Common Pitfalls (onLine, delete-on-dispatch, revisions)
    - CLAUDE.md § "Feed cold start" (mutex `createPromiseMutex` / single-flight pattern) and § event-bus re-read rule
  </read_first>
  <behavior>
    RED → GREEN → REFACTOR cycles, asserting through `dbQuery` (not the mirror) with mocked `fetch`:
    - persist-before-send: `enqueue(record)` writes a `research_upload_queue` envelope row via `dbExecute` before any fetch; with fetch stubbed to reject, the envelope remains queryable.
    - serialized flush: two concurrent `flushPendingUploads()` calls share one in-flight promise (single-flight); only one batch POST fires.
    - bounded batch: with 250 queued envelopes, one flush sends ≤100 and ≤256 KiB; ordering is oldest-first (sorted in JS).
    - ACK-only deletion: a 200 returning `acknowledgedIds` for a SUBSET deletes only those envelopes; the rest remain for the next flush.
    - retry retention: a network reject, an aborted request, and an HTTP 500 each leave all envelopes intact (nothing deleted).
    - lost-response safety: after a successful commit whose response is dropped (simulated), the envelope stays and a re-flush re-sends it (idempotent server makes this safe) — no data loss.
    - metadata: on a successful ACK, `research-metadata` records `lastSuccessfulUploadAt` and the pending count decreases; `UPLOAD_STATUS_CHANGED` is emitted with `{ pending, lastSuccessAt }`.
    - triggers: `registerRetryTriggers()` wires `window` `online` and (guarded) Capacitor `appStateChange` active → `flushPendingUploads`; `navigator.onLine` alone never deletes envelopes.
  </behavior>
  <action>
    Create `app/src/services/research-metadata.service.ts` persisting `pending`/`lastSuccessfulUploadAt` to the `research_metadata` store (row id `upload`) with sync-readable mirror + dbExecute write-through, emitting `UPLOAD_STATUS_CHANGED` on change. Create `app/src/services/upload-queue.service.ts` implementing `enqueue`, `flushPendingUploads` (single-flight via a promise-mutex like `createPromiseMutex`; oldest-first bounded batch; POST to `${researchConfig.apiBaseUrl}/v1/ingest`; parse `acknowledgedIds`; delete only acknowledged envelopes by PK; update metadata; emit status), `getPendingCount`, and `registerRetryTriggers`. Use ONLY the supported SQL subset (SELECT *, INSERT OR REPLACE, PK DELETE) and sort batches in JS (CLAUDE.md). Author `app/tests/services/upload-queue.service.test.mjs` with the localStorage shim and a mock `fetch`, covering every behavior above through `dbQuery`.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/upload-queue.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Envelope is durable in `research_upload_queue` (via dbQuery) before any fetch resolves.
    - Concurrent flushes fire exactly one POST (single-flight).
    - A partial `acknowledgedIds` deletes only acknowledged envelopes; unacknowledged remain.
    - Network reject / abort / HTTP 500 delete nothing; a re-flush after a lost response re-sends without loss.
    - `lastSuccessfulUploadAt` and pending count update in `research_metadata`; `UPLOAD_STATUS_CHANGED` emitted.
  </acceptance_criteria>
  <done>Records are durably queued, flushed single-flight in bounded batches, deleted only on ACK, and retried on online/resume — with durable, broadcast upload health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app action → durable queue → network | Records must survive offline/app-death between action and server ACK; only ACK removes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Repudiation | data loss on offline/response-loss | high | mitigate | Persist-before-send; ACK-only deletion; retry on online/resume (tests assert reject/abort/500 retain, lost-response re-sends). |
| T-01-21 | Tampering | delete-on-dispatch bug | high | mitigate | Delete only `acknowledgedIds`; partial-ACK test asserts unacknowledged remain. |
| T-01-22 | Denial of service | unbounded flush body | medium | mitigate | Bounded ≤100 envelopes / ≤256 KiB per flush (matches server bound in Plan 03). |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green.
- Manual UAT (device): go offline, generate records, kill/resume the app, regain network — confirm the queue drains and last-upload time advances (pairs with diagnostics in Plan 09).
</verification>

<success_criteria>
No record is lost across offline/restart/response-loss; ACK-only deletion; bounded single-flight flush; durable broadcast upload health; TDD suite green through the seam.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-06-SUMMARY.md` when done.
</output>
