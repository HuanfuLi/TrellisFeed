---
phase: 01-rebrand-research-shell-hardening
plan: 10
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-09"]
files_modified:
  - app/src/services/canonical-knowledge.service.ts
  - app/src/services/daily-read.service.ts
  - app/src/services/question.service.ts
  - app/src/types/index.ts
  - app/src/index.css
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/scripts/profile-trellis.mjs
  - app/tests/services/daily-read.service.test.mjs
  - app/tests/phase1/pruned-residue.test.mjs
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "No live `app/src` code, export, event, locale namespace, asset, or CSS var references a §15.3-pruned feature (mind-map reorganization, gamification credits, news/video styling) — verified by an import/call-site scan, not filename guessing (SHELL-04)."
    - "Load-bearing research-shell infrastructure survives untouched: `GRAPH_UPDATED`, `commitClassificationResult`/`classifyAndAnchorIncremental` canonical anchoring, `ChatInput`, and daily-read `exploredAnchors` lazy-skip."
    - "Inert dead test helpers and the stale profiler script that import removed services are deleted, and no live test imports them."
    - "All gates stay green after the sweep (`npm test`, `npm run lint` 0 errors, `npm run build`, `tsc -b`)."
  artifacts:
    - app/tests/phase1/pruned-residue.test.mjs
  key_links:
    - "reorg reorganization API ↔ REORG_SNAPSHOT_KEY ↔ REORG_* AppEvents ↔ reorg locale keys (removed together)"
    - "creditAwarded field/method/test ↔ preserved exploredAnchors behavior"
---

<objective>
Complete the remaining dead-code sweep the Phase 0 prune left behind: remove the unreferenced mind-map reorganization residue, gamification `creditAwarded` state, dead locale namespaces, dead CSS vars, and inert test/script helpers — verified by a call-site scan so no live path breaks and no load-bearing infrastructure is removed. Implements SHELL-04.

Purpose: A clean `app/src` with no pruned-feature remnants keeps later phases legible and prevents accidental resurrection.
Output: scanned-and-removed residue + a pruned-residue regression test that also asserts load-bearing infrastructure survives.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `app/tests/phase1/pruned-residue.test.mjs` (new test file)
- (This plan is net-removal; it creates no new production symbols.)
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@docs/prune_report.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mind-map reorganization + gamification-credit residue (call-site verified)</name>
  <files>app/src/services/canonical-knowledge.service.ts, app/src/services/question.service.ts, app/src/components/ui/ConfirmDialog.tsx, app/src/types/index.ts, app/src/services/daily-read.service.ts, app/tests/services/daily-read.service.test.mjs</files>
  <read_first>
    - app/src/services/canonical-knowledge.service.ts (reorganization API, `REORG_SNAPSHOT_KEY`, reorg snapshot/parse/retry code; preserve `commitClassificationResult`, `classifyAndAnchorIncremental`, `normalizeAnchorName`, the embedding pre-check, and `GRAPH_UPDATED`)
    - app/src/services/question.service.ts (any reorg reference)
    - app/src/components/ui/ConfirmDialog.tsx (reorg reference)
    - app/src/types/index.ts (`REORG_STARTED`/`REORG_COMPLETED`/`REORG_FAILED` AppEvent members)
    - app/src/services/daily-read.service.ts (`creditAwarded` field/methods; preserve `exploredAnchors` + lazy-skip)
    - app/tests/services/daily-read.service.test.mjs (creditAwarded assertions)
    - docs/prune_report.md § "Leftover Dead Context"; CLAUDE.md § "Best practices" (search for dead code before assuming; guard the LIVE path)
  </read_first>
  <action>
    Do an import/call-site scan starting from live entry points (router screens, hydration boot, event-bus subscribers). Confirm the mind-map REORGANIZATION feature (its reorganize API in canonical-knowledge, the `REORG_SNAPSHOT_KEY` snapshot store, the three `REORG_*` AppEvent members, and any ConfirmDialog/question.service reorg reference) has no live caller; then remove them together as one coherent deletion. If the scan finds a live caller, STOP and report it in the summary rather than breaking the build. Remove the `creditAwarded` field + its methods from `daily-read.service.ts` and the corresponding assertions from its test, while leaving `exploredAnchors` and the lazy-skip walker behavior intact (RESEARCH sweep-candidate table). Preserve ALL load-bearing canonical anchoring, `GRAPH_UPDATED`, and daily-read exploration mechanics (CLAUDE.md).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && npm run lint && node --test tests/services/daily-read.service.test.mjs tests/services/canonical-knowledge.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The reorganization API, `REORG_SNAPSHOT_KEY`, and the three `REORG_*` AppEvent members are gone; no live reference remains (`tsc -b`/`lint` clean).
    - `creditAwarded` is removed from the service and its test; `exploredAnchors` behavior and its tests still pass.
    - `commitClassificationResult`/`classifyAndAnchorIncremental`/`GRAPH_UPDATED` are unchanged and their tests pass.
  </acceptance_criteria>
  <done>Pruned-feature code residue is removed with the live canonical/exploration paths intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead locale namespaces, CSS vars, and inert test/script helpers</name>
  <files>app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json, app/src/index.css, app/scripts/profile-trellis.mjs</files>
  <read_first>
    - app/src/locales/en.json (dead `reorganize*`/reorg namespaces; the leaf-debug `settings.developer.trellisDevMode*` namespace whose only reader — SettingsDataScreen — was deleted in Plan 09) + zh/es/ja parity
    - app/src/index.css (`--news-card-*` and `--trellis-empty-bg` vars from the pruned news/empty-state styling)
    - app/scripts/profile-trellis.mjs (imports removed trellis services)
    - app/tests/services/_actions-mock-*.mjs, _trellis-mock-*.mjs, _actions-mock-podcast.mjs, _actions-mock-tts.mjs and any test importing them
    - app/tests/locales/bundle-parity.test.mjs, missing-key.test.mjs (parity gates)
  </read_first>
  <action>
    Remove dead locale keys IDENTICALLY across all four bundles (en/zh/es/ja) to preserve parity: the mind-map reorganization namespace and the `settings.developer` leaf-debug namespace (its screen reader was deleted in Plan 09). Scan for any live `t('...')` usage of a key before removing it; keep any key still referenced. Remove the dead `--news-card-*` and `--trellis-empty-bg` CSS variables from `index.css` after confirming no live component reads them. Delete `app/scripts/profile-trellis.mjs` (it imports removed services). Scan `tests/**` for importers of the `_actions-mock-*`/`_trellis-mock-*` helpers; delete the helpers that no live `*.test.mjs` imports, and delete any stale source-reading test that only exercised removed features. Run the parity gates after locale edits.
  </action>
  <verify>
    <automated>cd app && node --test tests/locales/bundle-parity.test.mjs tests/locales/missing-key.test.mjs && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The reorg + leaf-debug locale namespaces are removed from all four bundles with parity preserved (bundle-parity/missing-key green).
    - `--news-card-*` and `--trellis-empty-bg` are gone from `index.css`; no live component references them (build green).
    - `profile-trellis.mjs` and unreferenced `_actions-mock-*`/`_trellis-mock-*` helpers are deleted; `npm test` still discovers and passes all live tests.
  </acceptance_criteria>
  <done>Dead locales, CSS, and inert helpers are removed with parity and all gates preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pruned-residue regression test (residue gone + infrastructure preserved)</name>
  <files>app/tests/phase1/pruned-residue.test.mjs</files>
  <read_first>
    - app/tests/phase1/rebrand-surfaces.test.mjs (static-source assertion style)
    - docs/prune_report.md (list of pruned features)
    - CLAUDE.md § "Static cleanup that removes live shell infrastructure" (what must survive)
  </read_first>
  <behavior>
    - Negative: `app/src` contains no live reference to the reorganization API, `REORG_SNAPSHOT_KEY`, the `REORG_*` events, `creditAwarded`, or the removed locale/CSS symbols (comment-filtered so descriptive comments don't trip it).
    - Positive: load-bearing infrastructure is still present — `GRAPH_UPDATED`, `commitClassificationResult`, `ChatInput`, and daily-read `exploredAnchors` all exist.
    - `profile-trellis.mjs` and the deleted mock helpers no longer exist.
  </behavior>
  <action>
    Create `app/tests/phase1/pruned-residue.test.mjs`. For each removed symbol, read the relevant `app/src` files and assert the symbol is absent from NON-comment lines (filter `^\s*//` and `^\s*\*`), and hold the forbidden-token list as test-file string constants (not written into any scanned source). Add positive assertions that the load-bearing symbols still exist. Assert the deleted script/mock-helper paths are absent via `fs.existsSync`. Scope all scans to `app/src` (and the deleted script/test paths) — exclude the test directory so the test's own token constants are not self-matched.
  </action>
  <verify>
    <automated>cd app && node --test tests/phase1/pruned-residue.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The test fails if any pruned-feature residue reappears in live `app/src` code.
    - The test fails if any load-bearing symbol (`GRAPH_UPDATED`, `commitClassificationResult`, `ChatInput`, `exploredAnchors`) is missing.
    - `npm test` discovers and passes the file.
  </acceptance_criteria>
  <done>The absence of pruned residue and the survival of load-bearing infrastructure are both locked by an automated gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep edits → live shell | Over-aggressive deletion could remove load-bearing canonical/exploration infrastructure or break the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-33 | Denial of service | accidental removal of live infrastructure | high | mitigate | Call-site scan before deletion; pruned-residue test asserts load-bearing symbols survive; all gates re-run. |
| T-01-34 | Tampering | locale parity break | medium | mitigate | Remove dead keys identically across four bundles; bundle-parity/missing-key gates enforce. |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` and `tsc -b` all green after the sweep.
- Spot-check: no live import/route/event/locale/CSS reference to a §15.3-pruned feature remains.
</verification>

<success_criteria>
No unreferenced pruned-feature code/exports/assets/locales/CSS remain in `app/src`; load-bearing infrastructure preserved; all gates green.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-10-SUMMARY.md` when done.
</output>
