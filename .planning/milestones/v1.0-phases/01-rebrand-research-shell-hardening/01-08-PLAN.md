---
phase: 01-rebrand-research-shell-hardening
plan: 08
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - app/src/App.tsx
  - app/src/screens/HomeScreen.tsx
  - app/src/screens/PostDetailScreen.tsx
  - app/src/services/engagement.service.ts
  - app/tests/services/rq1-log-coverage.test.mjs
autonomous: true
requirements: [LOG-01, RQ-01]
must_haves:
  truths:
    - "Real participant actions emit the correct §14.1 events at their call sites: app_open + session_end (App.tsx), feed_impression (HomeScreen), post_open/post_close+durationMs/source_click (PostDetailScreen), question_suggestion_click/question_submit + Q/A record + ai_answer_view (PostDetailScreen Ask), save_post/not_interested (engagement)."
    - "Logging is an OBSERVER of existing signals — the exploration detectors keep emitting `CONCEPT_EXPLORED`; logging is added as a side effect, not a new semantic event (CLAUDE.md one-signal rule)."
    - "A fixture event timeline derives every RQ-01 measure (sessions, return days, session length, posts opened, time-on-post, questions asked, suggestion clicks, notification open rate, voluntary revisits) using only whitelisted fields (RQ-01)."
  artifacts:
    - app/tests/services/rq1-log-coverage.test.mjs
  key_links:
    - "PostDetail post_open/post_close ↔ durationMs ↔ time-on-post + revisit measures"
    - "handleAsk split ↔ question_suggestion_click vs question_submit ↔ QuestionAnswerRecord"
---

<objective>
Wire `interactionLog` calls into the real participant call sites so the §14.1 event stream reflects actual behavior, and prove the stream supports every RQ-01 re-engagement measure. Logging is attached as an observer of existing signals (the `CONCEPT_EXPLORED` detectors and engagement calls stay unchanged). Completes the client half of LOG-01 and satisfies RQ-01.

Purpose: A complete, correctly-placed event stream is what makes re-engagement measurable before any personalization exists.
Output: instrumented App/Home/PostDetail/engagement call sites + an RQ-01 coverage test over a fixture timeline.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
New symbols/files created by this plan (exclude from source-drift verification):
- `app/tests/services/rq1-log-coverage.test.mjs` (new test file)
- New logging call sites (no new exported symbols; uses `interactionLog` from Plan 07)
</artifacts_this_phase_produces>

<context>
@.planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md
@app/src/App.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-lifecycle + feed + engagement instrumentation</name>
  <files>app/src/App.tsx, app/src/screens/HomeScreen.tsx, app/src/services/engagement.service.ts</files>
  <read_first>
    - app/src/App.tsx (hydrate effect; Capacitor `appStateChange` listener; where to fire app_open after identity hydration)
    - app/src/screens/HomeScreen.tsx (feed batch render / location.pathname re-read effect — feed_impression boundary)
    - app/src/services/engagement.service.ts (save/like/dismiss call sites — save_post, not_interested)
    - app/src/services/interaction-log.service.ts (record API — from Plan 07)
    - CLAUDE.md § "Always-mounted screens must explicitly re-read" and § "Feed cold start"
  </read_first>
  <action>
    Instrument lifecycle and feed/engagement events via `interactionLog.record` (D-09):
    - `App.tsx`: after `studyContextService.hydrate()` resolves and identity is bound, record `app_open` once per foreground session. Record `session_end` (with `durationMs` since session start) on `pagehide` and on Capacitor `appStateChange` → inactive/background (reuse the existing appStateChange listener; do not add a second lifecycle source). Guard so `app_open` fires only when identity is bound (no events before setup).
    - `HomeScreen.tsx`: record `feed_impression` when a feed batch becomes visible at the render/batch boundary — no display position, no ranking context (D-08). Do not fork the existing feed refresh logic.
    - `engagement.service.ts`: at the existing save and dismiss call sites, record `save_post` and `not_interested` with `postId` only. Do not gate any of these by condition (both conditions log identically — research-scope rule).
    Keep all additions as side effects; do not introduce a new event-bus type.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `app_open` fires once per foreground session only when identity is bound; `session_end` carries `durationMs` and reuses the existing appStateChange/pagehide sources.
    - `feed_impression` is recorded at the feed batch boundary with no position/ranking fields.
    - `save_post` and `not_interested` are recorded at the engagement call sites with `postId` only; no condition gating.
    - `tsc -b` and `lint` pass.
  </acceptance_criteria>
  <done>Lifecycle, feed-exposure, and save/dismiss events are emitted from live call sites without forking existing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: PostDetail post/source/Q&A instrumentation (observer of existing detectors)</name>
  <files>app/src/screens/PostDetailScreen.tsx</files>
  <read_first>
    - app/src/screens/PostDetailScreen.tsx (resolved-post effect + unmount cleanup for post_open/post_close; `handleAsk`; source/link click; final answer render; the CONCEPT_EXPLORED detectors A/B/C)
    - app/src/services/interaction-log.service.ts (record, recordQuestionSubmit, recordAnswerViewed)
    - CLAUDE.md § "PostDetail exploration detectors" (extend, do not fork; one signal per semantic event)
  </read_first>
  <action>
    Instrument PostDetail as an OBSERVER (D-07/D-09):
    - `post_open` when the post resolves; `post_close` with `durationMs` (time on post) on unmount/route-away — reuse the existing lifecycle points that already drive the detectors, do not add new event types alongside `CONCEPT_EXPLORED`.
    - `source_click` when the participant opens the original source/link (no `sourceUrl` field — D-08).
    - Split `handleAsk` by source: a tapped suggested question records `question_suggestion_click` then `interactionLog.recordQuestionSubmit(...)`; a typed question records `question_submit` then `recordQuestionSubmit(...)`. Question TEXT goes only through `recordQuestionSubmit` (QuestionAnswerRecord), never as event payload.
    - When the final answer finishes rendering (observe once, not per stream token), call `interactionLog.recordAnswerViewed({ questionId, answerText })` (revision 2) and record an `ai_answer_view` event.
    Preserve the touchmove/preventDefault and back-swipe invariants (CLAUDE.md) — do not alter gesture handling.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `post_open`/`post_close` reuse existing PostDetail lifecycle points; `post_close` carries `durationMs`; no new event-bus type is added for exploration.
    - Suggested vs typed Ask produce `question_suggestion_click` vs `question_submit` respectively, plus a revisioned QuestionAnswerRecord; no question text appears in an event row.
    - `ai_answer_view` + revision-2 answer record fire once on final answer render.
    - Full `npm test` green (no regression in PostDetail concept-route/gesture tests).
  </acceptance_criteria>
  <done>PostDetail emits the post/source/Q&A event set as a side effect of existing signals, with text isolated to Q/A records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RQ-01 coverage test over a fixture event timeline</name>
  <files>app/tests/services/rq1-log-coverage.test.mjs</files>
  <read_first>
    - .planning/phases/01-rebrand-research-shell-hardening/01-RESEARCH.md § "Event Contract and RQ-01 Coverage" (event→measure mapping)
    - app/src/types/research.ts (event/record shapes)
  </read_first>
  <behavior>
    - Given a fixture timeline of whitelisted events/records (multiple sessions across days), pure derivations compute: session count, return-day count, session length (from app_open→session_end durationMs), posts opened (post_open), time-on-post (post_close durationMs), questions asked (question_submit + QuestionAnswerRecord), suggestion clicks (question_suggestion_click), notification open rate (notification_open / notification_received), and voluntary revisits (repeat post_open of the same postId across sessions).
    - The derivations use ONLY whitelisted fields; the test asserts no forbidden field (sourceUrl/route/device/position/payload) is present or required.
  </behavior>
  <action>
    Create `app/tests/services/rq1-log-coverage.test.mjs` with an in-test fixture timeline and small pure reducer helpers (defined in the test) that compute each RQ-01 measure from the event/record stream. Assert each measure is derivable and that the fixture contains no prohibited field. This is a contract test proving the schema supports RQ-01; it does not require a live UI.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/rq1-log-coverage.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every RQ-01 measure listed above is computed from the fixture stream and asserted.
    - The test asserts the stream carries no prohibited field.
    - `npm test` discovers and passes the file.
  </acceptance_criteria>
  <done>The event schema is proven to support every RQ-01 re-engagement measure using only allowed data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI call sites → event stream | Instrumentation must not smuggle prohibited context or fork exploration semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Information disclosure | source_click / feed_impression context | high | mitigate | Record ids/durations only; no sourceUrl/position/route (RQ-01 test asserts no forbidden field). |
| T-01-27 | Tampering | forked exploration event | medium | mitigate | Logging observes existing CONCEPT_EXPLORED/lifecycle signals; no new event-bus type (CLAUDE.md rule). |
| T-01-28 | Information disclosure | question/answer text in events | high | mitigate | Text only via recordQuestionSubmit/recordAnswerViewed; events carry ids only. |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green.
- Manual UAT (device): open the app, browse the feed, open a post, dwell, tap a suggested question and a typed question, view the answer, save and dismiss — then confirm (via the diagnostics screen / export in Plan 09/05) that the corresponding events and one Q/A record appeared with no prohibited fields.
</verification>

<success_criteria>
The live §14.1 event stream is emitted from real call sites without forking existing signals, and the RQ-01 measures are provably derivable; gates green.
</success_criteria>

<output>
Create `.planning/phases/01-rebrand-research-shell-hardening/01-08-SUMMARY.md` when done.
</output>
