---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - tools/content_pipeline/package.json
  - tools/content_pipeline/package-lock.json
  - tools/content_pipeline/README.md
  - tools/content_pipeline/src/cli.ts
  - tools/content_pipeline/src/collect/url-policy.ts
  - tools/content_pipeline/src/collect/fetch-candidate.ts
  - tools/content_pipeline/src/extract/article.ts
  - tools/content_pipeline/src/extract/youtube.ts
  - tools/content_pipeline/src/normalize/candidate.ts
  - tools/content_pipeline/src/dedupe/index.ts
  - tools/content_pipeline/src/quality/index.ts
  - tools/content_pipeline/test/collector.test.mjs
  - tools/content_pipeline/test/dedupe.test.mjs
  - tools/content_pipeline/test/quality.test.mjs
  - tools/content_pipeline/test/fixtures/hostile-page.html
autonomous: true
requirements: [CONT-02]
must_haves:
  truths:
    - "D-01 fixes the pilot topic to AI agents & future work; D-02 enforces an approximately 70% text / 30% YouTube candidate mix; D-03 favors evergreen material; D-04 targets 100–150 operator-curated candidates."
    - "D-09 is implemented as a curated URL-list CLI plus bounded fetch/extraction—never search, scraping API discovery, or participant-runtime collection."
    - "The locked pilot invocation is bounded to 100–150 candidates, while the same stage contracts and CLI can process the RSD final-study range of 400–800 candidates per topic without redesign."
    - "Every redirect destination is revalidated against public HTTP(S) policy and collection is bounded by bytes, MIME, redirects, and time."
    - "Article HTML becomes inert structured text and YouTube extraction is an operator-side transcript adapter; neither path emits active HTML."
    - "Normalization, exact/near dedupe, and mechanical quality scoring are deterministic, resumable, and independently testable."
  artifacts:
    - tools/content_pipeline/src/cli.ts
    - tools/content_pipeline/src/collect/fetch-candidate.ts
    - tools/content_pipeline/src/dedupe/index.ts
  key_links:
    - "operator URL seed file ↔ secure collector ↔ normalized candidate artifacts"
    - "normalized content hash ↔ resumable stage cache and later AI provenance"
---

<objective>
Build the deterministic, secure front half of the offline content pipeline for the locked pilot topic and source mix.

Purpose: Turn a human-curated list of 100–150 URLs into bounded, inert, reproducible candidates without giving the participant app any live acquisition capability.
Output: Pipeline CLI, secure URL collector, article/video extraction adapters, normalization, dedupe, quality filters, and hostile-fixture tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- CLI flags: `collect --topic`, `--seeds`, `--run-dir`, `--max-candidates`, `--max-bytes`, `--timeout-ms`, `--resume`, and `--dry-run` in `tools/content_pipeline/src/cli.ts`.
- Types/symbols: `UrlPolicy`, `CollectionLimits`, `RawCandidate`, `NormalizedCandidate`, `DedupeGroup`, `QualityVerdict`.
- Functions/classes: `assertPublicHttpUrl`, `resolveAndValidateDestination`, `fetchCandidate`, `extractArticle`, `extractYouTubeTranscript`, `normalizeCandidate`, `groupDuplicates`, `scoreMechanicalQuality`.
- Files: `tools/content_pipeline/README.md`, collector/extractor/normalize/dedupe/quality modules, `collector.test.mjs`, `dedupe.test.mjs`, `quality.test.mjs`, hostile HTML fixture.
- Dependency contract: `@mozilla/readability@0.6.0` and `jsdom@29.1.1` are exact direct dependencies in `tools/content_pipeline/package.json` and its lockfile; installation scripts are forbidden and tested.
</artifacts_this_phase_produces>

<context>
@docs/research_system_design.md §8.1–§8.5
@docs/SCOPE.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md D-01–D-04 and D-09
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-RESEARCH.md § Pipeline and § Security Domain
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md § Guardrails
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the seed-list CLI and SSRF-resistant collector</name>
  <files>tools/content_pipeline/README.md, tools/content_pipeline/src/cli.ts, tools/content_pipeline/src/collect/url-policy.ts, tools/content_pipeline/src/collect/fetch-candidate.ts, tools/content_pipeline/test/collector.test.mjs</files>
  <read_first>
    - docs/research_system_design.md §8.2, §8.4, §8.5
    - 02-CONTEXT.md D-01–D-04 and D-09
    - 02-RESEARCH.md § Security Domain SSRF controls
    - tools/content_pipeline/package.json and schemas from Plan 01
  </read_first>
  <action>
    Implement a Node CLI that accepts only an operator-authored JSON/CSV URL list and writes resumable stage artifacts below a resolved `--run-dir`. Default the topic slug to `ai-agents-future-work`; make the locked pilot command reject more than 150 candidates, while a separately explicit/scoped `--max-candidates` safety setting supports up to 800 for later topics without changing stage schemas. Report the text/video mix and evergreen/date signals without silently manufacturing URLs. For each request, allow only `http:`/`https:`, resolve hostnames, reject loopback/private/link-local/multicast/unspecified addresses including alternate numeric encodings, re-resolve and revalidate every redirect, cap redirects/time/bytes, enforce allowed MIME, disable credential-bearing URLs, and use injected DNS/fetch seams for tests. Do not shell interpolate URLs. Store collection metadata from RSD §8.5, normalized source hash, collector version, and safe failure reason; redact query secrets and response bodies from logs. Constrain all output paths with resolved-prefix checks.
  </action>
  <verify><automated>cd tools/content_pipeline && node --test test/collector.test.mjs</automated></verify>
  <acceptance_criteria>
    - Loopback, RFC1918, link-local, IPv6 local, alternate-IP syntax, credential URLs, private redirect targets, oversized bodies, bad MIME, and timeout fixtures fail closed.
    - Public fixture requests succeed through injected adapters and no test reaches the network.
    - The CLI cannot write outside `--run-dir`, does not discover/search sources, and documents D-01–D-04/D-09 operator inputs.
    - A mocked 800-record run uses the same contracts/stable ordering without network calls, while the pilot profile remains capped at 150.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Extract and normalize inert article/video candidates</name>
  <files>tools/content_pipeline/package.json, tools/content_pipeline/package-lock.json, tools/content_pipeline/src/extract/article.ts, tools/content_pipeline/src/extract/youtube.ts, tools/content_pipeline/src/normalize/candidate.ts, tools/content_pipeline/test/collector.test.mjs, tools/content_pipeline/test/fixtures/hostile-page.html</files>
  <read_first>
    - docs/research_system_design.md §8.5 raw fields
    - 02-CONTEXT.md D-02, D-03, D-06, D-07
    - 02-RESEARCH.md § Stored XSS and YouTube extraction constraints
    - tools/content_pipeline/src/collect/fetch-candidate.ts
  </read_first>
  <action>
    Add exact reviewed dependencies `@mozilla/readability@0.6.0` and `jsdom@29.1.1` to the pipeline package and regenerate its lockfile using install scripts disabled; assert the resolved lock entries, importability, and absence of lifecycle install scripts in `collector.test.mjs`. Extract article title/byline/date/plain block text with scripts, styles, event handlers, forms, embedded resources, and active URLs excluded; configure jsdom with no script execution and no resource loading, then run Readability only over the inert document. For YouTube seeds, parse only canonical video IDs and invoke an injected, explicitly configured transcript adapter without shell interpolation; do not auto-download `yt-dlp`. Permit operator-supplied transcript files when the verified binary is unavailable. Normalize Unicode/whitespace/URL/date/language/duration, preserve source attribution, stable block IDs, and full canonical article text/transcript for later D-06/D-07 use. Store plain text/structured blocks only—not source HTML or unsafe Markdown.
  </action>
  <verify><automated>cd tools/content_pipeline && node --test test/collector.test.mjs --test-name-pattern="extract|XSS|YouTube"</automated></verify>
  <acceptance_criteria>
    - Hostile script/event-handler/iframe payloads never execute and never appear as active markup in normalized output.
    - Article and video fixtures produce stable blocks, attribution, hashes, and RSD raw metadata.
    - Transcript extraction failure is explicit/resumable and cannot trigger a participant-runtime fetch.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Add deterministic dedupe and mechanical quality gates</name>
  <files>tools/content_pipeline/src/dedupe/index.ts, tools/content_pipeline/src/quality/index.ts, tools/content_pipeline/test/dedupe.test.mjs, tools/content_pipeline/test/quality.test.mjs</files>
  <read_first>
    - docs/research_system_design.md §8.6–§8.7
    - 02-CONTEXT.md D-03 and D-04
    - 02-AI-SPEC.md § Evidence and viewpoint integrity
    - app/src/lib/text-normalization.ts (pure-helper precedent only)
  </read_first>
  <action>
    Implement deterministic canonical-URL/content-hash exact dedupe plus bounded token-shingle similarity for near-duplicate grouping; keep all candidate IDs and designate a review representative without deleting evidence. Implement mechanical quality signals for missing/short extraction, unstable/login URL, language, date staleness, source/stance concentration inputs, transcript availability, and topic-keyword relevance. These scores may reject obvious mechanical failures or prioritize review but may never approve content. Preserve stable ordering independent of async collection completion.
  </action>
  <verify><automated>cd tools/content_pipeline && node --test test/dedupe.test.mjs test/quality.test.mjs</automated></verify>
  <acceptance_criteria>
    - Exact and near-duplicate fixtures group deterministically with no order dependence.
    - Evergreen and dated/promotional/too-short fixtures expose reason-coded quality signals.
    - No score or dedupe result sets final human approval.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Operator URLs and hostile remote documents cross into a local privileged collector; subprocess adapters and npm packages cross into the operator workstation.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-02-T01 | Spoofing | collector DNS/redirect boundary | HIGH | mitigate | Public HTTP(S) only with pre-connect and per-redirect IP validation plus caps | hostile DNS/redirect/IP fixtures in `collector.test.mjs` |
| P02-02-T02 | Elevation of Privilege | article extraction DOM | HIGH | mitigate | jsdom scripts/resources disabled; Readability emits inert text blocks only | hostile-page execution/markup assertions |
| P02-02-T03 | Tampering | run directory/subprocess arguments | HIGH | mitigate | resolved-root checks and fixed argument arrays without shell interpolation | traversal/metacharacter fixtures |
| P02-02-T04 | Information Disclosure | collector logs/artifacts | HIGH | mitigate | environment-only credentials, URL/error redaction, no response bodies | secret-pattern log/artifact scans |
| P02-02-T05 | Tampering | parser/transcript dependencies | HIGH | mitigate | exact Readability/jsdom lock pins, lifecycle scripts disabled, no yt-dlp download | lockfile/import/no-install-script assertions |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- `cd tools/content_pipeline && npm test`
- Run `collect --dry-run` against a fixture seed list and confirm zero network/process calls.
</verification>
<success_criteria>The operator can reproducibly turn the D-01–D-04/D-09 URL list into safe normalized candidates, with SSRF/XSS/path/secret controls proven by tests.</success_criteria>
<output>Create `02-02-SUMMARY.md` when complete.</output>
