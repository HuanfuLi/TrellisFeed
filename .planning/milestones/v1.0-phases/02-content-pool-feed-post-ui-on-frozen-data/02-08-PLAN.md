---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 08
type: execute
wave: 2
depends_on: [02-05]
files_modified:
  - app/src/services/db.service.ts
  - app/src/services/post-qa.service.ts
  - app/src/services/ai-observability.service.ts
  - app/src/services/interaction-log.service.ts
  - app/src/services/upload-queue.service.ts
  - app/src/types/research.ts
  - app/src/screens/PostDetailScreen.tsx
  - app/tests/services/post-qa.service.test.mjs
  - app/tests/services/post-qa.condition-parity.test.mjs
  - app/tests/services/interaction-log.service.test.mjs
  - app/tests/services/upload-queue.service.test.mjs
  - research-backend/migrations/0002_canonical_qa.sql
  - research-backend/src/validation.ts
  - research-backend/src/worker.ts
  - research-backend/src/export.ts
  - research-backend/test/validation.test.mjs
  - research-backend/test/ingest.test.mjs
  - research-backend/test/export.test.mjs
autonomous: true
requirements: [ASK-01]
must_haves:
  truths:
    - "Both conditions use one identical typed/suggested Ask path; condition is recorded only at canonical persistence/logging boundaries and never changes prompt, grounding, provider/model, retry, or answer behavior."
    - "The untouched raw question reaches the load-bearing RAW-ARGMAX malicious gate before context, provider, or persistence; malicious input causes deterministic refusal, zero provider call, and zero raw write."
    - "Grounding uses only checksum-approved blocks/claims/wrapper and prior complete turns from the current user/current post; no live retrieval, cross-post history, or runtime summarization exists."
    - "Canonical RSD UserQuestion and AIAnswer rows persist through `dbQuery` only after allowed branches/normal completion and hydrate across restart; derived upload DTOs never replace canonical storage."
    - "Streaming partial/abnormal responses are never canonical answers; fixed budgets and one 60% context-length retry follow AI-SPEC."
    - "Backend ingest is field-allowlisted, monotonic-revision/idempotent, server-owned for identity context, and exports required canonical Q&A fields without creating a second uploader."
  artifacts:
    - app/src/services/post-qa.service.ts
    - app/tests/services/post-qa.service.test.mjs
    - research-backend/migrations/0002_canonical_qa.sql
  key_links:
    - "PostDetail typed/suggested submit ↔ postQaService ↔ raw filter ↔ same-post grounding ↔ shared provider adapter"
    - "canonical UserQuestion/AIAnswer DB rows ↔ revisioned upload projection ↔ backend export"
---

<objective>
Implement condition-neutral, post-bounded Ask with canonical local persistence and research-backend projection.

Purpose: Give both study groups identical contextual Q&A while preserving the security-critical filter, frozen-post grounding, durable research traces, and upload/export guarantees.
Output: Canonical Q&A coordinator/stores, PostDetail integration, revisioned transport/backend schema, and malicious/parity/persistence tests.
</objective>
<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Types: `AskPostQuestionInput`, `PostAnswer`, `GroundingBlock`, `PostQaThread`, canonical Q&A upload revision fields.
- Functions/service methods: `askPostQuestion`, `selectApprovedGrounding`, `loadSamePostThread`, `hydratePostQa`, `persistCompletedAnswer`, `projectQuestionAnswerUpload`, and allowlisted `recordAiOperationMetadata`.
- Database tables/stores: `user_questions`, `ai_answers` (app DB) and backend canonical Q&A migration tables/columns in `0002_canonical_qa.sql`.
- Files: `app/src/services/post-qa.service.ts`, two post-Q&A test files, backend migration, updated validation/worker/export tests.
</artifacts_this_phase_produces>

<context>
@docs/research_system_design.md §6.6, §7.6, §9.6–§9.7, §17.3
@CLAUDE.md § Heavy stores, § Question filter, § ChatInput, § Events
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md (entire online Q&A contract)
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-PATTERNS.md § Canonical Q&A orchestration
@app/src/services/question-filter.service.ts
@app/src/services/post-context-qa.service.ts
@app/src/providers/llm/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add canonical Q&A stores and same-post thread hydration</name>
  <files>app/src/services/db.service.ts, app/src/services/post-qa.service.ts, app/tests/services/post-qa.service.test.mjs</files>
  <read_first>
    - app/src/services/db.service.ts after Plan 05
    - app/src/domain/content.types.ts `UserQuestion` and `AIAnswer`
    - app/src/services/content-pool.repository.ts
    - CLAUDE.md § Heavy stores
  </read_first>
  <action>
    Add `user_questions` and `ai_answers` to shared DDL/table lists/version upgrade/clear-all with identical backend SQL support. Implement DB-seam repositories/hydration for exact RSD records. Load a thread only by current `(userId, postId)` and join complete question/answer pairs in timestamp order; reject cross-user/cross-post/dangling/partial rows from prompt context. Canonical local rows are the source of truth and survive restart; do not write retired localStorage keys or squeeze these records into Phase 1 `QuestionAnswerRecord`. No ORM/schema-push task applies because only local DDL/IndexedDB and raw backend SQL are scoped.
  </action>
  <verify><automated>cd app && node --test tests/services/post-qa.service.test.mjs --test-name-pattern="hydrate|thread|persistence"</automated></verify>
  <acceptance_criteria>
    - DB-query assertions prove canonical rows persist/reload in both backend modes.
    - Cross-post/user and incomplete pairs never enter a thread.
    - Reset clears both stores and no direct heavy localStorage write exists.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement raw-gated, frozen-post-grounded Ask and condition parity</name>
  <files>app/src/services/post-qa.service.ts, app/src/services/ai-observability.service.ts, app/src/screens/PostDetailScreen.tsx, app/tests/services/post-qa.service.test.mjs, app/tests/services/post-qa.condition-parity.test.mjs</files>
  <read_first>
    - CLAUDE.md § Question filter RAW-ARGMAX gate
    - app/src/services/question.service.ts (pre-gate order)
    - app/src/services/question-filter.service.ts (do not change algorithm)
    - app/src/services/post-context-qa.service.ts and app/src/providers/llm/index.ts
    - 02-AI-SPEC.md § Implementation Guidance, Context Window, Online Guardrails
  </read_first>
  <action>
    Create one coordinator called by typed and suggested submissions. Pass untouched raw text to the existing filter first: malicious returns the deterministic refusal with no provider call and no raw/canonical/log/upload persistence; benign off-topic uses the existing gentle redirect condition-neutrally. For on-topic input, require a checksum-ready current post, select deterministic stable blocks only from that post (wrapper/claims first, locally matching source blocks second, complete latest same-post turns third), use 12k source/2k thread/1k instruction budgets and truncate only on boundaries. Freshly delimiter-wrap untrusted source/thread/question data and call only `providers/llm/index.ts` with the configured main provider/model, `maxTokens: 800`, locale directive, AbortSignal, no tools/search/retrieval. Stream visible prose in memory, persist only normal complete response, honor refusal/safety/empty/abnormal termination, and retry one context-length failure at 60% budgets. Persist exact source (`typed`/`suggested_question`) and suggestion ID. `studyCondition` is never a behavior input. Add an injected/disabled-by-default AI operation observer compatible with Plan 03's local Phoenix adapter and restricted to request ID, post ID, pool/prompt/schema/model versions, filter outcome, selected block IDs, stop reason, token counts, latency, and persistence outcome—never raw prompts/answers, user ID, condition, credentials, or hidden reasoning. Integrate PostDetail/ChatInput without breaking minWidth, follow-up exploration event, or duplicate submit/answer-view events.
  </action>
  <verify><automated>cd app && node --test tests/services/post-qa.service.test.mjs tests/services/post-qa.condition-parity.test.mjs</automated></verify>
  <acceptance_criteria>
    - Malicious/buried-payload fixtures prove raw-only filter order and zero call/write; off-topic redirects; supported/insufficient/cross-post/injection cases are bounded correctly.
    - Paired conditions have identical feed-independent request envelope, selected blocks/thread, provider/model/options/retries, fake answer, and local rows except the recorded condition field.
    - Partial/aborted/error responses are not canonical; completed typed/suggested answers reload after restart.
    - Observability allowlist tests reject identity, condition, raw question/answer/source, credentials, arbitrary payload, and hidden-reasoning fields.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Project canonical Q&A through the existing upload/backend/export seam</name>
  <files>app/src/types/research.ts, app/src/services/interaction-log.service.ts, app/src/services/upload-queue.service.ts, app/tests/services/interaction-log.service.test.mjs, app/tests/services/upload-queue.service.test.mjs, research-backend/migrations/0002_canonical_qa.sql, research-backend/src/validation.ts, research-backend/src/worker.ts, research-backend/src/export.ts, research-backend/test/validation.test.mjs, research-backend/test/ingest.test.mjs, research-backend/test/export.test.mjs</files>
  <read_first>
    - app/src/types/research.ts and Phase 1 QuestionAnswerRecord
    - app/src/services/interaction-log.service.ts and upload-queue.service.ts
    - research-backend/migrations/0001_init.sql
    - research-backend/src/validation.ts, worker.ts, export.ts and tests
    - 02-PATTERNS.md § Interaction and upload evolution
  </read_first>
  <action>
    Derive a strict revisioned transport record from canonical rows after local persistence, retaining canonical question/answer IDs, post/topic, typed/suggested source and suggestedQuestionId, timestamps, model, citations/concepts/claims, and monotonic revision. Preserve upload queue bounds (100 records/256KiB), retry triggers, at-least-once behavior, and one uploader/endpoint. Keep arbitrary payload out of `UserInteractionEvent`; record allowlisted `question_submit` and `ai_answer_view` once using identity from study context. Add backend SQL migration/allowlisted validation/idempotent monotonic revision ingest and CSV export columns; server owns/validates study identity context and refuses unknown/oversized fields. Do not log raw malicious input or export hidden reasoning/telemetry.
  </action>
  <verify><automated>cd app && node --test tests/services/interaction-log.service.test.mjs tests/services/upload-queue.service.test.mjs; cd ../research-backend && npm test</automated></verify>
  <acceptance_criteria>
    - Canonical local persistence precedes enqueue; duplicate/reordered revisions converge idempotently.
    - Backend rejects unknown, oversized, identity-conflicting, or malformed records and exports all required canonical fields.
    - Existing queue limits/retries and event privacy invariants remain green; no second endpoint/uploader exists.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Participant text and stored source cross into the provider; canonical local research data crosses into the upload/backend boundary.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-08-T01 | Elevation of Privilege | Ask filter/prompt | HIGH | mitigate | RAW-ARGMAX first; delimit untrusted data; no tools/write on malicious | buried-payload and branch spies |
| P02-08-T02 | Information Disclosure | thread/condition boundary | HIGH | mitigate | ready current-post checks, same-post query, no condition behavior input | cross-post/parity tests |
| P02-08-T03 | Tampering | streamed answer persistence | HIGH | mitigate | termination/refusal checks and persist after normal completion | abort/refusal/context tests |
| P02-08-T04 | Information Disclosure | logs/upload DTOs | HIGH | mitigate | shared adapter, allowlists, no malicious raw/hidden reasoning | log/queue/backend assertions |
| P02-08-T05 | Tampering | backend ingest | HIGH | mitigate | strict allowlist, bounds, server identity, monotonic idempotent revisions | replay/conflict tests |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- `cd app && node --test tests/services/post-qa.service.test.mjs tests/services/post-qa.condition-parity.test.mjs tests/services/interaction-log.service.test.mjs tests/services/upload-queue.service.test.mjs`
- `cd research-backend && npm test`
- `npm --prefix evals/phase-2 ci --ignore-scripts && npm --prefix evals/phase-2 exec -- promptfoo eval -c promptfooconfig.yaml --no-cache`
- `cd app && npm run lint && npm run build`
</verification>
<success_criteria>Typed and suggested Ask work identically in both conditions, stay strictly within the current frozen post, preserve the raw malicious boundary, and durably/exportably persist exact RSD UserQuestion/AIAnswer records.</success_criteria>
<output>Create `02-08-SUMMARY.md` when complete.</output>
