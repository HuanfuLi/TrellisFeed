---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/src/domain/content.types.ts
  - app/tests/fixtures/content-pool/minimal-valid-pool.json
  - app/tests/services/content-pool.schema.test.mjs
  - app/tests/services/content-pool.import.test.mjs
  - app/tests/services/frozen-feed.service.test.mjs
  - app/tests/services/suggested-questions.test.mjs
  - app/tests/services/post-qa.service.test.mjs
  - app/tests/services/post-qa.condition-parity.test.mjs
  - app/tests/screens/HomeScreen.frozen-feed.test.mjs
  - app/tests/screens/PostDetailScreen.frozen-content.test.mjs
  - app/tests/screens/PostDetailScreen.suggested-questions.test.mjs
  - app/tests/components/FeedCard.test.mjs
  - app/tests/components/OriginalContent.test.mjs
  - app/tests/phase2/frozen-cutover.test.mjs
  - app/tests/native/phase-2-content-pool-uat.md
  - research-backend/test/canonical-qa.contract.test.mjs
  - tools/content_pipeline/package.json
  - tools/content_pipeline/package-lock.json
  - tools/content_pipeline/schemas/topic.schema.json
  - tools/content_pipeline/schemas/post.schema.json
  - tools/content_pipeline/schemas/concept.schema.json
  - tools/content_pipeline/schemas/claim.schema.json
  - tools/content_pipeline/schemas/suggested-question.schema.json
  - tools/content_pipeline/schemas/user-question.schema.json
  - tools/content_pipeline/schemas/ai-answer.schema.json
  - tools/content_pipeline/schemas/recommendation.schema.json
  - tools/content_pipeline/schemas/user-concept-state.schema.json
  - tools/content_pipeline/schemas/frozen-pool.schema.json
  - tools/content_pipeline/src/schema/validate.ts
  - tools/content_pipeline/test/schema.test.mjs
  - tools/content_pipeline/test/collector.test.mjs
  - tools/content_pipeline/test/dedupe.test.mjs
  - tools/content_pipeline/test/quality.test.mjs
  - tools/content_pipeline/test/codex-gate.test.mjs
  - tools/content_pipeline/test/ai-preprocess.test.mjs
  - tools/content_pipeline/test/review.test.mjs
  - tools/content_pipeline/test/freeze.test.mjs
autonomous: true
requirements: [CONT-01]
must_haves:
  truths:
    - "Topic, Post, Concept, Claim, SuggestedQuestion, UserQuestion, AIAnswer, Recommendation, and UserConceptState match the canonical RSD fields, optionality, enums, and numeric ranges exactly."
    - "Runtime types and strict JSON Schemas describe the same records; pipeline-only source assets, review state, and provenance never become extra fields on RSD domain records."
    - "Malformed fields, forbidden extras, invalid suggestion targets, and dangling cross-record references fail executable validation before any later import or freeze work."
    - "Wave 0 provides cross-platform Node test commands and fake IndexedDB support without weakening pre-existing assertions."
    - "D-01 through D-12 remain downstream workflow decisions; this contract does not encode a topic, condition, provider, review verdict, or mutable artifact into the canonical domain types."
  artifacts:
    - app/src/domain/content.types.ts
    - tools/content_pipeline/schemas/frozen-pool.schema.json
    - tools/content_pipeline/src/schema/validate.ts
    - app/tests/services/content-pool.schema.test.mjs
  key_links:
    - "RSD §9 and §10.5 ↔ TypeScript domain types ↔ strict JSON Schemas"
    - "FrozenPoolBundle validator ↔ all record schemas and referential-integrity checks"
---

<objective>
Establish the exact domain and validation contract for Phase 2 and close the Nyquist Wave 0 test-infrastructure gaps that every pipeline and runtime plan depends on.

Purpose: Prevent schema drift and false-green persistence tests before either side of the frozen-pool boundary is built.
Output: Exact RSD TypeScript types, strict JSON Schemas, whole-bundle validation, a minimal valid fixture, and cross-platform executable test infrastructure.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Types: `Topic`, `Post`, `Concept`, `Claim`, `SuggestedQuestion`, `UserQuestion`, `AIAnswer`, `Recommendation`, `UserConceptState`, `OriginalContentAsset`, `FrozenPoolManifest`, `FrozenPoolBundle` in `app/src/domain/content.types.ts`.
- Schemas: `topic.schema.json`, `post.schema.json`, `concept.schema.json`, `claim.schema.json`, `suggested-question.schema.json`, `user-question.schema.json`, `ai-answer.schema.json`, `recommendation.schema.json`, `user-concept-state.schema.json`, and aggregate `frozen-pool.schema.json`.
- Functions: `validateDomainRecord`, `validateFrozenPoolBundle`, and `assertFrozenPoolReferences` in `tools/content_pipeline/src/schema/validate.ts`.
- Package/scripts: `tools/content_pipeline/package.json` with `test`; cross-platform `app` `test:main`; development dependencies `ajv` and `fake-indexeddb` with lockfile entries.
- Wave-0 scaffolds: executable contract/fixture harnesses for pipeline collection/dedupe/quality/Codex/freeze, app import/feed/suggestions/Q&A/screens, backend canonical-Q&A transport, and the native UAT checklist. Each scaffold runs security/behavior fixtures against a minimal injected contract fake before production exists; the owning task switches it to the real seam before implementation. No skipped/TODO tests, source-reading, or empty smoke placeholders.
</artifacts_this_phase_produces>

<context>
@docs/research_system_design.md §9 and §10.5
@docs/SCOPE.md
@CLAUDE.md § Heavy stores and § Question filter
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-RESEARCH.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-PATTERNS.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the cross-platform test baseline and install validation fixtures</name>
  <files>app/package.json, app/package-lock.json, tools/content_pipeline/package.json, tools/content_pipeline/package-lock.json, tools/content_pipeline/test/collector.test.mjs, tools/content_pipeline/test/dedupe.test.mjs, tools/content_pipeline/test/quality.test.mjs, tools/content_pipeline/test/codex-gate.test.mjs, tools/content_pipeline/test/ai-preprocess.test.mjs, tools/content_pipeline/test/review.test.mjs, tools/content_pipeline/test/freeze.test.mjs, app/tests/services/content-pool.import.test.mjs, app/tests/services/frozen-feed.service.test.mjs, app/tests/services/suggested-questions.test.mjs, app/tests/services/post-qa.service.test.mjs, app/tests/services/post-qa.condition-parity.test.mjs, app/tests/components/FeedCard.test.mjs, app/tests/components/OriginalContent.test.mjs, app/tests/screens/HomeScreen.frozen-feed.test.mjs, app/tests/screens/PostDetailScreen.frozen-content.test.mjs, app/tests/screens/PostDetailScreen.suggested-questions.test.mjs, app/tests/phase2/frozen-cutover.test.mjs, research-backend/test/canonical-qa.contract.test.mjs, app/tests/native/phase-2-content-pool-uat.md</files>
  <read_first>
    - app/package.json (current POSIX-only `find` command)
    - app/tests/services/anchor-persistence.test.mjs (dbQuery durability precedent)
    - .planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-VALIDATION.md (Wave 0 requirements)
    - tools/content_pipeline directory state (must be absent or Phase-2-only)
  </read_first>
  <action>
    Replace the POSIX command substitution in `app`'s `test:main` with a checked-in cross-platform Node invocation that discovers `tests/**/*.test.mjs` deterministically on Windows and Unix; do not filter out, rename, or weaken the six currently failing tests. Re-run the baseline, classify any Phase-1-in-flight failures in the summary, and only repair a failure here if it is caused by test discovery itself. Add `fake-indexeddb` as an app dev dependency. Create the isolated `tools/content_pipeline` Node/TypeScript package with `node --test test/*.test.mjs`, Ajv 2020 strict validation, and pinned lockfile; this operator package must never be imported by `app/src`. Perform a lockfile/license/source review and record resolved versions; no auto-downloaded binaries or install scripts are allowed. Create every Wave-0 scaffold named in `02-VALIDATION.md`: each defines the intended injection seam and runs representative secure-behavior fixtures against a minimal contract fake so Wave 0 is green (never `skip`, `todo`, source-reading, or empty). Add the backend contract scaffold and native checklist. Each downstream task first switches its scaffold to the production module, observes the expected red failure, then implements to green; tests are modified in-place rather than deferred.
  </action>
  <verify>
    <automated>cd app && npm test -- --test-name-pattern="content pool schema|storage"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` contains no shell `find`, glob expansion, or command substitution and discovers tests on PowerShell and Unix.
    - `fake-indexeddb` is pinned in `app/package-lock.json`; Ajv is pinned in the pipeline lockfile.
    - The pipeline package has no dependency on the participant app and no package install script downloads executables.
    - Any pre-existing failure remains visible and is not skipped or assertion-weakened.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define exact RSD domain types and strict record schemas</name>
  <files>app/src/domain/content.types.ts, tools/content_pipeline/schemas/topic.schema.json, tools/content_pipeline/schemas/post.schema.json, tools/content_pipeline/schemas/concept.schema.json, tools/content_pipeline/schemas/claim.schema.json, tools/content_pipeline/schemas/suggested-question.schema.json, tools/content_pipeline/schemas/user-question.schema.json, tools/content_pipeline/schemas/ai-answer.schema.json, tools/content_pipeline/schemas/recommendation.schema.json, tools/content_pipeline/schemas/user-concept-state.schema.json</files>
  <read_first>
    - docs/research_system_design.md §9.1–§9.9 and §10.5 (canonical field lists)
    - app/src/types/research.ts (plain interface style and Phase 1 privacy narrowing)
    - .planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-PATTERNS.md § Exact domain contract
    - .planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md § Structured Outputs
  </read_first>
  <action>
    Create plain exported TypeScript types matching the RSD field-for-field, including `UserQuestion.source: "typed" | "suggested_question"`, the exact `Post.status`, source-platform, viewpoint, suggestion-type, recommendation-strategy enums, and `UserConceptState` fields from §10.5. Do not add source body, transcript, review, checksum, candidate, or AI-run fields to those records; define separate `OriginalContentAsset` and manifest/bundle types for the transport boundary. Mirror each domain record with strict draft-2020-12 JSON Schema: `additionalProperties: false`, required keys exactly matching non-optional TypeScript keys, ranges for all 0–1 scores, and explicit formats/length bounds. Preserve Phase 1's privacy-narrowed `UserInteractionEvent`; do not redefine or broaden it here. CONT-01 is a schema contract only—no graph behavior or ranking logic is implemented.
  </action>
  <verify>
    <automated>cd tools/content_pipeline && npm test -- test/schema.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every CONT-01 entity exists and matches its RSD keys, optionality, and enums.
    - Pipeline/review/source-asset convenience fields are absent from canonical RSD types.
    - Strict schemas reject one extra key, one invalid enum, one missing required field, and one out-of-range score per relevant record.
    - `npm run build` type-checks all new exported types.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aggregate bundle and reference validation tests</name>
  <files>tools/content_pipeline/schemas/frozen-pool.schema.json, tools/content_pipeline/src/schema/validate.ts, tools/content_pipeline/test/schema.test.mjs, app/tests/fixtures/content-pool/minimal-valid-pool.json, app/tests/services/content-pool.schema.test.mjs</files>
  <read_first>
    - tools/content_pipeline/schemas/*.json (Task 2 schemas)
    - docs/research_system_design.md §8.8 (artifact layout and manifest metadata)
    - app/tests/services/filter-classifier.eval.test.mjs (fixture-loop style)
    - app/tests/services/anchor-persistence.test.mjs (executable seam assertions)
  </read_first>
  <action>
    Define `FrozenPoolManifest` and aggregate bundle validation with fixed artifact filenames, `contentPoolVersion`, generation/preprocessing/review metadata, counts, SHA-256 hashes, deterministic `feedOrderPostIds`, and separate source-asset descriptors keyed by `postId`; artifact JSON must never supply arbitrary filesystem paths. Compile Ajv in strict mode and add explicit cross-record checks: topic ownership, post concept/claim/suggestion targets, claim concept targets, suggestion post/topic/concept/claim targets, feed-order completeness/uniqueness, approved/frozen status, count/hash manifest consistency hooks, and source asset ownership. Create one minimal valid article/video fixture and adversarial fixtures generated in tests. Make the app schema test execute the validator logic through an importable pure module or a runtime-owned equivalent, not source-reading assertions.
  </action>
  <verify>
    <automated>cd tools/content_pipeline && npm test -- test/schema.test.mjs; cd ../../app && node --test tests/services/content-pool.schema.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The valid fixture passes both pipeline and app contract tests.
    - Dangling IDs, duplicate feed IDs, non-frozen posts, unexpected paths, count mismatch, and schema extras fail with stable error paths.
    - Validation performs no network or model calls.
    - CONT-01 has executable evidence in the exact Nyquist file named by `02-VALIDATION.md`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Untrusted JSON/model output crosses into canonical schemas; package registry artifacts cross into the developer toolchain.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-01-T01 | Tampering | bundle schema/path boundary | HIGH | mitigate | Strict schemas, fixed filenames, no artifact-provided paths, cross-reference validation | `schema.test.mjs` rejects traversal, absolute paths, extras, and dangling IDs |
| P02-01-T02 | Elevation of Privilege | participant renderer contract | HIGH | mitigate | Text-only domain fields; no active HTML field; React escaping is required downstream | inert-markup fixture and schema rejection of `html` |
| P02-01-T03 | Tampering | npm dependency boundary | HIGH | mitigate | Minimal exact lockfile pins, source/license review, and no install-time binary download | package/lockfile/no-install-script assertions |
| P02-01-T04 | Tampering | schema/runtime parity | HIGH | mitigate | One canonical strict schema family exercised by pipeline and app | shared gold/adversarial fixture corpus |

Execution gate: every HIGH threat must have `disposition=mitigate` and its automated verification must be green. Any unresolved or unmitigated HIGH threat blocks this plan from execution/completion.
</threat_model>

<verification>
- `cd tools/content_pipeline && npm test`
- `cd app && node --test tests/services/content-pool.schema.test.mjs && npm run lint && npm run build`
</verification>

<success_criteria>
All nine domain entities match RSD exactly, invalid bundles fail closed, Wave 0 test discovery works cross-platform, and downstream plans have executable strict validators.
</success_criteria>

<output>Create `02-01-SUMMARY.md` when complete.</output>
