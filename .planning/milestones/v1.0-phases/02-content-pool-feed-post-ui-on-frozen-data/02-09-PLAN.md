---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 09
type: execute
wave: 4
depends_on: [02-04, 02-07, 02-08]
files_modified:
  - app/package.json
  - app/scripts/package-content-pool.mjs
  - app/src/generated/content-pool-v1/index.ts
  - app/src/generated/content-pool-v1
  - app/src/data/content-pool-bundle.ts
  - app/src/App.tsx
  - app/src/components/InfoFlow.tsx
  - app/src/components/MasonryFeed.tsx
  - app/src/components/SuggestionCard.tsx
  - app/src/services/concept-feed.service.ts
  - app/src/services/post-queue.service.ts
  - app/src/services/infiniteScroll.service.ts
  - app/src/services/style-assignment.ts
  - app/src/services/feed-spread.ts
  - app/src/services/session.service.ts
  - app/src/services/post-context-qa.service.ts
  - app/src/types/index.ts
  - app/tests/native/phase-2-content-pool-uat.md
  - app/tests/phase2/frozen-cutover.test.mjs
  - docs/research_system_design.md
  - docs/SCOPE.md
  - docs/prune_report.md
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, FEED-01, FEED-02, ASK-01]
must_haves:
  truths:
    - "The temporary AI-generated primary feed is fully retired only after all callers use the frozen facade; no half-live daily/derived/queue path, refill event, generated style, on-open essay/image generation, or DailyPost adapter remains."
    - "The two-slot Home/Settings shell, `/posts/:id` overlay, commit-on-release edge swipe, Header portal/in-tree split, root overflow, keyboard resize, and four-locale parity remain load-bearing and green."
    - "All D-01–D-12 decisions are realized and auditable: pilot/topic/mix/evergreen/candidate count (D-01–D-04), embedded canonical originals and bundled version (D-05–D-08), curated URL pipeline/top model/two gates/local review UI (D-09–D-12)."
    - "All six requirements have executable automated evidence and justified manual evidence; no more than three task steps elapse without an automated sample."
    - "The participant app contains no collector/preprocessor/review/freeze code, content-service endpoint, live article/thumbnail fetch, global chat, graph UI, collections, flashcards/SRS, gamification, podcast, social, semantic search, or Phase 3 ranker."
    - "Android and iOS clean-install/restart/offline UAT verifies packaged pool, article/video behavior, Ask parity, and source/event behavior before Phase 2 is accepted."
  artifacts:
    - app/scripts/package-content-pool.mjs
    - app/src/generated/content-pool-v1/index.ts
    - app/tests/phase2/frozen-cutover.test.mjs
    - app/tests/native/phase-2-content-pool-uat.md
  key_links:
    - "all content callers ↔ frozenFeedService; zero callers ↔ generated feed pipeline"
    - "verified content_pool_v1 ↔ Capacitor packaged asset ↔ clean-install ready IndexedDB version"
    - "Phase 2 verification matrix ↔ all six requirements and D-01–D-12"
---

<objective>
Perform the atomic generated-feed retirement, package the real frozen pool, align canonical docs, and run the complete automated/native acceptance matrix.

Purpose: Prove the temporary shell is fully replaced—not bypassed—and hand Phase 3 a clean frozen-feed/ranking insertion boundary with no protocol or platform regressions.
Output: Deleted obsolete generator surfaces/tests, final cutover guard, synced documentation, packaged native builds, and signed UAT evidence.
</objective>
<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Files: deterministic `app/scripts/package-content-pool.mjs`, generated runtime projection `app/src/generated/content-pool-v1/` with static index, production binding in `content-pool-bundle.ts`, cutover test, and native UAT checklist.
- Test symbols: generated-shell forbidden import/symbol inventory, participant-runtime network allowlist, D-01–D-12 artifact audit helper.
- No new participant feature symbol is introduced; this plan deletes obsolete `DailyPost`/generated-style/queue/refill/session symbols after call-site proof and updates canonical documentation to the shipped frozen architecture.
</artifacts_this_phase_produces>

<context>
@AGENTS.md
@CLAUDE.md (all load-bearing sections)
@docs/research_system_design.md
@docs/SCOPE.md
@docs/prune_report.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-RESEARCH.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-VALIDATION.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-UI-SPEC.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind the verified pool, audit every caller, and atomically retire the generated feed shell</name>
  <files>app/package.json, app/scripts/package-content-pool.mjs, app/src/generated/content-pool-v1/index.ts, app/src/generated/content-pool-v1, app/src/data/content-pool-bundle.ts, app/src/App.tsx, app/src/components/InfoFlow.tsx, app/src/components/MasonryFeed.tsx, app/src/components/SuggestionCard.tsx, app/src/services/concept-feed.service.ts, app/src/services/post-queue.service.ts, app/src/services/infiniteScroll.service.ts, app/src/services/style-assignment.ts, app/src/services/feed-spread.ts, app/src/services/session.service.ts, app/src/services/post-context-qa.service.ts, app/src/types/index.ts, app/tests/phase2/frozen-cutover.test.mjs, obsolete generator tests discovered by call-site audit</files>
  <read_first>
    - CLAUDE.md § Concept Feed Generation Pipeline and Feed cold start (must remain exact until deletion)
    - 02-PATTERNS.md § Generator retirement boundary and file classification
    - all `rg` results for `conceptFeedService|postQueueService|DailyPost|PostSnapshot|FEED_REFILL_COMPLETED|style-assignment|generateMorePosts|generatePostEssay|SuggestionCard|InfoFlow|post-context-qa`
    - Phase 2 summaries 02-05 through 02-08
  </read_first>
  <action>
    After 02-04 freeze verification, create a deterministic packaging script that reads only the fixed allowlisted files under `data/content_pool_v1`, revalidates manifest hashes/references, refuses missing/extra/tampered assets, and regenerates `app/src/generated/content-pool-v1/` plus a static `index.ts` projection. Wire `prebuild` to run it, change `content-pool-bundle.ts` from Plan 05's fail-closed adapter to static imports from that generated projection, and prove no filesystem path or remote URL reaches runtime. The cutover/package test must run a production build, inspect `dist` and the post-`npx cap sync` Android/iOS web asset trees, and confirm the manifest plus every manifest-hashed runtime file is present byte-for-byte; it must also prove zero pipeline/review/run-cache/credential files are present.
    First create an executable cutover test/inventory and run `rg` for every reader/writer/import/export of the transitional daily concept → append-only derived list → cyclic queue pipeline, generated `DailyPost`/presentation styles, refill/cold-start events, on-open essay/image/deep-dive generation, and obsolete generated-post session context. Switch any remaining live caller to `frozenFeedService`/`postQaService`, then delete the entire obsolete subsystem and its tests/types/branches in one atomic task; do not temporarily bypass/collapse the three-list pipeline while any caller remains. Retain only provider/filter/DB/event/navigation utilities actually used by the new architecture. The guard test must fail on forbidden generated-shell imports/symbols and on participant imports from `tools/content_pipeline`; it must permit the selected YouTube iframe/source click but reject participant live article/search/thumbnail/content acquisition. Do not preserve generated code as the Phase 3 seam—`frozenFeedService` is that seam.
  </action>
  <verify><automated>cd app &amp;&amp; node --test tests/phase2/frozen-cutover.test.mjs &amp;&amp; npm test</automated></verify>
  <acceptance_criteria>
    - `rg` finds zero live callers/imports of the retired generator/queue/style/refill/generated-session symbols and no orphan export.
    - All obsolete tests are deleted/replaced only after executable frozen-path coverage exists.
    - Participant source imports no pipeline code and cannot live-acquire primary content.
    - Full app tests pass without weakening load-bearing tests.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Align canonical documentation and execute the complete automated release gate</name>
  <files>docs/research_system_design.md, docs/SCOPE.md, docs/prune_report.md, app/tests/phase2/frozen-cutover.test.mjs</files>
  <read_first>
    - generated final implementation and all Phase 2 summaries
    - docs/research_system_design.md §7–§9, §17.1, §17.3
    - docs/SCOPE.md and docs/prune_report.md
    - 02-VALIDATION.md and 02-AI-SPEC.md acceptance gates
  </read_first>
  <action>
    Update only behaviorally stale canonical documentation: describe the shipped fixed filenames/import boundary/feed facade/Q&amp;A boundary, record the generated-shell removal in prune report, and preserve the RSD research contract/framing. Explicitly audit and cite all D-01–D-12, all six requirement IDs, UI-SPEC, AI-SPEC, and no-pruned-feature scope in the cutover test/summary. Run pipeline, the deterministic Phoenix local-safety test, Promptfoo, app, backend, lint, and build gates; scan built participant assets for secrets, operator/review/pipeline modules, remote content endpoints, and active HTML paths. The Phoenix gate must re-prove the Plan 02-03 exact-version/missing/mismatch exit contract, explicit opt-in, forced `127.0.0.1`, telemetry disabled, no hosted/OTLP exporter, and zero network egress using mocks only. Any HIGH threat or Critical eval failure blocks proceeding to native UAT.
  </action>
  <verify><automated>npm --prefix tools/content_pipeline test &amp;&amp; python evals/phase-2/test_phoenix_local.py &amp;&amp; npm --prefix evals/phase-2 ci --ignore-scripts &amp;&amp; npm --prefix evals/phase-2 exec -- promptfoo eval -c promptfooconfig.yaml --no-cache &amp;&amp; npm --prefix app test &amp;&amp; npm --prefix app run lint &amp;&amp; npm --prefix app run build &amp;&amp; npm --prefix research-backend test</automated></verify>
  <acceptance_criteria>
    - All package gates and Critical/High AI thresholds pass; zero HIGH threat is open.
    - Phoenix local-safety tests pass offline, including exact pins, fail-closed diagnostics, loopback/opt-in enforcement, disabled telemetry, and absence of hosted exporter/network egress.
    - Built app contains the verified pool and none of the collector/review credentials/run cache/operator modules.
    - Documentation matches shipped code and does not reframe the system as an AI tutor/learning feed or resurrect §15.3 features.
  </acceptance_criteria>
</task>
<task type="checkpoint:human-verify">
  <name>Task 3: Package and verify Android/iOS clean-install study behavior</name>
  <files>app/tests/native/phase-2-content-pool-uat.md</files>
  <read_first>
    - CLAUDE.md § iOS device build, gestures, Header, root overflow, SwipeTabContainer
    - 02-VALIDATION.md Manual-Only Verifications
    - data/content_pool_v1/manifest.json and freeze verification output
    - app/capacitor.config.ts and native bundle-ID invariants
  </read_first>
  <action>
    Create a signed checklist, run `npm run build` then `npx cap sync`, inspect native diffs, and test clean install/reinstall/restart on Android and iOS (iOS on a macOS/Xcode/device environment). With network disabled from first launch: import the pinned version, browse multiple full article posts, navigate Home/PostDetail/Saved, restart, and confirm identical ready content. Online/offline, exercise selected YouTube playback/progress, origin/referrer error 153/unavailable fallback and exact transcript notice. In both conditions ask identical typed, suggested, follow-up, insufficient-evidence, off-topic, and local fixture-safe malicious cases; compare feed/suggestions/provider configuration/quality and confirm durable same-post recovery without recording malicious raw input. Verify source clicks/events, Header stability, scroll/keyboard/pull/back gestures, four UI locales with English content, and no live primary-content fetch. Record platform/build/pool version, results, evidence, and reviewer; failures block acceptance and must be fixed prospectively without editing `content_pool_v1`.
  </action>
  <verify><automated>cd app &amp;&amp; npm run build &amp;&amp; npx cap sync</automated></verify>
  <acceptance_criteria>
    - Android and iOS checklist rows are signed with installed build and matching pool checksum/version.
    - Offline clean install/restart article experience, online/offline video fallback, and both-condition Ask parity pass.
    - Native bundle IDs/signing, headers, gestures, keyboard, root overflow, navigation, events, and locales show no regression.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
This convergence plan crosses every Phase 2 boundary: collector→review, review→artifact, artifact→IndexedDB, stored content→WebView/model, identity→condition-neutral behavior, and build dependencies→native package.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-09-T01 | Spoofing | offline collector boundary | HIGH | mitigate | public-destination/redirect caps; collector excluded from app | hostile suite/build scan |
| P02-09-T02 | Elevation of Privilege | extraction/rendering | HIGH | mitigate | inert text and React rendering; no raw HTML | hostile pipeline/component tests |
| P02-09-T03 | Elevation of Privilege | preprocessing/Codex/Ask | HIGH | mitigate | no-tool delimited prompts and raw malicious gate | reference set/Q&A spies |
| P02-09-T04 | Spoofing | local review server | HIGH | mitigate | loopback/token/origin/CSRF; excluded from bundle | HTTP suite/build scan |
| P02-09-T05 | Tampering | freeze/package/import | HIGH | mitigate | fixed names, hashes, references, atomic generation/import | tamper suites/package verification |
| P02-09-T06 | Tampering | IndexedDB import | HIGH | mitigate | whole prevalidation and importing-to-ready barrier | interruption/restart/device test |
| P02-09-T07 | Information Disclosure | credentials/telemetry/artifact | HIGH | mitigate | env-only credentials and allowlisted scans | secret/payload tests |
| P02-09-T08 | Information Disclosure | condition boundary | HIGH | mitigate | no condition behavior inputs; exact paired envelopes | parity/native paired UAT |
| P02-09-T09 | Tampering | dependency/build chain | HIGH | mitigate | pinned reviewed lockfiles/checksums, deterministic Phoenix dependency/self-check, local binaries, no auto-download | lockfile/reproducible gates plus `python evals/phase-2/test_phoenix_local.py` |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- Pipeline: `cd tools/content_pipeline && npm test && npm run cli -- freeze --output ../../data/content_pool_v1 --verify-only`
- AI: `python evals/phase-2/test_phoenix_local.py && npm --prefix evals/phase-2 ci --ignore-scripts && npm --prefix evals/phase-2 exec -- promptfoo eval -c promptfooconfig.yaml --no-cache`
- App: `cd app && npm test && npm run lint && npm run build`
- Backend: `cd research-backend && npm test`
- Native: `cd app && npx cap sync`, then signed Android+iOS UAT checklist.
</verification>
<success_criteria>The generated feed shell is absent, the immutable real pool is the only primary-content source, all six requirements and D-01–D-12 pass automated/manual gates, and both native platforms demonstrate offline browsing plus condition-neutral post-scoped Ask.</success_criteria>
<output>Create `02-09-SUMMARY.md` when complete.</output>
