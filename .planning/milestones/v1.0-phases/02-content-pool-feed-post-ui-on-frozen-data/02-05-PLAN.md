---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 05
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - app/src/data/content-pool-bundle.ts
  - app/src/services/db.service.ts
  - app/src/services/content-pool.repository.ts
  - app/src/App.tsx
  - app/tests/services/content-pool.import.test.mjs
autonomous: true
requirements: [CONT-01, CONT-03]
must_haves:
  truths:
    - "The importer is completed against an injected packaged-bundle reader and fixtures before the real pool exists; Plan 09 alone binds D-08's frozen artifact into the production build. No content is fetched remotely."
    - "D-07 exposes only stored canonical article/transcript assets from a checksum-verified `ready` pool version."
    - "Whole-bundle schema/hash/count/reference validation completes before any version can become visible."
    - "Interrupted imports are idempotently cleaned/retried via `importing`→`ready` metadata; fake SQL BEGIN/COMMIT is never treated as cross-store atomicity."
    - "LocalStorageBackend and IndexedDBBackend support the same SQL subset, new stores are cleared by research reset, and durability tests assert through `dbQuery`."
  artifacts:
    - app/src/data/content-pool-bundle.ts
    - app/src/services/content-pool.repository.ts
    - app/tests/services/content-pool.import.test.mjs
  key_links:
    - "packaged manifest/hash ↔ staged IndexedDB rows ↔ ready-version exposure barrier"
    - "App hydration gate ↔ repository hydrate/import completion"
---

<objective>
Build the participant runtime's read-only, versioned frozen-pool import and repository boundary.

Purpose: Ensure a clean install is fully offline and can never expose a partial, corrupt, mismatched, or remotely acquired pool.
Output: Bundled artifact loader, IndexedDB schema/version upgrade, staged repository import, boot hydration, and interruption/tamper tests.
</objective>
<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Types: `ContentPoolImportStatus`, `ContentPoolRepositorySnapshot`.
- Functions/classes: `loadBundledContentPool`, `validateBundledContentPool`, `ContentPoolRepository.hydrate`, `.importBundledVersion`, `.getPost`, `.getTopic`, `.getConcepts`, `.getClaims`, `.getSuggestedQuestions`, `.getOriginalContent`, `.getReadyVersion`.
- Database tables/stores: `content_pool_meta`, `content_pool_topics`, `content_pool_posts`, `content_pool_concepts`, `content_pool_claims`, `content_pool_suggestions`, `content_pool_assets` with an `IDB_VERSION` bump.
- Files: `app/src/data/content-pool-bundle.ts`, `app/src/services/content-pool.repository.ts`, `app/tests/services/content-pool.import.test.mjs`.
- Integration boundary: `content-pool-bundle.ts` exposes an injectable `PackagedPoolReader`; its production default is fail-closed `POOL_NOT_PACKAGED` in this plan, while tests inject the Wave-0 fixture. It must not import nonexistent `data/content_pool_v1` files. Plan 09 binds the real artifact after 02-04.
</artifacts_this_phase_produces>

<context>
@CLAUDE.md § Heavy stores and IndexedDB
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md D-07 and D-08
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-PATTERNS.md § IndexedDB contract and Boot hydration
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-VALIDATION.md
@app/src/services/db.service.ts
@app/src/App.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add parity-safe heavy stores and executable fake IndexedDB coverage</name>
  <files>app/src/services/db.service.ts, app/tests/services/content-pool.import.test.mjs</files>
  <read_first>
    - app/src/services/db.service.ts (SHARED_DDL, TABLE_NAMES, IDB_VERSION, both backends, clearAllTables)
    - CLAUDE.md § Heavy stores live in IndexedDB
    - app/tests/services/anchor-persistence.test.mjs
    - Plan 01 domain/bundle contracts
  </read_first>
  <action>
    Add versioned stores/tables for pool meta and each canonical record/asset family, bump `IDB_VERSION`, register every table in shared DDL/table lists/clear-all, and use only SQL statements handled identically by LocalStorageBackend and IndexedDBBackend. Keep frozen records as version-qualified data and never use localStorage directly. Add fake-indexeddb tests that execute upgrade, insert/query/delete/reset through `dbQuery`/`dbExecute`; do not assert against in-memory mirrors or source strings. Do not add an ORM or schema-push task: scoped changes are local DDL/IndexedDB only and match none of the configured Payload/Prisma/Drizzle/Supabase/TypeORM patterns.
  </action>
  <verify><automated>cd app && node --test tests/services/content-pool.import.test.mjs --test-name-pattern="schema|backend|clear"</automated></verify>
  <acceptance_criteria>
    - Both backends execute every new statement and `clearAllTables()` removes every new heavy store.
    - IndexedDB upgrade and fallback parity are exercised, not source-read.
    - No ORM dependency/config/schema push is introduced.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement whole-bundle validation and staged idempotent import</name>
  <files>app/src/data/content-pool-bundle.ts, app/src/services/content-pool.repository.ts, app/tests/services/content-pool.import.test.mjs</files>
  <read_first>
    - app/src/domain/content.types.ts and minimal-valid-pool fixture
    - tools/content_pipeline/schemas/frozen-pool.schema.json and validator contract
    - 02-RESEARCH.md § Runtime import and acquisition rules
    - app/src/services/post-history.service.ts (hydrate/mirror precedent, not content ownership)
  </read_first>
  <action>
    Implement against an injected `PackagedPoolReader` and Wave-0 fixture only. The production/default reader returns deterministic `POOL_NOT_PACKAGED` until Plan 09; accept no arbitrary URL/path, provide no remote fallback, and do not statically import or assume `data/content_pool_v1` exists in this wave. Validate all strict schemas, fixed filenames, hashes, counts, exact version, IDs, references, feed order, approved/frozen status, and assets in memory before writes. Import under the version namespace: write meta `importing`, delete incomplete rows for that version, insert via the DB seam, re-query counts/hashes, then set `ready`. Hide importing/failed/mismatched versions and refuse mutation of a ready version.
  </action>
  <verify><automated>cd app && node --test tests/services/content-pool.import.test.mjs</automated></verify>
  <acceptance_criteria>
    - Fresh, repeat, interrupted, tampered, checksum/count/reference mismatch, version mismatch, and quota/failure fixtures have deterministic outcomes.
    - No getter returns partial/version-mismatched rows.
    - Network spies observe zero calls during load/import.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Gate participant boot on frozen-pool hydration</name>
  <files>app/src/App.tsx, app/tests/services/content-pool.import.test.mjs</files>
  <read_first>
    - app/src/App.tsx `hydrateAllFromSQLite()` and `setHydrated`
    - app/src/services/content-pool.repository.ts
    - CLAUDE.md navigation/header/root overflow invariants
  </read_first>
  <action>
    Add repository hydration/import to the existing awaited app boot barrier before `setHydrated(true)`, preserving all Phase 1 hydration ordering and the two-slot Home/Settings navigation shell. Present a bounded localized fatal/retry state when the packaged artifact is corrupt rather than rendering partial/empty content as ready. Do not add a body scroll root, route, remote repair, or mutable pool UI.
  </action>
  <verify><automated>cd app && node --test tests/services/content-pool.import.test.mjs && npm run build</automated></verify>
  <acceptance_criteria>
    - First participant render occurs only after a ready pinned pool or explicit fail-closed state.
    - Retry is idempotent; no reload is required after a successful import.
    - App/navigation/header/root overflow invariants remain intact.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Packaged JSON/assets cross into local durable storage and UI-visible synchronous mirrors.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-05-T01 | Tampering | injected bundle reader/validator | HIGH | mitigate | fixed logical filenames, strict schema/hash validation, no artifact paths | tampered/path/version fixtures |
| P02-05-T02 | Tampering | IndexedDB staged import | HIGH | mitigate | prevalidation, version namespace, importing/ready barrier, cleanup/retry | interruption/quota tests |
| P02-05-T03 | Elevation of Privilege | stored source assets | HIGH | mitigate | inert text/blocks only; no active HTML | hostile asset roundtrip |
| P02-05-T04 | Information Disclosure | acquisition boundary | HIGH | mitigate | injected fixture reader and fail-closed default; network deny | fetch spy stays zero |
| P02-05-T05 | Information Disclosure | diagnostics | HIGH | mitigate | shipping allowlist validation and redaction | secret-pattern failure test |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- `cd app && node --test tests/services/content-pool.schema.test.mjs tests/services/content-pool.import.test.mjs`
- `cd app && npm run lint && npm run build`
</verification>
<success_criteria>A clean install validates and imports only the pinned D-08 bundle, exposes it atomically through the DB seam, survives interruption/restart, and performs zero live content acquisition.</success_criteria>
<output>Create `02-05-SUMMARY.md` when complete.</output>
