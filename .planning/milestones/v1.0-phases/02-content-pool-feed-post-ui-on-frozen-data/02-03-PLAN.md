---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - tools/content_pipeline/package.json
  - tools/content_pipeline/package-lock.json
  - tools/content_pipeline/src/cli.ts
  - tools/content_pipeline/src/ai/provider.ts
  - tools/content_pipeline/src/ai/anthropic.ts
  - tools/content_pipeline/src/ai/openai.ts
  - tools/content_pipeline/src/ai/gemini.ts
  - tools/content_pipeline/src/ai/validate.ts
  - tools/content_pipeline/src/preprocess/prompt.ts
  - tools/content_pipeline/src/preprocess/run.ts
  - tools/content_pipeline/src/codex-gate/run.ts
  - tools/content_pipeline/src/codex-gate/schema.json
  - tools/content_pipeline/test/ai-preprocess.test.mjs
  - tools/content_pipeline/test/codex-gate.test.mjs
  - evals/phase-2/package.json
  - evals/phase-2/package-lock.json
  - evals/phase-2/promptfooconfig.yaml
  - evals/phase-2/fixtures/reference-set.json
  - evals/phase-2/phoenix-local.py
  - evals/phase-2/test_phoenix_local.py
  - evals/phase-2/requirements.txt
  - tools/content_pipeline/src/observability/trace.ts
autonomous: true
requirements: [CONT-02]
must_haves:
  truths:
    - "D-10 uses the strongest operator-approved model through provider-neutral structured preprocessing, recording exact model/prompt/schema versions and never silently downgrading permanent wrapper quality."
    - "D-11 gate 1 is a read-only, no-tools, no-network Codex advisory verdict; it can reject or request edits but can never approve a post into the pool."
    - "Stored source text is freshly delimited untrusted data, model termination/refusal is checked before parse, and strict local validation remains authoritative after at most two eligible repair attempts."
    - "The initial 16-example AI reference set and condition-paired Q&A cases run with fake/local providers only in CI."
  artifacts:
    - tools/content_pipeline/src/preprocess/run.ts
    - tools/content_pipeline/src/codex-gate/run.ts
    - evals/phase-2/fixtures/reference-set.json
  key_links:
    - "normalizedContentSha256 + provider/model/prompt/schema versions ↔ resumable preprocessing cache"
    - "schema-valid candidate ↔ Codex advisory verdict ↔ later operator gate"
---

<objective>
Implement the AI preprocessing and advisory Codex gate under the approved AI-SPEC, together with deterministic regression evaluation.

Purpose: Generate permanent wrappers faithfully while making prompt injection, malformed output, hidden fallback, and AI auto-approval structurally impossible.
Output: Provider-neutral structured adapters, validated preprocessing, read-only Codex review, and Phase 2 Promptfoo/reference fixtures.
</objective>
<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Types: `StructuredProvider`, `StructuredRequest`, `StructuredResult`, `PreprocessRunKey`, `CodexGateVerdict`.
- Functions/classes: `deriveProviderSchema`, `runStructuredPreprocess`, `validateCompletedResult`, `buildPreprocessPrompt`, `runCodexGate`.
- CLI commands registered in `tools/content_pipeline/src/cli.ts`: `preprocess --provider`, `--model`, `--prompt-version`, `--schema-version`, `--max-concurrency`, `--spend-limit`, `--resume`; `codex-review --run-dir`, `--codex-command`, `--timeout-ms`. The package guarantees `npm run cli -- ...`, and dispatch tests cover both routes.
- Files: provider adapters, preprocessing prompt/run modules, Codex gate schema/runner/tests, CLI/package wiring, allowlisted tracing, Promptfoo files, a local-only `phoenix-local.py` with pinned `requirements.txt`, executable `test_phoenix_local.py`, and the 16-example reference set.
</artifacts_this_phase_produces>

<context>
@docs/research_system_design.md §8.6, §8.7, §17.1
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md D-10 and D-11
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md (entire contract)
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-RESEARCH.md § AI preprocessing
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build provider-neutral, schema-validated preprocessing</name>
  <files>tools/content_pipeline/package.json, tools/content_pipeline/package-lock.json, tools/content_pipeline/src/cli.ts, tools/content_pipeline/src/ai/provider.ts, tools/content_pipeline/src/ai/anthropic.ts, tools/content_pipeline/src/ai/openai.ts, tools/content_pipeline/src/ai/gemini.ts, tools/content_pipeline/src/ai/validate.ts, tools/content_pipeline/src/preprocess/prompt.ts, tools/content_pipeline/src/preprocess/run.ts, tools/content_pipeline/test/ai-preprocess.test.mjs</files>
  <read_first>
    - 02-AI-SPEC.md §2–§4b (framework, provider contracts, retries, budgets)
    - docs/research_system_design.md §8.6 and §17.1
    - tools/content_pipeline/schemas from Plan 01
    - normalized candidate contract from Plan 02
  </read_first>
  <action>
    First extend the package-owned `cli` script and `src/cli.ts` dispatcher with a tested `preprocess` route and exact argument forwarding; retain the exact collector dependency pins from Plan 02 and regenerate the lockfile locally.
    Implement one normalized structured-result envelope and provider-compatible projections for Anthropic/OpenAI/Gemini, while retaining the full local Ajv schema as authority. Build the RSD preprocessing prompt with source in a fresh random data delimiter, no tools, no network/retrieval, `max_tokens: 4096`, explicit fidelity/uncertainty/reliability rules, and exact required output. Check HTTP/provider refusal, empty output, and stop reason before parsing. Permit at most two repair retries only for malformed/truncated/local-validation failures; never retry safety/auth/schema-compilation failures. Cache by candidate content hash + provider/model/prompt/schema versions, retain stable candidate ordering, bound concurrency, honor Retry-After, and stop before the operator spend ceiling. Log allowlisted metadata only—never source text, credentials, or hidden reasoning. Unsupported local structured output may use prompt schema but cannot relax local validation.
  </action>
  <verify><automated>cd tools/content_pipeline &amp;&amp; node --test test/ai-preprocess.test.mjs</automated></verify>
  <acceptance_criteria>
    - Valid article/video fixtures produce complete RSD wrapper drafts; malformed/refused/truncated/injection fixtures fail or repair within policy.
    - Prompt-injection source text cannot select tools, URL, schema, model, destination, or approval state.
    - Model/prompt/schema/provenance and token/cost metadata are present; secrets/raw source are absent from logs.
    - No live provider runs in tests.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement D-11 read-only Codex gate 1</name>
  <files>tools/content_pipeline/src/cli.ts, tools/content_pipeline/src/codex-gate/run.ts, tools/content_pipeline/src/codex-gate/schema.json, tools/content_pipeline/test/codex-gate.test.mjs</files>
  <read_first>
    - 02-CONTEXT.md D-11
    - 02-AI-SPEC.md § Tool Use and § Pre-Freeze Guardrails
    - CLAUDE.md § Agent Delegation Policy (Codex role)
    - tools/content_pipeline/src/preprocess/run.ts
  </read_first>
  <action>
    Register and test the `codex-review` route in the same CLI dispatcher before implementing its runner.
    Run Codex as an injected subprocess with a fixed executable/argument array, fresh delimited source/candidate data, read-only workspace, no network, no tools/filesystem mutation, timeout/output-size caps, and a strict advisory verdict schema (`advance_to_human | needs_edit | reject`, reason codes, fidelity/reliability notes). Validate exit status/termination/schema and bind verdict to the candidate content hash and preprocessing version. Never translate `advance_to_human` into approval, and never accept a stale verdict after candidate edits. Redact subprocess failures and provide a deterministic fixture adapter for CI.
  </action>
  <verify><automated>cd tools/content_pipeline &amp;&amp; node --test test/codex-gate.test.mjs</automated></verify>
  <acceptance_criteria>
    - Injection fixtures cannot cause a command/tool/network/write action.
    - Invalid, timed-out, oversized, stale-hash, or missing verdicts block human advancement.
    - No Codex result creates `approved`/`frozen` state; D-11 operator gate remains mandatory.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Establish the adjudicated AI evaluation harness</name>
  <files>evals/phase-2/package.json, evals/phase-2/package-lock.json, evals/phase-2/promptfooconfig.yaml, evals/phase-2/fixtures/reference-set.json, evals/phase-2/phoenix-local.py, evals/phase-2/test_phoenix_local.py, evals/phase-2/requirements.txt, tools/content_pipeline/src/observability/trace.ts</files>
  <read_first>
    - 02-AI-SPEC.md §5 Evaluation Strategy and Reference Dataset
    - 02-AI-SPEC.md §7 Production Monitoring privacy boundary
    - app/tests/services/filter-classifier.eval.test.mjs
  </read_first>
  <action>
    Pin Promptfoo in the eval lockfile. Pin `arize-phoenix==17.26.0` and `opentelemetry-sdk==1.43.0` exactly in `evals/phase-2/requirements.txt`; `phoenix-local.py` must never auto-install and must expose a side-effect-free `--check` path backed by injectable version/import/environment seams. Define this observable contract exactly: an exact installed pair exits 0 and prints `PHOENIX_CHECK_OK arize-phoenix=17.26.0 opentelemetry-sdk=1.43.0 host=127.0.0.1 telemetry=disabled exporter=none opt_in=QUESTIONTRACE_PHOENIX_LOCAL`; a missing distribution/import exits 2 and prints `PHOENIX_CHECK_DEPENDENCY_MISSING package=&lt;name&gt; install="python -m pip install --requirement evals/phase-2/requirements.txt"`; a version mismatch exits 3 and prints `PHOENIX_CHECK_VERSION_MISMATCH package=&lt;name&gt; expected=&lt;version&gt; actual=&lt;version&gt; install="python -m pip install --requirement evals/phase-2/requirements.txt"`; and unsafe host/exporter/telemetry environment exits 4 with `PHOENIX_CHECK_UNSAFE_CONFIG reason=&lt;reason&gt;`. Starting without `QUESTIONTRACE_PHOENIX_LOCAL=1` exits 5 with `PHOENIX_START_OPT_IN_REQUIRED set QUESTIONTRACE_PHOENIX_LOCAL=1` before importing/launching Phoenix.
    Add `test_phoenix_local.py` using only Python stdlib test/mocking facilities. Exercise the real `--check` CLI/main and launch decision functions with fake import/version resolvers for exact, absent, and mismatched dependencies; capture the CLI result and assert each specified exit code and complete stdout line exactly (with empty stderr). Use hostile environment fixtures for non-loopback hosts, Phoenix Cloud/collector endpoints, `OTEL_EXPORTER_OTLP_ENDPOINT`, and telemetry enabled, plus a fake `phoenix.launch_app` spy. Assert the enabled path forces `127.0.0.1`, sets `PHOENIX_TELEMETRY_ENABLED=false`, constructs no hosted/OTLP exporter, and performs no outbound connection (network primitives patched to fail). The test must not launch a server or contact/install any external service.
    Create the 16-example reference dataset exactly as specified: eight content/preprocessing cases and eight Ask cases, with allowed block IDs, forbidden claims, provenance, rubric labels, and a candidate Codex advances/operator rejects. Configure Promptfoo with checked-in fixture/custom providers only and paired control/experimental replay assertions. Pin Promptfoo. Add a local Python launcher that binds Phoenix only to loopback with no hosted exporter, plus an injected pipeline tracing adapter that emits only run/request ID, candidate hash/post ID, pool/prompt/schema/model versions, filter outcome, selected block IDs, stop reason, token counts, latency, validation paths, and persistence outcome; raw content, participant IDs/questions/answers, condition, credentials, and hidden reasoning must be structurally absent. Instrument pipeline stage boundaries through this adapter and keep it disabled unless the local operator explicitly enables it. Encode release gates: all Critical cases pass, ≥90% source/Q&amp;A fidelity, ≥85% evidence/suggestion quality, >5-point regression blocks, judge correlation/agreement thresholds leave an uncalibrated judge triage-only.
  </action>
  <verify><automated>python evals/phase-2/test_phoenix_local.py &amp;&amp; npm --prefix evals/phase-2 ci --ignore-scripts &amp;&amp; npm --prefix evals/phase-2 exec -- promptfoo eval -c promptfooconfig.yaml --no-cache</automated></verify>
  <acceptance_criteria>
    - All 16 cases run offline with no live provider/network use.
    - Every Q&amp;A case replays both conditions and compares full request envelopes.
    - No raw participant data, identity, condition, source text, credentials, or hidden reasoning is configured for telemetry/export.
    - Phoenix binds only to loopback, has no hosted exporter, and tracing remains opt-in/metadata-allowlisted.
    - `test_phoenix_local.py` invokes the real CLI/main with injected fixtures and proves the exact exit-code, complete stdout, and empty-stderr contract for installed, missing, mismatched, unsafe-config, and no-opt-in branches without live services or network egress.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Untrusted stored source crosses into top-tier provider and Codex contexts; credentials and model output cross the operator environment.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-03-T01 | Elevation of Privilege | preprocessing/Codex prompts | HIGH | mitigate | random delimiters, no tools/network, fixed schemas, read-only Codex | adversarial fixtures and subprocess spies |
| P02-03-T02 | Information Disclosure | provider/trace boundary | HIGH | mitigate | environment-only keys, allowlisted metadata, redacted errors, no raw telemetry | secret/raw-fixture scans |
| P02-03-T03 | Tampering | structured output | HIGH | mitigate | termination checks, full local validation, bounded repair | refusal/truncation/schema tests |
| P02-03-T04 | Elevation of Privilege | Codex advisory state | HIGH | mitigate | advisory-only state machine with content-hash binding | no approved/frozen transition assertions |
| P02-03-T05 | Tampering | eval/Python dependencies | HIGH | mitigate | exact npm/Python pins, deterministic fail-closed `--check`, explicit opt-in, loopback-only/no-exporter launch | `python evals/phase-2/test_phoenix_local.py` plus lockfile/requirements/provenance and offline eval |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- `cd tools/content_pipeline && npm test`
- `npm --prefix evals/phase-2 ci --ignore-scripts && npm --prefix evals/phase-2 exec -- promptfoo eval -c promptfooconfig.yaml --no-cache`
- `python evals/phase-2/test_phoenix_local.py`
</verification>
<success_criteria>D-10 preprocessing and D-11 Codex gate are schema-valid, injection-resistant, advisory-only, versioned, and covered by the complete offline reference set.</success_criteria>
<output>Create `02-03-SUMMARY.md` when complete.</output>
