---
phase: 02-content-pool-feed-post-ui-on-frozen-data
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - tools/content_pipeline/package.json
  - tools/content_pipeline/package-lock.json
  - tools/content_pipeline/src/cli.ts
  - tools/content_pipeline/src/review/server.ts
  - tools/content_pipeline/src/review/store.ts
  - tools/content_pipeline/src/review/ui/index.html
  - tools/content_pipeline/src/review/ui/review.ts
  - tools/content_pipeline/src/review/ui/styles.css
  - tools/content_pipeline/src/freeze/build.ts
  - tools/content_pipeline/src/freeze/verify.ts
  - tools/content_pipeline/test/review.test.mjs
  - tools/content_pipeline/test/freeze.test.mjs
  - tools/content_pipeline/seeds/ai-agents-future-work.json
  - data/content_pool_v1/manifest.json
  - data/content_pool_v1/topics.json
  - data/content_pool_v1/posts.json
  - data/content_pool_v1/concepts.json
  - data/content_pool_v1/claims.json
  - data/content_pool_v1/suggested_questions.json
  - data/content_pool_v1/post_concept_edges.json
  - data/content_pool_v1/post_claim_edges.json
  - data/content_pool_v1/source_files
  - data/content_pool_v1/review_logs
autonomous: false
requirements: [CONT-02, CONT-03]
must_haves:
  truths:
    - "D-12 provides a tiny loopback-only review page showing wrapper, tags, suggestions, full stored article text or fixed video URL/ID plus derived digest, and current D-11 Codex verdict with approve/reject/edit controls."
    - "D-11 requires a current Codex advisory verdict and an explicit operator decision bound to the edited content hash; only the operator is gate of record."
    - "D-05, D-06, and D-07 preserve full canonical article text, source attribution/link, and a video URL/ID plus approved digest without storing active HTML, transcript, audio, or video."
    - "D-08 exports a bundled immutable `content_pool_v1` with fixed files, checksums, deterministic feed order, and overwrite refusal."
    - "The frozen artifact records D-01 AI agents & future work, D-02 roughly 70/30 text/video, D-03 mostly evergreen selection, and D-04 approximately 100–150 raw candidates yielding approximately 50 explicitly approved posts."
    - "No post freezes without rights/provenance/reliability review and current operator approval."
  artifacts:
    - tools/content_pipeline/src/review/server.ts
    - tools/content_pipeline/src/freeze/build.ts
    - data/content_pool_v1/manifest.json
  key_links:
    - "candidate content hash ↔ Codex verdict ↔ operator decision ↔ frozen record checksum"
    - "fixed manifest filenames/hashes ↔ participant bundle import"
---

<objective>
Complete the human-review and immutable-freeze half of the pipeline, then curate and freeze the actual Phase 2 pilot pool.

Purpose: Make the operator—not AI—the only acceptance gate and produce the versioned, checksum-verifiable artifact consumed by the participant app.
Output: Secure local review UI, separate audit decisions, immutable freezer/verifier, and `data/content_pool_v1/` containing roughly 50 approved AI-agents/future-work posts.
</objective>
<execution_context>
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.agents/gsd-core/templates/summary.md
</execution_context>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Types: `ReviewDecision`, `ReviewSession`, `FreezeInput`, `FreezeAudit`, `RightsReview`.
- Functions/classes: `startReviewServer`, `loadReviewQueue`, `writeReviewDecision`, `buildFrozenPool`, `verifyFrozenPool`, `atomicPromotePool`.
- CLI commands registered in the existing dispatcher: `review --run-dir`, `--host` (loopback only), `--port`, `--open`; `freeze --run-dir`, `--output`, `--version`, `--verify-only`. Package/lock ownership is serialized after 02-03; dispatch tests prove `npm run cli -- review ...` and `npm run cli -- freeze ...` reach these modules without globals.
- Files: local review server/store/UI/tests; freeze builder/verifier/tests; `tools/content_pipeline/seeds/ai-agents-future-work.json`; every fixed file under `data/content_pool_v1/`, including `source_files/` and non-shipping/audit-safe `review_logs/`.
</artifacts_this_phase_produces>

<context>
@docs/research_system_design.md §8.7–§8.8
@docs/SCOPE.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-CONTEXT.md D-01–D-12
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-RESEARCH.md § Review and immutable packaging
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-AI-SPEC.md § Domain Context, Guardrails, Evaluation
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the D-12 local-only, CSRF-safe review application</name>
  <files>tools/content_pipeline/package.json, tools/content_pipeline/package-lock.json, tools/content_pipeline/src/cli.ts, tools/content_pipeline/src/review/server.ts, tools/content_pipeline/src/review/store.ts, tools/content_pipeline/src/review/ui/index.html, tools/content_pipeline/src/review/ui/review.ts, tools/content_pipeline/src/review/ui/styles.css, tools/content_pipeline/test/review.test.mjs</files>
  <read_first>
    - 02-CONTEXT.md D-11 and D-12
    - 02-AI-SPEC.md § Pre-Freeze Guardrails and expert review fields
    - normalized/preprocessed/Codex artifact contracts from Plans 02–03
    - tools/content_pipeline/src/schema/validate.ts
  </read_first>
  <action>
    Extend the existing CLI dispatcher/package script with the `review` route and exact argument forwarding; retain all prior commands and pins, regenerate the lockfile, and add dispatch assertions to `review.test.mjs`.
    Serve only on an OS-verified loopback address with no wildcard CORS, a high-entropy per-run token, strict same-origin/Origin checks, a separate CSRF token on every mutation, small JSON body limits, short session lifetime, and no durable cookie. Render candidate fields as text nodes (never `innerHTML`) and include hook, summaries, concepts/claims, stance/difficulty/scores, suggested questions and targets, full extracted text/transcript, source attribution/link, Codex verdict, source-quality/reliability/bias/misinformation/content-warning fields, and rights/terms review. Approve/reject/needs-edit writes an append-only decision record separate from candidate content with reviewer, timestamp, rubric version, notes, edited content hash, and explicit disposition. Edits invalidate the previous Codex verdict and require rerunning gate 1 before approval. Do not expose the server on LAN or deploy/bundle it.
  </action>
  <verify><automated>cd tools/content_pipeline && node --test test/review.test.mjs</automated></verify>
  <acceptance_criteria>
    - Non-loopback bind, missing/invalid origin-token-CSRF, oversized body, stale hash/verdict, and XSS fixture requests fail.
    - UI covers every D-12 field and records every RSD §8.7 review dimension plus rights review.
    - No Codex state alone satisfies approval.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic immutable freeze and verification</name>
  <files>tools/content_pipeline/src/cli.ts, tools/content_pipeline/src/freeze/build.ts, tools/content_pipeline/src/freeze/verify.ts, tools/content_pipeline/test/freeze.test.mjs</files>
  <read_first>
    - docs/research_system_design.md §8.8
    - 02-CONTEXT.md D-05–D-08 and D-11
    - aggregate schemas and validators from Plan 01
    - review-decision contract from Task 1
  </read_first>
  <action>
    Register the `freeze` route in the same dispatcher, guarantee `npm run cli -- freeze ...`, and test dispatch plus exit-code propagation in `freeze.test.mjs`.
    Project only current explicitly operator-approved candidates with current content-hash-bound Codex verdicts and rights/provenance metadata into exact RSD records plus fixed source assets. Write to a fresh sibling staging directory, derive all filenames internally from validated IDs, reject separators/absolute/`..`, verify schemas/references/counts/feed-order uniqueness/source ownership, compute platform SHA-256 per file, then atomically rename to a destination that must not exist. Never overwrite or edit `content_pool_v1`; corrections require a new version/directory and protocol-impact review. Manifest must include version, generation date, model/prompt/schema/collector versions, raw/approved/rejected counts, source-format and stance distributions, review procedure summary, fixed filenames/hashes, and deterministic non-personal order. Add tests for stale approval, missing gate, path traversal, tamper, overwrite, partial staging cleanup, and byte-identical rerun into a new destination.
  </action>
  <verify><automated>cd tools/content_pipeline && node --test test/freeze.test.mjs</automated></verify>
  <acceptance_criteria>
    - Missing/stale human approval, rights review, source asset, reference, or checksum blocks freeze.
    - Existing destination and all traversal/tamper attempts fail without mutation.
    - Identical approved input produces identical data bytes/checksums and order (except explicitly excluded run timestamp metadata).
  </acceptance_criteria>
</task>
<task type="checkpoint:human-action">
  <name>Task 3: Curate, review, and freeze the actual pilot artifact</name>
  <files>tools/content_pipeline/seeds/ai-agents-future-work.json, data/content_pool_v1</files>
  <read_first>
    - 02-CONTEXT.md D-01–D-12
    - tools/content_pipeline/README.md and all CLI help
    - 02-AI-SPEC.md § Acceptance Gates and expert roles
    - institutional rights/IRB/protocol guidance applicable to full-text storage
  </read_first>
  <action>
    Human/operator checkpoint: provide the curated 100–150 URL list for AI agents & future work, configured Gemini credential, and reviewed Codex CLI configuration. Run collection/preprocessing/dedupe/quality/Codex gate locally; for every public YouTube candidate, use Gemini's official fixed-URL video input to generate the structured wrapper/digest without downloading or storing transcript/audio/video. Have the AI-and-work SME/HCI reviewer and rights reviewer perform their recorded checks, then use the D-12 UI for explicit operator disposition of every candidate. Select approximately 50 approved posts with approximately 70% text/30% video, mostly evergreen coverage, documented viewpoint/source distribution, 3–5 useful pre-generated questions per post, stored full article text or video URL/ID plus approved digest, thumbnails where permitted, and source link attribution. Freeze once to `data/content_pool_v1/`, run verification, and never mutate it afterward. If rights/protocol approval for a full article is absent, reject it rather than weakening D-06 silently. This checkpoint cannot be automated or bypassed with fixtures.
  </action>
  <verify><automated>cd tools/content_pipeline && npm run cli -- freeze --output ../../data/content_pool_v1 --verify-only</automated></verify>
  <acceptance_criteria>
    - Manifest reports roughly 100–150 raw candidates and roughly 50 accepted frozen posts for D-01, with documented D-02/D-03 distribution.
    - Every post has current Codex verdict, explicit operator approval, rights/provenance review, source asset, exact schemas, and valid suggestion targets.
    - All hashes/counts/references verify, the destination is immutable, and no secret/raw run cache is shipped.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
Browser requests cross into a privileged local review server; mutable run artifacts cross into an immutable participant artifact.

## ASVS-L1 STRIDE Threat Register
| threat_id | STRIDE category | component | severity | disposition | mitigation_plan | automated verification |
|---|---|---|---|---|---|---|
| P02-04-T01 | Spoofing | local review HTTP server | HIGH | mitigate | loopback bind, random token, Origin/CSRF checks, no CORS, body caps | negative HTTP tests |
| P02-04-T02 | Elevation of Privilege | review UI rendering | HIGH | mitigate | text nodes, strict CSP, structured inert data only | hostile candidate UI test |
| P02-04-T03 | Tampering | freeze staging/output | HIGH | mitigate | fixed filenames, resolved root, hashes, atomic promote, overwrite refusal | traversal/tamper/overwrite tests |
| P02-04-T04 | Elevation of Privilege | two-gate approval state | HIGH | mitigate | current-hash Codex verdict plus separate explicit operator approval | stale/missing gate tests |
| P02-04-T05 | Information Disclosure | frozen artifact | HIGH | mitigate | shipping allowlist and secret scans; exclude cache/env | secret-pattern fixture |
| P02-04-T06 | Tampering | remote-video/dependency boundary | HIGH | mitigate | pinned Gemini model; only validated frozen YouTube URL/ID; never download or persist transcript/audio/video | provider-envelope, URL-allowlist, and freeze audit tests |

Execution gate: every HIGH threat must have `disposition=mitigate` and green automated verification. Any unresolved or unmitigated HIGH threat blocks this plan.
</threat_model>

<verification>
- `cd tools/content_pipeline && npm test`
- `cd tools/content_pipeline && npm run cli -- freeze --output ../../data/content_pool_v1 --verify-only`
- Human audit: sample every accepted post's gate records and review distribution summary; no AI-only approval.
</verification>
<success_criteria>The secure D-12/D-11 two-gate workflow produces one immutable, rights-reviewed, checksum-valid ~50-post D-01 pilot pool satisfying D-02–D-10.</success_criteria>
<output>Create `02-04-SUMMARY.md` when complete.</output>
