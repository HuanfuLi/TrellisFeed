---
phase: 03-graph-memory-recommendation-engine
plan: 11
type: execute
wave: 9
depends_on: ["03-10"]
files_modified:
  - app/src/services/content-pool-boot.service.ts
  - app/src/App.tsx
  - app/tests/services/content-pool-boot.service.test.mjs
  - app/tests/fixtures/fresh-graph-pool-run.mjs
  - app/tests/phase3/fresh-graph-pool-cutover.test.mjs
autonomous: true
gap_closure: true
requirements: [GRAPH-01, RANK-01, RANK-02, RANK-03, RANK-05, RANK-06]
must_haves:
  truths:
    - "App does not reveal Home until the content pool is ready and globalGraphRepository.load() has successfully hydrated its in-memory indexes"
    - "A global graph load failure remains on the pool recovery/error surface and starts no recommendation session or building batch"
    - "One executed regression creates a fresh graph pool, packages it, imports it through the durable repository seam, loads the global graph, and builds the first eight-item control and experimental batches"
    - "The cross-boundary regression proves control reasons use only the three allowed non-personal templates, experimental rows carry real contributing concept/post trace IDs, and neither condition leaves a building batch"
    - "No live network, provider, enrollment backend, or participant data is used by the regression"
  artifacts:
    - path: "app/src/services/content-pool-boot.service.ts"
      provides: "Executable boot barrier coupling pool import to global graph index hydration"
    - path: "app/tests/phase3/fresh-graph-pool-cutover.test.mjs"
      provides: "Fresh-freeze through first-batch regression for both study conditions"
  key_links:
    - from: "app/src/App.tsx"
      to: "app/src/services/content-pool-boot.service.ts"
      via: "hydrateAllFromSQLite awaits the combined pool-plus-global-graph barrier before setHydrated(true)"
      pattern: "contentPoolBoot"
    - from: "app/src/services/content-pool-boot.service.ts"
      to: "app/src/services/global-graph.repository.ts"
      via: "a ready content-pool snapshot is followed by awaited global graph load"
      pattern: "globalGraphRepository.load"
    - from: "app/tests/phase3/fresh-graph-pool-cutover.test.mjs"
      to: "app/src/services/recommendation.service.ts"
      via: "real imported frozen-feed/global-graph dependencies materialize both first batches"
      pattern: "RecommendationService"
  prohibitions:
    - statement: "Must not make global graph queries lazily load during ranking or add a generated/network feed fallback"
      status: unresolved
    - statement: "Must not fix the failure by weakening globalGraphRepository.requireLoaded(), swallowing boot failure, or exposing condition-specific UI shape"
      status: unresolved
    - statement: "Control integration fixtures must not provide question history or personal graph state to the control branch"
      status: unresolved
---

<objective>
Complete the runtime half of the Phase 3 cutover: put global-graph index loading inside the app hydration barrier and pin the whole fresh-freeze-to-first-recommendation path with a deterministic two-condition integration test.

Purpose: the pool currently imports successfully but Home ranks before the in-memory graph repository is loaded, stranding a building batch and presenting a false empty feed.
Output: a testable boot service, corrected App lifecycle wiring, and a network-free cross-boundary regression.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@CLAUDE.md
@AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-graph-memory-recommendation-engine/03-UAT.md
@.planning/debug/phase3-fresh-graph-pool-uat-gap.md
@.planning/phases/03-graph-memory-recommendation-engine/03-02-SUMMARY.md
@.planning/phases/03-graph-memory-recommendation-engine/03-06-SUMMARY.md
@.planning/phases/03-graph-memory-recommendation-engine/03-07-SUMMARY.md
@.planning/phases/03-graph-memory-recommendation-engine/03-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce a pool-plus-global-graph boot barrier</name>
  <files>app/src/services/content-pool-boot.service.ts, app/src/App.tsx, app/tests/services/content-pool-boot.service.test.mjs</files>
  <read_first>
    - app/src/App.tsx (`hydrateAllFromSQLite`, contentPool ready branch, setHydrated/startResearchSession ordering)
    - app/src/services/content-pool.repository.ts (`hydrate`, ContentPoolRepositorySnapshot, ready/error codes)
    - app/src/services/global-graph.repository.ts (`load`, reset, requireLoaded contract)
    - app/src/services/recommendation.service.ts (`requireFeatures` and persisted building-to-ready lifecycle)
    - app/src/screens/HomeScreen.tsx (beginSession failure presentation; do not redesign the screen in this task)
  </read_first>
  <behavior>
    - "When content pool hydration returns non-ready, the graph loader is not called and the same pool error snapshot returns."
    - "When the pool is ready, the service awaits exactly one global graph load before returning ready."
    - "When global graph loading fails, the combined barrier returns a pool recovery error and App never calls setHydrated(true), session startup, extraction resume, graph-memory repair, or Home recommendation startup."
    - "A successful retry repeats pool and graph hydration and only then reveals participant routes."
  </behavior>
  <action>
    RED: create executable boot-service tests with injected pool and graph repositories for non-ready pool, ready+graph-success, graph-failure, and successful retry. Assert call order and returned status/error rather than reading App source text.

    GREEN: add a small `content-pool-boot.service.ts` that awaits `contentPoolRepository.hydrate()`, returns an existing non-ready snapshot unchanged, and for a ready pool awaits `globalGraphRepository.load()`. Map a failed graph load to the existing pool recovery boundary (use the semantically correct stored-corrupt/import error code; do not expose raw DB detail). Wire App's hydration Promise.all through this service. Keep `setHydrated(true)`, background job resume, research session start, and filter prewarm strictly after the combined ready result. Preserve cancellation checks and all unrelated load-bearing navigation/header/overflow logic. Do not move loading into RecommendationService or relax `requireLoaded()`.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test tests/services/content-pool-boot.service.test.mjs tests/services/global-graph.repository.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Tests execute the same boot service App calls and prove pool→graph ordering plus fail-closed retry behavior.
    - Home cannot mount under a ready pool snapshot unless global graph indexes are loaded.
    - Graph load failure uses the existing pool recovery UI path and produces no recommendation batch row.
    - App's existing cancellation and post-ready background service ordering remain intact.
  </acceptance_criteria>
  <done>The app exposes participant routes only after both durable pool import and in-memory global graph hydration succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute a fresh-freeze-to-first-batch regression for both conditions</name>
  <files>app/tests/fixtures/fresh-graph-pool-run.mjs, app/tests/phase3/fresh-graph-pool-cutover.test.mjs</files>
  <read_first>
    - tools/content_pipeline/test/freeze-graph-artifacts.test.mjs (approved-run fixture shape and offline build invocation)
    - app/scripts/content-pool-package-contract.mjs (temporary-root packaging entry point from Plan 03-10)
    - app/tests/services/content-pool.import.test.mjs (reader and fake-IndexedDB import patterns)
    - app/tests/services/global-graph.repository.test.mjs (durable load/query assertions)
    - app/tests/services/recommendation.service.test.mjs (dependency injection, condition isolation, ready-batch and reason assertions)
  </read_first>
  <behavior>
    - "The test constructs at least ten approved same-topic posts locally, freezes them to a unique temp destination, and proves the manifest has nine valid runtime hashes."
    - "The temp packager emits the exact ten runtime files; ContentPoolRepository imports them through fake IndexedDB and the real boot service loads GlobalGraphRepository."
    - "Control first batch is ready with eight rows, zero personal/LLM reads, strategies limited to topic_baseline/quality_baseline/diversity_baseline, and reasons matching only Related to &lt;concept&gt;, Popular explanation, or Different viewpoint."
    - "Experimental cold-start first batch is ready with eight rows, strategies limited to continue/deepen/contrast/bridge/echo, and every row has real contributing concept or post IDs resolving within the frozen fixture."
    - "After both sessions, no recommendation_batches row has status building; corrupt graph hash and graph-load failure paths create zero served recommendations."
  </behavior>
  <action>
    Build a deterministic fixture helper that writes only local approved review artifacts for enough posts to fill an eight-item batch; use inert example.test URLs and no secrets, network, live AI, or user data. The integration test must call the real `buildFrozenPool`, Plan 03-10's packager function with temporary output roots, `ContentPoolRepository.hydrate()` through the combined boot service, `GlobalGraphRepository`, `FrozenFeedService`, `RecommendationRepository`, and `RecommendationService`. Inject synthetic condition identities, a throwing personal loader for control, and a deterministic local experimental reason responder/personal cold-start snapshot. Query recommendation and batch rows through `dbQuery`, not in-memory mirrors. Test both positive conditions plus a tampered graph hash/load negative. Clean up only test-owned temp directories through the test runner's cleanup hook. Do not depend on `tools/content_pipeline/runs/`, production credentials, the checked-in 77-post pool, or source-text assertions.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test tests/phase3/fresh-graph-pool-cutover.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - One executed test crosses freeze, package, checksum validation, durable import, graph load, feed resolution, ranker branch, reason policy, and batch persistence.
    - Both conditions persist exactly eight ready recommendations on first batch and no building ledger remains.
    - Control path proves no personal state or reason LLM access; experimental trace IDs resolve to fixture graph IDs.
    - Negative graph integrity/load cases fail before ranking and leave zero recommendations.
    - Test is deterministic, offline, and independent of operator-local runs and secrets.
  </acceptance_criteria>
  <done>The exact integration path that failed Android UAT is pinned end-to-end for both study conditions without a live service.</done>
</task>

<task type="auto">
  <name>Task 3: Run full app, package, and Android build gates</name>
  <files>app/src/App.tsx, app/src/services/content-pool-boot.service.ts, app/tests/services/content-pool-boot.service.test.mjs, app/tests/phase3/fresh-graph-pool-cutover.test.mjs</files>
  <read_first>
    - app/package.json (canonical test/lint/build scripts)
    - app/capacitor.config.ts (Android sync target)
    - .planning/phases/03-graph-memory-recommendation-engine/03-UAT.md (exact rerun truth after implementation)
  </read_first>
  <action>
    Run the targeted boot/cutover tests, full app suite, lint, production build, package `--check`, Capacitor Android sync, and Android debug assembly. Preserve existing lint-warning policy but accept zero errors only. Confirm the assembled WebView assets contain the selected graph files and that the cross-boundary test leaves no durable building batch. Do not mark the original UAT passed here; execution prepares the fixed build, while `/gsd-verify-work 3` reruns the Android reason-surface acceptance against production source afterward.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; node scripts/package-content-pool.mjs --check &amp;&amp; npx cap sync android &amp;&amp; cd android &amp;&amp; .\gradlew.bat assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - App tests pass with zero failures/skips; lint has zero errors; production build passes.
    - Package `--check`, Android sync, and debug APK assembly pass from the selected graph pool.
    - No live network or AI service is contacted by any automated test.
    - The resulting APK is ready for the original API 36 control/experimental UAT rerun.
  </acceptance_criteria>
  <done>All local gates and Android packaging succeed on the corrected production path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable ready pool → in-memory graph indexes | Ranking must not consume partially loaded or stale graph state. |
| Study condition → recommendation materialization | Control must remain structurally isolated from personal/question data. |
| Frozen fixture → persisted batches | Integrity-checked content becomes participant-visible ordering and reasons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-32 | Denial of service | App hydration / graph load | high | mitigate | Awaited combined barrier, recovery error path, retry test, no Home exposure on failure. |
| T-03-33 | Tampering | ranking against unloaded/stale graph | high | mitigate | Preserve requireLoaded; boot-order test; fresh import+load integration. |
| T-03-34 | Information disclosure / experiment contamination | control ranker dependencies | high | mitigate | Throwing personal/LLM spies and exact fixed-reason/strategy assertions in cross-boundary test. |
| T-03-35 | Tampering | interrupted recommendation ledger | medium | mitigate | Negative and positive DB assertions prove no building batch or served rows before successful graph load. |
</threat_model>

<verification>
- `cd app && node --test tests/services/content-pool-boot.service.test.mjs tests/phase3/fresh-graph-pool-cutover.test.mjs`
- `cd app && npm test && npm run lint && npm run build`
- `cd app && node scripts/package-content-pool.mjs --check && npx cap sync android`
- `cd app/android && .\gradlew.bat assembleDebug`
- Follow with `/gsd-verify-work 3` to rerun the original API 36 visual/event UAT on checked-in production source.
</verification>

<success_criteria>
- App boot cannot reach recommendation ranking with an unloaded global graph.
- A deterministic fresh-freeze/package/import/load test produces correct eight-item first batches for both conditions and zero stranded building rows.
- The selected graph pool builds and syncs into an Android debug APK with all gates green.
</success_criteria>

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-11-SUMMARY.md` when done.
</output>
