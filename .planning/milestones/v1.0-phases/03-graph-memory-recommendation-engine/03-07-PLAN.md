---
phase: 03-graph-memory-recommendation-engine
plan: 07
type: execute
wave: 5
depends_on: ["03-06"]
files_modified:
  - app/src/screens/HomeScreen.tsx
  - app/src/components/FeedCard.tsx
  - app/src/services/interaction-log.service.ts
  - app/src/locales/en.json
  - app/src/locales/zh.json
  - app/src/locales/es.json
  - app/src/locales/ja.json
  - app/tests/screens/HomeScreen.recommendation-feed.test.mjs
autonomous: true
requirements: [RANK-05]
must_haves:
  truths:
    - "Home renders recommendation batches from recommendationService — session start builds/recovers batch 1, bottom pull appends the next persisted batch; frozenFeedService manifest-order rendering is no longer the feed source (D-10, D-11)"
    - "One feed_impression event is logged per actually-exposed recommendation with postId + recommendationId (no display position), replacing the single fieldless whole-feed event — §10.6 exposure deltas become replayable"
    - "Both conditions render the identical reason surface; tapping/expanding a reason logs recommendation_reason_view with postId + recommendationId"
    - "reasonText renders as plain React text (never dangerouslySetInnerHTML, never markdown rendering)"
    - "Already-served batches are never re-ranked or reshuffled by GRAPH_UPDATED; the screen re-reads service state on navigation per the always-mounted rule"
    - "All new UI chrome strings land in all 4 locale bundles in this plan (bundle-parity green); reasonText itself renders as data, untranslated"
  artifacts:
    - path: "app/src/screens/HomeScreen.tsx"
      provides: "Recommendation-batch feed with per-item impression logging"
    - path: "app/src/components/FeedCard.tsx"
      provides: "Shared condition-neutral reason affordance"
    - path: "app/tests/screens/HomeScreen.recommendation-feed.test.mjs"
      provides: "Feed-source, impression-logging, reason-render tests"
  key_links:
    - from: "app/src/screens/HomeScreen.tsx"
      to: "app/src/services/recommendation.service.ts"
      via: "beginSession/nextBatch drive the rendered list"
      pattern: "recommendationService"
    - from: "app/src/components/FeedCard.tsx"
      to: "app/src/services/interaction-log.service.ts"
      via: "recommendation_reason_view on reason interaction"
      pattern: "recommendation_reason_view"
  prohibitions:
    - statement: "No knowledge-graph/mind-map visualization may appear — the per-recommendation reason (and at most a future chip list) is the §7.7 visibility ceiling; the chip list itself stays deferred to Phase 4"
      status: unresolved
    - statement: "User-facing copy must follow DEC-framing-rules — never 'AI learning feed', 'AI tutor', or 'mind map' in any locale string added here"
      status: unresolved
---

<objective>
Wire the participant feed to the recommendation seam: Home renders persisted Recommendation batches with a condition-neutral reason surface, and impression/reason-view logging becomes per-recommendation so §10.6 exposure updates and reason-view analysis are replayable (D-10, D-11, RANK-05 render surface; RESEARCH Pitfall 6).

Output: HomeScreen + FeedCard changes, EVENT_FIELDS update, 4-locale chrome strings, screen tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@app/src/screens/HomeScreen.tsx
@app/src/services/recommendation.service.ts
</context>

## Artifacts this phase produces (plan 03-07 subset)

- Files: `app/tests/screens/HomeScreen.recommendation-feed.test.mjs`
- View model: `{ recommendation, post, conceptLabels }` passed from HomeScreen into the card list
- EVENT_FIELDS change: `feed_impression: ['postId', 'recommendationId']`
- Locale namespace: `feed.reason.*` chrome keys (e.g. `feed.reason.toggleLabel`) in en/zh/es/ja

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomeScreen on recommendation batches + per-item impressions</name>
  <files>app/src/screens/HomeScreen.tsx, app/src/services/interaction-log.service.ts, app/tests/screens/HomeScreen.recommendation-feed.test.mjs</files>
  <read_first>app/src/screens/HomeScreen.tsx (current frozenFeedService.getFeed usage ~lines 10–60, the single feed_impression call, the location.pathname re-read pattern, pull-to-load gesture — do NOT alter gesture/DIRECTION_SLOP logic), app/src/services/interaction-log.service.ts (EVENT_FIELDS map ~line 31; feed_impression currently []), app/src/services/recommendation.service.ts (beginSession/nextBatch/currentSessionItems API), CLAUDE.md (always-mounted re-read rule; feed cold start rules; WKWebView touch rules), app/src/services/research-wire-contract.ts + research-backend/src/validation.ts (confirm optional postId/recommendationId on events already pass validation — UserInteractionEvent schema has them; extend only if a per-event-type allowlist exists on the backend)</read_first>
  <behavior>
    - Test: Home resolves its list from recommendationService session batches (mocked service), not frozenFeedService.getFeed
    - Test: rendering N items logs N feed_impression events each carrying postId + recommendationId; re-render of the same batch does not duplicate impressions (per-recommendation once-per-session dedup)
    - Test: bottom pull appends the next batch's items in persisted order; a GRAPH_UPDATED emission does NOT reorder already-rendered items
    - Test: empty ready batch renders the existing no-more-posts state, not an error
  </behavior>
  <action>
    Replace HomeScreen's frozenFeedService feed sourcing with recommendationService: on mount/session, beginSession() and render currentSessionItems() as { recommendation, post, conceptLabels } view models (post resolved by postId from frozenFeedService/contentPool by ID — frozenFeedService remains the immutable post-record resolver per Phase 2 decisions; only ORDERING moves to recommendations). Bottom pull-to-load → recommendationService.nextBatch() append. Update EVENT_FIELDS.feed_impression to ['postId', 'recommendationId'] and log one impression per exposed item with a per-session seen-recommendationId set for dedup. Keep the location.pathname re-read effect and FEED-cold-start semantics (loading state until the first batch is ready). Do not touch the touch/gesture code paths flagged load-bearing in CLAUDE.md.
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/HomeScreen.recommendation-feed.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getFeed()" app/src/screens/HomeScreen.tsx` returns 0 (ordering no longer manifest-driven)
    - `grep -c "'postId', 'recommendationId'" app/src/services/interaction-log.service.ts` ≥ 1 (feed_impression fields)
    - Impression dedup + append-order tests green; existing HomeScreen empty-state test suite green or consciously rewritten in this plan's test file
  </acceptance_criteria>
  <done>Feed served from persisted batches; impressions replayable per §10.6.</done>
</task>

<task type="auto">
  <name>Task 2: FeedCard reason surface + reason-view logging + 4-locale chrome</name>
  <files>app/src/components/FeedCard.tsx, app/src/locales/en.json, app/src/locales/zh.json, app/src/locales/es.json, app/src/locales/ja.json, app/tests/screens/HomeScreen.recommendation-feed.test.mjs</files>
  <read_first>app/src/components/FeedCard.tsx (current card layout, inline-style + CSS-var conventions: --surface-variant, --muted-foreground, --radius-xl), app/src/locales/en.json (namespace structure), app/scripts/translate-locales.md (translation workflow), app/src/services/interaction-log.service.ts (recommendation_reason_view fields: postId, recommendationId — already allowlisted)</read_first>
  <action>
    Add a compact reason affordance to FeedCard: a small labeled row/chip (inline styles with CSS vars, respecting 44px touch minimum) showing recommendation.reasonText as plain React text — identical component and styling for both conditions (rendering-identical per D-10; only the text content differs). Interaction (tap to expand when truncated, or on-visible if always shown — pick tap-to-expand for a clean reason-view signal) logs interactionLog.record('recommendation_reason_view', { postId, recommendationId }). Add chrome keys under feed.reason.* to en.json then all 3 other bundles in this same task (EN-first workflow; ~"Why this post?" toggle label — phrase it within DEC-framing-rules). reasonText itself is data: no translation call, no locale key.
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/HomeScreen.recommendation-feed.test.mjs tests/locales/bundle-parity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Comment-filtered gate: `grep -v '^\s*//' app/src/components/FeedCard.tsx | grep -v '^\s*\*' | grep -c "dangerouslySetInnerHTML"` returns 0 (reason renders as a plain text node; comment lines excluded)
    - bundle-parity + missing-key locale tests green (all 4 bundles updated)
    - Reason tap test asserts a recommendation_reason_view event with both IDs
    - `npm test`, `npm run lint`, `npm run build` green from app/
  </acceptance_criteria>
  <done>Interpretable reasons visible identically in both conditions with logged views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted reasonText → DOM | LLM-generated text reaches the participant's screen |
| UI events → research log | impression/reason-view events become exported study data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | Tampering (XSS) | reasonText rendering | high | mitigate | Plain React text node only; no HTML/markdown rendering; upstream validation (03-06) already strips control chars |
| T-03-22 | Information disclosure | impression events | low | accept | Events carry only postId/recommendationId (no position, no content), within the §14.1 allowlist and §14.2 exclusions |
| T-03-23 | Repudiation | missed/duplicated impressions skewing §10.6 replay | medium | mitigate | Per-session recommendationId dedup set + once-per-exposure logging test |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green
- Manual (phase UAT, per 03-VALIDATION.md manual-only row): load feed in browser with a fixture pool; confirm reasons render per condition (experimental: trace-backed prose; control: fixed labels) — deferred to /gsd-verify-work because the frozen pool ships with Phase 2's 02-09
</verification>

<success_criteria>
- The Phase 2 feed UI renders whatever the recommendation seam returns (D-10) with a shared reason surface (RANK-05) and replayable per-item logging.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- In-browser reason rendering is the phase's manual-only verification (Node cannot exercise WebView); recorded in 03-VALIDATION.md, executed at /gsd-verify-work once a pool artifact exists.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-07-SUMMARY.md` when done.
</output>
