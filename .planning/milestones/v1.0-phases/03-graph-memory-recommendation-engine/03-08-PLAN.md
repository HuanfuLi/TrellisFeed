---
phase: 03-graph-memory-recommendation-engine
plan: 08
type: execute
wave: 6
depends_on: ["03-05", "03-07"]
files_modified:
  - app/src/services/concept-feed.service.ts
  - app/src/services/post-queue.service.ts
  - app/src/services/style-assignment.ts
  - app/src/services/feed-spread.ts
  - app/src/services/refill-mutex.ts
  - app/src/services/infiniteScroll.service.ts
  - app/src/services/post-essay.service.ts
  - app/src/services/session.service.ts
  - app/src/services/starter-posts-decay.ts
  - app/src/services/text-art-fragment.ts
  - app/src/services/engagement.service.ts
  - app/src/services/daily-read.service.ts
  - app/src/services/post-history.service.ts
  - app/src/services/db.service.ts
  - app/src/App.tsx
  - app/src/components/InfoFlow.tsx
  - app/src/state/keyboard-hysteresis.ts
  - app/src/types/index.ts
  - app/tests/**
autonomous: true
requirements: [RANK-06]
must_haves:
  truths:
    - "The five transitional modules (concept-feed.service, post-queue.service, style-assignment, feed-spread, refill-mutex) are deleted, and a final rg over app/src + app/tests finds zero references to their module names or exported symbols (D-12)"
    - "Every deletion was guarded: callers/tests were grepped at execution time before removing each module, and every reader/writer found was either deleted with it or rewired to the recommendation seam"
    - "No source-reading test pins the removed shell: the 37 inventoried test files are removed or rewritten as executable recommendation-path tests (false-green elimination, RANK-06 hygiene)"
    - "IDB_VERSION is 7 and the upgrade deletes only the verified-retired object stores (posts, post_queue, sessions) after an execution-time grep confirms no live reader; post_history, engagement, canonical Q&A, research, content_pool_*, and all Phase 3 stores survive"
    - "PostDetail's three CONCEPT_EXPLORED detectors, post-history.service durable bodies, and the daily exploration dedup state are preserved"
    - "All gates green after the sweep: npm test, npm run lint, npm run build from app/"
  artifacts:
    - path: "app/src/services/db.service.ts"
      provides: "IDB v7 with retired stores deleted"
      contains: "IDB_VERSION = 7"
  key_links:
    - from: "app/src/App.tsx"
      to: "app/src/services/recommendation.service.ts"
      via: "boot path initializes recommendation seam services only — no concept-feed/post-queue boot calls remain"
      pattern: "recommendationService"
  prohibitions:
    - statement: "Must not delete or weaken load-bearing survivors: post-history durable bodies, CONCEPT_EXPLORED detectors, engagement store, root overflow clip, Header portal pattern, SwipeTabContainer resize guards"
      status: unresolved
    - statement: "Must not leave the removed pipeline resurrectable — no dormant exports, commented-out call sites, or feature-flagged stubs of the three-list/cyclic-queue machinery may remain"
      status: unresolved
---

<objective>
Execute the D-12 guarded removal sweep: with Home now serving persisted recommendation batches (03-07 green), delete the transitional concept-feed / post-queue / style-assignment / feed-spread / refill-mutex shell, its dead callers, its 37 pinning test files, and its retired IndexedDB stores — leaving zero dormant residue and all gates green.

Purpose: dormant transitional machinery plus source-reading tests are exactly the false-green residue class CLAUDE.md warns about; removal is scoped to Phase 3 by D-12.

Output: deletions + rewrites + IDB v7 cleanup + final residue proof.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-graph-memory-recommendation-engine/03-RESEARCH.md
</context>

## Authoritative removal inventory (verified by orchestrator grep 2026-07-17 — re-grep at execution time; the list may drift)

**Modules to remove** (`app/src/services/`): `concept-feed.service.ts`, `post-queue.service.ts`, `style-assignment.ts`, `feed-spread.ts`, `refill-mutex.ts`.

**Source files importing/referencing them (15):** `src/App.tsx`, `src/components/InfoFlow.tsx`, `src/services/engagement.service.ts`, `src/services/infiniteScroll.service.ts`, `src/services/post-essay.service.ts`, `src/services/post-history.service.ts`, `src/services/session.service.ts`, `src/services/starter-posts-decay.ts`, `src/services/text-art-fragment.ts`, `src/state/keyboard-hysteresis.ts`, plus the five modules themselves.

**Test files referencing them (37):** tests/concept-feed.test.mjs, tests/components/BottomNavigation.slide.test.mjs, tests/phase1/rebrand-surfaces.test.mjs, tests/providers/gemini-text-art-budget.test.mjs, tests/providers/privacy-callsite-structural.test.mjs, tests/screens/HomeScreen.image-pregen-filter.test.mjs, tests/services/{bonus-post-cap, concept-batch-filter, concept-feed-bonus-cap, concept-feed-cache-date, concept-feed-dismiss-filter, daily-generation-cap, derived-list, fast-model-routing, filter-cache, image-gen-key-gate, like-boost, post-essay.service, post-history-fallback, post-queue, post-queue-dedup, post-queue-rehydrate, post-queue-remove-by-id, post-queue-yesterday-snapshot, refill-mutex, refill-queue-integration, refill-reliability, spread-by-concept, starter-posts, starter-posts-persist, storage-migration, storage-namespace, style-assignment, style-assignment-stratified, text-art-no-truncate-on-regen, text-art-tightener, walker-empty-derived-list}.test.mjs.

## Artifacts this phase produces (plan 03-08 subset)

- `IDB_VERSION = 7` upgrade with deleteObjectStore for verified-retired stores
- Rewritten tests: `tests/phase1/rebrand-surfaces.test.mjs`, `tests/services/storage-namespace.test.mjs` re-targeted at live recommendation-path modules
- No new runtime symbols (this plan removes)

<tasks>

<task type="auto">
  <name>Task 1: Guarded source removal — five modules + dead callers</name>
  <files>app/src/services/concept-feed.service.ts, app/src/services/post-queue.service.ts, app/src/services/style-assignment.ts, app/src/services/feed-spread.ts, app/src/services/refill-mutex.ts, app/src/services/infiniteScroll.service.ts, app/src/services/post-essay.service.ts, app/src/services/session.service.ts, app/src/services/starter-posts-decay.ts, app/src/services/text-art-fragment.ts, app/src/services/engagement.service.ts, app/src/services/daily-read.service.ts, app/src/services/post-history.service.ts, app/src/App.tsx, app/src/components/InfoFlow.tsx, app/src/state/keyboard-hysteresis.ts, app/src/types/index.ts</files>
  <read_first>CLAUDE.md ("Heavy stores", "Concept Feed Generation Pipeline" — the shell being removed; "PostDetail exploration detectors" and "Best practices" #9 call-site sweep rule), .planning/phases/03-graph-memory-recommendation-engine/03-RESEARCH.md ("D-12 Guarded Removal Sweep" table + "Guarded disposition" bullets), app/src/App.tsx (current boot wiring), app/src/services/post-history.service.ts (PRESERVE — read to separate its concept-feed import from its durable-store role)</read_first>
  <action>
    For EACH of the five modules, run rg for its filename and every exported symbol across app/src BEFORE deleting (guard per D-12 — the inventory above may have drifted). Then: delete the five modules; delete dead-by-inventory helpers infiniteScroll.service.ts, starter-posts-decay.ts, text-art-fragment.ts; delete post-essay.service.ts and session.service.ts ONLY after the execution-time grep confirms no non-test production caller (RESEARCH found none) — if a live caller surfaces, rewire it to the recommendation/frozen seam instead and record the deviation. Update survivors in place: App.tsx (drop concept-feed/post-queue boot + imports; keep hydration gate, clearLegacyHeavyLocalStorageKeys, 03-05's resumeOnBoot/repairOnBoot wiring), InfoFlow.tsx (drop retired imports or delete the component if it has no remaining renderer — grep its usages), engagement.service.ts + keyboard-hysteresis.ts (comment-only references — update comments), post-history.service.ts (remove retired imports ONLY; durable body store stays), daily-read.service.ts (keep the PostDetail exploration dedup state; remove getAnchorIdForPost and getConceptQuota plus dead lib/concept-target.ts if grep confirms only retired callers). In types/index.ts remove generated-feed-only types (DailyPost, style/session types) only after a type-level caller grep. Preserve untouched: PostDetailScreen detectors, Header, SwipeTabContainer, root overflow clip.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "concept-feed|post-queue|style-assignment|feed-spread|refill-mutex" app/src` returns no files
    - `rg -l "postQueueService|conceptFeedService|walkDerivedList|appendToDerivedList|refillQueue" app/src` returns no files
    - post-history.service.ts still exports its durable patch/read API; PostDetailScreen.tsx unmodified
    - tsc green (build-level proof no orphan imports remain)
  </acceptance_criteria>
  <done>Shell removed with per-module guards; survivors intact; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Test sweep — remove pinning tests, rewrite the load-bearing ones</name>
  <files>app/tests/** (the 37 inventoried files), app/tests/phase1/rebrand-surfaces.test.mjs, app/tests/services/storage-namespace.test.mjs</files>
  <read_first>app/tests/phase1/rebrand-surfaces.test.mjs and app/tests/services/storage-namespace.test.mjs (both directly require concept-feed/session sources today — their INTENT survives, their targets change), .planning/phases/03-graph-memory-recommendation-engine/03-RESEARCH.md (test-match table), CLAUDE.md (false-green warning: a source-reading test asserting a retired key is present pins the bug)</read_first>
  <action>
    Delete every inventoried test file whose subject was removed in Task 1 (re-grep each of the 37 before deleting — a file may have gained live-path assertions since the inventory). Rewrite rather than delete the two contract tests: rebrand-surfaces.test.mjs re-targets its no-Trellis-string sweep at the current live module set (drop the concept-feed/session requires); storage-namespace.test.mjs asserts the questiontrace namespace against db.service and live services without requiring removed sources. Also re-check tests/components/BottomNavigation.slide.test.mjs, tests/providers/gemini-text-art-budget.test.mjs, tests/providers/privacy-callsite-structural.test.mjs, tests/screens/HomeScreen.image-pregen-filter.test.mjs, tests/services/fast-model-routing.test.mjs, filter-cache.test.mjs, post-history-fallback.test.mjs, like-boost.test.mjs, image-gen-key-gate.test.mjs, daily-generation-cap.test.mjs: keep any that guard a LIVE path after Task 1 (strip only the retired references), delete those whose entire subject died. Zero new tests may read implementation source to assert the shell's absence-by-string where an executable assertion exists (prefer import-failure/behavioral checks).
  </action>
  <verify>
    <automated>cd app && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l "concept-feed|post-queue|style-assignment|feed-spread|refill-mutex" app/tests` returns no files
    - `npm test` green with zero skipped suites
    - rebrand-surfaces + storage-namespace tests still exist and pass against live modules
  </acceptance_criteria>
  <done>No test pins the removed shell; contract tests guard live paths.</done>
</task>

<task type="auto">
  <name>Task 3: IDB store cleanup + full gates + residue proof</name>
  <files>app/src/services/db.service.ts</files>
  <read_first>app/src/services/db.service.ts (DDL list, IDB_VERSION 6 from 03-02, upgrade handler creating stores — extend it to delete), CLAUDE.md ("Runtime State Inventory" reasoning: schema cleanup, not merely source deletion)</read_first>
  <action>
    Before deleting each store, rg app/src for its table name in SQL strings ("FROM posts", "INTO posts", "post_queue", "sessions") to confirm zero live readers/writers post-Task-1. Then: bump IDB_VERSION 6→7; in the upgrade handler, deleteObjectStore for posts, post_queue, and sessions when present; remove their CREATE TABLE DDL entries (keep post_history, engagement, research_*, content_pool_*, user_questions, ai_answers, and all nine Phase 3 tables). Both backends must stay parity: the LocalStorage fallback drops the same table namespaces. Run the full gate set and the final residue sweep.
  </action>
  <verify>
    <automated>cd app && npm test && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IDB_VERSION = 7" app/src/services/db.service.ts` = 1
    - `grep -c "post_queue" app/src/services/db.service.ts` returns 0; DDL entries for posts/sessions removed while post_history/engagement remain
    - Fresh-DB test proves upgrade path: opening v7 over a seeded v6 fixture (fake-indexeddb) retains post_history rows and lacks the three retired stores
    - Full gates green: `npm test`, `npm run lint` (0 errors), `npm run build`
    - Final proof: `rg "concept-feed|post-queue|style-assignment|feed-spread|refill-mutex|walkDerivedList" app/src app/tests` → zero matches
  </acceptance_criteria>
  <done>Schema and source both clean; D-12 complete; phase gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDB upgrade → participant data | store deletion is irreversible on-device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-24 | Denial of service / data loss | deleteObjectStore removing a live store | high | mitigate | Per-store execution-time caller grep before deletion; upgrade-path test proving survivors retain rows; only the three verified-retired stores named |
| T-03-25 | Tampering | false-green tests hiding a broken recommendation path | medium | mitigate | Pinning tests deleted; contract tests rewritten against executable live paths; full suite + build gate |
</threat_model>

<verification>
- `cd app && npm test && npm run lint && npm run build` green
- Residue rg (Task 3 final proof) empty
- `npx cap sync` after build before any device UAT (bundled retired code persists until synced — RESEARCH Runtime State Inventory)
</verification>

<success_criteria>
- D-12 complete: nothing in Phase 3+ can reuse the three-list/cyclic-queue machinery; no dormant residue; no false-green pins; all gates green.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- **RANK-06 unclassified probe (hygiene half):** resolved as "no source-reading test pins the retired shell" truths; row remains flagged for verifier attention.
- The 37-file inventory is a point-in-time orchestrator grep (2026-07-17); Tasks 1–3 re-grep at execution time and treat the inventory as a floor, not a ceiling.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-08-SUMMARY.md` when done.
</output>
