---
phase: 03-graph-memory-recommendation-engine
plan: 09
type: execute
wave: 7
depends_on: ["03-08"]
files_modified:
  - tools/content_pipeline/src/freeze/runtime-artifacts.ts
  - tools/content_pipeline/src/freeze/build.ts
  - tools/content_pipeline/src/freeze/verify.ts
  - tools/content_pipeline/schemas/frozen-pool.schema.json
  - tools/content_pipeline/test/freeze-graph-artifacts.test.mjs
  - tools/content_pipeline/test/schema.test.mjs
  - app/tests/fixtures/content-pool/minimal-valid-pool.json
  - data/content_pool_graph_20260718/**
autonomous: true
gap_closure: true
requirements: [GRAPH-01]
must_haves:
  truths:
    - "Every fresh freeze hashes all nine runtime artifacts in manifest.artifactHashes, including sources.json, global_edges.json, and ranking_features.json (D-01)"
    - "The frozen-pool schema and verifier reject a missing, extra, or checksum-mismatched graph runtime artifact; bundleFileHashes and artifactHashes cannot disagree silently"
    - "The existing approved pilot review run is promoted only to a new immutable destination and version; data/content_pool_v1 is never edited, replaced, or deleted"
    - "The new immutable pool contains 77 posts plus the three graph artifacts and passes the full offline verifier with no POOL_INVALID contract mismatch"
  artifacts:
    - path: "tools/content_pipeline/src/freeze/runtime-artifacts.ts"
      provides: "One nine-file runtime artifact inventory consumed by freeze writing and verification"
    - path: "data/content_pool_graph_20260718/manifest.json"
      provides: "New immutable pilot-graph-20260718 pool with exact nine-key runtime hash map"
  key_links:
    - from: "tools/content_pipeline/src/freeze/build.ts"
      to: "tools/content_pipeline/src/freeze/runtime-artifacts.ts"
      via: "artifactHashes is built from the shared nine-file inventory"
      pattern: "RUNTIME_ARTIFACT"
    - from: "tools/content_pipeline/src/freeze/verify.ts"
      to: "tools/content_pipeline/src/freeze/runtime-artifacts.ts"
      via: "verification recomputes every runtime hash from the same inventory"
      pattern: "RUNTIME_ARTIFACT"
  prohibitions:
    - statement: "Must not patch or regenerate data/content_pool_v1 in place; the freeze command must fail if the new destination already exists"
      status: unresolved
    - statement: "Must not weaken the app's exact nine-artifact importer to accept an incomplete manifest"
      status: unresolved
---

<objective>
Repair the offline half of the Phase 3 cutover: make the freeze writer, JSON schema, and verifier agree on the exact nine runtime artifacts, then produce a separately versioned immutable graph pool from the existing operator-approved review run.

Purpose: D-01 requires a ready-made global graph frozen into the pool. The current writer emits graph files but omits their runtime hashes, so its own verifier accepts an artifact the app correctly rejects.
Output: a shared runtime inventory, fail-closed hash/schema tests, and `data/content_pool_graph_20260718/` version `pilot-graph-20260718`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@CLAUDE.md
@AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@.planning/phases/03-graph-memory-recommendation-engine/03-UAT.md
@.planning/debug/phase3-fresh-graph-pool-uat-gap.md
@.planning/phases/03-graph-memory-recommendation-engine/03-01-SUMMARY.md
@.planning/phases/03-graph-memory-recommendation-engine/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify the nine-artifact freeze hash contract</name>
  <files>tools/content_pipeline/src/freeze/runtime-artifacts.ts, tools/content_pipeline/src/freeze/build.ts, tools/content_pipeline/src/freeze/verify.ts, tools/content_pipeline/schemas/frozen-pool.schema.json, tools/content_pipeline/test/freeze-graph-artifacts.test.mjs, tools/content_pipeline/test/schema.test.mjs, app/tests/fixtures/content-pool/minimal-valid-pool.json</files>
  <read_first>
    - tools/content_pipeline/src/freeze/build.ts (`RUNTIME_FILES`, text projection, manifest construction, atomic promotion)
    - tools/content_pipeline/src/freeze/verify.ts (`RUNTIME_FILES`, graph-file reads, artifactHashes and bundleFileHashes checks)
    - tools/content_pipeline/schemas/frozen-pool.schema.json (`artifactHashes` exact-key contract)
    - tools/content_pipeline/test/freeze-graph-artifacts.test.mjs (current graph hash assertions and tamper helper)
    - app/src/data/content-pool-bundle.ts (`CONTENT_POOL_FILENAMES` and exact runtime hash enforcement; preserve this strict boundary)
  </read_first>
  <behavior>
    - "A newly built fixture manifest has exactly nine artifactHashes keys: the six content files plus sources.json, global_edges.json, and ranking_features.json."
    - "Changing bytes in any graph artifact without updating artifactHashes makes verifyFrozenPool report that artifact's runtime checksum mismatch even if bundleFileHashes was updated."
    - "The frozen-pool schema rejects a manifest missing any graph hash and rejects unknown artifact hash keys."
  </behavior>
  <action>
    RED: extend the executable graph-freeze and schema tests to assert the exact nine-key runtime hash set and graph-file tamper rejection. Update the shared minimal manifest fixture with well-formed graph hash keys so schema tests continue to exercise the canonical manifest shape.

    GREEN: add `runtime-artifacts.ts` exporting one readonly nine-file inventory. Import it in both build.ts and verify.ts; build `manifest.artifactHashes` from every entry and verify every entry before structural validation. Extend frozen-pool.schema.json so artifactHashes requires exactly those nine named properties with no extras. Keep `fixedFilenames`/`bundleFileHashes` as the broader immutable inventory for source files and review logs; do not collapse their distinct purpose into artifactHashes. Do not change the strict app importer.
  </action>
  <verify>
    <automated>cd tools/content_pipeline &amp;&amp; node --test test/freeze-graph-artifacts.test.mjs test/schema.test.mjs &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Fresh fixture manifest artifactHashes has exactly nine keys and every value matches the corresponding file bytes.
    - Graph-file tamper produces a runtime checksum error for the changed filename.
    - Schema validation fails for missing/extra graph hash keys.
    - Pipeline TypeScript build passes without duplicating the runtime filename array in build.ts or verify.ts.
  </acceptance_criteria>
  <done>Freeze writer, schema, and verifier expose one exact nine-artifact runtime integrity contract compatible with the app importer.</done>
</task>

<task type="auto">
  <name>Task 2: Produce a separately versioned immutable graph pool</name>
  <files>data/content_pool_graph_20260718/**</files>
  <read_first>
    - tools/content_pipeline/src/cli.ts (`freeze` argument contract and verify-only mode)
    - tools/content_pipeline/src/freeze/build.ts (`atomicPromotePool` destination-exists guard)
    - tools/content_pipeline/runs/pilot-v1-20260716/review/decisions.jsonl (presence and operator-gate status only; do not rewrite the run)
    - CLAUDE.md (`Frozen feed boundary` immutability rules)
  </read_first>
  <action>
    Preflight that `tools/content_pipeline/runs/pilot-v1-20260716` contains the existing normalized, preprocessed, Codex-advisory, and operator-decision artifacts and that `data/content_pool_graph_20260718` does not exist. Do not repair, rewrite, or commit the run directory. From `tools/content_pipeline/`, execute the offline freeze with `--run-dir runs/pilot-v1-20260716 --output ../../data/content_pool_graph_20260718 --version pilot-graph-20260718`; this makes no provider/network call and the build's atomic promotion must refuse an existing destination. Then run verify-only against that destination and inspect the manifest invariants: approvedCount 77, nine exact runtime hashes, all three graph filenames in fixedFilenames/bundleFileHashes, and no retired untyped edge helper files. Preserve `data/content_pool_v1` byte-for-byte; if the destination already exists or approval inputs are incomplete, stop rather than deleting or overwriting anything.
  </action>
  <verify>
    <automated>cd tools/content_pipeline &amp;&amp; npm run cli -- freeze --output ../../data/content_pool_graph_20260718 --verify-only &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `data/content_pool_graph_20260718/manifest.json` identifies `pilot-graph-20260718`, approvedCount 77, and exactly nine runtime hashes.
    - sources.json, global_edges.json, and ranking_features.json exist and verify under both runtime and immutable bundle hashes.
    - post_concept_edges.json and post_claim_edges.json are absent from the new pool.
    - The original `data/content_pool_v1` tree has no worktree diff.
  </acceptance_criteria>
  <done>A verified, immutable Phase 3 graph pool exists at a new destination without mutating the legacy pool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-approved run → immutable freeze | Reviewed local artifacts become participant-visible frozen content. |
| Frozen files → manifest hashes | File bytes cross the integrity contract used by packaging and runtime import. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-26 | Tampering | artifactHashes graph omissions | high | mitigate | One shared nine-file inventory, exact-key schema, per-file checksum tamper tests. |
| T-03-27 | Tampering / data loss | immutable pool promotion | high | mitigate | New destination/version only; atomic promotion refuses an existing directory; verify original pool has no diff. |
| T-03-28 | Information disclosure | operator run/source files | medium | mitigate | Freeze publishes only fixed reviewed artifacts; run directory remains uncommitted and is never projected into the app. |
</threat_model>

<verification>
- `cd tools/content_pipeline && npm test && npm run build`
- `cd tools/content_pipeline && npm run cli -- freeze --output ../../data/content_pool_graph_20260718 --verify-only`
- Manifest audit proves exact nine runtime hashes and graph files under both integrity inventories.
- Git diff proves `data/content_pool_v1` is untouched.
</verification>

<success_criteria>
- A pipeline-produced Phase 3 pool passes both the offline verifier and the strict nine-artifact contract expected by the app.
- The immutable-content rule is preserved by creating a new versioned pool rather than altering the legacy one.
</success_criteria>

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-09-SUMMARY.md` when done.
</output>
