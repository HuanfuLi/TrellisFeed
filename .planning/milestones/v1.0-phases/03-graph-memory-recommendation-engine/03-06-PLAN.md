---
phase: 03-graph-memory-recommendation-engine
plan: 06
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - app/src/services/recommendation.repository.ts
  - app/src/services/recommendation.service.ts
  - app/tests/services/recommendation.service.test.mjs
autonomous: true
requirements: [RANK-01, RANK-03, RANK-04, RANK-05, RANK-06]
must_haves:
  truths:
    - "recommendationService is the sole feed source: it reads studyContextService.getRequired().condition and branches BEFORE any personal-store read — the control path never constructs, imports, or receives a personal repository (D-10)"
    - "§12.3 test 5 passes: with a throwing personal-store spy injected, two control batches built against two DIFFERENT persisted question histories complete without throwing and produce deep-equal scores/strategies/labels (RANK-01, RANK-06)"
    - "§12.3 test 6 passes: experimental Recommendation rows read back through dbQuery carry contributingQuestionIds/contributingConceptIds/contributingPostIds resolving to real fixture graph IDs (D-06, RANK-06)"
    - "Ranking is batched per session start and per swipe-for-more: each batch materializes field-exact §9.9 Recommendation rows plus a recommendation_batches ledger row (sessionId, seq, ordered IDs, status, diversity counters) — served content is a stable persisted snapshot (D-11)"
    - "Experimental reasonText is LLM-generated via the §17.4 prompt ONLY for selected items, generated once at creation and persisted; re-reads return the stored text with zero further LLM calls (D-06, D-07)"
    - "Control reasons come from the fixed non-personal vocabulary — strategy topic_baseline → 'Related to {conceptLabel}', quality_baseline → 'Popular explanation', diversity_baseline → 'Different viewpoint' — with no LLM call on the control path ever (D-08)"
    - "Reason text validation: single sentence, plain text, length ≤ 280 Unicode code points (counted via code-point iteration, not UTF-16 units), no newline or C0/C1 control characters; invalid LLM output is retried per-item then falls back to a strategy-templated non-personal sentence — RANK-05 encoding probe"
    - "A batch with zero legal candidates persists an empty ready batch (no LLM call, no throw); cold-start experimental batches run the single §11.3 formula with no special-case fallback (D-09) — RANK-05 empty probe"
    - "Nothing is marked ready/served until every selected item has its final reasonText and structured contributing IDs persisted"
    - "Diversity counters persist in the batch ledger and carry into the next batch's selectDiverse call (RANK-04 session-wide enforcement)"
  artifacts:
    - path: "app/src/services/recommendation.repository.ts"
      provides: "Field-exact Recommendation + batch-ledger persistence via dbExecute/dbQuery"
      exports: ["recommendationRepository"]
    - path: "app/src/services/recommendation.service.ts"
      provides: "Single serving seam: condition branch, batch lifecycle, reason generation"
      exports: ["recommendationService"]
  key_links:
    - from: "app/src/services/recommendation.service.ts"
      to: "app/src/services/study-context.service.ts"
      via: "getRequired().condition branch before any personal read"
      pattern: "getRequired"
    - from: "app/src/services/recommendation.service.ts"
      to: "app/src/services/ranking/experimental-ranker.ts"
      via: "pure scoring over assembled ExperimentalRankerInput"
      pattern: "scoreExperimental"
    - from: "app/src/services/recommendation.repository.ts"
      to: "app/src/services/db.service.ts"
      via: "INSERT OR REPLACE into recommendations / recommendation_batches"
      pattern: "recommendation_batches"
  prohibitions:
    - statement: "The control branch must never invoke the LLM, read user_questions/user_concept_states/personal_graph_edges/graph_contributions, or receive graphMemoryService — reason-quality asymmetry between conditions is intended and must not be 'fixed'"
      status: unresolved
    - statement: "reasonText must never reveal internal scores, component values, or weights, and must not overclaim (§17.4 rules) — validation strips/rejects numeric score leakage patterns"
      status: unresolved
    - statement: "Must not re-rank or reshuffle an already-served batch on GRAPH_UPDATED — graph mutations affect the NEXT batch only (D-11 snapshot cadence)"
      status: unresolved
---

<objective>
Build the single recommendation/feed seam (D-10): condition-branched batch materialization persisting field-exact §9.9 Recommendation records with strategies, diversity-selected ordering, and persisted interpretable reasons — experimental via §17.4 LLM with structural trace IDs, control via fixed non-personal labels (RANK-05, D-06–D-11) — plus §12.3 tests 5 and 6.

Purpose: this is the one place both conditions get their feeds; its structure is the study's isolation guarantee.

Output: recommendation.repository.ts, recommendation.service.ts, recommendation.service.test.mjs.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@docs/research_system_design.md
@app/src/services/ranking/experimental-ranker.ts
@app/src/services/graph-memory.service.ts
</context>

## Artifacts this phase produces (plan 03-06 subset)

- Files: `app/src/services/recommendation.repository.ts`, `app/src/services/recommendation.service.ts`, `app/tests/services/recommendation.service.test.mjs`
- Service singletons: `recommendationRepository` (`saveBatch`, `readBatch`, `readSessionBatches`, `readRecommendation`), `recommendationService` (`beginSession`, `nextBatch`, `currentSessionItems`)
- Control label vocabulary constant: `CONTROL_REASON_LABELS` mapping `topic_baseline | quality_baseline | diversity_baseline` to fixed strings
- Reason validation constant: `REASON_MAX_CODE_POINTS = 280`
- Batch size constant: `RECOMMENDATION_BATCH_SIZE = 8` (mirrors the existing swipe-for-more cadence)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recommendation.repository — field-exact rows + batch ledger</name>
  <files>app/src/services/recommendation.repository.ts, app/tests/services/recommendation.service.test.mjs</files>
  <read_first>docs/research_system_design.md (§9.9 Recommendation field-for-field — id, userId, condition, topicId, postId, generatedAt, strategy, score, reasonText, contributingQuestionIds?, contributingConceptIds?, contributingPostIds?, componentScores?), app/src/domain/content.types.ts (existing Recommendation type), app/src/domain/graph.types.ts (RecommendationBatch), app/tests/services/anchor-persistence.test.mjs (dbQuery durability pattern)</read_first>
  <behavior>
    - Test: saveBatch persists N Recommendation rows + one recommendation_batches row; a fresh repository instance reads them back via dbQuery with identical field values
    - Test: Recommendation rows contain ONLY §9.9 fields (assertion iterates Object.keys against the exact allowlist) — batch metadata lives in the ledger row, never on the Recommendation
    - Test: readSessionBatches returns batches ordered by seq with their diversity counters intact
  </behavior>
  <action>
    Create recommendation.repository.ts: saveBatch(batch: RecommendationBatch, recommendations: Recommendation[]) writing INSERT OR REPLACE into recommendations (id, user_id, data) and recommendation_batches (id, user_id, session_id, data); readBatch(batchId), readSessionBatches(userId, sessionId) (SELECT + seq sort), readRecommendation(id). Keep §9.9 field separation per RESEARCH Pattern 7: sessionId/seq/status/counters only on the ledger. ServiceResult<T> convention; stay within the INSERT OR REPLACE / DELETE / SELECT SQL subset.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/recommendation.service.test.mjs --test-name-pattern="repository"</automated>
  </verify>
  <acceptance_criteria>
    - Field-allowlist test passes (no extra keys on persisted Recommendation JSON)
    - Durability asserted through dbQuery after re-instantiation
  </acceptance_criteria>
  <done>Snapshot persistence exact and durable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recommendation.service — condition branch + batch lifecycle</name>
  <files>app/src/services/recommendation.service.ts, app/tests/services/recommendation.service.test.mjs</files>
  <read_first>docs/research_system_design.md (§11.2, §11.7 control-must-not-use list), app/src/services/study-context.service.ts (getRequired), app/src/services/ranking/control-ranker.ts + experimental-ranker.ts + diversity-reranker.ts (pure APIs from 03-04), app/src/services/graph-memory.service.ts (readSnapshot), app/src/services/frozen-feed.service.ts (dismissed-post filtering source), app/src/services/global-graph.repository.ts</read_first>
  <behavior>
    - Test (§12.3 #5): service constructed with a throwing personal-store dependency; seed question history A → control batch 1; seed different history B through the dbQuery seam → control batch 2; assert no throw and deepEqual(projectScoresStrategiesLabels(batch1), projectScoresStrategiesLabels(batch2)); assert dbQuery('SELECT * FROM recommendations') rows exist
    - Test: experimental branch reads snapshot + extracted questions and produces strategy-labeled, diversity-selected batches; cold-start (empty state) produces a valid batch with only deepen/contrast/bridge strategies via the SAME code path (no fallback branch exists to test — asserted by code-path coverage of a single materialize function)
    - Test: beginSession recovers an interrupted 'building' batch instead of duplicating it (idempotent session start); nextBatch increments seq and carries diversity counters forward
    - Test: dismissed posts never appear; batch of size ≤ RECOMMENDATION_BATCH_SIZE; empty candidate pool → empty ready batch, no error
  </behavior>
  <action>
    Create recommendation.service.ts (singleton, injectable dependencies for tests). beginSession(): create sessionId, build batch seq 1. nextBatch(): build next seq with counters from the previous ledger row. Internal materializeBatch(): const identity = studyContextService.getRequired(); if condition === 'control' → assemble ControlRankerInput ONLY from frozen pool features (globalGraphRepository.rankingFeatures), topic, and seen/dismissed session state; scoreControl; select top-N with the same diversity mechanics allowed at session level; assign baseline strategies (planner discretion, documented: highest TopicRelevance component → topic_baseline, highest ContentQuality → quality_baseline, the reserved diversity slot / format-variety pick → diversity_baseline); attach CONTROL_REASON_LABELS text (concept label from the post's frozen primaryConceptId — non-personal topic-level data only); persist via saveBatch; NO personal-store access on this path (the personal dependency is never dereferenced before the branch). Experimental → graphMemoryService.readSnapshot(userId) + extracted questions via dbQuery on user_questions + globalGraphRepository views → generateCandidates → scoreExperimental → selectStrategy → selectDiverse with carried counters → Task 3 reason generation → persist. Single formula always (D-09): no trace-count threshold, no mode switch. Batch status 'building' until reasons complete, then 'ready'.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/recommendation.service.test.mjs --test-name-pattern="control|session|batch"</automated>
  </verify>
  <acceptance_criteria>
    - §12.3 #5 control-isolation test green with throwing spy + byte-equal projection + dbQuery row assertion
    - Comment-filtered gate: `grep -v '^\s*//' app/src/services/recommendation.service.ts | grep -v '^\s*\*' | grep -n "readSnapshot"` shows readSnapshot referenced only inside the experimental branch function (single call site)
    - Session recovery test green (no duplicate seq-1 batches after simulated interrupt)
  </acceptance_criteria>
  <done>One serving seam; branch-before-read; persisted stable batches for both conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reason generation — §17.4 experimental LLM + fixed control labels + §12.3 #6</name>
  <files>app/src/services/recommendation.service.ts, app/tests/services/recommendation.service.test.mjs</files>
  <read_first>docs/research_system_design.md (§17.4 prompt inputs/rules verbatim; §7.7 visibility ceiling), app/src/providers/llm/index.ts (chatCompletion jsonMode; applyUserContentBracketing for question-trace text included in prompts), app/src/services/interaction-log.service.ts (no logging changes here — reason views are plan 03-07)</read_first>
  <behavior>
    - Test (§12.3 #6): with a mocked reason LLM, materialize an experimental batch; dbQuery('SELECT * FROM recommendations') rows have contributingQuestionIds/contributingConceptIds/contributingPostIds that are non-empty where strategy evidence exists and every ID resolves to a fixture graph/question ID
    - Test: reason LLM is called exactly once per batch (batched request), only for selected experimental items; control batch materialization performs zero LLM calls (spy asserts call count 0)
    - Test: invalid LLM item (two sentences, 300+ code points, embedded newline, or a numeric score leak like 'score: 0.83') is retried once then replaced by the strategy-templated fallback sentence; the persisted row always has valid reasonText
    - Test: re-reading a ready batch performs zero LLM calls (D-07 persisted-once)
    - Test: reasonText survives round-trip with multi-code-point content (emoji/CJK) — length counted in code points
  </behavior>
  <action>
    In recommendation.service.ts add generateReasons(selected, evidence): build ONE §17.4-based batched jsonMode request containing per-item: strategy, recommended post title/summary, contributing prior question text (bracketed with applyUserContentBracketing — participant text is untrusted), contributing concept labels, contributing prior post titles; §17.4 rules verbatim (one sentence, no internal scores, helpful not creepy, no overclaim). Validate each returned item by candidate ID: single sentence (no newline/control chars), ≤ REASON_MAX_CODE_POINTS via [...text].length, no digit-colon score-leak pattern; retry only missing/invalid items once; final fallback = deterministic per-strategy template naming the contributing concept label (still experimental-personal via evidence, no fabricated claims). Structured contributing IDs are attached from the strategy/component evidence BEFORE prose generation and persist regardless of prose content (D-06 — the IDs are what §12.3 #6 verifies). componentScores? persisted from the scorer for audit. Control path: reasonText from CONTROL_REASON_LABELS only, contributing* fields omitted (non-personal), zero LLM.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/recommendation.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - §12.3 #6 trace-ID test green through dbQuery
    - LLM-call-count spies: 1 per experimental batch, 0 for control, 0 on re-read
    - Code-point length validation test green
    - Full suite `npm test` green
  </acceptance_criteria>
  <done>RANK-05 satisfied: persisted interpretable reasons, structural trace IDs, condition-correct generation paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| condition branch | personal data must be unreachable on the control side (study-validity boundary) |
| question traces → §17.4 prompt | participant text enters an LLM prompt for reason generation |
| LLM prose → persisted reasonText → UI | untrusted model output becomes participant-visible persisted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Information disclosure | control branch touching personal data | high | mitigate | Branch before dependency dereference; throwing-spy §12.3 test; control input assembled only from frozen features + session state |
| T-03-18 | Tampering | prompt injection via question traces in §17.4 | high | mitigate | applyUserContentBracketing on all participant text; jsonMode + per-item ID validation; invalid items replaced by deterministic templates |
| T-03-19 | Information disclosure | reasonText leaking internal scores or creepy inference | medium | mitigate | Validation rejects score-leak patterns; §17.4 rules in prompt; fallback templates are score-free |
| T-03-20 | Tampering | served-batch integrity (exported study data) | high | mitigate | Batch not 'ready' until reasons + IDs persisted; field-exact §9.9 rows; dbQuery-asserted tests; no post-hoc mutation API on the repository |
</threat_model>

<verification>
- `cd app && node --test tests/services/recommendation.service.test.mjs` green (§12.3 #5 and #6 included)
- `cd app && npm test && npx tsc -b --noEmit` green
</verification>

<success_criteria>
- D-10/D-11 realized: one seam, persisted stable batches, both conditions emitting Recommendation records + reasons.
- §12.3 tests 5–6 complete the six-test RANK-06 set together with plan 03-04.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- **RANK-01 / RANK-04 / RANK-06 unclassified probes (service-level half):** resolved as isolation/counter/trace-ID truths above; rows remain flagged for verifier attention.
- Control baseline-strategy assignment rule (which of the three baseline labels a control item gets) is not specified in §11.7; the mapping in Task 2 is documented discretion using only non-personal signals.
- reasonText language: persisted as generated (content-adjacent data, English canonical like the frozen pool, per Phase 1 content-language decision); UI chrome localization happens in plan 03-07. Flagged for operator awareness alongside RSD §20's open participant-language question.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-06-SUMMARY.md` when done.
</output>
