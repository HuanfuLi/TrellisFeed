---
phase: 03-graph-memory-recommendation-engine
plan: 10
type: execute
wave: 8
depends_on: ["03-09"]
files_modified:
  - app/content-pool.package.json
  - app/scripts/content-pool-package-contract.mjs
  - app/scripts/package-content-pool.mjs
  - app/tests/phase2/frozen-cutover.test.mjs
  - app/src/generated/content-pool-v1/**
  - app/public/content-pool-v1/**
autonomous: true
gap_closure: true
requirements: [GRAPH-01]
must_haves:
  truths:
    - "The standard app prebuild packages the tracked operator-selected immutable pool rather than a script-internal legacy version pin"
    - "Generated and public runtime projections contain exactly manifest.json plus all nine hashed artifacts, including the three Phase 3 graph files"
    - "Changing the selected immutable pool or any projected graph byte makes --check/build fail closed instead of silently retaining stale output"
    - "The selected production projection is pilot-graph-20260718 and the native sync copies byte-identical graph assets"
  artifacts:
    - path: "app/content-pool.package.json"
      provides: "Reviewable operator selection of data/content_pool_graph_20260718 for the production build"
    - path: "app/scripts/content-pool-package-contract.mjs"
      provides: "Reusable ten-file projection contract and side-effect-free packaging function"
    - path: "app/src/generated/content-pool-v1/index.ts"
      provides: "Compiled in-process reader for the selected nine-artifact pool"
  key_links:
    - from: "app/content-pool.package.json"
      to: "app/scripts/package-content-pool.mjs"
      via: "prebuild resolves the selected pool inside repository data/ and validates its immutable manifest"
      pattern: "poolRoot"
    - from: "app/scripts/package-content-pool.mjs"
      to: "app/src/generated/content-pool-v1/index.ts"
      via: "deterministic projection emits every runtime file and selected version"
      pattern: "PACKAGED_CONTENT_POOL_VERSION"
    - from: "app/public/content-pool-v1"
      to: "app/android/app/src/main/assets/public/content-pool-v1"
      via: "Capacitor sync copies the exact verified runtime projection"
      pattern: "content-pool-v1"
  prohibitions:
    - statement: "Must not accept an arbitrary untracked production pool path, a missing graph file, or a manifest version hard-coded in executable logic"
      status: unresolved
    - statement: "Must not project source_files, review_logs, credentials, pipeline runs, or any non-runtime artifact into app assets"
      status: unresolved
---

<objective>
Repair the production package cutover: replace the legacy version/file pin with a reviewable immutable-pool selection and generate the exact nine-artifact app projection from the pool created in Plan 03-09.

Purpose: a correct fresh freeze is unusable if `npm run build` still selects the Phase 2 pool and copies only six artifacts.
Output: tracked package selection, reusable deterministic packager, graph-complete generated/public assets, and invariant-based cutover tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@CLAUDE.md
@AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-graph-memory-recommendation-engine/03-UAT.md
@.planning/debug/phase3-fresh-graph-pool-uat-gap.md
@.planning/phases/03-graph-memory-recommendation-engine/03-09-SUMMARY.md
@.planning/phases/02-content-pool-feed-post-ui-on-frozen-data/02-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make pool selection reviewable and package all nine runtime artifacts</name>
  <files>app/content-pool.package.json, app/scripts/content-pool-package-contract.mjs, app/scripts/package-content-pool.mjs, app/tests/phase2/frozen-cutover.test.mjs</files>
  <read_first>
    - app/scripts/package-content-pool.mjs (legacy poolRoot/version pin, runtimeFiles, deterministic check/write flow)
    - app/scripts/content-pool-package-validation.mjs (existing base reference validation)
    - app/src/data/content-pool-bundle.ts (`CONTENT_POOL_FILENAMES`, exact hash/shape/reference validation)
    - app/tests/phase2/frozen-cutover.test.mjs (legacy version and seven-file assertions that must become invariants)
    - data/content_pool_graph_20260718/manifest.json (selected version and fixed inventory from Plan 03-09)
  </read_first>
  <behavior>
    - "The packager reads the selected pool from a tracked config, accepts its manifest version without a code pin, and emits exactly ten runtime files including manifest.json."
    - "Packaging fails when any graph artifact is absent, its artifactHashes entry is absent/mismatched, or the selected source contains an extra/missing immutable file."
    - "A side-effect-free package function can target temporary generated/public roots so tests do not overwrite checked-in production projections."
    - "The production selection must resolve to an existing directory inside the repository data directory; path traversal and absolute external selections fail."
  </behavior>
  <action>
    RED: revise frozen-cutover.test.mjs so it derives the expected version from the selected manifest, asserts the exact ten-file runtime projection, checks graph hashes in generated/public/native assets, and rejects stale graph output. Remove assertions whose only purpose is to pin `pilot-v1-20260717` or the seven-file Phase 2 projection.

    GREEN: add `content-pool.package.json` with one repository-relative `poolRoot` selecting `../data/content_pool_graph_20260718`. Add a side-effect-free packager module exporting the exact runtime file list and a function that accepts explicit source/generated/public roots for executable tests. Keep `package-content-pool.mjs` as the CLI/prebuild wrapper: resolve and realpath the tracked production selection inside repo `data/`, read the manifest version, require exact fixed-file inventory and SHA-256 values, parse and runtime-hash all nine artifacts, run the existing package reference validation, then generate or `--check` the exact output. Do not introduce live acquisition or import pipeline code into app/src. The generated TypeScript reader must expose every runtime filename required by `CONTENT_POOL_FILENAMES`.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test tests/phase2/frozen-cutover.test.mjs --test-name-pattern="deterministic package|participant content" &amp;&amp; node scripts/package-content-pool.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - Production pool selection is an explicit tracked file and cannot escape repo data/.
    - No executable equality check pins a single manifest version string.
    - Packager and test share one exact ten-file projection contract; all nine artifact hashes are verified.
    - Temporary-root test proves stale/missing graph projection fails without touching production outputs.
  </acceptance_criteria>
  <done>The standard prebuild can package any explicitly selected new immutable pool and fails closed on incomplete or stale graph projections.</done>
</task>

<task type="auto">
  <name>Task 2: Cut production and native assets over to the graph pool</name>
  <files>app/src/generated/content-pool-v1/**, app/public/content-pool-v1/**, app/tests/phase2/frozen-cutover.test.mjs</files>
  <read_first>
    - app/content-pool.package.json (selected immutable source)
    - app/scripts/package-content-pool.mjs (`--check` and generation semantics from Task 1)
    - app/capacitor.config.ts (native webDir and platform sync contract)
  </read_first>
  <action>
    Run the package script normally to replace only the generated/public projections with the selected `pilot-graph-20260718` bytes. Confirm the output roots contain no files beyond the ten runtime files plus generated `index.ts` under src/generated. Run production build and `npx cap sync android`; extend/retain the cutover test so dist and Android assets are exact SHA-256 matches for all ten selected files and contain no source_files, review_logs, pipeline artifacts, environment files, or secret-like values. Do not edit the immutable source pool during projection.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm run package:content-pool &amp;&amp; npm run build &amp;&amp; npx cap sync android &amp;&amp; node --test tests/phase2/frozen-cutover.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Generated index exports `PACKAGED_CONTENT_POOL_VERSION` from the selected manifest and includes sources/global edges/ranking features.
    - Public, dist, and Android web assets contain exactly ten runtime files with byte-identical hashes to the selected immutable source.
    - `node scripts/package-content-pool.mjs --check` passes immediately after build/sync.
    - No operator-only source/review/run/credential material appears under generated, public, dist, or Android web assets.
  </acceptance_criteria>
  <done>The normal app build and Android sync now ship the verified graph-complete pool projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tracked pool selection → production package | A reviewed repository path decides participant-visible immutable content. |
| Immutable source → generated/public/native assets | Only the exact runtime projection may cross into the participant bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-29 | Tampering | poolRoot selection | high | mitigate | Tracked config; canonical path containment under repo data/; exact immutable inventory verification. |
| T-03-30 | Tampering | stale generated graph assets | high | mitigate | `--check` byte comparison plus exact graph hash assertions across generated/public/dist/native roots. |
| T-03-31 | Information disclosure | package projection | high | mitigate | Ten-file allowlist and scans excluding run, source, review, env, and credential material. |
</threat_model>

<verification>
- `cd app && node scripts/package-content-pool.mjs --check`
- `cd app && node --test tests/phase2/frozen-cutover.test.mjs`
- `cd app && npm run build && npx cap sync android`
- Hash parity covers manifest plus all nine runtime artifacts in public, dist, and Android web assets.
</verification>

<success_criteria>
- Standard app prebuild no longer depends on the legacy version or seven-file projection.
- The production and Android bundles contain a verified nine-artifact graph pool and no operator-only material.
</success_criteria>

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-10-SUMMARY.md` when done.
</output>
