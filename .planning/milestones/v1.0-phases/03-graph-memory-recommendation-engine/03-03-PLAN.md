---
phase: 03-graph-memory-recommendation-engine
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/src/services/graph-memory.service.ts
  - app/src/services/interaction-log.service.ts
  - app/tests/services/graph-memory.service.test.mjs
autonomous: true
requirements: [GRAPH-02]
must_haves:
  truths:
    - "Each persisted UserInteractionEvent applies its §10.6 delta immediately via an idempotent contribution ledger and the updated UserConceptState row is durable through dbQuery (D-02)"
    - "interestWeight deltas are the exact §10.6 values: +0.05 feed_impression, +0.10 post_open, +0.20 source_click / qualifying video_progress, +0.30 question involving concept, +0.25 save_post, -0.15 not_interested, -0.10 repeated skip; result clamps to [0,1]"
    - "Applying the same UserInteractionEvent twice produces byte-identical UserConceptState rows (contribution key = eventId:conceptId:rule, INSERT OR REPLACE, state rebuilt from contributions) — GRAPH-02 idempotency probe"
    - "A crash or retry between contribution write and state rebuild cannot double-apply weight: state is always recomputed from the full contribution set for the affected concept, and replayFromLog() rebuilds identical state from the durable research_records event log (D-03) — GRAPH-02 concurrency probe"
    - "UserConceptState rows are field-exact per §10.5 (userId, conceptId, exposureCount, questionCount, savedPostCount, skippedPostCount, lastActivatedAt?, interestWeight, uncertaintyWeight, familiarityEstimate)"
    - "snapshot() returns a serializable dump of all personal graph state (UserConceptState rows + personal edges + contributions) suitable for research export (D-03)"
    - "GRAPH_UPDATED is emitted with payload kind 'interaction' and affectedIds after each committed state change — no new event type"
    - "A graph-memory failure never fails or blocks interaction logging (error-isolated hook)"
  artifacts:
    - path: "app/src/services/graph-memory.service.ts"
      provides: "Contribution ledger, §10.6 weight updates, personal edges, snapshot, replay"
      exports: ["graphMemoryService"]
    - path: "app/tests/services/graph-memory.service.test.mjs"
      provides: "Idempotency, replay-equality, durability-through-dbQuery tests"
  key_links:
    - from: "app/src/services/interaction-log.service.ts"
      to: "app/src/services/graph-memory.service.ts"
      via: "post-persist hook applies the event's contribution (fire-and-forget, error-isolated)"
      pattern: "graphMemoryService"
    - from: "app/src/services/graph-memory.service.ts"
      to: "app/src/services/db.service.ts"
      via: "INSERT OR REPLACE into graph_contributions / user_concept_states / personal_graph_edges"
      pattern: "graph_contributions"
  prohibitions:
    - statement: "Personal graph state must never be readable by the control ranker path — this service's outputs feed only the experimental input assembly (DEC-control-no-question-history)"
      status: unresolved
    - statement: "Must not use read-modify-write arithmetic on UserConceptState — every update flows through the contribution ledger so replay and crash-retry stay idempotent"
      status: unresolved
---

<objective>
Build the personal graph-memory substrate (GRAPH-02): an event-driven, incremental, replayable contribution ledger that maintains field-exact §10.5 UserConceptState rows and §10.4 personal edges from logged UserInteractionEvents (D-02), serializable for analysis snapshots (D-03).

Purpose: this is what the experimental ranker reads. Determinism and replayability make the treatment auditable and the §12.3 tests executable.

Output: graph-memory.service.ts, interaction-log hook, tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@docs/research_system_design.md
@app/src/services/interaction-log.service.ts
@app/src/domain/graph.types.ts
</context>

## Artifacts this phase produces (plan 03-03 subset)

- Files: `app/src/services/graph-memory.service.ts`, `app/tests/services/graph-memory.service.test.mjs`
- Service singleton: `graphMemoryService` with methods `applyEvent(event)`, `applyQuestionExtraction(question, conceptIds, claimIds)`, `readSnapshot(userId)`, `snapshot()` (serializable dump), `replayFromLog(userId)`, `conceptState(userId, conceptId)`
- Config constant: `GRAPH_MEMORY_RULES` (the §10.6 delta table + discretionary uncertainty/familiarity deltas)
- Contribution key format: `${eventId}:${conceptId}:${rule}`
- Event payload: `GRAPH_UPDATED` with `kind: 'interaction'`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: graph-memory.service — contribution ledger + §10.6 weight rules + personal edges</name>
  <files>app/src/services/graph-memory.service.ts, app/tests/services/graph-memory.service.test.mjs</files>
  <read_first>docs/research_system_design.md (§10.5 UserConceptState field-for-field, §10.6 exact interest deltas + qualitative uncertainty/familiarity rules, §10.4 personal edge names), app/src/domain/graph.types.ts (GraphContribution, PersonalEdge types from 03-02), app/src/services/interaction-log.service.ts (UserInteractionEvent shape, EVENT_FIELDS allowlist), app/tests/services/anchor-persistence.test.mjs (dbQuery durability pattern), app/src/services/db.service.ts (SQL subset: INSERT OR REPLACE / DELETE / SELECT only)</read_first>
  <behavior>
    - Test: post_open event for a post with concepts [c1, c2] writes contributions eventId:c1:post_open and eventId:c2:post_open, and both UserConceptState rows show interestWeight 0.10 and exposureCount/lastActivatedAt updated — asserted via dbQuery on user_concept_states
    - Test: applying the identical event object twice yields identical dbQuery rows (idempotency)
    - Test: interestWeight clamps at 1.0 (repeated +0.30 contributions) and at 0.0 (repeated negatives)
    - Test: not_interested on the same concept twice increments skippedPostCount both times but applies the -0.10 repeated-skip delta only from the second occurrence (first is -0.15 not_interested only)
    - Test: replayFromLog(userId) over a seeded research_records event log reproduces byte-identical user_concept_states rows after clearing derived tables
    - Test: personal edges are written per §10.4 names — save_post → saved, post_open → viewed (revisited on second open of same post), not_interested → skipped
  </behavior>
  <action>
    Create graph-memory.service.ts (singleton graphMemoryService, ServiceResult<T> style). applyEvent(event: UserInteractionEvent): resolve the event's affected conceptIds (from the event's postId via globalGraphRepository rankingFeatures/mentions edges, or event.payload conceptIds when present); for each (conceptId, rule) write a GraphContribution with id `${event.id}:${conceptId}:${rule}` via INSERT OR REPLACE into graph_contributions; then rebuild the affected UserConceptState rows by folding ALL contributions for that (userId, conceptId) — counts (exposureCount from feed_impression, questionCount from question_submit, savedPostCount from save_post, skippedPostCount from not_interested), interestWeight from the exact §10.6 delta table clamped to [0,1], uncertaintyWeight/familiarityEstimate from GRAPH_MEMORY_RULES; INSERT OR REPLACE into user_concept_states; write personal_graph_edges rows; then emit GRAPH_UPDATED { kind: 'interaction', affectedIds }. Emission happens only after persistence succeeds.
    GRAPH_MEMORY_RULES (discretionary values — locked here, cited as planner discretion per CONTEXT "Claude's Discretion"): uncertainty +0.15 for questionType clarification/confusion, +0.10 for a repeated similar question on the same concept; familiarity +0.05 per repeat exposure at exposureCount ≥ 3, +0.10 for deeper questionTypes (evidence, counterpoint, implication), +0.10 for a followed echo recommendation; both clamp [0,1]. video_progress qualifies for +0.20 when durationMs-derived progress exceeds 0.5 (config constant VIDEO_PROGRESS_THRESHOLD = 0.5).
    applyQuestionExtraction(question, conceptIds, claimIds): writes asks_about personal edges + question contributions (rule question_submit, +0.30) — called by plan 03-05's extraction service. replayFromLog(userId): reads events from research_records via dbQuery ordered by timestamp, clears derived rows for the user, re-applies. snapshot(): serializable JSON of user_concept_states + personal_graph_edges + graph_contributions (D-03). readSnapshot(userId): in-memory view for the experimental ranker input.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/graph-memory.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior test above passes; durability asserted via dbQuery (`grep -c "dbQuery" app/tests/services/graph-memory.service.test.mjs` ≥ 3)
    - The exact §10.6 delta constants appear once in GRAPH_MEMORY_RULES: 0.05, 0.10, 0.20, 0.30, 0.25, -0.15, -0.10
    - `grep -c "INSERT OR REPLACE INTO graph_contributions" app/src/services/graph-memory.service.ts` ≥ 1
    - No read-modify-write of user_concept_states without ledger fold (state writes only occur inside the rebuild function)
  </acceptance_criteria>
  <done>Ledger + field-exact state + personal edges durable, idempotent, replayable; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Interaction-log hook + boot repair wiring</name>
  <files>app/src/services/interaction-log.service.ts, app/tests/services/graph-memory.service.test.mjs</files>
  <read_first>app/src/services/interaction-log.service.ts (record() flow: storeAndEnqueue then return — hook goes after successful persistence; the service must stay Phase 1-stable for both conditions), app/src/services/graph-memory.service.ts (Task 1 API)</read_first>
  <action>
    In interaction-log.service.ts record(): after storeAndEnqueue(event, 'event') succeeds, invoke graphMemoryService.applyEvent(event) as fire-and-forget with a caught/logged error path — a graph-memory failure must never reject or delay the logging promise (D-02: log is canonical; derived state is repairable). Import graphMemoryService lazily (dynamic import) to avoid a load-order cycle with study-context. Add graphMemoryService.repairOnBoot(): scans research_records events for contribution keys missing from graph_contributions and applies them (covers events logged while a previous applyEvent failed); export it for App.tsx wiring in plan 03-05's boot task (do not touch App.tsx in this plan — it is owned by 03-05 in wave 4).
    Add tests: (a) a throwing graphMemoryService stub does not fail interactionLog.record(); (b) repairOnBoot fills a deliberately deleted contribution and converges state to the replay result.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/graph-memory.service.test.mjs tests/services/interaction-log.service.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - interactionLog.record() resolves successfully when applyEvent throws (test asserts)
    - Existing interaction-log.service tests remain green (Phase 1 contract untouched: same return values, same EVENT_FIELDS)
    - repairOnBoot test converges to replayFromLog output (deepEqual on dbQuery rows)
  </acceptance_criteria>
  <done>Every logged event feeds the ledger without coupling logging reliability to graph health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interaction events → derived personal state | behavioral data becomes ranker input and exportable study data |
| derived state → research export (D-03) | snapshot must be faithful to the canonical log |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | double-applied weight after crash/retry | high | mitigate | Stable contribution keys + INSERT OR REPLACE + full-fold rebuild; replay/repair tests prove convergence (IndexedDB export-data integrity requirement) |
| T-03-08 | Information disclosure | personal state read by control path | high | mitigate | Service exposes readSnapshot(userId) only; plan 03-06 gives the control branch an input type with no access to this service; §12.3 throwing-spy test enforces |
| T-03-09 | Repudiation | derived state diverging from canonical log | medium | mitigate | replayFromLog + repairOnBoot reconstruct state from research_records; snapshot() is derivable, log remains source of truth (D-03) |
</threat_model>

<verification>
- `cd app && node --test tests/services/graph-memory.service.test.mjs` green
- `cd app && npm test` green (interaction-log Phase 1 contract intact)
</verification>

<success_criteria>
- GRAPH-02 satisfied: personal edges + field-exact UserConceptState updating per §10.6 from question/interaction traces, idempotent, replayable, serializable.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- **GRAPH-02 idempotency + concurrency probes:** resolved as covered via the ledger design (truths above).
- Uncertainty/familiarity numeric deltas are qualitative in §10.6; values locked in GRAPH_MEMORY_RULES as documented discretion (RESEARCH open question 3) — tunable in config without architectural change.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-03-SUMMARY.md` when done.
</output>
