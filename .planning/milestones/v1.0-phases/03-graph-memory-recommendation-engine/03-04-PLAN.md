---
phase: 03-graph-memory-recommendation-engine
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/src/services/recommendation-config.ts
  - app/src/services/ranking/control-ranker.ts
  - app/src/services/ranking/experimental-ranker.ts
  - app/src/services/ranking/diversity-reranker.ts
  - app/tests/services/ranking-components.test.mjs
  - app/tests/services/diversity-reranker.test.mjs
autonomous: true
requirements: [RANK-01, RANK-02, RANK-03, RANK-04, RANK-06]
must_haves:
  truths:
    - "scoreControl computes the exact §11.7 formula (0.40 ContentQuality + 0.25 TopicRelevance + 0.15 GeneralInterestingness + 0.10 DifficultyBalance + 0.10 Diversity - 0.20 RecentlySeenPenalty) over a ControlRankerInput type that structurally contains no question history, no UserConceptState, no unresolved tracking, no echo evidence, and no personal-graph API (RANK-01)"
    - "scoreExperimental computes the exact §11.3 formula (0.25 QuestionRelevance + 0.20 ConceptInterestMatch + 0.15 ContinuityWithRecentPosts + 0.15 NoveltyOrContrast + 0.15 ContentQuality + 0.10 DifficultyFit - 0.20 RedundancyPenalty) with all seven §11.4 components returning normalized [0,1] values plus contributor evidence; weights come from an injected RecommendationConfig (RANK-02)"
    - "Every experimental candidate receives exactly one strategy from {continue, deepen, contrast, bridge, echo} by maximum affinity; exact affinity ties resolve by the fixed precedence order continue > echo > contrast > bridge > deepen (RANK-03 adjacency probe)"
    - "Echo eligibility is inclusive at the boundary: a prior question aged exactly echoMinAgeMs IS eligible; one millisecond younger is NOT (fake-clock tested both sides) — RANK-03 boundary probe"
    - "With zero traces (cold start), continue and echo are structurally unavailable, deepen/contrast/bridge derive from global evidence, and no experimental item ever carries a baseline_* strategy (D-09) — RANK-03 empty probe"
    - "Candidates with equal final scores order deterministically by postId ascending; all scores clamp to [0,1] and equal-score comparison uses the explicit postId tie-break, never float-equality heuristics — RANK-03 ordering + precision probes"
    - "§12.3 test 1 passes: QuestionRelevance and total score rise for a post sharing a question's target concept vs an identical fixture without it (RANK-06)"
    - "§12.3 test 2 passes: contrast candidate generation includes posts linked to opposing claims via contrasts_with edges, labeled strategy contrast (RANK-06)"
    - "§12.3 test 3 passes: RedundancyPenalty ranks a near-duplicate (shared claim / same source / high vector similarity) below a distinct post (RANK-06)"
    - "§12.3 test 4 passes: echo requires prior questions older than the threshold (RANK-06)"
    - "Diversity selection enforces ≤2 same-source per session and ≤2 same-primary-concept in a row using counters carried ACROSS batches, reserves one contrast/bridge slot after sufficient history, and returns a smaller batch rather than violating a hard cap (RANK-04)"
    - "When frozen summary vectors are absent or the runtime embedding fingerprint mismatches, semantic subcomponents renormalize their subweights deterministically instead of comparing cross-model vectors"
  artifacts:
    - path: "app/src/services/recommendation-config.ts"
      provides: "Immutable DEFAULT_RECOMMENDATION_CONFIG: §11.3 + §11.7 weights, echoMinAgeMs, diversity caps, tie-break order, subweights, thresholds"
      exports: ["DEFAULT_RECOMMENDATION_CONFIG", "RecommendationConfig"]
    - path: "app/src/services/ranking/control-ranker.ts"
      provides: "Pure §11.7 control scorer with narrow input type"
      exports: ["scoreControl", "ControlRankerInput"]
    - path: "app/src/services/ranking/experimental-ranker.ts"
      provides: "Seven §11.4 components, §11.3 scorer, §11.2 candidate generation, §11.5 strategy selection"
      exports: ["scoreExperimental", "generateCandidates", "selectStrategy", "ExperimentalRankerInput"]
    - path: "app/src/services/ranking/diversity-reranker.ts"
      provides: "Deterministic greedy §11.6 selector with cross-batch session counters"
      exports: ["selectDiverse"]
  key_links:
    - from: "app/src/services/ranking/experimental-ranker.ts"
      to: "app/src/services/recommendation-config.ts"
      via: "weights injected, never hardcoded in component bodies"
      pattern: "RecommendationConfig"
  prohibitions:
    - statement: "ControlRankerInput must never gain a field carrying question history, concept weights derived from questions, unresolved status, echo evidence, or a graph-traversal API — the control formula's strength comes only from static pool features and allowed session seen/dismissed state"
      status: unresolved
    - statement: "No participant-facing weight-tuning UI — weights are configurable via injected config only (deferred idea stays deferred)"
      status: unresolved
---

<objective>
Implement the pure ranking core: control §11.7 scorer with a structurally isolated input type, experimental §11.3/§11.4 scorer with seven evidence-bearing components, §11.2 candidate generation, §11.5 single-strategy selection with the echo age gate, and §11.6 diversity reranking — plus four of the six §12.3 algorithm-verification tests (RANK-01..04, RANK-06 part 1).

Purpose: pure functions make condition isolation and formula fidelity executable as fast unit tests; the service wiring (plan 03-06) then only assembles inputs.

Output: recommendation-config.ts, ranking/ modules, two test suites.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@docs/research_system_design.md
@app/src/domain/graph.types.ts
</context>

## Artifacts this phase produces (plan 03-04 subset)

- Files: `app/src/services/recommendation-config.ts`, `app/src/services/ranking/control-ranker.ts`, `app/src/services/ranking/experimental-ranker.ts`, `app/src/services/ranking/diversity-reranker.ts`, `app/tests/services/ranking-components.test.mjs`, `app/tests/services/diversity-reranker.test.mjs`
- Exported symbols: `DEFAULT_RECOMMENDATION_CONFIG`, `RecommendationConfig`, `scoreControl`, `ControlRankerInput`, `scoreExperimental`, `generateCandidates`, `selectStrategy`, `ExperimentalRankerInput`, `ComponentResult` (`{ value, evidence }`), `selectDiverse`, `DiversitySessionCounters`
- Config keys: `weights` (§11.3 seven keys), `controlWeights` (§11.7 six keys), `echoMinAgeMs` (86400000), `diversity.maxSameSourcePerSession` (2), `diversity.maxSamePrimaryConceptRun` (2), `diversity.sufficientHistoryQuestionCount` (3), `strategyTieBreakOrder` (['continue','echo','contrast','bridge','deepen']), `videoProgressThreshold`, `contentQualitySubweights`, `redundancyVectorThreshold` (0.85), `difficultyFitCurve`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecommendationConfig + control ranker (§11.7) with narrow input type</name>
  <files>app/src/services/recommendation-config.ts, app/src/services/ranking/control-ranker.ts, app/tests/services/ranking-components.test.mjs</files>
  <read_first>docs/research_system_design.md (§11.7 exact formula + the explicit control-must-NOT-use list; §11.3 weights; §11.6 caps; §11.5 echo threshold requirement), app/src/domain/graph.types.ts (PostRankingFeatures), app/src/domain/content.types.ts (Post, Topic)</read_first>
  <behavior>
    - Test: scoreControl reproduces §11.7 exactly for hand-computed fixtures (weights 0.40/0.25/0.15/0.10/0.10 minus 0.20 penalty), clamped to [0,1]
    - Test: ControlRankerInput type contains only { posts/features, topic, seenPostIds, dismissedPostIds, sessionServed } — a compile-level assertion file confirms no personal fields (see acceptance criteria)
    - Test: two calls with identical input produce identical output (purity/determinism)
    - Test: empty candidate list returns empty ranked list (no throw)
  </behavior>
  <action>
    Create recommendation-config.ts: deep-frozen DEFAULT_RECOMMENDATION_CONFIG holding every key listed in "Artifacts this phase produces" with the exact §11.3 weights (questionRelevance 0.25, conceptInterestMatch 0.20, continuityWithRecentPosts 0.15, noveltyOrContrast 0.15, contentQuality 0.15, difficultyFit 0.10, redundancyPenalty 0.20) and §11.7 control weights (contentQuality 0.40, topicRelevance 0.25, generalInterestingness 0.15, difficultyBalance 0.10, diversity 0.10, recentlySeenPenalty 0.20). echoMinAgeMs 86400000 (24h pilot default per RESEARCH). Discretionary component definitions are config-visible constants, documented per CONTEXT "Claude's Discretion".
    Create ranking/control-ranker.ts: scoreControl(input: ControlRankerInput, config): ranked candidates with componentScores. Component definitions (non-personal by construction, planner discretion documented in code comments): ContentQuality = subweighted mean of qualityScore/educationalValueScore/interestingnessScore; TopicRelevance = overlap of post conceptIds with topic.coreConceptIds (normalized); GeneralInterestingness = interestingnessScore; DifficultyBalance = 1 - |difficulty - 0.5| * 2 clamped; Diversity = 1 - normalized session repetition of the post's sourceId/format among sessionServed; RecentlySeenPenalty = 1 when postId in seenPostIds else 0. All statics come from frozen PostRankingFeatures; allowed session state per §11.7 is seen/dismissed/served only.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/ranking-components.test.mjs --test-name-pattern="control"</automated>
  </verify>
  <acceptance_criteria>
    - Hand-computed §11.7 fixture matches to 1e-9
    - Comment-filtered isolation gate: `grep -v '^\s*//' app/src/services/ranking/control-ranker.ts | grep -v '^\s*\*' | grep -c -i -E "UserConceptState|unresolved|graphMemory|readSnapshot|UserQuestion"` returns 0 (no personal construct named in code; comment lines excluded so a rationale comment cannot trip the gate)
    - `grep -c "0.40\|0.25\|0.15\|0.10\|0.20" app/src/services/recommendation-config.ts` ≥ 5 (weights live in config, not the ranker body)
    - tsc green; targeted test green
  </acceptance_criteria>
  <done>Control scoring pure, exact, and structurally non-personal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Experimental ranker — seven components, candidate generation, strategy selection</name>
  <files>app/src/services/ranking/experimental-ranker.ts, app/tests/services/ranking-components.test.mjs</files>
  <read_first>docs/research_system_design.md (§11.2 candidate sources, §11.3 formula, §11.4 all seven component input definitions, §11.5 five strategies with semantics), app/src/services/recommendation-config.ts (Task 1), app/src/providers/embedding/index.ts (cosine helper — reuse, do not reimplement), app/src/domain/graph.types.ts</read_first>
  <behavior>
    - Test (§12.3 #1): two identical fixtures except one post shares a prior question's extracted target concept — QuestionRelevance component value AND total score are strictly higher for the sharing post
    - Test (§12.3 #2): a fixture with a contrasts_with edge between claim A (supported by viewed post) and claim B (supported by candidate) — generateCandidates includes the candidate with contrast evidence and selectStrategy labels it contrast
    - Test (§12.3 #3): near-duplicate fixture (shared claim + same sourceId + summaryVector cosine above redundancyVectorThreshold) ranks below a distinct post of equal base quality
    - Test (§12.3 #4): with a fake now(), a prior question aged echoMinAgeMs - 1 yields no echo strategy; aged exactly echoMinAgeMs yields echo eligibility (inclusive boundary)
    - Test: exact affinity tie between two strategies resolves by config.strategyTieBreakOrder
    - Test: cold-start input (no questions, empty concept states) produces only deepen/contrast/bridge strategies, never continue/echo, never a baseline_* label (D-09)
    - Test: equal final scores order by postId ascending (stable, deterministic)
    - Test: fingerprint mismatch (input embeddingFingerprint differs from config runtime fingerprint) renormalizes QuestionRelevance/RedundancyPenalty subweights — output is still in [0,1] and deterministic
  </behavior>
  <action>
    Create ranking/experimental-ranker.ts as pure functions over ExperimentalRankerInput = { candidates: PostRankingFeatures + Post metadata, globalEdges index views, questions (extracted UserQuestions), conceptStates (UserConceptState map), recentServed/viewed post history, now }. Implement each §11.4 component returning ComponentResult { value: [0,1], evidence: { questionIds?, conceptIds?, postIds?, claimIds? } } per the RESEARCH Pattern 5 table: QuestionRelevance = max over prior questions of (semantic similarity via cosine of question vector vs summaryVector when fingerprints match; target-concept overlap; supports/challenges relation to asked-about claims; unresolved boost) with subweight renormalization when the vector leg is unavailable; ConceptInterestMatch = weighted mean of post-concept interestWeight plus one-hop related_to/prerequisite_of proximity; ContinuityWithRecentPosts = shared/one-hop concept + claim relation + stance/source transition vs recentServed; NoveltyOrContrast = max of opposing-claim edge, viewpoint change, underexplored neighbor concept; ContentQuality = same subweighted static as control; DifficultyFit = 1 - |difficulty - target(familiarity mean)| via config.difficultyFitCurve; RedundancyPenalty = max of vector similarity vs viewed, repeated claim, repeated sourceId, repeated format. scoreExperimental applies §11.3 weights, subtracts the penalty term, clamp01. generateCandidates implements §11.2: exclude dismissed, penalize recently viewed (do not exclude), union candidates from high quality / active concepts / question links / claim support-challenge / bridge pairs / echo-eligible, deduped by postId with merged evidence. selectStrategy: affinity per §11.5 — continue requires a direct question link; deepen from repeated/high-weight concepts (cold-start default); contrast requires opposing claim/viewpoint evidence; bridge requires two separately activated concepts on one post; echo requires a prior question with (now - createdAt) >= echoMinAgeMs; max affinity wins, ties via strategyTieBreakOrder; attach the winning evidence IDs to the result (these become the Recommendation contributing IDs in plan 03-06).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/ranking-components.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - §12.3 tests 1–4 pass with the exact fixture shapes described in behavior
    - Every component returns value in [0,1] for randomized fixture sweeps (property-style loop in test)
    - selectStrategy never returns a baseline label for experimental input (test asserts against the full RecommendationStrategy union)
    - `grep -c "import { cosine" app/src/services/ranking/experimental-ranker.ts` = 1 (reuses provider cosine)
  </acceptance_criteria>
  <done>Experimental scoring + strategies pure and §12.3-verified (tests 1–4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Diversity reranker (§11.6) with cross-batch session counters</name>
  <files>app/src/services/ranking/diversity-reranker.ts, app/tests/services/diversity-reranker.test.mjs</files>
  <read_first>docs/research_system_design.md (§11.6 four constraints), app/src/services/recommendation-config.ts (diversity caps), app/src/domain/graph.types.ts (RecommendationBatch.diversityCounters shape)</read_first>
  <behavior>
    - Test: a candidate pool with 3 posts from one source yields at most 2 selected in a session — including when the first was selected in a PREVIOUS batch (counters carried in)
    - Test: three consecutive candidates sharing primaryConceptId — only 2 adjacent selected, the third deferred or replaced (run cap applies across the batch boundary via recentPrimaryConceptIds)
    - Test: with sufficientHistoryQuestionCount met, the output contains ≥1 contrast or bridge item when any legal one exists in the pool
    - Test: when hard caps make a full batch impossible, output is SHORTER than requested, never violating a cap
    - Test: format mixing is a soft preference — a pool of all-article candidates still fills the batch
    - Test: deterministic — same input + counters → identical output ordering
  </behavior>
  <action>
    Create ranking/diversity-reranker.ts: selectDiverse(scored, batchSize, counters: DiversitySessionCounters, config) → { selected, nextCounters }. Greedy over score-sorted candidates (postId tie-break): skip a candidate that would exceed maxSameSourcePerSession (counting counters.sourceCounts) or create a >maxSamePrimaryConceptRun adjacent run (counting counters.recentPrimaryConceptIds carried from the previous batch); after the greedy pass, if history is sufficient (question count ≥ sufficientHistoryQuestionCount) and no contrast/bridge item selected, swap the lowest-scoring selected item for the highest-scoring legal contrast/bridge candidate; prefer format variety only as a tie-preference among equal-score legal candidates. Returns updated counters for persistence in the batch ledger (plan 03-06). Pure function, no I/O.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/diversity-reranker.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass; `node --test tests/services/diversity-reranker.test.mjs` green in <30s
    - selectDiverse performs no imports from db.service or any repository (grep for "db.service" in the file returns 0)
  </acceptance_criteria>
  <done>§11.6 constraints enforced deterministically across batch boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| personal traces → experimental scoring | personal data enters ranking only via ExperimentalRankerInput |
| condition boundary | control input type is the structural guarantee of the study's isolated variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information disclosure | control ranker consuming personal data | high | mitigate | Narrow ControlRankerInput type with no personal fields; grep-gated source assertion; §12.3 throwing-spy integration test lands in plan 03-06 |
| T-03-11 | Tampering | cross-model cosine producing invalid scores | medium | mitigate | Fingerprint match required for vector legs; deterministic subweight renormalization otherwise; property test keeps outputs in [0,1] |
| T-03-12 | Elevation of privilege | caller-supplied condition/weights overriding study config | medium | mitigate | Rankers take injected immutable config; condition branching happens only in plan 03-06's service via studyContextService.getRequired() |
</threat_model>

<verification>
- `cd app && node --test tests/services/ranking-components.test.mjs tests/services/diversity-reranker.test.mjs` green
- `cd app && npx tsc -b --noEmit` green
</verification>

<success_criteria>
- RANK-01/02/03/04 pure logic complete and §12.3 tests 1–4 (of RANK-06) pass.
- Plan 03-06 can assemble inputs and persist outputs without touching scoring logic.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- **RANK-01 / RANK-02 / RANK-04 / RANK-06 unclassified probes:** resolved as concrete formula/caps/test truths above; rows remain flagged for verifier attention.
- §11.7 control component sub-definitions (TopicRelevance, DifficultyBalance, Diversity, RecentlySeenPenalty) and §11.4 sub-formulas are RSD-named but not formula-locked; definitions chosen here are documented discretion (CONTEXT "Claude's Discretion"), config-visible, and non-personal by construction.
- "Sufficient history" (§11.6) locked as ≥3 extracted user questions — pilot-tunable config.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-04-SUMMARY.md` when done.
</output>
