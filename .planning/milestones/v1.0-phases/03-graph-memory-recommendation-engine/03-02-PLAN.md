---
phase: 03-graph-memory-recommendation-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/domain/graph.types.ts
  - app/src/types/index.ts
  - app/src/services/db.service.ts
  - app/src/data/content-pool-bundle.ts
  - app/src/services/content-pool.repository.ts
  - app/src/services/global-graph.repository.ts
  - app/tests/services/global-graph.repository.test.mjs
autonomous: true
requirements: [GRAPH-01]
must_haves:
  truths:
    - "The app bundle reader accepts sources.json, global_edges.json, ranking_features.json under the existing hash/version verification, and pool import persists them into dedicated content_pool_* stores through dbExecute (D-01)"
    - "globalGraphRepository answers type-indexed queries (edges by type, edges from/to a node, opposing claims via contrasts_with, one-hop concept relations, ranking features by postId) from IndexedDB-persisted rows — durability asserted through dbQuery, not in-memory mirrors"
    - "Import rejects a bundle whose global edges reference node IDs absent from the imported pool (GRAPH-01 endpoint validation at the runtime boundary too)"
    - "IDB_VERSION is bumped to 6 and all nine new Phase 3 tables exist after upgrade: content_pool_sources, content_pool_global_edges, content_pool_ranking_features, user_concept_states, graph_contributions, personal_graph_edges, extraction_jobs, recommendations, recommendation_batches"
    - "GRAPH_UPDATED payload kind union is extended to 'classification' | 'interaction' | 'extraction' — no parallel graph event exists"
  artifacts:
    - path: "app/src/domain/graph.types.ts"
      provides: "Field-exact Phase 3 domain types (GlobalEdge, SourceRecord, PostRankingFeatures, GraphContribution, PersonalEdge, ExtractionJob, RecommendationBatch)"
      exports: ["GlobalEdge", "GlobalEdgeType", "SourceRecord", "PostRankingFeatures", "EmbeddingFingerprint", "GraphContribution", "PersonalEdge", "PersonalEdgeType", "ExtractionJob", "RecommendationBatch"]
    - path: "app/src/services/global-graph.repository.ts"
      provides: "Type-indexed global graph + ranking-feature queries over IndexedDB"
      exports: ["globalGraphRepository"]
    - path: "app/src/services/db.service.ts"
      provides: "Nine new tables + IDB_VERSION 6"
      contains: "content_pool_global_edges"
  key_links:
    - from: "app/src/services/content-pool.repository.ts"
      to: "app/src/services/db.service.ts"
      via: "import writes graph artifacts through dbExecute into content_pool_* tables"
      pattern: "content_pool_global_edges"
    - from: "app/src/services/global-graph.repository.ts"
      to: "app/src/services/db.service.ts"
      via: "queries via dbQuery seam"
      pattern: "dbQuery"
  prohibitions:
    - statement: "Must not add a parallel event type for graph mutations — GRAPH_UPDATED with a payload kind is the single signal (CLAUDE.md one-signal rule)"
      status: unresolved
    - statement: "Must not access IndexedDB or localStorage directly — every persistence operation goes through dbExecute/dbQuery, and both LocalStorageBackend and IndexedDBBackend must handle any new SQL shape identically"
      status: unresolved
---

<objective>
Import the frozen typed global graph into the app and stand up the Phase 3 storage substrate: domain types, all new IndexedDB tables (one owner for db.service.ts across the phase), extended bundle reader/importer, and the type-indexed global-graph repository (D-01, GRAPH-01 runtime side).

Purpose: every later plan (personal graph, rankers, extraction, recommendations) reads types from graph.types.ts and rows from tables created here. Centralizing the db.service.ts edit prevents same-wave file conflicts.

Output: graph.types.ts, db.service.ts v6 schema, extended pool import, global-graph.repository.ts + dbQuery-asserted tests.
</objective>

<execution_context>
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/16079/Code/QuestionTrace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-graph-memory-recommendation-engine/03-CONTEXT.md
@docs/research_system_design.md
@app/src/services/db.service.ts
@app/src/data/content-pool-bundle.ts
@app/src/services/content-pool.repository.ts
</context>

## Artifacts this phase produces (plan 03-02 subset)

- Files: `app/src/domain/graph.types.ts`, `app/src/services/global-graph.repository.ts`, `app/tests/services/global-graph.repository.test.mjs`
- Types: `GlobalEdge`, `GlobalEdgeType` (nine §10.4 values), `SourceRecord`, `PostRankingFeatures`, `EmbeddingFingerprint`, `GraphContribution`, `PersonalEdge`, `PersonalEdgeType` (§10.4 personal edge names: viewed, asked, under, asks_about, interested_in, confused_about, revisited, saved, skipped, echoed_by, served), `ExtractionJob`, `RecommendationBatch`
- Tables: `content_pool_sources`, `content_pool_global_edges`, `content_pool_ranking_features`, `user_concept_states`, `graph_contributions`, `personal_graph_edges`, `extraction_jobs`, `recommendations`, `recommendation_batches`
- Service singleton: `globalGraphRepository`
- Event: `GRAPH_UPDATED` payload `kind` extended with `'interaction' | 'extraction'`

<tasks>

<task type="auto">
  <name>Task 1: Phase 3 domain types + GRAPH_UPDATED payload extension</name>
  <files>app/src/domain/graph.types.ts, app/src/types/index.ts</files>
  <read_first>docs/research_system_design.md (§10.2–§10.5 node/edge/UserConceptState definitions — field-for-field; §9.9 Recommendation), app/src/domain/content.types.ts (existing Recommendation, UserConceptState, RecommendationStrategy types from Phase 2 — do NOT duplicate them; import/reuse), app/src/types/index.ts (AppEvent union around line 467–503, current GRAPH_UPDATED payload kind 'classification'), tools/content_pipeline/schemas/global-edge.schema.json (artifact shape from plan 03-01)</read_first>
  <action>
    Create app/src/domain/graph.types.ts with (per D-01/D-02 and RSD §10, citing exact fields):
    - GlobalEdgeType = 'explains' | 'mentions' | 'supports' | 'challenges' | 'about' | 'contrasts_with' | 'related_to' | 'prerequisite_of' | 'targets'
    - GlobalEdge = { id, topicId, type: GlobalEdgeType, sourceId, targetId }
    - SourceRecord = { id, name, platform, url }
    - EmbeddingFingerprint = { provider, model, dimensions } ; PostRankingFeatures = { postId, topicId, primaryConceptId, sourceId, format, qualityScore, educationalValueScore, interestingnessScore, difficulty, viewpoint?, summaryVector? }
    - PersonalEdgeType = 'viewed' | 'asked' | 'under' | 'asks_about' | 'interested_in' | 'confused_about' | 'revisited' | 'saved' | 'skipped' | 'echoed_by' | 'served' ; PersonalEdge = { id, userId, type: PersonalEdgeType, sourceId, targetId, createdAt }
    - GraphContribution = { id, userId, conceptId, eventId, rule, delta: { interest?, uncertainty?, familiarity?, counts? }, createdAt }
    - ExtractionJob = { id, questionId, status: 'pending' | 'succeeded' | 'failed', attempts, lastError?, createdAt, updatedAt }
    - RecommendationBatch = { id, userId, sessionId, seq, recommendationIds: string[], status: 'building' | 'ready', diversityCounters: { sourceCounts: Record<string, number>, recentPrimaryConceptIds: string[] }, createdAt }
    Reuse (import) Recommendation and UserConceptState from domain/content.types.ts — do not redeclare.
    In app/src/types/index.ts, extend the GRAPH_UPDATED payload: kind?: 'classification' | 'interaction' | 'extraction' (keep anchorId/affectedIds optional fields intact). Do not add any new event type.
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - graph.types.ts exports every symbol listed in "Artifacts this phase produces" above; `tsc -b --noEmit` exits 0
    - `grep -c "interaction' | 'extraction'\|'interaction' \| 'extraction'" app/src/types/index.ts` ≥ 1 (payload union extended)
    - `grep -c "interface Recommendation" app/src/domain/graph.types.ts` returns 0 (reused, not redeclared)
  </acceptance_criteria>
  <done>All Phase 3 shared types exist; event payload extended; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: db.service v6 tables + bundle reader + pool import of graph artifacts</name>
  <files>app/src/services/db.service.ts, app/src/data/content-pool-bundle.ts, app/src/services/content-pool.repository.ts</files>
  <read_first>app/src/services/db.service.ts (DDL list lines ~44–84, IDB_NAME/IDB_VERSION lines ~197–212, backend SQL-subset comments — both backends must handle new statements), app/src/data/content-pool-bundle.ts (CONTENT_POOL_FILENAMES, artifactCollections mapping, hash validation), app/src/services/content-pool.repository.ts (import flow into content_pool_* version-qualified rows)</read_first>
  <action>
    db.service.ts: append nine CREATE TABLE IF NOT EXISTS entries following the existing storage_id/version pattern for pool tables and id/data pattern for user tables — content_pool_sources, content_pool_global_edges, content_pool_ranking_features (storage_id TEXT PRIMARY KEY, version TEXT NOT NULL, record_id TEXT NOT NULL, data TEXT NOT NULL); user_concept_states (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, concept_id TEXT NOT NULL, data TEXT NOT NULL); graph_contributions (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, concept_id TEXT NOT NULL, data TEXT NOT NULL); personal_graph_edges (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, data TEXT NOT NULL); extraction_jobs (id TEXT PRIMARY KEY, status TEXT NOT NULL, data TEXT NOT NULL); recommendations (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, data TEXT NOT NULL); recommendation_batches (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, session_id TEXT NOT NULL, data TEXT NOT NULL). Bump IDB_VERSION 5→6 (upgrade creates missing object stores; delete nothing — store deletion is plan 03-08's 6→7 bump). Stay within the documented INSERT OR REPLACE / DELETE / SELECT subset both backends implement.
    content-pool-bundle.ts: add 'sources.json', 'global_edges.json', 'ranking_features.json' to CONTENT_POOL_FILENAMES and artifactCollections (bundle keys: sources, globalEdges, rankingFeatures); they participate in existing checksum/version verification.
    content-pool.repository.ts: import the three collections into their content_pool_* tables under the same version-qualified staged-rows + ready-marker discipline as existing collections; before marking ready, validate every edge endpoint ID exists among imported posts/concepts/claims/suggested-questions and every edge topicId matches — on violation, fail import with the existing error-snapshot path (code POOL_INVALID).
  </action>
  <verify>
    <automated>cd app && node --test tests/services/content-pool.repository.test.mjs && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IDB_VERSION = 6" app/src/services/db.service.ts` = 1
    - All nine table names present in db.service.ts DDL (grep each name ≥ 1)
    - `grep -c "global_edges.json" app/src/data/content-pool-bundle.ts` ≥ 1 (plus sources.json, ranking_features.json)
    - Existing content-pool repository tests still green; import of a fixture bundle containing a dangling edge endpoint fails with POOL_INVALID (new test case in Task 3's file or existing repo test file)
  </acceptance_criteria>
  <done>Schema v6 live; bundle contract extended; graph artifacts import validated and versioned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: global-graph.repository with type-indexed queries + dbQuery durability tests</name>
  <files>app/src/services/global-graph.repository.ts, app/tests/services/global-graph.repository.test.mjs</files>
  <read_first>app/tests/services/anchor-persistence.test.mjs (the canonical dbQuery-durability test pattern — replicate its seam usage), app/src/services/content-pool.repository.ts (how ready-version rows are read), app/src/domain/graph.types.ts (Task 1 types)</read_first>
  <behavior>
    - Test: after importing a fixture bundle, edgesByType('contrasts_with') returns exactly the fixture's contrast pairs — asserted after a fresh repository load backed by dbQuery rows (not the import-time in-memory objects)
    - Test: edgesFrom(claimId) / edgesTo(conceptId) return correctly typed edges; oneHopConcepts(conceptId) unions related_to + prerequisite_of neighbors
    - Test: opposingClaims(claimId) resolves contrasts_with symmetric pairs; rankingFeatures(postId) returns the frozen features row
    - Test: repository refuses to serve queries when no ready pool version exists (explicit error, not empty success)
  </behavior>
  <action>
    Create app/src/services/global-graph.repository.ts exporting singleton globalGraphRepository with: load() (reads ready-version rows from content_pool_global_edges / content_pool_sources / content_pool_ranking_features via dbQuery, builds in-memory indexes by type / sourceId / targetId), edgesByType(type), edgesFrom(nodeId), edgesTo(nodeId), oneHopConcepts(conceptId), opposingClaims(claimId), rankingFeatures(postId), embeddingFingerprint(). Follow the ServiceResult<T> convention for load; queries throw if called before a successful load. Bounded one/two-hop traversals only — no graph library (RESEARCH "Don't Hand-Roll"). Keep the repository read-only: no mutation API.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/global-graph.repository.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test file asserts durability through dbQuery (pattern: seed via import, re-instantiate, query) — `grep -c "dbQuery" app/tests/services/global-graph.repository.test.mjs` ≥ 1
    - All query methods listed above exist and are covered by at least one assertion each
    - `node --test tests/services/global-graph.repository.test.mjs` green; `npm test` from app/ green
  </acceptance_criteria>
  <done>Type-indexed global graph queryable from durable rows; tests green through the seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged bundle → IndexedDB | frozen artifacts cross into participant-local storage |
| repository queries → rankers | graph rows become ranking inputs for the study treatment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | pool import of graph artifacts | high | mitigate | Existing sha256 + version verification extended to the three new files; endpoint/topic referential validation before ready-marker; POOL_INVALID on violation |
| T-03-05 | Denial of service | IDB v6 upgrade path | medium | mitigate | Upgrade only creates missing stores (idempotent); both SQL backends handle the new statements — parity asserted by running the same tests against the LocalStorage fallback in Node |
| T-03-06 | Information disclosure | new user-data tables | medium | mitigate | Tables created empty here; population rules (allowlists, §14.2 exclusions) are enforced by the writing services in plans 03-03/03-05/03-06 |
</threat_model>

<verification>
- `cd app && npm test` green; `npx tsc -b --noEmit` green
- Fixture pool with graph artifacts imports to ready; dangling-endpoint fixture fails with POOL_INVALID
</verification>

<success_criteria>
- The app holds a queryable, hash-verified copy of the frozen §10.4 global graph with zero in-app graph computation (D-01).
- Every downstream Phase 3 plan can rely on graph.types.ts symbols and the nine v6 tables.
</success_criteria>

## Flagged assumptions (spec-less probe — unresolved rows surfaced, not dropped)

- **GRAPH-01 / unclassified probe:** runtime-side resolution = import-boundary endpoint validation + type-indexed queries; encoded in truths, row remains flagged.
- Tests use fixture bundles: the real `data/content_pool_v1/` freeze is a Phase 2 (02-09) deliverable; importer behavior against the real pool is confirmed at phase UAT.

<output>
Create `.planning/phases/03-graph-memory-recommendation-engine/03-02-SUMMARY.md` when done.
</output>
