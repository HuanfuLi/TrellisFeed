---
phase: 48-graph-command-service-and-trust-invariants
plan: 03
type: execute
wave: 3
# Wave 3: sequenced after Plan 02 commits to graph-command.service.ts (Warning #1 fix — promoting from Wave 2 to Wave 3 prevents parallel mutation of the same file by independent agents).
depends_on: ["48-01", "48-02"]
files_modified:
  - app/src/services/graph-command.service.ts
  - app/tests/services/graph-command-service.merge.test.mjs
  - app/tests/services/graph-command-service.detach.test.mjs
  - app/tests/services/graph-command-service.prune.test.mjs
  - app/package.json
autonomous: true
requirements:
  - GRAPH-03
must_haves:
  truths:
    - "merge(loserId, survivorId) reparents loser's children to survivor, hard-deletes loser via questionService.delete (with success check), recomputes survivor qaCount, and either patches embeddingVector alongside (when re-embed succeeds) or leaves the old vector untouched (when embedding unconfigured OR re-embed fails) — never silently clears."
    - "detach(qaId) clears parentId/branchLabel/clusterLabel/clusterNodeId/nodeSummary/placementReason, fires classifyAndAnchorIncremental fire-and-forget with AbortSignal threading; emitting LOCALE_CHANGED before the classify resolves cancels the pending call (signal.aborted === true at next checkpoint)."
    - "prune(anchorId) delegates to trellisActionsService.prune (preserving ANCHOR_DELETED emit for PrunedSection), writes journal entry, emits one GRAPH_UPDATED."
    - "Every successful command writes exactly ONE GraphEditLogEntry. For merge, the LAST GRAPH_UPDATED observed by subscribers has payload.kind === 'merge' (typed emit from command boundary AFTER the delete success check). An ADDITIONAL untyped GRAPH_UPDATED fires from questionService.delete(loserId); subscribers are idempotent per CLAUDE.md §Event bus."
    - "If questionService.delete(loserId) returns { success: false }, the merge command aborts BEFORE writing the journal entry or emitting GRAPH_UPDATED from the command boundary — failed mutations are never journaled (Blocker #2 fix)."
    - "At no point does the post-merge survivor have embeddingVector === undefined when it had a vector before the merge — graceful degradation (D-11) guaranteed."
  artifacts:
    - path: "app/src/services/graph-command.service.ts"
      provides: "merge, detach, prune methods filled in (replacing Plan 02 stubs)."
    - path: "app/tests/services/graph-command-service.merge.test.mjs"
      provides: "Merge test suite — reparent children + hard-delete loser (with success check) + recompute survivor + full pre-image journal + graceful re-embed + last-event-kind-discriminator."
    - path: "app/tests/services/graph-command-service.detach.test.mjs"
      provides: "Detach test suite — clear placement fields + fire-and-forget classify + LOCALE_CHANGED-mid-flight cancellation."
    - path: "app/tests/services/graph-command-service.prune.test.mjs"
      provides: "Prune test suite — delegation to trellisActionsService + journal + GRAPH_UPDATED."
  key_links:
    - from: "app/src/services/graph-command.service.ts merge()"
      to: "app/src/services/question.service.ts delete + patchQuestion + providers/embedding embedText"
      via: "patch reparented children → check delete result → recompute survivor qaCount + re-embed (graceful degradation)"
      pattern: "questionService\\.delete\\(loserId\\)"
    - from: "app/src/services/graph-command.service.ts detach()"
      to: "app/src/services/canonical-knowledge.service.ts classifyAndAnchorIncremental"
      via: "void classifyAndAnchorIncremental(question, allQuestions, llmConfig, signal).catch(...) — signal honored mid-flight"
      pattern: "classifyAndAnchorIncremental"
    - from: "app/src/services/graph-command.service.ts prune()"
      to: "app/src/services/trellis-actions.service.ts prune"
      via: "trellisActionsService.prune(anchorId) — delegates per R6"
      pattern: "trellisActionsService\\.prune"
---

<objective>
Replace the `merge`, `detach`, `prune` stubs in `graph-command.service.ts` with full implementations. These are the multi-record commands (merge touches reparented children + survivor + loser; detach triggers a downstream classify; prune delegates to existing trellisActionsService.prune).

Purpose: complete the command verbs needed for GRAPH-03 ("merge duplicate anchors, detach misplaced Q&As, prune/delete graph nodes"). Plan 02 covered rename/move/delete (single-target); Plan 03 covers the multi-record/delegated/async-fire cases.

Output:
- `app/src/services/graph-command.service.ts` — `merge`, `detach`, `prune` implementations replace NOT_IMPLEMENTED stubs.
- `app/tests/services/graph-command-service.merge.test.mjs` — covers reparenting, hard-delete loser with success check (Blocker #2), survivor recompute with graceful re-embed (Blocker #4), full-record snapshot, last-event-kind-discriminator (Warning #4).
- `app/tests/services/graph-command-service.detach.test.mjs` — covers placement-field clearing, fire-and-forget classification call, single GRAPH_UPDATED emit (downstream classification's second emit is documented but not failed), LOCALE_CHANGED-mid-flight cancellation (Warning #2).
- `app/tests/services/graph-command-service.prune.test.mjs` — covers delegation, journal, single GRAPH_UPDATED.
- `app/package.json` — `test:actions` script extended with the three new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Code/EchoLearn/CLAUDE.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-VALIDATION.md
@/Users/Code/EchoLearn/app/src/services/graph-command.service.ts
@/Users/Code/EchoLearn/app/src/services/graph-edit-journal.service.ts
@/Users/Code/EchoLearn/app/src/services/question.service.ts
@/Users/Code/EchoLearn/app/src/services/canonical-knowledge.service.ts
@/Users/Code/EchoLearn/app/src/services/trellis-actions.service.ts
@/Users/Code/EchoLearn/app/src/providers/embedding/index.ts
@/Users/Code/EchoLearn/app/src/types/index.ts
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-question.mjs
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-embedding.mjs
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-llm.mjs
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-hooks.mjs

<interfaces>
<!-- Existing surfaces this plan calls -->

From app/src/services/question.service.ts (verified signature at question.service.ts:565-571):

  // Hard-delete; emits QUESTION_DELETED + untyped GRAPH_UPDATED.
  // CRITICAL: returns Promise<ServiceResult<void>> — caller MUST check success.
  async delete(id: string): Promise<ServiceResult<void>>;

From app/src/services/canonical-knowledge.service.ts:

  // Called fire-and-forget by detach (R7 — D-13).
  // signal MUST be threaded so LOCALE_CHANGED can cancel mid-flight (D-19).
  export async function classifyAndAnchorIncremental(
    question: Question,
    allQuestions: Question[],
    llmConfig: LLMConfig,
    signal?: AbortSignal,
  ): Promise<void>;

From app/src/services/trellis-actions.service.ts:

  // Called by graphCommandService.prune (R6 — D-14 delegate, don't replace).
  // Sets flagged=true + prunedFromTrellis=true; emits ANCHOR_DELETED.
  prune(anchorId: string): { pruned: true };

  // Called by future undo() of a prune entry.
  unpruneQuestion(anchorId: string): void;

From app/src/providers/embedding/index.ts:

  // Survivor re-embed after merge (D-11 graceful degradation).
  export function embedText(text: string, cfg: EmbeddingConfig): Promise<number[]>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement merge(loserId, survivorId) — multi-record consolidation with success-check + graceful re-embed</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.merge.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (Plans 01/02 — skeleton with stubs + completed rename/move/delete)
    - app/src/services/graph-edit-journal.service.ts
    - app/src/services/question.service.ts lines 565-571 (delete signature: Promise<ServiceResult<void>>; emits untyped GRAPH_UPDATED at :569)
    - app/src/providers/embedding/index.ts (embedText for survivor recompute)
    - app/src/services/canonical-knowledge.service.ts lines 660-680 (existing embedText backfill pattern for graceful degradation)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-07, D-08, D-09, D-10, D-11, D-17)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R2 "merge" diff spec (lines 217-249) and §R10 risk 6 (embedding failure)
    - CLAUDE.md §"Event bus — unified GRAPH_UPDATED" — subscribers are idempotent; double-emit acceptable
    - app/tests/services/_actions-mock-embedding.mjs (mock for survivor re-embed tests)
  </read_first>
  <behavior>
    - `merge('loser-X', 'survivor-Y')` where loser has 4 child QAs AND embedding.isConfigured=true AND embedText succeeds:
        - All 4 children re-parent to survivor: `parentId: 'survivor-Y'`, `clusterNodeId: survivor.clusterNodeId`, `branchLabel: survivor.branchLabel`, `clusterLabel: survivor.clusterLabel` (D-08 — survivor's fields win, including cross-cluster case).
        - Survivor's `qaCount` recomputed to `count(store.filter(q => q.parentId === survivor.id))` AFTER reparent (D-11).
        - Survivor's `embeddingVector` REPLACED with the new vector in ONE atomic patchQuestion call (alongside qaCount + nodeSummary). NEVER set to undefined.
        - Survivor's `nodeSummary` gets appended lines from loser's children (`[childId] shortSummary\n...`). Uses `child.shortSummary ?? child.content.slice(0, 80)` per the verified Question type at types/index.ts:36.
        - Loser is hard-deleted via `await questionService.delete('loser-X')` (D-10). **Blocker #2 fix:** if delete returns `{ success: false }`, the command aborts BEFORE journal append AND BEFORE command-boundary GRAPH_UPDATED emit. Children are left in their re-parented state; survivor's qaCount/embeddingVector/nodeSummary are left in their updated state. Returns `{ success: false, error: { code: 'STORAGE_ERROR', ... } }`. Acceptable partial-state per T-48-14 (same rationale as Plan 02 delete).
        - On `success`: returns `{ success: true, data: { reparentedCount: 4, newSurvivorQaCount: <survivor old count + 4> } }`.
        - Journal: ONE entry with `cmd: 'merge'`, `targetIds: ['loser-X', 'survivor-Y']`, `before = { loser: <full loser Question>, survivor: { qaCount: <old>, embeddingVector: <old vec>, nodeSummary: <old> }, reparentedChildren: [{id, parentId, clusterNodeId, branchLabel, clusterLabel}...4] }`.
    - **Graceful degradation — embedding unconfigured (Blocker #4 fix):** `merge(...)` when `isConfigured === false`: skip re-embed entirely. Patch survivor with `{ qaCount, nodeSummary }` ONLY (no `embeddingVector` in the patch object). The old vector remains valid (slightly stale because qaCount changed, but searchable per D-11). Journal still snapshots the OLD survivor.embeddingVector so undo can restore it. Merge still succeeds.
    - **Graceful degradation — embedding provider fails (Blocker #4 fix):** `merge(...)` when `isConfigured=true` but `embedText` rejects: catch the rejection, log via `console.warn('[Trellis] merge survivor re-embed failed:', err)`. Patch survivor with `{ qaCount, nodeSummary }` ONLY (vector preserved). Merge still succeeds; survivor keeps the pre-merge vector.
    - **Invariant:** at no point does the post-merge survivor have `embeddingVector === undefined` when it had a vector before.
    - `merge('X', 'X')` → VALIDATION_ERROR (R10 risk 13 — self-merge).
    - `merge('not-real', 'survivor-Y')` → NOT_FOUND.
    - `merge('loser-X', 'not-real')` → NOT_FOUND.
    - **Emit invariant (Warning #4 fix — Option C):** On success, TWO `GRAPH_UPDATED` events fire in quick succession:
      1. The UNTYPED `{ type: 'GRAPH_UPDATED' }` from `questionService.delete(loserId)` at question.service.ts:569.
      2. The TYPED `{ type: 'GRAPH_UPDATED', payload: { kind: 'merge', anchorId: survivorId, affectedIds: [loserId, survivorId, ...childIds] } }` from the command boundary AFTER delete succeeds AND journal entry is appended.
      The LAST event observed by subscribers has `payload.kind === 'merge'`. Subscribers are already idempotent per CLAUDE.md §Event bus. Inline comment documents this.
  </behavior>
  <action>
    Replace `merge` stub in `graph-command.service.ts`. Inside mutex:
    1. Validate: `loserId !== survivorId` (VALIDATION_ERROR if equal). Read fresh store. Find both records; if either missing, NOT_FOUND.
    2. Snapshot `before.loser = { ...loser }` (full record). Snapshot `before.survivor = { qaCount: survivor.qaCount, embeddingVector: survivor.embeddingVector, nodeSummary: survivor.nodeSummary }`. Collect `children = store.filter(q => q.parentId === loserId)`. Snapshot `before.reparentedChildren = children.map(c => ({ id: c.id, parentId: c.parentId, clusterNodeId: c.clusterNodeId, branchLabel: c.branchLabel, clusterLabel: c.clusterLabel }))`.
    3. Reparent each child via `questionService.patchQuestion(child.id, { parentId: survivorId, clusterNodeId: survivor.clusterNodeId, branchLabel: survivor.branchLabel, clusterLabel: survivor.clusterLabel })`. (D-09 — survivor's fields win.)
    4. Build new survivor nodeSummary: existing `survivor.nodeSummary` plus, for each child, `\n[${child.id}] ${child.shortSummary ?? child.content.slice(0, 80)}` (Warning #3 — `shortSummary?: string` verified on Question at types/index.ts:36).
    5. Compute new `qaCount = (survivor.qaCount ?? 0) + children.length`.
    6. **Embedding strategy (Blocker #4 fix):**
       - Read `const embCfg = settingsService.getSync().embedding;`.
       - If `embCfg?.isConfigured !== true`: SKIP re-embed. `questionService.patchQuestion(survivorId, { qaCount, nodeSummary: newNodeSummary });` — `embeddingVector` NOT in patch; old vector preserved.
       - Else: `let newVec: number[] | undefined; try { newVec = await embedText(survivor.title ?? survivor.content, embCfg); } catch (err) { console.warn('[Trellis] merge survivor re-embed failed:', err); newVec = undefined; }`. If `newVec` is `undefined`: `patchQuestion(survivorId, { qaCount, nodeSummary: newNodeSummary });` (vector preserved). If a vector: `patchQuestion(survivorId, { qaCount, nodeSummary: newNodeSummary, embeddingVector: newVec });` (atomic single call).
    7. **Blocker #2 fix:** `const deleteResult = await questionService.delete(loserId);`. If `deleteResult.success === false`: return `{ success: false, error: { code: 'STORAGE_ERROR', message: deleteResult.error?.message ?? 'Hard delete of loser failed.', retryable: true } }`. DO NOT append journal entry. DO NOT emit from command boundary. (Survivor's qaCount/nodeSummary/vector and children's parentId are left in the updated state — acceptable partial-state per T-48-14; operator can retry and no-op guards prevent double-reparent.)
    8. On `success === true`: append journal entry: `graphEditJournal.append({ cmd: 'merge', targetIds: [loserId, survivorId], before, after: { reparentedCount: children.length, newSurvivorQaCount: qaCount } })`.
    9. **Warning #4 fix (Option C — emit from command boundary):** AFTER the journal append, emit `eventBus.emit({ type: 'GRAPH_UPDATED', payload: { kind: 'merge', anchorId: survivorId, affectedIds: [loserId, survivorId, ...children.map(c => c.id)] } });`. Inline comment: `// NOTE: questionService.delete(loserId) at step 7 already emitted an untyped GRAPH_UPDATED (question.service.ts:569). We emit a SECOND, typed GRAPH_UPDATED here so subscribers that filter on payload.kind === 'merge' see the discriminator. Subscribers are already idempotent per CLAUDE.md §"Event bus — unified GRAPH_UPDATED" — re-reading store twice is harmless. The LAST event observed has payload.kind === 'merge' (subscriber dedup pattern).`
    10. Return `{ success: true, data: { reparentedCount: children.length, newSurvivorQaCount: qaCount } }`.

    Inline comment at step 6: `// Per Blocker #4 fix (revision 1): never overwrite a vector with undefined. Either the new vector replaces it atomically, or the old vector stays. This mirrors rename's strategy and preserves D-11 retrieval-identity-degrades-gracefully.`

    Test file `app/tests/services/graph-command-service.merge.test.mjs`:
    - Seed store with `loser-X` (anchor, qaCount=4), `survivor-Y` (anchor, qaCount=2, embeddingVector=<old>), 4 children parented to loser-X.
    - Call `merge('loser-X', 'survivor-Y')` with isConfigured=true + working embedText. Assert:
      - 4 children now have `parentId === 'survivor-Y'`.
      - `survivor.qaCount === 6`.
      - `survivor.embeddingVector === <new vec>` (REPLACED, never undefined).
      - loser-X no longer in store.
      - Journal entry: `cmd === 'merge'`, `targetIds === ['loser-X', 'survivor-Y']`, `before.loser.title === <original>`, `before.survivor.embeddingVector === <old vec>`, `before.reparentedChildren.length === 4`.
    - **Graceful degradation (Blocker #4) — embedding unconfigured test:** stub settingsService to return `isConfigured: false`; call merge; assert survivor.embeddingVector is identical to the pre-merge value (preserved, not undefined); assert merge still succeeded.
    - **Graceful degradation (Blocker #4) — embedText fails test:** stub `_actions-mock-embedding.mjs` to reject; assert merge still succeeds; assert survivor.embeddingVector is identical to the pre-merge value (preserved); assert console.warn was called.
    - **Failed-delete (Blocker #2) test:** stub `_actions-mock-question.mjs` so that delete(loserId) returns `{ success: false, error: {...} }`. Call merge; assert result is STORAGE_ERROR; assert journal length is 0; assert NO command-boundary GRAPH_UPDATED with `payload.kind === 'merge'` was emitted; survivor + children are left in their updated state (acceptable partial — comment in test).
    - Self-merge test: `merge('X', 'X')` → VALIDATION_ERROR.
    - NOT_FOUND tests for missing loser + missing survivor.
    - **Last-event-kind-discriminator (Warning #4) test:** subscribe before call, perform successful merge, assert `events.length >= 1` AND `events[events.length - 1].payload?.kind === 'merge'` (the LAST event has the discriminator). Document the double-emit invariant inline.

    Add `graph-command-service.merge.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.merge.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+merge\s*\(" app/src/services/graph-command.service.ts` finds implementation (not stub).
    - Source contains `loserId !== survivorId` validation: `grep -E "loserId\s*===\s*survivorId|loserId\s*!==\s*survivorId" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains full-loser-record snapshot: `grep -F "loser:" app/src/services/graph-command.service.ts` returns >= 1 within the merge body.
    - Source checks deleteResult.success for loser (Blocker #2): the merge body contains an `if (...success === false)` or `if (!...success)` branch after the `await questionService.delete(loserId)` call.
    - Source emits typed GRAPH_UPDATED with kind=merge from command boundary (Warning #4): `grep -F "kind: 'merge'" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains the "subscribers are idempotent" / "LAST event observed" inline comment in BOTH delete + merge bodies (T-48 / D-17 trace): `grep -E "subscribers are already idempotent|LAST event observed" app/src/services/graph-command.service.ts` returns >= 2.
    - Source contains the Blocker #4 inline comment in the merge body too: `grep -F "never overwrite a vector with undefined" app/src/services/graph-command.service.ts` returns >= 2 (rename + merge).
    - `cd app && npm run test:actions -- tests/services/graph-command-service.merge.test.mjs` exits 0.
    - Test file contains: reparent assertion, hard-delete assertion, survivor qaCount recompute assertion, embeddingVector REPLACED assertion (working path), embeddingVector PRESERVED assertion (unconfigured path), embeddingVector PRESERVED assertion (embedText-fails path), failed-delete-aborts-journal assertion (Blocker #2), self-merge VALIDATION_ERROR, NOT_FOUND tests, last-event-kind-discriminator assertion (Warning #4 — events[-1].payload?.kind === 'merge').
    - `package.json` `test:actions` script includes `graph-command-service.merge.test.mjs`.
  </acceptance_criteria>
  <done>
    Merge reparents children, hard-deletes loser (with success check per Blocker #2), recomputes survivor qaCount + graceful-degradation embedding (Blocker #4 — never silently undefined), emits typed GRAPH_UPDATED with kind=merge from command boundary (Warning #4 — accepts double-emit; subscribers idempotent). Full loser record + reparent diff in journal (sufficient for Plan 04 undo).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detach(qaId) — clear placement + fire classifyAndAnchorIncremental with cancellable signal</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.detach.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (Plan 02 + Task 1 of this plan)
    - app/src/services/canonical-knowledge.service.ts lines 1007-1030 (classifyAndAnchorIncremental signature + pre-check)
    - app/src/services/question.service.ts lines 320-350 (existing fire-and-forget call pattern for `classifyAndAnchorIncremental`)
    - app/src/services/settings.service.ts (for llmConfig retrieval pattern)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-13, D-17, D-19)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R7 (detach fire-and-forget) + §R10 risk 5 (two-event sequence — intentional) + risk 14 (no-op detach)
    - app/tests/services/_actions-mock-llm.mjs (LLM stub for tests; extend to record signal arg)
  </read_first>
  <behavior>
    - `detach('qa-1')` where qa-1 has `parentId: 'anchor-A'`, `branchLabel: 'Psychology'`, `clusterLabel: 'Learning Theory'`, `clusterNodeId: 'cluster-X'`, `nodeSummary: undefined`, `placementReason: 'Routed via SM-2'`:
        - Patches qa-1 with `{ parentId: undefined, branchLabel: undefined, clusterLabel: undefined, clusterNodeId: undefined, nodeSummary: undefined, placementReason: undefined }`.
        - Old parent's `qaCount` decrements; if old parent is an anchor, its `nodeSummary` strips the matching `[qa-1] ` line.
        - Returns `{ success: true }` immediately (NOT awaiting classification).
        - Journal: `cmd: 'detach'`, `before = { parentId: 'anchor-A', branchLabel: 'Psychology', clusterLabel: 'Learning Theory', clusterNodeId: 'cluster-X', placementReason: 'Routed via SM-2' }`, `targetIds: ['qa-1']`. `after` records `{ classificationFired: true }`.
        - Emits ONE `GRAPH_UPDATED` with `payload.kind === 'detach'` (synchronously from detach command).
        - Fires `classifyAndAnchorIncremental(<patched-qa>, <fresh store snapshot>, llmConfig, opts?.signal)` fire-and-forget. The downstream classification's eventual GRAPH_UPDATED is acknowledged but not awaited (R10 risk 5 — two-event sequence is intentional).
    - `detach('qa-already-unassigned')` where `parentId === undefined`: returns `{ success: true }` BUT no journal entry, no GRAPH_UPDATED emit, no classification fired (R10 risk 14 — no-op).
    - `detach('not-real')` → NOT_FOUND.
    - `detach('anchor-A')` (an anchor, not a QA): VALIDATION_ERROR ("can only detach Q&A nodes, not anchors or clusters").
    - LLM provider not configured: classification still fires; the call internally handles config-absent case.
    - **AbortSignal cancellation (Warning #2 fix):** AbortSignal threaded into the classification call. Calling `detach(qaId, { signal })` then aborting the signal BEFORE classifyAndAnchorIncremental resolves: at the next await checkpoint inside classify, `signal.aborted === true` and the call short-circuits. This matches the Phase 27 D-22 LOCALE_CHANGED pattern.
  </behavior>
  <action>
    Replace `detach` stub in `graph-command.service.ts`. Inside mutex:
    1. Read fresh store. Find target. NOT_FOUND if missing.
    2. Validate target is a QA — reject if `target.isAnchorNode === true` or `target.isClusterNode === true` with VALIDATION_ERROR.
    3. No-op guard: if `target.parentId === undefined`, return `{ success: true }` (no journal/emit/classify) per R10 risk 14.
    4. Snapshot `before = { parentId: target.parentId, branchLabel: target.branchLabel, clusterLabel: target.clusterLabel, clusterNodeId: target.clusterNodeId, nodeSummary: target.nodeSummary, placementReason: target.placementReason }`.
    5. Find old parent in store (for side-effect update). Compute new qaCount + new nodeSummary (strip the `[${target.id}] ` line, same logic as Plan 02 `move`'s old-parent update).
    6. Patch target: `questionService.patchQuestion(target.id, { parentId: undefined, branchLabel: undefined, clusterLabel: undefined, clusterNodeId: undefined, nodeSummary: undefined, placementReason: undefined })`.
    7. Patch old parent: `questionService.patchQuestion(oldParent.id, { qaCount: newQaCount, nodeSummary: newSummary })` (if oldParent exists).
    8. Append journal entry: `graphEditJournal.append({ cmd: 'detach', targetIds: [target.id], before, after: { classificationFired: true } })`.
    9. Emit `{ type: 'GRAPH_UPDATED', payload: { kind: 'detach', anchorId: target.id, affectedIds: [target.id, ...(oldParent ? [oldParent.id] : [])] } }` (D-17 — ONE emit from THIS command; downstream classification's emit is its OWN command per R7).
    10. Re-read store AFTER patches: `const allQuestionsAfter = questionService.getAll({ includeFlagged: true });` and `const targetAfter = allQuestionsAfter.find(q => q.id === target.id)`. Then fire fire-and-forget: `void classifyAndAnchorIncremental(targetAfter, allQuestionsAfter, settingsService.getSync().llmConfig, opts?.signal).catch(err => console.warn('[Trellis] detach re-classify failed:', err));`. Do NOT await. Mirror the pattern at `question.service.ts:340-342`.
    11. Inline comment at step 10: `// NOTE: classifyAndAnchorIncremental emits its OWN GRAPH_UPDATED upon completion (canonical-knowledge.service.ts:974). This is a second emit — but it's from a downstream COMMAND, not a duplicate. Subscribers re-read twice. Per R7 documented intentional behavior. opts?.signal is forwarded so LOCALE_CHANGED (Phase 27 D-22) or operator-initiated cancel terminates the classify cleanly (Warning #2 fix — test asserts signal.aborted is observed at the next checkpoint).`
    12. Return `{ success: true }`.

    Test file `app/tests/services/graph-command-service.detach.test.mjs`:
    - Seed store with anchor-A (qaCount=3) and 3 QAs parented to it.
    - Call `detach('qa-1')`. Assert placement fields all `undefined`. Assert anchor-A's `qaCount === 2`.
    - Assert journal entry shape: `cmd === 'detach'`, `before.parentId === 'anchor-A'`.
    - Subscribe to GRAPH_UPDATED; assert exactly ONE emit fires SYNCHRONOUSLY from the command (downstream classification's emit happens later but the test doesn't wait long enough to see it — assert `events.length === 1` right after `await detach()`).
    - Validation tests: `detach('anchor-A')` → VALIDATION_ERROR; `detach('not-real')` → NOT_FOUND; `detach(<already unassigned>)` → success but no journal entry.
    - Fire-and-forget assertion: stub the LLM mock to record calls; assert classify was invoked exactly once with the post-patch question (i.e., placement fields undefined in the call arg).
    - **AbortSignal mid-flight cancellation (Warning #2 fix):** the actions-mock LLM must support a multi-await-point classify mock that checks `signal.aborted` at each await checkpoint. Test setup: `const controller = new AbortController(); detach(qaId, { signal: controller.signal });` then immediately `controller.abort();` (or after a microtask). The mock's classify should observe `signal.aborted === true` at its NEXT internal checkpoint and short-circuit. Assertion: the LLM mock records `signal.aborted === true` at a checkpoint AND the call does NOT complete to the final patchQuestion that classify would normally make. Acceptance criterion: `"calling detach(qaId) and then emitting LOCALE_CHANGED (simulated via controller.abort()) before classifyAndAnchorIncremental resolves cancels the pending call (signal.aborted === true at the next checkpoint)"`.

    Add `graph-command-service.detach.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.detach.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+detach\s*\(" app/src/services/graph-command.service.ts` finds implementation.
    - Source contains the fire-and-forget classify call: `grep -F "classifyAndAnchorIncremental" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains the two-emit-intentional comment: `grep -F "downstream COMMAND" app/src/services/graph-command.service.ts` OR `grep -F "second emit" app/src/services/graph-command.service.ts` returns >= 1.
    - Source forwards opts?.signal to the classify call: `grep -E "opts\?\.signal|opts\.signal" app/src/services/graph-command.service.ts` returns >= 1 within detach body.
    - Source validates target is a QA, not anchor: `grep -E "isAnchorNode|isClusterNode" app/src/services/graph-command.service.ts` returns >= 1 within detach body.
    - `cd app && npm run test:actions -- tests/services/graph-command-service.detach.test.mjs` exits 0.
    - Test file contains: placement-clear assertion, parent-qaCount-decrement assertion, journal-before-shape assertion, single-sync-emit assertion, VALIDATION_ERROR for anchor target, NOT_FOUND for missing target, no-op for already-unassigned, fire-and-forget classify-was-called assertion, and the explicit AbortSignal-cancellation-mid-flight assertion from Warning #2 fix (signal.aborted observed at next checkpoint; classify does NOT complete to its final patchQuestion).
    - `package.json` `test:actions` script includes `graph-command-service.detach.test.mjs`.
  </acceptance_criteria>
  <done>
    Detach clears placement fields, recomputes old parent, fires classify fire-and-forget with AbortSignal forwarded. Single sync emit; downstream classify's emit documented as intentional second emit per R7. Warning #2 cancellation test enforces that LOCALE_CHANGED (or any abort) mid-flight cancels the pending call at the next checkpoint.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement prune(anchorId) — delegate to trellisActionsService.prune</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.prune.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (Plan 02 + Tasks 1-2 of this plan)
    - app/src/services/trellis-actions.service.ts lines 120-140 (`prune` + `unpruneQuestion`)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-14, D-17)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R6 "trellisActionsService.prune consolidation — keep or fold in?" (lines 540-592)
    - app/src/components/trellis/PrunedSection.tsx lines 20-30 (subscriber to ANCHOR_DELETED — preserved by delegation)
  </read_first>
  <behavior>
    - `prune('anchor-A')` where anchor-A is `flagged: false, prunedFromTrellis: false`:
        - Calls `trellisActionsService.prune('anchor-A')` — sets flagged=true + prunedFromTrellis=true; emits ANCHOR_DELETED.
        - Writes journal entry `{ cmd: 'prune', targetIds: ['anchor-A'], before: { flagged: false, prunedFromTrellis: false }, after: { flagged: true, prunedFromTrellis: true } }`.
        - Emits ONE `GRAPH_UPDATED` with `payload.kind === 'prune'`.
        - Returns `{ success: true }`.
    - `prune('anchor-already-pruned')` where target is already pruned: returns `{ success: true }` BUT no journal entry, no GRAPH_UPDATED, no trellisActionsService.prune call (no-op).
    - `prune('qa-1')` (not an anchor): VALIDATION_ERROR — "prune is for anchors only".
    - `prune('not-real')` → NOT_FOUND.
    - PrunedSection subscriber: after delegated `trellisActionsService.prune` runs, ANCHOR_DELETED fires. Test asserts PrunedSection-style subscriber receives the event.
  </behavior>
  <action>
    Replace `prune` stub in `graph-command.service.ts`. Inside mutex:
    1. Read fresh store. Find target. NOT_FOUND if missing.
    2. Validate `target.isAnchorNode === true` — else VALIDATION_ERROR.
    3. No-op guard: if `target.flagged === true && target.prunedFromTrellis === true`, return `{ success: true }` (no journal/emit/delegate call).
    4. Snapshot `before = { flagged: target.flagged ?? false, prunedFromTrellis: target.prunedFromTrellis ?? false }`.
    5. Call `trellisActionsService.prune(anchorId)` — DELEGATE per R6 / D-14. This patches the question AND emits ANCHOR_DELETED (preserving PrunedSection's existing subscriber chain).
    6. Append journal: `graphEditJournal.append({ cmd: 'prune', targetIds: [anchorId], before, after: { flagged: true, prunedFromTrellis: true } })`.
    7. Emit `{ type: 'GRAPH_UPDATED', payload: { kind: 'prune', anchorId } }` (D-17). NOTE: trellisActionsService.prune emits ANCHOR_DELETED, NOT GRAPH_UPDATED — so the command-boundary GRAPH_UPDATED emit is the ONLY GRAPH_UPDATED. No double-emit risk on this verb.
    8. Return `{ success: true }`.

    Test file `app/tests/services/graph-command-service.prune.test.mjs`:
    - Seed store with anchor-A (flagged=false).
    - Call `prune('anchor-A')`. Assert `flagged === true`, `prunedFromTrellis === true`, journal entry written, GRAPH_UPDATED emitted with `payload.kind === 'prune'`, ANCHOR_DELETED also emitted (assert via separate subscriber).
    - Already-pruned no-op: setup anchor with `flagged: true, prunedFromTrellis: true`; prune; assert journal length unchanged + no GRAPH_UPDATED emit.
    - QA-not-allowed: setup `qa-1` (not anchor); prune → VALIDATION_ERROR.
    - NOT_FOUND for missing target.
    - Delegation assertion: spy on `trellisActionsService.prune` (or assert side-effect on store) to confirm the delegated call ran.

    Add `graph-command-service.prune.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.prune.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+prune\s*\(" app/src/services/graph-command.service.ts` finds implementation.
    - Source delegates to trellisActionsService: `grep -F "trellisActionsService.prune" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains the `isAnchorNode` validation: `grep -F "isAnchorNode" app/src/services/graph-command.service.ts` returns >= 2 (one each in detach + prune).
    - `cd app && npm run test:actions -- tests/services/graph-command-service.prune.test.mjs` exits 0.
    - Test file contains: success case with both flagged + prunedFromTrellis assertion, ANCHOR_DELETED emit assertion (R6 PrunedSection invariant), GRAPH_UPDATED with kind=prune assertion, no-op for already-pruned, VALIDATION_ERROR for QA target, NOT_FOUND for missing.
    - `package.json` `test:actions` script includes `graph-command-service.prune.test.mjs`.
  </acceptance_criteria>
  <done>
    Prune delegates to existing trellisActionsService.prune (preserving ANCHOR_DELETED → PrunedSection chain), writes one journal entry, emits one GRAPH_UPDATED. R6 consolidation strategy honored (delegate, don't replace).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graphCommandService.detach ↔ LLM provider | Detach fires `classifyAndAnchorIncremental` fire-and-forget; if LLM is malicious/MITM'd, classification may route the detached QA to a wrong anchor. Same risk surface as every other Trellis classification path — no new exposure. |
| graphCommandService.merge ↔ embedding provider | Survivor re-embed sends survivor title to embedding provider; failure mode is graceful degradation (D-11; Blocker #4 fix preserves old vector instead of clearing). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | Tampering | Merge loser hard-deleted, child reparent partial-failure (e.g., patchQuestion throws mid-loop) | accept | Single localStorage path — patchQuestion is atomic per-call. If a throw happens mid-batch, store is in a partial state. Acceptable for local-only single-user app; mutex prevents concurrent corruption. Worst case: user retries merge, no-op guards prevent double-reparent. |
| T-48-10 | Information Disclosure | Merge journal entry persists full loser record (including potentially sensitive QA content) | accept | Same data already in localStorage before merge; journal snapshot persists existing access. PRIVACY-01 (Phase 53) sanitizer covers provider payloads, not local journal. |
| T-48-11 | Denial of Service | Detach's downstream classifyAndAnchorIncremental spins indefinitely | mitigate | AbortSignal threading (D-19); LOCALE_CHANGED or operator-initiated cancel terminates the classify call at next checkpoint. Phase 27 pattern. Warning #2 fix enforces this via explicit test. |
| T-48-15 | Integrity | Merge partial-state on failed loser delete (children reparented + survivor updated, loser still present) | accept | Blocker #2 fix: on delete failure, the command returns STORAGE_ERROR without journaling. Operator can retry; no-op guards prevent double-reparent (children already have parentId=survivorId). Survivor's qaCount may be off-by-N on retry — researcher accepts; integration test in Plan 04 verifies. |
</threat_model>

<verification>
- `cd /Users/Code/EchoLearn/app && npm run test:actions -- tests/services/graph-command-service.merge.test.mjs tests/services/graph-command-service.detach.test.mjs tests/services/graph-command-service.prune.test.mjs` exits 0.
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit` exits 0.
- `cd /Users/Code/EchoLearn/app && npm test` full suite exits 0.
- `grep -c "questionService.delete" app/src/services/graph-command.service.ts` returns >= 2 (merge step 7 + delete from Plan 02).
- All seven command methods now implemented (no NOT_IMPLEMENTED stubs except `undo`, which Plan 04 fills): `grep -c "code: 'NOT_IMPLEMENTED'" app/src/services/graph-command.service.ts` returns exactly 1 (only `undo`).
</verification>

<success_criteria>
- All three multi-record commands deliver GRAPH-03:
  - merge consolidates duplicates with full undo-ready snapshot AND graceful-degradation embedding (Blocker #4) AND command-boundary typed GRAPH_UPDATED emit (Warning #4) AND delete-success check (Blocker #2).
  - detach clears placement and triggers re-routing with cancellable AbortSignal (Warning #2 enforced).
  - prune delegates without breaking PrunedSection's ANCHOR_DELETED chain.
- One journal entry per command. Last-observed GRAPH_UPDATED has `payload.kind === cmd` (subscriber dedup pattern works). Merge produces 2 GRAPH_UPDATED events (one untyped from questionService.delete, one typed from command boundary — accepted; subscribers idempotent per CLAUDE.md).
- Phase 49-ready return shapes per R9: merge returns `{ reparentedCount, newSurvivorQaCount }`; detach returns `void`; prune returns `void`.
- Wave 3 placement (Warning #1 fix) ensures Plan 03 starts only after Plan 02 has committed its skeleton + rename/move/delete to graph-command.service.ts — eliminates parallel-mutation risk.
</success_criteria>

<output>
Create `.planning/phases/48-graph-command-service-and-trust-invariants/48-03-SUMMARY.md` when done.
</output>
</output>
