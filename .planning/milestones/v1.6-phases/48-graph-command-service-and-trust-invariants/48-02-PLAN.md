---
phase: 48-graph-command-service-and-trust-invariants
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - app/src/services/graph-command.service.ts
  - app/tests/services/graph-command-service.rename.test.mjs
  - app/tests/services/graph-command-service.move.test.mjs
  - app/tests/services/graph-command-service.delete.test.mjs
  - app/tests/services/_actions-mock-hooks.mjs
  - app/package.json
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
must_haves:
  truths:
    - "graphCommandService.rename validates (empty/whitespace/100-char cap), bypasses normalizeAnchorName, patches title+content+summary in one go; preserves retrieval identity by either patching embeddingVector alongside title (when re-embed succeeds) or leaving the old vector untouched (when embedding provider is unconfigured OR the re-embed call fails)."
    - "graphCommandService.move updates parentId/branchLabel/clusterLabel/clusterNodeId, writes one journal entry, emits one GRAPH_UPDATED."
    - "graphCommandService.delete hard-deletes the target, cascades children to the deleted node's parent (cluster for anchors, root for clusters), records full pre-image + reparented-children diff for undo."
    - "Every successful command writes exactly ONE GraphEditLogEntry. Emit invariant: the LAST GRAPH_UPDATED observed after a successful command has payload.kind === cmd, emitted from the command boundary. For delete (and Plan 03 merge), an ADDITIONAL untyped GRAPH_UPDATED fires from questionService.delete; subscribers already handle this idempotently (CLAUDE.md §Event bus). Intentional double-emit; documented inline."
    - "If questionService.delete returns { success: false }, the command aborts BEFORE writing the journal entry or emitting GRAPH_UPDATED from the command boundary — failed mutations are never journaled."
    - "At no point does the post-command Question have embeddingVector === undefined when it had a vector before the command — graceful degradation (D-11) guaranteed."
  artifacts:
    - path: "app/src/services/graph-command.service.ts"
      provides: "graphCommandService skeleton with rename, move, delete; placeholder method-stubs for merge/detach/prune/undo (Plans 03+04 fill these in)."
      exports: ["graphCommandService"]
    - path: "app/tests/services/graph-command-service.rename.test.mjs"
      provides: "Per-verb rename test suite using _actions-mock-hooks.mjs."
    - path: "app/tests/services/graph-command-service.move.test.mjs"
      provides: "Per-verb move test suite (anchor→cluster, QA→anchor)."
    - path: "app/tests/services/graph-command-service.delete.test.mjs"
      provides: "Per-verb delete test suite (anchor delete with child cascade, cluster delete, journal pre-image shape, failed-delete-aborts-journal)."
  key_links:
    - from: "app/src/services/graph-command.service.ts"
      to: "app/src/services/question.service.ts"
      via: "questionService.patchQuestion and questionService.delete — single write path (R1)"
      pattern: "questionService\\.(patchQuestion|delete|getAll)"
    - from: "app/src/services/graph-command.service.ts"
      to: "app/src/services/graph-edit-journal.service.ts"
      via: "graphEditJournal.append({ cmd, targetIds, before, after }) on every successful command"
      pattern: "graphEditJournal\\.append"
    - from: "app/src/services/graph-command.service.ts"
      to: "app/src/lib/event-bus.ts"
      via: "eventBus.emit({ type: 'GRAPH_UPDATED', payload: { kind, ... } }) from the command boundary (D-17). For delete: a second untyped GRAPH_UPDATED also fires from questionService.delete — accepted double-emit; subscribers are idempotent."
      pattern: "eventBus\\.emit\\(\\{\\s*type:\\s*'GRAPH_UPDATED'"
---

<objective>
Create `graph-command.service.ts` with three single-target commands: `rename`, `move`, `delete`. Build the skeleton so Plan 03 can extend the same file with `merge`, `detach`, `prune` and Plan 04 can add `undo`.

Purpose: deliver the canonical write-path discipline (read-fresh → snapshot before → patch via `questionService.patchQuestion` / `questionService.delete` → check delete success → append journal → emit GRAPH_UPDATED from command boundary) for the simpler single-target commands first. Plan 03's multi-record commands reuse the same boilerplate.

Output:
- `app/src/services/graph-command.service.ts` — service with `rename`, `move`, `delete` fully implemented; `merge`, `detach`, `prune`, `undo` as TODO stubs returning `{ success: false, error: { code: 'NOT_IMPLEMENTED', ... } }` (so Plan 03 + 04 don't fight over file-structure decisions).
- `app/tests/services/graph-command-service.rename.test.mjs`
- `app/tests/services/graph-command-service.move.test.mjs`
- `app/tests/services/graph-command-service.delete.test.mjs`
- `app/tests/services/_actions-mock-hooks.mjs` — extend resolve hook to stub `graph-edit-journal.service` only if needed (the journal is a leaf module using only localStorage; should NOT need stubbing — verify by importing it directly under the actions loader first).
- `app/package.json` — add `tests/services/graph-command-service.*.test.mjs` glob to `test:actions` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/Code/EchoLearn/CLAUDE.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md
@/Users/Code/EchoLearn/.planning/phases/48-graph-command-service-and-trust-invariants/48-VALIDATION.md
@/Users/Code/EchoLearn/app/src/services/question.service.ts
@/Users/Code/EchoLearn/app/src/services/canonical-knowledge.service.ts
@/Users/Code/EchoLearn/app/src/services/graph-edit-journal.service.ts
@/Users/Code/EchoLearn/app/src/types/index.ts
@/Users/Code/EchoLearn/app/src/lib/event-bus.ts
@/Users/Code/EchoLearn/app/src/services/trellis-actions.service.ts
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-question.mjs
@/Users/Code/EchoLearn/app/tests/services/_actions-mock-hooks.mjs
@/Users/Code/EchoLearn/app/tests/services/trellis-prune.test.mjs

<interfaces>
<!-- Existing surfaces this plan calls into. -->

From app/src/services/question.service.ts:

  // Single write path (R1) — every command writes via patchQuestion.
  patchQuestion(questionId: string, patch: Partial<Question>): void;

  // Single hard-delete path (R1). VERIFIED SIGNATURE at question.service.ts:565-571:
  //   async delete(id: string): Promise<ServiceResult<void>>
  // The implementation already emits QUESTION_DELETED + GRAPH_UPDATED (the GRAPH_UPDATED
  // is the UNTYPED variant — no payload — emitted at question.service.ts:569).
  // CRITICAL: callers MUST inspect the returned ServiceResult.success BEFORE writing
  // a journal entry or emitting their own GRAPH_UPDATED. Failed deletes are NEVER
  // journaled (see Warning #4 fix in Task 3).
  async delete(id: string): Promise<ServiceResult<void>>;

  // Read API — used to snapshot `before` and find target.
  getAll(opts?: { includeFlagged?: boolean }): Question[];

From app/src/services/graph-edit-journal.service.ts (Plan 01):

  graphEditJournal.append(entry: Omit<GraphEditLogEntry, 'id' | 'ts'>): GraphEditLogEntry;

From app/src/lib/event-bus.ts:

  eventBus.emit(event: AppEvent): void;

From app/src/types/index.ts (verified at types/index.ts:5-39):

  interface Question {
    id: string;
    timestamp: number;
    date: string;
    content: string;
    answer: string;
    summary: string;
    title?: string;
    storyHook?: string;
    parentId?: string;
    keywords: string[];
    relatedQuestionIds: string[];
    categoryIds: string[];
    reviewSchedule: ReviewSchedule;
    createdAt: number;
    rootLabel?: string;
    branchLabel?: string;
    clusterLabel?: string;
    nodeSummary?: string;
    placementReason?: string;
    lastReviewedAt?: number;
    pinned?: boolean;
    embeddingVector?: number[];
    flagged?: boolean;
    prunedFromTrellis?: boolean;
    isAnchorNode?: boolean;
    qaCount?: number;
    shortSummary?: string;          // <=80 words; verified at types/index.ts:36 (Warning #3 — exists; safe to fall back to in move/delete child-line builders)
    isClusterNode?: boolean;
    clusterNodeId?: string;
    // ... more fields out of scope for graph commands per R2
  }

  type ServiceResult<T> =
    | { success: true; data?: T }
    | { success: false; error: { code: string; message: string; retryable: boolean } };

<!-- Contract this plan establishes for Plan 03 + Plan 04 -->

In app/src/services/graph-command.service.ts:

  // Error codes (string literals — used by Plan 03/04 tests + Phase 49 UI)
  type GraphCommandErrorCode =
    | 'VALIDATION_ERROR'      // empty title, length cap, same-source/target merge, no-op rename
    | 'NOT_FOUND'             // target id not in store
    | 'STORAGE_ERROR'         // questionService.delete returned { success: false }
    | 'NOT_IMPLEMENTED';      // Plan 03/04 stubs

  export const graphCommandService: {
    rename(id: string, newTitle: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<void>>;
    move(id: string, newParentId: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<void>>;
    delete(id: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<{ cascadedChildIds: string[] }>>;
    // STUBS — Plan 03 + 04 implement these
    merge(loserId: string, survivorId: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<{ reparentedCount: number; newSurvivorQaCount: number }>>;
    detach(qaId: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<void>>;
    prune(anchorId: string, opts?: { signal?: AbortSignal }): Promise<ServiceResult<void>>;
    undo(): Promise<ServiceResult<{ undoneCmd: string; targetIds: string[]; summary: string }>>;
  };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create graph-command.service.ts skeleton + rename method with graceful-degradation embedding strategy</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.rename.test.mjs, app/tests/services/_actions-mock-hooks.mjs, app/package.json</files>
  <read_first>
    - app/src/services/question.service.ts lines 600-620 (patchQuestion — single write path)
    - app/src/services/question.service.ts lines 565-571 (delete signature — Promise<ServiceResult<void>>, emits QUESTION_DELETED + GRAPH_UPDATED)
    - app/src/services/canonical-knowledge.service.ts lines 769-786 (normalizeAnchorName — BYPASSED per D-16)
    - app/src/services/canonical-knowledge.service.ts lines 880-910 (anchor record shape — title/content/summary mirror)
    - app/src/services/graph-edit-journal.service.ts (Plan 01)
    - app/src/services/refill-mutex.ts (per-process mutex pattern; R10 risk 9 + risk 1 — serialization)
    - app/src/services/settings.service.ts (settingsService.getSync().embedding?.isConfigured)
    - app/src/providers/embedding/index.ts (embedText signature)
    - app/src/types/index.ts (Question + ServiceResult shapes; line 36 confirms `shortSummary?: string`)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-11, D-12, D-16, D-17, D-18, D-19)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R2 rename diff spec, §"Code Examples" Example 2
    - app/tests/services/_actions-mock-question.mjs (in-memory store + delete + patchQuestion mock)
    - app/tests/services/_actions-mock-hooks.mjs (current resolve hook — extend if needed)
    - app/tests/services/_actions-mock-embedding.mjs (for graceful-degradation test stubs)
    - app/tests/services/trellis-prune.test.mjs (canonical command-service test shape using actions mocks)
    - CLAUDE.md §"Anchor name normalization" (load-bearing — D-16 explicit bypass)
  </read_first>
  <behavior>
    - `rename('q-1', '')` → returns `{ success: false, error: { code: 'VALIDATION_ERROR', message: contains 'empty', retryable: false } }`; NO journal entry; NO GRAPH_UPDATED emit.
    - `rename('q-1', '   ')` → same VALIDATION_ERROR (whitespace-only).
    - `rename('q-1', 'A'.repeat(101))` → VALIDATION_ERROR (over 100-char cap).
    - `rename('not-real', 'X')` → `{ success: false, error: { code: 'NOT_FOUND', ... } }`.
    - `rename('q-1', '  Photosynthesis  ')` on a Question with `title: 'Photosyntheis'` AND `embedding.isConfigured === true` AND a working embedText: trims; calls `embedText('Photosynthesis', embCfg)`; on success, patches `{ title, content, summary, embeddingVector: <new vec> }` in ONE patchQuestion call. Returns `{ success: true }`. Journal contains one entry with `cmd: 'rename'`, `targetIds: ['q-1']`, `before.title === 'Photosyntheis'`, `before.embeddingVector === <old vec>`. GRAPH_UPDATED emitted exactly ONCE with `payload.kind === 'rename'` and `payload.anchorId === 'q-1'`.
    - **Graceful degradation — embedding unconfigured (Blocker #4 fix):** `rename('q-1', 'New Title')` when `settingsService.getSync().embedding?.isConfigured === false`: patches `{ title: 'New Title', content: 'New Title', summary: 'New Title' }` only. Does NOT touch `embeddingVector` (no clear, no overwrite). The old vector remains valid (slightly stale label but searchable per D-11). Journal `before` still snapshots the old `embeddingVector` so undo can restore it identically. GRAPH_UPDATED emitted once.
    - **Graceful degradation — embedding provider fails (Blocker #4 fix):** `rename('q-1', 'New Title')` when isConfigured=true but `embedText` rejects: catches the rejection, patches `{ title, content, summary }` only (no vector touch). Logs via `console.warn('[Trellis] rename re-embed failed:', err)`. Returns `{ success: true }`. Old vector retained.
    - **Invariant:** at no point does the post-command Question have `embeddingVector === undefined` when it had a vector before. (Test asserts this explicitly.)
    - `rename('q-1', 'Same Title')` on a Question already titled `'Same Title'` (case + whitespace identical post-trim) → `{ success: true }` BUT no journal entry, no GRAPH_UPDATED emit, no embedText call (R10 risk 11 — no-op).
    - normalizeAnchorName is NOT called: source must not import `normalizeAnchorName`, and a Question titled `'What is Photosynthesis'` renamed to `'What is Mitosis'` keeps the operator's literal `'What is Mitosis'` — no stripping of the "What is" prefix.
    - Concurrent serialization (R10 risk 9): if two rename calls fire in parallel against the SAME id, the second one observes the first's mutation (read-fresh inside command body — no held snapshots).
  </behavior>
  <action>
    Create `app/src/services/graph-command.service.ts`. Imports: `questionService` from `./question.service`, `graphEditJournal` from `./graph-edit-journal.service`, `eventBus` from `../lib/event-bus`, `settingsService` from `./settings.service`, `embedText` from `../providers/embedding`, types `Question` + `ServiceResult` from `../types`.

    Add a module-scope `createPromiseMutex`-style per-process mutex (mirror `app/src/services/refill-mutex.ts` pattern — single shared mutex instance for the whole service) so commands serialize against each other at the boundary. Each public method wraps its body in `_mutex.run(async () => {...})`.

    Implement `rename(id, newTitle, opts?)`:
    1. Trim `newTitle`; validate non-empty + ≤ 100 chars (D-16). On validation failure, return `{ success: false, error: { code: 'VALIDATION_ERROR', message, retryable: false } }`.
    2. Inside mutex: `const store = questionService.getAll({ includeFlagged: true });` (read fresh per Pattern 1, R10 risk 1). Find target. If missing, return NOT_FOUND.
    3. No-op guard (R10 risk 11): if `(target.title?.trim() ?? '') === trimmed`, return `{ success: true }` WITHOUT journal/emit/embed.
    4. Snapshot `before = { title: target.title, content: target.content, summary: target.summary, embeddingVector: target.embeddingVector }`. (The `embeddingVector` snapshot is preserved regardless of whether re-embed happens later — undo always restores to the pre-rename state.)
    5. **Embedding strategy (Blocker #4 fix — Graceful degradation matches D-11):**
       - Read `const embCfg = settingsService.getSync().embedding;`.
       - If `embCfg?.isConfigured !== true`: SKIP re-embed entirely. `questionService.patchQuestion(id, { title: trimmed, content: trimmed, summary: trimmed })` — note: `embeddingVector` is NOT in the patch object, so the existing vector is preserved untouched. Set `after = { title: trimmed, content: trimmed, summary: trimmed, embeddingVector: target.embeddingVector }` for journal symmetry.
       - Else (isConfigured=true): `let newVec: number[] | undefined; try { newVec = await embedText(trimmed, embCfg); } catch (err) { console.warn('[Trellis] rename re-embed failed:', err); newVec = undefined; }`. If `newVec` is `undefined` (provider rejected): patch `{ title, content, summary }` only (vector preserved). If `newVec` is a vector: patch `{ title, content, summary, embeddingVector: newVec }` in ONE atomic patchQuestion call. Set `after` accordingly.
    6. `graphEditJournal.append({ cmd: 'rename', targetIds: [id], before, after })`.
    7. `eventBus.emit({ type: 'GRAPH_UPDATED', payload: { kind: 'rename', anchorId: id } });` (D-17 single emit from command boundary).
    8. Return `{ success: true }`.

    **Critical inline comment at step 5:** `// Per Blocker #4 fix (revision 1): never overwrite a vector with undefined. Either the new vector replaces it atomically, or the old vector stays. Embedding-unconfigured and embed-failed paths both preserve the existing vector — retrieval identity degrades gracefully (slightly stale label) rather than silently breaking.`

    Stub `merge`, `detach`, `prune`, `undo` to return `{ success: false, error: { code: 'NOT_IMPLEMENTED', message: 'See Plan 48-03 / 48-04', retryable: false } }`. Plan 03/04 will replace these.

    Stub `move`, `delete` — they are filled in Tasks 2 + 3 of THIS plan. Keep the stubs initially with NOT_IMPLEMENTED so Task 1 can land independently and TS compilation passes.

    Test file `app/tests/services/graph-command-service.rename.test.mjs`:
    - Use the `_actions-mock-loader.mjs` register-hook pattern (the test runs under `test:actions`).
    - localStorage shim (copy from `trellis-prune.test.mjs:4-11`).
    - At top of EACH test: `storage.clear()` AND `_resetStore([...])` (R10 risk 8 — test isolation).
    - Subscribe to `GRAPH_UPDATED` events to assert single-emit.
    - Cover every `<behavior>` bullet INCLUDING:
      - Test: rename with `isConfigured=false` → vector preserved (stub embedding settings to unconfigured; assert post-rename `embeddingVector` is identical to pre-rename).
      - Test: rename with `embedText` rejection → vector preserved (use the actions-mock embedding to throw; assert post-rename `embeddingVector` is identical to pre-rename AND a console.warn was logged).
      - Test: rename with successful re-embed → vector REPLACED with new value in a SINGLE patchQuestion call (assert by spying on patchQuestion call count: exactly 1 call with both `title` AND `embeddingVector` in the patch).
      - Test: the "never undefined" invariant — after each of the three scenarios above, `target.embeddingVector` is defined (or is whatever it was pre-rename; never silently nulled).

    Extend `app/tests/services/_actions-mock-hooks.mjs`: verify whether `graph-edit-journal.service` needs stubbing. It only depends on localStorage + types/index.ts. Should NOT need stubbing — but the `node:module` resolve hook for relative imports without extensions (lines 76-88 of the existing file) must continue to find `.ts` files. Add NO new stubs unless an import fails; if a failure occurs, add a passthrough specifically for `graph-edit-journal.service` and `graph-edit-journal-phrasing` that does NOT short-circuit (lets it resolve to the real file).

    Extend `app/package.json`'s `test:actions` script — change from the explicit file list to add `tests/services/graph-command-service.rename.test.mjs` to the existing list (preserve the existing trellis-* entries).
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.rename.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const graphCommandService" app/src/services/graph-command.service.ts` returns 1.
    - File contains all 7 method names: `grep -cE "^\s+(rename|move|merge|detach|prune|delete|undo)\s*\(" app/src/services/graph-command.service.ts` returns >= 7.
    - File does NOT import `normalizeAnchorName` (D-16): `grep -F "normalizeAnchorName" app/src/services/graph-command.service.ts` returns 0.
    - File contains the per-process mutex pattern: `grep -F "refill-mutex" app/src/services/graph-command.service.ts` OR `grep -F "createPromiseMutex" app/src/services/graph-command.service.ts` returns >= 1.
    - File contains the Blocker #4 inline comment: `grep -F "never overwrite a vector with undefined" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains the isConfigured gate: `grep -F "isConfigured" app/src/services/graph-command.service.ts` returns >= 1.
    - Source does NOT contain a pattern like `embeddingVector: undefined` followed by a fire-and-forget re-embed (the regressed pattern): `grep -F "embeddingVector: undefined" app/src/services/graph-command.service.ts` returns 0 in the rename body (note: move/delete journals may legitimately reference undefined via destructured fields).
    - Rename test file exists; `cd app && npm run test:actions -- tests/services/graph-command-service.rename.test.mjs` exits 0.
    - Test asserts NORMALIZE bypass: at least one test renames `'q-1'` to `'What is Mitosis'` and asserts the stored title is exactly `'What is Mitosis'` (not stripped).
    - Test asserts SINGLE emit: at least one test subscribes to GRAPH_UPDATED, performs one rename, asserts `events.length === 1` and `events[0].payload.kind === 'rename'`.
    - Test asserts NO emit on validation failure: at least one test sets up subscriber, calls `rename(id, '')`, asserts `events.length === 0`.
    - Test asserts NO emit on no-op rename: subscriber count remains 0 after a same-title rename.
    - Test asserts vector preservation when embedding unconfigured: explicit test case.
    - Test asserts vector preservation when embedText rejects: explicit test case.
    - Test asserts atomic single patchQuestion call when re-embed succeeds (no two-step clear-then-fill).
    - `npx tsc -b --noEmit` exits 0.
    - `package.json` `test:actions` script includes `graph-command-service.rename.test.mjs` (verify via `grep -F "graph-command-service.rename" app/package.json` returns 1).
  </acceptance_criteria>
  <done>
    Skeleton service + rename method shipped. Stubs in place for merge/detach/prune/undo (Plans 03/04) and move/delete (Tasks 2/3 of this plan). Mutex serialization in place. Test suite enforces D-16 bypass, D-17 single-emit, validation/no-op guards, and the Blocker #4 graceful-degradation invariant (vector never silently undefined).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement move(id, newParentId) — anchor→cluster and QA→anchor</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.move.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (the skeleton from Task 1)
    - app/src/services/graph.service.ts lines 180-200 (existing dead moveToParent — reference only; will NOT delegate to it)
    - app/src/services/canonical-knowledge.service.ts lines 940-975 (commitClassificationResult — pattern for setting placement fields on a QA)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-12)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R2 "move" diff spec (lines 186-216) and §R10 risk 12 (no-op move)
    - app/src/types/index.ts (Question shape — focus on parentId, branchLabel, clusterLabel, clusterNodeId, qaCount, nodeSummary, shortSummary)
  </read_first>
  <behavior>
    - `move('anchor-1', 'cluster-B')` where anchor-1's current `clusterNodeId` is `'cluster-A'`: updates anchor-1's `clusterNodeId`, `parentId`, `branchLabel`, `clusterLabel` to inherit from cluster-B; sets `placementReason: 'Manually moved under {newBranch} > {newCluster}'`. Returns `{ success: true }`. Journal one entry with `cmd: 'move'`, `before` containing old placement fields. One GRAPH_UPDATED emit with `payload.kind === 'move'`.
    - `move('qa-1', 'anchor-B')` where qa-1's current parent is `'anchor-A'`: updates qa-1's `parentId`, `clusterNodeId` (from anchor-B), `branchLabel`, `clusterLabel`; sets `placementReason: 'Manually moved under {branch} > {cluster} > {anchor}'`.
    - Side effects on OLD parent (recompute per R2): `qaCount` decrements by 1; if old parent is an anchor, `nodeSummary` line for this QA is removed.
    - Side effects on NEW parent: `qaCount` increments by 1; if new parent is an anchor, `nodeSummary` gets an appended `[qaId] shortSummary` line. Use `target.shortSummary ?? target.content.slice(0, 80)` for the line text — Warning #3 verified: `shortSummary?: string` exists on Question type at types/index.ts:36, so the fallback is correct.
    - `move('q-1', 'q-1')` → VALIDATION_ERROR (cannot be own parent).
    - `move('q-1', target.parentId)` where the new parent equals the current parent → `{ success: true }` BUT no journal entry, no GRAPH_UPDATED emit (R10 risk 12 — no-op).
    - `move('not-real', 'cluster-A')` → NOT_FOUND.
    - `move('q-1', 'not-real')` → NOT_FOUND.
    - Validation: refuses to move a Question UNDER one of its own descendants (cycle prevention). If `newParentId` is in the descendant subtree of `id`, return VALIDATION_ERROR with `code: 'VALIDATION_ERROR'` and message containing 'cycle' or 'descendant'.
  </behavior>
  <action>
    Replace the `move` stub in `app/src/services/graph-command.service.ts`. Wrap body in the shared mutex. Read fresh store. Find target and newParent. Validation:
    - target exists; newParent exists; targetId !== newParentId; new parent not in target's descendant subtree (BFS over store filtering by `parentId === current`).
    - if `target.parentId === newParentId` → no-op return `{ success: true }` (no journal/emit).

    For each move case (anchor vs QA — detect by `target.isAnchorNode`):
    - ANCHOR move: patch target with `{ parentId: newParent.id, clusterNodeId: newParent.id, branchLabel: newParent.branchLabel, clusterLabel: newParent.clusterLabel ?? newParent.title, placementReason: 'Manually moved under ${newParent.branchLabel} > ${newParent.title}' }`.
    - QA move: patch target with `{ parentId: newParent.id, clusterNodeId: newParent.clusterNodeId, branchLabel: newParent.branchLabel, clusterLabel: newParent.clusterLabel, placementReason: 'Manually moved under ${newParent.branchLabel} > ${newParent.clusterLabel} > ${newParent.title}' }`.

    Side-effect recomputation on OLD parent (if `target.parentId` was set):
    - Find OLD parent in store. New `qaCount = (old.qaCount ?? 1) - 1` (clamp at 0).
    - If OLD parent is an anchor (`isAnchorNode`), strip the line matching `[${target.id}] ` from `nodeSummary` (split on `\n`, filter, rejoin).
    - `questionService.patchQuestion(oldParent.id, { qaCount: newQaCount, nodeSummary: newSummary })`.

    Side-effect on NEW parent: `qaCount = (newParent.qaCount ?? 0) + 1`. If NEW parent is an anchor, append `\n[${target.id}] ${target.shortSummary ?? target.content.slice(0, 80)}` to `nodeSummary` (Warning #3 verified — `shortSummary?: string` exists on Question type; this fallback is correct).

    Journal: `before = { parentId, clusterNodeId, branchLabel, clusterLabel, placementReason }` snapshot of target before patch. `after = { parentId: newParent.id, clusterNodeId, branchLabel, clusterLabel, placementReason }`. `targetIds: [id]`. Side-effect qaCount/nodeSummary changes on old/new parents are NOT stored in journal (R2: "recomputed deterministically on undo by walking children").

    Emit ONE `GRAPH_UPDATED` with `payload: { kind: 'move', anchorId: id, affectedIds: [id, newParent.id, ...(oldParentId ? [oldParentId] : [])] }`.

    Test file `app/tests/services/graph-command-service.move.test.mjs` covers every `<behavior>` bullet. Use the `_actions-mock-question.mjs` store with seeded anchors and clusters. Subscribe to GRAPH_UPDATED for emit-count assertions. Cover cycle-prevention by setting up `cluster-A → anchor-B → qa-C` then attempting `move(cluster-A, qa-C)` and asserting VALIDATION_ERROR.

    Add `graph-command-service.move.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.move.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+move\s*\(" app/src/services/graph-command.service.ts` finds the implementation (not a stub).
    - Source mentions cycle/descendant check — `grep -E "descendant|cycle" app/src/services/graph-command.service.ts` returns >= 1.
    - Source uses the shortSummary fallback for nodeSummary lines: `grep -F "shortSummary" app/src/services/graph-command.service.ts` returns >= 1.
    - `cd app && npm run test:actions -- tests/services/graph-command-service.move.test.mjs` exits 0.
    - Test file contains tests for: anchor-move, QA-move, self-parent rejection, same-parent no-op (no journal/emit), missing target (NOT_FOUND), cycle rejection.
    - Test asserts qaCount decrement on old parent: setup parent with `qaCount: 3` and 3 children; move 1 child away; assert new old parent `qaCount === 2`.
    - Test asserts SINGLE emit per successful move: `events.length === 1` with `payload.kind === 'move'`.
    - `package.json` `test:actions` script includes `graph-command-service.move.test.mjs`.
  </acceptance_criteria>
  <done>
    Move command works for both anchor and QA targets. Cycle prevention enforced. qaCount and nodeSummary recomputed on both old + new parents using the verified shortSummary fallback. One journal entry and one GRAPH_UPDATED per successful move.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement delete(id) — hard removal + cascade child re-parenting + emit from command boundary</name>
  <files>app/src/services/graph-command.service.ts, app/tests/services/graph-command-service.delete.test.mjs, app/package.json</files>
  <read_first>
    - app/src/services/graph-command.service.ts (after Tasks 1-2)
    - app/src/services/question.service.ts lines 560-575 (`delete(id)` returns Promise<ServiceResult<void>> AND emits QUESTION_DELETED + untyped GRAPH_UPDATED — Warning #4 fix: emit our typed GRAPH_UPDATED AFTER success check)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-CONTEXT.md (D-12, D-15, D-17)
    - .planning/phases/48-graph-command-service-and-trust-invariants/48-RESEARCH.md §R2 "delete" diff spec (lines 276-291) and §R10 risk 7 (cluster delete cascade)
    - CLAUDE.md §"Event bus — unified GRAPH_UPDATED" (subscribers are already idempotent; double-emit is acceptable)
  </read_first>
  <behavior>
    - `delete('anchor-A')` where anchor-A has 3 child QAs:
        - Children re-parent to `anchor-A.parentId` (the cluster) per D-15 / R2 cascade rule. Each child's `parentId`, `clusterNodeId`, `branchLabel`, `clusterLabel` inherit from the cluster (= anchor-A's parent).
        - `anchor-A` is hard-deleted via `await questionService.delete('anchor-A')`.
        - **Blocker #2 fix:** The returned `ServiceResult` is checked. If `success === false`, the command aborts BEFORE writing the journal entry or emitting GRAPH_UPDATED from the command boundary. Returns `{ success: false, error: { code: 'STORAGE_ERROR', message: <delegated message>, retryable: true } }`. No journal entry. No command-boundary emit.
        - On `success === true`: returns `{ success: true, data: { cascadedChildIds: ['qa-1', 'qa-2', 'qa-3'] } }`. One journal entry with `cmd: 'delete'`, `before = { deletedRecord: <full anchor-A Question>, reparentedChildren: [{id:'qa-1', parentId:'anchor-A', clusterNodeId:..., branchLabel:..., clusterLabel:...}, ...] }` per R2.
    - `delete('cluster-X')` where cluster-X has 2 child anchors (each with their own QAs): per R10 risk 7, child anchors re-parent to root (no parentId; clear branchLabel etc.); QAs of those anchors are NOT touched (only single-level cascade). Cluster is hard-deleted.
    - `delete('qa-1')` (a leaf Q&A): no children to cascade; just hard-delete + journal `before = { deletedRecord: <full qa Question>, reparentedChildren: [] }`.
    - `delete('not-real')` → NOT_FOUND.
    - **Emit invariant (Warning #4 fix — Option C):** On success, TWO `GRAPH_UPDATED` events fire in quick succession:
      1. The UNTYPED `{ type: 'GRAPH_UPDATED' }` from `questionService.delete` at question.service.ts:569 (inherited from the existing single hard-delete path; we cannot suppress it).
      2. The TYPED `{ type: 'GRAPH_UPDATED', payload: { kind: 'delete', anchorId: id, affectedIds: [id, ...cascadedChildIds] } }` from the command boundary AFTER the delete succeeds AND the journal entry is appended.
      The LAST event observed by subscribers therefore has `payload.kind === 'delete'`. Subscribers are already idempotent per CLAUDE.md §Event bus, so re-reading the store twice is harmless. Inline comment documents this as intentional.
    - cascadedChildIds in the journal `before.reparentedChildren` lists ONLY IDs + their old placement fields (not full child records) — children stay in the store, only their parentage changed (R10 risk 3).
    - Invariant: if questionService.delete fails, no journal entry is written and no command-boundary GRAPH_UPDATED emit occurs.
  </behavior>
  <action>
    Replace the `delete` stub in `app/src/services/graph-command.service.ts`. Inside mutex:
    1. Read fresh store.
    2. Find target; if missing, return NOT_FOUND.
    3. Compute children: `store.filter(q => q.parentId === id)`.
    4. Snapshot `deletedRecord = { ...target }` (full Question record per D-04). Snapshot `reparentedChildren = children.map(c => ({ id: c.id, parentId: c.parentId, clusterNodeId: c.clusterNodeId, branchLabel: c.branchLabel, clusterLabel: c.clusterLabel }))`.
    5. Cascade re-parent (R2 + R10 risk 7):
       - For each child, compute the new placement fields:
         - If target is an ANCHOR: child re-parents to `target.parentId` (the cluster). New `clusterNodeId = target.clusterNodeId`, `branchLabel = target.branchLabel`, `clusterLabel = target.clusterLabel`, `parentId = target.parentId`.
         - If target is a CLUSTER: child anchor re-parents to root. New `parentId = undefined`, `clusterNodeId = undefined`, `branchLabel = undefined`, `clusterLabel = undefined`. (Per R10 risk 7: single-level cascade — DO NOT recurse into the QAs of the child anchors.)
         - If target is a Q&A (no children expected — leaf): cascade loop is empty.
       - Call `questionService.patchQuestion(child.id, {...newPlacement})` for each child.
    6. **Blocker #2 fix:** `const deleteResult = await questionService.delete(id);` — the return type is `Promise<ServiceResult<void>>` per question.service.ts:565-571. If `deleteResult.success === false`: return `{ success: false, error: { code: 'STORAGE_ERROR', message: deleteResult.error?.message ?? 'Hard delete failed.', retryable: true } }`. DO NOT append a journal entry. DO NOT emit from the command boundary. (Children have already been re-parented — they remain re-parented; this is acceptable since the operator can retry the delete and the no-op guards prevent double-reparent. Inline comment notes the partial-state acceptance.)
    7. On `deleteResult.success === true`: append journal entry `graphEditJournal.append({ cmd: 'delete', targetIds: [id], before: { deletedRecord, reparentedChildren }, after: {} })`.
    8. **Warning #4 fix (Option C — emit from command boundary):** AFTER the journal append, emit `eventBus.emit({ type: 'GRAPH_UPDATED', payload: { kind: 'delete', anchorId: id, affectedIds: [id, ...children.map(c => c.id)] } });`. Add an inline comment: `// NOTE: questionService.delete at step 6 already emitted an untyped GRAPH_UPDATED (question.service.ts:569). We emit a SECOND, typed GRAPH_UPDATED here so subscribers that filter on payload.kind === 'delete' see the discriminator. Subscribers are already idempotent per CLAUDE.md §"Event bus — unified GRAPH_UPDATED" — re-reading store twice is harmless. The LAST event observed has payload.kind === 'delete' (subscriber dedup pattern works as expected).`
    9. Return `{ success: true, data: { cascadedChildIds: children.map(c => c.id) } }`.

    Test file `app/tests/services/graph-command-service.delete.test.mjs`:
    - Anchor delete with 3 children → asserts children's parentId is now the cluster's id; asserts target removed from store.
    - Cluster delete with 2 child anchors → asserts anchors have parentId=undefined; asserts CHILDREN of those anchors (the QAs) are UNCHANGED.
    - Leaf QA delete → no cascade; just removed.
    - NOT_FOUND for missing target.
    - **Failed-delete-aborts-journal test (Blocker #2 fix):** stub `_actions-mock-question.mjs` to make delete return `{ success: false, error: {...} }`; call delete; assert result is STORAGE_ERROR; assert journal length is 0 after the call; assert NO command-boundary GRAPH_UPDATED was emitted (only the untyped one from the mocked questionService.delete — and even that may be absent if the mock simulates failure without emitting; the test asserts on COMMAND-BOUNDARY emit specifically by filtering for `payload.kind === 'delete'`).
    - **Emit-discrimination test (Warning #4 fix):** subscribe to GRAPH_UPDATED. Call delete on success path. Assert `events.length === 2`. Assert `events[events.length - 1].payload?.kind === 'delete'` (last event has the kind discriminator). Document the double-emit invariant inline in the test.
    - Journal `before.deletedRecord` contains the full Question (verify by checking title, content, isAnchorNode all match).
    - Journal `before.reparentedChildren` length matches children count and entries have OLD parentId (not the new one).

    Add `graph-command-service.delete.test.mjs` to package.json `test:actions` script.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app &amp;&amp; npm run test:actions -- tests/services/graph-command-service.delete.test.mjs &amp;&amp; npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s+async\s+delete\s*\(" app/src/services/graph-command.service.ts` finds the implementation.
    - Source checks deleteResult.success: `grep -E "deleteResult\.success|result\.success" app/src/services/graph-command.service.ts` returns >= 1 within the delete body.
    - Source emits GRAPH_UPDATED with kind=delete from command boundary AFTER the await: `grep -F "kind: 'delete'" app/src/services/graph-command.service.ts` returns >= 1.
    - Source contains the double-emit-intentional inline comment: `grep -F "subscribers are already idempotent" app/src/services/graph-command.service.ts` OR `grep -F "LAST event observed" app/src/services/graph-command.service.ts` returns >= 1.
    - `cd app && npm run test:actions -- tests/services/graph-command-service.delete.test.mjs` exits 0.
    - Test file contains anchor-cascade test, cluster-cascade test, leaf-delete test, NOT_FOUND test, failed-delete-aborts-journal test (Blocker #2), emit-discrimination test (Warning #4 — asserts last event has payload.kind === 'delete'), journal-shape test.
    - Test asserts failed-delete behavior: subscribe before call, simulate questionService.delete failing, call delete, assert events with `payload?.kind === 'delete'` array length is 0 AND journal length is 0.
    - Test asserts journal contains `deletedRecord` with FULL question fields (title + content + isAnchorNode all present and matching pre-delete values).
    - `package.json` `test:actions` script includes `graph-command-service.delete.test.mjs`.
  </acceptance_criteria>
  <done>
    Delete command hard-removes target, single-level cascades children to grandparent, checks delete success BEFORE journal/emit (Blocker #2), emits typed GRAPH_UPDATED with payload.kind='delete' from command boundary so Phase 49 can dispatch on it (Warning #4 — accepts the double-emit; subscribers are idempotent). Plan 04's undo will resurrect via the journal's full record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graphCommandService (boundary) ↔ questionService | All graph mutations route through `questionService.patchQuestion` + `questionService.delete` — single write path (R1). New commands MUST NOT add a second writer. |
| graphCommandService ↔ journal | Each successful command writes exactly one journal entry; tampered journal entries are handled by Plan 04's `isValidPreImage` (T-48-01 inherited from Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Tampering | Bypass of single-write discipline | mitigate | All commands use `questionService.patchQuestion` / `questionService.delete` — no direct `localStorage.setItem('trellis_questions', ...)`. Verified by `grep -c "localStorage.setItem" app/src/services/graph-command.service.ts` returning 0. |
| T-48-06 | Repudiation | Operator unsure what action ran | mitigate | Each command writes one journal entry with `cmd` discriminator + `targetIds` + `before` + `after`. Inspectable via `localStorage.getItem('trellis_graph_edit_log')` — sufficient audit trail for single-user local app. |
| T-48-07 | Information Disclosure | `deletedRecord` snapshot retains full Question (including potentially private QA content) in localStorage | accept | Same data is already in localStorage's `trellis_questions` before delete; snapshot only persists the state user already had access to. PRIVACY-01 (Phase 53) sanitizer will scrub provider payloads, but the local journal stays as-is. |
| T-48-08 | Denial of Service | Pathological 200-child anchor delete | mitigate | Per R10 risk 3, we store only child IDs + 4 string fields per child (not full records). 200 children × ~150 bytes = 30KB; well within budget. |
| T-48-14 | Integrity | Partial-state on failed delete (children reparented, target NOT deleted) | accept | Blocker #2 fix: on delete failure, we return STORAGE_ERROR without journaling. Operator can retry; the no-op guards in move/delete prevent double-reparent. Children are in a valid placement state (under the cluster), which is the same state they'd be in after a successful retry. Acceptable for local-only single-user app. |
</threat_model>

<verification>
- `cd /Users/Code/EchoLearn/app && npm run test:actions -- tests/services/graph-command-service.rename.test.mjs tests/services/graph-command-service.move.test.mjs tests/services/graph-command-service.delete.test.mjs` exits 0.
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit` exits 0.
- `cd /Users/Code/EchoLearn/app && npm test` full suite exits 0.
- `grep -F "normalizeAnchorName" app/src/services/graph-command.service.ts` returns 0 (D-16 bypass enforced).
- `grep -F "localStorage.setItem" app/src/services/graph-command.service.ts` returns 0 (no direct writes — T-48-05).
- `grep -F "isConfigured" app/src/services/graph-command.service.ts` returns >= 1 (Blocker #4 graceful degradation gate).
</verification>

<success_criteria>
- `rename`, `move`, `delete` deliver:
  - Single boundary write discipline (GRAPH-01).
  - Preserved parent IDs / labels / cluster IDs / counts / summaries on move (GRAPH-02).
  - Source Q&A content preservation on cascading delete (GRAPH-03).
  - Reload survival via localStorage journal entries (success criterion 3 — to be end-to-end verified in Plan 04 integration).
- File structure ready for Plan 03 (merge/detach/prune) and Plan 04 (undo) — stubs in place; no method-name collisions.
- Threat model T-48-05 (no second writer), T-48-06 (audit trail), T-48-08 (DoS), T-48-14 (partial-state acceptance) addressed.
- Blocker #2 + Blocker #4 + Warning #3 + Warning #4 from plan-checker revision-round-1 are closed.
</success_criteria>

<output>
Create `.planning/phases/48-graph-command-service-and-trust-invariants/48-02-SUMMARY.md` when done.
</output>
</output>
