---
phase: 52-podcast-quality-defaults-and-learner-controls
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/types/index.ts
  - app/src/services/podcast-prompt.ts
  - app/tests/services/podcast-prompt.test.mjs
  - app/tests/services/podcast-options.test.mjs
  - app/tests/screens/PodcastScreen.options.test.mjs
autonomous: true
requirements:
  - PODCAST-01
  - PODCAST-02
  - PODCAST-04

must_haves:
  truths:
    - "PodcastLength + PodcastStyle + PodcastOptions types compile and are exported from app/src/types/index.ts."
    - "PodcastLength enum has exactly four members: brief, standard, deep, extended (per D-01)."
    - "PodcastStyle enum has exactly three members: focused, conversational, review (per D-02)."
    - "buildPodcastPrompt(conceptLines, options) returns { system, user } for every length × style combination (4 × 3 = 12)."
    - "Every assembled system prompt contains all five section names (RECAP, CONNECTIONS, MISCONCEPTION, RETRIEVAL QUESTIONS, NEXT ACTION) regardless of length (PODCAST-01, PODCAST-04)."
    - "Every assembled system prompt contains the coverage constraint substring (case-insensitive 'must mention every concept')."
    - "computeOptionsHash(conceptIds, locale, options) is deterministic across conceptId order and changes when any input changes."
    - "Wave 0 test files exist and run under `node --test` with red-then-green progression (tests fail until podcast-prompt.ts + later plans land — but the screen test asserting source-read invariants is allowed to fail until 52-03)."
  artifacts:
    - path: "app/src/types/index.ts"
      provides: "PodcastLength, PodcastStyle, PodcastOptions exports; additive optional fields on DailyPodcast, PodcastSettings, TTSConfig"
      contains: "export type PodcastLength"
    - path: "app/src/services/podcast-prompt.ts"
      provides: "Leaf-module prompt assembly + optionsHash"
      exports: ["buildPodcastPrompt", "computeOptionsHash"]
    - path: "app/tests/services/podcast-prompt.test.mjs"
      provides: "~12 prompt assembly tests (4 lengths × 3 styles + coverage + sections)"
    - path: "app/tests/services/podcast-options.test.mjs"
      provides: "~8 cache + hash + signature source-reading invariants"
    - path: "app/tests/screens/PodcastScreen.options.test.mjs"
      provides: "~6 source-reading invariants for chip selectors + dirty badge + playback rate + locale parity"
  key_links:
    - from: "app/src/services/podcast-prompt.ts"
      to: "app/src/types/index.ts"
      via: "type-only import"
      pattern: "import type \\{[^}]*PodcastOptions"
    - from: "app/tests/services/podcast-prompt.test.mjs"
      to: "app/src/services/podcast-prompt.ts"
      via: "dynamic import + source read"
      pattern: "podcast-prompt"
---

<objective>
Wave 0 establishes the type contracts and leaf prompt module that Waves 1 + 2 implement against, plus the red tests that fail until later plans land.

Purpose: Interface-first task ordering per planner guidance — downstream plans implement against a known type surface and prompt API. Without these contracts, Wave 1 (`podcast.service.ts` integration) would have to invent them on the fly.

Output:
- New types: `PodcastLength` (4 members), `PodcastStyle` (3 members), `PodcastOptions` interface, plus additive optional `?:` fields on `DailyPodcast` (`options`, `optionsHash`), `PodcastSettings` (`defaultLength`, `defaultStyle`), `TTSConfig` (`model`).
- New leaf module `app/src/services/podcast-prompt.ts` exporting `buildPodcastPrompt` (assembles BASE + SECTION + COVERAGE + LENGTH_MAP[length] + STYLE_MAP[style]) and `computeOptionsHash` (deterministic JSON-string hash of sorted `conceptIds` + `locale` + `length` + `style`).
- Three Wave-0 test files. The prompt + options tests turn green at the end of Wave 0. The PodcastScreen test stays red until Wave 2 wires the UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-RESEARCH.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md
@.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-VALIDATION.md
@CLAUDE.md
@app/src/types/index.ts
@app/src/lib/i18n-leaf.ts
@app/tests/services/classification-dedup.test.mjs
@app/tests/services/reorg-prompt-journal-injection.test.mjs
@app/tests/screens/ReviewScreen.anchor-empty-state.test.mjs

<interfaces>
<!-- Existing exports the new code consumes; extracted from src/types/index.ts and src/lib/i18n-leaf.ts. -->

From app/src/types/index.ts (existing — to be extended additively):
- export interface DailyPodcast { id, date, questionIds, script, audioPath?, audioDataUri?, duration?, status, progress?, error?, createdAt }  (line 187)
- export type PodcastStatus = 'pending' | 'generating' | 'ready' | 'failed'  (line 202)
- export interface TTSConfig { provider, apiKey?, baseUrl?, voice, speed, isConfigured }  (line 269)
- export interface PodcastSettings { sleepTime, advanceMinutes, autoGenerate }  (line 284)
- export type SupportedLocale = 'en' | 'zh' | 'es' | 'ja'  (existing locale type)

From app/src/lib/i18n-leaf.ts:
- export function t(key: string, opts?: Record<string, unknown>): string
- export function getCurrentLocale(): string
- export function bindI18nLeaf(tFn, getLocale): void

From researcher-locked prompt slices (52-PATTERNS.md):
- LENGTH_MAP keys: 'brief' | 'standard' | 'deep' | 'extended'  (D-01: FOUR options)
- STYLE_MAP keys: 'focused' | 'conversational' | 'review'  (D-02)
- COVERAGE_CONSTRAINT literal must contain substring matching /MUST mention every concept/i
- Section names that must appear literally in the system prompt: RECAP, CONNECTIONS, MISCONCEPTION, RETRIEVAL QUESTIONS, NEXT ACTION
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PodcastLength + PodcastStyle + PodcastOptions types and additive optional fields</name>
  <files>app/src/types/index.ts</files>
  <read_first>
    - app/src/types/index.ts (entire file — must understand existing DailyPodcast/PodcastSettings/TTSConfig shapes at lines 187, 269, 284 before extending)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-01, D-02, D-07, D-11, D-14)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/src/types/index.ts (MODIFIED — additive optional fields)")
    - ~/.claude/projects/-Users-Code-EchoLearn/memory/feedback_no_normalize_for_optional_fields.md (load-bearing rule: no migration code, read-site fallback only)
  </read_first>
  <behavior>
    - Importing `PodcastLength` from '../types' returns a type alias accepting exactly the four string literals: 'brief', 'standard', 'deep', 'extended'. No fifth member, no broader `string`.
    - Importing `PodcastStyle` returns a type alias accepting exactly: 'focused', 'conversational', 'review'.
    - Importing `PodcastOptions` returns an interface with required `length: PodcastLength` and required `style: PodcastStyle`.
    - `DailyPodcast` gains optional `options?: PodcastOptions` and `optionsHash?: string` without breaking any existing usage (all existing readers omit them).
    - `PodcastSettings` gains optional `defaultLength?: PodcastLength` and `defaultStyle?: PodcastStyle`.
    - `TTSConfig` gains optional `model?: string`.
    - `tsc -b --noEmit` from `app/` exits 0 with no new errors.
  </behavior>
  <action>
    In `app/src/types/index.ts`, immediately after the existing `PodcastStatus` declaration (around line 202), add four exports per D-01 (Length has FOUR members: 'brief', 'standard', 'deep', 'extended') and D-02 (Style has THREE members: 'focused', 'conversational', 'review'):
    - `export type PodcastLength = 'brief' | 'standard' | 'deep' | 'extended';`
    - `export type PodcastStyle = 'focused' | 'conversational' | 'review';`
    - `export interface PodcastOptions { length: PodcastLength; style: PodcastStyle; }`

    Extend the existing `DailyPodcast` interface (around line 187) by appending two new optional members on their own lines — `options?: PodcastOptions;` and `optionsHash?: string;` — with end-of-line comments noting `// Phase 52 PODCAST-03`.

    Extend the existing `PodcastSettings` interface (around line 284) by appending `defaultLength?: PodcastLength;` and `defaultStyle?: PodcastStyle;` — comment `// Phase 52 — undefined falls back at read site (D-14)`.

    Extend the existing `TTSConfig` interface (around line 269) by appending `model?: string;` — comment `// Phase 52 PODCAST-05 (D-07): default 'tts-1', opt-in 'tts-1-hd'`.

    Do NOT introduce a `normalize()` function, a migration helper, or any boot-time defaulting code. Per D-14 + operator memory `feedback_no_normalize_for_optional_fields.md`, additive `?:` fields are migrated by read-site fallback only.

    Do NOT add fields to any other interface. Do NOT reorder existing fields. Do NOT remove the `gptsovits` member of `TTSConfig.provider` union (D-09 deferred).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^export type PodcastLength = 'brief' \| 'standard' \| 'deep' \| 'extended';" app/src/types/index.ts` returns exactly one line.
    - `grep -nE "^export type PodcastStyle = 'focused' \| 'conversational' \| 'review';" app/src/types/index.ts` returns exactly one line.
    - `grep -nE "options\?: PodcastOptions;" app/src/types/index.ts` returns at least one line (inside DailyPodcast).
    - `grep -nE "optionsHash\?: string;" app/src/types/index.ts` returns at least one line.
    - `grep -nE "defaultLength\?: PodcastLength;" app/src/types/index.ts` returns one line; same for `defaultStyle\?: PodcastStyle;`.
    - `grep -nE "model\?: string;" app/src/types/index.ts` returns one line in the TTSConfig section.
    - `grep -c "normalize" app/src/types/index.ts` does not increase compared to before this task (no migration helpers introduced).
    - `cd app && npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>Types compile clean. Wave 1 + Wave 2 can import these names directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create podcast-prompt.ts leaf module (buildPodcastPrompt + computeOptionsHash) plus prompt + options tests</name>
  <files>app/src/services/podcast-prompt.ts, app/tests/services/podcast-prompt.test.mjs, app/tests/services/podcast-options.test.mjs</files>
  <read_first>
    - app/src/lib/i18n-leaf.ts (lines 1-26 — leaf-module header pattern, copy comment shape)
    - app/src/services/podcast.service.ts (lines 200-260 — current inline prompt literal at 204-211 that this module replaces)
    - app/tests/services/reorg-prompt-journal-injection.test.mjs (lines 1-100 — source-reading test analog)
    - app/tests/services/classification-dedup.test.mjs (lines 1-100 — structural source-read invariants analog)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (sections "app/src/services/podcast-prompt.ts" + "app/tests/services/podcast-prompt.test.mjs" + "app/tests/services/podcast-options.test.mjs")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-RESEARCH.md (R9 — prompt template; R7 — types; D-04..D-06 cache invalidation)
    - CLAUDE.md (§"i18n Workflow" — leaf-module rule prohibits JSON imports / lib/date / react-i18next)
  </read_first>
  <behavior>
    podcast-prompt.test.mjs (~12 tests):
    - `buildPodcastPrompt` is dynamically imported from the new module and called once per (length, style) combination — 4 × 3 = 12 cases.
    - Each call's `system` string matches /RECAP/ /CONNECTIONS/ /MISCONCEPTION/ /RETRIEVAL QUESTIONS/ /NEXT ACTION/ and /MUST mention every concept/i.
    - `buildPodcastPrompt(lines, { length: 'brief', style: 'focused' }).system` matches /150 words/.
    - `buildPodcastPrompt(lines, { length: 'standard', style: 'conversational' }).system` matches /225 words/.
    - `buildPodcastPrompt(lines, { length: 'deep', style: 'review' }).system` matches /450 words/.
    - `buildPodcastPrompt(lines, { length: 'extended', style: 'focused' }).system` matches /750 words/.
    - Given concept lines for three concepts, every concept name appears as a substring of the returned `user` string (coverage substring check).
    - Source-read assertion that `podcast-prompt.ts` does NOT contain `from '../locales/index'`, `from '../lib/date'`, or `from 'react-i18next'` (leaf-module rule).

    podcast-options.test.mjs (~8 tests). Some assertions reference `podcast.service.ts` source that lands in Plan 52-02 — these are EXPECTED to fail in Wave 0 and turn green in Wave 1. Use literal substring/regex assertions that exactly match the shapes Plan 52-02 will add:
    - `computeOptionsHash(['c1','c2','c3'], 'en', { length: 'standard', style: 'conversational' })` equals `computeOptionsHash(['c3','c1','c2'], 'en', { length: 'standard', style: 'conversational' })` (sort-order independence).
    - `computeOptionsHash([], 'en', { length: 'brief', style: 'focused' })` differs from `computeOptionsHash([], 'en', { length: 'deep', style: 'focused' })` (length change).
    - Same for style change, locale change, conceptIds change.
    - Source-read of `app/src/services/podcast.service.ts` asserts `generatePodcast(date: string, conceptIds?: string[], options?: PodcastOptions` substring present (red until Wave 1).
    - Source-read asserts `existing.optionsHash === computeOptionsHash(` substring present (red until Wave 1).
    - Source-read asserts `import` statement bringing `buildPodcastPrompt` and `computeOptionsHash` from `./podcast-prompt` (red until Wave 1).
    - Source-read asserts fallback literal `'standard'` AND `'conversational'` appear in `podcast.service.ts` (read-site defaults; red until Wave 1).
  </behavior>
  <action>
    Create `app/src/services/podcast-prompt.ts` as a leaf module:

    File header (copy comment shape from `app/src/lib/i18n-leaf.ts:1-26`) explains Phase 52 PODCAST-01..04, leaf-module rule (NO JSON imports, NO lib/date, NO react-i18next), inputs, outputs, and references CLAUDE.md §"i18n Workflow".

    Imports: type-only — `import type { PodcastOptions, PodcastLength, PodcastStyle, SupportedLocale } from '../types';`. NO runtime imports of any other module.

    Define module-level constants:
    - `BASE_INSTRUCTION` — single literal string: "You are creating a spoken learning podcast recap. Write ONLY the words to be spoken — no stage directions, no music cues, no markdown formatting."
    - `SECTION_INSTRUCTION` — multi-line literal containing the five section names verbatim and in upper-case: RECAP, CONNECTIONS, MISCONCEPTION CHECK, RETRIEVAL QUESTIONS, NEXT ACTION. Phrase as guidance ("Structure the podcast with these sections (use natural transitions, don't announce section names): 1. RECAP: ... 2. CONNECTIONS: ... 3. MISCONCEPTION CHECK: ... 4. RETRIEVAL QUESTIONS: ... 5. NEXT ACTION: ...").
    - `COVERAGE_CONSTRAINT` — literal containing the byte-stable substring "IMPORTANT: You MUST mention every concept listed below. Do not skip any." (note: substring `MUST mention every concept` is the test-anchored invariant).
    - `LENGTH_MAP: Record<PodcastLength, string>` with four entries — D-01 word counts: brief ~150 words / ~60s; standard ~225 words / ~90s; deep ~450 words / ~3 minutes; extended ~750 words / ~5 minutes. Each value must contain the literal substring "150 words" / "225 words" / "450 words" / "750 words" respectively so the test regexes match.
    - `STYLE_MAP: Record<PodcastStyle, string>` with three entries from D-02 and 52-PATTERNS.md.

    Exported functions:
    - `export function buildPodcastPrompt(conceptLines: string, options: PodcastOptions): { system: string; user: string }` — assembles `[BASE_INSTRUCTION, SECTION_INSTRUCTION, LENGTH_MAP[options.length], STYLE_MAP[options.style], COVERAGE_CONSTRAINT].join('\n\n')` for system, and `\`Concepts to cover:\n${conceptLines}\`` for user.
    - `export function computeOptionsHash(conceptIds: string[], locale: SupportedLocale, options: PodcastOptions): string` — returns `JSON.stringify({ conceptIds: [...conceptIds].sort(), locale, length: options.length, style: options.style })`. Not a security primitive — local cache invalidation key only. Per Claude's Discretion in 52-CONTEXT.md.

    Do NOT export any other names. Do NOT import from `../locales`, `../lib/date`, or `react-i18next`. Do NOT use side-effecting top-level code.

    Create `app/tests/services/podcast-prompt.test.mjs`:
    - Use the imports and source-reading scaffold from `app/tests/services/reorg-prompt-journal-injection.test.mjs:1-32`. Read `app/src/services/podcast-prompt.ts` as text and assert it does NOT match /from ['"]\.\.\/locales/, /from ['"]\.\.\/lib\/date/, /from ['"]react-i18next/.
    - Then `await import('../../src/services/podcast-prompt.ts')` and exercise `buildPodcastPrompt` with concept lines `'- Concept Alpha: short summary\n- Concept Beta: short summary\n- Concept Gamma: short summary'`.
    - Loop over `for (const length of ['brief','standard','deep','extended']) for (const style of ['focused','conversational','review'])` asserting each system contains /RECAP/, /CONNECTIONS/, /MISCONCEPTION/, /RETRIEVAL QUESTIONS/, /NEXT ACTION/, /MUST mention every concept/i. 12 it() blocks.
    - Add four word-count-specific tests: brief → /150 words/, standard → /225 words/, deep → /450 words/, extended → /750 words/.
    - Add coverage substring test on the `user` half: every concept name from the input lines appears in the returned `user` string.

    Create `app/tests/services/podcast-options.test.mjs`:
    - Imports + source-read scaffold from `app/tests/services/classification-dedup.test.mjs:1-32`.
    - Read `app/src/services/podcast-prompt.ts` text. Read `app/src/services/podcast.service.ts` text.
    - Dynamically import `computeOptionsHash` from `../../src/services/podcast-prompt.ts`. Three positive determinism tests (sorted conceptIds, length change differs, style change differs, locale change differs, conceptIds change differs).
    - Source-read invariants on `podcast.service.ts` (these will be red in Wave 0, green after 52-02):
      - assert.match(podcastServiceSrc, /generatePodcast\(\s*date:\s*string,\s*conceptIds\?:\s*string\[\],\s*options\?:\s*PodcastOptions/);
      - assert.match(podcastServiceSrc, /existing\.optionsHash\s*===\s*computeOptionsHash\(/);
      - assert.match(podcastServiceSrc, /import\s*\{[^}]*buildPodcastPrompt[^}]*computeOptionsHash[^}]*\}\s*from\s*['"]\.\/podcast-prompt['"]/);
      - assert.match(podcastServiceSrc, /['"]standard['"]/);  // read-site default
      - assert.match(podcastServiceSrc, /['"]conversational['"]/);

    Mark each cross-plan invariant with a comment `// RED until Plan 52-02` so the executor running Wave 0 knows the expected outcome: prompt tests pass, options tests pass on hash determinism but fail on the four cross-plan source-reads.

    Do NOT use describe.skip / it.skip. Failing tests at end of Wave 0 are the explicit Nyquist signal that Wave 1 has work left.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/podcast-prompt.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/services/podcast-prompt.test.mjs` exits 0 with at least 16 passing tests (12 matrix + 4 word-count) AND zero failures.
    - `cd app && node --test tests/services/podcast-options.test.mjs` exits non-zero with hash-determinism tests passing AND the four cross-plan source-read assertions failing (expected RED until Plan 52-02). Failures must mention `generatePodcast` signature or `optionsHash`.
    - `grep -c "from '../locales/index'" app/src/services/podcast-prompt.ts` returns 0.
    - `grep -c "from '../lib/date'" app/src/services/podcast-prompt.ts` returns 0.
    - `grep -c "from 'react-i18next'" app/src/services/podcast-prompt.ts` returns 0.
    - `grep -cE "^export function buildPodcastPrompt" app/src/services/podcast-prompt.ts` returns 1.
    - `grep -cE "^export function computeOptionsHash" app/src/services/podcast-prompt.ts` returns 1.
    - `grep -E "MUST mention every concept" app/src/services/podcast-prompt.ts` returns the COVERAGE_CONSTRAINT literal.
    - `grep -oE "'brief'|'standard'|'deep'|'extended'" app/src/services/podcast-prompt.ts | sort -u | wc -l` returns 4 (all 4 length keys present in LENGTH_MAP).
    - `cd app && npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <done>Leaf prompt module + its tests are green; options test is partially red on the cross-plan invariants (intentional Nyquist signal for Wave 1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create PodcastScreen.options.test.mjs source-reading invariants + 4-locale podcast.options namespace check</name>
  <files>app/tests/screens/PodcastScreen.options.test.mjs</files>
  <read_first>
    - app/tests/screens/ReviewScreen.anchor-empty-state.test.mjs (entire file ~85 lines — the source-reading + 4-locale bundle parity analog)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-PATTERNS.md (section "app/tests/screens/PodcastScreen.options.test.mjs")
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-UI-SPEC.md (Copywriting Contract — length/style labels)
    - .planning/phases/52-podcast-quality-defaults-and-learner-controls/52-CONTEXT.md (D-04, D-06, D-08, D-11)
    - app/src/screens/PodcastScreen.tsx (lines 1-50, 500-640 — current generate call site + audioRef pattern)
    - app/src/locales/en.json (line 618+ — existing podcast.* namespace shape)
  </read_first>
  <behavior>
    The test file holds 6+ source-reading invariants that PodcastScreen.tsx must satisfy after Plan 52-03 lands. All assertions are RED at the end of Wave 0 except locale-parity which is partially red (en.json still lacks `podcast.options.*` until 52-03 lands the bundle). The test EXISTS so 52-03 has a green target.

    Invariants:
    1. `app/src/screens/PodcastScreen.tsx` source contains `podcast.options.lengthLabel` and `podcast.options.styleLabel` (t() keys for chip group headings).
    2. Source contains `settings.podcast.defaultLength ?? 'standard'` and `settings.podcast.defaultStyle ?? 'conversational'` (D-11 chip initialization with D-14 fallback).
    3. Source contains a `generatePodcast(...)` call passing an object literal with both `length` and `style` properties.
    4. Source contains the i18n key `podcast.options.regenerateWithNew` (D-04 explicit button).
    5. Source contains the i18n key `podcast.player.optionsBadge` (D-06 inline diff badge).
    6. Source contains `audioRef.current.playbackRate =` (D-08 HTML5 playback-rate wiring).

    Locale parity (one loop over en/zh/es/ja):
    - Each bundle's `podcast.options` object exists.
    - Required keys present and non-empty strings: lengthLabel, brief, standard, deep, extended, styleLabel, focused, conversational, review, regenerateWithNew.
    - Each bundle's `settings.fields.podcastDefaultLength`, `settings.fields.podcastDefaultStyle`, `settings.fields.ttsModel`, `settings.fields.ttsModelStandard`, `settings.fields.ttsModelHd` are non-empty strings (D-07 TTS model row).
  </behavior>
  <action>
    Create `app/tests/screens/PodcastScreen.options.test.mjs` by copying the imports and `LOCALE_PATHS` scaffold from `app/tests/screens/ReviewScreen.anchor-empty-state.test.mjs:15-29`. Swap the screen path to `app/src/screens/PodcastScreen.tsx`.

    Write a `describe('PodcastScreen length+style chips (Phase 52 PODCAST-02)', ...)` block with six `it()` cases, each asserting one of the six source-read regexes listed in <behavior>. Use literal matchers exactly as 52-PATTERNS.md "app/tests/screens/PodcastScreen.options.test.mjs" specifies:
    - `assert.match(source, /podcast\.options\.lengthLabel/);`
    - `assert.match(source, /podcast\.options\.styleLabel/);`
    - `assert.match(source, /settings\.podcast\.defaultLength\s*\?\?\s*['"]standard['"]/);`
    - `assert.match(source, /settings\.podcast\.defaultStyle\s*\?\?\s*['"]conversational['"]/);`
    - `assert.match(source, /generatePodcast\([^)]*\{\s*length[^}]*style[^}]*\}\)/);`
    - `assert.match(source, /podcast\.options\.regenerateWithNew/);`
    - `assert.match(source, /podcast\.player\.optionsBadge/);`
    - `assert.match(source, /audioRef\.current\.playbackRate\s*=/);`

    Write a `describe('Locale bundles carry podcast.options.* keys (Phase 52 i18n)', ...)` block looping over `LOCALE_PATHS`. For each, JSON.parse the bundle and assert:
    - `bundle.podcast.options` is a truthy object.
    - For every required key in `['lengthLabel','brief','standard','deep','extended','styleLabel','focused','conversational','review','regenerateWithNew']`: `typeof bundle.podcast.options[k] === 'string'` and `bundle.podcast.options[k].trim().length > 0`.
    - `bundle.settings.fields.podcastDefaultLength`, `.podcastDefaultStyle`, `.ttsModel`, `.ttsModelStandard`, `.ttsModelHd` are non-empty strings.
    - `bundle.podcast.player.optionsBadge` exists and is a non-empty string containing `{{length}}` AND `{{style}}` placeholders (D-06 badge format).

    These tests intentionally fail in Wave 0 — Plan 52-03 wires the UI strings into PodcastScreen.tsx AND adds the i18n bundles in all four locales. Add a comment `// RED until Plan 52-03` at the top of the file.

    Do NOT use describe.skip / it.skip. Do NOT mock React rendering — these are static source-read invariants.
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/PodcastScreen.options.test.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && node --test tests/screens/PodcastScreen.options.test.mjs` exits non-zero with at least the eight `PodcastScreen.tsx` source-read assertions failing (intentional RED until Plan 52-03). At least one failure message mentions `podcast.options.lengthLabel` or `podcast.options.regenerateWithNew` or `audioRef.current.playbackRate`.
    - `grep -cE "describe\('PodcastScreen length\+style chips" app/tests/screens/PodcastScreen.options.test.mjs` returns 1.
    - `grep -cE "describe\('Locale bundles carry podcast\.options" app/tests/screens/PodcastScreen.options.test.mjs` returns 1.
    - `grep -cE "for \(const \{ code, path \} of LOCALE_PATHS\)" app/tests/screens/PodcastScreen.options.test.mjs` returns 1.
    - `grep -c "describe.skip\|it.skip" app/tests/screens/PodcastScreen.options.test.mjs` returns 0 (no skipped tests — failing is the intended Nyquist signal).
  </acceptance_criteria>
  <done>Screen test file in place with intentional RED failures targeting 52-03. Locale-parity loop ready to flip green when 52-03 lands all 4 bundles.</done>
</task>

</tasks>

<verification>
After Plan 52-01 lands:
- `cd app && npx tsc -b --noEmit` exits 0 (types compile clean).
- `cd app && node --test tests/services/podcast-prompt.test.mjs` PASSES with all 16+ tests green.
- `cd app && node --test tests/services/podcast-options.test.mjs` PARTIALLY RED — hash-determinism cases pass, four cross-plan source-read invariants fail with messages mentioning `generatePodcast` signature, `optionsHash`, or `import { ... buildPodcastPrompt`. This is the explicit Wave-1 work signal.
- `cd app && node --test tests/screens/PodcastScreen.options.test.mjs` RED on PodcastScreen.tsx source-reads + missing `podcast.options.*` keys in all four bundles. Wave-2 target.
- `cd app && node --test tests/locales/bundle-parity.test.mjs` continues to PASS (no new keys yet, so bundles still parallel).
</verification>

<success_criteria>
- PodcastLength has exactly four members (brief / standard / deep / extended) — D-01 honored against the researcher's 3-option suggestion.
- PodcastStyle has exactly three members (focused / conversational / review) — D-02.
- PodcastOptions is exported and used by podcast-prompt.ts.
- Additive optional fields added to DailyPodcast / PodcastSettings / TTSConfig per D-07, D-11, D-14 with NO migration helper.
- podcast-prompt.ts is a leaf module: no JSON / lib/date / react-i18next imports.
- buildPodcastPrompt covers all 12 (length × style) combinations with five-section + coverage prompt.
- computeOptionsHash deterministic across conceptId order and sensitive to all four inputs (conceptIds, locale, length, style).
- Three Wave-0 test files exist; podcast-prompt.test.mjs is green, podcast-options.test.mjs + PodcastScreen.options.test.mjs are partially RED on cross-plan invariants — the explicit Wave 1 + Wave 2 work signal.
</success_criteria>

<output>
Create `.planning/phases/52-podcast-quality-defaults-and-learner-controls/52-01-SUMMARY.md` capturing:
- New exports: PodcastLength, PodcastStyle, PodcastOptions
- Additive optional fields landed on DailyPodcast / PodcastSettings / TTSConfig
- podcast-prompt.ts public surface (buildPodcastPrompt, computeOptionsHash) + LENGTH_MAP key set
- COVERAGE_CONSTRAINT byte-stable literal
- Test status: prompt tests green, options + screen tests partially RED (Wave 1 + Wave 2 targets)
</output>