---
phase: 50-retrieval-and-library-foundation
plan: 05
type: execute
wave: 2
depends_on: [50-03]
files_modified:
  - app/src/services/engagement.service.ts
  - app/tests/services/engagement.service.pinned-ids.test.mjs
  - app/tests/services/post-history.purge-collections.test.mjs
autonomous: true
requirements: [RETRIEVE-02]
threat_refs: [T-50-PURGE-REGRESSION]
must_haves:
  truths:
    - "engagementService.getPinnedIds() returns the union of saved ∪ liked ∪ collectionService.getAllMemberPostIds() (D-09)"
    - "A post added ONLY to a custom collection (not Saved, not Liked) survives postHistoryService.purgeExpired() even when its generatedAt is older than retentionDays cutoff"
    - "The import direction stays one-way: engagementService → collectionService → postHistoryService (no circular dep)"
    - "purgeExpired() call site in post-history.service.ts is UNCHANGED — the pin-union extension is internal to engagementService"
  artifacts:
    - path: app/src/services/engagement.service.ts
      provides: "Extended getPinnedIds() with collection-member union (D-09)"
      contains: "collectionService.getAllMemberPostIds()"
    - path: app/tests/services/engagement.service.pinned-ids.test.mjs
      provides: "Behavioral test: union semantics correct; orphan tolerance preserved"
    - path: app/tests/services/post-history.purge-collections.test.mjs
      provides: "End-to-end: collection membership pins post against 7-day purge"
  key_links:
    - from: app/src/services/engagement.service.ts
      to: app/src/services/collection.service.ts
      via: "import { collectionService } + getPinnedIds union"
      pattern: "collectionService\\.getAllMemberPostIds"
    - from: app/src/services/post-history.service.ts
      to: app/src/services/engagement.service.ts
      via: "engagementService.getPinnedIds() consumed by purgeExpired() (UNCHANGED call site)"
      pattern: "engagementService\\.getPinnedIds"
---

<objective>
Extend `engagementService.getPinnedIds()` to union collection-member IDs (D-09) so the existing `postHistoryService.purgeExpired()` automatically pins collection-member posts against the 7-day rolling purge with no purge-side change. Turn 50-02's `engagement.service.pinned-ids.test.mjs` and `post-history.purge-collections.test.mjs` from RED to GREEN.

Purpose: D-09 — "Collection membership pins a post against the 7-day rolling history purge." If a user adds a post to "For thesis" but does NOT save it globally, the post still survives purge. Honors operator intent: "they kept it for a reason." Implementation is a single import + one line in `getPinnedIds()` — the leverage is high.

Output: One service modification + two green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-retrieval-and-library-foundation/50-CONTEXT.md
@.planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md
@.planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md
@.planning/phases/50-retrieval-and-library-foundation/50-03-SUMMARY.md
@app/src/services/engagement.service.ts
@app/src/services/post-history.service.ts

<interfaces>
<!-- Current engagement.service.ts getPinnedIds (lines 194-197) — extend this method only -->

getPinnedIds(): Set<string> {
  const s = loadState();
  return new Set<string>([...s.saved, ...s.liked]);
}

<!-- D-09 target — union with collectionService.getAllMemberPostIds() (returns Set<string>): -->

getPinnedIds(): Set<string> {
  const s = loadState();
  const collectionMembers = collectionService.getAllMemberPostIds();
  return new Set<string>([...s.saved, ...s.liked, ...collectionMembers]);
}

<!-- post-history.service.ts purgeExpired (lines 60-71) — UNCHANGED. The pin set expansion is internal to engagementService. -->

purgeExpired(): void {
  const settings = settingsService.getSync();
  const retentionDays = settings.feed?.postRetentionDays;
  if (retentionDays == null || retentionDays <= 0) return;
  const cutoff = Date.now() - retentionDays * 24 * 60 * 60 * 1000;
  const pinned = engagementService.getPinnedIds();
  const posts = loadPosts().filter(p => pinned.has(p.id) || p.generatedAt > cutoff);
  savePosts(posts);
}

<!-- Circular dep guard (RESEARCH Pitfall 2): engagementService → collectionService is a NEW one-way edge. collectionService MUST NOT import engagementService (enforced in plan 50-03). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend engagementService.getPinnedIds() with collection-member union (D-09)</name>
  <files>app/src/services/engagement.service.ts, app/tests/services/engagement.service.pinned-ids.test.mjs, app/tests/services/post-history.purge-collections.test.mjs</files>
  <read_first>
    - app/src/services/engagement.service.ts (full file — verify getPinnedIds at lines 194-197; current import block at lines 26-28)
    - app/src/services/post-history.service.ts (full file — verify purgeExpired uses engagementService.getPinnedIds and that call site does NOT need modification)
    - app/src/services/collection.service.ts (from plan 50-03 — verify getAllMemberPostIds returns Set<string>)
    - .planning/phases/50-retrieval-and-library-foundation/50-CONTEXT.md (D-09 — operator intent quote)
    - .planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md §"Pattern 2" + §"Pitfall 2" (circular dep guard)
    - .planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md §"engagement.service.ts getPinnedIds() extension"
    - app/tests/services/engagement.service.pinned-ids.test.mjs (Wave 0 scaffold from 50-02)
    - app/tests/services/post-history.purge-collections.test.mjs (Wave 0 scaffold from 50-02)
    - app/tests/services/post-history.test.mjs (full file — existing purgeExpired test pattern with settings shim — copy the setRetentionDays pattern at lines 19-24)
  </read_first>
  <behavior>
    - getPinnedIds() with empty engagement + 1 collection containing postId 'p1' returns Set(['p1'])
    - getPinnedIds() with saved=['p1'], liked=['p2'], collection containing 'p3' returns Set(['p1', 'p2', 'p3'])
    - getPinnedIds() with saved=['p1'] AND a collection also containing 'p1' returns Set(['p1']) (Set de-duplicates)
    - getPinnedIds() with no collections returns the prior saved ∪ liked union (zero regression for users with no collections)
    - postHistoryService.purgeExpired() with retentionDays=7 + a post generatedAt=Date.now() - 8*86400000 + that post added to a collection → post survives purge
    - Same setup but post NOT in any collection AND NOT saved/liked → post is purged (negative control)
    - collectionService import statement appears exactly once at the top of engagement.service.ts; no engagementService import appears in collection.service.ts (circular dep guard)
  </behavior>
  <action>
1. Modify `app/src/services/engagement.service.ts`:
   a. Add import: `import { collectionService } from './collection.service.ts';` placed AFTER the existing `postHistoryService` import at line 28 (matches existing import-block ordering).
   b. Update `getPinnedIds()` (currently lines 194-197) to include `collectionService.getAllMemberPostIds()` in the union. Implementation per `<interfaces>` D-09 target block above.
   c. Update the inline JSDoc comment above `getPinnedIds()` (lines 186-193) to reference D-09 and note that the union now includes collection members. Keep mention of the existing `postHistoryService.purgeExpired()` consumer.
   d. Do NOT modify ANY other method in this file.

2. Replace the Wave 0 scaffold body in `app/tests/services/engagement.service.pinned-ids.test.mjs` with concrete assertions for the behaviors above. Use the localStorage shim from `engagement.service.test.mjs` at lines 13-19. Dynamic-import BOTH `engagementService` and `collectionService`. Each test seeds localStorage state (saved/liked + create collection + addPost), then asserts on `getPinnedIds()` return value.

3. Replace the Wave 0 scaffold body in `app/tests/services/post-history.purge-collections.test.mjs` with concrete assertions:
   - Use the settings shim pattern from `app/tests/services/post-history.test.mjs` (lines 17-24 — `setRetentionDays(7)` helper).
   - Use the `makePost(id, overrides)` helper at lines 27-46 with `generatedAt: Date.now() - 8*86400000` (8 days old, beyond cutoff).
   - Test 1 (positive): addPost(p1, oldDate); createCollection('c1') + addPost('c1', 'p1'); purgeExpired(); getPosts() still contains p1.
   - Test 2 (negative control): addPost(p2, oldDate); no save, no like, no collection; purgeExpired(); getPosts() does NOT contain p2.
   - Test 3: zero-collection backward compat — getPinnedIds() with NO collections returns the same union as before (saved ∪ liked).

Do NOT modify `app/src/services/post-history.service.ts`. The call site at `engagementService.getPinnedIds()` (line 68) is UNCHANGED — the behavior shift is entirely internal to engagementService.

Do NOT add a `collectionService.getAllMemberPostIds()` import anywhere outside of engagement.service.ts in this plan.
  </action>
  <verify>
    <automated>cd app && node --test tests/services/engagement.service.pinned-ids.test.mjs tests/services/post-history.purge-collections.test.mjs tests/services/engagement.service.test.mjs tests/services/post-history.test.mjs 2>&1 | tail -25</automated>
  </verify>
  <done>
    All four test files pass (two new + two existing — zero regression on engagement / post-history). `grep -c "collectionService.getAllMemberPostIds" src/services/engagement.service.ts` returns 1. `grep -c "import.*engagement.service" src/services/collection.service.ts` still returns 0 (circular dep guard preserved). `grep -c "engagementService.getPinnedIds" src/services/post-history.service.ts` returns 1 (call site unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collectionService → engagementService | One-way data flow: engagementService READS from collectionService. collectionService never reads from engagementService (circular dep prevention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-PURGE-REGRESSION | Spoofing / Data Loss | postHistoryService.purgeExpired() pin set | mitigate | Negative-control test (Test 2 above) asserts NON-pinned posts STILL get purged — guards against an accidental "pin everything" regression. Positive-control test (Test 1) asserts D-09 works. Existing engagementService + post-history test suites also re-run to catch any regression in saved/liked union semantics. |
| T-50-CIRCULAR-DEP | Availability | Module load order | mitigate | Source assertion: `grep -c "import.*engagement.service" src/services/collection.service.ts` returns 0. Documented in 50-03 plan; verified again here. |
</threat_model>

<verification>
- `cd app && node --test tests/services/engagement.service.pinned-ids.test.mjs` passes.
- `cd app && node --test tests/services/post-history.purge-collections.test.mjs` passes.
- `cd app && node --test tests/services/engagement.service.test.mjs` passes (no regression).
- `cd app && node --test tests/services/post-history.test.mjs` passes (no regression).
- `cd app && grep -c "collectionService" src/services/engagement.service.ts` returns 2 (import + use site).
- `cd app && grep -c "engagement.service" src/services/collection.service.ts` returns 0 (no circular dep).
</verification>

<success_criteria>
- getPinnedIds() returns saved ∪ liked ∪ collection-members.
- purgeExpired() honors the expanded pin set (no purge-side change required).
- Collection membership demonstrably pins a 8-day-old post against a 7-day retention cutoff.
- No circular dependency introduced.
- All four affected tests pass.
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-05-SUMMARY.md` when done. Note the exact single-line addition to getPinnedIds and the single import added — Wave 3 consumers (CollectionDrillInScreen + SettingsDataScreen Clear-All) reference this contract.
</output>
