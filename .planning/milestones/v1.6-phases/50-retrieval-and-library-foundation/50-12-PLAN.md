---
phase: 50-retrieval-and-library-foundation
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/screens/SavedScreen.tsx
  - app/tests/screens/SavedScreen.tab-preserves-query.test.mjs
  - app/tests/screens/SavedScreen.chip-blur-race.test.mjs
autonomous: true
gap_closure: true
gaps_addressed: [G2, G6, G7]
requirements: [RETRIEVE-01]

must_haves:
  truths:
    - "On /saved with an empty search bar, tapping a filter chip (Concept / Source / Date) opens its FilterPickerSheet — the chip-row does not collapse before the tap handler fires"
    - "On /saved, switching tabs (Saved ↔ Liked ↔ History ↔ Collections) preserves the search-bar query AND rescopes results against the new tab's corpus — query is NOT cleared"
    - "Filter chips (Concept · Source · Date) render with uniform horizontal padding so pill widths feel rhythmic regardless of label length"
    - "Tab-change still flushes the pending debounce timer and clears filter chips (NO regression of Pitfall 8's debounce-flush)"
  artifacts:
    - path: "app/src/screens/SavedScreen.tsx"
      provides: "Chip-row tap survives empty-query blur via pointer-down preventDefault; tab-change effect preserves query; FilterChip uses fixed 6px 12px padding"
      contains: "onPointerDown"
  key_links:
    - from: "SavedScreen.tsx FilterChip"
      to: "FilterPickerSheet open state"
      via: "onPointerDown preventDefault keeps focus → showFilterChips latch open → onClick fires"
      pattern: "onPointerDown"
    - from: "SavedScreen.tsx tab-change useEffect"
      to: "query state"
      via: "preserved on tab change (no setQuery'')"
      pattern: "useEffect.*activeTab.*clearTimeout"
---

<objective>
Close gaps G2, G6, and G7 from 50-UAT.md Test 5 + Test 6. All three live in `app/src/screens/SavedScreen.tsx` and are localized edits to the same file — combining them avoids three duplicate read-modify-test cycles on the same source.

**G2 (blocker)** — Chip blur-race: when the search bar is empty, tapping a filter chip is interpreted as a blur on the input. The `showFilterChips` latch (`searchFocused || inputDraft.length > 0 || anyFilterActive`) collapses (all three falsey on empty-query path) → the chip-row unmounts BEFORE the chip's tap handler fires → the FilterPickerSheet never opens. Chips are non-interactive on the empty-query path.

**G6 (major)** — Tab switch on /saved clears the search bar query. Contradicts 50-VALIDATION.md row 4 and D-11. The 50-09 plan mis-cited Pitfall 8 to motivate the query-clear; Pitfall 8 is actually about flushing the pending debounce TIMER, not the query state itself.

**G7 (cosmetic)** — FilterChip horizontal padding looks uneven across Concept / Source / Date at different label widths.

Purpose: Make filter chips usable on the empty-query path, restore D-11 query-survives-tab-change contract, and give the chip row uniform visual rhythm.
Output: Updated `SavedScreen.tsx` + two new regression test files codifying the blur-race fix and the tab-preserves-query contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-retrieval-and-library-foundation/50-UAT.md
@.planning/phases/50-retrieval-and-library-foundation/50-VALIDATION.md
@.planning/phases/50-retrieval-and-library-foundation/50-CONTEXT.md
@.planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md
@.planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md

@app/src/screens/SavedScreen.tsx

Current shape of FilterChip + tab-change effect + showFilterChips latch is documented inline in SavedScreen.tsx lines 360-408 (FilterChip), 498-505 (tab-change effect), 581 (latch expression), 783-820 (chip-row usage). The chip is a Caption 12/500 pill at 32px height per 50-UI-SPEC.md Typography + Spacing tables. The validation contract row 4 ("Tab switch preserves query, rescopes results") is the source of truth for G6.

Pitfall 8 in 50-RESEARCH.md reads: "Reset search state ... clear `query` and `filterConcept/Source/Date` on tab switch". The UAT analysis re-interprets this: the intent is "flush the pending debounce timer so a stale setTimeout from the previous tab does not bleed results", NOT "clear the user-typed query string". 50-VALIDATION.md row 4 overrides the Pitfall 8 wording for query state.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix chip blur-race (G2), preserve query on tab change (G6), uniform chip padding (G7)</name>
  <files>app/src/screens/SavedScreen.tsx</files>
  <read_first>
    - app/src/screens/SavedScreen.tsx (current implementation — lines 360-408 FilterChip inline component; lines 437-505 state hooks + tab-change effect; lines 577-581 showFilterChips latch; lines 783-820 FilterChip usage block)
    - .planning/phases/50-retrieval-and-library-foundation/50-UAT.md (Test 5 + Test 6 reported behavior; analysis blocks specify fix shapes)
    - .planning/phases/50-retrieval-and-library-foundation/50-VALIDATION.md (row 4 — tab-preserves-query contract)
    - .planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md (Pitfall 8 — original wording; UAT clarifies it means flush-debounce, NOT clear-query)
    - .planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md (Spacing Scale + Typography — chip is Caption 12/500 at 32px height; 6px is on the spacing scale via xs/sm halves)
    - CLAUDE.md "ChatInput flex shrink" + "SwipeTabContainer resize + keyboard" — focus/blur invariants relevant context
    - /Users/huanfuli/.claude/projects/-Users-Code-EchoLearn/memory/feedback_no_refresh_assumption.md (durable rule context — SavedScreen subscribes to ENGAGEMENT_CHANGED + COLLECTIONS_CHANGED events and renders fresh state without remount; any chip-row latch edit must preserve those subscriptions so the no-refresh rule continues to hold for sibling features)
  </read_first>
  <behavior>
    - G2-A (chip-row survives pointer-down): When the user touches a filter chip while the search bar is empty, the chip-row must NOT collapse before the chip's tap handler fires. The chip's `onPointerDown` AND `onMouseDown` handlers call `e.preventDefault()`, which suppresses the input's blur. `searchFocused` stays true → the `showFilterChips` latch stays open → the chip's `onClick` fires → FilterPickerSheet opens.
    - G2-B (no regression on populated-query path): When the search bar has text, chip taps continue to work. The preventDefault is harmless because pointer-down on the input was never the trigger.
    - G6-A (query preserved): The `useEffect([activeTab])` block keeps `clearTimeout(debounceRef.current)` AND `setFilterConcept(null) / setFilterSource(null) / setFilterDate('all')`, but DROPS `setInputDraft('')` and `setQuery('')`. The Fuse index already rebuilds on `[activeTab, savedPosts, likedPosts, historyGroups]` change so rescoping is automatic once the query persists.
    - G6-B (debounce-timer flush preserved): `clearTimeout(debounceRef.current)` still runs — this is the actual Pitfall 8 mitigation. Add an inline comment distinguishing the two: timer flush is Pitfall 8; clearing the query was the 50-09 misread that G6 reverses.
    - G7 (uniform padding): FilterChip inline style changes from `padding: '0 12px'` to `padding: '6px 12px'`. Chip height stays 32px. The 6px vertical padding gives deterministic top/bottom space independent of glyph metrics.
    - Sibling subscriptions preserved (durable-rule context): SavedScreen's `ENGAGEMENT_CHANGED` and `COLLECTIONS_CHANGED` `eventBus.subscribe(...)` useEffects (lines ~475-493) must remain intact. None of the three edits in this task should touch those effects; if a refactor produces churn near them, double-check the unsub cleanup return is still present.
  </behavior>
  <action>
    Edit `app/src/screens/SavedScreen.tsx` in three localized places.

    EDIT 1 (G2 — chip pointer-down preventDefault). Inside `function FilterChip({ label, active, onTap, onClear }: FilterChipProps)` (currently around lines 360-408), add two new handlers to the outer `<button>`. The handlers must be placed BEFORE the existing `onClick={onTap}` prop so they fire first in the event sequence:
    - `onPointerDown={(e) => { e.preventDefault(); }}`
    - `onMouseDown={(e) => { e.preventDefault(); }}`
    Both are needed because Capacitor's pointer event model on Android WebView dispatches both pointerdown and mousedown on touch; either one can trigger the input blur. Add inline comment immediately above: `// G2 fix (50-12): preventDefault on pointer/mouse down suppresses the search input's blur so the chip-row latch (showFilterChips) does not collapse before onClick fires. Required for the empty-query path where searchFocused / inputDraft / anyFilterActive are all falsey and the latch hinges entirely on input focus.`

    EDIT 2 (G6 — tab-change effect drops query-clear). Inside the existing `useEffect(() => { ... }, [activeTab])` block (currently around lines 498-505), remove the lines `setInputDraft('');` and `setQuery('');`. KEEP `clearTimeout(debounceRef.current)`, `setFilterConcept(null)`, `setFilterSource(null)`, and `setFilterDate('all')`. Update the comment immediately above the effect from "Tab-change reset (RESEARCH §Pitfall 8): clear query + all 3 filter chips when active tab changes. Tab corpora are independent so leftover query/filters from a previous tab would feel broken." to: "Tab-change reset (50-09 + 50-12 gap G6 fix). Pitfall 8 means flush the pending debounce TIMER so a stale setTimeout from the previous tab does not bleed results into the new tab. It does NOT mean clear the user-typed query — per 50-VALIDATION.md row 4 and D-11, query persists across tab change and Fuse re-indexes against the new tab's corpus automatically (the index useMemo is keyed on [activeTab, ...])."

    EDIT 3 (G7 — uniform chip padding). Inside the same FilterChip inline component as EDIT 1, change the inline style property `padding: '0 12px'` to `padding: '6px 12px'`. Add inline comment immediately above the style property: `// G7 fix (50-12): fixed 6px/12px padding for rhythm. Concept/Source/Date label widths vary; without explicit vertical pad the perceived chip height drifted by glyph descender.`

    Do NOT touch: the `showFilterChips` latch expression at line ~581 (the latch remains as a defense-in-depth; EDIT 1 fixes the bug at the pointer-event source), `clearAllFilters` callback (which intentionally clears query — invoked from the no-match "Clear filters" text-button, NOT from tab change), debounceRef logic, FilterPickerSheet hosting block, or the `onClear` X-button handler shape inside FilterChip. Also do NOT touch the `ENGAGEMENT_CHANGED` and `COLLECTIONS_CHANGED` subscription useEffects (lines ~475-493) — those uphold the durable no-refresh rule for sibling features (collection list, saved-corpus pin updates).

    Comment header note in the file's existing "Phase 50 search wiring" block (currently around line 35): add a bullet at the end of the list: `- 50-12 gap closure: G2 chip-blur-race fix (onPointerDown preventDefault), G6 tab-change preserves query (only filters + debounce timer reset; query persists per 50-VALIDATION.md row 4), G7 fixed-value chip padding for visual rhythm.`
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/screens/SavedScreen.tab-preserves-query.test.mjs tests/screens/SavedScreen.chip-blur-race.test.mjs tests/screens/SavedScreen.search-scope.test.mjs tests/screens/SavedScreen.collections-tab.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File contains `onPointerDown` AND `onMouseDown` handlers inside the FilterChip inline component. Verify: both `grep -c "onPointerDown" app/src/screens/SavedScreen.tsx` and `grep -c "onMouseDown" app/src/screens/SavedScreen.tsx` return at least 1 (note: SavedRow already has pointer/mouse handlers, so the count overall may be ≥2 — confirm by line-grepping within the FilterChip function body).
    - File contains the literal `e.preventDefault();` inside a pointer-down or mouse-down arrow body. Verify: `grep -nE "onPointerDown=\\{\\(e\\)\\s*=>\\s*\\{\\s*e\\.preventDefault" app/src/screens/SavedScreen.tsx` returns at least 1 match.
    - Tab-change effect no longer contains `setInputDraft('')` or `setQuery('')`. Verify after running `awk '/useEffect.*=>.*{/,/}, \[activeTab\]/' app/src/screens/SavedScreen.tsx` (or equivalent inspection): the matched block contains `clearTimeout(debounceRef.current)` AND `setFilterConcept(null)` AND `setFilterDate('all')` but does NOT contain `setInputDraft('')` or `setQuery('')`. The clearAllFilters callback ELSEWHERE still contains those setters — that callback is preserved unchanged. Sanity command: `grep -nE "setInputDraft\\(''\\)" app/src/screens/SavedScreen.tsx` should return at least 2 hits (Clear-X button + clearAllFilters callback) but zero hits inside the `useEffect(..., [activeTab])` block lines.
    - FilterChip inline style uses `padding: '6px 12px'`. Verify: `grep -nE "padding:\\s*['\"]6px 12px['\"]" app/src/screens/SavedScreen.tsx` returns at least 1 match.
    - FilterChip no longer uses `padding: '0 12px'`. Verify: `grep -nE "padding:\\s*['\"]0 12px['\"]" app/src/screens/SavedScreen.tsx` returns 0 matches (other padding shorthands like '0 16px' for search-bar wrapper or different paddings are fine; only the chip's '0 12px' is targeted).
    - File contains the literal token "50-12" so the gap-closure provenance is searchable in source.
    - Sibling subscriptions still intact: `grep -cE "eventBus\\.subscribe\\(['\"]ENGAGEMENT_CHANGED" app/src/screens/SavedScreen.tsx` returns ≥ 1 AND `grep -cE "eventBus\\.subscribe\\(['\"]COLLECTIONS_CHANGED" app/src/screens/SavedScreen.tsx` returns ≥ 1 (durable-rule regression guard).
    - All existing tests in `tests/screens/SavedScreen.search-scope.test.mjs` and `tests/screens/SavedScreen.collections-tab.test.mjs` continue to pass.
  </acceptance_criteria>
  <done>
    Tapping a filter chip on an empty-query /saved opens the FilterPickerSheet (G2 closed). Switching tabs preserves the search-bar query while clearing filters and debounce timer (G6 closed). FilterChip padding is uniform at 6px / 12px (G7 closed).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tab-preserves-query regression test</name>
  <files>app/tests/screens/SavedScreen.tab-preserves-query.test.mjs</files>
  <read_first>
    - app/tests/screens/SavedScreen.search-scope.test.mjs (existing source-reading test for SavedScreen — mirror file naming, imports, helper structure)
    - app/tests/screens/SavedScreen.collections-tab.test.mjs (sibling source-reading test for chained patterns)
    - app/src/screens/SavedScreen.tsx (updated source from Task 1 — confirm the tab-change effect shape)
    - .planning/phases/50-retrieval-and-library-foundation/50-VALIDATION.md (row 4 contract this test enforces)
  </read_first>
  <behavior>
    - TPQ-01: The source contains a `useEffect(...)` whose deps array is `[activeTab]` and whose body contains `clearTimeout(debounceRef.current)` (Pitfall 8's actual mitigation — preserved).
    - TPQ-02: The same effect's body contains `setFilterConcept(null)`, `setFilterSource(null)`, `setFilterDate('all')` (filter chips reset on tab change).
    - TPQ-03: The same effect's body does NOT contain `setQuery('')` or `setInputDraft('')` (G6 fix — the user-typed query persists across tab change).
    - TPQ-04: The source contains the literal token `50-VALIDATION.md row 4` (or equivalent provenance reference) in a comment near the tab-change effect.
    - TPQ-05: Regression guard — the `clearAllFilters` callback (which is a separate function called from the no-match "Clear filters" button, not from tab change) STILL contains `setQuery('')` and `setInputDraft('')` so query-clearing remains available via explicit user action.
  </behavior>
  <action>
    Create `app/tests/screens/SavedScreen.tab-preserves-query.test.mjs` as a source-reading test file. Mirror the structure of `app/tests/screens/SavedScreen.search-scope.test.mjs`: imports from `node:test` and `node:assert/strict`, read source via `readFileSync(path.join(appRoot, 'src/screens/SavedScreen.tsx'), 'utf8')`. Write the 5 named tests TPQ-01 through TPQ-05 listed in `<behavior>`.

    Each test uses `assert.match` or `assert.doesNotMatch` against the source string. For TPQ-03 the regex must scope to the lines inside the tab-change useEffect, not the whole file (because clearAllFilters legitimately calls setQuery('')). Implementation strategy: extract the block using a non-greedy regex from `useEffect\(\(\) => \{` through the matching `\}, \[activeTab\]\);`, then assert the extracted substring does NOT match `setQuery\(''\)` or `setInputDraft\(''\)`. Use a helper function `extractTabChangeEffect(src)` that returns the substring or fails the test if the block cannot be located.

    For TPQ-05 conversely extract the body of `const clearAllFilters = useCallback(...)` (delimited by `}, [])` at the end) and assert that BOTH `setQuery('')` and `setInputDraft('')` ARE present in that block.

    File header comment must explain: "Phase 50 gap closure 50-12 (G6). Enforces 50-VALIDATION.md row 4: tab switch on /saved preserves the search-bar query and rescopes results. Tab change still clears filters + debounce timer (Pitfall 8 mitigation), but NOT the user-typed query. Source-reading only — no DOM render. Helpers extract the targeted useEffect / useCallback bodies so the regex scope does not catch the legitimate clearAllFilters path."
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/screens/SavedScreen.tab-preserves-query.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `app/tests/screens/SavedScreen.tab-preserves-query.test.mjs` exists.
    - File contains 5 test names matching `TPQ-0[1-5]`. Verify: `grep -cE "TPQ-0[1-5]" app/tests/screens/SavedScreen.tab-preserves-query.test.mjs` returns at least 5.
    - File contains a helper function (named or inline) that extracts the tab-change useEffect body separately from the rest of the source — TPQ-03 must not regress because of clearAllFilters's setQuery call elsewhere.
    - All 5 tests pass after Task 1 is applied.
    - File header comment cites `50-VALIDATION.md row 4`.
  </acceptance_criteria>
  <done>
    A dedicated source-reading test enforces the tab-preserves-query contract. If a future edit re-introduces setQuery('') / setInputDraft('') into the tab-change effect, this test fails loudly with provenance pointing to row 4 of 50-VALIDATION.md.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author chip blur-race regression test</name>
  <files>app/tests/screens/SavedScreen.chip-blur-race.test.mjs</files>
  <read_first>
    - app/tests/screens/SavedScreen.search-scope.test.mjs (canonical sibling test pattern)
    - app/src/screens/SavedScreen.tsx (updated source from Task 1 — the FilterChip block now contains onPointerDown + onMouseDown preventDefault)
    - .planning/phases/50-retrieval-and-library-foundation/50-UAT.md (Test 5 analysis describing the blur-race + remediation patterns a/b/c)
  </read_first>
  <behavior>
    - CBR-01: The source contains a `function FilterChip` declaration (regression guard — if the chip is ever extracted to `ui/`, the test grep would need to follow; until then it stays inline per UI-SPEC).
    - CBR-02: Inside the FilterChip function body, the source contains an `onPointerDown` handler that calls `e.preventDefault()`.
    - CBR-03: Inside the FilterChip function body, the source contains an `onMouseDown` handler that calls `e.preventDefault()`. Both events are guarded — Capacitor Android WebView dispatches both on touch.
    - CBR-04: The handlers are placed BEFORE the `onClick={onTap}` prop on the same `<button>` element (regression guard: if reordered after onClick, React's synthetic event ordering is unaffected but the explicit code ordering documents intent).
    - CBR-05: Source contains a code comment near the new handlers citing "G2" so future readers know why preventDefault is there.
    - CBR-06: FilterChip uses `padding: '6px 12px'` (G7 paired in the same component — sanity that EDIT 3 also landed).
  </behavior>
  <action>
    Create `app/tests/screens/SavedScreen.chip-blur-race.test.mjs` mirroring the structure of the sibling source-reading test files. The test file should extract the FilterChip function body (using a regex from `function FilterChip` to the matching closing brace before the next top-level declaration) and run all chip-scoped assertions against that extracted substring so other parts of SavedScreen.tsx (SavedRow's own pointer handlers, the CollectionRow's long-press start/end) do not produce false positives.

    Use a helper `extractFilterChipBody(src)` that returns the substring containing the FilterChip function. CBR-02..CBR-06 all assert against that substring with `assert.match` / `assert.doesNotMatch`.

    For CBR-04 the ordering test: extract the JSX `<button ...>` element opening tag and verify the order of attributes via a regex that requires `onPointerDown` AND `onMouseDown` to appear before `onClick` in the attribute list. Acceptable implementation: a single regex `/<button[^>]*onPointerDown[^>]*onMouseDown[^>]*onClick/s` matching against the chip-only substring.

    File header comment must explain: "Phase 50 gap closure 50-12 (G2). Enforces the chip-tap-survives-empty-query-blur fix at source level. Without onPointerDown + onMouseDown preventDefault on the FilterChip wrapper, the search input's blur fires before the chip's onClick, and showFilterChips's latch (searchFocused || inputDraft.length > 0 || anyFilterActive) collapses the chip-row on the empty-query path before tap. Source-reading only."
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && node --test tests/screens/SavedScreen.chip-blur-race.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `app/tests/screens/SavedScreen.chip-blur-race.test.mjs` exists.
    - File contains 6 test names matching `CBR-0[1-6]`. Verify: `grep -cE "CBR-0[1-6]" app/tests/screens/SavedScreen.chip-blur-race.test.mjs` returns at least 6.
    - File contains an `extractFilterChipBody` helper (or inline equivalent) that scopes assertions to the FilterChip function block.
    - All 6 tests pass after Task 1 is applied.
    - File header comment cites "G2" and the UAT analysis pattern (preventDefault on pointer/mouse down).
  </acceptance_criteria>
  <done>
    A dedicated source-reading test enforces the chip-blur-race fix at the FilterChip block. If a future edit drops onPointerDown / onMouseDown / preventDefault from the FilterChip wrapper, this test fails loudly with provenance pointing to 50-UAT.md Test 5.
  </done>
</task>

</tasks>

<verification>
- `cd app && node --test tests/screens/SavedScreen.tab-preserves-query.test.mjs tests/screens/SavedScreen.chip-blur-race.test.mjs` exits 0
- Existing SavedScreen tests (`tests/screens/SavedScreen.search-scope.test.mjs`, `tests/screens/SavedScreen.collections-tab.test.mjs`) continue to pass
- `cd app && npm test` exits 0 (full suite)
- Source-level grep checks pass per acceptance criteria
</verification>

<success_criteria>
- G2 closed: On /saved with an empty search bar, tapping a filter chip opens the FilterPickerSheet. The chip-row no longer collapses pre-emptively.
- G6 closed: Switching tabs on /saved preserves the user-typed query. Filters and the debounce timer still reset; the Fuse index rebuilds automatically against the new tab's corpus.
- G7 closed: Concept / Source / Date chips render with uniform 6px × 12px padding.
- Two new regression tests enforce the fixes at source level so future edits cannot silently regress.
- Pitfall 8's actual mitigation (debounce timer flush) is preserved — the test suite proves both halves of the distinction.
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-12-SUMMARY.md` when done.
</output>
</content>
</invoke>