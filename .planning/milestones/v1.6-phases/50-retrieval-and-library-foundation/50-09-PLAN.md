---
phase: 50-retrieval-and-library-foundation
plan: 09
type: execute
wave: 3
depends_on: [50-04, 50-06, 50-07, 50-08]
files_modified:
  - app/src/screens/SavedScreen.tsx
  - app/src/screens/HomeScreen.tsx
  - app/tests/screens/SavedScreen.collections-tab.test.mjs
  - app/tests/screens/SavedScreen.search-scope.test.mjs
autonomous: true
requirements: [RETRIEVE-01, RETRIEVE-02]
threat_refs: [T-50-XSS-HL, T-50-QUERY-DOS, T-50-ORPHAN, T-50-PERF-INDEX]
must_haves:
  truths:
    - "SavedScreen exposes 4 tabs: Saved | Liked | History | Collections (4th tab is the new Phase 50 addition)"
    - "Search bar is pinned below Header (sticky, in-tree), 44px tall, with minWidth: 0 on the input alongside flex: 1 (load-bearing per CLAUDE.md §ChatInput rule)"
    - "Filter chip row (Concept | Source | Date) renders only when search bar is focused OR query has text; collapses on blur unless query/filters are active"
    - "Search is scoped to the active tab corpus (D-11): typing on Saved searches only saved posts; switching to History re-runs search on history corpus"
    - "Switching tabs clears query + all filter chips (Pitfall 8)"
    - "Fuse index is built inside useMemo keyed on activeTab + corpus identity (Pitfall 3 — never inside render body or onChange handler)"
    - "Search results render title with HighlightedText + 120-char body snippet ALSO with HighlightedText when bodyMarkdown matched"
    - "Empty/no-match state: 'No matches in {tab}' + 'Clear filters' text button that resets query + chips"
    - "Collections tab lists user collections as Folder-icon rows; tap → navigate /collections/:id; long-press → opens Rename/Delete sheet via reused useLongPress hook"
    - "LongPressMenu on HomeScreen feed tiles now opens CollectionPickerSheet via onOpenCollectionPicker — Save tap is no longer a direct toggle"
    - "Highlight match indices are spliced into JSX via HighlightedText (never dangerouslySetInnerHTML — T-50-XSS-HL)"
    - "Search query is capped at 200 chars at the service boundary (capQuery in library-search.service)"
  artifacts:
    - path: app/src/screens/SavedScreen.tsx
      provides: "4th tab + sticky search bar + focus-conditional filter chips + Fuse search wiring + per-tab corpus + result row variant + Collections list + CollectionPickerSheet host"
    - path: app/src/screens/HomeScreen.tsx
      provides: "LongPressMenu wiring updated: passes onOpenCollectionPicker → CollectionPickerSheet host state"
    - path: app/tests/screens/SavedScreen.collections-tab.test.mjs
      provides: "Tab=collections renders, COLLECTIONS_CHANGED subscription, collection row tap navigates to /collections/:id"
    - path: app/tests/screens/SavedScreen.search-scope.test.mjs
      provides: "minWidth: 0 enforcement, Fuse in useMemo not render body, ignoreLocation: true imported from library-search service, tab-scoped search"
  key_links:
    - from: app/src/screens/SavedScreen.tsx
      to: app/src/services/library-search.service.ts
      via: "buildIndex + search + capQuery + dateFilter + extractSnippet + rebaseIndices"
      pattern: "library-search"
    - from: app/src/screens/SavedScreen.tsx
      to: app/src/services/collection.service.ts
      via: "getCollections subscriber for Collections tab + getPostCollections (Picker)"
      pattern: "collectionService\\."
    - from: app/src/screens/SavedScreen.tsx
      to: app/src/components/ui/HighlightedText.tsx
      via: "Title + body snippet rendering inside SavedRow variant"
      pattern: "HighlightedText"
    - from: app/src/screens/SavedScreen.tsx
      to: app/src/components/CollectionPickerSheet.tsx
      via: "Hosted inside SavedScreen for Save-from-result-row flow"
      pattern: "CollectionPickerSheet"
    - from: app/src/screens/SavedScreen.tsx
      to: app/src/components/FilterPickerSheet.tsx
      via: "Hosted 3x — one each for Concept / Source / Date pickers"
      pattern: "FilterPickerSheet"
    - from: app/src/screens/HomeScreen.tsx
      to: app/src/components/CollectionPickerSheet.tsx
      via: "Picker hosted on HomeScreen so LongPressMenu Save row opens it"
      pattern: "CollectionPickerSheet"
---

<objective>
Integrate every Phase 50 primitive into the user-facing surface. SavedScreen gains: (1) a 4th Collections tab, (2) a sticky search bar with focus-conditional filter chips (Concept/Source/Date), (3) per-tab Fuse search with highlighted result rows and 120-char body snippets, (4) the no-match empty state, (5) a hosted CollectionPickerSheet for save-from-result-row flow, and (6) COLLECTIONS_CHANGED subscription. HomeScreen gains the picker host state so LongPressMenu Save tap opens the picker per D-04 (replacing direct-toggle behavior).

Purpose: This is the unifying integration plan. Plans 50-03 through 50-08 produced isolated primitives; this plan composes them into the surfaces the user actually sees. Turns 50-02 scaffolds `SavedScreen.collections-tab.test.mjs` and `SavedScreen.search-scope.test.mjs` from RED to GREEN. The final phase milestone — "user can search Saved/Liked/History by title/body/concept/source/date and reopen post" + "user can create/rename/delete collections and add/remove posts" — passes through this plan.

Output: Modified SavedScreen.tsx + modified HomeScreen.tsx + 2 green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md
@.planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md
@.planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md
@.planning/phases/50-retrieval-and-library-foundation/50-04-SUMMARY.md
@.planning/phases/50-retrieval-and-library-foundation/50-06-SUMMARY.md
@.planning/phases/50-retrieval-and-library-foundation/50-07-SUMMARY.md
@.planning/phases/50-retrieval-and-library-foundation/50-08-SUMMARY.md
@app/src/screens/SavedScreen.tsx

<interfaces>
<!-- Tab type extension (SavedScreen.tsx:48 currently `type Tab = 'saved' | 'liked' | 'history';`): -->

type Tab = 'saved' | 'liked' | 'history' | 'collections';

<!-- New screen state (in addition to existing savedPosts / likedPosts / historyGroups / activeTab): -->

const [query, setQuery] = useState('');           // debounced value passed to Fuse
const [inputDraft, setInputDraft] = useState('');  // raw input bound to <input value>
const debounceRef = useRef<NodeJS.Timeout | null>(null);  // ~200ms debounce timer (CONTEXT Claude's Discretion)
const [searchFocused, setSearchFocused] = useState(false);
const [filterConcept, setFilterConcept] = useState<string | null>(null);
const [filterSource, setFilterSource] = useState<string | null>(null);
const [filterDate, setFilterDate] = useState<'today' | 'last7' | 'last30' | 'all'>('all');

const [collections, setCollections] = useState<Collection[]>(() => collectionService.getCollections());

// Picker sheet host (CollectionPickerSheet used by row-level Save):
const [pickerOpen, setPickerOpen] = useState(false);
const [pickerPostId, setPickerPostId] = useState<string | null>(null);

// Filter picker hosts (one each for Concept / Source / Date):
const [conceptPickerOpen, setConceptPickerOpen] = useState(false);
const [sourcePickerOpen, setSourcePickerOpen] = useState(false);
const [datePickerOpen, setDatePickerOpen] = useState(false);

// Rename/Delete state for Collections sub-tab long-press:
const [collectionActionId, setCollectionActionId] = useState<string | null>(null);
const [collectionActionSheetOpen, setCollectionActionSheetOpen] = useState(false);

<!-- Per-tab corpus selector: -->

const corpusForTab = (tab: Tab): DailyPost[] => {
  if (tab === 'saved') return savedPosts;
  if (tab === 'liked') return likedPosts;
  if (tab === 'history') return Array.from(historyGroups.values()).flat();
  return [];  // collections tab has no Fuse search; it lists collection titles
};

<!-- Fuse index: useMemo keyed on activeTab + corpus identity. NEVER built inside render body or onChange handler (Pitfall 3): -->

import { buildIndex, search as fuseSearch, capQuery, dateFilter, extractSnippet, rebaseIndices } from '../services/library-search.service';

const fuseIndex = useMemo(() => buildIndex(corpusForTab(activeTab)), [activeTab, savedPosts, likedPosts, historyGroups]);

<!-- Results computation: -->

const trimmed = capQuery(query.trim());
const baseResults: Array<{ item: DailyPost; matches?: readonly Fuse.FuseResultMatch[] }> =
  trimmed.length >= 2
    ? fuseSearch(fuseIndex, trimmed)
    : corpusForTab(activeTab).map(item => ({ item }));

const filtered = baseResults.filter(({ item }) => {
  if (filterConcept && !(item.sourceQuestionTitles ?? []).includes(filterConcept)) return false;
  if (filterSource && item.contextLabel !== filterSource) return false;
  if (!dateFilter(item.generatedAt, filterDate)) return false;
  return true;
});

<!-- Search bar wrapper (UI-SPEC Surface 1): -->

<div style={{ position: 'sticky', top: `calc(var(--safe-area-top) + ${HEADER_HEIGHT}px)`, zIndex: 5, background: 'var(--surface)', padding: '8px 16px 0' }}>
  <div style={{ display: 'flex', alignItems: 'center', gap: '8px', padding: '0 16px', backgroundColor: 'var(--surface-variant)', borderRadius: 'var(--radius-pill)', height: '44px', border: `1.5px solid ${searchFocused || query.length > 0 ? 'var(--primary-40)' : 'transparent'}`, transition: 'border-color 0.2s' }}>
    <Search size={18} color={searchFocused || query.length > 0 ? 'var(--primary-40)' : 'var(--muted-foreground)'} style={{ flexShrink: 0 }} />
    <input
      type="text"
      value={inputDraft}
      onChange={(e) => {
        const value = e.target.value;
        setInputDraft(value);
        if (debounceRef.current) clearTimeout(debounceRef.current);
        debounceRef.current = setTimeout(() => { setQuery(value); }, 200);
      }}
      onFocus={() => setSearchFocused(true)}
      onBlur={() => setSearchFocused(false)}
      placeholder={t('library.search.placeholder')}
      style={{ flex: 1, minWidth: 0, background: 'transparent', color: 'var(--foreground)', fontSize: 14, border: 'none', outline: 'none' }}
    />
    {inputDraft.length > 0 && (
      <button onClick={() => { if (debounceRef.current) clearTimeout(debounceRef.current); setInputDraft(''); setQuery(''); }} aria-label={t('library.search.clearAria')} style={{ ... 44×44 hit area, X icon ... }}><X size={18} /></button>
    )}
  </div>
  {(searchFocused || inputDraft.length > 0 || filterConcept || filterSource || filterDate !== 'all') && (
    <FilterChipRow ... />   // inline JSX or extracted helper
  )}
</div>

<!-- Filter chip row (UI-SPEC Surface 2 — inline JSX in SavedScreen, NOT extracted to a separate component): -->

<div style={{ display: 'flex', gap: '8px', padding: '8px 16px 0', overflowX: 'auto', scrollbarWidth: 'none' as any }}>
  <FilterChip label={filterConcept ?? t('library.filters.concept.label')} active={!!filterConcept} onTap={() => setConceptPickerOpen(true)} onClear={() => setFilterConcept(null)} />
  <FilterChip label={filterSource ?? t('library.filters.source.label')} active={!!filterSource} onTap={() => setSourcePickerOpen(true)} onClear={() => setFilterSource(null)} />
  <FilterChip label={filterDate === 'all' ? t('library.filters.date.label') : t(`library.filters.date.${filterDate}`)} active={filterDate !== 'all'} onTap={() => setDatePickerOpen(true)} onClear={() => setFilterDate('all')} />
</div>

<!-- Concept picker options: questionService.getAnchors() mapped to { label: title, value: id-or-title }.
     Source picker options: deduped contextLabel from corpusForTab(activeTab) (only labels present in current tab).
     Date picker options: 4 fixed presets (today / last7 / last30 / all). -->

<!-- Result row variant (UI-SPEC Surface 7): -->

When trimmed.length >= 2, render SavedRow with two additions:
  - Title wrapped in <HighlightedText text={post.title} indices={titleIndices} />
  - 3rd line below contextLabel: snippet from extractSnippet(post.bodyMarkdown ?? '', firstBodyMatchStart) + HighlightedText with rebaseIndices(bodyIndices, snippet.offset, snippet.text.length)

<!-- Empty no-match state (UI-SPEC Surface 8): -->

When trimmed.length >= 2 AND filtered.length === 0:
  Search icon (40px var(--muted-foreground)) +
  Heading t('library.search.noMatches', { tab: t(`saved.tabs.${activeTab}`) }) +
  "Clear filters" button: setQuery(''), setFilterConcept(null), setFilterSource(null), setFilterDate('all')

<!-- Collections tab — UI-SPEC Surface 5: -->

When activeTab === 'collections':
  if collections.length === 0 → empty state
  else → list of collection rows (Folder icon 22 + name + postCount badge + ChevronRight). Tap → navigate(`/collections/${c.id}`). Long-press → open rename/delete sheet.

<!-- COLLECTIONS_CHANGED subscription (sibling effect to ENGAGEMENT_CHANGED): -->

useEffect(() => {
  const unsub = eventBus.subscribe('COLLECTIONS_CHANGED', () => {
    setCollections(collectionService.getCollections());
    setSavedPosts(engagementService.getSavedPosts());   // pin-set may have changed → history view subset
  });
  return unsub;
}, []);

<!-- Tab change effect — clear search + filters (Pitfall 8): -->

useEffect(() => {
  if (debounceRef.current) clearTimeout(debounceRef.current);
  setInputDraft('');
  setQuery('');
  setFilterConcept(null);
  setFilterSource(null);
  setFilterDate('all');
}, [activeTab]);

<!-- HomeScreen.tsx changes (lines 92-100 area + line 1053 LongPressMenu element): -->

Add picker host state alongside existing menu state:
  const [pickerOpen, setPickerOpen] = useState(false);
  const [pickerPostId, setPickerPostId] = useState<string | null>(null);

Update existing LongPressMenu JSX (lines 1053-1058) to pass onOpenCollectionPicker:
  <LongPressMenu
    open={menuOpen}
    onClose={closeMenu}
    postId={menuPostId}
    anchorId={menuAnchorId}
    onOpenCollectionPicker={(pid) => { setPickerPostId(pid); setPickerOpen(true); }}
  />

Render CollectionPickerSheet adjacent:
  <CollectionPickerSheet
    open={pickerOpen}
    onClose={() => { setPickerOpen(false); setPickerPostId(null); }}
    postId={pickerPostId}
  />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SavedScreen — 4th tab + search bar + filter chips + Fuse wiring + result rows + Collections list + picker hosts</name>
  <files>app/src/screens/SavedScreen.tsx</files>
  <read_first>
    - app/src/screens/SavedScreen.tsx (FULL file — 430 lines; the changes are extensive; keep all existing day-grouping / EmptyState / TabButton patterns)
    - app/src/services/library-search.service.ts (from 50-04 — buildIndex, search, capQuery, dateFilter, extractSnippet, rebaseIndices, FUSE_OPTIONS, MAX_QUERY_LENGTH)
    - app/src/services/collection.service.ts (from 50-03 — getCollections, getPostCollections, renameCollection, deleteCollection, addPost, removePost, getCollectionPosts)
    - app/src/services/question.service.ts (getAnchors for Concept filter options — use existing API; verify path with grep)
    - app/src/components/ui/HighlightedText.tsx (from 50-06)
    - app/src/components/CollectionPickerSheet.tsx (from 50-06)
    - app/src/components/FilterPickerSheet.tsx (from 50-06)
    - app/src/components/LongPressMenu.tsx (from 50-07 — verify onOpenCollectionPicker + collectionContext props)
    - app/src/hooks/useLongPress.ts (480ms long-press for Collections tab rows)
    - app/src/lib/event-bus.ts
    - .planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md (FULL — Surfaces 1, 2, 3, 5, 7, 8)
    - .planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md §"app/src/screens/SavedScreen.tsx — extension"
    - .planning/phases/50-retrieval-and-library-foundation/50-RESEARCH.md §"Pattern 6" + §"Pitfall 1" (ignoreLocation) + §"Pitfall 3" (useMemo discipline) + §"Pitfall 8" (tab change reset)
    - CLAUDE.md §"ChatInput flex shrink" (minWidth: 0 rule — same applies to the search bar input here)
    - CLAUDE.md §"Header positioning" (don't add transform/will-change/filter/contain to ancestors)
    - app/tests/screens/SavedScreen.collections-tab.test.mjs (Wave 0 scaffold)
    - app/tests/screens/SavedScreen.search-scope.test.mjs (Wave 0 scaffold)
  </read_first>
  <behavior>
    - 4 tabs render: Saved / Liked / History / Collections; tab strip styling unchanged (same TabButton component)
    - At viewport widths <360px, tab labels drop to 13px (CSS media query per UI-SPEC Surface 3 adaptive override) — accept simple `@media (max-width: 359px)` block via <style> tag
    - Search bar pinned sticky below Header; 44px height; var(--surface-variant) background; border transitions to var(--primary-40) on focus or when query non-empty
    - Search input has BOTH `flex: 1` AND `minWidth: 0` inline (load-bearing per CLAUDE.md ChatInput rule — without this, the Clear-X button overflows on Android WebView)
    - Search input is split into a raw `inputDraft` state (bound to `<input value>`) and a `query` state (passed to Fuse). The onChange handler clears any pending `debounceRef.current` via `clearTimeout` and schedules a new `setTimeout(() => setQuery(value), 200)` (~200ms debounce per CONTEXT.md Claude's Discretion). The user sees instant input echo; Fuse only re-searches after the 200ms idle window.
    - Clear-X button visible only when inputDraft.length > 0; clearing also clears the pending debounce timer and resets both `inputDraft` and `query` to empty so Fuse stops searching immediately
    - Filter chip row mounts when `searchFocused || inputDraft.length > 0 || any filter is non-default`; collapses on blur otherwise (UI-SPEC Surface 1)
    - 3 chips: Concept | Source | Date. Inactive label = filter type name; active label = picked value with trailing X icon to clear (stopPropagation so tap on chip body re-opens picker)
    - Concept chip tap opens FilterPickerSheet with options derived from `questionService.getAnchors().map(a => ({ label: a.title, value: a.title }))` (use title as both label and value since Concept filter compares against `post.sourceQuestionTitles[]` which contains titles); empty-state title/body keys from library.filters.concept.*
    - Source chip tap opens FilterPickerSheet with options derived from a `Set` of `corpusForTab(activeTab).map(p => p.contextLabel).filter(Boolean)`; empty-state title from library.filters.source.emptyTitle
    - Date chip tap opens FilterPickerSheet with 4 fixed preset rows (today / last7 / last30 / all); default selected = filterDate
    - Fuse index built in `useMemo` keyed on `[activeTab, savedPosts, likedPosts, historyGroups]` — NOT in render body, NOT inside onChange handler (Pitfall 3)
    - When query.trim().length >= 2: results = `fuseSearch(fuseIndex, capQuery(query.trim()))`; further filtered by chips
    - When query.trim().length < 2: results = full active-tab corpus, further filtered by chips
    - Result rows render SavedRow with HighlightedText for title (when titleIndices present) and a 120-char body snippet below contextLabel (when bodyIndices present) — snippet rendered via extractSnippet + rebaseIndices + HighlightedText
    - Switching tabs clears query + all 3 filter chips (Pitfall 8)
    - Tap on collection row in Collections tab → navigate(`/collections/${c.id}`); long-press → opens rename/delete sheet (use compact BottomSheet with two rows)
    - Rename sheet inside Collections tab: TextInput + "Save name" button → collectionService.renameCollection — same validation as picker create
    - Delete sheet inside Collections tab: confirmation with "Keep collection" + "Delete" buttons → collectionService.deleteCollection
    - CollectionPickerSheet is hosted at SavedScreen level too, so a future row-level Save action (e.g., from a long-press inside Saved tab row — optional UX, not required by UI-SPEC) opens it. For Phase 50, the picker host on SavedScreen is for forward compat; the primary entry path remains HomeScreen tile long-press.
    - SavedScreen subscribes to COLLECTIONS_CHANGED (sibling effect to existing ENGAGEMENT_CHANGED)
    - No transform/will-change/filter/contain on the outer screen container (Header portal rule)
  </behavior>
  <action>
1. Read SavedScreen.tsx in full (430 lines).

2. Extend the `Tab` type union at line 48 to include `'collections'`.

3. Add the new state hooks per `<interfaces>` block (after existing savedPosts/likedPosts/historyGroups state around lines 243-252). This includes BOTH `query` (debounced value, fed to Fuse) and `inputDraft` (raw input echo) plus a `debounceRef = useRef<NodeJS.Timeout | null>(null)` for the ~200ms debounce timer. Ensure `useRef` is imported from React alongside `useState` and `useEffect` (add it to the existing React import if not already present).

4. Add imports:
   - lucide-react: `Search`, `X`, `Folder`, `ChevronRight`, `MoreVertical`, `FolderPlus`, `Check` (some only used by Filter chips / Collections tab branch).
   - From new modules: `buildIndex`, `search as fuseSearch`, `capQuery`, `dateFilter`, `extractSnippet`, `rebaseIndices` from `../services/library-search.service`; `collectionService` from `../services/collection.service`; `HighlightedText` (default import) from `../components/ui/HighlightedText`; `CollectionPickerSheet` from `../components/CollectionPickerSheet`; `FilterPickerSheet` from `../components/FilterPickerSheet`; `LongPressMenu` from `../components/LongPressMenu` (if needed for Saved/Liked/History row long-press wiring — see step 11 note); `useLongPress` from `../hooks/useLongPress`; `BottomSheet` from `../components/ui/BottomSheet`; `questionService` from `../services/question.service`; `type Collection` from `../types`; `type DailyPost` already imported.

5. Add the COLLECTIONS_CHANGED subscription as a SIBLING useEffect to the existing ENGAGEMENT_CHANGED one (do NOT merge into a single subscriber — keep concerns separate per PATTERNS.md note).

6. Add the tab-change reset effect: `useEffect([activeTab], () => { if (debounceRef.current) clearTimeout(debounceRef.current); setInputDraft(''); setQuery(''); setFilterConcept(null); setFilterSource(null); setFilterDate('all'); });`

7. Build `corpusForTab` helper and `fuseIndex` useMemo per `<interfaces>` block. Verify dependency array does NOT include `query` (build vs search separation; only corpus changes invalidate index).

8. Compute `trimmed`, `baseResults`, `filtered` per `<interfaces>` block. Compose `firstBodyMatchStart` and `titleIndices` from a single Fuse result by iterating `result.matches`.

9. Insert the search bar wrapper per UI-SPEC Surface 1 (sticky position with `top: calc(var(--safe-area-top) + HEADER_HEIGHT + 'px')`, `zIndex: 5`). CRITICAL: input has BOTH `flex: 1` AND `minWidth: 0` inline AND `value={inputDraft}`. The `onChange` handler MUST debounce: synchronously call `setInputDraft(e.target.value)` for instant input echo, then `clearTimeout(debounceRef.current)` and `debounceRef.current = setTimeout(() => setQuery(value), 200)` so Fuse re-searches only after a 200ms idle window. Clear-X button must also `clearTimeout(debounceRef.current)` before resetting both `inputDraft` and `query` to empty.

10. Insert the conditional filter chip row per UI-SPEC Surface 2 below the search bar. Inline FilterChip helper (sub-component within SavedScreen.tsx — NOT extracted) handles the active/inactive visual states.

11. Update the JSX render: the existing tab strip gets a 4th TabButton for Collections. The content branch becomes:
    - if activeTab === 'collections' → render collections list (or empty state per UI-SPEC Surface 5)
    - else if filtered.length === 0 AND trimmed.length >= 2 → render no-match empty state per UI-SPEC Surface 8
    - else if isFlatTab (saved/liked) → render filtered.map → SavedRow with HighlightedText title + optional body-snippet line
    - else (history) → render day-grouped layout; if any filter is active OR query non-empty, flatten history and render as a flat filtered list with day separators preserved if practical (simplification: if filters/query active on History, render as flat list sorted by generatedAt desc — match operator's "Spotify search expectation" mention in CONTEXT D-11)

12. Add three FilterPickerSheet instances (Concept / Source / Date) hosted at the end of the JSX tree alongside CollectionPickerSheet. Options computation:
    - Concept: `questionService.getAnchors().map(a => ({ label: a.title, value: a.title }))` (or `a.id` if the matching field in `post.sourceQuestionTitles` is id-based — verify by reading question.service.ts and matching to the field in DailyPost.sourceQuestionTitles)
    - Source: `Array.from(new Set(corpusForTab(activeTab).map(p => p.contextLabel).filter(Boolean))).map(label => ({ label, value: label }))`
    - Date: 4 fixed presets with i18n labels (`today` / `last7` / `last30` / `all`)

13. Add CollectionPickerSheet at the end of the JSX tree (forward-compat host).

14. Add Collections tab long-press rename/delete BottomSheet (small inline component or just two BottomSheet instances triggered by collectionActionId + collectionActionSheetOpen state).

15. Add a `<style>` tag for the <360px adaptive tab font-size override.

16. Update the no-history-error-state branch to also account for the new tab states (only render the error block when `activeTab === 'history' && historyError`).

17. Do NOT add transform / will-change / filter / contain styles to the outer container.

18. Update the SavedRow component to OPTIONALLY accept a `searchMatch?: readonly Fuse.FuseResultMatch[]` prop. When provided, render the title via `<HighlightedText text={post.title} indices={titleIndices} />` and the body snippet below contextLabel. When absent, behavior is unchanged (backward-compat with the History day-grouped branch).
  </action>
  <verify>
    <automated>cd app && node --test tests/screens/SavedScreen.collections-tab.test.mjs tests/screens/SavedScreen.search-scope.test.mjs tests/screens/SavedScreen.test.mjs 2>&1 | tail -25 && grep -c "clearTimeout" src/screens/SavedScreen.tsx && grep -E "setTimeout\(.{0,80}200\)" src/screens/SavedScreen.tsx | head -1</automated>
  </verify>
  <done>
    Both new tests pass; existing SavedScreen.test.mjs still passes. `grep -c "minWidth: 0" src/screens/SavedScreen.tsx` returns ≥1 (search input rule). `grep -c "ignoreLocation" src/screens/SavedScreen.tsx` returns 0 (Fuse options are imported from library-search.service, NOT inlined). `grep -c "useMemo" src/screens/SavedScreen.tsx` returns ≥2 (existing historyFlatIndex + new fuseIndex). `grep -c "HighlightedText" src/screens/SavedScreen.tsx` returns ≥1. `grep -c "clearTimeout" src/screens/SavedScreen.tsx` returns ≥1 AND `grep -E "setTimeout\(.{0,80}200\)" src/screens/SavedScreen.tsx` matches ≥1 line (~200ms search debounce per CONTEXT.md Claude's Discretion).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire HomeScreen LongPressMenu to CollectionPickerSheet (D-04 — Save tap opens picker)</name>
  <files>app/src/screens/HomeScreen.tsx</files>
  <read_first>
    - app/src/screens/HomeScreen.tsx (lines 90-105 — LongPressMenu host state; lines 1045-1058 — LongPressMenu JSX element)
    - app/src/components/CollectionPickerSheet.tsx (from 50-06 — prop signature)
    - app/src/components/LongPressMenu.tsx (from 50-07 — new onOpenCollectionPicker + collectionContext props)
    - .planning/phases/50-retrieval-and-library-foundation/50-UI-SPEC.md §"Surface 9" (Save-row behavior change on feed tiles)
    - .planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md (no specific HomeScreen analog listed but the change is mechanical)
  </read_first>
  <behavior>
    - HomeScreen owns picker host state: pickerOpen + pickerPostId
    - LongPressMenu element on HomeScreen passes `onOpenCollectionPicker={(pid) => { setPickerPostId(pid); setPickerOpen(true); }}`
    - CollectionPickerSheet element renders adjacent to LongPressMenu with open/pickerPostId/onClose props
    - Long-press a feed tile → LongPressMenu opens → tap Save → LongPressMenu closes + CollectionPickerSheet opens with Saved pre-checked (per D-04 + D-05)
    - Tap Done on picker with no changes → engagementService.savePost(postId) fires (single-tap-save preserved)
  </behavior>
  <action>
1. Add to HomeScreen state (insert adjacent to existing menu state at lines 98-100):
   ```
   const [pickerOpen, setPickerOpen] = useState(false);
   const [pickerPostId, setPickerPostId] = useState<string | null>(null);
   ```

2. Add CollectionPickerSheet import near other component imports.

3. Update the existing LongPressMenu JSX (lines 1053-1058) to add the `onOpenCollectionPicker` prop:
   ```
   <LongPressMenu
     open={menuOpen}
     onClose={closeMenu}
     postId={menuPostId}
     anchorId={menuAnchorId}
     onOpenCollectionPicker={(pid) => { setPickerPostId(pid); setPickerOpen(true); }}
   />
   ```

4. Render CollectionPickerSheet immediately after LongPressMenu (sibling JSX):
   ```
   <CollectionPickerSheet
     open={pickerOpen}
     onClose={() => { setPickerOpen(false); setPickerPostId(null); }}
     postId={pickerPostId}
   />
   ```

5. Do NOT change any other HomeScreen behavior or styling. Do NOT add collectionContext to LongPressMenu on HomeScreen — HomeScreen is the FEED context, not a drill-in context (per UI-SPEC §Surface 9: "On feed tiles (HomeScreen), LongPressMenu keeps its existing 3 rows").
  </action>
  <verify>
    <automated>cd app && grep -c "onOpenCollectionPicker" src/screens/HomeScreen.tsx && grep -c "CollectionPickerSheet" src/screens/HomeScreen.tsx && cd app && npx tsc -b --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    Both greps return ≥2 (import + use). `npx tsc -b --noEmit` clean modulo pre-existing SavedScreen errors. `cd app && npm test` overall green (no regression in any HomeScreen test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → search query | Fuse-only (no LLM, no network). capQuery from library-search service caps at 200 chars. |
| Fuse match indices → highlighted JSX | React text-node escaping via HighlightedText (50-06). |
| Collection postIds → orphan resolution | postHistoryService dropouts handled by collectionService.getCollectionPosts() silently. |
| Index construction frequency | Fuse rebuild on every keystroke = jank. useMemo with corpus-identity dependency = once per corpus change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-XSS-HL | Tampering / XSS | Highlighted match indices in SavedScreen result rows | mitigate | HighlightedText component (50-06) is the rendering primitive — no dangerouslySetInnerHTML. SavedScreen imports it and passes indices; never serializes HTML strings. |
| T-50-QUERY-DOS | Denial of Service | Long search query | mitigate | SavedScreen calls capQuery(query.trim()) from library-search.service before passing to fuseSearch. 200-char hard cap. |
| T-50-ORPHAN | Spoofing / Stale Data | Collections sub-tab + result rows | mitigate | corpusForTab('collections') is intentionally empty (collections list is collection-level, not post-level). Collection drill-in (50-08) handles orphan postIds; SavedScreen Collections tab only shows collection summaries (not posts). |
| T-50-PERF-INDEX | Availability / UX | Fuse index construction inside render body | mitigate | useMemo with [activeTab, savedPosts, likedPosts, historyGroups] dependencies. Test asserts source contains `useMemo` AND the Fuse builder call is INSIDE the useMemo block. |
</threat_model>

<verification>
- `cd app && node --test tests/screens/SavedScreen.collections-tab.test.mjs` passes.
- `cd app && node --test tests/screens/SavedScreen.search-scope.test.mjs` passes.
- `cd app && node --test tests/screens/SavedScreen.test.mjs` passes (existing test — no regression).
- `cd app && npm test` overall green.
- `cd app && grep -c "minWidth: 0" src/screens/SavedScreen.tsx` returns ≥1.
- `cd app && grep -c "capQuery" src/screens/SavedScreen.tsx` returns ≥1.
- `cd app && grep -c "HighlightedText" src/screens/SavedScreen.tsx` returns ≥1.
- `cd app && grep -c "useMemo" src/screens/SavedScreen.tsx` returns ≥2.
- `cd app && grep -v '^\\s*//' src/screens/SavedScreen.tsx | grep -cE "transform: |willChange:|contain: |filter: " | head -1` returns 0 (no Header-portal-busting properties).
- `cd app && grep -c "clearTimeout" src/screens/SavedScreen.tsx` returns ≥1 AND `grep -c "setTimeout" src/screens/SavedScreen.tsx` returns ≥1 AND `grep -E "setTimeout\(.*200\)" src/screens/SavedScreen.tsx` returns ≥1 (search debounce per CONTEXT.md Claude's Discretion).
- `cd app && grep -c "CollectionPickerSheet" src/screens/HomeScreen.tsx` returns ≥2.
- `cd app && npx tsc -b --noEmit` clean modulo pre-existing SavedScreen errors (the new SavedScreen edits should remove the pre-existing errors noted in STATE.md by replacing the old code paths).
</verification>

<success_criteria>
- Phase 50 ROADMAP success criteria #1: ✓ User can search Saved / Liked / History items by title, body, concept, source, date (and reopen original post via row tap).
- Phase 50 ROADMAP success criteria #2: ✓ Reopen original post preserves concept/source context (existing /posts/:id navigation, unchanged).
- Phase 50 ROADMAP success criteria #3: ✓ User can tag (= add to Collection) and bookmark (= Save) posts; metadata persists.
- Phase 50 ROADMAP success criteria #4: ✓ Filter by saved/liked/history/collection/concept/source/date (Collection filtering happens via the Collections tab itself + drill-in; no separate "Collection" chip).
- All Wave-0 SavedScreen tests turn green.
- No regression in existing SavedScreen or HomeScreen tests.
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-09-SUMMARY.md` when done. Note the final integration state and any deferred follow-ups (e.g., extracting SearchBar/FilterChip to ui/ if Phase 51 needs them — flagged in UI-SPEC Component Inventory).
</output>
