---
phase: 50-retrieval-and-library-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/types/index.ts
autonomous: true
requirements: [RETRIEVE-02]
threat_refs: [T-50-XSS-NAME]
must_haves:
  truths:
    - "AppEvent union compile-checks COLLECTIONS_CHANGED emissions with the discriminating kind payload"
    - "Collection interface is exported from app/src/types and reusable by collection.service.ts and consumers"
  artifacts:
    - path: app/src/types/index.ts
      provides: "Collection interface + COLLECTIONS_CHANGED AppEvent union member"
      contains: "export interface Collection"
  key_links:
    - from: app/src/types/index.ts
      to: app/src/lib/event-bus.ts
      via: "AppEvent union extension consumed by EventBusImpl.subscribe<T> generic"
      pattern: "type: 'COLLECTIONS_CHANGED'"
---

<objective>
Extend the type layer with the `Collection` domain interface and the `COLLECTIONS_CHANGED` AppEvent union member so that all Wave 1+ work (`collection.service.ts`, screens, components) compiles when it references the new event type. This is foundation-only: zero runtime behavior changes.

Purpose: Unblocks every later plan. RESEARCH Pitfall 7 calls this out explicitly — without the AppEvent union extension, `eventBus.emit({ type: 'COLLECTIONS_CHANGED', ... })` fails type-check at every call site. Per D-03, the `Collection` shape is `{ id, name, postIds[], createdAt, updatedAt }`.

Output: Modified `app/src/types/index.ts` (additions only — no edits to existing types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-retrieval-and-library-foundation/50-CONTEXT.md
@.planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md

<interfaces>
<!-- Existing AppEvent union shape — copy the discriminated-payload pattern from ENGAGEMENT_CHANGED and GRAPH_UPDATED. See `<read_first>` files for exact source. -->

ENGAGEMENT_CHANGED precedent at app/src/types/index.ts:722 — payload is `{ kind: 'save' | 'unsave' | 'like' | 'unlike' | 'undismiss'; id: string }`.

GRAPH_UPDATED precedent at app/src/types/index.ts:737-745 — payload `kind` discriminator includes a verb literal union (`'rename' | 'move' | ... | 'undo' | 'classification' | 'replant' | 'unprune'`).

COLLECTIONS_CHANGED kind union per D-03 + Claude's Discretion in CONTEXT.md:
  'create' | 'rename' | 'delete' | 'add-post' | 'remove-post'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Collection interface + COLLECTIONS_CHANGED AppEvent union member</name>
  <files>app/src/types/index.ts</files>
  <read_first>
    - app/src/types/index.ts (full file — verify AppEvent union location at line 692, ENGAGEMENT_CHANGED shape at line 722, GRAPH_UPDATED shape at lines 737-745, DailyPost at line 543)
    - .planning/phases/50-retrieval-and-library-foundation/50-PATTERNS.md (§"app/src/types/index.ts" — exact insertion shape; quote at lines 193-222)
    - .planning/phases/50-retrieval-and-library-foundation/50-CONTEXT.md (D-03 Collection shape; Claude's Discretion event payload shape)
  </read_first>
  <behavior>
    - tsc -b --noEmit succeeds after additions (no existing types broken)
    - Importing `Collection` from `app/src/types` yields the shape `{ id: string; name: string; postIds: string[]; createdAt: number; updatedAt: number }`
    - `eventBus.emit({ type: 'COLLECTIONS_CHANGED', payload: { kind: 'create', collectionId: 'c1' } })` type-checks against AppEvent union
    - `eventBus.emit({ type: 'COLLECTIONS_CHANGED', payload: { kind: 'bogus', collectionId: 'c1' } })` is rejected by tsc
    - `eventBus.subscribe('COLLECTIONS_CHANGED', (e) => e.payload.kind)` narrows correctly
  </behavior>
  <action>
Add two additions to app/src/types/index.ts (additions only — do not modify any existing type):

1. Export interface `Collection` with fields `id: string`, `name: string`, `postIds: string[]`, `createdAt: number`, `updatedAt: number`. Place this near other domain-data interfaces (insert after the `DailyPost` block around line 543 — same neighborhood as PostSnapshot / DailyPost). Add a one-line JSDoc comment referencing D-03 (CONTEXT) and noting that storage lives at `trellis_collections_v1`.

2. Add `COLLECTIONS_CHANGED` as a new AppEvent union member. Insert AFTER the `ENGAGEMENT_CHANGED` member at line 722 (keep related-event neighborhood). Discriminated payload literal `kind: 'create' | 'rename' | 'delete' | 'add-post' | 'remove-post'` plus `collectionId: string`. Include a one-line JSDoc comment referencing CONTEXT D-03 + Claude's Discretion + the "one signal per semantic event" rule from CLAUDE.md §"Event bus — unified GRAPH_UPDATED".

Do not introduce a new `Collection`-related event variant. Do not extend GRAPH_UPDATED. Do not change ENGAGEMENT_CHANGED kind literals (D-09 union happens inside `engagementService.getPinnedIds()`, not at the event-type layer).
  </action>
  <verify>
    <automated>cd app && npx tsc -b --noEmit 2>&1 | grep -E "(Collection|COLLECTIONS_CHANGED)" ; cd app && grep -c "type: 'COLLECTIONS_CHANGED'" src/types/index.ts</automated>
  </verify>
  <done>
    Collection interface exists, COLLECTIONS_CHANGED member added to AppEvent union, tsc passes without new errors, grep of the union literal returns ≥1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → collection name | Collection name accepted from user keyboard input in CollectionPickerSheet / drill-in rename sheet, persisted to localStorage as part of `Collection.name`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-XSS-NAME | Information Disclosure / Tampering | `Collection.name` field (typed here, validated in 50-03) | mitigate | Type allows any string; mitigation lives at the service boundary (50-03 validateName trims, ≤50 chars, dedup) and at render boundary (React text-node escaping). This plan only declares the type — actual mitigation is enforced in 50-03 and across consumers in 50-06 / 50-09. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean modulo pre-existing SavedScreen errors noted in STATE.md (no new errors introduced).
- `grep -c "type: 'COLLECTIONS_CHANGED'" app/src/types/index.ts` returns 1.
- `grep -c "export interface Collection " app/src/types/index.ts` returns 1.
</verification>

<success_criteria>
- AppEvent union compiles with COLLECTIONS_CHANGED member.
- Collection interface is exported and usable from `app/src/types`.
- No existing type or test broken.
</success_criteria>

<output>
Create `.planning/phases/50-retrieval-and-library-foundation/50-01-SUMMARY.md` when done. Note: explicitly list (a) the line range where Collection was inserted and (b) the line range where COLLECTIONS_CHANGED was inserted — Wave 1 consumers will read this SUMMARY.
</output>
