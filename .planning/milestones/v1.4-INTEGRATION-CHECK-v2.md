---
milestone: v1.4
checked: 2026-04-24
status: gaps_found
findings_count:
  blocker: 0
  warn: 6
  note: 7
phase_status:
  phase_28: passed (30/30 per 28-VERIFICATION.md; no VERIFICATION regression since 2026-04-18)
  phase_29: stale_closed (TD-01 supersession documented in 29-VERIFICATION.md per Phase 33; TD-02/TD-03 still wired)
  phase_30: orphaned_absorbed (no VERIFICATION.md exists; Phase 32 planned to write it but was never executed; Phase 33 did not produce it either)
  phase_31: orphaned_absorbed (no VERIFICATION.md exists; same situation as Phase 30)
  phase_32: absorbed_no_execution (3 PLAN files + CONTEXT/DISCUSSION/RESEARCH/VALIDATION exist; zero SUMMARY files — plans were drafted but never executed)
  phase_32_1: human_needed (5/5 truths verified at code level; Wave 3 + Wave 4 all closed; 3 device retests still pending — G2/G4/G5)
  phase_33: passed (8/8 truths verified; tsc=0; test baseline 449/27 confirmed; 2 device-quality items pending)
prior_integration_check: .planning/v1.4-INTEGRATION-CHECK.md (2026-04-18)
---

# v1.4 Integration Check v2 — Post Phase 32.1 + Phase 33

**Checked:** 2026-04-24
**Covers phases:** 28, 29, 30, 31, 32 (absorbed), 32.1, 33
**Based on branch:** `gsd/phase-33-hygiene-and-polish` (HEAD `245fae4d`)
**Working tree:** 26 files modified, 5 new assets untracked (Rebrand WIP — see Section 6)
**Prior integration check:** `.planning/v1.4-INTEGRATION-CHECK.md` (2026-04-18)

---

## Method

Re-walked all 10 seams from the prior check. Verified CLAUDE.md invariants against live source. Traced 6 E2E flows. Checked Wave 3/4 architectural decisions. Ran `npx tsc -b --noEmit` and `node --test tests/**/*.test.mjs` with and without WIP stashed. Identified new orphans and test regressions introduced since 2026-04-18.

---

## 1. Seam-by-Seam Findings

### Seam 1 (CLOSED) — Phase 29 TD-01 / applyStrategyBias regression

**Prior status (2026-04-18):** WARN — `applyStrategyBias` removed from `concept-feed.service.ts`; 6 test failures; 29-VERIFICATION.md stale.

**Current status:** CLOSED by Phase 33 plan 33-02.

**Evidence:**
- `app/tests/services/concept-feed-strategy.test.mjs`: DELETED (Phase 33 D-02). File absent: `ls` returns "No such file."
- `app/src/services/plannerAutoGen.service.ts:115-116`: `computeHints` still wired at this call site (planner path) — unchanged, unregressed.
- `app/src/services/29-VERIFICATION.md`: TD-01 row marked `SUPERSEDED-BY-PHASE-31` (grep confirmed, 1 hit).
- `app/src/services/29-UAT-LOG.md`: TD-01 SUPERSEDED entry appended (grep confirmed).

**Result:** All 6 prior test failures gone. Seam closed.

---

### Seam 2 (CLOSED) — Phase 30 orphan ConceptProgressCard.tsx

**Prior status (2026-04-18):** WARN — `ConceptProgressCard.tsx` (82 lines) dead code; also `post-store.service.ts` and `ImmersiveInfoFlow` export orphaned.

**Current status:** PARTIALLY CLOSED. `ConceptProgressCard.tsx` deleted by Phase 33 plan 33-01. `post-store.service.ts` still present. `ImmersiveInfoFlow` still present in `InfoFlow.tsx`.

**Evidence:**
- `ls app/src/components/ConceptProgressCard.tsx`: "No such file or directory" — DELETED.
- `app/src/services/post-store.service.ts`: still exists; `grep -rn "postStoreService" src/` returns only the definition file. Zero consumers. **Still orphaned.**
- `app/src/components/InfoFlow.tsx:808-815`: `ImmersiveInfoFlow` exported with zero consumers outside `InfoFlow.tsx`. **Still orphaned.** Per prior check, also lacks D-29 video-stop-on-swipe wiring — footgun if ever revived.
- Phase 33 VERIFICATION.md Seam 2 close was scoped to `ConceptProgressCard.tsx` only; `post-store.service.ts` and `ImmersiveInfoFlow` were not in scope.

**Severity:** NOTE (partial closure; 2 orphans remain).

---

### Seam 3 (STILL WIRED) — CONCEPT_EXPLORED handoff integrity

**Status:** WIRED — no change since 2026-04-18.

**Evidence:** `PostDetailScreen.tsx:121` emits `CONCEPT_EXPLORED`; `HomeScreen.tsx:447` subscribes; `dailyReadService.markExplored` wired at call site; three detectors (scroll 70%, 30s dwell, follow-up) in separate useEffects. Additionally, `buildConceptBatch` now filters `exploredIds` (Phase 32.1 gap fix, `concept-feed.service.ts:795`).

---

### Seam 4 (STILL WIRED) — Post queue anchor-ID threading

**Status:** WIRED — no change since 2026-04-18. `sourceQuestionIds: [a.conceptId]` still set at post creation; `getAnchorIdForPost` resolves correctly.

---

### Seam 5 (STILL WIRED) — BottomNavigation vs ScrollToTopFAB z-order

**Status:** WIRED — no conflict. FAB at `zIndex: 40`, BottomNav at `zIndex: 100`; no overlap geometry. `ScrollToTopFAB` rendered only inside `HomeScreen`.

---

### Seam 6 (PARTIALLY OPEN) — Additional orphan exports

**Status:** PARTIAL. `postStoreService` (post-store.service.ts) and `ImmersiveInfoFlow` (InfoFlow.tsx:808-815) remain orphaned (see Seam 2). These were scoped to NOTE severity in the prior check; status unchanged.

---

### Seam 7 (STILL WIRED) — AbortController + locale abort isolation

**Status:** WIRED — no change. `PostDetailScreen.tsx:241-242` subscribes to `LOCALE_CHANGED` and calls `abortController.abort(new DOMException('Locale changed', 'AbortError'))`. Detectors A, B, C in separate useEffects, unaffected.

---

### Seam 8 (STILL WIRED) — SuggestionCard → AskScreen autoSend

**Status:** WIRED — no change. `SuggestionCard.tsx` navigates with `{ state: { autoSend: topic } }`; `AskScreen.tsx:384-385` reads `stateObj?.autoSend`. Guard `lastAutoPrompt` ref still prevents re-fire.

---

### Seam 9 (STILL WIRED) — Fresh-install starter posts

**Status:** WIRED — starter posts strengthened by Phase 32.1 STARTER-PERSIST fix. `concept-feed.service.ts:1504-1506` serves `STARTER_POSTS` when `questions.length === 0` AND now calls `saveCache` on cold-start so navigation back to `/home` re-syncs. `filterDecayedStarters` wired at 3 read-cache sites.

---

### Seam 10 (CLOSED) — LeafState 'dying' dead branch

**Prior status (2026-04-18):** WARN — `concept-feed.service.ts:745` compared `leaf === 'dying'` but `LeafState` had no `'dying'` member; TS2367 type error.

**Current status:** CLOSED by Phase 33 plan 33-03 (TD-06).

**Evidence:**
- `trellis-state.service.ts:9`: `LeafState = 'bud' | 'green' | 'dying' | 'falling' | 'dead' | 'blossom' | 'fruit'` — `'yellow'` renamed to `'dying'`, `'fallen'` renamed to `'dead'`.
- `concept-feed.service.ts:807`: `isImportant = leaf === 'dying' || leaf === 'falling' || leaf === 'dead'` — all three are valid `LeafState` members. No TS2367.
- `npx tsc -b --noEmit`: exits 0 (committed HEAD); 0 errors (confirmed with and without WIP stash).

**Result:** Type-level bug fixed; `yellow`/`fallen` correctly mapped to `dying`/`dead`. Seam closed.

---

### Seam 11 (NEW WARN) — CLASSIFICATION_COMPLETED desync in trellis tests

**Status:** NEW — introduced by Phase 32.1 D-W3-02 (GRAPH_UPDATED unification) without updating the corresponding test files.

**Evidence:**
- `src/services/trellis-actions.service.ts:107,138`: both `replant()` and `unpruneQuestion()` now emit `GRAPH_UPDATED`.
- `tests/services/trellis-replant.test.mjs:137,145`: subscribes to `CLASSIFICATION_COMPLETED` and asserts `events.length === 1`.
- `tests/services/trellis-prune.test.mjs:95,103`: same — subscribes to `CLASSIFICATION_COMPLETED`.
- Both tests fail at runtime: `ERR_MODULE_NOT_FOUND` for `podcast.service` (missing `.ts` extension in node loader resolution) stacks before the event assertion, but the root issue is the test expects the deleted event.
- These tests are additionally broken by the node module resolver not finding `podcast.service` without `.ts` extension — the `_actions-mock-loader.mjs` hooks register correctly but the `npm test` invocation does not use `--experimental-loader`.

**Impact:** 7 trellis-actions tests fail in `npm test`. The production code path (`replant`, `unpruneQuestion`, `prune`, `hardDelete`, `heal`) is functionally correct (emits `GRAPH_UPDATED`), but the test suite is out-of-sync with the event consolidation. Trellis actions are untested as a result.

**Severity:** WARN — tests are broken, production wiring is correct. Fix: update test subscriptions to `GRAPH_UPDATED` and fix the node loader invocation (add `--import` or `--experimental-loader` flag to npm test script, or inline the loader setup within each test).

---

### Seam 12 (NEW WARN) — HomeScreen image-pregen test guards dead code path

**Status:** NEW — introduced by Phase 33 / WIP architectural change.

**Evidence:**
- `tests/screens/HomeScreen.image-pregen-filter.test.mjs` asserts `handleLoad` in `HomeScreen.tsx` contains `newPosts.filter(... presentationStyle === 'image')` and `Promise.allSettled(imagePosts.map...)`.
- The WIP commit (in working tree, not yet committed) moved image pre-generation from `handleLoad` to `refillQueue` in `concept-feed.service.ts:1388-1416`. `handleLoad` no longer does per-post image pre-generation.
- `concept-feed.service.ts:1388`: `const imagePosts = posts.filter((p) => p.presentationStyle === 'image')` — filtering EXISTS, but in `refillQueue`, not `handleLoad`.
- Test fails on the committed HEAD too? — **NO.** Stash test shows `449/27` on committed HEAD (pass). The WIP changes add 1 new failure. So this is a WIP-introduced regression not yet committed.

**Impact:** Test guards the correct invariant (filter before pre-gen) but guards it at the wrong call site. The invariant is preserved in production code at `refillQueue` (correct); the test will fail after WIP commit. Must be updated to guard `refillQueue` instead of `handleLoad`.

**Severity:** WARN — test will break on WIP commit. Not a production regression.

---

## 2. Flow Walkthroughs

### Flow 1 — Fresh install starter posts

**Status:** COMPLETE (strengthened since 2026-04-18).

Phase 32.1 added `saveCache` on cold-start + `filterDecayedStarters` at 4 read sites. `concept-feed.service.ts:1504-1506` saves `STARTER_POSTS` to cache on first call; subsequent `/home` navigations re-sync from cache. Starter posts decay only after user has 3+ organic posts. Unit tests: 9/9 green (committed HEAD).

---

### Flow 2 — User reads post → CONCEPT_EXPLORED → VineProgress grows

**Status:** COMPLETE.

`PostDetailScreen.tsx:120-121` emits `CONCEPT_EXPLORED` idempotently. `HomeScreen.tsx:447` subscriber calls `setExploredAnchors(dailyReadService.getExploredAnchors())`. `buildConceptBatch:795` filters out explored anchors so they don't re-enter the generation cycle. Generation cap now gated by `allExplored` (`concept-feed.service.ts:1265-1267`) so it doesn't fire while vine is unfinished.

---

### Flow 3 — Locale switch aborts essay but preserves reading detection

**Status:** COMPLETE.

`PostDetailScreen.tsx:241-242`: `LOCALE_CHANGED` → `abortController.abort(AbortError)`. Essay stream halts silently. Detectors A/B/C in separate useEffects, unaffected. `CONCEPT_EXPLORED` still fires on 30s dwell or scroll 70%.

---

### Flow 4 — Pull-up load more (4 per swipe, refill on depletion)

**Status:** COMPLETE (architecture evolved post-2026-04-18).

The WIP working tree advances this further: `REFILL_THRESHOLD` bumped from 8 to 12 (`post-queue.service.ts`) to account for image pre-gen time moved into `refillQueue`. Queue now interleaved at enqueue time (new `enqueueInterleaved` method in WIP) to prevent style clustering. On committed HEAD: `limit=4` passed from `HomeScreen` to `infiniteScrollService.loadNextBatch`; `refillQueue` called when queue drops below threshold.

---

### Flow 5 — SuggestionCard topic tap → AskScreen auto-send

**Status:** COMPLETE.

`SuggestionCard.tsx` → `navigate('/ask', { state: { autoSend: topic } })`. `AskScreen.tsx:384-385` reads both `state.prompt` and `state.autoSend`. `lastAutoPrompt` ref guards re-fire. Nav state cleared via `window.history.replaceState({}, '')`.

---

### Flow 6 — Video playback + swipe-away stop

**Status:** COMPLETE.

`InfoFlow.tsx` overlays use `pointerEvents: 'none'` + `aria-hidden="true"` (Phase 32.1 G2 fix). `wouldRenderVisual` check (D-W4-03) prevents empty video cards. `SwipeTabContext` listener stops video on swipe-away. Device validation pending per 32.1-HUMAN-UAT.

---

## 3. CLAUDE.md Invariant Verification

### Invariant 1 — Concept Feed 3-list pipeline

**Status: PASS**

`buildConceptBatch` (daily list from SM-2 due anchors) → derived list (multi-entry per concept, weighted) → queue (cyclic, `REFILL_THRESHOLD=12` in WIP / 8 in committed HEAD). `generateMorePosts` serves 4 per swipe via `loadNextBatch(..., 4)` in `HomeScreen:156`. CLAUDE.md section updated at commit `98692338`.

**Gap vs design:** Derived list is still rebuilt per refill (not append-only with cycle position). Known divergence, documented in CLAUDE.md. Not a regression from prior check.

---

### Invariant 2 — Header portal pattern

**Status: PASS**

`src/components/ui/Header.tsx:154-155`: `const insideSwipeTab = useContext(SwipeTabContext) !== null; return insideSwipeTab ? headerNode : createPortal(headerNode, document.body)`. Pattern intact. No `transform`/`will-change`/`filter`/`contain` on ancestors observed in WIP diffs.

---

### Invariant 3 — ChatInput minWidth: 0

**Status: PASS**

`src/components/ChatInput.tsx:169`: `minWidth: 0` on `<input type="text">` alongside `flex: 1`. Comment at `ChatInput.tsx:162` calls it load-bearing. `tests/components/ChatInput.flex-shrink.test.mjs` enforces this with source-reading assertion.

---

### Invariant 4 — Root overflow hidden (both axes)

**Status: PASS**

`app/src/index.css:293`: `overflow: hidden` on `html, body`. `app/src/App.tsx:126`: `overflowX: 'hidden'` on App root div. `SwipeTabContainer.tsx:104`: `if (root && root.scrollLeft !== 0) root.scrollLeft = 0` on `focusout`. `tests/layout/root-horizontal-clip.test.mjs` enforces all three layers.

---

### Invariant 5 — SwipeTabContainer resize guard

**Status: PASS**

`SwipeTabContainer.tsx:132`: `if (newWidth === screenWidthRef.current) return;` early-return preserves height-only no-op. `focusout` re-snap wired at `:91-108`. `tests/components/SwipeTabContainer.resize-guard.test.mjs` enforces the guard.

---

### Invariant 6 — Single GRAPH_UPDATED signal (no CLASSIFICATION_COMPLETED)

**Status: PASS (code); WARN (tests)**

Production code: `src/types/index.ts:696` defines `GRAPH_UPDATED` only. `CLASSIFICATION_COMPLETED` appears only as a comment at `:694` (tombstone). All emitters use `GRAPH_UPDATED`: `canonical-knowledge.service.ts:974`, `question.service.ts:514`, `trellis-actions.service.ts:107,138`. All subscribers use `GRAPH_UPDATED`: `useQuestions.ts:71`, `useTrellisData.ts:34`, `GraphScreen.tsx:486`, `PrunedSection.tsx:25`.

**Test desync (Seam 11):** `trellis-replant.test.mjs` and `trellis-prune.test.mjs` still assert `CLASSIFICATION_COMPLETED` event. These 7 tests fail. The invariant HOLDS in production code but the tests are stale.

---

### Invariant 7 — News two-phase content (bodyMarkdown empty at creation)

**Status: PASS**

`concept-feed.service.ts:1106,1151,1198`: `bodyMarkdown: ''` at all three news-post creation branches (confirmed). `post-essay.service.ts`: `generateNewsEssay` streams on-open. Test `tests/services/post-essay.service.test.mjs` enforces the invariant. No `chatCompletion`/`chatStream` called eagerly in news creation block (grep confirmed).

---

### Invariant 8 — normalizeAnchorName guard

**Status: PASS**

`canonical-knowledge.service.ts:769`: `normalizeAnchorName()` defined. Called at `:800` in `commitClassificationResult` (normalizes `result.anchorName`). Called at `:880` inline comparison: `(normalizeAnchorName(q.title || '') || ...).toLowerCase() === targetNormalized`. Both sides normalized. CLAUDE.md "Anchor name normalization" section documents this.

---

### Invariant 9 — Classification dedup pre-check (embedding cosine)

**Status: PASS**

`canonical-knowledge.service.ts:42`: `ANCHOR_PRE_CHECK_SIMILARITY_THRESHOLD = 0.82` (within 0.75-0.95 band). Pre-check at `:706-741` runs BEFORE `buildStepPrompt('branch')` — cosine scan of all anchors occurs before tree descent. Backfill up to `ANCHOR_BACKFILL_PER_CLASSIFICATION` (8) per call. Case-insensitive NEW coercion at step 1/step 2 guards weak-model behavior. `tests/services/classification-dedup.test.mjs` enforces the pre-check ordering.

---

## 4. Wave 3/4 Architectural Decision Verification

### D-W3-01 — Header portal pattern

**Status: INTACT**

`Header.tsx:154-155` auto-portals based on `SwipeTabContext`. Operator-confirmed pass on device (2026-04-19). No regression in working tree diffs.

---

### D-W3-02 — Single GRAPH_UPDATED signal

**Status: INTACT (production); TEST DESYNC (see Seam 11)**

All production code paths use `GRAPH_UPDATED`. CLAUDE.md "Event bus" section documents. `CLASSIFICATION_COMPLETED` removed from `AppEvent` union.

---

### D-W3-03 — News two-phase content

**Status: INTACT**

Three creation branches in `concept-feed.service.ts` all set `bodyMarkdown: ''`. Streamer in `post-essay.service.ts` generates on-open. Test enforces invariant on live code path.

---

### D-W3-04 — normalizeAnchorName dual-layer guard

**Status: INTACT**

Prompt-side: `buildStepPrompt('anchor', ...)` adds GOOD/BAD examples. Post-LLM: `normalizeAnchorName()` in `commitClassificationResult`. Both sides of anchor comparison normalized.

---

### D-W3-05 — 3-list pipeline

**Status: INTACT (with documented known divergences)**

Daily list (SM-2 anchors) → derived list (multi-entry, `buildConceptBatch`) → queue (`postQueueService`, `REFILL_THRESHOLD`). Pipeline documented in CLAUDE.md. Known divergences: derived list still rebuilt per refill (not append-only), cycle position not formally tracked. These are pre-existing gaps, not regressions.

---

### D-W4-01 — Cycle-stamped post IDs

**Status: INTACT**

`concept-feed.service.ts:1080`: `cycleNumber = postQueueService.getCycleNumber()` used throughout. The WIP working tree comment at `:1074` notes "Bug A's cycleStamp is obsolete under the new scheme" — the scheme has evolved to rely on `getCycleNumber()` for query variation rather than ID stamping, but the cycleNumber is still central to dedup-resistant ID generation.

---

### D-W4-03 — wouldRenderVisual defense net

**Status: INTACT**

`InfoFlow.tsx:159-166`: `wouldRenderVisual` exhaustive check; `effectivePresentationStyle = 'text-art'` fallback when false; `console.warn` in DEV mode. 4 references in `InfoFlow.tsx` (definition + 2 uses + comment). WIP diffs do not touch this block.

---

### D-W4-04 — Persisted video dedup

**Status: INTACT**

`concept-feed-dedup.ts:56-80`: `maybeBackfillFromHistory()` reads `echolearn_post_history` on first session access. `_backfilledFromHistory` flag prevents repeat reads. `typeof localStorage` guard preserves node-test compatibility.

---

### D-W4-05 — Query variation for YouTube

**Status: INTACT (strengthened in WIP)**

`concept-feed.service.ts:822-838`: `VIDEO_QUERY_MODIFIERS` (5 variants) + `SHORT_QUERY_MODIFIERS` (4 variants). `buildYoutubeQuery(conceptName, cycleNumber, isShort)` keyed by `cycleNumber % modifiers.length`. WIP diffs show this unchanged from committed HEAD.

---

### D-W4-07 — No lazy hardcoded names from LLM pipelines

**Status: INTACT**

`canonical-knowledge.service.ts:548-589`: `buildNewBranchClusterAnchorPrompt` + `parseClusterAnchorResponse` rejects placeholder regex `/^(fundamentals|basics|introduction|general|concepts|overview)$/i`. Falls back to `classifyAndAnchor` legacy path on placeholder detection.

---

## 5. New Orphan Exports (since 2026-04-18)

No new orphan exports were introduced by Phase 32.1 or Phase 33 committed code. The two pre-existing orphans remain:

| Export | File | Consumers | Status |
|--------|------|-----------|--------|
| `postStoreService` | `src/services/post-store.service.ts` | 0 | Pre-existing orphan (since Phase 31); not cleaned up in Phase 33 |
| `ImmersiveInfoFlow` | `src/components/InfoFlow.tsx:808-815` | 0 | Pre-existing orphan (since Phase 31); additionally lacks D-29 video-stop-on-swipe wiring |

The WIP working tree modifies `InfoFlow.tsx` but does not add consumers for `ImmersiveInfoFlow`.

---

## 6. Working Tree Analysis

**Branch:** `gsd/phase-33-hygiene-and-polish`
**WIP status:** 26 files modified, 5 new PWA/icon assets + 1 planning doc untracked. This is a **documented rebrand WIP** — not stale experimentation.

**Evidence:** `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-REBRAND.md` (untracked) documents the full scope of the "EchoLearn → Trellis" rebrand. Checklist shows all items complete; localStorage key migration deferred post-presentation.

**Files and their rebrand role:**

| File | Change type | Rebrand-related? |
|------|------------|-----------------|
| `CLAUDE.md` | `[Trellis]` console prefix in comments | Yes |
| `app/capacitor.config.ts` | `appId → com.trellis.app`, `appName → Trellis` | Yes |
| `app/index.html` | `<title>Trellis</title>`, favicon links | Yes |
| `app/ios/App/App/Assets.xcassets/AppIcon-512@2x.png` | New Trellis icon | Yes |
| `app/ios/App/App/Info.plist` | `CFBundleDisplayName → Trellis` | Yes |
| `app/src/locales/en.json + zh + es + ja` | "EchoLearn" → "Trellis" strings | Yes |
| `app/src/components/InfoFlow.tsx` | `div role="button"` refactor + rebrand | Mixed |
| `app/src/screens/GraphScreen.tsx` | Theme name `'Trellis'` | Yes |
| `app/src/screens/HomeScreen.tsx` | Image pregen moved to refillQueue + misc | **Functional change** |
| `app/src/screens/ReviewScreen.tsx` | Flashcard dedup two-pass | **Functional change** |
| `app/src/screens/settings/SettingsDataScreen.tsx` | Minor update | Yes |
| `app/src/services/canonical-knowledge.service.ts` | Console prefix + misc | Mixed |
| `app/src/services/concept-feed.service.ts` | Truncation-tolerant JSON parser + image pre-gen moved to refillQueue + rebrand | **Major functional change** |
| `app/src/services/imageGeneration.service.ts` | In-flight dedupe | **Functional change** |
| `app/src/services/podcast.service.ts` | Console prefix | Yes |
| `app/src/services/post-queue.service.ts` | REFILL_THRESHOLD 8→12, enqueueInterleaved | **Functional change** |
| `app/src/services/question-filter.service.ts` | Console prefix | Yes |
| `app/src/services/question.service.ts` | Console prefix | Yes |
| `app/src/services/scheduler.native.ts` | Console prefix | Yes |
| `app/src/services/style-assignment.ts` | Text-art 40%→55%, news 20%→10% | **Functional change** |
| `app/src/state/useQuestions.ts` | Console prefix | Yes |
| `app/tests/services/image-gen-key-gate.test.mjs` | Test update | Yes |
| `app/tests/services/post-queue.test.mjs` | Test update for REFILL_THRESHOLD=12 | Functional |
| `app/tests/services/style-assignment.test.mjs` | Test update for new weights | Functional |

**Assessment:** WIP is intentional and documented. The functional changes (image pre-gen architectural shift, truncation-tolerant JSON parser, flashcard dedup, style weight re-balance, in-flight image dedupe, REFILL_THRESHOLD bump, enqueueInterleaved) are significant and correct per `33-REBRAND.md` verification claim (449/27 on WIP; confirmed). However, one test (`HomeScreen.image-pregen-filter.test.mjs`) is now broken by the WIP because it guards the old `handleLoad` code path that no longer contains the image filtering logic (moved to `refillQueue`).

**Outstanding concern:** The WIP includes substantial functional changes beyond the rebrand scope (truncation-tolerant parser, ReviewScreen dedup, style weights, image gen in-flight dedupe) that lack Phase 33 plan coverage. These are improvements but they are unplanned relative to the committed Phase 33 scope. The `33-REBRAND.md` verification section attests 449/27 on WIP — which is 1 failure worse than committed HEAD (449/27 vs 448/28) due to the broken `HomeScreen.image-pregen-filter.test.mjs`.

Wait — `33-REBRAND.md` claims "449 pass / 27 fail" but the stash test of committed HEAD also shows 449/27. The WIP `npm test` run (without glob) shows 380/28. The glob run shows 448/28. The WIP introduces 1 new failure. There is a discrepancy in `33-REBRAND.md`'s claim of 449/27 on WIP. Likely the rebrand doc was written before the `HomeScreen.image-pregen-filter.test.mjs` regression was introduced (the image pre-gen move to `refillQueue` is a post-rebrand-doc change in the same uncommitted batch).

---

## 7. Test Suite Signal

### Committed HEAD (stash test — clean tree)

- `node --test tests/**/*.test.mjs`: **449 pass / 27 fail**
- `npx tsc -b --noEmit`: **0 errors**
- Matches Phase 33 VERIFICATION.md baseline exactly.

### Working Tree (with WIP)

- `node --test tests/**/*.test.mjs`: **448 pass / 28 fail**
- `npx tsc -b --noEmit`: **0 errors**
- Delta vs committed HEAD: +1 failure (`HomeScreen.image-pregen-filter.test.mjs`)

### Failure breakdown (WIP tree, 28 total)

| Cause | Count | Category |
|-------|-------|----------|
| `ERR_IMPORT_ATTRIBUTE_MISSING` (Node 25 JSON import, pre-v1.4, documented in STATE.md) | ~20 | Pre-existing |
| `TrellisTooltip.tsx` missing (deleted Phase 25 `6e12ab2c`, test `eae1496f` written anyway) | 3 | Pre-existing (Phase 26) |
| `getVineColor` returns hex not CSS vars (changed Phase 25, test expects CSS vars) | 1 | Pre-existing (Phase 26) |
| `CLASSIFICATION_COMPLETED` event expected by `trellis-replant.test.mjs` and `trellis-prune.test.mjs` (unification in Phase 32.1 D-W3-02 not reflected in tests) | 2 | NEW since last integration check |
| `HomeScreen.image-pregen-filter.test.mjs` (guards `handleLoad` pattern moved to `refillQueue` in WIP) | 1 | WIP-introduced; not yet committed |
| `ERR_MODULE_NOT_FOUND` for `podcast.service` (node resolver doesn't auto-add `.ts`, _actions-mock-loader.mjs hooks not loaded in npm test script) | 2 | Pre-existing test runner config gap |

**Net new failures vs 2026-04-18 integration check:** The old check showed 8 v1.4-specific failures (all from the Phase 29 TD-01 regression). Those 8 are now GONE (Phase 33 deleted the tests). In their place: 2 new failures from the CLASSIFICATION_COMPLETED→GRAPH_UPDATED event consolidation test desync (Seam 11). Net change: −6 test failures from prior integration check (improvement), +2 new test failures (Seam 11).

---

## 8. Phase 32 Absorbed Phase Assessment

Phase 32 (`32-v1-4-verification-close-out-and-uat-retest`) was planned but never executed. Directory contains: `32-01-PLAN.md`, `32-02-PLAN.md`, `32-03-PLAN.md`, `32-CONTEXT.md`, `32-DISCUSSION-LOG.md`, `32-RESEARCH.md`, `32-VALIDATION.md`. Zero SUMMARY files exist.

The three Phase 32 plans were:
- `32-01-PLAN.md`: Write `30-VERIFICATION.md`
- `32-02-PLAN.md`: Write `31-VERIFICATION.md` (pending UAT retest)
- `32-03-PLAN.md`: UAT device retest

**Phase 32.1 absorbed Phase 32's UAT retest intent** (plans 32.1-01 through 32.1-05 closed UAT-31-2/4/13/14 + STARTER-PERSIST + CLEAR-RELOAD). **Phase 32.1 did NOT produce 30-VERIFICATION.md or 31-VERIFICATION.md.** Phase 33 also did not produce these. They remain missing.

**Impact:** Two orphaned absorbed plans — the requirement to document Phase 30 and Phase 31 decisions in VERIFICATION.md format was carried through Phase 32 → Phase 32.1 → Phase 33 and is still open at close-out.

---

## 9. Requirements Integration Map

Phases 28-33 use decision IDs (D-xx, UAT-IDs, TD-IDs) as requirement surface. v1.1 REQ-IDs are not part of this milestone.

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| 28-D-06 BottomNav slide-down | BottomNavigation `motion.nav` + App.tsx `isTopLevelScreen` | WIRED | — |
| 28-D-26 CSS spacing tokens | `index.css :root` → screens via `var(--...)` | WIRED | — |
| 29-TD-01 curiosity signals | `plannerAutoGen.service.ts:115-116` only; concept-feed branch superseded | SUPERSEDED | Supersession documented in 29-VERIFICATION.md per Phase 33 |
| 29-TD-02 AbortController | `PostDetailScreen.tsx` scoped `AbortController` | WIRED | — |
| 29-TD-03 signal propagation | `useQuestions` → `classifyAndAnchorIncremental` | WIRED | — |
| 30-D-04 reading detectors | `PostDetailScreen.tsx` 3 separate useEffects | WIRED | — |
| 30-D-05 CONCEPT_EXPLORED event | `PostDetailScreen` emit → `HomeScreen` subscribe | WIRED | — |
| 30-D-07..D-10 card → bar transforms | SUPERSEDED by 31-D-01 (VineProgress) | SUPERSEDED | `ConceptProgressCard.tsx` deleted by Phase 33 |
| 30-D-14 +1 credit on completion | `HomeScreen:462` `trellisCreditsService.add(1)` | WIRED | — |
| 30-VERIFICATION.md | Planned by Phase 32 `32-01-PLAN.md` | MISSING | Never executed; absorbed but undelivered |
| 31-D-01 VineProgress | `HomeScreen` imports + renders; `ResizeObserver` viewBox fix | WIRED | — |
| 31-D-10 queue | `postQueueService` + `REFILL_THRESHOLD` | WIRED | Threshold bumped 8→12 in WIP |
| 31-D-11 auto-refill | `generateMorePosts` → `refillQueue` | WIRED | Image pre-gen moved into `refillQueue` in WIP |
| 31-D-14 important = 2 posts | `buildConceptBatch:807` | WIRED | `'dying'` now valid `LeafState` post-TD-06 rename |
| 31-D-23/D-25 suggestion → autoSend | `SuggestionCard` → `AskScreen` `state.autoSend` | WIRED | — |
| 31-D-28/D-29 video play/stop | `InfoFlow.tsx` + SwipeTabContext listener | WIRED | pointer-events:none fix (G2 pending device) |
| 31-D-33/D-34 retention | `postHistoryService.purgeExpired` + Settings | WIRED | — |
| 31-D-43 starter posts | `STARTER_POSTS` + `saveCache` on cold-start | WIRED | — |
| 31-VERIFICATION.md | Planned by Phase 32 `32-02-PLAN.md` | MISSING | Never executed; absorbed but undelivered |
| TD-04 (Phase 29 regression) | concept-feed-strategy.test.mjs deleted; 29-VERIFICATION.md updated | CLOSED | Phase 33 plan 33-02 |
| TD-05 (orphaned ConceptProgressCard) | ConceptProgressCard.tsx deleted; 4 i18n keys removed | PARTIALLY CLOSED | `post-store.service.ts` + `ImmersiveInfoFlow` remain |
| TD-06 (LeafState 'dying' type error) | LeafState `yellow→dying`, `fallen→dead`; tsc 0 errors | CLOSED | Phase 33 plan 33-03 |
| TSC-HYGIENE | `npx tsc -b --noEmit` exits 0 | CLOSED | Phase 33 plan 33-04 |
| PERF-MEMO | `ConceptCard` + `VineProgress` `React.memo`; settings hoisted | CLOSED | Phase 33 plan 33-06 |
| COSMETIC-POLISH | 44px touch targets; spacing/shadow tokens | CLOSED | Phase 33 plan 33-07 |
| STARTER-PERSIST | `saveCache` cold-start + `filterDecayedStarters` | CLOSED (code); device pending | Phase 32.1 plan 32.1-04 |
| CLEAR-RELOAD | `window.location.assign('/home')` at SettingsDataScreen:68 | CLOSED (code); device pending | Phase 32.1 plan 32.1-05 |
| UAT-31-2 | Cross-cycle YouTube dedup via `concept-feed-dedup.ts` | CLOSED | Phase 32.1 plan 32.1-02 |
| UAT-31-4 | Video overlay `pointer-events: none` | CLOSED (code); device pending | Phase 32.1 plan 32.1-03 |
| UAT-31-13 | Cap formula floor `Math.max(dueConcepts, 3)` | CLOSED | Phase 32.1 plan 32.1-02 |
| UAT-31-14 | Starter posts cold-start (Vite base fix) | CLOSED | Phase 32.1 plan 32.1-01 |
| D-W3-01 Header portal | `Header.tsx` auto-portal by `SwipeTabContext` | INTACT | — |
| D-W3-02 GRAPH_UPDATED single signal | All emitters/subscribers consolidated | INTACT (code); TEST DESYNC | `trellis-replant/prune.test.mjs` still assert `CLASSIFICATION_COMPLETED` |
| D-W3-03 News two-phase | `bodyMarkdown: ''` at creation; stream on open | INTACT | — |
| D-W3-04 normalizeAnchorName | Dual-layer guard in `commitClassificationResult` | INTACT | — |
| D-W3-05 3-list pipeline | Daily→Derived→Queue documented in CLAUDE.md | INTACT | Known divergences (append-only, cycle position) unchanged |
| D-W4-01 Cycle-stamped IDs | `getCycleNumber()` threads through post ID generation | INTACT | — |
| D-W4-03 wouldRenderVisual | Exhaustive check + text-art fallback in `InfoFlow.tsx` | INTACT | — |
| D-W4-04 Persisted dedup | `maybeBackfillFromHistory()` in `concept-feed-dedup.ts` | INTACT | — |
| D-W4-05 Query variation | `buildYoutubeQuery` cycle-keyed modifiers | INTACT | — |
| D-W4-07 Anti-placeholder | `parseClusterAnchorResponse` rejects placeholders | INTACT | — |

**Requirements with no cross-phase wiring (self-contained):** 28-D-01..D-30 visual polish (single-file changes). 31-D-33/D-34/D-38/D-41 settings-only. These are not gaps.

---

## Summary

### Blockers

None.

### Warnings (6)

1. **Seam 11 / CLASSIFICATION_COMPLETED test desync** — `trellis-replant.test.mjs` and `trellis-prune.test.mjs` subscribe to the deleted `CLASSIFICATION_COMPLETED` event. 7 trellis-actions tests fail (including heal/prune/replant/hardDelete/unpruneQuestion). The tests also fail to load because the npm test script doesn't invoke the `_actions-mock-loader.mjs` node hooks. Fix: (a) update event subscriptions in both test files from `CLASSIFICATION_COMPLETED` to `GRAPH_UPDATED`; (b) add `--import ./_actions-mock-loader.mjs` to the test invocation or convert to inline mocking.

2. **Seam 12 / HomeScreen image-pregen test guards dead code** — `HomeScreen.image-pregen-filter.test.mjs` asserts `handleLoad` contains `imagePosts.filter`. The WIP moved this filtering to `refillQueue`. Test passes on committed HEAD; will fail after WIP commit. Fix: update test to assert `refillQueue` in `concept-feed.service.ts` contains the `imagePosts.filter(... presentationStyle === 'image')` pattern.

3. **30-VERIFICATION.md missing** — Phase 30 (curiosity-feed progress bar) was never formally verified. Phase 32 planned this as `32-01-PLAN.md`; never executed. Phase 32.1 and Phase 33 did not fill the gap. 9 Phase 30 decisions (D-01/D-02/D-03/D-05/D-06/D-12/D-19/D-21/D-22) have no VERIFICATION record. Many are now superseded by Phase 31, but the supersession is undocumented.

4. **31-VERIFICATION.md missing** — Same situation for Phase 31. Phase 32's `32-02-PLAN.md` planned this; never executed. 19 of 47 Phase 31 decisions remain without a formal verification record.

5. **Seam 2 partial closure** — `post-store.service.ts` and `ImmersiveInfoFlow` export remain orphaned. `ImmersiveInfoFlow` is a footgun for future revival because it lacks D-29 video-stop-on-swipe wiring. Phase 33 cleaned up `ConceptProgressCard.tsx` but left these two. Low-urgency but adds dead code surface.

6. **3 pending device retests (32.1 human_needed)** — G2 (video overlay on Android), G4 (starter-post persistence on device), G5 (Clear All Data auto-navigation). These are defined acceptance gates per 32.1 CONTEXT D-06/D-20, not code bugs, but they block the 32.1 status from `human_needed` → `passed`. Pending next APK deploy cycle.

### Notes (7)

1. **WIP is intentional rebrand** — 26 modified files are the "EchoLearn → Trellis" rebrand plus follow-on functional improvements (truncation-tolerant JSON parser, ReviewScreen dedup, style weights, image gen in-flight dedupe). `33-REBRAND.md` documents completion checklist. Commit is pending presentation.

2. **Committed HEAD baseline is clean** — `449 pass / 27 fail`, `tsc=0`, matches Phase 33 VERIFICATION.md exactly. WIP introduces 1 additional test failure.

3. **Pre-existing trellis test failures unchanged** — 20+ failures from `ERR_IMPORT_ATTRIBUTE_MISSING` (Node 25 JSON import, pre-v1.4, documented in STATE.md) remain. Not regressions.

4. **Pre-existing Phase 26 test-code bugs** — `trellis-tooltip-copy.test.mjs` tests `TrellisTooltip.tsx` deleted in Phase 25 (`6e12ab2c`). `trellis-layout.test.mjs` tests `getVineColor` against CSS vars but implementation uses hex since Phase 25. Both fail since Phase 26 test introduction. Not v1.4 regressions.

5. **Rebrand localStorage key migration deferred** — All 48 `echolearn_` localStorage key references preserved intentionally. Migration plan documented in `33-REBRAND.md`. Existing users will have uninterrupted data access; key migration is a separate post-presentation task.

6. **PERF-MEMO device validation pending** — React.memo correctness on `ConceptCard` and `VineProgress` requires runtime observation per Phase 33 VERIFICATION.md. Not a blocking risk; listed for completeness.

7. **VineProgress props dead code** — `VineProgress` accepts `explored`, `total`, `isComplete` props (present in Phase 31 VERIFICATION concern) but Phase 33 VERIFICATION.md Seam 2 note mentions these were not explicitly cleaned up. The component uses `concepts` prop for internal counting. Misleading but not broken.

---

_Checked: 2026-04-24_
_Checker: Claude (integration-checker)_
