---
phase: 34-v1-4-close-out-verification-debt-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/services/trellis-replant.test.mjs
  - app/tests/services/trellis-prune.test.mjs
autonomous: true
requirements:
  - SEAM-11
must_haves:
  truths:
    - "trellis-replant.test.mjs test 6 subscribes to GRAPH_UPDATED and asserts events.length === 1"
    - "trellis-prune.test.mjs test 5 subscribes to GRAPH_UPDATED and asserts events.length === 1"
    - "Neither test references CLASSIFICATION_COMPLETED in the assertion path"
    - "Both test files run under default `npm test` invocation (no --loader / --import flag) and the previously-failing event tests now pass"
    - "Production code in trellis-actions.service.ts is NOT modified (D-04 scope guard)"
  artifacts:
    - path: "app/tests/services/trellis-replant.test.mjs"
      provides: "GRAPH_UPDATED subscription + Phase 32.1 D-W3-02 history comment"
      contains: "GRAPH_UPDATED"
    - path: "app/tests/services/trellis-prune.test.mjs"
      provides: "GRAPH_UPDATED subscription + Phase 32.1 D-W3-02 history comment"
      contains: "GRAPH_UPDATED"
  key_links:
    - from: "trellis-replant.test.mjs:eventBus.subscribe"
      to: "trellis-actions.service.ts:107 eventBus.emit"
      via: "shared event-bus.ts ESM module instance"
      pattern: "eventBus\\.subscribe\\('GRAPH_UPDATED'"
    - from: "trellis-prune.test.mjs:eventBus.subscribe"
      to: "trellis-actions.service.ts:138 eventBus.emit"
      via: "shared event-bus.ts ESM module instance"
      pattern: "eventBus\\.subscribe\\('GRAPH_UPDATED'"
---

<objective>
Close SEAM-11: rename `CLASSIFICATION_COMPLETED` to `GRAPH_UPDATED` in two trellis-actions test files and remove the now-stale payload assertion. Per 34-RESEARCH.md Q1 the fix is **4 surgical lines per file**, NOT the full inline-mock rewrite originally scoped in 34-CONTEXT.md D-01. Research showed the loader gap is a separate failure class and the event tests pass after the rename alone — the existing `_actions-mock-question.mjs` import already works.

Purpose: Restore the 2 failing trellis-actions event tests so the test baseline counts are accurate going into the WIP commit set. Production code at `trellis-actions.service.ts:107,138` correctly emits `GRAPH_UPDATED` (no payload) per Phase 32.1 Wave 3 D-W3-02 — these tests just need to catch up to that consolidation.

Output: Two patched `.mjs` test files; both event tests pass; tsc unaffected (no production code change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-CONTEXT.md
@.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md
@.planning/v1.4-INTEGRATION-CHECK-v2.md

<interfaces>
<!-- Production emit sites already correct — DO NOT TOUCH -->

From app/src/services/trellis-actions.service.ts (lines 107, 138):
```ts
// replant() (line ~107):
eventBus.emit({ type: 'GRAPH_UPDATED' });
// unpruneQuestion() (line ~138):
eventBus.emit({ type: 'GRAPH_UPDATED' });
```

From app/src/types/index.ts (lines 694-696):
```ts
// AppEvent union: GRAPH_UPDATED is present; CLASSIFICATION_COMPLETED is tombstoned.
// Tombstone comment exists explaining the Phase 32.1 D-W3-02 consolidation.
```

Current broken test sites (to patch):

```
app/tests/services/trellis-replant.test.mjs:
  136: // D-14 (voided): replant emits CLASSIFICATION_COMPLETED so useTrellisData recomputes
  137: test('replant emits CLASSIFICATION_COMPLETED event', async () => {
  145:   const unsub = eventBus.subscribe('CLASSIFICATION_COMPLETED', (e) => events.push(e));
  151:   assert.equal(events.length, 1, 'must emit exactly one CLASSIFICATION_COMPLETED');

app/tests/services/trellis-prune.test.mjs:
   94: // D-18: unpruneQuestion clears both flags and emits CLASSIFICATION_COMPLETED
   95: test('unpruneQuestion clears flagged and prunedFromTrellis and emits CLASSIFICATION_COMPLETED', async () => {
  103:   const unsub = eventBus.subscribe('CLASSIFICATION_COMPLETED', (e) => events.push(e));
  112:   assert.equal(events.length, 1, 'must emit CLASSIFICATION_COMPLETED');
```
</interfaces>

# Phase 34 D-01 reframing (READ THIS BEFORE TOUCHING TESTS)

34-CONTEXT.md D-01 says "inline mocks per test, drop loader entirely (~80-150 lines per file)". 34-RESEARCH.md Q1 (verified by running Node 25 + reading test source) found that:

1. The `_actions-mock-question.mjs` import already works without `--loader` because the test imports it directly.
2. The `_actions-mock-loader.mjs` failure mode causes a separate `ERR_MODULE_NOT_FOUND` for `podcast.service` — but the replant/prune/unpruneQuestion tests do NOT call `heal()`, so they don't actually need `podcastService` mocked.
3. The two CLASSIFICATION_COMPLETED test failures are a DISTINCT issue from the loader gap — they fail because the test subscribes to a tombstoned event.

**Honor D-01's intent (less surface area, more isolation) by making the minimum change that closes SEAM-11.** Do NOT rewrite tests with `t.mock.method()` or any new mocking surface. Do NOT delete `_actions-mock-loader.mjs` or sibling mock files in this plan — that's tangential cleanup deferred to v1.5 per 34-CONTEXT.md `<deferred>`. Document the reframing in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename event in trellis-replant.test.mjs and remove payload assertion</name>
  <files>app/tests/services/trellis-replant.test.mjs</files>
  <read_first>
    - app/tests/services/trellis-replant.test.mjs (read entire file; the test starts at line 137)
    - app/src/services/trellis-actions.service.ts (lines 100-115 — confirm GRAPH_UPDATED emit has no payload)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q1 + "Pitfall 1: GRAPH_UPDATED Payload Mismatch")
  </read_first>
  <action>
    Make exactly these textual changes to `app/tests/services/trellis-replant.test.mjs`:

    1. **Line 136** (comment above test) — replace:
       `// D-14 (voided): replant emits CLASSIFICATION_COMPLETED so useTrellisData recomputes`
       with:
       `// D-14 (voided) — Phase 32.1 D-W3-02 consolidated CLASSIFICATION_COMPLETED into GRAPH_UPDATED. Tests now assert the consolidated event with no payload.`

    2. **Line 137** (test name) — replace:
       `test('replant emits CLASSIFICATION_COMPLETED event', async () => {`
       with:
       `test('replant emits GRAPH_UPDATED event (Phase 32.1 D-W3-02 rename)', async () => {`

    3. **Line 145** (subscription) — replace:
       `const unsub = eventBus.subscribe('CLASSIFICATION_COMPLETED', (e) => events.push(e));`
       with:
       `const unsub = eventBus.subscribe('GRAPH_UPDATED', (e) => events.push(e));`

    4. **Line 151** (assertion) — replace:
       `assert.equal(events.length, 1, 'must emit exactly one CLASSIFICATION_COMPLETED');`
       with:
       `assert.equal(events.length, 1, 'must emit exactly one GRAPH_UPDATED');`

    5. **Lines 146-150 (between subscribe and assert)** — if any payload-shape assertion exists referencing `events[0].payload.anchorId` or similar, DELETE that assertion line. The GRAPH_UPDATED emit at trellis-actions.service.ts:107 is `eventBus.emit({ type: 'GRAPH_UPDATED' })` with NO payload field. Keep `events.length === 1` assertion only. Per 34-RESEARCH.md "Pitfall 1": leaving the payload assertion in place will cause "Cannot read property 'anchorId' of undefined" failures.

    Do NOT modify any other test in this file. Do NOT modify the file imports or the `_actions-mock-question.mjs` import — that pattern works.

    After editing, run from `app/`:
    ```
    node --test tests/services/trellis-replant.test.mjs 2>&1 | tail -20
    ```
    The renamed test must show in the pass list. If it fails with `ERR_MODULE_NOT_FOUND` for `podcast.service`, that is the SEPARATE loader gap and is OUT OF SCOPE for this task — flag it in the SUMMARY but do NOT modify the test further.
  </action>
  <verify>
    <automated>cd app && grep -c "CLASSIFICATION_COMPLETED" tests/services/trellis-replant.test.mjs</automated>
    Expected output: `0` (zero occurrences after rename).

    <automated>cd app && grep -c "eventBus.subscribe('GRAPH_UPDATED'" tests/services/trellis-replant.test.mjs</automated>
    Expected output: `1` (one subscription).

    <automated>cd app && node --test tests/services/trellis-replant.test.mjs 2>&1 | grep -E "^# (pass|fail)"</automated>
    Expected: pass count >= prior pass count + 1; the GRAPH_UPDATED test name appears in the output.
  </verify>
  <acceptance_criteria>
    - `grep -c "CLASSIFICATION_COMPLETED" app/tests/services/trellis-replant.test.mjs` returns `0`.
    - `grep -c "GRAPH_UPDATED" app/tests/services/trellis-replant.test.mjs` returns `>= 2` (subscribe + assert message).
    - `grep -c "Phase 32.1 D-W3-02" app/tests/services/trellis-replant.test.mjs` returns `>= 1` (history comment present).
    - `grep -c "events\\[0\\]\\.payload\\.anchorId" app/tests/services/trellis-replant.test.mjs` returns `0` (payload assertion removed).
    - The test labeled `replant emits GRAPH_UPDATED event` appears in `node --test` output as PASS.
    - `app/src/services/trellis-actions.service.ts` is NOT in `git status --short` (D-04 scope guard).
  </acceptance_criteria>
  <done>
    The renamed test passes; CLASSIFICATION_COMPLETED zero matches; trellis-actions.service.ts unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename event in trellis-prune.test.mjs and remove payload assertion</name>
  <files>app/tests/services/trellis-prune.test.mjs</files>
  <read_first>
    - app/tests/services/trellis-prune.test.mjs (read entire file; the test starts at line 95)
    - app/src/services/trellis-actions.service.ts (lines 130-145 — confirm unpruneQuestion's GRAPH_UPDATED emit has no payload)
    - .planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-RESEARCH.md (Q1 + Code Examples)
  </read_first>
  <action>
    Make exactly these textual changes to `app/tests/services/trellis-prune.test.mjs`:

    1. **Line 94** (comment) — replace:
       `// D-18: unpruneQuestion clears both flags and emits CLASSIFICATION_COMPLETED`
       with:
       `// D-18 — Phase 32.1 D-W3-02 consolidated CLASSIFICATION_COMPLETED into GRAPH_UPDATED. unpruneQuestion now emits the consolidated event.`

    2. **Line 95** (test name) — replace:
       `test('unpruneQuestion clears flagged and prunedFromTrellis and emits CLASSIFICATION_COMPLETED', async () => {`
       with:
       `test('unpruneQuestion clears flagged and prunedFromTrellis and emits GRAPH_UPDATED (Phase 32.1 D-W3-02 rename)', async () => {`

    3. **Line 103** (subscription) — replace:
       `const unsub = eventBus.subscribe('CLASSIFICATION_COMPLETED', (e) => events.push(e));`
       with:
       `const unsub = eventBus.subscribe('GRAPH_UPDATED', (e) => events.push(e));`

    4. **Line 112** (assertion) — replace:
       `assert.equal(events.length, 1, 'must emit CLASSIFICATION_COMPLETED');`
       with:
       `assert.equal(events.length, 1, 'must emit GRAPH_UPDATED');`

    5. **Lines 104-111** — if any payload-shape assertion exists referencing `events[0].payload.anchorId` or similar, DELETE that line. GRAPH_UPDATED at trellis-actions.service.ts:138 is `eventBus.emit({ type: 'GRAPH_UPDATED' })` with no payload.

    Do NOT modify any other test in this file. Do NOT modify any imports.

    After editing, run from `app/`:
    ```
    node --test tests/services/trellis-prune.test.mjs 2>&1 | tail -20
    ```
    The renamed test must pass. If it shows `ERR_MODULE_NOT_FOUND`, flag in SUMMARY (separate loader gap, out of scope).
  </action>
  <verify>
    <automated>cd app && grep -c "CLASSIFICATION_COMPLETED" tests/services/trellis-prune.test.mjs</automated>
    Expected: `0`.

    <automated>cd app && grep -c "eventBus.subscribe('GRAPH_UPDATED'" tests/services/trellis-prune.test.mjs</automated>
    Expected: `1`.

    <automated>cd app && node --test tests/services/trellis-prune.test.mjs 2>&1 | grep -E "^# (pass|fail)"</automated>
    Expected: pass count >= prior + 1; renamed test in PASS list.
  </verify>
  <acceptance_criteria>
    - `grep -c "CLASSIFICATION_COMPLETED" app/tests/services/trellis-prune.test.mjs` returns `0`.
    - `grep -c "GRAPH_UPDATED" app/tests/services/trellis-prune.test.mjs` returns `>= 2`.
    - `grep -c "Phase 32.1 D-W3-02" app/tests/services/trellis-prune.test.mjs` returns `>= 1`.
    - `grep -c "events\\[0\\]\\.payload\\.anchorId" app/tests/services/trellis-prune.test.mjs` returns `0`.
    - `app/src/services/trellis-actions.service.ts` unmodified (D-04).
  </acceptance_criteria>
  <done>
    The renamed unpruneQuestion test passes; CLASSIFICATION_COMPLETED zero matches in this file.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "CLASSIFICATION_COMPLETED" app/tests/` returns **zero** matches across both edited files.
- `cd app && node --test tests/services/trellis-replant.test.mjs tests/services/trellis-prune.test.mjs 2>&1 | grep -E "^# (pass|fail)"` shows pass count increased by **2** versus pre-Plan-34-01 baseline (the two GRAPH_UPDATED rename tests).
- `cd app && npx tsc -b --noEmit` exits 0 (no source changes; should be unchanged).
- `git status --short app/src/services/trellis-actions.service.ts` is empty (production code untouched per D-04).
- Reframing of D-01 ("inline mocks rewrite" → "minimal event rename") is documented in the SUMMARY citing 34-RESEARCH.md Q1.
</verification>

<success_criteria>
- 2 trellis-actions event tests rename `CLASSIFICATION_COMPLETED` → `GRAPH_UPDATED`.
- Both tests pass under `node --test` with no `--loader` flag.
- Payload-shape assertions removed (per 34-RESEARCH.md Pitfall 1 — GRAPH_UPDATED has no payload).
- History comment cites Phase 32.1 D-W3-02.
- Production code in `trellis-actions.service.ts` is NOT modified (D-04).
- The separate `ERR_MODULE_NOT_FOUND` loader gap is documented in SUMMARY as out-of-scope.
</success_criteria>

<output>
After completion, create `.planning/phases/34-v1-4-close-out-verification-debt-and-cleanup/34-01-SUMMARY.md` documenting:
- 4 lines patched per file (comment + test name + subscribe + assert message + payload-assertion deletion if present)
- Test pass-count delta
- Reframing rationale: 34-CONTEXT.md D-01 ("inline mocks rewrite") was scoped before research; 34-RESEARCH.md Q1 found a 4-line fix suffices because the existing `_actions-mock-question.mjs` import already works and replant/prune don't call `heal()`.
- Out-of-scope items flagged: `_actions-mock-loader.mjs` cleanup (v1.5), separate ERR_MODULE_NOT_FOUND loader gap.
</output>
