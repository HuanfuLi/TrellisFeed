---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 04
type: execute
wave: 3
depends_on: [33-03]
files_modified:
  - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md
  - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md
autonomous: true
requirements: [TSC-HYGIENE]
must_haves:
  truths:
    - "npx tsc -b --noEmit returns exit 0"
    - "npx vite build completes cleanly"
    - "npm test post-rename fail count ≤ pre-rename baseline AND post-rename signature set ⊆ pre-rename signature set — zero v1.4-specific regressions"
    - "git status is clean"
    - "D-16 / D-17 / D-18 recorded as SATISFIED-BY-760fa4f8 with evidence trail"
    - "33-VALIDATION.md Per-Task Verification Map rows flipped from ⬜ pending to ✅ green (frontmatter flags were already set by planner — this task only updates the per-task status column)"
  artifacts:
    - path: ".planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md"
      provides: "Phase 33 closure record covering TSC-HYGIENE satisfaction + 4 ROADMAP success criteria verification"
      must_contain: "SATISFIED-BY-760fa4f8"
    - path: ".planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md"
      provides: "Nyquist validation contract with Per-Task Verification Map populated + every row's Status column flipped to ✅ green"
      must_contain: "nyquist_compliant: true"
  key_links:
    - from: "33-CLOSURE.md"
      to: "commit 760fa4f8 (pre-existing tsc cleanup)"
      via: "documented evidence trail"
      pattern: "760fa4f8"
---

<objective>
Verify Phase 33's 4 ROADMAP success criteria are met, record D-16/D-17/D-18 as already satisfied by commit `760fa4f8` (per RESEARCH.md Finding #3 — "Commit 760fa4f8 Has Already Landed Part of Phase 33's Scope"), and flip the 33-VALIDATION.md Per-Task Verification Map rows from ⬜ pending to ✅ green.

**This is a documentation-only plan** — no production code changes. Per the critical planner alert: "commit 760fa4f8 already satisfies D-16/D-17/D-18. Do NOT create tasks that re-implement prop wiring or helper deletion. Instead, create a short verification-only plan that confirms tsc -b --noEmit is exit 0 and records the SATISFIED-BY-760fa4f8 evidence."

**Revision note (2026-04-19):** The 33-VALIDATION.md FRONTMATTER (`status`, `nyquist_compliant`, `wave_0_complete`) was already flipped to `validated` / `true` / `true` by the planner in commit `4422f293`. This task does NOT re-flip frontmatter flags — it ONLY updates the Per-Task Verification Map's Status column (⬜ pending → ✅ green) and the Approval line.

Purpose: D-18 verification + closing Phase 33 with a clean audit trail.
Output: 33-CLOSURE.md (new), 33-VALIDATION.md (Per-Task Verification Map flipped to green), single docs commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Run all 4 ROADMAP success-criterion checks and capture raw output</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 33 section — the 4 success criteria)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #8 (baseline notes — multiple pre-existing failure signatures)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #3 (commit 760fa4f8 rationale for D-16/D-17/D-18 SATISFIED-BY)
    - /tmp/phase-33-pre-rename-baseline.log + /tmp/phase-33-baseline-signatures.txt (captured in Plan 33-03 Task 3.5 Step 3 — pre-rename test failure baseline)
    - /tmp/phase-33-post-rename.log + /tmp/phase-33-post-rename-signatures.txt (captured in Plan 33-03 Task 3.5 Step 3 — post-rename test failure measurement)
  </read_first>
  <action>
    Capture verification evidence for the 4 ROADMAP success criteria plus 33-VALIDATION.md's criteria. Do NOT commit anything yet — this task only measures.

    Check 1 — tsc clean:
    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit 2>&1 | tee /tmp/33-closure-tsc.txt
    echo "TSC_EXIT=$?" >> /tmp/33-closure-tsc.txt
    ```
    Expected: empty output, `TSC_EXIT=0`.

    Check 2 — vite build:
    ```
    cd /Users/Code/EchoLearn/app && npx vite build 2>&1 | tail -5 | tee /tmp/33-closure-vite.txt
    ```
    Expected: clean build in ~3s, `built in` line visible.

    Check 3 — npm test: baseline-relative regression gate (authoritative per revision 2):

    The pre-Phase-33 working tree has multiple orthogonal pre-existing failure signatures: `ERR_IMPORT_ATTRIBUTE_MISSING` (Node-25 JSON imports), `ERR_MODULE_NOT_FOUND` (absent TrellisTooltip.tsx + absent podcast.service), `ERR_UNKNOWN_FILE_EXTENSION` (tsx direct imports), and `ERR_ASSERTION` / `AssertionError` (feed-strategy test assertions). The authoritative pass gate is NOT a fixed numeric band — it is "no new failures vs. the pre-rename baseline captured in Plan 33-03 Task 3.5 Step 3".

    Step 3a — Run the current test suite and measure:
    ```
    cd /Users/Code/EchoLearn/app && npm test 2>&1 | tee /tmp/33-closure-tests.txt
    FAIL_COUNT_CLOSURE=$(grep -cE "code: 'ERR_|AssertionError \\[ERR_ASSERTION\\]" /tmp/33-closure-tests.txt)
    grep -oE "code: 'ERR_[A-Z_]+'|AssertionError \\[ERR_ASSERTION\\]" /tmp/33-closure-tests.txt | sort -u > /tmp/33-closure-signatures.txt
    echo "CLOSURE_FAIL_COUNT=$FAIL_COUNT_CLOSURE"
    echo "CLOSURE_SIGNATURES:"; cat /tmp/33-closure-signatures.txt
    ```

    Step 3b — Compare against the pre-rename baseline captured by Plan 33-03 Task 3.5 Step 3:
    ```
    if [ ! -f /tmp/phase-33-baseline-signatures.txt ] || [ ! -f /tmp/phase-33-pre-rename-baseline.log ]; then
      echo "ERROR: pre-rename baseline files missing — Plan 33-03 Task 3.5 Step 3 did not capture them. Re-run Plan 33-03 Task 3.5 Step 3 before proceeding."
      exit 1
    fi

    FAIL_COUNT_BASELINE=$(grep -cE "code: 'ERR_|AssertionError \\[ERR_ASSERTION\\]" /tmp/phase-33-pre-rename-baseline.log)
    NEW_SIGNATURES=$(comm -23 /tmp/33-closure-signatures.txt /tmp/phase-33-baseline-signatures.txt)

    echo "BASELINE_FAIL_COUNT=$FAIL_COUNT_BASELINE"
    echo "CLOSURE_FAIL_COUNT=$FAIL_COUNT_CLOSURE"
    echo "NEW_SIGNATURES (must be empty):"
    echo "$NEW_SIGNATURES"
    ```

    **Authoritative tolerance (baseline-relative, per revision 2):**
    - **PASS** if BOTH: `$FAIL_COUNT_CLOSURE ≤ $FAIL_COUNT_BASELINE` (no new failures) AND `$NEW_SIGNATURES` is empty (the closure signature set is a SUBSET of the baseline — no new failure kinds).
    - **HALT** if EITHER: `$FAIL_COUNT_CLOSURE > $FAIL_COUNT_BASELINE` (new failure appeared) OR `$NEW_SIGNATURES` is non-empty (new signature appeared — something regressed beyond the pre-rename state).

    Do NOT enforce a "single signature" rule. Do NOT enforce `[24, 28]` or any other fixed count band. The baseline IS the number.

    Concrete HALT / PASS branching:
    ```
    if [ "$FAIL_COUNT_CLOSURE" -gt "$FAIL_COUNT_BASELINE" ]; then
      echo "HALT: closure fail count $FAIL_COUNT_CLOSURE > baseline $FAIL_COUNT_BASELINE — a new failure was introduced"
      exit 1
    fi
    if [ -n "$NEW_SIGNATURES" ]; then
      echo "HALT: new signature(s) appeared beyond pre-rename baseline:"
      echo "$NEW_SIGNATURES"
      exit 1
    fi
    echo "PASS: closure fail count $FAIL_COUNT_CLOSURE ≤ baseline $FAIL_COUNT_BASELINE AND signatures are a subset of baseline."
    ```

    Zero v1.4 regressions vs. the pre-rename baseline = PASS. Pre-existing Node-25 + absent-module + tsx-extension + assertion failures ALL carry through from the baseline — that is expected, not regressive.

    Check 4 — git status clean:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain --untracked-files=all | tee /tmp/33-closure-git.txt
    ```
    Expected: empty.

    Check 5 — TD-04 docs state (cross-check with Plan 33-02 output):
    ```
    cd /Users/Code/EchoLearn && grep -c "SUPERSEDED-BY-PHASE-31" .planning/phases/29-final-polishment/29-VERIFICATION.md
    grep -c "TD-01 SUPERSEDED" .planning/phases/29-final-polishment/29-UAT-LOG.md
    ```
    Both must return at least 1.

    Check 6 — TD-05 deletion state (cross-check with Plan 33-01):
    ```
    cd /Users/Code/EchoLearn && test ! -f app/src/components/ConceptProgressCard.tsx && echo "DELETED" || echo "STILL_EXISTS"
    cd /Users/Code/EchoLearn/app && node --test tests/locales/bundle-parity.test.mjs 2>&1 | tail -5
    ```
    First must print `DELETED`; second must show `fail 0`.

    Check 7 — TD-06 rename state (cross-check with Plan 33-03):
    ```
    cd /Users/Code/EchoLearn && grep -c "'yellow'" app/src/services/trellis-state.service.ts
    grep -c "'fallen'" app/src/services/trellis-state.service.ts
    grep -c "'dying'" app/src/services/trellis-state.service.ts
    grep -c "'dead'" app/src/services/trellis-state.service.ts
    grep -c "'falling'" app/src/services/trellis-state.service.ts
    ```
    Expected: 0, 0, >=3, >=2, >=2.

    Check 8 — Commit log summary:
    ```
    cd /Users/Code/EchoLearn && git log --oneline | head -10
    ```
    Expected: the most recent 5-7 commits are the Phase 33 commits (WIP flush, TD-05 delete, TD-04 supersession, SHA backfill, TD-06 rename) plus pre-Phase-33 heritage.

    Save all captured outputs to `/tmp/33-closure-*.txt` for inclusion in 33-CLOSURE.md in Task 4.2.
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit && cd /Users/Code/EchoLearn && test -z "$(git status --porcelain --untracked-files=all)" && echo "all-clean"</automated>
  </verify>
  <acceptance_criteria>
    - tsc exit code is 0.
    - vite build exit code is 0.
    - `$FAIL_COUNT_CLOSURE ≤ $FAIL_COUNT_BASELINE` AND `$NEW_SIGNATURES` (`comm -23` of closure vs. baseline signature sets) is empty. Do NOT enforce a fixed numeric band or a single-signature constraint.
    - `git status --porcelain --untracked-files=all` returns empty string.
    - All grep cross-checks (Check 5-7) match expectations.
    - Raw outputs captured at `/tmp/33-closure-tsc.txt`, `/tmp/33-closure-vite.txt`, `/tmp/33-closure-tests.txt`, `/tmp/33-closure-signatures.txt`, `/tmp/33-closure-git.txt`.
  </acceptance_criteria>
  <done>Evidence captured; ready to record in 33-CLOSURE.md.</done>
</task>

<task type="auto">
  <name>Task 4.2: Create 33-CLOSURE.md documenting SATISFIED-BY-760fa4f8 evidence and 4 success-criterion checks; flip 33-VALIDATION.md Per-Task Verification Map rows to green</name>
  <read_first>
    - /tmp/33-closure-tsc.txt, /tmp/33-closure-vite.txt, /tmp/33-closure-tests.txt, /tmp/33-closure-git.txt (captured in Task 4.1)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md (frontmatter flags ALREADY set to `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true` by planner commit `4422f293` — do NOT re-flip)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md Finding #3 (full text of the SATISFIED-BY-760fa4f8 argument to reproduce in CLOSURE.md)
  </read_first>
  <action>
    Step 1 — Create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md` with this structure (substitute `{CAPTURED_OUTPUT}` placeholders with actual text from /tmp/33-closure-*.txt files):

    ```markdown
    ---
    phase: 33-phase-29-regression-and-phase-31-code-hygiene
    type: closure
    created: 2026-04-19
    verdict: passed
    ---

    # Phase 33 Closure — Phase 29 regression + Phase 31 code hygiene

    Phase 33 executed in 4 waves. This document records the final verification state against the 4 ROADMAP success criteria plus the SATISFIED-BY-760fa4f8 evidence for D-16/D-17/D-18.

    ## Summary

    | Success Criterion (ROADMAP Phase 33) | Status | Evidence |
    |---|---|---|
    | 1. npm test shows zero v1.4 regressions vs. pre-rename baseline | PASSED | See Check 3 below |
    | 2. npx tsc -b --noEmit exit 0 | PASSED | See Check 1 below |
    | 3. git status clean | PASSED | See Check 4 below |
    | 4. 29-VERIFICATION.md no longer stale w.r.t. TD-01 | PASSED | See Check 5 below |

    ## D-16 / D-17 / D-18 Status: SATISFIED-BY-760fa4f8

    Per RESEARCH.md Finding #3, commit `760fa4f8` ("chore(types): clear 10 stale tsc errors blocking device build", 2026-04-18 23:34) — landed ONE DAY BEFORE Phase 33 CONTEXT.md was authored — already:

    - D-16 (VineProgress 3 unused props): SATISFIED. Dropped `explored`, `total`, `isComplete` from VineProgressProps and caller sites in HomeScreen.tsx.
    - D-17 (4 unused helpers in concept-feed.service.ts): SATISFIED. Removed `generateDailyPostsWithLLM`, `_backgroundGenerateVideos`, `shuffleArray`, `_backgroundGenerateNews` + orphaned `graphService` / `newsService` imports.
    - D-18 (tsc clean after fixes): SATISFIED. `npx tsc -b --noEmit` exits 0; the 4 "pre-existing" errors listed in 29-03-SUMMARY.md (AskScreen, PlannerScreen, SettingsFeaturesScreen, SettingsScreen) were also cleared by 760fa4f8.

    Phase 33 itself introduced no additional tsc fixes. All verification is grep + test-run, no code delta.

    ## Check 1 — npx tsc -b --noEmit

    ```
    {PASTE CONTENTS OF /tmp/33-closure-tsc.txt}
    ```

    Exit code: 0.

    ## Check 2 — npx vite build

    ```
    {PASTE CONTENTS OF /tmp/33-closure-vite.txt}
    ```

    ## Check 3 — npm test (baseline-relative comparison)

    Pre-rename baseline (captured in Plan 33-03 Task 3.5 Step 3):
    - `FAIL_COUNT_BASELINE` = {PASTE CAPTURED VALUE}
    - Baseline signatures:
    ```
    {PASTE CONTENTS OF /tmp/phase-33-baseline-signatures.txt}
    ```

    Closure run (this task):
    - `FAIL_COUNT_CLOSURE` = {PASTE CAPTURED VALUE}
    - Closure signatures:
    ```
    {PASTE CONTENTS OF /tmp/33-closure-signatures.txt}
    ```

    New signatures (baseline subtraction, `comm -23 closure baseline`):
    ```
    {PASTE OUTPUT — expected empty}
    ```

    Verdict: `FAIL_COUNT_CLOSURE ≤ FAIL_COUNT_BASELINE` AND closure signature set ⊆ baseline signature set. PASSED.

    Failure breakdown (pre-existing from v1.3/1.4 WIP state, NOT Phase 33 regressions):
    - `ERR_IMPORT_ATTRIBUTE_MISSING` — Node-25 JSON import attribute requirement (en.json, zh.json, es.json, ja.json imports in trellis test files).
    - `ERR_MODULE_NOT_FOUND` — absent `app/src/components/trellis/TrellisTooltip.tsx` (deferred to v1.5 per Plan 33-03 Scope boundary); absent `app/src/services/podcast.service` import from `trellis-actions.service.ts` (pre-existing defect documented in STATE.md).
    - `ERR_UNKNOWN_FILE_EXTENSION` — direct `.tsx` imports (TrellisLeaf.tsx) that the tsx loader rejects — pre-existing.
    - `ERR_ASSERTION` / `AssertionError` — feed-strategy test assertions (e.g., `applyStrategyBias` function enumeration) — pre-existing from earlier phase.

    Zero v1.4-specific regressions. All four failure kinds carry through from the baseline.

    ## Check 4 — git status

    ```
    {PASTE CONTENTS OF /tmp/33-closure-git.txt}
    ```

    Working tree clean.

    ## Check 5 — 29-VERIFICATION.md no longer stale

    ```
    grep "SUPERSEDED-BY-PHASE-31" .planning/phases/29-final-polishment/29-VERIFICATION.md
    ```

    Returns the updated TD-01 Requirements Coverage row AND the updated Observable Truths row 1. See Plan 33-02 for the full edit trail and commit SHA.

    ## Commit trail (Phase 33)

    ```
    {PASTE `git log --oneline | head -10` OUTPUT HERE}
    ```

    Phase 33 commits (in reverse chronological order):
    1. docs(33): record Phase 33 closure + SATISFIED-BY-760fa4f8 evidence (this commit — Plan 33-04)
    2. refactor(trellis): rename LeafState literals yellow->dying, fallen->dead per design vocabulary (TD-06) (Plan 33-03)
    3. docs(29): record TD-04 closure commit SHA in UAT-LOG supersession entry (Plan 33-02 amendment)
    4. refactor(29): supersede TD-01 — Phase 31 D-14 generation-time prioritization subsumes runtime sort bias (TD-04) (Plan 33-02)
    5. refactor(feed): delete orphaned ConceptProgressCard.tsx + its 4 dead i18n keys (TD-05) (Plan 33-01)
    6. chore(v1.4): flush WIP — quota refactor, i18n polish, starter-posts-decay helper (Plan 33-00)

    Pre-Phase-33 relevant commits:
    - `760fa4f8` chore(types): clear 10 stale tsc errors blocking device build — SATISFIES D-16/D-17/D-18.
    - `9486799a` test(32.1-02): commit existing concept-batch + daily-generation-cap + starter-posts tests — pre-Phase-33 test checkpoint (these 3 test files landed before Phase 33 and were NOT part of the 33-00 WIP flush).

    ## Deferred (per D-07, D-08, D-12, Phase 33 Scope boundary)

    - `post-store.service.ts` deletion — revisit at start of v1.5 milestone planning (D-07).
    - `ImmersiveInfoFlow` export deletion in InfoFlow.tsx — revisit at v1.5 (D-08).
    - Rename `'falling'` LeafState literal — not worth the churn; UI never exposes it (D-12).
    - Restore `app/src/components/trellis/TrellisTooltip.tsx` OR re-point `trellis-tooltip-copy.test.mjs` — out of scope for Phase 33 (Plan 33-03 objective Scope boundary); `ERR_MODULE_NOT_FOUND` failure carries through from baseline.

    ## Sign-off

    Phase 33 passed. All 21 decisions (D-01..D-21) satisfied or explicitly deferred:

    | Decision | Disposition | Plan |
    |---|---|---|
    | D-01..D-05 (TD-04 supersession) | SATISFIED | 33-02 |
    | D-06 (ConceptProgressCard delete) | SATISFIED | 33-01 |
    | D-07 (post-store.service.ts retain) | DEFERRED-TO-V1.5 | — |
    | D-08 (ImmersiveInfoFlow retain) | DEFERRED-TO-V1.5 | — |
    | D-09 (orphan i18n keys removed) | SATISFIED | 33-01 |
    | D-10, D-11, D-13, D-14 (LeafState rename) | SATISFIED | 33-03 |
    | D-12 (keep 'falling') | SATISFIED | 33-03 |
    | D-15 (atomic rename commit) | SATISFIED | 33-03 |
    | D-16, D-17, D-18 (tsc hygiene) | SATISFIED-BY-760fa4f8 | — |
    | D-19 (WIP flush commit) | SATISFIED | 33-00 |
    | D-20 (review policy) | SATISFIED | 33-00 |
    | D-21 (3 untracked tests green) | SATISFIED-BY-9486799a | — (tests landed pre-33 via commit 9486799a) |

    Ready for milestone v1.4 re-audit.
    ```

    Replace each `{PASTE CONTENTS OF /tmp/33-closure-*.txt}` placeholder with the actual file contents captured in Task 4.1. Also replace `{PASTE git log --oneline | head -10 OUTPUT HERE}` with a fresh run of `git log --oneline | head -10`, and substitute the `FAIL_COUNT_BASELINE` / `FAIL_COUNT_CLOSURE` captured values.

    Step 2 — Update `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md` Per-Task Verification Map.

    **IMPORTANT (revision note):** The frontmatter flags (`status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`) were ALREADY set by the planner in commit `4422f293`. DO NOT attempt to edit them here — the `Edit` tool would fail because the "from" strings (`status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`) no longer exist. Frontmatter is complete; skip directly to the Per-Task Verification Map update.

    The current Per-Task Verification Map in 33-VALIDATION.md has 12 rows covering tasks 0.1 through 4.2. Each row ends with `| ⬜ pending` in the Status column. Assuming Task 4.1 confirmed all checks pass, flip EACH row's Status column from `⬜ pending` → `✅ green`.

    Concrete approach: use the `Edit` tool with 12 separate calls (one per row), doing a precise content-match replacement of the trailing `| ⬜ pending` to `| ✅ green` within each row's unique leading columns. Example for row 0.1:
    - Match (unique enough): `| 0.1 | 33-00 | 0 | WIP-FLUSH | targeted tests | cd app && node --test tests/services/concept-batch-filter.test.mjs tests/services/daily-generation-cap.test.mjs tests/services/starter-posts.test.mjs tests/concept-quota.test.mjs | ✅ all 4 | ⬜ pending |`
    - Replace: same line but ending `| ✅ green |` in place of `| ⬜ pending |`.

    Alternatively (faster): replace the ENTIRE table body (13 rows: 0.1, 0.2, 1.1, 1.1b, 2.1, 2.2, 3.1, 3.2, 3.3, 3.4, 3.5, 4.1, 4.2) preserving the header row. Preserve each row's existing columns (Task ID, Plan, Wave, Requirement, Test Type, Automated Command, File Exists) and only change the final Status column.

    If ANY test or check FAILED in Task 4.1, do NOT mark that row ✅ green — mark it ❌ red and HALT the plan before committing. Escalate to the user.

    Step 3 — Update the Approval line in 33-VALIDATION.md. Current text (bottom of file):
    `**Approval:** approved 2026-04-19 (planner sign-off; executor status flips individual rows from ⬜ pending → ✅ green as waves land)`

    Replace with:
    `**Approval:** approved 2026-04-19 by planner; executor-verified 2026-04-19 (all 13 rows flipped ⬜ pending → ✅ green via Plan 33-04)`

    Step 4 — Stage + commit:
    ```
    cd /Users/Code/EchoLearn && git add \
      .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md \
      .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md

    git commit -m "$(cat <<'EOF'
    docs(33): record Phase 33 closure + SATISFIED-BY-760fa4f8 evidence for D-16/D-17/D-18

    Phase 33 passed all 4 ROADMAP success criteria:
    - npm test: zero v1.4 regressions vs. pre-rename baseline (baseline-relative
      subset check — closure signatures ⊆ baseline, fail count ≤ baseline)
    - npx tsc -b --noEmit: exit 0
    - git status: clean
    - 29-VERIFICATION.md: TD-01 row now SUPERSEDED-BY-PHASE-31

    D-16/D-17/D-18 (tsc hygiene) were already satisfied by commit 760fa4f8
    "chore(types): clear 10 stale tsc errors blocking device build", landed
    one day before 33-CONTEXT.md was authored. Plan 33-04 documents this
    rather than redoing the work.

    33-VALIDATION.md updates (frontmatter flags were already set by planner
    in 4422f293 — this commit only updates the Per-Task Verification Map):
    - All 13 rows flipped ⬜ pending -> ✅ green
    - Approval line updated with executor-verified timestamp

    Phase 33 ready for milestone v1.4 re-audit.
    EOF
    )"
    ```

    Step 5 — Final confirm:
    ```
    cd /Users/Code/EchoLearn && git status --porcelain
    # must be empty
    grep "nyquist_compliant: true" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md
    # must hit (flag was already set by planner; this confirms no regression)
    grep -c "SATISFIED-BY-760fa4f8" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md
    # must hit (at least 2 times)
    grep -c "⬜ pending" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md
    # must be 0 (all rows flipped)
    grep -c "✅ green" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md
    # must be at least 12 (one per row) — higher counts also acceptable if "✅" appears elsewhere
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -c "nyquist_compliant: true" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md && grep -c "SATISFIED-BY-760fa4f8" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md` exists.
    - 33-CLOSURE.md contains all 4 ROADMAP success-criterion evidence blocks with actual captured output (not `{PASTE ...}` placeholders).
    - 33-CLOSURE.md mentions `SATISFIED-BY-760fa4f8` at least twice (summary header + sign-off table).
    - 33-CLOSURE.md documents all four pre-existing failure signatures (ERR_IMPORT_ATTRIBUTE_MISSING, ERR_MODULE_NOT_FOUND, ERR_UNKNOWN_FILE_EXTENSION, ERR_ASSERTION / AssertionError) with provenance, and states that all carry through from the pre-rename baseline.
    - 33-VALIDATION.md frontmatter UNCHANGED from planner's pre-set state: `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true` all still present.
    - 33-VALIDATION.md Per-Task Verification Map: zero `⬜ pending` strings remain; at least 12 `✅ green` markers present (one per row).
    - Approval line contains `executor-verified 2026-04-19`.
    - `git status --porcelain | wc -l` returns `0`.
    - `git log -1 --format=%s` starts with `docs(33): record Phase 33 closure`.
  </acceptance_criteria>
  <done>Phase 33 closure documented, VALIDATION rows flipped to green, working tree clean, milestone v1.4 audit-ready.</done>
</task>

</tasks>

<verification>
- `grep "nyquist_compliant: true" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md` returns a hit (pre-existing flag, preserved).
- `grep -c "⬜ pending" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-VALIDATION.md` returns `0`.
- `grep -c "SATISFIED-BY-760fa4f8" .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CLOSURE.md` returns >= 2.
- `git status --porcelain` empty.
- `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns 0.
- `git log --oneline | head -8` shows the complete Phase 33 commit trail (6 commits: 33-00 WIP, 33-01 TD-05, 33-02 TD-04, 33-02 SHA backfill, 33-03 TD-06 rename, 33-04 closure).
</verification>

<success_criteria>
- 33-CLOSURE.md created with all 4 ROADMAP criteria evidence + SATISFIED-BY-760fa4f8 trail + baseline-relative test comparison (no new failures, signature set ⊆ baseline).
- 33-VALIDATION.md Per-Task Verification Map: every row's Status column flipped ⬜ pending → ✅ green. Frontmatter untouched (already correct).
- Single docs commit covers both files.
- Phase 33 ready for milestone v1.4 re-audit.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-04-SUMMARY.md` with the commit SHA, final test baseline (`FAIL_COUNT_BASELINE`, `FAIL_COUNT_CLOSURE`, signature diff), and a one-line verdict: "Phase 33 closed — TD-04/05/06 resolved + D-16/17/18 satisfied by 760fa4f8."
</output>
