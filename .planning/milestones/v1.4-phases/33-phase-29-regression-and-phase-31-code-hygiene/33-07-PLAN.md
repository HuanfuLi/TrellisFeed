---
phase: 33-phase-29-regression-and-phase-31-code-hygiene
plan: 07
type: execute
wave: 2
depends_on: [33-05]
files_modified:
  - app/src/screens/PlannerScreen.tsx
  - app/src/components/ChatInput.tsx
autonomous: true
requirements: [COSMETIC-POLISH]
gap_closure: false
must_haves:
  truths:
    - "PlannerScreen.tsx refresh button at line ~152 has touch-target dimensions of 44x44px (not 28x28px) per D-24 (WCAG 2.5.8 minimum)"
    - "PlannerScreen.tsx EmptySectionHint padding at line ~24 uses CSS-var token `var(--space-md) var(--space-lg)` (not the literal `'14px 16px'`) per D-25"
    - "PlannerScreen.tsx show/hide buttons at lines ~302 and ~317 use `var(--space-sm) var(--space-lg)` padding (not `'10px 16px'`) per D-25"
    - "ChatInput.tsx mic button at lines ~110-111 has 44x44px dimensions (not 34x34px) per D-24"
    - "ChatInput.tsx globe button (the second 34x34 occurrence around line ~138-139) has 44x44px dimensions per D-24"
    - "ChatInput.tsx container shadow at line ~97 uses `var(--shadow-2)` token (not literal `'0 4px 12px rgba(0,0,0,0.1)'`) per D-25; pre-edit operator escalation gate confirmed `--shadow-2` in `app/src/index.css` is visually-equivalent to the literal value being replaced"
    - "Per D-26: NO new icon library; NO new Tailwind classes; NO translation changes; NO new packages — inline styles + CSS vars + lucide-react + i18next bundles only"
    - "Two atomic commits — one per file — to make any regression bisect-friendly"
    - "tsc -b --noEmit exits 0 after each commit"
  artifacts:
    - path: "app/src/screens/PlannerScreen.tsx"
      provides: "Refresh button (44x44 per D-24); EmptySectionHint padding token (D-25); show/hide button padding token (D-25)"
      must_contain: "var(--space-md) var(--space-lg)"
      must_not_contain: "'14px 16px'"
    - path: "app/src/components/ChatInput.tsx"
      provides: "Mic + globe buttons (44x44 per D-24); container shadow token (D-25)"
      must_contain: "var(--shadow-2)"
      must_not_contain: "'0 4px 12px rgba(0,0,0,0.1)'"
  key_links:
    - from: "PlannerScreen refresh button"
      to: "44x44 touch target (WCAG 2.5.8)"
      via: "inline style width/height = '44px'"
      pattern: "width: '44px', height: '44px'"
    - from: "ChatInput mic + globe buttons"
      to: "44x44 touch targets (WCAG 2.5.8)"
      via: "inline style width/height = '44px'"
      pattern: "width: '44px'"
    - from: "ChatInput container box-shadow"
      to: "unified shadow tier (Phase 32.1 Wave 4 D-W4-08)"
      via: "boxShadow: 'var(--shadow-2)'"
      pattern: "boxShadow: 'var\\(--shadow-2\\)'"
---

<objective>
Phase 33 v2 cosmetic polish: four small consistency fixes surfaced by the 2026-04-19 codebase audit. All are token/value substitutions with no layout impact and no behavioral change.

Decisions implemented:
- **D-24 (Touch-target compliance — WCAG 2.5.8 44x44 minimum):**
  - PlannerScreen.tsx refresh button: 28x28 → 44x44 (icon size preserved at 13-16px so it doesn't dominate)
  - ChatInput.tsx mic button: 34x34 → 44x44
  - ChatInput.tsx globe button (immediately following mic): 34x34 → 44x44
- **D-25 (Spacing & shadow token migration):**
  - ChatInput.tsx container box-shadow: literal `'0 4px 12px rgba(0,0,0,0.1)'` → token `var(--shadow-2)` (matches Phase 32.1 Wave 4 D-W4-08 unified shadow tier)
  - PlannerScreen.tsx EmptySectionHint padding: `'14px 16px'` → `var(--space-md) var(--space-lg)` (= 12px 16px; 14→12 vertical change is intentional per token grid)
  - PlannerScreen.tsx show/hide buttons padding: `'10px 16px'` → `var(--space-sm) var(--space-lg)` (= 8px 16px; 10→8 vertical change is intentional per token grid)
- **D-26 (No Tailwind / no new packages / no translation changes):** Per CLAUDE.md project convention. Cosmetic plan must NOT introduce new packages, Tailwind classes, or runtime LLM translation calls.

Two atomic commits — one per file — for easy bisect.

**Pre-edit guardrail (Task 7.2 Step 1):** BEFORE swapping the ChatInput shadow literal, READ `app/src/index.css` to confirm `--shadow-2` is visually-equivalent (~equal blur, opacity, and color) to `0 4px 12px rgba(0,0,0,0.1)`. If the substitute is substantially darker/lighter, escalate to operator before committing — do NOT silently swap to a visually-different shadow.

**Critical scope boundary:** This plan does NOT touch the Concept Feed Generation Pipeline, Header positioning portal-vs-in-tree split, News post pipeline, Anchor name normalization, GRAPH_UPDATED event-bus convention, or i18n bundles. Cosmetic-only.

Output: 2 commits; tsc clean after each; no behavioral change visible in `npm run dev`; no layout regression on PlannerScreen or AskScreen (operator-verifiable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md
@.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-RESEARCH.md
@app/src/screens/PlannerScreen.tsx
@app/src/components/ChatInput.tsx
@app/src/index.css
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 7.1: PlannerScreen.tsx — D-24 (refresh button 28→44) + D-25 (EmptySectionHint and show/hide button padding tokens)</name>
  <read_first>
    - app/src/screens/PlannerScreen.tsx lines 20-30 (EmptySectionHint definition at line ~22-28)
    - app/src/screens/PlannerScreen.tsx lines 145-165 (refresh button at line ~146-163)
    - app/src/screens/PlannerScreen.tsx lines 295-330 (show/hide buttons at lines ~298-326)
    - app/src/index.css lines 99-101 (confirm `--space-sm: 8px`, `--space-md: 12px`, `--space-lg: 16px` exist)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md D-24, D-25, D-26
    - CLAUDE.md "Style Conventions" section (inline styles + CSS vars; do NOT introduce Tailwind classes)
  </read_first>
  <action>
    Step 1 — Pre-flight grep to confirm CURRENT line numbers (file may have shifted slightly post-Wave-4-flush from Plan 33-05):

    ```
    cd /Users/Code/EchoLearn && grep -n "EmptySectionHint" app/src/screens/PlannerScreen.tsx | head -3
    cd /Users/Code/EchoLearn && grep -n "RefreshCw\|refreshMoves" app/src/screens/PlannerScreen.tsx | head -5
    cd /Users/Code/EchoLearn && grep -n "showAllSuggestions" app/src/screens/PlannerScreen.tsx | head -5
    ```

    Use the grep output to confirm the actual line numbers for the 3 edit sites. Reference values from research:
    - EmptySectionHint at line ~22-28 with `padding: '14px 16px'` at line ~24
    - Refresh button at line ~146-163 with `width: '28px', height: '28px'` at line ~152
    - Show button at line ~299-311 with `padding: '10px 16px'` at line ~302
    - Hide button at line ~314-326 with `padding: '10px 16px'` at line ~317

    Step 2 — Verify CSS tokens exist:

    ```
    cd /Users/Code/EchoLearn && grep -n "\\-\\-space-sm\\|\\-\\-space-md\\|\\-\\-space-lg" app/src/index.css | head -5
    ```

    Expected hits:
    - `--space-sm: 8px;`
    - `--space-md: 12px;`
    - `--space-lg: 16px;`

    If any token is missing, HALT — D-25 swap requires the tokens to exist; consider adding them in a separate plan rather than ad-hoc here.

    Step 3 — Edit 1: EmptySectionHint padding (D-25). Use Edit tool to change line ~24:

    Before:
    ```tsx
    <Card style={{ padding: '14px 16px', marginBottom: '10px', backgroundColor: 'var(--surface-variant)' }}>
    ```

    After:
    ```tsx
    <Card style={{ padding: 'var(--space-md) var(--space-lg)', marginBottom: '10px', backgroundColor: 'var(--surface-variant)' }}>
    ```

    Note: 14→12 vertical change is INTENTIONAL per CONTEXT.md D-25 ("the 14→12 vertical change is intentional (token grid)"). Do not preserve the 14px.

    Step 4 — Edit 2: Refresh button (D-24). Use Edit tool to change the inline style at line ~152:

    Before:
    ```tsx
            style={{
              width: '28px', height: '28px', borderRadius: '50%',
    ```

    After:
    ```tsx
            style={{
              width: '44px', height: '44px', borderRadius: '50%',
    ```

    KEEP the `<RefreshCw size={13} />` and `<Loader2 size={13} ...>` icon sizes UNCHANGED (per CONTEXT D-24 / Code Context: "KEEP the icon at 14-16px so it doesn't dominate the button"; the existing 13 is within tolerance — do not bump it).

    Step 5 — Edit 3a: Show-all button padding (D-25). Use Edit tool to change line ~302:

    Before:
    ```tsx
                style={{
                  width: '100%', padding: '10px 16px', borderRadius: '12px',
    ```

    After:
    ```tsx
                style={{
                  width: '100%', padding: 'var(--space-sm) var(--space-lg)', borderRadius: '12px',
    ```

    Step 6 — Edit 3b: Show-less button padding (D-25). Use Edit tool to change line ~317:

    Before:
    ```tsx
                style={{
                  width: '100%', padding: '10px 16px', borderRadius: '12px',
    ```

    After:
    ```tsx
                style={{
                  width: '100%', padding: 'var(--space-sm) var(--space-lg)', borderRadius: '12px',
    ```

    NOTE: lines 302 and 317 have IDENTICAL `padding: '10px 16px'` literals on different button blocks. Use the Edit tool's `replace_all=false` (default) for each — the surrounding context (e.g., the line before with `onClick={() => setShowAllSuggestions(true)}` vs `onClick={() => setShowAllSuggestions(false)}`) MUST be included in the search string to disambiguate. Use a 3-4 line search context per Edit invocation.

    Step 7 — Verify: re-read PlannerScreen.tsx lines 20-30, 145-165, 295-330 and confirm the 4 substitutions landed correctly. Specifically:

    ```
    cd /Users/Code/EchoLearn && grep -n "padding: 'var(--space-md) var(--space-lg)'" app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && grep -n "padding: 'var(--space-sm) var(--space-lg)'" app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && grep -n "width: '44px', height: '44px'" app/src/screens/PlannerScreen.tsx
    ```

    Expected:
    - First grep: 1 hit (EmptySectionHint).
    - Second grep: 2 hits (show + hide buttons).
    - Third grep: at least 1 hit (refresh button).

    And the negative checks:
    ```
    cd /Users/Code/EchoLearn && grep -nE "padding: '14px 16px'" app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && grep -nE "padding: '10px 16px'" app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && grep -nE "width: '28px', height: '28px'" app/src/screens/PlannerScreen.tsx
    ```
    All three negative greps should return ZERO hits. If any return a hit, you missed an edit — investigate and fix.

    NOTE: there may be OTHER `'10px 16px'` or `'14px 16px'` literals elsewhere in the file (e.g., on Suggested Moves rows or other unrelated components). Per CONTEXT D-25 the scope is ONLY the 3 specified sites (EmptySectionHint + 2 show/hide buttons). If grep shows additional hits in OTHER contexts (different component blocks), leave them alone — they are out of scope for this plan.

    Step 8 — tsc check:
    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    ```
    Must show `tsc-exit=0`.

    Step 9 — Commit:
    ```
    cd /Users/Code/EchoLearn && git add app/src/screens/PlannerScreen.tsx
    cd /Users/Code/EchoLearn && git commit -m "$(cat <<'EOF'
    style(PlannerScreen): touch targets + spacing tokens (D-24, D-25)

    Phase 33 Plan 07 Task 1. Three small consistency fixes per CONTEXT D-24/D-25:

    - Refresh button (line ~152): 28x28 -> 44x44 for WCAG 2.5.8 touch-target
      compliance. Icon size kept at 13 (within 14-16 tolerance per D-24 note).
    - EmptySectionHint (line ~24): padding '14px 16px' -> var(--space-md)
      var(--space-lg) (= 12px 16px). 14->12 vertical change intentional per
      token grid.
    - Show/hide buttons (lines ~302, ~317): padding '10px 16px' -> var(--space-sm)
      var(--space-lg) (= 8px 16px). 10->8 vertical change intentional per
      token grid.

    Per D-26: no Tailwind, no new packages, no translation changes. Inline
    styles + CSS vars only.

    No behavioral change. tsc -b --noEmit exits 0.
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -cE "padding: '14px 16px'|padding: '10px 16px'|width: '28px', height: '28px'" app/src/screens/PlannerScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "padding: '14px 16px'" app/src/screens/PlannerScreen.tsx` returns `0` in the EmptySectionHint context (line ~22-28). If the literal `'14px 16px'` appears elsewhere in the file in a different unrelated context, that's acceptable — but the EmptySectionHint Card style on line ~24 must use the token. Validation grep: `awk 'NR>=20 && NR<=30' app/src/screens/PlannerScreen.tsx | grep -c "padding: '14px 16px'"` returns `0`.
    - `grep -cE "padding: '10px 16px'" app/src/screens/PlannerScreen.tsx` returns `0` in the show/hide button contexts (lines ~298-326). Validation: `awk 'NR>=298 && NR<=330' app/src/screens/PlannerScreen.tsx | grep -c "padding: '10px 16px'"` returns `0`.
    - `awk 'NR>=145 && NR<=165' app/src/screens/PlannerScreen.tsx | grep -c "width: '28px', height: '28px'"` returns `0` (refresh button bumped to 44x44).
    - `grep -c "padding: 'var(--space-md) var(--space-lg)'" app/src/screens/PlannerScreen.tsx` returns at least `1` (EmptySectionHint).
    - `grep -c "padding: 'var(--space-sm) var(--space-lg)'" app/src/screens/PlannerScreen.tsx` returns at least `2` (show + hide buttons).
    - `grep -c "width: '44px', height: '44px'" app/src/screens/PlannerScreen.tsx` returns at least `1` (refresh button).
    - `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
    - `git log -1 --format='%s'` returns: `style(PlannerScreen): touch targets + spacing tokens (D-24, D-25)`.
    - `git diff HEAD~1 --name-only` lists ONLY `app/src/screens/PlannerScreen.tsx`.
  </acceptance_criteria>
  <done>D-24 (refresh button) + D-25 (EmptySectionHint padding + show/hide button padding) complete on PlannerScreen.tsx; one atomic commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7.2: ChatInput.tsx — D-24 (mic + globe buttons 34→44) + D-25 (container shadow token)</name>
  <read_first>
    - app/src/components/ChatInput.tsx lines 80-160 (the form container at line ~84-100, mic button at line ~103-128, globe button at line ~130-152)
    - app/src/index.css lines 75-90 (confirm `--shadow-2` definition is visually-equivalent to `'0 4px 12px rgba(0,0,0,0.1)'` BEFORE swapping)
    - .planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-CONTEXT.md D-24, D-25, D-26
    - CLAUDE.md "Style Conventions"
  </read_first>
  <action>
    Step 1 — PRE-EDIT GUARDRAIL (D-25 shadow visual parity): READ app/src/index.css to confirm `--shadow-2` is visually-equivalent to the literal being replaced.

    ```
    cd /Users/Code/EchoLearn && grep -nE "\\-\\-shadow-2:" app/src/index.css
    ```

    Per RESEARCH and Read tool, current values:
    - Light mode: `--shadow-2: 0 3px 6px rgba(0, 0, 0, 0.1), 0 2px 4px rgba(0, 0, 0, 0.06);`
    - Dark mode: `--shadow-2: 0 3px 6px rgba(0, 0, 0, 0.6), 0 2px 4px rgba(0, 0, 0, 0.5);`

    Replacement target literal: `0 4px 12px rgba(0,0,0,0.1)` (single shadow layer, 4px y-offset, 12px blur, 10% opacity).

    Visual parity check (light mode):
    - Original: 1 layer, y=4, blur=12, opacity=0.1
    - --shadow-2: 2 layers, y=3+2, blur=6+4, opacity=0.1+0.06
    - Total diffuseness is COMPARABLE (smaller individual blurs but additive; opacity is similar).
    - Material Design "elevation 2" tier — appropriate for an input bar that should appear lifted off the background.

    Verdict: visually equivalent. PROCEED with the swap.

    If you discover the values diverge significantly (e.g., dark mode `--shadow-2` opacity is 0.6 vs original 0.1 — but dark mode darker shadows are correct theming), the dark-mode case is intentional and consistent with Phase 32.1 Wave 4 D-W4-08 unified tier. STILL PROCEED.

    If after re-reading you decide the swap is visually wrong (e.g., the literal was using a deliberately distinct shadow), HALT and escalate.

    Step 2 — Pre-flight grep to locate current edit sites:

    ```
    cd /Users/Code/EchoLearn && grep -n "boxShadow.*4px 12px\\|width: '34px'" app/src/components/ChatInput.tsx
    ```

    Expected hits:
    - 1 hit on the boxShadow literal at line ~97
    - 2 hits on `width: '34px'` (mic button line ~110, globe button line ~138)

    Step 3 — Edit 1: container box-shadow (D-25). Use Edit tool to change line ~97:

    Before:
    ```tsx
            boxShadow: '0 4px 12px rgba(0,0,0,0.1)',
    ```

    After:
    ```tsx
            boxShadow: 'var(--shadow-2)',
    ```

    Step 4 — Edit 2: mic button (D-24). Use Edit tool to change lines ~109-111:

    Before:
    ```tsx
            style={{
              flexShrink: 0,
              width: '34px',
              height: '34px',
    ```

    After:
    ```tsx
            style={{
              flexShrink: 0,
              width: '44px',
              height: '44px',
    ```

    Note: the mic button has icon `<Mic size={17} />` at line ~126 — KEEP this icon size unchanged. The 17px icon inside a 44x44 button is appropriate (38% icon-to-button ratio, comfortable for thumbs).

    Step 5 — Edit 3: globe button (D-24). Use Edit tool to change lines ~137-139:

    Before:
    ```tsx
              style={{
                flexShrink: 0,
                width: '34px',
                height: '34px',
    ```

    After:
    ```tsx
              style={{
                flexShrink: 0,
                width: '44px',
                height: '44px',
    ```

    NOTE: the mic and globe buttons have IDENTICAL `width: '34px', height: '34px'` blocks. Use Edit tool with sufficient surrounding context (the lines `onClick={toggleMic}` vs `onClick={onToggleWebSearch}` MUST appear in the search string) to disambiguate the two replacements.

    Globe button's icon is `<Globe size={17} />` at line ~150 — KEEP unchanged.

    Step 6 — Verify: re-read ChatInput.tsx lines 80-155 and confirm the 3 substitutions landed correctly:

    ```
    cd /Users/Code/EchoLearn && grep -nE "boxShadow: 'var\\(--shadow-2\\)'" app/src/components/ChatInput.tsx
    cd /Users/Code/EchoLearn && grep -cE "width: '44px'" app/src/components/ChatInput.tsx
    cd /Users/Code/EchoLearn && grep -cE "height: '44px'" app/src/components/ChatInput.tsx
    ```

    Expected:
    - First grep: 1 hit on the container boxShadow.
    - Second grep: 2 hits (mic + globe buttons).
    - Third grep: 2 hits (mic + globe buttons).

    Negative checks:
    ```
    cd /Users/Code/EchoLearn && grep -cE "boxShadow: '0 4px 12px rgba\\(0,0,0,0\\.1\\)'" app/src/components/ChatInput.tsx
    cd /Users/Code/EchoLearn && grep -cE "width: '34px'" app/src/components/ChatInput.tsx
    cd /Users/Code/EchoLearn && grep -cE "height: '34px'" app/src/components/ChatInput.tsx
    ```
    All three negative greps should return `0`.

    Step 7 — VERIFY total ChatInput height didn't break the bottom-nav clearance. Per CONTEXT.md "Code Context" / D-24 caveat:
    > Container at line 90-100 has `padding: '12px 16px'` and `gap: '10px'`. Bumping mic+globe from 34→44px will increase the total ChatInput height by ~10px.

    Read the parent layout that renders ChatInput (likely AskScreen) to confirm there's enough padding-bottom for the bottom-nav not to overlap. If there's any concern, document it in the SUMMARY for operator dev-mode validation. Do NOT modify the parent layout in this plan — that's out of scope for D-24.

    Step 8 — tsc check:
    ```
    cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo "tsc-exit=$?"
    ```
    Must show `tsc-exit=0`.

    Step 9 — Commit:
    ```
    cd /Users/Code/EchoLearn && git add app/src/components/ChatInput.tsx
    cd /Users/Code/EchoLearn && git commit -m "$(cat <<'EOF'
    style(ChatInput): touch targets + shadow token (D-24, D-25)

    Phase 33 Plan 07 Task 2. Three small consistency fixes per CONTEXT D-24/D-25:

    - Mic button (line ~110-111): 34x34 -> 44x44 for WCAG 2.5.8 compliance.
      Icon size (Mic size=17) preserved.
    - Globe button (line ~138-139): 34x34 -> 44x44 for WCAG 2.5.8 compliance.
      Icon size (Globe size=17) preserved.
    - Container box-shadow (line ~97): literal '0 4px 12px rgba(0,0,0,0.1)'
      -> var(--shadow-2). Pre-edit visual parity check confirmed --shadow-2
      (light mode: 0 3px 6px rgba(0,0,0,0.1), 0 2px 4px rgba(0,0,0,0.06))
      is visually equivalent to the replaced single-layer shadow. Aligns
      with Phase 32.1 Wave 4 D-W4-08 unified shadow tier.

    Per D-26: no Tailwind, no new packages, no translation changes.

    Caveat for operator dev-mode validation: total ChatInput height grows
    by ~10px due to mic+globe bump. Verify bottom-nav clearance on AskScreen
    is unaffected.

    No behavioral change. tsc -b --noEmit exits 0.
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>cd /Users/Code/EchoLearn && grep -cE "boxShadow: '0 4px 12px rgba\\(0,0,0,0\\.1\\)'|width: '34px'|height: '34px'" app/src/components/ChatInput.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boxShadow: '0 4px 12px rgba(0,0,0,0.1)'" app/src/components/ChatInput.tsx` returns `0` (literal shadow gone).
    - `grep -c "boxShadow: 'var(--shadow-2)'" app/src/components/ChatInput.tsx` returns at least `1` (container shadow now uses token).
    - `grep -c "width: '34px'" app/src/components/ChatInput.tsx` returns `0` (both 34px buttons bumped).
    - `grep -c "height: '34px'" app/src/components/ChatInput.tsx` returns `0`.
    - `grep -c "width: '44px'" app/src/components/ChatInput.tsx` returns at least `2` (mic + globe).
    - `grep -c "height: '44px'" app/src/components/ChatInput.tsx` returns at least `2` (mic + globe).
    - `grep -c "size=\\{17\\}" app/src/components/ChatInput.tsx` returns at least `2` (icon sizes preserved at 17px for both Mic and Globe).
    - `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
    - `git log -1 --format='%s'` returns: `style(ChatInput): touch targets + shadow token (D-24, D-25)`.
    - `git diff HEAD~1 --name-only` lists ONLY `app/src/components/ChatInput.tsx`.
    - No new packages added: `git diff HEAD~2 --stat -- app/package.json` returns no output (per D-26).
  </acceptance_criteria>
  <done>D-24 (mic + globe buttons) + D-25 (container shadow token) complete on ChatInput.tsx; one atomic commit landed.</done>
</task>

</tasks>

<verification>
- 2 atomic commits exist on the current branch with subjects matching D-24/D-25:
  ```
  cd /Users/Code/EchoLearn && git log HEAD~2..HEAD --format='%s'
  ```
  Expected output:
  ```
  style(ChatInput): touch targets + shadow token (D-24, D-25)
  style(PlannerScreen): touch targets + spacing tokens (D-24, D-25)
  ```
- tsc clean throughout: `cd /Users/Code/EchoLearn/app && npx tsc -b --noEmit; echo $?` returns `0`.
- D-26 honored: `git diff HEAD~2 -- app/package.json` returns empty (no new dependencies); no Tailwind class additions in the diff (`git diff HEAD~2 -- app/src | grep -E "className=\"[a-z-]+\"" | wc -l` should equal pre-task baseline since no new className additions); no locale bundle changes.
- Operator dev-mode smoke test (`npm run dev`):
  - PlannerScreen: refresh button is touch-friendly (44x44); EmptySectionHint and show/hide buttons render with new padding without visible layout regression
  - AskScreen: ChatInput mic + globe buttons are touch-friendly; container shadow visually consistent with rest of app; bottom-nav clearance is preserved (no overlap)
</verification>

<success_criteria>
- D-24 (touch targets) complete: PlannerScreen refresh button 44x44; ChatInput mic + globe buttons 44x44.
- D-25 (token migration) complete: PlannerScreen EmptySectionHint padding token; show/hide button padding tokens; ChatInput container shadow token.
- D-26 (no Tailwind / no new packages / no translation changes) honored.
- 2 atomic commits, one per file, bisect-friendly.
- tsc clean after each commit.
- Operator-verifiable: no layout regression on PlannerScreen or AskScreen in dev mode.
</success_criteria>

<output>
After completion, create `.planning/phases/33-phase-29-regression-and-phase-31-code-hygiene/33-07-SUMMARY.md` with:
- 2 commit SHAs + their subject lines
- Per-decision verification table (D-24a, D-24b, D-24c, D-25a, D-25b, D-25c) with grep results
- Pre-edit shadow parity check outcome (was --shadow-2 visually equivalent? operator-confirmed?)
- Pre-task vs. post-task tsc exit codes
- Operator dev-mode smoke-test outcome (or note "pending operator validation"); specifically:
  - Bottom-nav clearance on AskScreen after the +10px ChatInput height
  - Visual consistency of new shadow token vs. previous literal
  - No layout regression on PlannerScreen
- Confirmation that D-26 was honored (no new packages, no Tailwind classes, no translation changes)
</output>
