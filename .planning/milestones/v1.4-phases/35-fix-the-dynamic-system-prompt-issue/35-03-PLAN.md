---
phase: 35-fix-the-dynamic-system-prompt-issue
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md contains a new load-bearing section titled 'Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)'"
    - "The section documents the rule, the rationale (KV-cache prefix coverage), and a Don't-bypass list"
    - "The section is positioned adjacent to existing load-bearing sections (e.g., Classification dedup) following the project's established style"
  artifacts:
    - path: CLAUDE.md
      provides: "Phase 35 load-bearing rule documenting byte-stable Ask-chat system prompt"
      contains: "## Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)"
  key_links:
    - from: CLAUDE.md (new Phase 35 section)
      to: app/src/state/useQuestions.ts (the file the rule guards)
      via: explicit code path reference + test file reference
      pattern: "useQuestions\\.ts"
    - from: CLAUDE.md (new Phase 35 section)
      to: app/tests/state/useQuestions-system-prompt-stability.test.mjs
      via: "tests/state/useQuestions-system-prompt-stability.test.mjs enforces..."
      pattern: "useQuestions-system-prompt-stability"
---

<objective>
Add a new load-bearing-rule section to `CLAUDE.md` (project root — the canonical CLAUDE.md; there is no separate `app/CLAUDE.md` despite the CONTEXT.md path reference) titled `## Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)`. The section documents the Phase 35 rule, rationale, and don't-bypass list, sitting adjacent to existing load-bearing sections like "Classification dedup — embedding pre-check (Phase 33 UAT-4 — load-bearing)" and matching their writing style exactly.

Purpose: This is the second of the "documentation in three places" pillars from Phase 32.1 lesson #8. Without this section, future agents looking at `useQuestions.ts` after the Phase 35 refactor will wonder why the candidate context lives in an assistant-tail message instead of the (more obvious) system prompt — and may "helpfully" inline it back. The CLAUDE.md note plus the inline code comment (Plan 01) plus the source-reading test (Plan 02) form a triple guard.

Output: A single Markdown insertion (~40-60 lines) into `CLAUDE.md`, positioned after the "Classification dedup — embedding pre-check" section and before the "Best practices learned in Phase 32.1" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fix-the-dynamic-system-prompt-issue/35-CONTEXT.md
@.planning/phases/35-fix-the-dynamic-system-prompt-issue/35-01-PLAN.md

@CLAUDE.md
@app/src/state/useQuestions.ts

<interfaces>
<!-- Style template: existing load-bearing sections in CLAUDE.md follow this exact shape. -->
<!-- Plan 03 must mirror this shape — heading, opening paragraph, "Why this exists" subhead, "Rules" subhead with numbered list. -->

Pattern from CLAUDE.md "Classification dedup — embedding pre-check" section:

```
## Classification dedup — embedding pre-check (Phase 33 UAT-4 — load-bearing)

The by-layer classification pipeline ... has a structural flaw for cross-cutting concepts: ...

The fix is an O(N_anchors) cosine pre-check BEFORE the tree descent in classifyAndAnchorIncremental:

1. Pre-check (structural): ...
2. Opportunistic backfill: ...
3. Normalize anchor lookup on BOTH sides ...
4. Case-insensitive NEW coercion ...

### Rules

1. Don't remove the pre-check from classifyAndAnchorIncremental. tests/services/classification-dedup.test.mjs enforces it runs BEFORE buildStepPrompt('branch').
2. Don't raise the threshold above 0.95 or drop it below 0.75. Same test enforces the band.
3. ...
```

Pattern from CLAUDE.md "ChatInput flex shrink (Phase 33 UAT-4 — load-bearing)" section:

```
## ChatInput flex shrink (Phase 33 UAT-4 — load-bearing)

`app/src/components/ChatInput.tsx`: the `<input type="text">` MUST keep `minWidth: 0` ...

### Why this exists

Recurring device-only regression. Prior incidents:
- d45c228c switched ChatInput from position: fixed to a flex column but didn't add the guard — bug latent.
- 47d81049 bumped mic/globe buttons 34→44px ...

### Rules

1. Never remove minWidth: 0 ...
2. Don't grow ChatInput buttons past their current 44px ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Phase 35 load-bearing section into CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (project root — the file being edited; entire file. Note: per the planning context the path was `app/CLAUDE.md`, but no such file exists; the canonical CLAUDE.md lives at the project root. Verify with `ls /Users/Code/EchoLearn/CLAUDE.md` before editing)
    - .planning/phases/35-fix-the-dynamic-system-prompt-issue/35-CONTEXT.md (decision D-06 specifies title + adjacency to Classification dedup section + rule/rationale/don't-bypass shape)
    - .planning/phases/35-fix-the-dynamic-system-prompt-issue/35-01-PLAN.md (the exact code shape this section documents; ensures the prose matches what landed in useQuestions.ts)
    - app/src/state/useQuestions.ts (post-Plan-01 source — verify line numbers and inline-comment text match the CLAUDE.md prose)
  </read_first>
  <action>
    Insert a new H2 section into `CLAUDE.md` (project root: `/Users/Code/EchoLearn/CLAUDE.md`).

    **Insertion point:** Use the Read tool to locate the existing section heading `## Classification dedup — embedding pre-check (Phase 33 UAT-4 — load-bearing)` and find the section that immediately follows it. The Phase 35 section goes IMMEDIATELY AFTER that section's last line (which is the bullet item ending with `"...Threshold tuning is empirical, not mathematical."` plus its concluding sentences) and BEFORE the next H2 heading `## Best practices learned in Phase 32.1 (avoid the same mistakes)`.

    **Use the Edit tool** to insert the section. Find the boundary line "5. Threshold tuning is empirical..." and append the new section after the blank line that follows it (preserving the existing `---` horizontal rule if present, or inserting one if the existing pattern requires it — match what surrounding sections use).

    **Insert this exact Markdown content (preserve all leading/trailing whitespace, em-dashes, code blocks):**

    ```markdown
    ---

    ## Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)

    `app/src/state/useQuestions.ts:askStreaming` constructs its system prompt as a **byte-stable** string (identity directive + safety directive + `WEB_SEARCH_TOOL_PROMPT` only — no per-turn dynamic content). The per-turn `formatCandidateContextPack(candidatePack)` output lives in a **tail-position assistant message** placed AFTER `...historyMessages` and BEFORE the new user turn:

    ```typescript
    [
      { role: 'system', content: systemPrompt },                       // byte-stable across turns
      ...historyMessages,                                              // append-only history
      { role: 'assistant', content: assistantContextMessage },         // dynamic per-turn graph context
      { role: 'user', content },                                       // current turn
    ]
    ```

    Pass 2 (web-search) reuses the SAME `assistantContextMessage` closure variable in the same position, so Pass 1's prefix is still warm in the provider cache when Pass 2 fires moments later.

    ### Why this exists

    Provider KV-cache (Anthropic, OpenAI, Gemini all support automatic prefix caching with a 5-minute TTL) is keyed on the byte prefix of the request. The previous shape interpolated `formatCandidateContextPack(candidatePack)` directly into the system prompt — the candidate pack changes every turn (new question → new neighbor set), which moved the cache-break boundary to the FIRST byte after the static `WEB_SEARCH_TOOL_PROMPT`. Every Ask turn paid full re-attention on the entire prior conversation history.

    Moving the dynamic candidate context out of the system role and into a tail-position assistant message keeps the system + history prefix byte-stable across turns. The cache now covers everything up to the per-turn context message, which is exactly what we want.

    Public framing for this fix landed in `LabPresentation/SCRIPTS.md` slide 4.7. Phase 35 was the close-out of that disclosure.

    ### Rules

    1. **Never re-introduce dynamic content into the system prompt of `useQuestions.ts:askStreaming`.** `tests/state/useQuestions-system-prompt-stability.test.mjs` enforces this with a source-reading negative assertion — any PR that puts `formatCandidateContextPack` (or any other per-turn value) inside a string assigned to a `role: 'system'` element fails CI.
    2. **Don't drop the assistant-tail context message either.** The same test asserts `formatCandidateContextPack` IS referenced inside a `role: 'assistant'` content in BOTH chatStream calls (Pass 1 + Pass 2). The graph-grounded context is load-bearing for relevant cross-Q&A continuity — moving it didn't make it dead code.
    3. **Pass 1 and Pass 2 must reuse the SAME `assistantContextMessage` closure variable.** Constructing it twice (or recomputing it for Pass 2) breaks the Pass 1 → Pass 2 prefix-cache continuity. Single declaration, two references.
    4. **Don't bypass `applyLocaleDirective`'s expectations.** `app/src/providers/llm/locale-directive.ts` merges `Respond in {locale}.` into the FIRST `role: 'system'` message. The new static `systemPrompt` must remain that first message — don't swap it for a different element type or invent a pre-system message.
    5. **The 5-minute provider TTL is an inherent limit, not a Phase 35 bug.** Long-idle conversations still pay full re-attention on the next turn. Phase 35 fixes per-turn cache breaks WITHIN a 5-minute window; longer-session caching is not in scope and may need explicit `cache_control` markers (Anthropic-only) in a future phase.
    6. **The other one-shot LLM call sites (`concept-feed`, `planner`, `podcast`, `post-essay`, `post-context-qa`, `flashcard`, `canonical-knowledge` non-descent paths, `AskScreen.tsx:86` session-title) intentionally still interpolate dynamic content into their system prompts.** They have no multi-turn history → no prefix to preserve → no benefit from Phase 35 discipline. See `35-VERIFICATION.md` for the audit. Don't "consistency-fix" them; the cost-benefit doesn't justify the test-and-refactor.
    ```

    Then verify the insertion with:
    ```bash
    grep -n "Ask-chat system prompt — byte-stable across turns" /Users/Code/EchoLearn/CLAUDE.md
    ```

    Should return exactly 1 line, sandwiched between the Classification dedup section and the Best practices learned in Phase 32.1 section.
  </action>
  <verify>
    <automated>grep -c "## Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)" /Users/Code/EchoLearn/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Ask-chat system prompt — byte-stable across turns (Phase 35 — load-bearing)" /Users/Code/EchoLearn/CLAUDE.md` returns exactly `1`.
    - `grep -nE "^## " /Users/Code/EchoLearn/CLAUDE.md` shows the new heading positioned AFTER `## Classification dedup — embedding pre-check (Phase 33 UAT-4 — load-bearing)` and BEFORE `## Best practices learned in Phase 32.1 (avoid the same mistakes)`.
    - The new section contains a `### Why this exists` subheading and a `### Rules` subheading (matches the project's load-bearing-section template — verified by `grep -A 200 "Ask-chat system prompt" CLAUDE.md | grep -c "^### "` returning at least 2).
    - The Rules subsection contains exactly 6 numbered items (verified by `grep -A 80 "### Rules" CLAUDE.md | grep -cE "^[1-9]\." | head -1` returning >= 6 within the new section's window).
    - The section references `useQuestions-system-prompt-stability.test.mjs` by filename (verified by `grep -c "useQuestions-system-prompt-stability" /Users/Code/EchoLearn/CLAUDE.md` returning at least 1).
    - The section references `LabPresentation/SCRIPTS.md` slide 4.7 (verified by `grep -c "SCRIPTS.md.*4.7\|slide 4.7" /Users/Code/EchoLearn/CLAUDE.md` returning at least 1).
    - The section mentions the byte-stable code-shape block as a TypeScript snippet (verified by `grep -A 10 "Ask-chat system prompt" CLAUDE.md | grep -c "role: 'assistant', content: assistantContextMessage"` returning at least 1).
    - The full file remains valid Markdown (no broken code fences) — visual inspection or `node -e "require('fs').readFileSync('/Users/Code/EchoLearn/CLAUDE.md', 'utf8')"` succeeds.
  </acceptance_criteria>
  <done>The Phase 35 load-bearing section is present in `CLAUDE.md` between Classification dedup and Best practices learned in Phase 32.1, with rule/rationale/don't-bypass structure matching adjacent sections.</done>
</task>

</tasks>

<verification>
- `grep -n "Ask-chat system prompt" /Users/Code/EchoLearn/CLAUDE.md` returns exactly one match.
- The section positioning is verifiable via `grep -nE "^## " /Users/Code/EchoLearn/CLAUDE.md` — Phase 35 section sits between Classification dedup and Best practices learned in Phase 32.1.
- The section content references the test file, the LabPresentation script slide, and the exact code shape — three cross-references that downstream agents can follow.
</verification>

<success_criteria>
- A future agent reading `CLAUDE.md` (e.g., during a refactor of `useQuestions.ts` or during `/gsd:research-phase`) immediately sees the Phase 35 rule and avoids the previous interpolation pattern.
- The section style matches existing load-bearing sections — same heading prefix, same Why-this-exists / Rules subsection structure, same level of code-snippet specificity.
- Together with the inline code comment (Plan 01) and the source-reading test (Plan 02), this satisfies Phase 32.1 lesson #8 (documentation in three places).
</success_criteria>

<output>
After completion, create `.planning/phases/35-fix-the-dynamic-system-prompt-issue/35-03-SUMMARY.md` summarizing the section content and confirming positioning between Classification dedup and Best practices learned in Phase 32.1.
</output>
