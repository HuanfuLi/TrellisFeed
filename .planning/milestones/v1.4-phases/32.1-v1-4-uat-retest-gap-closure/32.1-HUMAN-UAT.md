---
status: partial
phase: 32.1-v1-4-uat-retest-gap-closure
source: [32.1-VERIFICATION.md]
started: 2026-04-19
updated: 2026-04-19 (Wave 4 added Tests 10-18)
---

## Current Test

[awaiting next APK deploy — pass/pending status reflects results from previous deploy cycles]

## Tests

### 1. G2 — Video touch overlay reaches YouTube native controls
expected: After fresh APK deploy, tap any inline video in the home feed. Tapping the video while it plays should reach YouTube's native controls (pause/scrub/fullscreen) — NOT restart playback. The close button must still stop playback. Per CONTEXT D-06 + D-07 fallback contingency: if YouTube native controls remain unreachable, escalate plan 32.1-03 to pattern (b) single-fire tap detector.
result: pass
verified_by: HuanfuLi 2026-04-19

### 2. G4 — Starter posts persist after viewing one
expected: After fresh APK deploy + Clear All Data, see 3 starter posts (Welcome / How knowledge grows / Explore feed). Tap any one of them, read, return to /home. All 3 starter posts MUST still be visible (they were persisted to cache + decayed only at 3+ organic posts).
result: pass
verified_by: HuanfuLi 2026-04-19

### 3. G5 — Clear All Data auto-navigates to /home
expected: After fresh APK deploy, Settings → Data → Clear All Data → confirm. Within ~1s of the toast appearing, the app should automatically navigate to /home (no manual nav needed). Wiped state should be immediately visible.
result: pass
verified_by: HuanfuLi 2026-04-19

### 4. Bug 1 — No dead space below text-only / image-fallback posts
fix: 1cc494ff fix(32.1) treat failed-image posts as text-art at render
expected: After fresh APK deploy, scroll the home feed. Any post that previously rendered as a text-only card (image style with failed image generation) should now render as text-art (dotted square + big title text). No huge empty space below any card. Cards size to their natural content.
result: [pending]

### 5. Bug 2 — PostDetailScreen / sub-screen header doesn't flicker on entry
fix: b4965feb fix(32.1) portal Header to document.body for viewport-anchored positioning + a7ce48ec cleanup
expected: After fresh APK deploy, tap any post in the feed → enter PostDetailScreen. Also: navigate to any Settings sub-page (AI Models / Content / Features / Data). Watch the top header bar on entry. Header should render at top edge from frame 1 — no mid-screen-then-snap-up motion.
result: pass
verified_by: HuanfuLi 2026-04-19

### 6. Bug 3 — Concept badges visible at bottom of every AI post + clean anchor names
fix: cd5b6e03 fix(32.1-06) provenance from style assignment + 93162265 fix(32.1-07) anchor-naming prompt + b2061554 fix(32.1) normalizeAnchorName guard
expected: After fresh APK deploy + Clear All Data, classify a NEW question via the Ask flow (e.g. "What is spaced repetition and why does it work?"). Visit /home. Every AI text post should show at least one pill-shaped badge near the bottom containing a clean concept anchor name like "Spaced Repetition" — NOT the full question text. Badge presence verified on b4965feb. Anchor name quality (clean noun, not question paraphrase) pending validation with the new normalizeAnchorName guard.
result: [partial-pending]
notes: Badges visible: confirmed by HuanfuLi 2026-04-19. Anchor name quality: previous deploy showed bad names ("Spaced repetition and why does it work") despite prompt fix. New post-LLM normalizeAnchorName guard at b2061554 should fix this. Pending validation on next deploy with fresh data + new question.

### 7. Bug 4 — Home/Planner empty after first question on fresh-install device
fix: b2061554 fix(32.1) unify CLASSIFICATION_COMPLETED → GRAPH_UPDATED + useQuestions reload from store
expected: After fresh APK deploy + Clear All Data, ask one new question via Ask flow. Wait for classification to complete (mindmap shows the new node). Visit /home → should show new AI-generated posts about the asked concept (not stuck on starter posts). Visit /planner → trellis should show the new anchor as a leaf node, not empty state. Swiping for more posts should produce content, NOT "No more posts to generate right now".
result: [pending]
notes: Previous deploy (before b2061554): operator confirmed home/planner stayed empty after asking new question. Mindmap updated correctly. Root cause was missing event subscription chain — useQuestions never picked up the new anchor. Fixed by unifying CLASSIFICATION_COMPLETED into GRAPH_UPDATED and adding useQuestions reload subscription.

### 8. Bug 5 — Swipe-for-more pops only 1 post; queue not refilling
fix: 98692338 fix(32.1) align concept feed with documented 3-list pipeline (CLAUDE.md)
expected: After fresh APK deploy + ask 1+ questions, scroll to bottom of /home, pull-up to load more. Each swipe should pop ~4 posts (per CLAUDE.md "Concept Feed Generation Pipeline" — design says 4 per swipe). Repeat several swipes — queue should keep refilling until daily generation cap is hit. Should NOT immediately show "No more posts to generate" toast after one swipe.
result: [pending]
notes: Previous deploy: operator confirmed swipe popped 1 post then "No more posts" toast. Two contributing causes: (a) HomeScreen was passing limit=6 (undocumented divergence from designed 4), (b) buildConceptBatch produced 1 entry per concept (1 post per refill cycle for 1 anchor). Fixed by changing limit to 4 and adding weighted multi-entry derived list (4 entries per concept, 8 if important) plus removing pendingIds filter and adding exploredAnchors filter (per design: removed-on-read). Also updated CLAUDE.md with the load-bearing 3-list pipeline architecture so future agents don't drift.

### 9. Bug 6 — News post body shows truncated Tavily snippet, not LLM essay
fix: 3263af4e fix(32.1) news essay regression — concept-feed news path bypassing streaming + db918264 dead news.service cleanup + relocated invariant test
expected: After fresh APK deploy, open any news post (one with NEWS tag in feed). Body should stream in with skeleton bars first, then LLM-generated 150-250 word essay grounded in the actual article snippet. Should NOT end mid-sentence. Source list should show "[1]" prefix (canonical), not "[0]".
result: [pending]
notes: Previous deploy: operator caught body was a short truncated Tavily snippet ("...how apps like Flashrecall time reviews so you" — cut mid-sentence). Root cause: `concept-feed.service.ts:905` stored `result.content` directly into `bodyMarkdown` at creation time, which made PostDetailScreen skip the on-enter streamer. Fixed by setting `bodyMarkdown: ''` and populating `sources[0].snippet` for streamer grounding. Also deleted dead `news.service.ts` (203 LOC orphan) — its existing test was guarding the dead file, which is why this regression slipped through. Replacement test on `tests/services/post-essay.service.test.mjs` now guards the LIVE news branch in concept-feed.service.ts.

### 10. Bug A — Swipe-for-more pops 4 fresh posts (no dedup-induced underserving)
fix: [Wave 4 — pending commit] cycle-stamp news/video/short post IDs in concept-feed.service.ts (lines 833, 868, 901)
expected: After fresh APK deploy + ask 1+ questions, swipe for more several times. Each swipe should consistently produce ~4 NEW posts. Should NOT show "only 1 new post" with the same news/video/short reappearing across cycles. Verify by checking the home feed — repeated topics with identical-looking news/video posts should not appear.
result: [pending]
notes: Root cause: news/video/short post IDs were `post-${date}-{type}-${conceptId}` — deterministic per concept per day. Re-generation across cycles produced identical IDs. Enqueue dedup blocked nothing (post not currently in queue), but seenPostIds in infiniteScrollService filtered them at serve time. Operator's queue snapshot showed two queue slots with literally identical news IDs. Fix appends `-c${cycleNumber}` so each generation is unique.

### 11. Bug B — No more empty cards with massive padding from session posts
fix: [Wave 4 — pending commit] restrict generateSessionPosts style assignment to text-only (concept-feed.service.ts:1376-1385)
expected: After fresh APK deploy + Clear All Data, ask 1+ questions to trigger session-post generation. Scroll the home feed. Should NOT see any empty cards (just card background, no content) with massive 320px padding between visible cards. Every card should render either an image / text-art square / video thumbnail / news layout / suggestion buttons.
result: [pending]
notes: Root cause: `generateSessionPosts` called `assignStyles` which can return ANY of `image | text-art | suggestion | news | video | short`. But the session path only generates LLM TEXT — no YouTube/Tavily fetch. So a `'short'`/`'video'`/`'news'` assignment shipped a post with `presentationStyle: 'short'` but no `videoMeta`. Render layer skipped the visual block; wrapper enforced `minHeight: 320px` from CSS. Fix restricts assignments to `text-art | image | suggestion`.

### 12. Bug C — YouTube returns only video items (no channel/playlist leakage)
fix: [Wave 4 — pending commit] defensive filter in youtube.service.ts:206-222
expected: After fresh APK deploy, every YouTube video/short card should render its iframe + thumbnail + play button correctly. Should NOT see video posts that fail to render (missing thumbnail, blank iframe area). Tap the video card — must navigate / play.
result: [pending]
notes: Defense-in-depth fix. URL already had `&type=video`. But YouTube rarely leaks channel/playlist results with `id.channelId`/`id.playlistId` shape (no `videoId`). Old map produced posts with `videoMeta.videoId: undefined` → render skipped → empty card. Fix filters items where `id.videoId` is non-empty string before mapping.

### 13. Bug D — Render fallback catches all "no visual block" cases (defense net)
fix: [Wave 4 — pending commit] generalize wouldRenderVisual in InfoFlow.tsx:140-167
expected: After fresh APK deploy, scroll the home feed. NEVER see a card that's just a title + paragraph text with no visual block (image, square, video, news header). Every card has SOMETHING visual.
result: [pending]
notes: Replaces the previous `isFailedImage` boolean (only caught `presentationStyle === 'image'`) with `wouldRenderVisual` exhaustive check (video+videoId, short+videoId, news, image, text-art). ANY post failing this check forces text-art render. Dev-mode console.warn fires on each fallback so future regressions surface in logs. This is the LAST line of defense — root-cause fixes are at the generation layer (Bugs A/B/C).

### 14. Repeated YouTube videos no longer reappear after app restart
fix: [Wave 4 — pending commit] backfill seenVideoIds from postHistoryService in concept-feed-dedup.ts
expected: After fresh APK deploy + several swipes that show YouTube video/short cards (note their titles), close the app fully, reopen. Continue swiping for more. The previously-shown YouTube videos should NOT reappear within the same day.
result: [pending]
notes: Old behavior: `_seenVideoIds` was in-memory only — cleared on every page reload (= every Capacitor Android app close). Combined with deterministic top-N YouTube results, same video reappeared on swipe-for-more after reopens. Fix: lazy backfill from `echolearn_post_history` (already persisted) on first dedup access per session. Per-day cutoff preserved (yesterday's IDs don't block today). Reverses original D-02 in-memory choice.

### 15. YouTube produces fresh result sets across cycles for the same anchor
fix: [Wave 4 — pending commit] query rotation in concept-feed.service.ts:680-707
expected: After fresh APK deploy with 1 anchor and many swipes, YouTube videos for that anchor should vary across cycles (different titles, different angles). Should NOT see the same 3 videos cycle endlessly.
result: [pending]
notes: Root cause: identical query → identical top-N results. Fix rotates query modifier per `cycleNumber % modifiers.length`: `["", " explained", " tutorial", " how it works", " deep dive"]` for video; 4 #shorts variants for shorts. Pool size bumped 3→15. Pre-validation and actual fetch use the same query.

### 16. Bug 7 — Vine progress bar fills full card width
fix: [Wave 4 — pending commit] ResizeObserver viewBox + stemEndFull = full availableWidth in VineProgress.tsx
expected: After fresh APK deploy, top of /home shows the vine progress card. The vine (with pot icon on the left and progress dots/flowers along the stem) should extend ALL THE WAY to the right edge of the card, both on the inline card and the compact header banner that slides in on scroll. Should NOT leave the right half empty.
result: [pending]
notes: Two contributing causes both fixed. (a) SVG had fixed `viewBox="0 0 300 ..."` + `preserveAspectRatio="xMidYMid meet"` → on phones wider than 330px, content centered with empty side margins. Fix: `ResizeObserver` measures wrapper clientWidth → viewBox matches actual canvas. (b) Vine geometry terminated at `lastFlowerX + 8`; with 1 concept, flower lands at `availableWidth/2`, so vine ends at half-card. Fix: `stemEndFull = stemStart + availableWidth` always reaches right edge; flowers redistributed evenly across the full vine length.

### 17. Bug 8 — New branches get LLM-generated cluster + anchor names (not "X fundamentals")
fix: [Wave 4 — pending commit] combined cluster+anchor LLM call in canonical-knowledge.service.ts:831-865
expected: After fresh APK deploy + Clear All Data, ask a question in a brand-new branch (e.g., the FIRST question, since all branches are new). Visit /graph. The cluster name above the new anchor should be a recognized 2-4 word sub-domain (e.g., "Learning Theory", "Behavioral Economics") — NOT "${branchName} fundamentals" (e.g., "Psychology fundamentals"). Repeat for several first-questions in different domains.
result: [pending]
notes: Root cause: when step 1 of incremental classifier returned `isNew`, code short-circuited with `clusterName = "${branchName} fundamentals"` — lazy hardcoded placeholder. Fix: when branch is new, fire one combined cluster+anchor LLM call with explicit anti-placeholder constraints. `parseClusterAnchorResponse` rejects any placeholder slip-through (regex `/^(fundamentals|basics|introduction|general|concepts|overview)$/i`) and falls back to legacy `classifyAndAnchor` rather than ship a placeholder. Step 2's `isNew` also no longer short-circuits — anchor name is always LLM-generated via step 3 with empty candidates.

### 18. Bug 9 — "You may also like" cards match regular post styling
fix: [Wave 4 — pending commit] unify shadow + bg + border in SuggestionCard.tsx
expected: After fresh APK deploy, scroll the home feed until you see a "You may also like" suggestion card (the one with topic buttons inside). Its shadow, background gradient, and border should look IDENTICAL to a regular concept post card right next to it. Should NOT have a noticeably weaker shadow or flat background that makes it stand out.
result: [pending]
notes: Old SuggestionCard used `var(--shadow-1)` and flat `var(--card)` background. Regular concept posts in InfoFlow.tsx use `var(--shadow-2)` + gradient + 1.5px colored border. Operator: "different shadows compared to other posts. Unify UI." Fix matches all three styles.

### 19. Bug 10 — All text-art posts show concept badges (no daily-path-vs-session-path divergence)
fix: [Wave 4 — pending commit] mirror daily-path provenance enrichment in generateSessionPosts (concept-feed.service.ts:1407-1426)
expected: After fresh APK deploy + Clear All Data, ask 1+ questions to trigger session-post generation. Scroll the home feed. EVERY text-art post should show at least one pill-shaped badge near the bottom containing a clean concept anchor name. Should NOT see some text-art posts with badges and others without.
result: [pending]
notes: Root cause: daily-path posts (generatePostBatch) explicitly enriched sourceQuestionIds/Titles from the style assignment after parseGeneratedPosts (commit cd5b6e03 in Wave 3). Session posts (generateSessionPosts) skipped this step — they relied entirely on the LLM providing valid sourceQuestionIds, which it frequently omits. Result: daily-path text-art = badges; session-path text-art = no badges. Operator noticed inconsistency. Fix: round-robin attribution to session anchors (from conceptGroups) for any session post with empty sourceQuestionIds after parsing.

## Summary

total: 19
passed: 4
issues: 0
pending: 14
partial: 1
skipped: 0
blocked: 0

## Gaps

None at this time.

### Known carry-overs (NOT bugs, NOT in 32.1 scope — flagged for v1.5 or follow-up)

- Existing wrong-named anchors created BEFORE b2061554 keep their old question-paraphrase names. No migration. Operator can manually rename or Clear-All-Data.
- Existing "${branch} fundamentals" cluster nodes created BEFORE Wave 4 keep their lazy names. No migration. Operator can manually rename via cluster reorganization or Clear-All-Data + re-classify.
- Persistent cycle position across queue refills (current code rebuilds derived list each refill; implicit cycling works via repeated calls + STYLE_WEIGHTS random sampling). See CLAUDE.md "Concept Feed Generation Pipeline" → "Known divergences from design".
- Append-only updates to derived list across refill cycles (vs. rebuild from scratch). Same gap.
- Session posts limited to text-only styles (Wave 4 Bug B fix). Could be extended to fetch YouTube/Tavily for video/news/short post styles. Deferred — operator declined when offered; current path works and adds latency/quota cost.
- Same factory/dead-code-prevention pattern could apply to video/short post construction if they ever grow a parallel path (currently single-path so deferred).
