---
phase: 44-dependency-version-sweep
status: complete
completed: 2026-05-12
requirements_closed: [TECHDEBT-08]
---

# Phase 44 - Dependency Version Sweep Summary

Phase 44 complete: TECHDEBT-08 closed after dependency metadata, automated verification, native sync, and manual smoke evidence were recorded.

## Package Targets

Plan 44-01 updated these package ranges and regenerated `app/package-lock.json` with npm:

| Package | Phase 44 target |
|---------|-----------------|
| `@capacitor/core` | `^8.3.3` |
| `@capacitor/cli` | `^8.3.3` |
| `@capacitor/android` | `^8.3.3` |
| `i18next` | `^26.1.0` |
| `react-i18next` | `^17.0.7` |
| `react-router-dom` | `^7.15.0` |
| `react` | `^19.2.6` |
| `react-dom` | `^19.2.6` |
| `@tailwindcss/vite` | `^4.3.0` |
| `tailwindcss` | `^4.3.0` |
| `@types/react` | `^19.2.14` |
| `typescript-eslint` | `^8.59.3` |
| `eslint-plugin-react-hooks` | `^7.1.1` |
| `eslint-plugin-react-refresh` | `^0.5.2` |

Plan 44-02 added `@capacitor/ios@^8.3.3` as a justified dependency-sweep deviation because the repo already had `app/ios/` and the exact required `npx cap sync` command could not succeed without the matching iOS platform package.

## Held-Back Majors

- Vite 8: held for a dedicated major-upgrade phase; `vite` remains `^7.3.1`.
- TypeScript 6.0: held because `typescript-eslint@8.59.3` peers TypeScript `<6.0.0`; `typescript` remains `~5.9.3`.
- ESLint 10: held because Phase 44 stayed on ESLint 9.x; `eslint` remains `^9.39.1`.
- lucide-react 1.x: held per roadmap scope; `lucide-react` remains `^0.575.0`.
- framer-motion to motion: held because the package rename is broad and mechanical; `framer-motion` remains `^12.38.0`.

## Verification Evidence

- `npm test`: exits 0 because the script chains `test:main; test:actions`. `npm run test:main` retains the known post-Phase-43 five-failure baseline only; `npm run test:actions` exits 0 with 16/16 passing.
- `npm run lint`: exits 0 after preserving the pre-sweep lint gate by disabling only the new React Hooks 7 compiler/refresh rules that fired against existing code.
- `npm run build`: exits 0; `tsc -b` and the Vite 7.3.1 production build both complete successfully.
- `npm audit --audit-level=high`: exits 1 with the unchanged pre-existing baseline of 10 vulnerabilities, 5 high, 0 critical, and new high/critical vulnerabilities: 0.
- `npx cap sync`: exits 0. Android produced no `app/android` diff; iOS generated `app/ios/App/Podfile` and `app/ios/App/Podfile.lock` updates after adding `@capacitor/ios@^8.3.3`.

## Manual Smoke

All five `44-UAT.md` rows are marked `pass`:

| id | Result | Evidence |
|----|--------|----------|
| `locale-switch` | pass | EN -> ZH -> ES -> JA visible UI strings changed without runtime error. |
| `ask-streaming` | pass | Ask streaming displayed text and completed or aborted without a stuck loading state. |
| `queue-refill` | pass | Feed refill produced content with no duplicate React key warning reported. |
| `saved-route-navigation` | pass | `/saved` opened, Saved/Liked tabs switched, and navigation returned to `/home`. |
| `android-sync-sanity` | pass | Plan 44-02 sync evidence accepted; `npx cap sync` exited 0 and Android had no diff. |

## Files Changed

- `app/package.json` and `app/package-lock.json`: approved dependency target ranges, lockfile regeneration, and `@capacitor/ios@^8.3.3` addition.
- `app/eslint.config.js`: Phase 44 lint-gate preservation for React Hooks 7 compiler/refresh rules.
- `app/ios/App/Podfile` and `app/ios/App/Podfile.lock`: generated by the successful `npx cap sync` run.
- `.planning/phases/44-dependency-version-sweep/44-DEPENDENCY-SWEEP.md`: package targets, audit baseline, and held-back majors.
- `.planning/phases/44-dependency-version-sweep/44-VERIFY.md`: automated test, lint, build, audit, and native-sync evidence.
- `.planning/phases/44-dependency-version-sweep/44-UAT.md`: five-pass manual smoke evidence.
- `.planning/phases/44-dependency-version-sweep/44-VALIDATION.md`: final Nyquist sign-off with all rows green and `nyquist_compliant: true`.
- `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md`: TECHDEBT-08 and Phase 44 close-out state.
